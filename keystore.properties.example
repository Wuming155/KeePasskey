# 发布签名密钥库配置模板（请勿将真实私钥与配置提交到公开仓库）
#
# 使用方法：复制此文件为 keystore.properties，并把下列占位符替换为**你自己的**高熵口令。
#
# 硬性要求（ISSUE-P2-55 / 审计 F-06，由 app/build.gradle.kts 构建期断言强制）：
#   1. 口令长度 ≥ 16 字符，且不得使用本文件中的任何占位符文本；
#   2. 绝不可复用历史上公开过的示例口令（例如 "keepasskey123"）——该值已随公开仓库泄露，
#      用它签名等于把发布密钥交给任何人；
#   3. 口令只写入本地 keystore.properties（已被 .gitignore 忽略）或经 CI Secret 注入
#      KEYSTORE_PASSWORD / KEY_PASSWORD 环境变量，任何情况下不得提交。
#
# 若密钥库已存在且使用了弱口令：**只 re-key、不换密钥**
# （换密钥会使已安装用户无法覆盖升级——Android 会在签名不符时拒绝升级安装）：
#   keytool -importkeystore -srckeystore release.jks -srcstorepass <旧口令> \
#     -srcalias keepasskey -srckeypass <旧口令> \
#     -destkeystore release_rekeyed.jks -deststorepass '<新高熵口令>' \
#     -destalias keepasskey -destkeytype PKCS12 -noprompt
#   # 校验证书指纹与旧库逐字一致后，用新库替换旧库，并更新 keystore.properties
#
# storeFile 支持绝对路径或相对于项目根目录的相对路径
storeFile=release.jks
storePassword=__REPLACE_WITH_HIGH_ENTROPY_PASSWORD__
keyAlias=keepasskey
keyPassword=__REPLACE_WITH_HIGH_ENTROPY_PASSWORD__
