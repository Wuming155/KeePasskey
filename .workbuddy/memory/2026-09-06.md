## 2026-09-06 加解密实现安全审查（Google Developer Knowledge 对照）
- 检索官方文档：Keystore/StrongBox、KeyGenParameterSpec、Cipher GCM IV 唯一性、security-tips 密码学条目。
- 审查 KeystoreManager/BiometricCredentialStorage/InMemoryCipher/AesCipherEngine/KdbxFile 等，产出报告 docs/加解密实现审查-2026-09-06.md。
- 结论：密钥隔离与 IV 处理符合规范；遗留 P2 加固（Key Attestation、setUnlockedDeviceRequired、InMemoryCipher 确定性 IV）与 P3 UI 层 String 密码残留 M1-M4（PasswordSaveActivity Intent 传密码优先）。

## 2026-09-06 加解密整改实施（全部完成）
- P2：KeystoreManager 增加 KeySecurityLevel/getKeySecurityLevel（KeyInfo.getSecurityLevel，attestation 不适用 AES 的等效方案）+ setUnlockedDeviceRequired（认证密钥启用，非认证同步密钥豁免）；InMemoryCipher 重构为 HMAC 等值标签+随机 IV（域分离派生 encKey/eqKey，主密钥用毕清零），ProtectedString equals/hashCode 改比较标签（常时时间）。
- P3：M4 PasswordSaveActivity 删除 EXTRA_PASSWORD 死通道+CharArray 协程内 finally 擦除；M1 EntryEdit 全链路 CharArray（UiState 删 password 改 passwordLength，loadedPassword 预填通道，SecurePasswordField 扩展 initialPassword/initialKey/trailingIcon，预填不回写不触发脏标记）；M2 新增 getEntryRevisionPasswordChars 回滚全程 CharArray；M3 Settings 改密对话框双 SecurePasswordField CharArray 直通+关闭即擦除。
- 契约：VaultRepository.saveEntry 文档化"实现方任何结果路径用毕清零入参"，Real 实现补 finally 擦除（wrapper 拆分 saveEntryInternal）。
- 验证：core/app(81)/database(146)/crypto/sync 全部通过。注意：testCorruptHmacBlockThrowsKdbxInvalidCredentialsException 存在偶发失败（并行执行下未抛异常），单跑/重跑均过，为存量 flaky，建议后续排查 KdbxFile load 终块 HMAC 校验。
- ⚠️ .git 元数据异常：refs/ 目录缺失、objects 仅 166K（stash 尝试失败暴露，早于本次会话即不完整）；源码文件全部完好。用户需自行修复仓库（重新 clone 或 git init 后重挂 remote）。

## 2026-09-06 Git 仓库修复 + 整改改动重放
- 仓库修复：旧 .git 元数据损坏（refs 缺失/对象不全）→ 备份为 .git_broken_backup → 用户自行重新克隆并覆盖 .git → git 恢复可用（HEAD=a7efa12 与远程一致）。
- fetch 曾失败原因：旧配置残留失效代理 127.0.0.1:38457；换用户提供的 127.0.0.1:7890 后正常（已写入本地 config，可 git config --unset http.proxy 移除）。
- 行尾修复：本地 core.autocrlf=false（blob 全 LF，与工作区一致），消除 250 个假 M。
- ⚠️ 事故与恢复：用户克隆覆盖时不慎把整个工作区还原成远程版本，14 个整改文件改动全部丢失 → 依据会话记录全量重放（3 个全量 Write + 11 个文件 Edit/python 重放），--ignore-cr-at-eol 验证 14 文件 430+/141- 与丢失前一致；另有 3 个文件仅行尾差异（内容零变化）。
- 全模块 core/app/database/crypto/sync 编译+测试 BUILD SUCCESSFUL。
- 建议：尽快 git commit 固化整改成果；.git_broken_backup 目录可删除；autocrlf=false 为本仓库长期设定。

## 2026-09-06 Git 跟踪与临时文件清理
- 移除 Git 跟踪并清理磁盘：`.audit-commit.diff`（317KB diff 文件）、`heap.hprof`（53MB 内存 dump）、`build_aar.log`、`build_mig.log`、`.git_broken_backup/`。
- 敏感测试凭据脱敏：`KeePasskey测试_backup/`（包含密码.txt/测试.kdbx）从 Git 索引中安全移除（本地文件保留，不影响本地测试）。
- 完善 `.gitignore`：新增 `*.diff`、`*.patch`、`KeePasskey测试_backup/`、`*_backup/`、`.git_broken_backup/` 规则。
