<a id="s302"></a>

# §302 安全与外观假开关移除批次

> `ISSUE-P3-274` **整条闭环**（P3 11 → **10 项**）。
> 覆盖面：`ISSUE-P3-274`（本体，`rememberRecentFiles` / `iconSet` 两项「偏好只落盘不消费」）。
> 生产改动＝两项**整体移除**（AC① 的「不接线则移除」分支）+ 裁决登记 **`PD-40`**；
> 新增接线守卫 **5 例**；中英各删 9 个字符串键（两侧仍 **1210** 键完全成对）。

---

## 1. 条目正文（原样收录）

### `ISSUE-P3-274`：安全与外观两项假开关（记住最近打开的数据库 / 图标集风格）

- **核实时间点**：2026-09-23 经全仓定向 grep 核实。
- **核实方式**：① **记住最近打开的数据库** `rememberRecentFiles` 有持久化键（`ExtendedSettingsStore.kt:91/178/332`，缺省 `true`），全仓命中仅 setter（`SettingsExtendedPreferencesController.kt:66`）/ 投影（`SettingsUiStateProjection.kt:172`）/ 设置页开关（`SecuritySettingsScreen.kt:375-378`）——**零消费方**，全仓无「最近数据库」列表或记录存取实现；**对照**同卡相邻的 `rememberKeyFileLocation` 真实接线于解锁侧（`SafKeyFileAccess.kt:43` 实际读取该偏好），故本项不能以「同类均未接线」为由豁免。② **图标集风格** `iconSet` 有持久化键（`ExtendedSettingsStore.kt:139/199`），可三选一（Material / KeePass 经典 / 极简单色，`ThemeSettingsListSections.kt:241-245`），但全仓 `IconSetOption` 命中仅 settings 域（`ExtendedSettings.kt` / `SettingsExtendedPreferencesController.kt` / `SettingsUiState.kt` / `SettingsViewModel.kt` / `ThemeSettingsScreen.kt` / `ThemeSettingsListSections.kt`）+ store——**渲染侧无任何消费**。
- **背景与根因**：均为「偏好只落盘不消费」。`rememberRecentFiles` 的界面语义是**安全项**（是否保留打开历史），实际不记录任何东西，用户以为「关闭即不留痕」——属安全语义误导；`iconSet` 属展示类，改选后全站图标无变化。
- **涉及文件**：`app/src/main/java/com/keepasskey/app/ui/screens/settings/SettingsExtendedPreferencesController.kt`、`app/src/main/java/com/keepasskey/app/ui/screens/settings/SettingsUiStateProjection.kt`、`app/src/main/java/com/keepasskey/app/ui/screens/settings/subscreens/SecuritySettingsScreen.kt`、`app/src/main/java/com/keepasskey/app/ui/screens/settings/subscreens/ThemeSettingsListSections.kt`、`app/src/main/java/com/keepasskey/app/data/repository/ExtendedSettingsStore.kt`（消费方落点待定：最近库记录源 / 图标渲染层）。
- **验收标准**：AC① 两项逐一给出「真实接线」或「如实移除 / 禁用并标注」结论；AC② `rememberRecentFiles`：若接线，须有真实的「最近打开库」记录源与清理逻辑，**关闭即不写入且清理既有记录**（对齐 `createBackupBeforeSave` 关闭时顺带清理历史 `.bak` 的既有口径），并说明记录存储位置与清理时机；若不接线则**移除**该项（安全项不得停留在「看起来能控制痕迹」的假状态）；AC③ `iconSet`：若接线，须把选择真实接入图标渲染层并覆盖三种风格；若不接线则如实禁用并标注（避免「可三选一但全站无变化」）；AC④ 修订中英文案，使其**仅**描述真实行为；AC⑤ 新增接线守卫用例，禁「UI 有开关、生产无消费方」复现。

**开工前前提复核（2026-09-24）**：成立，两项均**零消费方**。① `rememberRecentFiles` 全仓命中仅 store 读写 + 设置域（字段 / setter / 投影 / UI 开关），且**无任何「最近打开库」记录源或列表实现**（定向检索 `recent` 类符号只命中同步事件流与生成器历史，与本项无关）；其 UI 文案「清除最近文件记录 / 移除密码库的历史打开记录」是**命令式**措辞而非偏好语义，进一步印证该开关从未接线。② `iconSet` 全仓命中仅设置域 + store，渲染侧（图标投影 / 装饰装配 / 主题）无任何读取。**反例对照**：同卡 `rememberKeyFileLocation` 在 `SafKeyFileAccess.kt:43` 被真实读取，故「零消费方」是按项取证的结果，不是整片推论。

---

## 2. 整改

### 2.1 两项逐项结论（AC①）

| 项 | 结论 | 处理 |
|---|---|---|
| `rememberRecentFiles` | **如实移除**（AC② 的「不接线则移除」分支） | UI 开关 / 卡片形参 / 导航图实参 / VM 委托 / 控制器 setter / `SettingsUiState` 字段 / 投影 / `ExtendedSettings` 字段 / store 键（读写两侧）/ 中英字符串键（2 × 2）全链删除 |
| `iconSet` | **如实移除**（AC③ 的「不接线」分支，选移除而非「禁用并标注」） | 同上，另删 `IconSetOption` 枚举与整节 `themeIconSetSection`，中英字符串键 7 × 2 全删 |

> AC② 的「真实接线」分支未触发（无记录源可接）；AC③ 的「禁用并标注」分支亦未取——保留一个「可选但全站无变化」的单选组仍会损害设置页可信度，且实现三套图标族属独立视觉特性（见 `PD-40` 边界 ③）。

### 2.2 文案处置（AC④）

两项的**用户可见文案随控件一并删除**（无控件即无承诺）：`sec_recent_files_title` / `sec_recent_files_sub`（中文「清除最近文件记录 / 移除密码库的历史打开记录」）、`theme_section_iconset` 与 6 条 `theme_iconset_*`（三档图标集的名称与描述）。两侧各删 9 键，合并 `values/` 三份文件后中英仍**各 1210 键、双向差集皆空**。

### 2.3 保留面（防连坐）

- `rememberKeyFileLocation`（同卡相邻、**真实接线**于解锁侧）原样保留，其开关行仍在；
- 主题页其余分区（外观模式 / 调色盘 / 隐私预览 / 列表显示 / 语言）原样保留；
- 安全页其余四张卡与全部开关原样保留。

---

## 3. 验证

### 3.1 新增守卫用例（AC⑤）：`app/src/test/java/com/keepasskey/app/ui/screens/settings/SecurityAndThemeSwitchWiringTest.kt`（5 例）

1. `两项已移除假开关在 app 主源码与文案中零残留`——递归扫 `app/src/main` 全部 `.kt` / `.xml`（`.kt` 先剔注释），15 个可辨识符号任一命中即红；
2. `已移除项的偏好键与字符串键均已删除`——4 个偏好键 + 9 个字符串键 × 中英；
3. `相邻真接线项未被连坐误删`——`SafKeyFileAccess` 仍读 `rememberKeyFileLocation`；主题页 `themeListSection` / `themeLanguageSection` 仍在；
4. `两处 UI 不再引用已移除项且入口参数已摘除`——安全页无 `sec_recent_files` / `onRememberRecentFilesToggle` 但保留 `onRememberKeyFileLocationToggle`；主题页无 `themeIconSetSection` / `onIconSetSelected` 但保留 `themeLanguageSection(`；
5. `偏好数据类与设置域不再保留两项字段`——5 份设置域源码逐项反查。

**反空转设计**：第 1 / 2 例均带**正控制**（同一次扫描必须命中 `rememberKeyFileLocation` / `sec_keyfile_title` / `theme_section_list` 等仍存在的符号），否则判定为扫描器失灵而非「真的干净」。

> **踩坑留痕**：首版第 3 / 2 例的正控制写成了 `theme_section_density` 与 `themeListDensitySection` —— 二者**在全仓根本不存在**（密度在 `themeListSection` 内，键名是 `theme_density_*`）。正控制本身写错会退化为「恒红」或掩盖扫描失效，已在提交前改为真实存在的 `theme_section_list` / `themeListSection` / `themeLanguageSection`。

### 3.2 全量回归

- `.\gradlew.bat test --max-workers=1` → **BUILD SUCCESSFUL**；
- 聚合计数（`python tools/doc/count_test_results.py`）：**`xml=395 tests=2684 failures=0 errors=0 skipped=13`**（+5，即本批新增守卫；`skipped=13` 与基线持平）。

### 3.3 机检

| 机检 | 结果 |
|---|---|
| `check_md_links.py` / `check_resolved_index_sync.py` / `check_bounded_type_names.py` | EXIT 0 |
| `check_tautological_assertions.py` / `check_recheck_consistency.py` | EXIT 0 |
| `count_line_tiers.py` / `long_functions.py` | 仍 EXIT 1（**既存**，见 `ISSUE-P3-305`） |

---

## 4. 如实声明

1. **`iconSet` 的处置强于 AC 的最低要求**：AC③ 允许「如实禁用并标注」，本批直接移除。理由与重开条件见 `PD-40`；若判定此项应保留为「禁用态展示」，须回退本批并另立裁决。
2. **既有安装的残留偏好键未主动清理**（`remember_recent_files` / `icon_set`）——`save()` 为整体写而非清空重写，升级用户会遗留两个**惰性死键**（读写两侧均已无引用，无功能影响）。处置口径与 `§300` 一致。
3. **`hygiene-gate` 两条仍为红**（`tier1=4` / `functions_ge_100=2`），属 `ISSUE-P3-305` 范围，本批未触碰；不以「test 全绿」冒充「门禁全绿」。
4. **无设备侧必跑项**：未触 `*/src/androidTest/**`、未改 KDBX 序列化 / 合并 / 附件 / 原生内核。
5. **未跑**：`lint` / `assembleRelease` / 截图包装编译门禁（未改 `@Preview` 与预览生成器）。
