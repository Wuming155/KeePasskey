<a id="s69"></a>
## §69 文档索引悬空引用收口批次（2026-09-15）：ISSUE-P3-129 / ISSUE-P3-127

> **本批次缘起**：三处「指向不存在文件的引用」同时存在于 `AGENTS.md`、工作流注释与构建配置中——
> 归档缺 §68 批次正文、第二轮独立复核报告被整份删除且退役未登记、`docs/.handoff/ISSUE-P3-09.md`
> 从未入库。三者同属 `AGENTS.md` §4「索引纪律 / 退役纪律」针对的**同一失效模式：结论脱离索引与工作流入口**。
>
> **重合声明**：`ISSUE-P3-127` 与 `ISSUE-P3-129` ② 为**同根因重复登记**（均指复核报告退役零留痕），
> 本批**同批收口**，两条一并移出待办表（过程缺陷见 69.5）。

### 69.1 交付清单

| # | 悬空引用 | 处置 | 依据 |
|:--:|---|---|---|
| ① | **归档缺 §68 批次正文**：`AGENTS.md` §1 / §6 与 `ACTIVE_ISSUES.md`「续 15」三处引用 §68，而归档库仅 §1 ~ §67 | **补录** §68 正文 + 总索引加行 | `ISSUE-P3-129` AC① 首选分支；证据取自整改提交 `7a6789a` 与 2026-09-15 复跑实测（正文见 [`68-退出前清理易失缓存批次.md`](68-退出前清理易失缓存批次.md)） |
| ② | **复核报告退役未登记**：`docs/SECURITY_RECHECK_2026-09.md` 于提交 `523d0fd` 被整份删除（提交信息为 "Refactor code structure…"，与实际动作语义不符），`AGENTS.md` §4 仍列为必读、§5 闸门 `check_recheck_consistency.sh` 以它为默认报告路径 ⇒ 闸门恒 `exit 2` 停摆 | **恢复入库**（裁决见 69.2）并落位 `docs/security/`；脚本默认路径与 `AGENTS.md` §4 / §5 同步更正 | `git cat-file -e 523d0fd^:docs/SECURITY_RECHECK_2026-09.md` = 0（可按 git 对象完整取回，**区别于** §41 的「未跟踪文档不可取回」情形） |
| ③ | **`docs/.handoff/ISSUE-P3-09.md` 全仓不存在**：4 处「依据 / 判据见该文件」式注释（2 个 workflow、`app/proguard-rules.pro`、`gradle/libs.versions.toml`） | **承接**：新增 [`退役依据承接-ISSUE-P3-09.md`](../records/退役依据承接-ISSUE-P3-09.md)，4 处注释改为指向它 | `git log --all -- docs/.handoff/` **无任何输出** ⇒ 该目录**从未入库**，无可 `git show` 取回的对象 ⇒ AC③ 的「补录」分支**不可行**，取「改指现存依据」分支 |

### 69.2 ② 的裁决：为何恢复而不是退役

`ISSUE-P3-127` AC① 给出二选一（恢复入库 / 确认退役并三处同步）。本批取**恢复**，理由：

1. **可完整取回**：该报告曾入库，`git show 523d0fd^:docs/SECURITY_RECHECK_2026-09.md` 即得全文，
   恢复成本为一条命令，**无内容损失**；
2. **退役代价与收益不成比例**：该报告是 `ISSUE-P2-49` / `P3-122` / `P3-123` / `P3-124` 等条目的
   **severity 定级与 AC 修正的唯一权威依据**（各条「第四轮批注」），退役即须逐条分流 89 项结论与
   24 项反证结论并重评上述定级——**风险高、收益为零**；
3. **恢复即刻修复闸门**：`check_recheck_consistency.sh` 恢复有效（复跑 `PASS`，见 69.3），
   `AGENTS.md` §5「修改任何审计 / 复核报告后必须跑」的纪律**重新可执行**。

**同步落位**：按 `docs/` 分区纪律（不得平铺在 `docs/` 根），报告落入 `docs/security/`，
引用方一并更正——`AGENTS.md` §4 索引行、§5 闸门命令、脚本默认路径。
**如实声明**：本次恢复的版本为 `523d0fd^` 的对象（1272 行）；其**报告内声明的基线**
仍为当时的 `a669a48`，本批**不**改写报告正文，也不把其结论当作对当前 HEAD 的新复核。

### 69.3 验证证据（2026-09-15 复跑）

```
$ bash tools/audit/check_recheck_consistency.sh
PASS: 无残留禁用短语（已扫描 1272 行，11 条禁用短语）
exit=0
```

- 闸门由「恒 `exit 2` 报告不存在」恢复为**可执行且 PASS**；
- `grep -rn "\.handoff"` 在**工作流 / 构建配置**中**零命中**；`app/proguard-rules.pro` 与文档内仅余
  **说明性括注与历史登记**（「原引用的 …从未入库」），**不再有**「依据见 `docs/.handoff/…`」式**活引用**；
- 单测全量复跑 `BUILD SUCCESSFUL`，**1770 例 / 0 失败 / 0 错误 / 13 跳过**（本批为文档与注释改动，
  不涉行为，结果与 §68 记录一致）。

### 69.4 计数

`ACTIVE_ISSUES.md` 的 **P3 由 18 → 16**（`ISSUE-P3-129` 与 `ISSUE-P3-127` 同批移出）；
P2 计数不变（9），P1（0）不变。

### 69.5 过程缺陷（如实留痕）

1. **同根因重复登记**：`ISSUE-P3-127`（2026-09-15 由整改 `ISSUE-P2-69` 时的跨文件检索附带发现）与
   `ISSUE-P3-129`（同一日由文档归档重构时的全仓链接扫描发现）实为**同一份报告退役未登记**，
   两者在待办表内**并存**。⇒ 纪律：登记前应先按**关键词**检索既有条目，
   避免同一对象占两条编号并各自演算出不同 AC。
2. **`ACTIVE_ISSUES.md` 自身的索引注记一度与正文打架**：§4 索引表下方的「悬空索引」注记在
   本批前同时描述上述三处（其中两处已由本批收口），本批一并移除该注记，避免「注记说悬空、
   正文说已闭环」的新漂移。
3. **提交信息纪律（`ISSUE-P3-127` AC③）**：整份审计报告的删除**不得**以
   「重构 / 可读性 / 可维护性」类信息承载——本次误删（`523d0fd`）未被任何流程拦截，
   正因提交信息未提示语义变更。本纪律随本批次写入 `AGENTS.md` §4「退役纪律」段。
