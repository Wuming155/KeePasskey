# 334. 本地纵深防御四层移除批次（ISSUE-P1-277 + ISSUE-P3-326 ~ 328 闭环）

> **闭环条目**：`ISSUE-P1-277`（P1 解锁节流完整性层 fail-closed 缺陷 + 冗余性裁决）、
> `ISSUE-P3-326`（防回滚状态 MAC）、`ISSUE-P3-327`（快速解锁断言层）、`ISSUE-P3-328`（字段签名 HMAC）。
> **裁决**：2026-09-25 用户明示**四条全部移除**（原话与删除边界见
> [`产品裁决登记.md`](../../architecture/产品裁决登记.md) `PD-46`）；取证依据为
> [`本地文件级防护层的参考项目对照.md`](../../references/本地文件级防护层的参考项目对照.md)（五项目无一实现，净增量趋近 0）。
> **开工前前提复核（规则 6.1②）**：P1-277 三缺陷（gate 分支无门控 / 空串 MAC 循环 / 无自愈入口）、
> 出厂默认 `unlockThrottleEnabled=false`、消费链 `MasterPasswordUnlockSession` / `ChildDatabaseSessionManager`
> 均经逐行直读确认成立；四条「处置口径」所列行号以真实内容为准无漂移。

---

## 1. 整改内容

### 1.1 ISSUE-P1-277：解锁节流完整性层整体移除（基础节流保留）

- **整文件删除**：`app/src/main/java/com/keepasskey/app/security/UnlockThrottleIntegrity.kt`
  （含 `UnlockThrottleIntegrity` / `AndroidKeystoreUnlockThrottleIntegrity` / `UnlockThrottleMacPayload` /
  `UnlockThrottleExistenceMarkerAlias`）。
- **部分摘除**：`UnlockThrottle.kt` —— `UnlockThrottleRecord.integrityIntact` 字段、store 的 `integrity`
  构造参数与全部 MAC / 存在性标记读写、`gate` 的 `!integrityIntact` fail-closed 分支（对
  `MAX_BACKOFF_MS` 的硬编码一并消失）；`SecurityModule.kt` 的 `bindUnlockThrottleIntegrity` 绑定。
- **`reset` 回退为删键**：`ISSUE-P2-45` 的「写零值记录」动因随存在性标记消失，回退为删除计数 /
  锁定两键（`read` 对缺键回落零值记录，行为等价）。
- **保留**：`UnlockThrottlePolicy` / `ThrottleConfig` / `ThrottleConfigSource` / `UnlockThrottleConfigProvider` /
  `SharedPrefsUnlockThrottleStore` 计数主干 / `UnlockThrottleManager` 的 `gate`（`config.enabled` 门控 +
  计数退避判定）/ `registerFailure` / `registerSuccess`，及 `UnlockViewModel`、`ChildDatabaseSessionManager`
  两个消费点——基础节流对「UI 驱动 / 物理接触、无文件写权限」对手的防护**语义不变**。
- **`KeystoreSyncIntegrityMac.kt` 的 ISSUE-P0-10 纪律注释**：原引用 `UnlockThrottleIntegrity` 作为
  纪律记载点，改为自足表述（Redmi 4X / LineageOS / API 37 实测口径不变；该文件本批后段随 P3-326 删除，
  纪律以批次历史承载）。

### 1.2 ISSUE-P3-326：防回滚状态 MAC 移除（「已见内容摘要链」引擎保留）

- **整文件删除**：`app/src/main/java/com/keepasskey/app/sync/KeystoreSyncIntegrityMac.kt`、
  `app/src/main/java/com/keepasskey/app/sync/SyncIntegrityModule.kt`（Hilt `@Binds`）、
  `sync/src/main/java/com/keepasskey/sync/engine/SyncIntegrityMac.kt`（含 `NoopSyncIntegrityMac`）。
- **部分摘除**：`SyncRollbackGuard.kt` —— `integrityMac` 构造参数、`load` 的 MAC 分离 / 校验段、
  `persist` 的 MAC 计算与附加段（含 `java.util.Base64` import）；`SyncCycleSetup.kt` / `SyncCycleRunner.kt`
  的注入摘除（`SyncIntegrityMac` / `NoopSyncIntegrityMac` import 同删）。`SyncEngine.kt` 不依赖 MAC，未改动。
- **兼容处理（保留一行）**：`PREFIX_MAC` 常量与 `load` 中对 `mac=` 行的**过滤**保留，仅用于兼容升级前
  遗留状态文件（其 Base64 `=` 填充不得影响 key/value 切分）；常量注释标「遗留兼容」。
- **状态字段零失效**：`sequence` / `current` / `recent` 全部保留并继续参与裁决（`inspect` / `recordAccepted` /
  `RollbackVerdict` / `ReplayDetected` 语义逐条不变）；状态文件退化为**明文摘要文件**（不含明文口令 /
  凭据）。原 MAC 失效本就是 fail-open，移除后**行为不回退**。

### 1.3 ISSUE-P3-327：快速解锁断言层整层移除

- **整文件删除**：`UnlockPasskeyManager.kt`（含 `UnlockPasskeyAssertion` / `UnlockPasskeyGate`）、
  `UnlockPasskeyKeyPolicy.kt`。
- **部分摘除**：
  - `BiometricCredentialStorage.kt` —— `UnlockPasskeyRecord` / `UnlockPasskeyStore` 接口及
    `saveUnlockPasskey` / `getUnlockPasskey` / `commitSignCount` / `clearUnlockPasskey` / `integrityMac`、
    `DB_KEY_SUFFIXES` 的 4 个 `_passkey_*` 项；**封印凭据主体全部保留**（`saveEncryptedCredential` /
    `getEncryptedCredential` / `hasEncryptedCredential` / `clearCredential` / `clearAll` /
    `revokeAllBiometricData`）；
  - `KeystoreManager.kt` —— `getOrCreateUnlockPasskeyPair` / `getOrCreateUnlockPasskeyIntegrityMac` /
    `UNLOCK_PASSKEY_INTEGRITY_KEY_ALIAS` / `unlockPasskeyAliasFor`；
  - `KeystoreKeyMaterial.kt` —— `getOrCreateUnlockPasskeyPair` / `generateUnlockPasskeyPair` /
    `buildUnlockPasskeySpec` / `getOrCreateUnlockPasskeyIntegrityMac` 及随之无用的 import；
  - `SecurityModule.kt` —— `bindUnlockPasskeyStore`；`UnlockViewModel.kt` —— `unlockPasskeyManager`
    参数与两处协调器接线；`BiometricUnlockCoordinator.kt` —— `verifyUnlockPasskey` 整函数、两处
    `unlockPasskeyManager?.clear`、断言调用点改直通 `completeBiometricUnlock`；`BiometricEnrollmentCoordinator.kt`
    —— `enroll` 接线（封印成功落库后不再登记断言密钥）。
- **退役残留清理（清理钩子，AC②）**：`BiometricCredentialStorage.cleanupLegacyUnlockPasskeyData()`
  （init 期一次性，best-effort）——按遗留 `_passkey_*` prefs 后缀枚举库 ID，逐库删除
  `KeystoreManager.legacyUnlockPasskeyAliasFor(dbId)` 并删除
  `LEGACY_UNLOCK_PASSKEY_INTEGRITY_KEY_ALIAS`，同时清除遗留 prefs 键（Wave 12 清退旧 PIN 体系的
  同一模式；Keystore 不可达时跳过、下次启动重试）。
- **字符串**：删 `unlock_passkey_verify_failed`（`values/strings.xml:42` / `values-en/strings.xml:42` 成对）。

### 1.4 ISSUE-P3-328：字段签名去 Keystore 化改存明文目标键（屏蔽功能保留）

- **整文件删除**：`KeystoreHmacFieldSignatureSource.kt`、`HmacFieldSignatureSource.kt`、
  `AutofillModule.kt`（仅绑定该接口）。
- **`AutofillFieldSignature.kt` 重写**：去掉 `source` 参数与 HMAC 产出，目标键 =
  `v3|<包名>|<域>|<角色>` 规范化明文串；`SCHEMA_VERSION` 升 **v3** 触发一次性清空 v2 签名
  （hex 签名与明文键不同域，留着只会占计数）；新增 `isWellFormedTarget` 供读取侧过滤旧格式残留；
  `K_SALT` 删除逻辑保留。
- **`AutofillFieldBlocklistStore.kt`**：摘除 `signatureSource` / `signatureUnavailable(Flow)` /
  `hmacKeyUnavailable` / `signatureOf` 与 `isBlocked` 的密钥不可用置位分支；`isBlocked` / `block` /
  `unblock` / `blockedSignatures` / `clearAll` 与选择器 / 设置页入口**行为不变**；迁移删 v1 盐 +
  v2 签名键 + **v2 Keystore 别名**（`com.keepasskey.autofill_field_signature` 退役残留清理，
  在一次性 schema 迁移闸门内执行）。
- **健康自检面摘除**：`AutofillHealthProbe.kt` 采集项、`AutofillHealthPolicy.kt`
  （`FIELD_BLOCK_SIGNATURE_UNAVAILABLE` 枚举 + 报告字段 + `evaluate` 参数 + `issues` 构建）、
  `AutofillHealthCard.kt` 渲染分支。
- **安全表述同批改写（中英成对）**：`autofill_field_block_dialog_desc` 与
  `autofill_picker_block_confirm_desc` 不再声称「不可逆签名 / 不记录网址明文」，改「保存于应用私有存储」；
  `autofill_health_issue_field_signature_unavailable` 双语同删。**边界依据**：prefs 仅同 UID / root
  可读而该对手同时可读 `.kdbx`——与 `PD-36` 已接受边界同源，属元数据面而非新增秘密面
  （限界表 §33 与 KDoc 均如实声明）。

---

## 2. 测试资产对账（工程规则「测试资产纪律」；删除理由 = 被测生产代码整体移除，与生产代码同一次提交入库）

### 2.1 整文件删除

| 文件 | 例数 | 被测对象 |
|---|---|---|
| `UnlockThrottleIntegrityTest` | 7 | `UnlockThrottleIntegrity` 契约（P1-277） |
| `SharedPrefsUnlockThrottleStoreTest` | 10 | 删键复位判定 / MAC 校验语义（P1-277；持久化语义随生产语义收敛由 `UnlockThrottleManagerTest` 与设备侧既有覆盖承载） |
| `UnlockThrottleDeletionBypassDeviceTest`（设备侧） | 5 | 同上（真实 Keystore 面） |
| `UnlockPasskeyManagerGateTest` | 7 | `UnlockPasskeyManager`（P3-327） |
| `UnlockPasskeyKeySpecTest` | 6 | `UnlockPasskeyKeyPolicy` 规格守卫（P3-327） |
| `UnlockPasskeyAssertionTest` | 9 | 断言纯函数（P3-327） |
| `UnlockPasskeySigningDeviceTest`（设备侧） | 3 | 断言真机链路（P3-327） |
| `FakeUnlockPasskeyStore` / `FakeUnlockThrottleIntegrity` / `TestHmacFieldSignatureSource` | — | 测试假实现随接口删除 |

### 2.2 摘除 / 改造

- `UnlockThrottleManagerTest`：摘 `记录完整性校验失败时闸门failClosed并落有效锁定期`、
  `完整性failClosed不受开关关闭影响`（2 例，P1-277）。
- `SyncRollbackGuardTest`：删 `状态MAC失效时按无历史处理不产生误报回退` 与 `FixedMac`（P3-326），
  其余 8 例改构造；**新增** `遗留带mac行的状态文件可被读取且该行被忽略`（兼容行为锁定，净 8→8+1）。
- `SyncEngineTest`：删 `testMac()` 辅助、5 处构造改签名（P3-326）。
- `AndroidKeystoreSecretGuardsDeviceTest`（设备侧）：摘防回滚 MAC 2 例与字段签名 1 例及
  `AUTOFILL_FIELD_SIGNATURE_ALIAS` / `HMAC_OUTPUT_BYTES` 常量，类 KDoc 改「收窄留痕」；同步凭据封印
  2 例保留（P3-326 / P3-328）。
- `BiometricCredentialStorageTest`：`revokeAllBiometricData` 用例摘除 passkey 断言、保留封印凭据部分
  （P3-327）。
- `BiometricEnableCoordinatorTest`：关闭撤销用例摘除 passkey 断言（用例保留改名，P3-327）。
- `QuickUnlockSealDowngradeDeviceTest`：构造签名改（P3-327）。
- `AutofillFieldBlocklistStoreTest`：13 → 10（删密钥不可用 / 可观测标志 3 例，新增目标键格式 1 例，P3-328）。
- `AutofillFieldSignatureTest`：9 例重写为明文目标键语义（删「不可逆性」「密钥敏感性」2 组，
  新增版本前缀 / 良构过滤 2 例，P3-328）。
- `AutofillHealthPolicyTest`：摘字段签名密钥 2 例（P3-328）。
- `AlgoHotPathGuardsTest`：摘字段签名 hex 生成 / Keystore 句柄缓存断言（用例保留，P3-328）。
- 7 处构造点改签名：`SettingsSessionLockEraseTest` / `RecycleBinToggleApplyTest` / `HealthCheckViewModelTest` /
  `ChildDatabaseSettingsWiringTest` / `BreachCheckHealthTest` / `Argon2ParametersApplyTest` /
  `AlgorithmSelectionApplyTest`（P3-328）。

### 2.3 新增

- `SyncRollbackGuardTest.遗留带mac行的状态文件可被读取且该行被忽略`（P3-326 遗留兼容锁定）；
- `AutofillFieldBlocklistStoreTest.落盘目标键为本版本格式且含归一化目标明文`（P3-328 口径锁定）；
- `AutofillFieldSignatureTest.目标键为当前版本前缀格式且被读取侧判为良构` / `旧版本残留条目被判为非良构`。

---

## 3. 文档同步

- [`产品裁决登记.md`](../../architecture/产品裁决登记.md)：`PD-46` 新立（四条全部移除的用户裁决与
  删除边界）；`PD-34` 白名单中 `UnlockPasskeyManager` 行回写「已移除」。
- [`已知工程限界.md`](../../architecture/已知工程限界.md)：§3.6 标注**整节失效**（解锁断言层移除，
  平台约束留痕为历史）；§5 同步防回滚口径改写（不再有 MAC）；**§33 新立**（节流状态不设完整性认证 +
  升级残留惰性无害声明）。
- [`同步层记录级完整性威胁建模.md`](../../security/同步层记录级完整性威胁建模.md)：§3.3 改口径
  （状态文件为明文摘要文件、`mac=` 遗留兼容、fail-open 分支表述）、§5 / §6 落点表摘除已删文件。
- [`本地文件级防护层的参考项目对照.md`](../../references/本地文件级防护层的参考项目对照.md)：
  §0 回写裁决去向（事实取证部分不变）。
- **「升级后首轮重放窗口」与 fail-open 两条既有限界未被表述为已消除**（P3-326 AC④）。

## 4. 验证

- `.\gradlew.bat test --rerun-tasks --max-workers=1` 全绿：`tests=2709 → 2662`（-47，与 §2 对账一致：
  P1-277 JVM -19 / P3-326 净 0 / P3-327 JVM -22 / P3-328 JVM -6；设备侧 8 例另计）。
- 设备侧：本批改动涉及 AndroidKeyStore 真实行为面的**删除**与设备侧用例删改，按 AC 以
  `:crypto:` / `:database:` / `:sync:` / `:app:` 四层 `connectedDebugAndroidTest` 在 AVD
  Pixel_10（API 36，google_apis x86_64，无头）真机实跑承托（读数见 §5 末尾；设备为新建 AVD，
  无用户数据，符合 §263 设备侧前置检查规则）。

## 5. 机检读数（原样粘贴）

```
=== 门禁读数（ISSUE-P3-305 AC④：批次文档 §3 须原样粘贴本块）===
[1/7] tools/doc/count_line_tiers.py                EXIT 0  | tier1(>500)=0  tier2(400~500)=33  budget=37
[2/7] tools/doc/long_functions.py                  EXIT 0  | functions_ge_100=0
[3/7] tools/doc/check_md_links.py                  EXIT 0  | BROKEN_MD_LINKS=0
[4/7] tools/doc/check_resolved_index_sync.py       EXIT 0  | RESOLVED_INDEX_SYNC=OK（批次正文 332 份；分册登记 334 条；全量索引 334 条；最大 §334）
[5/7] tools/audit/check_tautological_assertions.py EXIT 0  | 汇总：命中 0 处 / 扫描 458 个测试文件
[6/7] tools/audit/check_recheck_consistency.py     EXIT 0  | PASS: 无残留禁用短语（已扫描 1331 行，11 条禁用短语）
[7/7] tools/doc/check_bounded_type_names.py        EXIT 0  | allowed=13  unregistered_manager_util_helper_common=0
=== 汇总：7/7 PASS ===
```

```
xml=401 tests=2662 failures=0 errors=0 skipped=13
已排除非 JVM 单测 XML：{'debug': 5}（这些目录里的结果是**上一批遗留**，不随 `test` 重跑，不得计入本计数）
```

```
四层 connectedDebugAndroidTest（AVD Pixel_10 / API 36 / google_apis x86_64 无头，ANDROID_SERIAL 锁定）：
:crypto:   tests=37  failures=0 errors=0 skipped=0
:database: tests=18  failures=0 errors=0 skipped=0
:sync:     tests=24  failures=0 errors=0 skipped=0（含 SyncCacheAndroidRuntimeTest 0600/0700 仅属主权限不变量）
:app:      tests=67  failures=0 errors=0 skipped=0
合计 tests=146 failures=0 errors=0 skipped=0
```

**设备侧环境前置（首轮 `:app:` 两红均环境性，处置后复跑全绿，非应用缺陷）**：

- AVD 出厂 locale 为英文，认证数据集文案渲染为
  `Unlock KeePasskey to view credentials`，而 `AutofillAuthChainDeviceTest` 按中文文案
  `解锁 KeePasskey` 等待 ⇒ 超时断言失败（证据文件 `autofill-auth-chain-evidence.txt` 的窗口快照
  可证数据集**已**下发并展示，仅文案语言不符）。处置：`settings put system system_locales zh-CN`
  + 重启模拟器后通过——**该用例族文案断言依赖设备 locale 为中文**，此前仅在中文 locale 真机
  （Redmi 4X）上跑过故未现形；宿主单测不受影响。
- `CredentialSaveChainDeviceTest` 首轮 `AssumptionViolatedException`（系统未登记本应用为凭据
  提供者）：按 240 批次 §3.2 处置 `settings put secure credential_service
  com.keepasskey/com.keepasskey.app.passkey.KeePasskeyCredentialProviderService` 预登记后实跑通过。
- 全程 `ANDROID_SERIAL=emulator-5554` 锁定 AVD；同机物理设备（unauthorized 态）未被触碰，
  符合 §263 设备侧前置检查规则（AVD 为本批新建、无用户数据）。
