# §197 对拍段登记批次（新登 `ISSUE-P3-196`：格式面 26 处字面量的判定与 `.kdbx` / passkey 对拍，**工具链实测已就位**）

> **起因**：`ISSUE-P3-188` 第 4 目收尾时写着「余 7 份待逐处判定，须与 `.kdbx` 对拍同批做，属独立一段」
> （§168 原话）。这段一直**没有独立条目**，只有那句话指代；而它到底是不是「等设备 / 等环境」，
> 此前没人实测过。本批做两件纯文档面的事：**实测前置条件** + **把它变成自包含条目**。

---

## 1. 实测结论：这一段**不是**阻塞项

| 前置条件 | 实测（2026-09-19，本机） | 判定 |
|---|---|---|
| 官方实现端到端对拍（§38 唯一认的互操作证据） | `keepassxc-cli` ⇒ `C:\Program Files\KeePassXC\keepassxc-cli`，`--version` = **2.7.12** | **可用** |
| 第二方实现交叉验证 | `python -c "import pykeepass"` ⇒ **4.2.0**（Python 3.12.10） | **可用** |
| 既有对拍产物 | `database/build/interop-probe/keepasskey-probe.kdbx` + `PROBE.md` 在位（`OwnProductInteropProbeTest` 产出，其 KDoc 已写好两条外用命令与期望） | **可复用** |
| 原生内核单测 | `command -v cargo` ⇒ `C:\Users\baiyun\.cargo\bin\cargo`（**装了**）；但代理执行 `cargo test` 被本机权限层拒绝 | **工具在、需用户跑或授权** |

⇒ 该段的真实门槛是**工作量与纪律**（逐处对照规范 + 每次触动格式面都要重跑对拍并留 `PROBE.md` 证据），
不是环境。此前文档里「属独立一段」的措辞容易被读成「等条件」，本批把这一点钉死。

## 2. 新登记条目要点（正文自包含在 `ACTIVE_ISSUES.md`，此处只记登记动作）

`ISSUE-P3-196`：26 处 / 7 份（`PasskeyPrf` 5、`InnerRandomStreamCipher` 5、`KdbxXmlStreamWriter` 4、
`CoseKey` 3、`PasskeyAssertionSigner` 3、`PasskeyPkcs8Codec` 3、`KdbxFile` 3——逐字转录 §168 §2 第 3 类），
AC 四条 + **设备义务边界**一条：只动 Kotlin 面**不触发**「原生面须真机四层 `connectedDebugAndroidTest`」，
故可无设备入库；一旦顺手触碰 `crypto/src/main/rust/**` / `jni_bridge_ext.rs` / `Native*` 分派即升级为阻塞。
并写明 `generate_corpus.py --verify` 只证文件头自洽、**不构成**互操作证据（§3 规则 8），
以及「与 `KdbxHeader` / `KdbxXmlParser` 的同面第二档搬运**同段推进以免两次对拍**」。

同时在 `ISSUE-P3-188` 第 4 目那句「属独立一段」处补了指向 `ISSUE-P3-196` 的指针，
使「本条不得据此读作已闭环」的判据有了明确的落点。

## 3. 验证（含一条**证据降级**的如实登记）

| 项 | 结果 |
|---|---|
| 改动面 | `git status` 仅 `docs/`——**零代码改动** |
| `python tools/doc/check_md_links.py` | `BROKEN_MD_LINKS=0`（§194 硬化后的判据） |
| `test --rerun-tasks --max-workers=1` | **本轮未跑**。全绿证据沿用 §196 的那一次（`114 actionable tasks: 114 executed`、`xml=315 tests=2209 failures=0 errors=0 skipped=13`、`Uncaught exception` 命中 0）——**其代码状态与本批一致**（本批只动文档） |
| 中途误跑的一次 `test`（无 `--rerun-tasks`） | 报 `BUILD SUCCESSFUL in 15s`、`114 actionable tasks: **1 executed, 113 up-to-date**` ⇒ **不构成证据**，按 §188 已登记的口径（聚合任务报 UP-TO-DATE 不证明执行过任何东西），故不写入绿灯依据 |
| `cargo test` | **未执行**（代理侧被权限层拦下）——已在 `ISSUE-P3-196` 里写明须由用户跑或授权，**不得**以宿主用例代替 |

**登记价值自评**：本批不减少任何存量数字（≥100 项仍 18、第二档仍 28），产出是一条**可执行的**待办
与被实测否证的「伪阻塞」前提。若不做这步，下一批很可能又以「等对拍环境」为由绕开这 26 处。
