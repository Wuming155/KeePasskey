# §98 上一会话遗留界面改动落盘批次（2026-09）

**条目**：无既有 ISSUE 编号（**上一会话遗留的未提交改动**，经用户裁决后原样落盘）
**来源**：2026-09-16 本批施工期间在工作区 `git status` 中发现——8 个界面 / 构建文件处于已修改未提交状态，
另有两个预览导出脚本被删除、两个新脚本未跟踪。改动**不是**本批（§97 / ISSUE-P3-132）作者所写，
亦不在用户提供的外部 UI 评审报告的 5 类问题之内。

## 98.1 事实与边界（先声明，避免后人对作者与依据产生误读）

- **设计依据缺失**：原作者的批次文档、条目与验证记录**均不存在**（未提交即中断）。
  本批只能对「改了什么」负责，**不能**为「为什么这样改」背书。
- **落盘裁决**：用户于 2026-09-16 明确裁决「并入一次独立提交并推送」（三选一），故本批原样落盘，
  **不做**任何二次设计改动（除下述 98.3 的编译性核对）。
- **可逆性**：整批为独立一笔提交，如需回退可按文件粒度 `git revert` / `git checkout`。

## 98.2 落盘内容

| 文件 | 改动 | 性质 |
|---|---|---|
| `app/build.gradle.kts` | 预览导出任务重构：`exportAllPreviewScreenshots`（all，按包路径）→ `exportSecondaryPreviewScreenshots`（次要界面，排除 20 张主屏白名单）；两个任务均按文件名「浅色/深色」路由到 `{light,dark}/` 子目录 | 开发工具链（不影响 APK） |
| `导出预览图-主页面.bat` / `导出预览图-全部.bat` | **删除**（旧入口，对应已移除的 `all` 布局） | 开发工具链 |
| `export-preview-main.bat` / `export-preview-secondary.bat` | **新增**：调用 `:app:exportMainPreviewScreenshots` / `:app:exportSecondaryPreviewScreenshots`（任务名与上表一致，已核对） | 开发工具链 |
| `GeneratorDisplayCard.kt` | 生成结果卡：删除右上角仅图标的「重新生成」`IconButton`；主操作改为整行 `Button`「重新生成」（含旋转动画 + 触感），「复制」降为 `OutlinedButton` | 界面 |
| `EntryEditScreen.kt` | 编辑页顶栏「保存」由仅图标 `IconButton` 改为文字 `TextButton` | 界面 |
| `SettingsScreen.kt` | 设置页顶栏「锁定」由仅图标 `IconButton`（红锁）改为 `TextButton`（图标 + 文案） | 界面 |
| `UnlockScreen.kt` | 「已启用第三方无障碍服务」提示由 `tertiaryContainer` 大卡片改为 `surfaceContainer` 轻量 Banner（`labelLarge`/`labelSmall` + `onSurfaceVariant`） | 界面 |
| `AutofillPickerScreen.kt` | 候选列表由裸 `Column` 改 M3 `ListItem`（前置锁图标 + 用户名/主机副标题）+ `HorizontalDivider`；请求方身份块改「标题 + 包名 + 应用名 + 指纹 + 自报域」逐行分区 | 界面（安全敏感页，语义未变） |
| `VaultEntryRowLayouts.kt` / `VaultEntryRows.kt` | **行为变更**：库列表行删除「复制用户名」`IconButton`，仅保留「复制密码」；`onCopyUsername` 退化为带默认值 `= {}` 的未使用参数 | 界面 + 行为 |

## 98.3 核验（本批实际执行）

- `.\gradlew.bat :app:compileDebugKotlin` — BUILD SUCCESSFUL。
- `.\gradlew.bat test --rerun-tasks --max-workers=1` — **BUILD SUCCESSFUL**，2060 例 / 0 失败 /
  13 例跳过（sync 真实联调）。**注意**：该次全量单测在含本批改动的树上执行，故本批的
  「编译 + 全量单测」结论成立；但**没有**针对本批任何改动新增专项回归。
- `.\gradlew.bat :app:exportMainPreviewScreenshots` — BUILD SUCCESSFUL（`已导出 主页面: light=20 dark=20`），
  即**新的导出工具链已被真实执行**，不只是纸面重构。

## 98.4 如实声明（残余与风险）

- **未做设备侧实测**；未重新导出次要界面预览（`exportSecondaryPreviewScreenshots` 未执行）。
- **行为变更需产品确认**：列表行不再提供「复制用户名」（详情页仍可复制）。该取舍由上一会话以代码注释
  （「列表行仅保留核心『复制密码』，降低误触」）说明，但**没有**任何条目 / 文档登记其依据与验收口径。
  本次落盘**未**改变该行为，也**未**为其补设计依据；如产品口径不同，应按独立条目回退或改造。
- `UnifiedVaultEntryRow` 的 `onCopyUsername` 已成为**带默认值的未使用参数**（调用方仍传入），
  属可清理的残留；因清理会牵动 `VaultListScreen` → `VaultListContent` 的整条上行链路，
  交由后续独立批次处理，**不在本批夹带**。

---

## 归档索引行原文（无损迁移承接）

> 迁移前本批次的正文同时存在于三处：总索引行、分册级索引行、本文件。以下按「只搬迁、不改写」
> 原文照录两处索引行正文（更正须另加小节，不得就地改），自此两处索引只留一行指针。
> 承接批次：§154。

### 总索引行原文（§98）

上一会话遗留界面改动落盘批次：施工期间发现工作区存在**上一会话未提交**的 8 个界面/构建文件与预览脚本重命名（生成器主操作改整行按钮、编辑页保存与设置页锁定改文字按钮、无障碍提示降权为 Banner、选择器候选改 `ListItem`+分隔线、**库列表行移除「复制用户名」**、预览导出任务重构为 main/secondary × light/dark）。经**用户裁决**原样落盘为独立一笔提交；本批**如实声明「原作者文档与设计依据均不存在，只为『改了什么』负责」**，核验为编译 + 全量单测绿（2060/0/13）+ 新导出工具链真实执行
