# §251 设备侧两处测试口径收窄批次

> 对应条目：`ISSUE-P3-246`（①）、`ISSUE-P3-247`（②）——**两条均只差设备侧证据**
> 日期：2026-09-21
> 涉及文件：`sync/src/androidTest/java/com/keepasskey/sync/engine/SyncCacheAndroidRuntimeTest.kt`（①）、
> `sync/src/androidTest/AndroidManifest.xml`（②）、`sync/src/androidTest/res/xml/network_security_config.xml`（②，**新建**）
> 性质：**纯测试资产批次**——只动 `sync/src/androidTest/**`。生产代码、`app/src/main/**`（含生产
> network security config）、`app/src/androidTest/**`、构建脚本、`参考项目/` **一行未触**。
> **归档状态**：设备侧证据已补录（§4），`docs/RESOLVED_LOG.md` 加行、`docs/resolved/README.md` 的
> 「当前最大 §250 → 下一批 §251 → 下批 §252」、`BATCH_158_PLUS.md` 分册登记与
> `docs/ACTIVE_ISSUES.md` 的条目剪切（P3 区 2 项 → 0 项）**均已随本批完成**。

---

## 1. 前提复核

| 序号 | 规格前提 | 核实方式 | 结论 |
|:--:|---|---|:--:|
| ① `:76` | `SyncCacheAndroidRuntimeTest` 以 `dir.listFiles()` **原始返回值**判「清理后不得残留任何文件」 | 直读该文件（施工前快照 `:76`）：`assertTrue("清理后不得残留任何文件", (dir.listFiles() ?: emptyArray()).isEmpty())`；`dir` 来自本类 `newCacheDir()`（`context.cacheDir` 下的私有目录，见 `:31-32`） | 前提**成立** |
| ① 参照写法 | 规格称参照 `sync/src/test/java/com/keepasskey/sync/engine/SyncCacheEvictorTest.kt` | **该路径不存在**（`sync/src/test/**` 下仅有 `SyncCacheTest.kt` / `SyncCacheStreamingTest.kt`）；实体口径的既有写法实为两处：`sync/src/test/.../SyncCacheTest.kt:136-137`（`walkTopDown().filter { it.isFile }` 对 `emptyList<File>()`，`clearAll` 用例）与 `app/src/test/java/com/keepasskey/app/sync/SyncCacheEvictorTest.kt:57-58`（`realFilesUnder(dir)` 助手 + `:127-128` 同款比对） | 路径**有误**，写法**一致**（本批按同款写法落地，见 §2①） |
| ② 现状 | `sync/src/androidTest/AndroidManifest.xml` 以 `usesCleartextTraffic="true"` **整进程**放行明文 | 直读该文件（施工前 `:22`）：`<application android:usesCleartextTraffic="true" />`；同文件 `:18-20` 注释自述「只放开**测试 APK 进程**的回环明文」，限界 §4.1（`已知工程限界.md:291-292`）亦写作「回环明文豁免」 | 前提**成立**（文档表述宽于实现） |
| ② 主机名勘察 | 规格要求先勘察「用例真到发请求的主机名与 scheme」，并警告**不得**只写 `127.0.0.1`/`localhost`（若用例用「假主机名 + 回环 Dns」则会被改红） | 逐份直读 `sync/src/androidTest/**` 全部 6 个用例 + `grep -rn "http://\|https://\|\.url(\|endpoint\|serverUrl"`：**无任何假主机名、无自定义 `Dns` 指向回环**（`SsrfRedirectBypassDeviceTest` 的 `RecordingDns` 只**记录**入参并委托 `SsrfGuardDns(Dns.SYSTEM)`，不参与将域名指向回环）。清单见 §2② 表 | **只有 `localhost` / `127.0.0.1` 两个主机名** ⇒ 规格列举的两个豁免名**恰好完备** |
| ② 清单冲突 | 规格称「`app` 的 androidTest 清单可能覆盖生产策略，若发现则停下」 | 直读 `app/src/androidTest/AndroidManifest.xml`：`<application>` 内**既无** `networkSecurityConfig` 亦无 `usesCleartextTraffic`（只有两个 `Activity` 声明）⇒ **不构成覆盖**，`CleartextPolicyDeviceTest` 所在进程仍受生产策略约束 | **无冲突**，按 ② 原样施工 |

---

## 2. 整改内容（逐条对 AC）

### ① `ISSUE-P3-246` — `SyncCacheAndroidRuntimeTest` 的残留判据改为**实体口径**（`kotlin:66-97`）

- **改法**：`clearAll 清空全部落盘内容` 的收尾断言由
  `assertTrue("清理后不得残留任何文件", (dir.listFiles() ?: emptyArray()).isEmpty())`
  改为
  `val leftovers = dir.walkTopDown().filter { it.isFile }.toList()` 与 `emptyList<File>()` 的 `assertEquals`
  ——与 `SyncCacheTest.kt:136-137` / `SyncCacheEvictorTest.kt:127-128` 的既有写法同款（同源依据：限界 §7 边界 1）。
- **AC①（实体口径）**：满足；`java.io.File` 与 `org.junit.Assert.assertEquals` **本已导入**（`:6`、`:12`），无新增 import；
  `assertTrue` 仍被本文件其它断言使用（`:48`、`:74`、`:87`），导入未成死码。
- **AC③（不得放宽判别力）**：满足。`isFile == true` 的真残留仍留在 `leftovers` 中 ⇒ 断言照旧变红；
  只排除「目录索引说有、磁盘上没有」的条目。失败信息同时打印
  **索引条目（含鬼影）** 与 **实体残留** 两份读数，使日后同形红能直接区分「鬼影」与「真残留」。
- **断言消息**：强化为「清理后不得残留任何**实体**文件（判据为 `isFile`，见限界 §7 边界 1；磁盘上没有的目录索引条目不计入）」。
- **用例注释**：新增 KDoc，写明**为何不用 `listFiles()`**：限界
  [`已知工程限界.md`](../../architecture/已知工程限界.md) §7 边界 1（宿主 Windows/NTFS 偶发返回大写拼写鬼影条目）、
  同源依据 `docs/records/SyncCache大写CACHE临时文件定位记录.md` 与 §107 / `ISSUE-P2-192` 既有口径、
  宿主侧两处同款更正，并如实登记 §7 边界 3 自陈的约 3% 假阳性残余（`isFile` 非百分百可靠）。
- **未动**：本文件另外两个用例（`:37-50`、`:52-64`）与其断言**一行未改**；未动任何生产代码。
- **AC②（真机跑 `:sync:connectedDebugAndroidTest`）**：**未执行**，见 §4。

### ② `ISSUE-P3-247` — sync 测试 APK 的明文放行收窄为**只放行测试实际使用的主机名**

**勘察结论（设备侧用例请求面全清单）**：

| 用例 | 是否发网络请求 | URL 里的主机名 | scheme |
|---|:--:|---|:--:|
| `WebDavTransferDeviceTest` | 是（`MockWebServer` 真回环 socket） | `localhost`（`:54` 的 `server.url("/")`；host 形态由 `SsrfRedirectBypassDeviceTest:61-67` 钉为 `localhost`） | `http` |
| `S3TransferDeviceTest` | 是 | `127.0.0.1`（`:47` `endpoint = "http://127.0.0.1:${server.port}"`，path-style 寻址） | `http` |
| `SsrfRedirectBypassDeviceTest` 第 1 例 | 否（只读 `server.url()`） | `localhost`（断言对象） | — |
| `SsrfRedirectBypassDeviceTest` 第 2 例 | 是（工厂客户端，自签 HTTPS） | `localhost` / `127.0.0.1` | `https`（明文策略不适用） |
| `SsrfRedirectBypassDeviceTest` 第 3 例 | 是（复刻 `Dns` 接线的明文客户端） | 初跳与重定向目标：`127.0.0.1`（`:129`/`:133`/`:153`）；控制组重定向目标：`localhost`（`:150`） | `http` |
| `WebDavPropfindParserDeviceTest` | 否（纯解析） | — | — |
| `SyncCacheAndroidRuntimeTest` / `RawFileWritePermissionDeviceTest` | 否（私有目录落盘） | — | — |

⇒ **需要明文豁免的主机名恰为 2 个：`localhost` 与 `127.0.0.1`**。勘察中**未发现**任何「假主机名 + 回环 `Dns`」形态，
故按规格给出的两个名字即可，不需要额外域名（**此点已按规格警告逐份核对 6 个用例**）。

**改动（三个落点）**：

1. **新建** `sync/src/androidTest/res/xml/network_security_config.xml`（**该测试 APK 专用**）：
   - `<base-config cleartextTrafficPermitted="false">` + `<trust-anchors><certificates src="system" /></trust-anchors>`
     ——与生产策略同形：**默认禁明文**、信任锚仅系统 CA；
   - 唯一的 `<domain-config cleartextTrafficPermitted="true">` 内含 **2 个域豁免**，逐条依据：
     - `<domain includeSubdomains="false">localhost</domain>` —— `WebDavTransferDeviceTest` 的
       `server.url("/")` host（`SsrfRedirectBypassDeviceTest:61-67` 的断言即该事实）+
       `SsrfRedirectBypassDeviceTest` 第 3 例控制组重定向目标；
     - `<domain includeSubdomains="false">127.0.0.1</domain>` —— `S3TransferDeviceTest` 的 endpoint +
       `SsrfRedirectBypassDeviceTest` 第 3 例的初跳与重定向目标（IPv4 字面量）。
   - 文件头注释写明：平台校验的是 **URL 里的主机名**（非解析后 IP），故逐名列举而不合并为 IP 字面量；
     以及「其余任何主机仍受平台禁令约束」。
2. **`sync/src/androidTest/AndroidManifest.xml`**：删除 `android:usesCleartextTraffic="true"`，
   改为 `<application android:networkSecurityConfig="@xml/network_security_config" />`；
   同段注释重写为「只放行设备侧用例**实际使用的主机名**（`localhost` 与 `127.0.0.1`，平台校验 URL 主机名，
   依据见 config 内各 domain 注释）……生产 APK 的全站禁明文
   （`app/src/main/res/xml/network_security_config.xml`）不受影响，且 `CleartextPolicyDeviceTest`
   依赖该禁令保持生效」；`INTERNET` 权限与「库不自带权限」的说明段**保留原样**。
3. **`app/src/main/res/xml/network_security_config.xml` 与 `app/src/androidTest/AndroidManifest.xml`**：
   **一行未触**（AC③）。

- **AC①（只对 `127.0.0.1` / `localhost` 放行，其余域仍禁）**：满足（`base-config` 为显式 `false`）；
  真机读数待补（见 §4）。
- **AC②（真机跑 `:sync:` 与 `:app:` 两层 connected）**：**未执行**，见 §4。
- **AC③（生产策略一行不得放宽）**：满足（未触该文件；§3 给出 `git status` 面）。

---

## 3. 宿主侧验证（现跑）

| 命令 | 真实输出 | 判定 |
|---|---|:--:|
| `.\gradlew.bat :sync:compileDebugAndroidTestKotlin --rerun` | `BUILD SUCCESSFUL in 3s`、`22 actionable tasks: 6 executed, 16 up-to-date`；其中 `generateDebugAndroidTestResources` / `packageDebugAndroidTestResources` / `generateDebugAndroidTestRFile` **均已执行**（非 UP-TO-DATE） | 通过（编译门禁） |
| `.\gradlew.bat :sync:assembleDebugAndroidTest --rerun-tasks` | `BUILD SUCCESSFUL in 9s`、`65 actionable tasks: 65 executed`；含 `processDebugAndroidTestResources` / `mergeProjectDexDebugAndroidTest` / `packageDebugAndroidTest` | 通过（测试 APK 实打） |

**② 的资源打包确认（规格要求「如实报告你怎么确认资源被打进测试 APK」）**——按「源文件 → 合并清单 → APK 三级」逐级读产物：

| 层级 | 命令 | 真实输出 |
|---|---|---|
| 资源进入 androidTest 源集产物 | `find sync/build/intermediates -ipath "*androidTest*" -iname "*network_security*"` | `sync/build/intermediates/packaged_res/debugAndroidTest/packageDebugAndroidTestResources/xml/network_security_config.xml`（`diff` 与源文件**逐字节一致**，输出 `PACKAGED_RES_IDENTICAL`） |
| 测试 APK 合并清单属性 | 读 `sync/build/intermediates/packaged_manifests/debugAndroidTest/processDebugAndroidTestManifest/AndroidManifest.xml:49` | `android:networkSecurityConfig="@xml/network_security_config"`（**且全文已无** `usesCleartextTraffic`） |
| APK 内资源条目 | `unzip -l sync/build/outputs/apk/androidTest/debug/sync-debug-androidTest.apk` | `852  1981-01-01 01:01  res/xml/network_security_config.xml` |
| APK 清单属性（链接后二进制） | `aapt2 dump xmltree --file AndroidManifest.xml <test-apk>` | `A: http://schemas.android.com/apk/res/android:networkSecurityConfig(0x01010527)=@0x7f030000` |
| 资源 ID 解析 | `aapt2 dump resources <test-apk>` | `resource 0x7f030000 xml/network_security_config` → `() (file) res/xml/network_security_config.xml type=XML` |
| APK 内 config 内容（回读） | `aapt2 dump xmltree --file res/xml/network_security_config.xml <test-apk>` | `base-config cleartextTrafficPermitted=false` + `trust-anchors > certificates src="system"`；唯一 `domain-config cleartextTrafficPermitted=true` 下 `domain includeSubdomains=false 'localhost'` 与 `'127.0.0.1'` |

⇒ **资源确已打进测试 APK**，且 APK 内读回的豁免面**恰为上列 2 个域**（无其它域、无全局放行）。
（`aapt2` 路径：`%LOCALAPPDATA%\Android\Sdk\build-tools\37.0.0\aapt2.exe`。）

**其它现跑自检**：

| 命令 | 真实输出 | 判定 |
|---|---|:--:|
| `python tools/doc/check_md_links.py` | 见 §5 第 4 条（`docs/` 相对链接） | 通过 |
| `python tools/doc/check_resolved_index_sync.py` | 施工期**报红**（正文已入库、分册登记尚未补录），补录后转绿——见 §5 第 4 条 | 已转绿（补录后） |
| `git status --short` | `M sync/src/androidTest/AndroidManifest.xml`、`M sync/src/androidTest/java/com/keepasskey/sync/engine/SyncCacheAndroidRuntimeTest.kt`、`?? sync/src/androidTest/res/` | 改动面**恰为**规格所列三处 |

> **一处未按规格执行**：`.\gradlew.bat test` **未跑**。本批无 JVM 单测可覆盖的改动
> （两处改动全在 `androidTest` 源集，宿主 `test` 任务不编译它），跑它只会得到 `UP-TO-DATE` 的空读数，
> 不构成判别性证据；判别性证据只有设备侧（§4）。此点如实登记。

---

## 4. 设备侧验证（真机执行，读数逐字补录）

**执行环境**：Redmi 4X / Android 17 · API 37；执行日期 2026-09-21。
设备操作（安装、`force-stop`、结果 XML 回读）按 §243 先例以 `adb` 为**声明式退路**，界面取证仍走 Android CLI。
读数由上级在真机执行后补录，本批归档时**已按落盘的结果 XML 逐份复核**（文件 mtime：`sync` 20:44、
`app` 20:59），下表即复核后的原文读数。

### 4.1 逐层读数

| 层 | 结果文件 | `tests` | `failures` | `errors` | `skipped` |
|---|---|:--:|:--:|:--:|:--:|
| `:sync:connectedDebugAndroidTest`（**本批 AC②**） | `sync/build/outputs/androidTest-results/connected/debug/TEST-Redmi 4X - 17-_sync-.xml` | **24** | **0** | **0** | **0** |
| `:app:connectedDebugAndroidTest`（**本批 AC②/AC③**） | `app/build/outputs/androidTest-results/connected/debug/TEST-Redmi 4X - 17-_app-.xml` | **71** | **0** | **1 skipped** | **1** |
| `:database:connectedDebugAndroidTest`（旁证，非本批 AC） | `database/.../TEST-Redmi 4X - 17-_database-.xml` | 17 | 0 | 0 | 0 |
| `:crypto:connectedDebugAndroidTest`（旁证，非本批 AC） | `crypto/.../TEST-Redmi 4X - 17-_crypto-.xml` | 37 | 0 | 0 | 0 |
| `:core:connectedDebugAndroidTest`（旁证，非本批 AC） | `core/.../TEST-Redmi 4X - 17-_core-.xml` | 4 | 0 | 0 | 0 |

**唯一的 skipped 用例**：`com.keepasskey.app.passkey.CredentialSaveChainDeviceTest > 系统保存请求必须能路由到本应用的凭据提供者`
——系**环境前提 `Assume`**（系统未把本应用登记为凭据提供者，即 `ISSUE-P2-239` / 限界 §27 登记的 ROM 面），
非本批改动所致；该判定口径与 §247 批次一致。
`:database:` / `:crypto:` / `:core:` 三层为同轮旁证，**不作为本批验收依据**。

进程一致性说明：`app` 结果 XML 的 71 例中 `com.keepasskey.app.security.QuickUnlockSealDowngradeDeviceTest`
（3 例）与 `TracedProcessProbeDeviceTest`（4 例）均为 `0 skipped` 通过，可证本轮设备上 UiAutomation 槽位未被占用。

### 4.2 逐条对 AC

**① `ISSUE-P3-246`（AC②：实体口径改动面真机实跑）**——`:sync:` 结果 XML 中该类的 3 例全部执行、0 失败：

| 类 | 例数 |
|---|:--:|
| `com.keepasskey.sync.engine.SyncCacheAndroidRuntimeTest`（**①的改动面**） | 3 |
| `com.keepasskey.sync.engine.RawFileWritePermissionDeviceTest` | 4 |
| `com.keepasskey.sync.network.SsrfRedirectBypassDeviceTest`（**②的明文面**） | 3 |
| `com.keepasskey.sync.s3.S3TransferDeviceTest`（**②的明文面**） | 3 |
| `com.keepasskey.sync.webdav.WebDavTransferDeviceTest`（**②的明文面**，`localhost`） | 5 |
| `com.keepasskey.sync.webdav.WebDavPropfindParserDeviceTest` | 6 |
| **合计** | **24** |

⇒ 改写后的实体口径断言（`walkTopDown().filter { it.isFile }` 对 `emptyList<File>()`）在真机上**通过**，
且判别力未被放宽（§2① 的判据分析 + §3 的宿主侧读数）。

**② `ISSUE-P3-247`（AC①：收窄后仍能连通回环；AC③：生产禁令未受影响）**——

- **AC①（收窄后回环仍放行）**：`WebDavTransferDeviceTest`（5 例，`localhost`）、`S3TransferDeviceTest`
  （3 例，`127.0.0.1` 字面量）、`SsrfRedirectBypassDeviceTest`（3 例，明文第 3 例走 `127.0.0.1` 初跳 +
  `localhost` 控制组）**全部通过** ⇒ §2② 勘察出的两个豁免域名**恰好完备**，
  未出现任何 `CLEARTEXT communication to <host> not permitted by network security policy` 形态的失败
  ——即 §2② 的「风险提示」所指的兜底动作（增补 `<domain>`）**未被触发**。
- **AC③（生产全站禁明文仍生效）**：`:app:` 层的 `CleartextPolicyDeviceTest`（**4 例，全绿**）通过
  ——其中「设备 LAN IP（非回环）明文请求被平台拦截」一项即「生产禁令仍生效」的硬判据。
  ⇒ 收窄 sync 测试 APK 的明文豁免**未**波及其它进程 / 其它 APK 的生产策略。

### 4.3 同轮如实留痕（三处工程现象，均非本批缺陷）

1. **`:app:` 首轮未跑成：`INSTALL_FAILED_UPDATE_INCOMPATIBLE`。**
   现象：首次执行 `:app:connectedDebugAndroidTest` 时安装阶段失败，报 `INSTALL_FAILED_UPDATE_INCOMPATIBLE`
   （设备上残留一条陈旧安装会话）。核实手段：实测 `pm list packages -u` / `dumpsys package` /
   `/data/data` 三处**均无** `com.keepasskey` 痕迹 ⇒ 属安装器会话残留，非应用数据冲突。
   处置：执行 `adb install -r -t` 复核返回 **`Success`**，随后重跑 `:app:` 全绿（即 §4.1 读数）。
   设备操作按 §243 先例以 `adb` 为**声明式退路**，界面取证仍走 Android CLI。

2. **UiAutomation 槽位被占（已登记约束的现场复现，不是新缺陷）。**
   现象：首轮 `:app:` / `:core:` 崩于
   `IllegalStateException: Cannot call disconnect() while connecting UiAutomation`。
   诊断：`dumpsys accessibility` 中存在 `Ui Automation[...]` 条目，`ps` 中存在
   `com.android.cli.interact.instrumentation` 进程。
   处置：按限界 **§4.1 约束 1** 的既有规避口径
   `am force-stop com.android.cli.interact.instrumentation` 释放槽位后重跑通过。
   ⇒ 这是**已登记约束**的现场复现，**不**作为新缺陷登记。

3. **`:core:` 一度卡 Windows 文件锁（与限界 §12 记录的宿主现象同类）。**
   现象：`java.nio.file.FileSystemException: … logcat-com.keepasskey.core.test-crash-report.txt:
   另一个程序正在使用此文件`（Gradle 守护进程持有 UTP 的 logcat 句柄）。
   处置：`gradlew --stop` 停止守护进程 + 清理该结果目录后重跑通过。

### 4.4 归档动作（本批已执行）

`docs/ACTIVE_ISSUES.md` 剪切 `ISSUE-P3-246` / `ISSUE-P3-247`（P3 区 2 项 → **0 项**，标题计数同步 + 引用块补闭环摘要）
→ `docs/RESOLVED_LOG.md` 加 §251 行 → `docs/resolved/BATCH_158_PLUS.md` 分册登记 §251 →
`docs/resolved/README.md` 最大编号 §250 → **§251**（下一批 §252）。


---

## 5. 如实声明

1. **改动面恰为三处文件**：`sync/src/androidTest/java/com/keepasskey/sync/engine/SyncCacheAndroidRuntimeTest.kt`（①）、
   `sync/src/androidTest/AndroidManifest.xml`（②）、`sync/src/androidTest/res/xml/network_security_config.xml`（②，新建）。
   **未触**任何生产代码、`app/src/main/**`（含生产 network security config 与 `SyncHttpClientFactory`）、
   `app/src/androidTest/**`、`build.gradle.kts`、`参考项目/`（`git status --short` 可对，输出见 §3）。
2. **设备侧证据已补录**：`:sync:connectedDebugAndroidTest`（**24 例 / 0 失败 / 0 error / 0 skipped**）
   与 `:app:connectedDebugAndroidTest`（**71 例 / 0 失败 / 0 error / 1 skipped**）均已在真机执行，
   `ISSUE-P3-246` AC② 与 `ISSUE-P3-247` AC②/AC③ 的**真机部分已完成**（逐条对 AC 见 §4.2，
   三层旁证与三处工程留痕见 §4.1 / §4.3）。本批的设备读数**不是**本代理现场跑到的新鲜输出，
   而是上级在真机上执行后落盘的结果 XML；归档时已按 XML 逐份复核读数与用例清单（§4.1 表）。
3. **未跑 `.\gradlew.bat test`**：理由见 §3 末（`androidTest` 源集不参与宿主 `test` 任务，读数无判别力）。
   **不得**把本批的宿主侧证据读作「测试全绿」——本批只有编译与打包门禁 + 设备侧读数。
4. **`check_resolved_index_sync.py` 的「未登记」红已在归档步转绿**：施工期该脚本因正文已入库而分册登记留待补录，
   必然报「批次正文未被任何分册登记: [251]」+「README 最大编号 §250 与批次正文最大值 §251 不一致」；
   本批归档步完成 `BATCH_158_PLUS.md` 登记、`README.md` 最大编号改 §251、`RESOLVED_LOG.md` 加行后
   **已复跑转绿**（见 §4.4 与 §5 末的现跑读数）。
5. **规格两处与实际不一致，已按实测落地**：
   ① 参照文件 `sync/src/test/java/com/keepasskey/sync/engine/SyncCacheEvictorTest.kt` **不存在**，
   实体口径的既有写法在 `sync/src/test/.../SyncCacheTest.kt:136-137` 与
   `app/src/test/java/com/keepasskey/app/sync/SyncCacheEvictorTest.kt:127-128`（见 §1 第 2 行）；
   本批采用与两者同款的写法（`walkTopDown().filter { it.isFile }` 对 `emptyList<File>()`）。
6. **② 的主机名勘察结果与规格预期一致**：只有 `localhost` / `127.0.0.1`，**未出现**假主机名 + 回环 `Dns` 形态，
   故豁免表所需域名已完备（§2② 表逐例列出依据）。勘察方式为**逐份直读该源集 6 个用例** + 全源集 `grep` 交叉核对。
7. **资源打包已确认到 APK 级**（§3 六级读数：源文件 → `packaged_res` 逐字节一致 → 合并清单属性 →
   APK 条目 → `aapt2 dump xmltree` 属性与资源 ID → APK 内 config 内容回读）；设备侧读数（§4.1 / §4.2）
   为**收窄后用例是否真的放行**的最终证据，两者互为交叉验证。
8. **未跑 `lint` / `assembleDebug` / `assembleRelease`**：本批不改生产面，这些任务不构成判别性证据，未执行、
   也不作为证据使用。
9. **未跑 KPEX 互操作对拍**：本批未触 `.kdbx` 产物格式、`PasskeyData` schema 与 `PasskeyPkcs8Codec`，
   按 `AGENTS.md` 规则 8 不适用，**不对其作任何声称**。
10. **设备侧读数的取证路径如实声明**：本批的设备证据由上级在真机（Redmi 4X / API 37）执行后落盘，
   归档代理**未**在本会话重跑设备侧用例；归档时以结果 XML（`TEST-Redmi 4X - 17-_sync-.xml` /
   `-_app-.xml` / `-_database-.xml` / `-_crypto-.xml` / `-_core-.xml`）**逐份解析复核**，
   读数与用例清单均与上级给出的一致（§4.1 / §4.2 的表即该复核结果）。
   三处工程现象（安装器会话残留 / UiAutomation 槽位被占 / Windows 文件锁）系上级现场留痕，
   本代理**未复现**，按上级原文转录。

---

## 6. 归档步现跑自检（2026-09-21）

| 命令 | 真实输出 | 判定 |
|---|---|:--:|
| `bash tools/audit/check_recheck_consistency.sh` | `PASS: 无残留禁用短语（已扫描 1326 行，11 条禁用短语）` | 全绿 |
| `python tools/doc/check_md_links.py` | `BROKEN_MD_LINKS=0` | 全绿 |
| `python tools/doc/check_resolved_index_sync.py` | `RESOLVED_INDEX_SYNC=OK` | **全绿**（施工期的「251 未被分册登记」红已随本归档步转绿） |
| 结果 XML 复核（五层） | 见 §4.1 表 | 与上级读数一致 |
| `git status --short` | 见 §4.4 所列归档落点 | 改动面符合 |

