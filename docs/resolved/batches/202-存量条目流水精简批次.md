# §202 存量条目流水精简批次（文档面，零代码改动；`ACTIVE_ISSUES.md` 355 → **310 行**）

> **起因**：会话命题「精简文档」。`AGENTS.md` §9 与 `ACTIVE_ISSUES.md` 文档定位的同一纪律——
> 「已闭环条目的正文、批次流水与索引段一律不留；历史实现与验收证据以 `RESOLVED_LOG.md` 与
> `docs/resolved/batches/` 为**单一真相源**」——在 `ISSUE-P3-188` 这条开放条目上失守：
> §155~§201 的逐批流水（每批削了多少行、哪批出了表）在条目内**复述了一份**，而批次文档本就承载它。

---

## 1. 改动原则与范围

**只删「批次文档已承载的流水」，一个不落全部活约束**：

| 删除 | 保留（活约束，逐条核对未丢） |
|---|---|
| 「当前进度」里逐批留痕的括号注（§159 降到 369、§189 再降……）与已出表 9 个 / 已消化 14 个的逐文件流水 | 现值数字（达标 6 + 现值行数、登记 2）、限界 §18 门面登记、`EntryDetailViewModel` 不满足门面前提**不得再外搬**的留痕 |
| 剩余清单注记块里的历史编号映射逐条解释（§190/§195/§189 的结案去向） | 「现为 1~4 项」、限界 §20 与 182 批次指针 |
| 第 1 项里 §178~§198 逐批出表明细、`KeePasskeyApp` / `FieldDiffRow` / §199 风险注等已闭环叙述 | 现跑计数（18 / 待拆 16）与 **§200 复跑纠偏**、现跑尺子义务、当前表内头部、`CreateVaultWizardDialog` 的限界 §179 下限、PD-11 豁免与重开条件指针、**搬移约束四条**（段落组件纪律 / §193 清单锁 / 距 400 余量逐案判 / 纯函数下沉）、**刻意不搬的三处**（逐字保留） |
| 第 2 项里已消化 14 个的逐文件明细 | 测量口径（两端皆含、**最终写盘后取数**）、「减而未出档」与 `CloudSyncScreen` 距出表 1 行、**刻意排后三条**（逐字保留） |

第 3 项（搬家守卫四形态）与第 4 项（断言响应材料）为**活条款 / 活工作项，一字未动**；
`AGENTS.md` 经通读无 §9 违例（只含长期规则、命令与索引指针），本批不动。

## 2. 验证

- `python tools/doc/check_md_links.py` ⇒ `BROKEN_MD_LINKS=0`；
- 行数：`ACTIVE_ISSUES.md` 355 → **310**（−45；其余文档零改动）；
- 内容不变量自查：剩余清单 1~4 项逐条仍在且自包含（计数 / 判据 / 活约束 / 指针四要素逐项核对），
  「见下方第 3 项」「见剩余清单第 1 项」等文内跳转指向仍成立；
- 零代码改动 ⇒ 不触发测试 / lint / 行数尺子重跑义务（本批前最后一次全量证据：§201，2221 全绿）。
