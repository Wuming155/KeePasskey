<a id="s292"></a>

# §292 安全声明按实测等级渲染批次

> `ISSUE-P2-285` **整条闭环**（P2 7 → **6 项**）。
> 触发＝用户命题「解决 Active Issues 内的存量问题」，按优先级认领。
> 覆盖面：`ISSUE-P2-285`（本体，UI 文案 / 实测接线；无加密语义改动）。**未触**原生内核 / `*/src/androidTest/**` / `参考项目/` / 构建脚本 ⇒ 无设备侧必跑项（实测等级分支的真机读数由既有 `BiometricEnableCoordinatorTest` 等覆盖，本批只接既有真值字段）。

---

## 1. 条目正文（原样收录）

### `ISSUE-P2-285`：硬件密钥与「明文不落地」类安全声明无条件渲染（5 处文案，其中 1 处被实现直接否证）

- **核实时间点**：2026-09-23 经渲染点穷举 + `UnlockUiState` 生产构造点穷举（第 ① 条另经逐行反校：`hardwareBackedSecurity` 命中 4 处，唯一赋值在 `@Preview`）。
- **核实方式与清单**（五处**全部无条件渲染**）：
  ① `app/.../ui/screens/unlock/UnlockScreen.kt:217-221` 在 QUICK_UNLOCK 分支显示 `strings.xml:385`「已通过硬件 KeyStore 保全加密主密钥」；真值字段 `UnlockUiState.hardwareBackedSecurity`（`UnlockUiState.kt:49`）**全仓唯一赋值点在 `@Preview`**（`UnlockContentSections.kt:251-261`，生产构造点 8 处无一回填）；`security/KeystoreKeyMaterial.kt:119-126` 对软件级 Keystore 仅 `debugLog.warn`；进入 QUICK_UNLOCK 不要求硬件级（`UnlockModePolicy.kt:31-36`，`BiometricEnrollmentCoordinator.kt:270-277` 降级路径同样置 `isQuickUnlockAvailable = true`）⇒ 软件级机型上「硬件保全」与同卡片 `:113-129` 的「不提供硬件级保护」**同屏并存**。
  ② `strings.xml:660 sec_arch_desc`（渲染于 `SecuritySettingsScreen.kt:424`）称「主密钥驻留 Android Keystore 硬件隔离区，**任何明文数据都不会写入磁盘**」——被两处否证：自产明文导出（`strings.xml:557-563` 自述含全部字段值），以及 `app/data/binary/FileBinaryStore.kt` 的 `store()` / `storeFromStream()` **无任何加密调用**（`core/security/BinaryStore.kt` 契约第 3 条只要求文件 0600 / 目录 0700）⇒ 大附件以明文落私有目录。
  ③ `strings.xml:591 sec_biometric_sub`「密钥由硬件安全模块保护」（`SecuritySettingsScreen.kt:134`），正挂在 ISSUE-P1-22 降级弹窗（`strings.xml:369` 自承软件级、无 TEE/StrongBox）所守护的开关上。
  ④ `strings.xml:745 sync_credential_auth_notice`「同步凭据经**硬件** Keystore 加密封印」（`CloudSyncSections.kt:306`）：走 `SyncCredentialSealer.kt:55/91` → `KeystoreKeyMaterial.kt:119-126`，且**同步面无解锁面那样的降级声明**。
  ⑤ `strings.xml:165 detail_passkey_chip`「FIDO2 硬件芯片保护」（`EntryDetailCards.kt:215`）与 `:212`「已绑定 Passkey 硬件凭据」（`EntryEditComponents.kt:141`）：私钥实为库内软件字段（`PasskeyKeyGeneration.kt:53/98/146` 走 BouncyCastle）+ 自证明 `fmt="none"`。
- **背景与根因**：属本项目明确反对的「界面谎报」族（先例 `ISSUE-P1-241` / `ISSUE-P3-65` / `ISSUE-P2-228` / `ISSUE-P0-02`），且本族**全为安全语义**——用户会据此形成「硬件隔离」「不留明文」的错误认知。限界表与 `产品裁决登记.md` 检索 `SECURITY_LEVEL_SOFTWARE|Keystore` 仅 2 处无关命中 ⇒ 未登记。
- **涉及文件**：上列 5 个渲染点 + `res/values/strings.xml` 与 `res/values-en/strings.xml` 对应键；真值来源 `KeystoreKeyMaterial` / `BiometricEnrollmentCoordinator` / `FileBinaryStore`。
- **验收标准**：AC① 硬件类声明改为**按实测安全等级条件渲染**（判据落单一函数，TEE/StrongBox 与软件级两套文案，中英成对），禁硬编码；AC② 同步面补齐与解锁面同形的软件级降级声明；AC③ `sec_arch_desc` 的「任何明文数据都不会写入磁盘」须如实改写（大附件明文落私有目录 + 用户可触发的明文导出两项事实都要反映），或按 AC⑤ 处置后改写；AC④ passkey 徽标与文案去「芯片 / 硬件」表述，如实写「软件密钥（库内加密存储）」；AC⑤ 对「附件明文落盘」单独裁定：加密落盘（则 ② 前半句可保留）或维持现状并如实措辞，结论登记 `PD-*`；AC⑥ 新增接线守卫用例，锁定「文案分支跟随实测等级」（沿用 `AutofillChannelSwitchWiringTest` 的静态消费点计数口径），禁「有真值字段但无生产赋值」复现。

**开工前前提复核（2026-09-23）**：成立。五处渲染点与文案逐字一致；`hardwareBackedSecurity` 仍无生产赋值；真值通道复核——`KeystoreManager.getKeySecurityLevel`（`KeyInfo.securityLevel` 探测）与 P1-22 实测回填的 `quickUnlockDowngradeAcknowledged` / `quickUnlockDowngraded` 均在位，本批判据直接复用，未新建第二套等级判定。

---

## 2. 整改

### 2.1 AC①：三处硬件声明按实测条件渲染（判据复用既有单一通道）

| 渲染点 | 实测判据 | 双文案（zh + en 成对） |
|---|---|---|
| 解锁页 QUICK_UNLOCK 副标题 | `quickUnlockDowngraded`（P1-22 实测回填） | `unlock_quick_subtitle` / 新增 `unlock_quick_subtitle_software` |
| 安全设置生物识别副标题 | `quickUnlockDowngradeAcknowledged`（P1-22 实测登记） | `sec_biometric_sub` / 新增 `sec_biometric_sub_software` |
| 同步凭据封印声明（ZeroKnowledgeCard） | 新增链路：`SyncCredentialsStore.syncSealSecurityLevel()`（`KeyInfo.securityLevel` 探测）→ `SettingsSyncController.syncSealHardwareBacked`（初始 + 保存 / 恢复时刷新）→ 投影 → `SettingsUiState` → `ZeroKnowledgeCard(sealHardwareBacked=...)`（**无默认值**，禁谎报） | `sync_credential_auth_notice`（措辞同步更正为「本机实测支持硬件隔离」）/ 新增 `sync_credential_auth_notice_software`（AC② 与解锁面同形的降级声明） |

**死字段清账**：`UnlockUiState.hardwareBackedSecurity`（唯一赋值在 `@Preview`）**删除**——字段、预览赋值、渲染行、`unlock_hardware_backed_security` 字符串（中英）一并移除（AC⑥ 守卫锁定禁复活）。

### 2.2 AC③：`sec_arch_desc` 如实改写

新文案（中英成对）如实包含三项事实：库文件始终密文存储；主密钥经 Android Keystore 封存、**支持的设备上**启用 TEE/StrongBox（以实测为准）；**大附件在应用私有目录以明文暂存**、**明文导出文件由用户显式确认后产生**。

### 2.3 AC④：passkey 文案去硬件表述

`detail_passkey_chip`「FIDO2 硬件芯片保护」→「FIDO2 软件密钥（库内加密存储）」；`edit_passkey_has_bound`「已绑定 Passkey 硬件凭据」→「已绑定 Passkey 凭据」（中英成对）。

### 2.4 AC⑤：附件明文落盘裁定

登记 **`产品裁决登记.md` PD-36**：维持现状（私有目录 0600/0700 沙箱边界内 + 锁库即清空）+ 文案如实声明（§2.2 已落地）；加密落盘属独立特性，重开条件随条登记。

---

## 3. 验证

### 3.1 AC⑥ 接线守卫（新增 `SecurityClaimWiringTest`，6 例）

死字段禁复现（`hardwareBackedSecurity` 不存在）；解锁副标题 / 生物识别副标题 / 同步封印声明三处的「实测字段 + 软件文案键」成对引用；同步面实测链路 `syncSealSecurityLevel` → `probeSealHardwareBacked` → 投影 → `SettingsUiState` 逐环接通；三个软件文案键中英成对存在。

### 3.2 全量回归与门禁

`.\gradlew.bat test --rerun-tasks --max-workers=1` **BUILD SUCCESSFUL**，`xml=388 tests=2637 failures=0 errors=0 skipped=13`（`count_test_results.py` 现跑；基线 §291 `tests=2631` ⇒ **+6**）；`:app:compileDebugScreenshotTestKotlin --rerun` 绿（本批改了 `UnlockQuickUnlockCardPreview`）。

## 4. 如实声明

- 未跑 `lint` / 真机：实测等级分支（TEE/StrongBox/软件）在真机上的呈现差异由既有 P1-22 通道承载，本批未新增实测路径，仅把既有真值接到文案分支；**软件级文案的真机核验建议**（非阻断）：在软件级 Keystore 模拟器上目视三处降级文案。
- `sec_arch_desc` 为静态文案（非条件渲染）：其「以实测为准」表述指向解锁页 / 设置页的降级声明实例，符合 AC③「如实改写」。
- passkey 详情页 `detail_passkey_desc` 等相邻文案未含硬件表述（复核过渲染点），本批只改被指出的两处。
