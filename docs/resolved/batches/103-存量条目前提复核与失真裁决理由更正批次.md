<a id="s103"></a>
## §103 存量条目前提复核与失真裁决理由更正批次（2026-09-16，docs）：无既有 ISSUE 编号（工作流自身产出的前提复核）

> **动机**：`ACTIVE_ISSUES.md` 条目维护规则（`ISSUE-P3-28`）第 1/2 条要求「新增条目必须附核实时间点与核实方式」
> 且「开工前复核前提，前提已不成立的就地修正或显式标注」。立规缘由是**顺向**失效（条目前提早已不成立，
> 致执行者去做已完成的工作）；本批暴露的是**逆向同源失效**——**据不成立的前提做出的用户裁决，其理由本身是失真的**。
> 三条「已裁决暂缓」条目（`ISSUE-P2-47` / `ISSUE-P2-79` / `ISSUE-P3-121`）均由规则第 1 条要求附「核实时间点 +
> 核实方式」，故在重新认领前必须再做一次前提复核。本次复核基线 **HEAD `2e5ce38`**、时间点 **2026-09-16 21:37 +0800**。

### 103.1 交付

1. **新建** [`docs/records/存量条目前提复核记录.md`](../../records/存量条目前提复核记录.md)（432 行）：三条目逐一给出
   「前提原文 → 取证（`文件:行号` / `git blame` / `git show`）→ 判定 → 可直接粘贴的正文修正文字」，
   并单列「据失真前提做出的裁决」「新发现」「不确定性（如实区分已核实/未能核实）」。
2. **主控落实的文档修正**（本批同一批内处置，避免「改写措辞」脱离批次）：
   - `docs/ACTIVE_ISSUES.md` —— `ISSUE-P2-47` AC① 的「`SyncEngine` 三处裁决点」更正为**全部重放裁决点 5 处**
     （`:108` / `:173` / `:215` / `:236` / `:277`）；`ISSUE-P2-79` 第②项补「真实下界是官方 `MinMemory = 8192` 字节
     （`KdbxKdfParameterCodec.kt:90`），`m=1 MiB` 只是示例而非下界」、第四轮定版口径补「`max` 须**逐参数**施加」；
     `ISSUE-P3-121` 替换核实行（补全仓 5 处代码命中与**零生产写入点**）并**追加「裁决理由前提更正（须用户重新裁决）」**。
   - `docs/architecture/已知工程限界.md` §5 —— 同一句「三处裁决点」同步更正为 5 处（该文件顶部「措辞纪律」规定：
     **改写措辞等同于变更结论，须走一次完整批次**，本批即该批次）。
   - `docs/README.md` —— 登记本批新文档到 `records/` 分区。

### 103.2 判定结论

| 条号 | 正文前提 | 裁决理由 | 关键证据 |
|---|---|---|---|
| `ISSUE-P3-121` | **仍成立** | **已失准（理由失真）** | 默认空集 `SyncNetworkOptions.kt:25`、两处默认构造 `SyncProviderResolver.kt:52,77`、内网恒拒均成立；但「可经既有显式白名单机制自行配置」**不成立** |
| `ISSUE-P2-79` | **仍成立**（两处措辞精度待修） | 成立 | 建库默认 `KdbxHeader.kt:168-170`；`KdfBenchmark` 生产消费方唯一是 `SettingsKdfBenchmarkController.kt:39`（仅展示）；弱参数照收且保存原样保留（`KdbxFile.kt:332,337`） |
| `ISSUE-P2-47` | **部分失准**（仅「三处裁决点」计数） | 成立 | 摘要链为本地记录（`DatabaseModule.kt:84-85`）、`recent` → `ReplayDetected`（`SyncRollbackGuard.kt:124`）成立；裁决点实为 **5 处** |

### 103.3 「据失真前提做出的裁决」（须用户重新裁决，未由实施者择一）

**唯一失真项：`ISSUE-P3-121` 的 2026-09-16 用户裁决理由**。理由称「需要内网同步的用户可经**既有显式白名单机制
自行配置**」，而 `ssrfAllowedHosts` 仅是 `sync` 模块的**库级构造参数**：全仓 5 处命中（定义 `SyncNetworkOptions.kt:25`；
消费 `SyncHttpClientFactory.kt:35`、`S3SyncProvider.kt:105`、`WebDavSyncProvider.kt:80`；注释 `DalVerifierModule.kt:64`），
**无 UI 入口、无持久化键、无 DI 装配点**——`SyncNetworkOptions(` 的构造点仅 `SyncProviderResolver.kt:52,77`（默认）与
`DalVerifierModule.kt:73`（未置该字段），唯一非默认赋值在单测 `SyncEndpointGuardTest.kt:179`；
`SyncCredentialsStore.kt:299-318` 键枚举与 `strings.xml` 均无对应入口；归档文档 `batches/86-DAL出口纵深防御批次.md:17`
亦自述该通道为「**预留**」。

⇒ 该裁决的实际语义是「**内网 / 自建同步对终端用户彻底不可用且无任何自配路径**」，而非「默认收紧但可自配」。
影响面：产品定位、`README.md:8` 文案（即 `ISSUE-P3-121` 的 AC③）、是否排期原 AC②。
**需用户回答**：① 是否维持「不支持自建 / 内网端点」？若维持，是否同批收窄 `README.md:8` 的
Nextcloud / ownCloud / MinIO 表述并在「已知局限」段登记，使 AC③ 闭环？② 是否排期原 AC② 的受控逃生通道；
若不排期，是否把 `ssrfAllowedHosts` 显式登记为「预留未接线参数」并收窄 `SyncNetworkOptions.kt:21-23` 的 KDoc？

> **`ISSUE-P2-79` / `ISSUE-P2-47` 的裁决理由未发现失真**（后者仅计数低估，不改变「AC① 实质已具备」的结论）。

### 103.4 新发现（P3 / INFO 级措辞问题，非代码缺陷）

- **D-1**：`SyncNetworkOptions.kt:21-23` / `SyncEndpointGuard.kt:29-30` 的 KDoc 声称存在「经上层放行」的豁免通道，
  该「上层」在仓内**不存在**（与 103.3 同源）。
- **D-2**：`batches/86-…:17` 与 `DalVerifierModule.kt:64-65` 的「预留通道」措辞易被读作**已可用**。

两项均**无反向偏差**（不存在「防护弱于文档」的缺陷）：① fail-closed；② 属已登记开放项；③ 防线**强于**文档描述。
是否收窄上述 KDoc 与 `ISSUE-P3-121` 的产品口径裁决**同批决定**（见 103.3）。

### 103.5 验证证据

- **主控独立复核（不照抄子代理结论）**：`grep -n "isReplay" sync/.../SyncEngine.kt` → 6 处命中 = 1 处定义（`:29`）
  + **5 处调用（`:108` / `:173` / `:215` / `:236` / `:277`）**，与「原文『三处』漏计后两处」完全吻合；
  `SyncCache.kt` / `SyncEngine.kt` 的 `isReplay` 调用点逐行直读确认「均 `return ... RollbackRejected`、不写缓存与基线」。
- **审计一致性闸门**：`bash tools/audit/check_recheck_consistency.sh` → `PASS: 无残留禁用短语（已扫描 1289 行，11 条禁用短语）`。
- **行号漂移实测（本批附带留痕）**：报告 §6.8 `IPC-02` 行写的落点 `PasskeyAssertionActivity.kt:112`，本次直读实测在
  **`:144`**（另 `:78` 有同型表述）；`B09-findings.md:1571` 写的 `endpointOverride` 在 `DigitalAssetLinksVerifier.kt:53-59`，
  整改后该字段已消失（现为 `:45-54` 的只读策略注入）。⇒ 再次印证「行号一律视为核实时刻的快照」。

### 103.6 边界与残余（如实声明）

- **未运行 Gradle**：对 `ISSUE-P2-47` AC③「回归用例仍存在且未放宽」的判定为**直读源码**所得，**未实证当前全绿**。
- **未实测**：`KdfBenchmark` 在真机上的实际推荐值区间仅由 `coerceIn` 边界静态推得，未在设备上实测；
  `deviceAvailableMemoryBytes()` 取 `memoryClass` 与形参文档「可用物理内存」存在语义差（未处置）。
- **未穷举**：`docs/` 全量用户可见文案中的内网承诺只检索了 `README.md`。
- **待用户裁决两项**：见 103.3（未由实施者择一，故 `ISSUE-P3-121` **仍留表**）。

---

---

## 归档索引行原文（无损迁移承接）

> 迁移前本批次的正文同时存在于三处：总索引行、分册级索引行、本文件。以下按「只搬迁、不改写」
> 原文照录两处索引行正文（更正须另加小节，不得就地改），自此两处索引只留一行指针。
> 承接批次：§154。

### 总索引行原文（§103）

存量条目前提复核与失真裁决理由更正批次（**工作流自身产出的前提复核**，无既有 ISSUE 编号，同轮处置）：对 `ACTIVE_ISSUES.md` 三条「已裁决暂缓」条目（`P2-47` / `P2-79` / `P3-121`）做**逆向同源失效**复核——条目前提**成立**，但**据以做出的用户裁决理由失真**。新建 [`docs/records/存量条目前提复核记录.md`](../../records/存量条目前提复核记录.md)（432 行，逐条 `文件:行号` + `git blame`）。三项结论：① `ISSUE-P2-47` AC① 的「`SyncEngine` 三处裁决点」实为 **5 处**（`:108`/`:173`/`:215`/`:236`/`:277`；`:236` 早于批注写就日即已存在）——主控以 `grep -n isReplay` 独立复核（6 命中 = 1 定义 + 5 调用）**逐字吻合**；② `ISSUE-P2-79` 的「`m=1 MiB`」**不是真实下界**（真实下界为官方 `MinMemory = 8192` 字节，`KdbxKdfParameterCodec.kt:90`），第四轮「`max(建议值,现默认)`」口径**须逐参数施加**（否则仍漏 `t=1` 降强）；③ **`ISSUE-P3-121` 的 2026-09-16 裁决理由失真**——理由称「可经既有显式白名单机制自行配置」，而 `ssrfAllowedHosts` 全仓 **零生产写入点**（无 UI / 无持久化键 / 无 DI 装配点；唯一非默认赋值在单测），归档文档自述该通道为「**预留**」⇒ 裁决实际语义是「内网 / 自建同步对终端用户**彻底不可用**」。**须用户重新裁决**（是否维持不支持自建并同批收窄 `README.md:8`；是否排期受控逃生通道）。另出两项 P3(INFO) 措辞问题 D-1/D-2（KDoc 声称的「上层豁免通道」仓内不存在）。**修正落点**：`ACTIVE_ISSUES.md` 五处 + `docs/architecture/已知工程限界.md` §5（「三处裁决点」→ 5 处；该文件「措辞纪律」要求改写措辞须走完整批次，本批即该批次）。验证：审计一致性闸门 `PASS`（1289 行 / 11 条短语）。**边界如实声明**：未跑 Gradle（`ISSUE-P2-47` AC③ 用例存在性为直读源码判定）；`KdfBenchmark` 真机推荐值未实测
