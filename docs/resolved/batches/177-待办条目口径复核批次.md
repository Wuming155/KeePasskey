# §177 待办条目口径复核批次（`ISSUE-P3-188` 文档面：数法收敛 + 复核脚本 + 分流欠账登记）

> **起因**：用户命题「修复存量问题 / **精简文档**」。§176 提交后通读 `docs/ACTIVE_ISSUES.md` 的
> `ISSUE-P3-188` 条目全文，除 §176 §5 已就地修掉的 4 处外，又核出 **4 处口径问题**，
> 其中 1 处是**数字与现状不符**（不是风格问题）。本批只动文档与新增一个复核脚本，**零生产代码改动**。

---

## 1. 核出并修正的四处

| # | 问题 | 修法 |
|---|---|---|
| ① | **同一事实三处数法**：第一档 8 文件的逐文件行数在「违例清单第 1 目」「验收标准」「当前进度」各写一份（§176 已把验收标准那份并掉），三份之间已出现 `369` vs `370` 的分叉 | 「违例清单第 1 目」改为**指针**（只留「超 500 仅余 2 个」这一条本目专属结论 + 指向「当前进度」），逐文件行数的**唯一权威处 = 当前进度** |
| ② | **数字过期**：`DatabaseSettingsScreen` 现值 **370**，条目仍写 369（§159 降到的 369 被后续批次回填了 1 行） | 当前进度改 370 并括注来源，避免被读成「§159 少算一行」 |
| ③ | **漏记两批**：「已消化 11 个」未含 §175 `HealthCheckScreen` 406→299 与 §176 `SettingsPreferencesController` 446→379 | 改为「**已消化 13 个**」并补两条，留痕区间 `159`~`174` → `159`~`176` |
| ④ | **复核不可复现**：第二档计数每批用一次性 `find \| wc -l` 手写命令，「400 与 500 是否含两端」散落在各批正文里 | 新增 `tools/doc/count_line_tiers.py`，一次输出两档清单与计数；第 2 项口径行明写「**400 与 500 两端皆含**」并指向该脚本 |

## 2. 新登记的欠账（剩余清单第 6 项）

「违例清单第 3 目」仍是核实时刻 20 处 ≥100 行函数的**原文快照**，而其结论已是「非 Compose 逻辑函数清零」
⇒ 属「应分流后压缩」的 historical list。**本批刻意不删**：实测 `grep -rl` 证明
`mergeConflictedEntry` / `saveEntryInternal` / `processFillRequest` / `startCreation` /
`buildPasskeyEntries` / `analyzeEntries` **六处在 `docs/resolved/` 内零命中**，删掉即失去「已审范围」的
唯一文字记录（与 §147「测试资产不得因暂时用不上而删除」同源）。已按规则 6.1 **当场登记**为
剩余清单第 6 项（附「先补留痕、再压缩」的顺序），而非只写在聊天或本批次文档里。

## 3. 关于「精简 = 减行数」的口径澄清

本批 `ACTIVE_ISSUES.md` 为 **+21 / −12，净 +9 行**（299 → 308）。净增**全部来自新登记的第 6 项待办（+9）**；
其余口径改动 **+12 / −12 持平**——删掉的重复明细换成了「脚本指针 + 含两端的口径行 + 370 的来源括注」，
不是单纯删行。**「精简文档」的正确度量是「同一事实只有一处权威表述、且每处可复核」**，
不是行数单调下降——把新发现的欠账压进批次正文而让待办条目变短，恰恰违反规则 6.1。

## 4. 验证

| 项 | 结果 |
|---|---|
| `python tools/doc/count_line_tiers.py` | `files_scanned=500`、**tier1(>500)=2**（`SettingsViewModel` 543 / `DatabaseSession` 535）、**tier2(400~500)=29** —— 与 §176 手写命令口径逐数一致 |
| 已消化 13 个文件逐个 `wc -l` | 全部 ≤400（最小 267、最大 397），「达标」表述与现状相符 |
| `.\gradlew.bat test --rerun-tasks --max-workers=1` | `BUILD SUCCESSFUL in 3m 9s` / `114 actionable tasks: 114 executed`；聚合 **tests=2198 failures=0 errors=0 skipped=13**（312 份 `testDebugUnitTest` 的 `TEST-*.xml`），`Uncaught exception` 命中文件 **0** —— 与 §176 逐项相同（本批零 `.kt` 改动，故应完全一致，**实测确认而非推定**） |
| **未执行** | `:app:lintDebug`（本批无任何 `.kt` 生产改动，§176 刚测得 216 与基线持平）、`cargo test`、`test -DliveSyncTest`、四层 `connectedDebugAndroidTest`（无设备） |

## 5. 下一批

第 3 目剩余清单第 1 项：`CreateVaultWizardDialog` 234 / `OpenExistingVaultDialog` 226（§175 表内次长两个）
的段落拆分，或 `HealthCheckScreen` 剩余段落（§175 已开该文件的段落组件先例）；
两个 NavGraph 与 `KeePasskeyApp` 是否计入本目的**口径问题仍未裁决**，裁前不开拆。

**本批顺带核到的搬家前置事实**（省下一批的排查）：`OpenExistingVaultDialog.kt` 在全仓测试里
**无任何源码文本引用**（可安全分段）；而 `CreateVaultWizardDialog.kt` 被
`CreateVaultPresetTest` 以 `readSource(WIZARD)` **点名读文本** ⇒ 动它之前必须先看清该守卫锚在哪几行，
搬出的段落要按剩余清单第 3 项改成「门面 + 分支文件」**并集**扫描。
