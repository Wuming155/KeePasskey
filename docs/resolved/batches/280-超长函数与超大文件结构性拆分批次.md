# §280 超长函数与超大文件结构性拆分批次

> **类型**：工程卫生（非既有条目整改）；触发＝用户命题「拆 6 个超长函数与 3 个超大文件」。
> **性质**：纯结构性拆分（公开 API / 行为 / 敏感语义零变化）。

## 1. 范围与目标

开工量测（`count_line_tiers.py` / `long_functions.py`）：

| 指标 | 开工 | 收工 |
| --- | --- | --- |
| `functions_ge_100` | **6** | **0** |
| `tier1(>500)` | **3** | **0** |

### 1.1 六个超长函数（≥100 行）

| 函数 | 原行数 | 拆法 |
| --- | --- | --- |
| `KeePasskeySettingsNavGraph.keepasskeySettingsNavGraph` | 257 | 逐条 `composable` 注册体下沉 `KeePasskeySettingsNavGraphRoutes.kt`，门面只按序装配 |
| `KeePasskeyNavGraph.keepasskeyNavGraph` | 202 | 同上，下沉 `KeePasskeyNavGraphRoutes.kt` |
| `CreateVaultWizardDialog.CreateVaultWizardDialog` | 201 | 状态对象 `CreateVaultWizardState` + SAF 启动器 + 对话框壳 + 表单体 + 密码对五段私有化；主密码 `CharArray` 管线与离场擦除仍在门面 |
| `SyncConflictController.resolveConflicts` | 139 | 抽 `collectResolvedByParent` / `adoptMergedDatabase` / `handleResolveUploadSuperseded` |
| `SyncConflictController.autoMergeAndUpload` | 128 | 整函数 + `AutoMergeUploadResult` 下沉 `SyncConflictAutoMerge.kt` |
| `PasskeyRegistrationPayload.build` | 104 | 抽 `buildAttestationMaterials` / `buildClientDataJson` / `buildRegistrationJson` |

### 1.2 三个超大文件（>500 行）

| 文件 | 原行数 | 收工 | 拆法 |
| --- | --- | --- | --- |
| `SyncConflictController.kt` | 616 | **493** | 自动合并上传下沉 `SyncConflictAutoMerge.kt`；`resolveConflicts` 再抽三助手 |
| `SettingsViewModel.kt` | 550 | **495** | 导入/子库装配下沉 `SettingsFeatureControllers`；生物识别门下沉 `SettingsBiometricGate`；多行单表达式门面压单行 |
| `DatabaseSession.kt` | 536 | **490** | 观察者注册下沉 `SessionLockRegistry`；外来库解析下沉 `SessionExternalParser` |

## 2. 新增 / 改动文件

| 路径 | 说明 |
| --- | --- |
| `app/.../ui/KeePasskeyNavGraphRoutes.kt` | **新建**：一级路由逐条注册体 |
| `app/.../ui/KeePasskeySettingsNavGraphRoutes.kt` | **新建**：二级设置页逐条注册体 |
| `app/.../sync/SyncConflictAutoMerge.kt` | **新建**：`autoMergeAndUpload` + `AutoMergeUploadResult` + `conflictUploadExpectedEtag` |
| `app/.../settings/SettingsBiometricGate.kt` | **新建**：生物识别门 + `SettingsFeatureControllers` |
| `database/.../session/SessionLockRegistry.kt` | **新建**：会话锁观察者注册表 |
| `database/.../session/SessionExternalParser.kt` | **新建**：外来 KDBX 字节解析（凭据克隆 finally 清零语义不变） |
| 上述 6 个被拆函数所在文件 + 4 处源码守卫测试 | 见 §3 |

## 3. 源码守卫并集扫描（拆分伴生改动）

路由体迁出后，四份**按文件扫接线**的守卫须改为「门面 + Routes」并集（口径同 `CreateVaultPresetTest` §179 先例——**只放宽定位，不降低强度**）：

| 守卫 | 改动 |
| --- | --- |
| `AppNavigationMotionTest` | `KeePasskeyNavGraph.kt` ∪ `KeePasskeyNavGraphRoutes.kt` |
| `ThemePaletteMutualExclusionWiringTest` | `KeePasskeySettingsNavGraph.kt` ∪ `…Routes.kt` |
| `RuntimeIntegrityCheckSwitchTest` | 同上 |
| `OneTapInteractionWiringTest` | 同上；防空扫清单 17 → **18**（增列 Routes） |

**未放宽的红线**（逐字仍在原文件）：

- `SettingsViewModel`：`verifyConnectionThenSync` / `refreshPrivilegedBrowsers` / `enableBreachCheckAndScan` / `privilegedBrowsers` get / `setBreachCheckEnabled` lambda / `setIntegrityCheckEnabled`（PD-23 单语句委托）
- `KeePasskeyNavGraph`：`themeMode: AppThemeMode` 在、`appSettings: SettingsUiState` 不在
- `CreateVaultWizardDialog.kt`：2 处 `AlertDialog(` + 2 处 `SecureOn`、`isKeyFileValid && isLocationValid`、`storageLocation == … INTERNAL || selectedVaultUri.isNotBlank()`、`mutableStateOf(CreateVaultPreset.DEFAULT)`
- `SyncConflictController`：`applyResolvedEntriesToGroup(` 在、`applyResolvedEntryToGroup(` 不在
- `DatabaseSession`：3 处 `val buffer = WipableByteArrayOutputStream()`、`keyFileSnapshot?.fill(0)` 晚于 `changeCredentials(newPasswordChars, keyFileSnapshot)`、双参 `changeCredentials` 无默认值

## 4. 验证读数

- `.\gradlew.bat test --rerun-tasks --max-workers=1` → **BUILD SUCCESSFUL in 2m 32s / 114 executed**
- `count_test_results.py` = **`xml=374 tests=2590 failures=0 errors=0 skipped=13`**
  （与 §278 / §279 **三项全持平**——纯结构拆分，零测试增减）
- `count_line_tiers.py`：`tier1=0`；`SyncConflictController` 493 / `SettingsViewModel` 495 / `DatabaseSession` 490
- `long_functions.py`：`functions_ge_100=0`
- `check_md_links.py` → `BROKEN_MD_LINKS=0`
- `check_resolved_index_sync.py` → `OK`（批次正文 278 份；分册登记 280 条；最大 §280）
- `check_tautological_assertions.py` → 命中 0

**过程留痕（已登记偶发红）**：全量首跑红 1 例 `SyncCacheEvictorTest > F23 锁库清理缓存不得删除防回滚状态文件`——该用例 KDoc 自陈**已登记偶发红**（Windows/NTFS 目录枚举鬼影，限界 §7 / `records/宿主开发环境现象.md`），与本批改动面无交集；定向复跑 + 全量复跑均绿。

## 5. 如实声明

- **纯结构拆分**：公开 API 形状、行为语义、敏感擦除次序与所有权契约零变化；未改任何判定逻辑。
- 未触 `crypto/src/main/rust/**` / `*/src/androidTest/**` / `参考项目/` / 构建脚本 ⇒ **无设备侧必跑项**。
- 未跑 `lint` / `assembleRelease` / 截图门禁 / KPEX 对拍 / 真机。
- 4 处源码守卫改并集扫描是**伴生必要改动**（否则路由体迁出即红），判据强度未降、防空扫清单已增列。
- `tier2(400~500)` 仍有 36 个文件（含拆后的 `CreateVaultWizardDialog` 400），属既有债务，本批只收 `tier1` 与 ≥100 行函数。
