<a id="s270"></a>

# §270 回收站开关真实落库批次

> `ISSUE-P2-270` **整条闭环**（P2 2 → **1 项**）。
> 触发＝2026-09-23 全仓假开关清点（同日新登 P2-270 / P2-271 / P3-272 ~ P3-274 五项），本条为 P2 区首项整改。
> 产物互操作对拍经官方实现端到端达成（keepassxc-cli 2.7.12）。

---

## 1. 条目正文（原样收录）

### `ISSUE-P2-270`：回收站开关为假开关——`setRecycleBinEnabled` 只改 UI 回显，不写库 Meta，与删除语义相反

- **核实时间点**：2026-09-23 经全仓定向核对（设置页控制器 / 回收站协调器 / UI 投影 / 开关行四处）核实。
- **核实方式**：① 写侧 `setRecycleBinEnabled`（`SettingsPreferencesController.kt:204-206`）只做 `databaseConfigStateFlow.update { it.copy(recycleBinEnabled = enabled) }`——**无** `databaseSession.updateDatabaseMeta`、**无** `save()`；同文件 `setArgon2Parameters`（`:181-202`）则经 `updateDatabaseMeta` + `session.save()` 真实落盘，构成同文件内的直接对照。② 读侧删除分流全部读**库 Meta 真值** `db.recycleBinEnabled`（`RecycleBinCoordinator.kt:53` 单条 / `:105` 分组 / `:214` 批量），与 UI 开关**无任何数据通路**。③ UI 绑定 `uiState.recycleBinEnabled`（`DatabaseSettingsComponents.kt:215-220`），投影取自 `dbState.recycleBinEnabled`（`SettingsUiStateProjection.kt:106`）；而 `init` 订阅会话 `databaseFlow` 时的回填**刻意跳过**该字段（`SettingsPreferencesController.kt:143-154` 仅 `copy` 八项），故开关值只活在内存 `MutableStateFlow`（同文件 `:58`，缺省 `true`），冷启动即回 `true`。④ 真值映射其实已具备——`SettingsUiStateProjection.kt:270` 的 `databaseConfigFromHeader` 已读 `db.recycleBinEnabled`，只是回填通道未采纳。
- **背景与根因**：属「密码库 Meta 未回写」类。用户关闭开关后界面显示「已关闭」，而删除仍走软删移入回收站——与「关闭回收站＝删除即永久」的界面声明**方向相反**；用户据此认为敏感条目已彻底删除，实际条目仍在库内并随同步外发。同时因回填跳过该字段，库重载后「界面值」与「文件头真值」仍会分叉，形成**显示漂移**。项目对假开关已有明确反对口径（`ISSUE-P3-65` / `ISSUE-P2-228` / `ISSUE-P0-02`）。
- **涉及文件**：`app/src/main/java/com/keepasskey/app/ui/screens/settings/SettingsPreferencesController.kt`（setter 与 `init` 回填）、`app/src/main/java/com/keepasskey/app/ui/screens/settings/SettingsUiStateProjection.kt`（投影与 `databaseConfigFromHeader`）、`app/src/main/java/com/keepasskey/app/ui/screens/settings/subscreens/DatabaseSettingsComponents.kt`（开关行）、`app/src/main/java/com/keepasskey/app/data/repository/RecycleBinCoordinator.kt`（唯一读真值处）、`database/src/main/java/com/keepasskey/database/session/DatabaseSession.kt`（`updateDatabaseMeta` / `save`）。
- **验收标准**：AC① 开关经 `updateDatabaseMeta { it.copy(recycleBinEnabled = ...) }` 写入 `KdbxDatabase.recycleBinEnabled` 并立即 `save()`，口径与 `setArgon2Parameters` 一致——该字段在写盘时经 `KdbxXmlSerializer.kt:13` 的 `KdbxDatabase.toMetaData()` 还原为 `KdbxMetaData.recycleBinEnabled` 并写出 `<RecycleBinEnabled>` 元素（读侧 `KdbxFile.kt:296` 反向装配），故闭环真实成立；关闭后单条 / 分组 / 批量删除**一律物理删除并写入墓碑**，不再移入回收站；AC② 回显改为**单一真相源**（取自会话 `databaseFlow` 的库 Meta，经 `databaseConfigFromHeader` 回填），消除显示漂移，重启后回显用户上次所设；AC③ 无活动会话 / 会话未就绪时如实 no-op 或置禁用态（保持文件头真值，不产生假变更），不得谎报成功；AC④ 新增接线守卫用例，同时锁定「setter 必下发到会话 Meta」与「删除分流读库 Meta」两侧，禁「UI 有开关、库无变化」复现；AC⑤ 互操作核验：关闭 / 开启后写出的 `RecycleBinEnabled` 元素与官方 KeePass / KeePassXC 行为一致（触及外层格式面时按 `AGENTS.md` 规则 8 对拍）。

> **开工前前提复核（2026-09-23）**：条目四处行号与前提逐一属实，无漂移、无勘误——
> setter 仍只改内存回显、init 回填仍跳过该字段、删除分流三处仍读库 Meta 真值、
> `databaseConfigFromHeader` 的真值映射已具备且未被回填通道采纳。

---

## 2. 整改

### 2.1 写侧真实落库（AC① / AC③）

`SettingsPreferencesController.setRecycleBinEnabled`：与 `setArgon2Parameters` 同口径——

```kotlin
fun setRecycleBinEnabled(enabled: Boolean) {
    val session = databaseSession ?: return   // 无活动会话时如实 no-op（AC③）
    scope.launch {
        session.updateDatabaseMeta { db -> db.copy(recycleBinEnabled = enabled) }
        session.save()
    }
}
```

- **不再回写 `databaseConfigStateFlow`**：回显不自持状态，改由会话 `databaseFlow` 重发统一
  下发（`SessionContentMutations.updateDatabaseMeta` 内 `databaseFlow.value = updated`）；
- `save()` 落盘时经 `KdbxDatabase.toMetaData()` → `KdbxXmlMetaSerializer` 的
  `boolElement(writer, RECYCLE_BIN_ENABLED, meta.recycleBinEnabled)` 写出 `<RecycleBinEnabled>`
  （`True` / `False` 大小写与官方约定一致）。

### 2.2 回填通道补齐字段（AC②）

`init` 订阅 `databaseSession.databaseFlow` 的回填块由「仅 `copy` 八项」补入
`recycleBinEnabled = header.recycleBinEnabled`（取自 `databaseConfigFromHeader` 的库 Meta 真值映射，
该映射本已具备，条目前提④ 所指）；`DatabaseConfigUiState` 的一次性占位初值 `true` 保留
（会话就绪前空态，另加注释说明「真实值由 init 回填通道统一下发」）。

### 2.3 读侧未动（本就正确）

`RecycleBinCoordinator.kt:53`（单条）/ `:105`（分组）/ `:214`（批量）的
`binRelated || !db.recycleBinEnabled` 物理删除分支一行未触——假开关修复后，删除分流自动
与 UI 开关建立数据通路，无需改动。

---

## 3. 验证

### 3.1 接线守卫用例（AC④）

**新增 `app/src/test/.../settings/RecycleBinToggleApplyTest.kt`**（4 例；手法与
`Argon2ParametersApplyTest` 同——真实 `CoroutineScope` + 真实时间轮询，真实建库 / 落盘 / 重开）：

1. `关闭回收站后库 Meta 置 false 落盘重开读回且回显随库真值下发`——锁定「setter → 会话 Meta →
   落盘 → 重开读回 false」与「回显经 init 回填随库真值下发」（AC① + AC②）；
2. `重新开启回收站后写回 true 并落盘读回`——开关双向真实（AC①）；
3. `无活动库时开关如实 no-op 不产生假变更`——库保持未挂载、状态不得置 `DIRTY`、回显保持
   文件头真值（AC③）；
4. `产出 RecycleBinEnabled 为 false 的互操作对拍产物`——走真实开关链路产出
   `app/build/interop-probe/keepasskey-recycle-bin-off-probe.kdbx` + `RECYCLE_BIN_PROBE.md`
   （含 SHA-256 与外用验证命令），供官方实现外用对拍（AC⑤，手法同 `OwnProductInteropProbeTest`）。

**`RealVaultRepositoryTest` 新增 2 例**（锁定「删除分流读库 Meta」侧）：

- `回收站禁用时删除条目走物理删除并追加墓碑`——`recycleBinEnabled = false` 时普通条目删除
  走物理删除 + 墓碑，不得软删移入回收站；
- `回收站禁用时删除分组走物理删除并追加墓碑`——分组同口径（官方 KeePass 分组删除分流）。

### 3.2 全量单测（JVM）

`.\gradlew.bat test --rerun-tasks --max-workers=1` **BUILD SUCCESSFUL**；
`python tools/doc/count_test_results.py`：**xml=367 tests=2554 failures=0 errors=0 skipped=13**
（已排除非 JVM 单测 XML：debug=5 / updateDebugScreenshotTest=1，上一批遗留不随本次重跑）。

### 3.3 官方实现端到端对拍（AC⑤）

本机工具链（keepassxc-cli **2.7.12** / pykeepass **4.2.0**，2026-09-23 实测）：

| 对拍对象 | 命令 | 官方读数 |
|---|---|---|
| 本仓产物（经真实开关链路写入 `RecycleBinEnabled=false`） | `echo -n '<口令>' \| keepassxc-cli db-info -q <产物路径>` | **「回收站未启用。」** |
| 对照组（pykeepass `create_database` 自建库，该字段官方缺省 true） | 同上 | **「回收站已启用。」** |

即官方 KeePassXC 客户端对**同一** `<RecycleBinEnabled>` 元素的读数随本仓写入值双向翻转，
互操作证据成立（规则 8 口径）。pykeepass 4.2.0 不解析该元素（源码全量核对零命中），故 pykeepass
仅承担对照组建库角色，不承担读数角色。格式层读写对称（元素缺省 true 语义 / `True`/`False`
写出值）既有 `KdbxMetaXmlSemanticsTest` 锁定，本批未改格式面。

### 3.4 验证面如实声明

- 本批**未触及** `crypto/src/main/rust/**`、JNI 绑定或任一 `Native*` 引擎，亦未新增
  `*/src/androidTest/**` 用例 ⇒ 无设备侧强制验证义务，未跑 `connectedDebugAndroidTest`；
- 被改面（设置页控制器 Meta 写入 / 库 Meta 读写往返）全部可在宿主 JVM 以真实文件端到端覆盖
  （`DatabaseSession.create/open/save` 真实落盘），无「只有真机才能证伪」的残余面。

---

## 4. 过程留痕

- 无过程缺陷：一次成型，定向用例与全量单测首轮全绿；
- 官方对拍阴性对照（pykeepass 自建库）是为证明 `keepassxc-cli db-info` 的回收站读数字段
  **确实区分两种状态**而补做，非条目要求；
- 产物与 `RECYCLE_BIN_PROBE.md` 落在 `build/`（可丢弃、不入库），由用例随时重跑再生。
