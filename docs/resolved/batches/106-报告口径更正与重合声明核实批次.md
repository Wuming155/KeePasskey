<a id="s106"></a>
## §106 报告「待复核区已逐条补齐」口径更正与重合声明逐条核实批次（2026-09-16，docs）：ISSUE-P3-145

> **批次正文体例**：本批**以** [`docs/security/待复核区IPC与SUPPLY项重合声明核实.md`](../../security/待复核区IPC与SUPPLY项重合声明核实.md)
> 为**唯一真相源**（362 行：9 项逐条判定 + `文件:行号`/commit 证据 + 2 处独立残余 + 边界 + 建议标注文字）。
> 本节只收录：条目原文（原样搬迁）、AC 逐项对照、残余落点与验证证据。

---

### 106.1 条目原文（自 `ACTIVE_ISSUES.md` 原样搬迁，**不改写**）

> ### ISSUE-P3-145：报告与待办对「待复核区已逐条补齐」的断言与实况不符（`IPC-*`/`SUPPLY-*` 19 项仅 10 项有逐条结论）
>
> - **优先级**：P3（可核对性 / 审计基线准确性——**不是技术漏洞**）
> - **当前状态**：**开放项**——`SECURITY_RECHECK_2026-09.md` §1 与 §2 声称待复核区已「逐条补齐」，但 §6.8 只有 9 行结论 + §6.6 的 `SUPPLY-05`，共覆盖 **10/19**。
> - **来源**：`ISSUE-P3-141` 收口时对 `IPC-*`/`SUPPLY-*` 处置归属做全表核定时发现（见 `docs/security/第四轮复核遗留编号处置.md` 第 2.2 节；条目归档正文见 `RESOLVED_LOG.md` §104.1）。核定时同时发现该编号族在 `.audit-recheck/` 内**以斜杠压缩组形式出现**（`IPC-01/02/03/05/06/08/11`、`SUPPLY-02/05/06/07/08` 等），字面对拍不可靠。
> - **核实时间点与核实方式（2026-09-16，对 HEAD `2e5ce38`）**：① 机器抽取报告 §6.8 表内编号 → 实测 9 个（`IPC-01/02/05/10`、`SUPPLY-02/04/06/07/08`）；② 全域范围取自报告 `:87` 自述引 `RESOLVED_LOG` §43.5（`IPC-01…IPC-11`、`SUPPLY-01…SUPPLY-08`，共 19）；③ 全文检索报告，确认 `IPC-03/04/06/07/08/09/11`、`SUPPLY-01/03` **在报告中无逐条真伪结论**（仅 `:87` 的范围记法命中）；④ 逐项确认其处置载体为「登记表重合声明 → 既有条目」，且声明本身按 `00-registry-and-plan.md:341` 原文**尚待复核**。
> - **问题描述**：
>   - 报告 §1（`:88`）写「本轮已逐条补齐（§6.8）」、§2（`:167`）写「10+ 条线索从未获结论（**本轮补上**）」，两处均为**口径过宽的断言**：`SUPPLY-05` 实际在 §6.6 裁定，另有 9 项（`IPC-03/04/06/07/08/09/11`、`SUPPLY-01/03`）**从未获得「成立 / 不成立」结论**；
>   - 这 9 项的处置载体是**第一轮自述的「重合声明」**（`IPC-03→P2-70`、`IPC-04→P3-88`、`IPC-06→P2-72`、`IPC-07→P2-51`、`IPC-11→P2-46`、`SUPPLY-01→P2-54`、`SUPPLY-03→P2-55`）与退役承接文档（`IPC-08/09` 于 `docs/security/退役审计承接-43-安全整改方案与附录.md:118`）。**对应条目确实有逐条裁决**（报告 §3 Finding Matrix），故**实质面不算漏报**；漏的是「**声明被验证**」与「**该编号自身有结论**」两层，与 `SYS-2`（「待复核区被归并替代复核」）**同型**——报告自己把 `SYS-2` 列为已修，而实际只修了 10/19。
>   - `ISSUE-P3-141` 归档正文（`RESOLVED_LOG.md` §104.1）中「报告 §6.8 只逐条裁定 `IPC-*`/`SUPPLY-*`」一句同样过宽，应改为「§6.8 裁定 9 项 + §6.6 裁定 `SUPPLY-05`；其余 9 项以**未经验证的重合声明**落到既有条目」。
> - **验收标准**：① 更正报告 §1（`:88`）与 §2（`:167`）的口径，明确「`IPC-*`/`SUPPLY-*` 共 19 项，其中 10 项已逐条裁定、9 项为重合归并（附归并去向）」——**不得**保留「已逐条补齐」的无条件表述（`tools/audit/check_recheck_consistency.sh` 须保持 PASS）。② 对 9 项**逐条**给出「重合声明成立 / 不成立」结论：其中 7 项指向的既有条目均已闭环（`P2-70` §53、`P3-88` §65、`P2-72` §48、`P2-51` §48、`P2-46` §75、`P2-54` §49、`P2-55` §49），复核重点是「该编号所述机制是否**确实**已被对应条目覆盖、有无独立残余」；`IPC-08/09` 复核其「产品裁决 / 设计边界」定性是否仍成立。③ 结论落档于本仓受跟踪文档（不得只留 `.audit-recheck/`）；④ 就地更新 `RESOLVED_LOG.md` §104.1 归档正文的引用口径（归档纪律「只搬迁不改写」⇒ 以**就地标注更正**方式处理，不改原句）。

---

### 106.2 验收标准逐项对照

| AC | 结论 | 落点 |
|:--:|---|---|
| ① 更正报告 §1 / §2 口径，不得保留「已逐条补齐」 | **完成**——报告**就地更正 4 处**：§1.2⑤（含标题「本轮首次补上」）、§2.5 `SYS-2` 行、§6.8.1（追加 2026-09-16 补注）、§15.2(j)（**超出 AC 明列两行**，理由见 106.4）；其余一字未动 | `SECURITY_RECHECK_2026-09.md` |
| ② 9 项逐条给出「重合声明成立 / 不成立」结论 | **完成**——判定 **7 项成立 / 2 项部分成立**（部分成立者：`IPC-06`、`IPC-11`）；每项附 `文件:行号` + commit | `待复核区IPC与SUPPLY项重合声明核实.md` §2 |
| ③ 结论落档于本仓受跟踪文档 | **完成**——新建受跟踪文档（362 行），并登记进 `docs/README.md` `security/` 分区 | 同上；`docs/README.md` |
| ④ 就地更新 §104.1 归档正文的引用口径 | **完成**——以**就地标注更正**方式在 §104.1 追加标注块（原句按归档纪律保留未改），并同批补正 `NEW-B02-3`「从未进入报告」的过宽表述 | `docs/resolved/batches/104-…md` §104.1 |

---

### 106.3 两项独立残余的落点（均**新增受跟踪落点**，非仅留痕）

| 残余 | 实质 | 落点 |
|---|---|---|
| **R-1**：`IPC-11` 族在 CM 通道的 `UNBOUND` 越权面 | 取舍**有理由**（CM 通道无选择器中立面；「首次填充即绑定」会把首个到来的调用方记成参照、致真应用永久失效），但此前**只在代码 KDoc 与 §82.2 声明**，本仓**唯一**的已接受限界登记表未登记——按登记纪律「**未登记即视为未处理缺陷**」 | 新增 `docs/architecture/已知工程限界.md` **§6**（事实 → 边界 → 解除条件 → 依据），并加编号提示消解与「**原 `AGENTS.md` §6**」的命名冲突 |
| **R-2**：`SUPPLY-01` 覆盖条目 `P2-54` 的 AC②（分支保护必需检查）无活动落点 | 条目随 §49 归档后，其未闭环 AC 在 `ACTIVE_ISSUES` / `已知工程限界` / `RESOLVED_LOG` **三处都无落点**；批次正文自述「本环境无权限（`gh` PAT 403）」且明写「**不因留痕而视为完成**」 | 登记为 **`ISSUE-P3-146`**（P3）；AC 内硬性写明：**不得**写成「仓库未受保护」（本环境无法从仓库侧核实），且只能由持管理员权限者核实环境设置、**不得**用改代码/跑测试伪闭环 |

---

### 106.4 越界说明（如实留痕）

AC① 明列的是报告 §1（`:88`）与 §2（`:167`）两行；实施时**另追加 §15.2(j)**（原 `:1148`）的括号口径补注。
**主控裁决：保留**——该处属**同一口径过宽**（同样把「`IPC-0x`/`SUPPLY-0x` 逐条结论」说成完备），不补则报告内部自相矛盾（§1 已更正而 §15.2 仍留旧口径）；
且该处仅**追加括号补注、未删改原句**，符合「只改过宽处」的边界。

---

### 106.5 验证证据

- **一致性闸门（主控独立复跑）**：`bash tools/audit/check_recheck_consistency.sh`
  改动前 `PASS`（1294 行）→ 改动后 **`PASS`（1308 行 / 11 条禁用短语）**，**未变红**。
- **主控独立抽查结论证据链**（不照抄子代理判定）：
  - `SUPPLY-03` 判「成立」——实测 `app/build.gradle.kts:39-60` 确有 fail-closed 构建期断言
    （口令长度 ≥ 16、命中黑名单即 `error(...)`，黑名单首项即历史公开值 `keepasskey123`），
    `keystore.properties.example:22,24` 已改为 `__REPLACE_WITH_HIGH_ENTROPY_PASSWORD__` ⇒ 证据链成立。
  - `IPC-04` 判「成立」——实测 `BrowserSigningFingerprints.kt:48-57` 四条指纹均为 **64 位大写 hex** ⇒ 与判定一致。
  - 报告更正文本：主控以 `git diff` 读取**被删除的原文**（4 行过宽表述）而非仅看新增，确认口径已由
    「已逐条补齐」改为「10 项逐条裁定 + 9 项重合归并（附去向）+ 7 成立 / 2 部分成立」。

---

### 106.6 边界与残余（如实声明，**不得**读作已核实）

1. **未做设备侧实测**（最高证据为 `[V]`）：`IPC-06` 的 RP 侧、`IPC-11` 的 CM 通道现场行为均未在真机验证。
2. **未能核实 GitHub 分支保护实际设置**（无管理员凭据）⇒ R-2 的表述严格限定为「无法从仓库侧核实 + 此前无活动跟踪项」。
3. `.audit-recheck/` 为未跟踪工作目录，行号是快照且**已实测漂移**（如 `PasskeyAssertionActivity.kt:112` → 实测 `:144`/`:78`/`:291`），仅作定位提示。
4. `keystore.properties` 的口令熵值**未验证**（刻意未读明文；仅统计历史公开值出现 0 次 + 断言存在）。
5. `IPC-08`/`IPC-09` 的「产品裁决」未找到独立裁决记录文件，依据为批次正文（§29.3 / §72.4）与代码 KDoc。

---

---

## 归档索引行原文（无损迁移承接）

> 迁移前本批次的正文同时存在于三处：总索引行、分册级索引行、本文件。以下按「只搬迁、不改写」
> 原文照录两处索引行正文（更正须另加小节，不得就地改），自此两处索引只留一行指针。
> 承接批次：§154。

### 总索引行原文（§106）

报告「待复核区已逐条补齐」口径更正与重合声明逐条核实批次（`ISSUE-P3-145` 闭环）：报告 §1.2⑤ 与 §2.5 `SYS-2` 行原称待复核区已「**逐条补齐**」，实为**口径过宽**——待复核区全域 **19 项**（`IPC-01…11`/`SUPPLY-01…08`）中仅 **10 项**有逐条真伪结论（§6.8 九项 + §6.6 的 `SUPPLY-05`），其余 **9 项**只有第一轮自述的「**重合声明**」（登记表 `00-registry-and-plan.md:341` 原文自注「须复核其声明是否正确」，**从未复核**），与报告自认已修的 `SYS-2` **同型**。本批：① 报告**就地更正 4 处**（§1.2⑤ 含标题、§2.5 `SYS-2` 行、§6.8.1 追加补注、**§15.2(j)** 追加括号补注——最后一处超出 AC 明列两行，主控裁决**保留**：同一口径不补则报告自相矛盾，且仅追加未删改原句）；② 对 9 项**逐条**给出结论（**7 项成立 / 2 项部分成立**：`IPC-06` 原句后半「未用请求 `clientDataHash`」无任何条目裁定、`IPC-11` 的 CM 通道 `UNBOUND` 面已由 §82 明示仍在），每项附 `文件:行号` + commit；③ 新建受跟踪文档 [`待复核区IPC与SUPPLY项重合声明核实.md`](../../security/待复核区IPC与SUPPLY项重合声明核实.md)（362 行）并登记文档地图；④ 以**就地标注更正**方式更新 §104.1 归档正文（原句按「只搬迁不改写」保留）。**两项独立残余均补上受跟踪落点**：R-1（`IPC-11` 族 CM 通道 `UNBOUND` 越权面——取舍有理由但此前**只在代码 KDoc 与批次正文声明**，按登记纪律「未登记即视为未处理缺陷」）→ 新增 `已知工程限界.md` **§6**；R-2（`SUPPLY-01` 覆盖条目 `P2-54` 的 AC② 分支保护必需检查未闭环且**三处文档均无落点**）→ 登记 `ISSUE-P3-146`。验证：一致性闸门改动前后均 `PASS`（1294 → 1308 行）；**主控独立抽查** `SUPPLY-03`（`app/build.gradle.kts:39-60` fail-closed 断言 + `keystore.properties.example` 占位符）与 `IPC-04`（`BrowserSigningFingerprints.kt:48-57` 四条 64 位大写 hex）证据链均成立。**边界**：未做设备侧实测（`IPC-06` RP 侧 / `IPC-11` CM 现场）；**未能核实** GitHub 分支保护实际设置（无管理员凭据）
