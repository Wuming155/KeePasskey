# §188 二级设置页共用骨架批次（`ISSUE-P3-188` 第 6 项首段：五页各 −21 行，附带一个 ≥100 函数出表）

> **起因**：§187 实测并登记的剩余清单第 6 项——`settings/` 系 11 处 / 241 行**逐字相同**的
> `Scaffold + TopAppBar` 骨架。本批做**零守卫引用的那五页**（§187 已实测 `grep` 结果为 0 处文本点名），
> 剩余六页都被接线守卫点名 ⇒ 留下一段并先做锚点核实。

---

## 1. 改动

新建 `subscreens/SettingsSubscreenScaffold.kt`（**70 行**，`internal`），五页各替换 25~27 行为 5~6 行：

| 页面 | 行数 | 剪除失效 import |
|---|---|---:|
| `AboutSettingsScreen.kt` | 125 → **104** | 8 |
| `ThemeSettingsScreen.kt` | 173 → **152** | 10 |
| `AutofillSettingsScreen.kt` | 252 → **231** | 10 |
| `PrivilegedBrowserSettingsScreen.kt` | 165 → **145** | 6 |
| `TotpSettingsScreen.kt` | 341 → **320** | 5 |

五页净 **−105 行**（含骨架 +70 ⇒ 全仓净 −35），但**重复从 5 份收敛为 1 份**；
后续六页每替换一页再省 ~18 行。

**附带收益**：`PrivilegedBrowserSettingsScreen` 函数 **115 → 95 行 ⇒ 出第 3 目表**
（≥100 项 24 → **23**，扣除 PD-11 豁免两个 NavGraph ⇒ **待拆 21 个**）。
其余四页的函数本来就不在表内，分档不变（第一档 2、第二档 28——这五页都 <400，
真正能压第二档的是 `DebugSettingsScreen` 443 / `SecuritySettingsScreen` 433 / `CloudSyncScreen` 427 三页，见 §4）。

## 2. 骨架刻意保留了哪些差异（不强行统一）

| 参数 | 为何以参数暴露 |
|---|---|
| `snackbarHost: @Composable () -> Unit = {}` | 五页中仅 `TotpSettingsScreen` 有 `SnackbarHost(snackbarHostState)`；默认空实现让其余四页行为不变 |
| `modifier: Modifier = Modifier` | 各页形参名与透传方式一致，但仍由调用方决定 |
| **不设** `contentWindowInsets` | 被替换的各处原本都没传 ⇒ 沿用 `Scaffold` 默认，等价 |
| `containerColor = background` / `topAppBarColors(surface, onSurface)` 写死 | 实测五页这两项**逐字相同**（这正是收敛的前提）；**主页 `SettingsScreen` 的 42 行块不写死**，它另有 actions，不属本骨架范围 |

## 3. 逐字性与等价判据

- 脚本对每页**双重断言**后才动手：① `Scaffold(` 起点唯一、② 尾行恰为一处 `) { innerPadding ->` /
  `) { padding ->`（两种参数名都兼容，且**原样保留尾行**，正文与闭括号一字不动）；
  ③ **标题资源必须出现在被替换块内**——`PrivilegedBrowserSettingsScreen` 与 `TotpSettingsScreen`
  的标题由块内正则**读出**（不靠我手抄），读出的值再回断言必须在块内 ⇒ 张冠李戴会当场失败。
- `git diff --numstat` 逐页 `+4 / −25`（Totp `+4 / −25`+snackbar 行），无整段增删。
- 编译 `--rerun-tasks` **零 error 零 warning**（这五页）；五页各留一个 `@OptIn(ExperimentalMaterial3Api::class)`
  **未判定是否仍必要**：删它有编译风险（页内或另有实验性 API），留着最坏是一条冗余注解 ⇒ 如实登记，不顺手删。

## 4. 剩余六页的前置核实（下一段用）

`CloudSyncScreen` / `SecuritySettingsScreen`（`OneTapInteractionWiringTest`）、`DebugSettingsScreen`
（`AlgoHotPathGuardsTest`）、`HealthCheckScreen`（`UiMd3AlignmentWiringTest`）、
`DatabaseSettingsScreen` / `SettingsScreen`（另有 `SourceTextSanitizer` /
`DatabaseSettingsSectionWiringTest` / `ExportTicketSinkGuardTest`）。
**已核的一条**：`DebugSettingsScreen` 的锚点是
`val coloredLines = remember(logLines) {`（日志段配色预计算）与 `!contains("logLines.forEach …")`
——**都在 topBar 块之外** ⇒ 该页的骨架替换不会碰锚点，可安心做，且它 443 行、替换后逼近第二档出口。

## 5. 验证

| 命令 | 结果 |
|---|---|
| `:app:compileDebugKotlin --rerun-tasks` | `BUILD SUCCESSFUL in 28s`，这五页**零 error 零 warning** |
| `test --rerun-tasks --max-workers=1` | `BUILD SUCCESSFUL in 1m 56s` / `114 actionable tasks: 114 executed`；聚合 **tests=2203 failures=0 errors=0 skipped=13**（313 份 XML），`Uncaught exception` 命中 **0**（用例数与 §187 一致） |
| `:app:lintDebug` | 任务**确实执行**；`issue` 元素 = **215**，与基线一致、**无新增**（新增的骨架文件本身不引入 MD3 违规） |
| `:app:compileDebugScreenshotTestKotlin --rerun` | 任务**确实执行**、`BUILD SUCCESSFUL`（五页的 `@Preview` 包装全部仍可编译） |
| `python tools/doc/long_functions.py` | `functions_ge_100=23`（原 24）、`files_scanned=506` |
| `python tools/doc/logic_lines.py … PrivilegedBrowserSettingsScreen` | 总行 **95**（原 115）⇒ 出表 |
| `python tools/doc/count_line_tiers.py` | 第一档 2、第二档 **28**（本批五页均 <400，不动分档） |
| `git diff --numstat` | 四页 `+4 / −25`、`PrivilegedBrowserSettingsScreen` `+4 / −24`（其原块少一行），正文与闭括号未被触碰 |
| `python tools/doc/check_md_links.py` | `BROKEN_MD_LINKS=0` |
| **未执行** | `cargo test`、`test -DliveSyncTest`、四层 `connectedDebugAndroidTest`（无设备） |

**证据边界**：五页的替换在自动化层面只证到「编译 + 成图包装编译 + 用例不回归」——
**标题栏的实际呈现（含 insets、snackbar 叠加）没有断言**，骨架的参数默认值与逐字等价的判断
来自 §3 的脚本断言与 §1 的对照，而非运行时验证。

## 6. 下一批

第 6 项剩余六页。**先纠一处本批正文的口径**：`DebugSettingsScreen` 443、`SecuritySettingsScreen` 433、
`CloudSyncScreen` 427 替换后是 422 / 412 / 406——**每页只省 21 行，仍留在第二档**
（各差 7~12 行才到 400 线），所以那三页的收益是「少 63 行重复」而**不是**「第二档 28 → 25」，
不得按后者立项。要继续压第二档，须另找整段可下沉的实质逻辑（如 `CloudSyncScreen` 的表单段落）。
之后回到第 3 目的 21 个 ≥100 行 Compose 函数。
