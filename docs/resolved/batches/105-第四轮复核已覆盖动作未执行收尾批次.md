<a id="s105"></a>
## §105 第四轮复核「已覆盖但动作未执行」收尾批次（2026-09-16，app + docs）：无既有 ISSUE 编号（同轮处置）

> **动机**：`ISSUE-P3-141` 收口（§104）时，[`docs/security/第四轮复核遗留编号处置.md`](../../security/第四轮复核遗留编号处置.md)
> §5 列出 **4 项「已被某条目覆盖、但建议动作从未执行」**——其中 `NEW-B04-01` 的整改建议由报告 §6.8 的 `IPC-02` 行
> **在第四轮即已给出**，却一直未落地。本批把其中**无风险的三项**执行完毕（第四项见 105.4「未做」），
> 并顺手清掉一处被该批复核撞出的**既有基线告警**与一处**测试名口径不一致**。

### 105.1 交付（全部为注释 / 文档层，零可执行逻辑变更）

1. **`NEW-B04-01`：修正「extras 不可伪造」的错误归因**——`app/src/main/java/com/keepasskey/app/passkey/PasskeyAssertionActivity.kt`
   修正 **3 处**（`:78` 交叉核对说明、`:144` 非浏览器路径、**`:291` `clientDataJSON.androidPackageName` 写入点**）。
   > **第 3 处是报告与整改建议都未列出的同型表述**，由「全文件检索 `不可伪造` / `extras`」补出。
   统一口径：「预期包名由本应用在**候选组装阶段**写入 base `Intent`；其完整性由『落地 Activity `exported=false`』+
   『`PendingIntent` 仅由系统发送』共同保证，**不得**理解为 extras 本身不可伪造」。`不可伪造` 三处均只剩**否定/边界**用法。
2. **`NEW-B04-03`：承接文档补「表述更正」指针**——`docs/security/退役审计承接-43-安全整改方案与附录.md`
   在 §43.7 强项 9 紧邻处**追加**（原句一字未改，遵退役纪律「归档正文只搬迁、不改写」）3 空格缩进的
   「2026-09-16 表述更正」块引用，指向 `SECURITY_RECHECK_2026-09.md` §5.8。
3. **`NEW-B09-02`：`BrowserSigningFingerprints` KDoc 补通道口径边界**——新增「## 通道口径差异」小节。
4. **既有基线告警清理（本批撞出，非本批引入）**：`app/src/main/java/com/keepasskey/app/apps/InstalledAppsCatalog.kt:128`
   的 `Unnecessary safe call on a non-null receiver of type 'CharSequence'`——由提交 `5e561d4`（`ISSUE-P3-139`）引入、
   与 §104 的复核无关；本批去掉多余 `?.` 并留注说明来源（`PackageManager#loadLabel` 在 compileSdk 37 上返回非空）。
5. **测试名口径不一致**：`app/src/test/java/com/keepasskey/app/passkey/CallingOriginResolverAttributionTest.kt:36`
   的方法名仍作「系统包名被跳过后回退到**不可伪造的** extras 记录」——与第 1 项刚统一的口径**直接矛盾**，
   改名并补否定语：「回退到 extras 中的预期包名（**extras 本身不可伪造性不成立**）」。

### 105.2 两项前提的独立复核（先复核、后动手）

| 项 | 复核结论 | 证据 |
|---|---|---|
| `NEW-B04-03` 原句「导出组件面仅 `MainActivity` + 两个 `BIND_*` 服务」 | **对源清单成立、对合并清单不成立** ⇒ 更正必要 | 源清单 `app/src/main/AndroidManifest.xml` 仅 3 个 `exported="true"`（`:81` `MainActivity`、`:135`/`:155` 两个 `BIND_*` 服务）；**release 合并清单** `app/build/intermediates/merged_manifests/release/processReleaseManifest/AndroidManifest.xml` 有 **6 个**，多出 `androidx.work.impl.background.systemjob.SystemJobService`（`:234/237`）、`androidx.work.impl.diagnostics.DiagnosticsReceiver`（`:310/313`）、`androidx.profileinstaller.ProfileInstallReceiver`（`:333/336`） |
| `NEW-B09-02`「CM 侧浏览器白名单未含 Firefox」 | **成立** | CM 侧唯一白名单 `CallingOriginResolver.kt:37-54`（JSON 内唯一 `package_name` 为 `com.android.chrome`，`:43`）；其两个消费点即 CM 通道全部出口（`KeePasskeyCredentialProviderService.kt:318-321`、`CredentialResponseAssembler.kt:259-264`）；Firefox 两条指纹（`BrowserSigningFingerprints.kt:52/56`）**仅**被自动填充侧消费（`AutofillWebDomainPolicy.kt:55-60` → `AutofillOriginResolver.kt:42`） |

> **未照抄「有意的窄口径」**：该「意图」未被取证，KDoc 只写「两通道口径不同、较窄一侧（CM）fail-closed、
> 不构成攻击面；是否对齐未作裁决；Firefox 经 CM 通道的**实际可用性未经设备侧验证**」。

### 105.3 验证证据

- `bash /d/tmp/gwlock.sh :app:testDebugUnitTest` → **BUILD SUCCESSFUL in 21s**；聚合 **167 套 / 1134 例 / 0 失败 / 0 错误 / 0 跳过**。
  断言源码文本的接线守卫全绿：`CredentialManagerCallerBindingWiringTest` 10、`BrowserFingerprintFormatTest` 3、
  `PackageVisibilityQueriesWiringTest` 4、`MultiSignerCertMatchingTest` 8、`AutofillWebDomainPolicyTest` 9、
  `CallingOriginResolverAttributionTest` 6、`ExportedComponentHygieneTest` 5。
- `bash /d/tmp/gwlock.sh :app:compileDebugKotlin --rerun` → **BUILD SUCCESSFUL in 13s**，`e:` 0 条；本批改动**新增告警 0**
  （首跑该 task 为 `UP-TO-DATE`，故以 `--rerun` 强制全量重编）。
- `bash tools/audit/check_recheck_consistency.sh` → **PASS（1294 行 / 11 条禁用短语）**。

### 105.4 未做与残余（**不得**读作已完成）

1. **`NEW-B02-1`（`KdbxKdfParameterCodec` 的 50% 堆门槛 vs `Argon2KdfEngine` 的 60%）刻意未做**：唯一安全方向是
   取**较严**的 `0.5`，但那会把**原生侧上限收紧**，存在影响既有库可解锁性的风险，须先做影响评估。
2. **Firefox 经 CM（通行密钥）通道的真机可用性未验证**——KDoc 已如实声明，不构成可用性结论。
3. `NEW-B04-03` 的合并清单证据取自**构建中间产物**（debug / release 变体），故指针中如实表述为「合并清单」。
4. **行尾复核的仪器教训（如实留痕）**：本批曾用 `grep -c $'\r'` 判定行尾，得到「全文件 CRLF」的**错误结论**；
   改用字节级 `od -c` 复核后确认三份文件均为 **LF**，且 `.gitattributes` 已有 `* text=auto eol=lf`（git 层权威约束）。
   ⇒ **行尾判定必须以字节级手段为准**，`grep` 计数不可作依据。

---

---

## 归档索引行原文（无损迁移承接）

> 迁移前本批次的正文同时存在于三处：总索引行、分册级索引行、本文件。以下按「只搬迁、不改写」
> 原文照录两处索引行正文（更正须另加小节，不得就地改），自此两处索引只留一行指针。
> 承接批次：§154。

### 总索引行原文（§105）

第四轮复核「已覆盖但动作未执行」收尾批次（无既有 ISSUE 编号，同轮处置）：`ISSUE-P3-141` 收口（§104）时其处置文档 §5 列出 4 项「已被某条目覆盖、但建议动作从未执行」，其中 **`NEW-B04-01` 的整改建议由报告 §6.8 的 `IPC-02` 行在第四轮即已给出却一直未落地**。本批执行其中无风险的三项（**全部为注释/文档层，零可执行逻辑变更**）：① 修正 `PasskeyAssertionActivity.kt` **3 处**「extras 不可伪造」错误归因（`:78`/`:144`/**`:291`**——**第 3 处报告未列出**，由全文件检索补出），统一为「预期包名由本应用在候选组装阶段写入 base `Intent`；其完整性由『落地 Activity `exported=false`』+『`PendingIntent` 仅由系统发送』共同保证，**不得**理解为 extras 本身不可伪造」；② `退役审计承接-43` 于 §43.7 强项 9 紧邻处**追加**（不改原句，遵退役纪律）「表述更正」指针指向报告 §5.8，并以**源清单 3 个 vs release 合并清单 6 个 `exported="true"`**（多出 `SystemJobService`/`DiagnosticsReceiver`/`ProfileInstallReceiver`）独立复核该前提；③ `BrowserSigningFingerprints` KDoc 补「通道口径差异」小节——先独立核实「CM 侧白名单未含 Firefox」**成立**（`CallingOriginResolver.kt:37-54` 仅 `com.android.chrome`，Firefox 两条指纹仅被自动填充侧消费），且**未**照抄「属有意窄口径」的未取证意图。**附带清掉两处残留**：`InstalledAppsCatalog.kt:128` 由 `5e561d4` 引入的既有基线告警（去多余 `?.`）与 `CallingOriginResolverAttributionTest.kt:36` 与统一口径矛盾的测试名。验证：`:app:testDebugUnitTest` **1134 例全绿**（含全部源码文本接线守卫）、`:app:compileDebugKotlin --rerun` **零新增告警**、审计闸门 `PASS`。**未做**：`NEW-B02-1` 的 KDF 阈值常量收敛（取较严 `0.5` 会收紧原生侧上限，有影响既有库可解锁性风险，须先做影响评估）；Firefox 经 CM 通道的真机可用性未验证。**仪器教训留痕**：曾用 `grep -c $'\r'` 误判行尾为 CRLF，字节级 `od -c` 复核确认为 LF（`.gitattributes` 已有 `* text=auto eol=lf`）⇒ 行尾判定须以字节级手段为准
