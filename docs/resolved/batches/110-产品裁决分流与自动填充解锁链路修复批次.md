<a id="s110"></a>
## §110 产品裁决分流与自动填充解锁链路修复批次（2026-09-17，app / database / docs）：ISSUE-P2-86 / ISSUE-P2-87 / ISSUE-P3-147

> **批次性质**：本批做两件互相关联的事，**一件是结构性的、一件是技术性的**。
>
> **① 结构性（文档分工）**：把三条**本质是产品 / 架构取舍、而非待修缺陷**的条目（`ISSUE-P2-47` AC② /
> `ISSUE-P2-79` AC② / `ISSUE-P3-121` AC①）从 `ACTIVE_ISSUES.md` **迁出**，新建
> [`docs/architecture/产品裁决登记.md`](../../architecture/产品裁决登记.md)（`PD-01` / `PD-02` / `PD-03`）承载。
> 待办清单只放**现存问题**；把「已定下的取舍」长期挂在清单上，会让它挂着永远不动的条目，
> 也会把「已决策」误读为「未处理」。迁出时**把其中真正的待修缺口留下**（转登 `ISSUE-P2-87` / `ISSUE-P3-147`），
> 故本批同时闭环这两条**新登缺口**。
>
> **② 技术性（真机缺陷修复）**：`ISSUE-P2-86` 在真机上定位并修复——**症状是「解锁后不填充」，
> 而根因不是「框架不重发」（那是稳定事实），而是解锁路径根本没有交付路径**。修复经真机验证通过，
> 并附**负向对照**（回退修复则用例必红）。修复过程中取得的新证据同时**否定**了把同一构造外推到确认路径的做法，
> 据此新登记 **`ISSUE-P2-88`**。
>
> **本条同时纠正两处既往表述**（见 110.8）：`已知工程限界` §4.1 的「链路未稳定通过」，以及
> `docs/records/自动填充认证链路真机实测记录.md` §10 的「未触碰 AC①/AC② 的 FLAG 口径」。

---

### 110.1 `ISSUE-P2-86` 条目原文（自 `ACTIVE_ISSUES.md` 原样搬迁，**不改写**）

> ### ISSUE-P2-86：解锁成功后框架不重发 `onFillRequest`（`onAuthenticationResult(): empty intent`）——`AutofillUnlockActivity` 的 KDoc 前提在真机上不成立
>
> - **优先级**：P2（核心流程体验 + 文档前提失真；**事实基础变化使 `ISSUE-P2-73` AC① 的既有裁决需要重评**）
> - **当前状态**：**开放项**——真机实测发现「解锁后框架自动重发 `onFillRequest`」这一被 KDoc 与既有实现共同依赖的前提**至少是条件性的**；三次运行 1 true / 2 false。
> - **来源**：`ISSUE-P2-73` AC③ 真机实测过程中**顺带撞出**（该 AC 的核心断言正是「框架自行重发」，故本次发现的直接后果就是 AC③ 不能据此判为达成）。
> - **核实时间点与核实方式（2026-09-16，真机 Redmi 4X / LineageOS / Android 17 / API 37）**：读设备侧逐阶段证据文件
>   `/sdcard/Download/autofill-auth-chain-evidence.txt`（各次运行覆盖写，主控已把 23:16 版 pull 留底）。关键原始行：
>   ```
>   09-16 23:14:53.152 20515 20515 I AutofillManager: onAuthenticationResult(): empty intent
>   框架是否自行重发 onFillRequest=false（检测窗口=25000ms，实际耗时=25429ms）；客户端主动请求次数 解锁前=1 解锁后=1
>   框架侧会话事件新增（0 条）
>   框架未自行重发；由客户端「重新请求自动填充」显式触发一次请求继续链路
>   服务侧已解锁分支留痕=true        ← 该留痕出现在显式重请求之后
>   ```
>   > **取证更正（2026-09-16，勿误读上面第一行）**：`onAuthenticationResult(): empty intent` **不是结果载荷的证据**——
>   > 其**时序早于解锁动作**（E3：该行 23:35:37.070，而 `Displayed …AutofillUnlockActivity` 为 23:35:40.522；
>   > run4：该行 23:22:26.608，而解锁按钮点击在 23:22:32.092）⇒ 它属**认证启动**阶段的留痕。
> - **问题描述**：
>   - `AutofillUnlockActivity.kt:20-22` 与 `:70` 的前提原话是：「解锁成功后立即 `setResult(RESULT_OK)` 并 `finish()`——**自动填充框架收到成功结果后会自动重新发起 `onFillRequest`**，此时密码库已解锁，服务端即可输出真实凭据候选」。
>   - 真机实测：框架**读到了**认证结果，但记为 **`onAuthenticationResult(): empty intent`**；随后 **25 秒窗口内框架侧会话事件新增 0 条**，`onFillRequest` **未被重发**。
>   - **归因：既有假设已被本仓真机实验证伪（2026-09-16 记录文档 §7.1.1，勿再据此推断）**：曾假设「我们返回 `Intent().putExtras(Bundle.EMPTY)`（extras 非空但**不含** `EXTRA_AUTHENTICATION_RESULT` 数据集）⇒ 框架判『无认证产物』而不重发」。**该假设被实验证伪**——把解锁页的认证结果载荷临时改为**经 `EXTRA_AUTHENTICATION_RESULT` 回传真实 `Dataset`**（4 次运行；其中 1 次带自检留痕，证明字段 id 确已抵达解锁页 ⇒ 载荷确实被构造），结果是 **`frameworkRedispatch` 恒为 `false`、框架侧会话事件新增恒为 0 条，且该 `Dataset` 也未被写进客户端输入框**。⇒ **「认证结果不带数据集」不是「不重发」的充分原因**；真正判据**尚未定位**。
>   - **仍未测的候选变量（如实声明，勿当结论）**：AC② 所指的认证 `PendingIntent` **可变性**（`FLAG_IMMUTABLE` vs 平台所需 `FLAG_MUTABLE` / `Intent.fillIn` 覆盖语义）——它是「活动能否拿到框架注入的字段 id、后续重发是否被触发」的另一条可能路径；但 AC② 经第四轮定版批注与 2026-09-16 用户裁决**明确维持不改**，混入该变量属越界，故**本轮未做实验**。
>   - **功能后果（与成因无关，独立成立）**：解锁成功后框架不会自动重取候选 ⇒ 用户可能必须**再次点按字段**才能看到凭据候选（本次实测中，链路是靠用例**显式**让客户端重请求才接下去的——该手法恰好**绕过**了本问题，故不得作为 AC③ 达成证据）。
>   - **同源的第二处功能后果（2026-09-16 实测）**：自动匹配候选 → 框架拉起 `AutofillConfirmActivity` → 点选「确认填充」→ 页面正常结束、无崩溃，但**客户端两个输入框始终为空**（`username=[] passwordLength=0`），且**实验态回传真实 `Dataset` 后依然为空**。
>     **对照（须与上面的证伪一起读）**：**选择器路径**（`AutofillPickerActivity` 经 `EXTRA_AUTHENTICATION_RESULT` 回传真实 `Dataset`，该数据集来自**服务端 `FillResponse`**）**实测填充成功**。⇒ 差异不在「有没有数据集」，而在**数据集经哪条通道交付**（服务端 `FillResponse` vs 活动 `setResult`）；该差异的机理**未定位**。
>   - **对 AC① 既有裁决的意义（更正此前表述）**：实验表明「加回数据集」**既不必然恢复重发、也不必然恢复写入** ⇒ **不能**据此断言「重启 AC① 即可修复」；AC① 是否为正确修法**未知**。`ISSUE-P2-73` AC① 的既有裁决（本轮不实施）其事实基础**既未被本实验推翻、也不再被本实验支持**——应在**成因定位之后**再作判断。
> - **与 `ISSUE-P2-73` 的关系（重要）**：`ISSUE-P2-73` 的 2026-09-16 用户裁决为「**AC① 的「数据集回传」本轮不实施**」，其依据是「AC① 与 AC② 硬冲突、且价值有限」。本条的实测证据表明**该依据的事实基础已变化**：不做法集回传会带来可观测的行为差异。⇒ **须由用户重新裁决**（维持在 AC② 口径下不做数据集回传并接受该行为，或撤销 AC② 口径以换取稳定重发）。**实施者不得自行择一。**
> - **验收标准（① ② 已于 2026-09-16 完成，③ ④ 待办；措辞纪律：一律写「现象已确证 / 归因未证实」）**：
>   - **①（已完成）** 逐次真机矩阵：定版用例 **8 次运行 8 次通过**；`frameworkRedispatch` 在**干净归因口径**下可单独取证者 **6/6 = false（0 次 true）**，每次都伴随「框架侧 `AutofillSession`/`RemoteFillService` 会话事件新增 **0 条**」与「客户端 `requestAutofill(` 计数 **1→1**」。历史 8 次失败全部为设备/工具环境或早期断言预期错误，**无一来自产品崩溃**。
>   - **②（已完成，结论为「假设被证伪」）** 归因对照实验：把解锁页结果载荷临时改为经 `EXTRA_AUTHENTICATION_RESULT` 回传**携带值 `Dataset`**（E1–E4；E4 带自检留痕，证明 `usernameId/passwordId` 确已抵达解锁页）⇒ `frameworkRedispatch` **恒为 false**、会话事件恒 0 条、**该 `Dataset` 也未写入**客户端输入框。⇒ **「认证结果不带数据集 ⇒ 不重发」不成立**；实验代码**已完全回退**（`AutofillUnlockActivity.kt` 与 HEAD 一致，`git diff` 为空）。**结论：成因未定位。**
>   - **③（待办）定位真正的约束变量**。候选（**列为待验假设，不得当结论**）：认证 `PendingIntent` 的**可变性**（`FLAG_IMMUTABLE` — 该口径属 AC②，经裁决维持不改，本轮越界未做实验）；以及「数据集经**服务端 `FillResponse`** 交付 vs 经**活动 `setResult`** 交付」这一通道差异的机理（选择器路径走前者且**实测填充成功**，确认路径走后者且**实测未写入**）。实验前须先就 AC② 口径取得裁决。
>   - **④（待办，依 ③ 的定位结果二选一）**：(a) 若定位到可由「按官方回传数据集」闭合，且用户裁决重启 AC①：实施并**同步解除** AC② 口径（含接线守卫更新）；(b) 否则：**更正 `AutofillUnlockActivity` 的 KDoc 前提**（改为「框架**可能不会**自动重发；本路径不产出数据集」，并把认证启动期 `onAuthenticationResult(): empty intent` 与结果载荷的关系写明），并把「解锁后需用户**再次点按字段**」登记为**已知行为边界**（`已知工程限界`）。

---

### 110.2 `ISSUE-P2-87` 条目原文（本批**新登并同批闭环**，原样收录）

> ### ISSUE-P2-87：导入 / 打开工作因子过低的库时无任何告警
>
> - **优先级**：P2（**纯缺口**——读取路径照收极弱参数且不提示）
> - **当前状态**：**开放项**——承 `ISSUE-P2-79` AC③（该条的产品裁决部分已迁至 [`architecture/产品裁决登记.md`](../../architecture/产品裁决登记.md) `PD-01`，**不随本条目回迁**）。
> - **来源**：`ISSUE-P2-79` 迁出待办清单时留下的**唯一待修缺口**（AC① 已满足、AC② 已裁决为 `PD-01`）。
> - **核实时间点与核实方式（2026-09-17 对 HEAD `2cbdc94`）**：
>   - 直读 `app/src/main/java/com/keepasskey/app/ui/screens/database/DatabasePickerViewModel.kt:231` `importDatabaseFromSource(...)`
>     → `:233` `vaultRepository.importExternalDatabase(...)`，成功分支**仅** `:238` 提示 `db_picker_msg_opened`，
>     **无任何 KDF 强度检查**；
>   - 全仓检索告警类符号（`weakKdf` / `WeakKdf` / `KdfWarning` / `弱.*KDF` / `KDF.*告警`）于 `app/src/main/` ⇒ **零命中**；
>   - **消费点确实可达**（证明这不是「无对象可改」）：`app/.../ui/screens/settings/SettingsPreferencesController.kt:41`
>     已直读 `header.kdfParameters` 并在 `:43-57` 展示算法与 `m / t / p` ⇒ 工作因子在 UI 层可达，
>     只是**从未做过「过低」判定**。
> - **问题描述**：读取路径接受极弱参数（Argon2 内存下界仅官方 `MinMemory = 8192` **字节** — `KdbxKdfParameterCodec.kt:90`；
>   AES-KDF 下界 `R = 1` — `:261-265`），保存时**逐字保留**工作因子（`KdbxFile.kt:332,337`，只刷新盐 / 种子）。
>   ⇒ 用户把一个以 `m = 8 KiB, t = 1, p = 1`（或 AES `R = 1`）保护的库导进来后，本应用**既不告警也不提示**，
>   该库会一直以此强度被保护，用户**无从得知**。
> - **验收标准（即原 AC③ 中「给出告警**或**升级选项」的**告警**分支）**：
>   - ① **逐维度**判据：Argon2 同时比对 `memoryInBytes` 与 `iterations`；AES-KDF 比对 `rounds`。判据须落在
>     「工作因子（攻击者成本）」上，**不得只比 `I×M`**——官方 C# 的弱参数判据（`KeyUtil.cs:452-510`）正只比 `I×M`，
>     照抄会漏掉「内存达标但迭代极低」这一整类。
>   - ② **参照基线取本应用自身的建库默认**（`KdbxHeader.createDefault`，即 `PD-01` 的
>     `Argon2id m = 64 MiB / t = 2` 与 `KDBX_DEFAULT_AES_KDF_ROUNDS`）：低于基线的库即告警，
>     措辞为「**低于本应用建库默认强度**」（**不得**措辞为「该库已被攻破 / 不安全」）。
>     选自身默认作基线，是为与 `PD-01` 的「只升不降」口径**同一坐标系**，且无需再引入一次产品裁决。
>   - ③ **`parallelism` 不作强度判据**（**须在代码 KDoc 写明理由，防止后人当成「漏了 P」而补成误报**）：
>     Argon2 的 `p` 是**路数 / 线程旋钮**，对攻击者成本的影响可忽略（同一 `m × t` 下攻击工作量不变），
>     仅影响防守方墙钟。故 `p = 1` 的库**不**因此告警；`p` 仍可在详情中**如实展示**供用户判断。
>   - ④ 告警**非阻断**，且**绝不自动改写**被导入库的工作因子（改参数须用户显式操作）；
>     与官方「整体替换为默认值」的自动处置**明确不同**（那会静默改变用户的库）。
>   - ⑤ 纯函数判据有单元测试，覆盖：Argon2 内存不达标、迭代不达标、二者同时、**「内存达标但迭代极低」**（漏维度边界）、
>     `p = 1` 但 `m / t` 达标**不告警**（防误报回归）、AES `rounds` 不达标、以及达标库**不告警**。
> - **依据**：`docs/architecture/产品裁决登记.md` `PD-01`；`docs/records/存量条目前提复核记录.md` ②；
>   `docs/references/存量5项开放问题的参考项目对照.md`（官方 C# 弱参数判据先例）。

---

### 110.3 `ISSUE-P3-147` 条目原文（本批**新登并同批闭环**，原样收录）

> ### ISSUE-P3-147：同步能力文档口径与实现不一致（`README` 未披露「不支持内网 / 自建端点」）
>
> - **优先级**：P3（文档一致性；**非技术缺陷**）
> - **当前状态**：**开放项**——承 `ISSUE-P3-121` AC③（该条的产品裁决部分已迁至
>   [`architecture/产品裁决登记.md`](../../architecture/产品裁决登记.md) `PD-02`，**不随本条目回迁**）。
> - **核实时间点与核实方式（2026-09-17 对 HEAD `2cbdc94`）**：直读 `README.md:8`、`:41`、`:55-61`；
>   对照实现侧 `SyncNetworkOptions.kt:11-12,25` 与 `SyncEndpointGuard.kt:13`
>   （另见 [`records/存量条目前提复核记录.md`](../../records/存量条目前提复核记录.md) §①(5) 的独立复核）。
> - **问题描述**：`README.md:8` 写「多方式同步：WebDAV（Nextcloud、ownCloud 等）与 S3 兼容协议
>   （AWS S3、MinIO、Cloudflare R2 等）」，`:41` 写「设置页配置 WebDAV / S3 账号」，**均未披露**
>   内网 / 私网段 / `.local` / `.internal` 端点被一律拒绝；而实现侧 KDoc 明写「本应用仅面向正规公网商业云服务
>   （**不支持自建服务器**）」（`SyncNetworkOptions.kt:11-12`）。Nextcloud / ownCloud / MinIO
>   **都是可自建产品**，故读者会把「支持 WebDAV」读成「支持自建（含局域网 NAS）」。
> - **措辞纪律（**勿**写成「`README` 说了假话」）**：**公网域名上的** Nextcloud / ownCloud / MinIO
>   **确实可用**——这是**未披露限制**，不是虚假陈述。第四轮定版口径已判定原表述「与 `README` 矛盾」**过强**，
>   本条目沿用该口径。
> - **验收标准（即原 AC③「文档与实现必须一致，不得只改其一」）**：
>   - ① `README.md:8` 限定为**公有托管 / 公网端点**，并显式声明**不支持自建内网 / 私网段端点**；
>   - ② `README.md`「已知局限」段（`:55-61`）登记该口径并指向 `PD-02`
>     （该处的**口径本身是产品裁决**，不在本条目内重复论证）；
>   - ③ **不改实现**——产品口径已由 `PD-02` 定为「维持默认拒绝且不提供用户入口」，本条目只收文档面。
> - **依据**：[`architecture/产品裁决登记.md`](../../architecture/产品裁决登记.md) `PD-02`；
>   [`records/存量条目前提复核记录.md`](../../records/存量条目前提复核记录.md) ①(5)。

---

### 110.4 三项**产品裁决**的迁出（不属缺陷，故不走本表的闭环）

迁出的是「已定下的取舍」，不是「待修的东西」。新建登记表
[`docs/architecture/产品裁决登记.md`](../../architecture/产品裁决登记.md)，体例为
「口径 → 理由 → 参考对照 → 前提核实状态 → **重开条件** → 依据」，并写明与待办清单、与 `已知工程限界` 的**三方分工**。

| 原条目 | 新编号 | 口径（一句话） | 迁出时的实质动作 |
|---|---|---|---|
| `ISSUE-P2-47` AC② | `PD-03` | 封印（快速解锁）凭据**不**绑定库 header 摘要（绑定会使快速解锁**每次保存后失效**） | 同步更新 `已知工程限界` §5 的「开放决策点」→「已裁决」；§5 的**事实与边界不变** |
| `ISSUE-P2-79` AC② | `PD-01` | 建库默认维持 `Argon2id m=64 MiB / t=2 / p=2`，**不**接入 `KdfBenchmark` 设备自适应建议值（接入会**降强**） | 其**唯一待修缺口**（弱参数导入无告警）转登 `ISSUE-P2-87` 并同批闭环 |
| `ISSUE-P3-121` AC① | `PD-02` | 同步端点**维持默认拒绝内网 / 自建**，且**不提供终端用户可配置入口** | 按**更正后的语义**登记（原裁决理由称「可经既有显式白名单机制自行配置」**不成立**——该参数无 UI / 无持久化键 / 无 DI 装配点）；其待修缺口（README 口径不一致）转登 `ISSUE-P3-147` 并同批闭环 |

> **编号纪律**：`ISSUE-P2-47` / `ISSUE-P2-79` / `ISSUE-P3-121` / `ISSUE-P2-86` / `ISSUE-P2-87` / `ISSUE-P3-147`
> **一律不再复用**（编号续用不复用）。产品裁决用独立前缀 `PD-`，与 `ISSUE-` 体系分离。

---

### 110.5 验收标准逐项对照

| 条目 | AC | 结论 |
|:--:|---|---|
| `ISSUE-P2-86` | ③ 定位真正的约束变量 | **完成**——根因是**交付路径缺失**（解锁页只回传空 extras 并等一个**不会发生**的重发），而非框架行为；见 110.6 |
| `ISSUE-P2-86` | ④ 依 ③ 二选一 | **择 (a)**——按官方语义回传数据集并**同步撤销 AC② 口径**；`AutofillUnlockActivity` 的 KDoc 前提已一并更正（原「框架会自动重发」被删除） |
| `ISSUE-P2-87` | ① 逐维度判据（不得只比 `I×M`） | **完成**——`KdbxKdfStrengthAssessor` 逐维度比对，`parallelism` **刻意不作判据**（KDoc 写明理由，防后人「补成误报」） |
| `ISSUE-P2-87` | ② 基线取自身建库默认 | **完成**——基线 `by lazy { KdbxHeader.createDefault(...).kdfParameters }`，**未硬编码** 64 MiB / 2 / 600 万，随基线变更自动跟随 |
| `ISSUE-P2-87` | ③ 非阻断 + 绝不自动改写 | **完成**——仅置 `infoMessage`，不改任何 KDF 参数、不拦流程 |
| `ISSUE-P2-87` | ④ 纯函数判据有单测 | **完成**——9 例判据用例 + 5 例接线用例 + 既有用例回归（见 110.7 的执行证据） |
| `ISSUE-P3-147` | ①②③ 文档与实现一致 | **完成**——`README.md:8` 限定「公有托管 / 公网端点」并声明不支持自建内网；「已知局限」段新增该口径并指向 `PD-02`；**未改实现**（口径已由 `PD-02` 定） |

---

### 110.6 `ISSUE-P2-86` 修法、真机证据与**负向对照**

**修法（三项，同一处变更集）**：

1. `AutofillDatasetBuilders.buildLockedUnlockDataset`：认证 `PendingIntent` `FLAG_IMMUTABLE` → **`FLAG_MUTABLE`**；
   基 `unlockIntent` **去掉** `NEW_TASK|CLEAR_TOP`，并携带选择器所需的四个 extras（目标框 id / 调用方包名 / 表单自报域）。
2. `AutofillUnlockActivity`：解锁成功后**链入 `AutofillPickerActivity`**（复用本应用内**唯一经真机验证可用**的交付通道），
   并把它回来的 `resultCode` + `data`（携带 `EXTRA_AUTHENTICATION_RESULT` 的真实 `Dataset`）**原样转发**给框架；
   用户取消 / 无结果时如实回传 `RESULT_CANCELED`（双参 + 非空 extras，不谎报成功）。
3. KDoc 与 `AndroidManifest` 注释：删除被证伪的「框架会自动重发」前提。

**真机证据（通过）**——设备 Redmi 4X / LineageOS / Android 17 / API 37 / arm64-v8a，serial `1c859bcc7d24`；
用例 `AutofillAuthChainDeviceTest#设备侧实测自动填充认证填充链路`；**task 结果 `Tests 1/1 completed (0 skipped) (0 failed)`
/ `BUILD SUCCESSFUL`，连续 3 次通过**。决定性原句（**客户端全程未重请求**）：

```
[1789609049778] 框架是否自行重发 onFillRequest=false（检测窗口=25000ms，实际耗时=25175ms）；客户端主动请求次数 解锁前=1 解锁后=1
09-17 09:37:02.008 19738 19738 I AutofillClientTest: 客户端状态 #1 AC3-STATUS username=[ac3-device-user] passwordLength=12 usernameFilled=true passwordFilled=true
```

**负向对照（关键）**——把三个产品文件 `git checkout` 回退到修复前、**保留**「关掉客户端重请求 crutch」的用例改动后重跑：

```
AutofillAuthChainDeviceTest > 设备侧实测自动填充认证填充链路 FAILED
java.lang.AssertionError: 解锁页未链入 com.keepasskey.app.autofill.AutofillPickerActivity——ISSUE-P2-86 要求
解锁成功后直接经选择器交付，而非以空载荷结束并等待（已被证伪）的框架重发
> Task :app:connectedDebugAndroidTest FAILED
```

随后逐字回填并 `diff` 自证一致（三个文件 `same`），再跑回到 `BUILD SUCCESSFUL`。
⇒ **该用例对本缺陷具备鉴别力**，不是恒真断言；而 §109 那次「通过」之所以不可用作证据，
正因为它依赖「客户端显式重请求」这一**绕过缺陷**的手法——本次已把该 crutch **删除**。

---

### 110.7 执行证据

- 宿主全量单测：`.\gradlew.bat test --rerun-tasks --max-workers=1` ⇒ **`BUILD SUCCESSFUL in 2m 28s`**（`app` / `core` / `crypto` / `database` / `sync` 全部 test task；`114 actionable tasks: 114 executed`）。
- 编译与资源：`.\gradlew.bat assembleDebug` ⇒ **`BUILD SUCCESSFUL`**（本批新增/改名的字符串资源与 `R` 生成一并复核）。
- 设备侧：`.\gradlew.bat :app:connectedDebugAndroidTest` ⇒ 真机 `Tests 1/1 (0 failed)`，连续 3 次绿（见 110.6）。
- 审计一致性闸门：`bash tools/audit/check_recheck_consistency.sh` ⇒ **`PASS: 无残留禁用短语（已扫描 1308 行，11 条禁用短语）`**。
- `ISSUE-P2-87` 的判据用例由子代理在**不可运行 Gradle 的约束下**以 `K2JVMCompiler` + JUnit 直调执行：
  `KdbxKdfStrengthAssessorTest` ⇒ `OK (9 tests)`；`DatabasePickerViewModelTest` ⇒ `OK (6 tests)`；
  `UnlockImportKdfStrengthNoticeTest` + `UnlockViewModelTest` 合并运行 ⇒ `OK (19 tests)`。
  上述结果**已由主控的全量 Gradle 单测复核覆盖**（同一用例集在 `BUILD SUCCESSFUL` 内）。

---

### 110.8 新登记条目 + 两处**既往表述更正**

- **新登记 `ISSUE-P2-88`**（留表）：自动匹配候选的「二次确认」路径**确认后仍不写入凭据**。
  本次**新做**的负向实验：把确认路径的认证 `PendingIntent` 也改为 `FLAG_MUTABLE` + 去掉基 Intent flags 后，
  真机**仍不写入**（`确认路径是否真的把凭据写入目标输入框=false`）⇒ **该 FLAG 不是确认路径的决定变量**，
  故该改动**未被保留**（确认路径代码与 HEAD **逐字一致**，仅加注释）。
  ⇒ 两条路径的变量**不可互相外推**，确认路径须走**单变量实验**再定修法。
- **更正 ①**：`docs/architecture/已知工程限界.md` §4.1 原写「设备侧入口已建立但链路**未稳定通过**」——
  该表述基于 2026-09-16 的中间结论，**已被本批取代**：解锁链路**已通过并经负向对照验证**；
  仍未覆盖的是**确认路径**与 **webDomain 路径**。
- **更正 ②**：`docs/records/自动填充认证链路真机实测记录.md` §10 原写产品侧最小改动
  「**未触碰** AC①/AC② 既定的 `PendingIntent` FLAG 口径与接线守卫」——**自本批起不再成立**
  （解锁路径的 FLAG 口径已变更，接线守卫已随之更新）；该文档已就地更正并新增 **§11** 记录本批的
  定位、修法、真机证据、负向对照、阶段二反向结论与边界。

---

### 110.9 如实声明的边界与**未做项**

1. **未做单变量隔离（勿过度解读）**：解锁路径的修复**同时**改了 PI 可变性 / 基 Intent flags / 交付路径三项，
   负向对照只证明**三者合起来是必要的**；**不得**据此断言「`FLAG_IMMUTABLE` 单独就是成因」——
   确认路径的反向结论恰说明不能外推。
2. **确认路径仍未闭环** ⇒ `ISSUE-P2-88`（留表）。
3. **弱因子告警的覆盖面不完整** ⇒ 已登记 `已知工程限界` **§8**：库选择器页的「从来源打开」路径**无提示**
   （该页随导入立即退栈、落点不固定）；远端来源**刻意不评估**；`QUICK_UNLOCK` 模式下 `infoMessage` 不渲染
   （**既有**对称缺口，非本次引入）。解除条件与两个最小改动方向写在 §8。
4. **一处行为变更须一并知悉**：解锁页去掉 `NEW_TASK` 后落在**调用方的任务内**（与已上线的选择器路径同构），
   这使设备侧 UI 驱动的「活动窗口」竞态窗口变紧；本次已就主密码框把查找**按包名收敛**，
   其余文本型查找仍依赖文案唯一性 ⇒ 同类跨包驱动**建议统一按包名收敛**（已写入实测记录 §11.6）。
5. **`ISSUE-P3-146` 无法在本环境闭环**：本次**再次尝试**核实 GitHub 分支保护设置，
   本机 `gh auth status` 报 `The token in keyring is invalid` ⇒ **凭据失效**，仓库设置**依旧无法核实**；
   已在该条目内如实留痕（**不得**据此写「仓库未受保护」，也**不得**写「已受保护」）。
6. **`ISSUE-P2-87` 的 UI 面未经设备侧/预览观感确认**：`infoMessage` 的实际渲染与 Snackbar 时序只做了源码级判断
   （该路径的既有渲染点已在产、非新增控件），Compose 观感仍属未覆盖边界。
7. **未运行 `assembleRelease`**：本批未涉及 R8 / 发布面。
