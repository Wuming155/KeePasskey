<a id="s273"></a>

# §273 附件池引用计费重定口径批次

> `ISSUE-P1-276` **整条闭环**（P1 1 → **0 项**，本区归零）。
> 触发＝2026-09-23 两轮全仓审查（8 铺开 + 7 对抗复核）P1 区末项整改。
> 条目 AC①（按去重 `refIndex` 计费）与 AC③（「同一条目被引数千次仍被拒」）**不能同用字节维度实现**——
> 去重后被引用集合 ⊆ 池集合 ⇒ 旧整库字节式**恒真**、防护归零；该冲突经**用户当场裁决**（AC⓪ 式）
> 取「去重计费 + 单条目引用次数 ≤ 1024 + 单条目物化字节 ≤ 64 MiB」三面方案。
> 覆盖面：`ISSUE-P1-276`（本体）+ `ISSUE-P3-292` 的耦合说明（该条仍开放，仅在正文与限界表互设指针）。

---

## 1. 条目正文（原样收录）

### `ISSUE-P1-276`：附件引用预算把自产与合法第三方库判为「引用放大攻击」，整库无法打开（合并轮次会把受害窗口撑宽）

- **核实时间点**：2026-09-23 经全仓定向核对 + 两轮独立对抗复核（复核代理 C 组四处攻击全部落空）+ 边界算式与用户共同定版。
- **核实方式**：① 计费侧 `database/src/main/java/com/keepasskey/database/xml/KdbxXmlBinaryNode.kt:221` 对**每一个**非落盘引用调 `attachmentBudget.accountPoolReference(item.size)`，`:329-335` 越界即抛 `KdbxCorruptFileException` 文案「疑似引用放大攻击」（`:331` 为**严格大于**）。② 上限侧同文件 `:382-385` 的 `forParse(pool)` = `2 × 池总字节 + BUDGET_SLACK_BYTES(1 MiB)`，无第二条更宽上限（`MAX_INLINE_MATERIALIZED_BYTES = 64 MiB` 是另一维度）。③ 落盘阈值侧 `core/src/main/java/com/keepasskey/core/security/BinaryStore.kt:52-57` 为 `size > 1 MiB` **严格大于**⇒ 恰好 1 MiB 的附件仍内联、仍被计费。④ 计费**无 refIndex 去重**：同一池条目被引 N 次即计 N 份整尺寸。⑤ 历史快照确实复用 `<Ref>`——`database/src/main/java/com/keepasskey/database/xml/KdbxXmlEntrySerializer.kt:85-95` 递归序列化 `<History>` 时**原样透传 `binaryPoolSize`**，`:126-129` 命中池即写 `Ref` 属性、不写内联字节；`<History>` 与主条目共用同一预算实例（`KdbxXmlGroupReader.kt:205/259`、`KdbxXmlParser.kt:53` 为唯一生产构造点）。⑥ 回归缺口：`AttachmentReferenceBudgetTest` 只测「2 引用放行 / 5000 引用拒绝」，**未声明 ≥3 引用为预期失败**，`:59` 用例名还自称「不被误拒」。
- **判据（定版，整改与用例都按此写）**：设池内各条目尺寸为 `sᵢ`、被引用次数为 `nᵢ`，触发拒绝的条件是
  `Σᵢ nᵢ·sᵢ > 2·Σⱼ sⱼ + 1 MiB`。
  ① **单条目**情形化简为 `s × (N − 2) > 1 MiB` ⇒ `N ≤ 3` 安全；`N = 4`（主条目 + 3 条历史）需 `s > 512 KiB`；`N = 5` 需 `s > 341 KiB`；`N = 6` 需 `s > 256 KiB`。因内联上界恰为 1 MiB，**受害区间是 `(512 KiB, 1 MiB]`（含 1 MiB 端点）**——正是手机照片附件的常见尺寸。② **多条目不组合成安全**：两个条目各带一个 1 MiB 内联附件、各仅 2 条历史（各 `N = 3`）时，计费 `6 MiB > 2×2 MiB + 1 MiB = 5 MiB` **同样触发**。③ 反直觉处：`> 1 MiB` 的附件因走 spill 分支（`KdbxXmlBinaryNode.kt:209-217` 不计费）**反而免疫**——预算惩罚的是「中等尺寸」而非「大附件」。
- **与 `ISSUE-P3-291` 的耦合（定级依据之一）**：合并器 `sync/.../merge/KdbxEntryMerger.kt:168` 取 `local + remote + base.history` 三方历史并集且不截断，**每多一条历史快照就给同一池条目多写一个 `<Ref>`**，即 `N` 每轮 +1 ⇒ 受害窗口随合并轮次向更小尺寸、更少本地编辑次数方向持续变宽。用户**全程不碰附件**也会从中止条件滑入触发条件。
- **背景与根因**：这是自伤型误拒——后果不是降级而是**整库打不开**（只读通道与修复通道同走 `parse`），且本仓产物被自己的解析器判为损坏。三家参考实现均只按 `pool[i]` 取引用、无「按引用次数复制并计费」的机制（官方 `KdbxFile.Read.cs` 的 `ReadProtectedBinary` 直接取池条目），故本护栏属自加且口径错：要封的是「单池条目 + 海量引用」的放大攻击，实际却把「正常历史版本数 × 正常尺寸附件」一并拦下。
- **涉及文件**：`database/src/main/java/com/keepasskey/database/xml/KdbxXmlBinaryNode.kt`（`:221` 计费、`:329-335` 抛出、`:382-385` 上限、`:209-217` spill 分支）、`core/src/main/java/com/keepasskey/core/security/BinaryStore.kt`（阈值语义）、`database/src/main/java/com/keepasskey/database/xml/KdbxXmlEntrySerializer.kt`（历史透传 `binaryPoolSize`）、`database/src/test/.../AttachmentReferenceBudgetTest.kt`（回归缺口）、`sync/src/main/java/com/keepasskey/sync/merge/KdbxEntryMerger.kt:168`（放大器）。
- **验收标准**：AC① 计费口径改为**按去重后的 `refIndex` 集合**计量（同一池条目被引 N 次只计 1 份整尺寸），保留对「单条目 + 海量不同引用者」的真实放大面防护；AC② 判据与阈值以**注释内的封闭公式**写明（`Σ nᵢsᵢ` vs `2Σsᵢ + 余量`），禁「魔法数 + 散文说明」；AC③ 回归用例两级：单条目 `N = 4, s ∈ (512 KiB, 1 MiB]` **必须放行**，两条目各 `N = 3, s = 1 MiB` **必须放行**，同时保留「真放大攻击（同一条目被引数千次 / 池外越界索引）」仍被拒的用例——既有 `AttachmentReferenceBudgetTest` 用例只可加不可删；AC④ 端到端往返：`:database:` 层构造「1 MiB 附件 + 3 条历史」的真实产物并断言 `save → load` 成功解锁（对齐 `ISSUE-P2-271` 批次的换轴往返口径）；AC⑤ 与 `ISSUE-P3-292`（合并历史截断）同批或后续批收口，且整改说明须写明二者耦合，禁「只修一个就算闭环」；AC⑥ 触发条件须以**构造文件实测**验证（不得只算式推演），并按规则 8 与官方实现交叉核对同类库的可打开性。

### 开工前前提复核与勘误（2026-09-23）

1. **前提全部成立、无实质漂移**：计费点、上限式、落盘阈值语义（严格大于）、历史透传 `binaryPoolSize`、
   唯一生产构造点、回归缺口六项逐条反校属实；行号因本批拆分而前移，新位置见 §2.6。
2. **勘误①（条目自陈的耦合编号有误）**：正文写「与 `ISSUE-P3-291` 的耦合」，而 AC⑤ 写「与 `ISSUE-P3-292`」；
   经核对 **P3-291 是「同步配置只按 `remotePath` 键控」**（与附件引用无关），
   「合并取三方历史并集且不截断」确为 **`ISSUE-P3-292`**（`sync/.../merge/KdbxEntryMerger.kt:168`）⇒ 本批按 **P3-292** 处置。
3. **勘误②（AC③ 括注的一半与现况不符）**：「池外越界索引仍被拒」**不成立**——按缺陷 D9 对齐官方
   （`KdbxFile.Read.Streamed.cs:980-1028`），`Ref` 越界**回退读内联正文**、不抛错，
   该语义由 `KdbxAttachmentAliasIsolationTest::引用索引越界时返回空数组且不抛异常` 锁定，
   越界面的护栏由**内联通道**的累计字节 / 节点数两条承担。本批**不改**该语义，
   AC③ 中「同一条目被引数千次仍被拒」这一半按新判据完整保留（用例见 §3.1）。
4. **AC⓪ 裁决（用户当场作出，选项即裁决记录）**：AC① 的去重与 AC③ 的「数千次仍被拒」**不能同用字节维度实现**
   （去重后 `Σ_distinct sᵢ ≤ Σ_pool sⱼ` ⇒ 旧式恒真）。三候选——**①「去重 + 单条目次数上限 1024 + 单条目
   物化上限 64 MiB」**、②「去重 + 次数上限 256」、③「不去重，给原判据加 64 MiB 绝对下限」——**取候选 ①**：
   AC① 字面落地、历史引用全放行、单条目放大被「次数 × 字节」双封顶；残余（分散引用的放大上限）登记限界表 **§29**。

---

## 2. 整改

### 2.1 AC① + AC②：计费口径按 `refIndex` 重定，封闭公式进 KDoc

`AttachmentBudget.accountPoolReference(bytes)` → **`accountPoolReference(refIndex: Int, bytes: Long)`**
（调用点 `KdbxXmlBinaryNode.emit` 补传 `resolved.refIndex`），三面判据逐面记账：

| 判据 | 实现 | 是否承担拦截 |
|---|---|---|
| ① 去重计费 `Σ_{i ∈ 去重引用集} sᵢ ≤ 2·Σⱼ sⱼ + BUDGET_SLACK_BYTES` | `countedPoolItems: HashSet<Int>` + `accountedPoolReferenceBytes` | **否**（去重口径下恒真，见下） |
| ② 单条目引用次数 `nᵢ ≤ MAX_REFERENCES_PER_POOL_ITEM` | `poolItemReferenceCounts: HashMap<Int, Int>` | 是（放大**倍数**维度） |
| ③ 单条目物化字节 `nᵢ·sᵢ ≤ MAX_POOL_ITEM_MATERIALIZED_BYTES` | `poolItemMaterializedBytes: HashMap<Int, Long>` | 是（单条目**内存**维度） |

- **①为何恒真（如实写在 KDoc 里，不装作它会拦）**：被引用的 `refIndex` 必然落在池范围内（`BinaryNode` 仅在池内
  命中时才保留索引）⇒ 去重后的左式 ≤ 池总字节 < 右式，故它记录的是**记账不变量**（去重计费绝不覆盖池外字节、
  不因引用倍数虚增），保留它是为让 AC① 的口径在代码与判据中显式存在并捕获记账漂移。
- **AC② 的封闭公式全部落 KDoc**：类 KDoc 写明旧判据 `拒绝 ⟺ Σᵢ nᵢ·sᵢ > 2·Σⱼ sⱼ + BUDGET_SLACK_BYTES` 的
  **代数化简与受害区间推导**（单条目 `s·(N − 2) > 1 MiB` ⇒ `(512 KiB, 1 MiB]`），以及新三式；
  `MAX_REFERENCES_PER_POOL_ITEM` / `MAX_POOL_ITEM_MATERIALIZED_BYTES` 各自 KDoc 写明**取值依据**与**已知取舍**
  （后者取 `MAX_INLINE_MATERIALIZED_BYTES` 派生，不另立字面量）。
- **消息口径**：③ 的文案保留旧措辞「累计物化字节超出预算」并把作用域写进括号（`refIndex=… 单条目累计 X >
  Y，共被引用 N 次`）——既有用例的断言因此**一字未改**仍绿；② 另给「单池条目被引用次数超出上限」文案。

### 2.2 AC③：回归用例两级（既有只增不删）

- **保留**：`单池条目加海量引用被物化预算拒绝而非 OOM`（fixture 与断言一字未动，仅**改名**：`累计预算` → `物化预算`
  以匹配 ③ 的作用域，并补注释说明现由第 1025 份引用触发）、`共享池条目的少量引用不被误拒`（一字未动）。
- **新增 4 例**（`AttachmentReferenceBudgetTest` 2 → **6 例**）：
  `一兆内联附件带三条历史引用必须放行`（上界**端点** `s = 1 MiB, N = 4`）、
  `五百一十二K加一字节的内联附件带三条历史引用必须放行`（窗口**下界内侧**）、
  `两条目各挂两条历史引用的一兆附件必须放行`（AC③ 的「2 × `N = 3`」形态，`6 MiB` 对旧预算 `5 MiB`）、
  `单池条目引用次数的边界`（② 的 `1024` 放行 / `1025` 拒绝，且条目仅 1 KiB ⇒ 拦截只可能来自次数维度）。

### 2.3 AC④：`:database:` 层端到端往返 + 官方对拍探针

新增 `AttachmentHistoryReferenceBudgetRoundtripTest`（**2 例**）：

- `一兆附件带三条历史的库保存后可解锁且四个引用共享同一池条目`——`KdbxFile.save` 造真产物（1 MiB 附件，
  恰为落盘阈值端点 ⇒ 仍内联、仍计费）+ `KdbxFile.load` 断言**解锁成功**、池 1 条目 / 4 个引用者（主条目 + 3 历史）
  共享索引 0、四个引用者字节逐字节一致；同时把产物与清单落到 `build/attachment-budget-probe/`
  （`.kdbx` + `.expected.json` + `ATTACHMENT_BUDGET_PROBE.md`，含口令与复现命令）供外部对拍。
- `二次保存往返后引用形态与字节均保持`——载荷重写后引用与池的对应关系不漂移。
- **AC④ 的换轴口径**：沿用 `ISSUE-P2-271` 批次确立的「真实 save→load（而非只断言算式）」，故本批的
  触发条件验证是**构造文件实测**，不是算式推演（AC⑥ 前半）。

### 2.4 AC⑥：与官方实现的交叉核对（规则 8）

- **本仓产 → 官方读**：`keepassxc-cli`（KeePassXC 官方 CLI **2.7.12**）打开 §2.3 的探针产物
  （`db-info` 成功 / `ls -R` 见条目 / `attachment-export` 导出的 1 MiB 附件 **SHA-256 与清单逐字节一致**），
  读数见 §3.3。
- **官方产 → 本仓读**：新增 fixture `database/src/test/resources/fixtures/attachment-history/`
  （**落盘方 = KeePassXC 官方 CLI**：以本仓 v4 探针产物为基线、经官方 `edit` 两轮改写而由官方实现重写落盘，
  形态 = 1 个池条目被引 **6 次**、库头 KDBX 4.0）与 `ThirdPartyAttachmentHistoryFixtureTest`（**1 例**）；
  生成命令、逐项读数与 SHA-256 记于同目录 `FIXTURE.md`。
- **过程勘误（如实登记）**：首版 fixture 走 `keepassxc-cli db-create`，实测该命令产出 **KDBX 3.1**
  （文件头 `01 00 03 00`，本仓读取侧只支持 v4，用例首跑即 `KdbxUnsupportedVersionException`）⇒ 改为
  「v4 基线 + 官方改写」路径，并把该事实写进 `FIXTURE.md` 注 1。

### 2.5 AC⑤：与 `ISSUE-P3-292` 的耦合（本条侧已闭环，对方侧留指针）

- **耦合事实**（原样保留在 `ISSUE-P3-292` 正文并经本批改写）：合并器取三方历史并集且不截断 ⇒ 每轮合并给同一
  池条目多写一个 `<Ref>`，即 `nᵢ` 单调增长。整改前该耦合把受害窗口撑宽；整改后其表现**降级**为
  「放大上限被推高」——② 的 `1024` 正是按「合并历史未截断」这一现状取的宽值。
- **单向闭环与后续义务**：`ISSUE-P3-292` 的 AC③ 已改写为「耦合已单向闭环（本条 §273）+ 该条落地后**必须复评**
  `MAX_REFERENCES_PER_POOL_ITEM` 取值依据并回填限界表 §29」。**禁「只修一个就算闭环」**在本批的落实方式 =
  该指针 + 限界表 §29 的解除条件，而非强行把 P3-292 拖进本批（其整改面在 `sync` 合并链，属独立战斗面）。

### 2.6 附带：文件拆分（工程规则「单文件 ≤ 约 400 行」）

计费口径重定后 `KdbxXmlBinaryNode.kt` 达 **507 行**（`tools/doc/count_line_tiers.py` 的 `tier1(>500)`）⇒
按职责拆出 [`KdbxAttachmentBudget.kt`](../../../database/src/main/java/com/keepasskey/database/xml/KdbxAttachmentBudget.kt)
（SAX 节点 / 预算记账分离），语义与 `internal` 可见性逐字不变。现读数：`KdbxXmlBinaryNode.kt` **292 行** /
`KdbxAttachmentBudget.kt` **220 行**；全仓 `tier1(>500)=3` 均为本批未触的既有文件（616 / 550 / 536 行）。

### 2.7 未触面

- 未改 `> 1 MiB` 附件走落盘**不计费**的既有语义（`BinaryStorePolicy.shouldSpill` 严格大于，一行未动）；
- 未改内联通道的两条判据（累计 64 MiB / 节点数 1024）与 `KdbxXmlValueUtil` 宽松解码；
- 未改 `sync` 合并链（`KdbxEntryMerger` 一行未动 ⇒ `ISSUE-P3-292` 原样开放）；
- 未改 `crypto/src/main/rust/**`、`*/src/androidTest/**`、`参考项目/`、构建脚本与依赖清单。

---

## 3. 验证

### 3.1 定向用例与判别力实验（AC③ / AC⑥ 前半）

| 测试类 | 例数 | 结果 |
|---|:--:|---|
| `AttachmentReferenceBudgetTest` | 6 | 6 / 0 失败 / skipped 0 |
| `AttachmentHistoryReferenceBudgetRoundtripTest` | 2 | 2 / 0 失败 / skipped 0 |
| `ThirdPartyAttachmentHistoryFixtureTest` | 1 | 1 / 0 失败 / skipped 0 |
| `InlineCompressedAttachmentBudgetTest`（邻域回归） | 3 | 3 / 0 失败 |
| `KdbxAttachmentAliasIsolationTest`（别名隔离契约，不得放宽） | 4 | 4 / 0 失败 |

**判别力实验（把计费口径临时还原为旧式：按引用累计、无去重、无单项护栏）** ⇒

| 用例 | 旧口径读数 |
|---|---|
| `一兆内联附件带三条历史引用必须放行` | 红：`累计物化字节超出预算（4194304 > 3145728）` |
| `五百一十二K加一字节…必须放行` | 红：`（2097156 > 2097154）` |
| `两条目各挂两条历史引用…必须放行` | 红：`（6291456 > 5242880）` |
| `单池条目引用次数的边界` | 红：`expected KdbxCorruptFileException to be thrown, but nothing`（旧口径无次数维度） |
| `一兆附件带三条历史的库保存后可解锁…`（AC④） | 红：`（4194304 > 3145728）` ⇒ **整库打不开**，原缺陷逐位复现 |
| `二次保存往返…` | 红：同上 |
| `官方CLI产出的…库可解锁…`（AC⑥ 官方 fixture） | 红：`（4194304 > 3145728）` |
| `单池条目加海量引用被物化预算拒绝而非 OOM` | 绿（旧口径亦拦，属两代口径的公共面） |
| `共享池条目的少量引用不被误拒` | 绿 |

合计 **7 例红 / 2 例绿**；还原后 9 例全绿（读数如上表）。**该实验同时证明**：新增用例不是「改写期望值以适配新实现」，
而是钉住真实缺陷——旧口径下它们必红。

### 3.2 全量单测（JVM）

```
.\gradlew.bat test --rerun-tasks --max-workers=1     → BUILD SUCCESSFUL in 2m 32s / 114 executed
python tools/doc/count_test_results.py               → xml=371 tests=2579 failures=0 errors=0 skipped=13
```

对账（§272 基线 `xml=369 tests=2572 skipped=13`）：**+2 XML 类 / +7 例**，与新增（2 类 3 例 + 既有类 +4 例）逐条相符；
`skipped` 持平（13，均为既有登记项，本批未新增跳过）。

### 3.3 官方实现读数（规则 8，命令与原始输出）

**方向一 · 本仓产 → 官方读**（探针产物 `build/attachment-budget-probe/keepasskey-medium-attachment-history.kdbx`）：

```bash
printf '<口令>' | keepassxc-cli db-info -q  <产物>          # 名称 AttachmentBudgetProbe / 加密 AES 256 位 / KDF AES（6000 次）/ 群组数 1 / 条目数 1
printf '<口令>' | keepassxc-cli ls -R -q    <产物>          # Medium Photo v4
printf '<口令>' | keepassxc-cli attachment-export -q <产物> "/Medium Photo v4" photo.jpg out.jpg
sha256sum out.jpg                                          # 631b84027d6b9e52b539c4e8373622d23032dfadc64d60af87339c9037e4f769（= 清单期望值）/ 1048576 B
```

**方向二 · 官方产 → 本仓读**（fixture，落盘方为官方 CLI）：

```bash
printf '<口令>' | keepassxc-cli export -q <fixture>   # <History> 1 块 / <Entry> 6 个 / <Value Ref="0"> 6 个
od -A d -t x1 -N 12 <fixture>                        # 03 d9 a2 9a 67 fb 4b b5 00 00 04 00 ⇒ KDBX 4.0
```

⇒ 两个方向都落在**非本仓实现**上：`ThirdPartyAttachmentHistoryFixtureTest` 的输入不是本仓产物，
`AttachmentHistoryReferenceBudgetRoundtripTest` 的产物被官方 CLI 端到端读通且附件字节一致。

### 3.4 文档同步

- `docs/ACTIVE_ISSUES.md`：剪出 `ISSUE-P1-276`（P1 1 → **0 项**）并在 P1 区补本批闭环摘要；
  `ISSUE-P3-292` 的「后果③」与 AC③ 按 §2.5 改写（单向闭环 + 复评义务）；
  `ISSUE-P3-295` 的 AC③ 指针由已归档的条目号改为现行常量落点。
- `docs/architecture/已知工程限界.md`：新增 **§29**（本批显式接受的残余：只封单条目维度、
  放大上限从 `≤ 2× 池总字节 + 1 MiB` 放宽为 `≤ MAX_REFERENCES_PER_POOL_ITEM × 池总字节`、边界与解除条件），
  并把「附」摘要的末节标号 §21 ~ §28 → §21 ~ §29。
- `docs/records/限界表证据与实测数据汇录.md`：§附 表补 **§29** 行，并**顺带补登 §28 的漏登行**；
  附录 A 增 §29 的整句搬移块。
- `docs/RESOLVED_LOG.md` / `docs/resolved/README.md`（最大编号 → §273）/ `docs/resolved/BATCH_158_PLUS.md`。

### 3.5 验证面如实声明

- **无设备侧义务**：未改原生面（`crypto/src/main/rust/**` 与 JNI 绑定一行未动）、未改 `*/src/androidTest/**`、
  未改 `参考项目/`、未改构建脚本与依赖清单 ⇒ 无「四层 connected 必跑」义务；本会话**无设备**，
  未跑任何 `connectedDebugAndroidTest`（§263 立规：装有用户数据的设备禁止直跑）。
- **未跑**：`lint` / `assembleRelease` / 截图测试包装门禁 / `verify_interop.py`（未改 `PasskeyData` schema、
  `PasskeyPkcs8Codec` 与 KPEX 字段）。已跑：`check_md_links.py`、`check_resolved_index_sync.py`、
  `check_recheck_consistency.sh`、`count_line_tiers.py`（读数见 §2.6 与提交信息）。
- **公式面与实测面的分工**：AC② 要求的封闭公式是**判据文档**；AC⑥ 要求的「构造文件实测」由 AC④ 往返与
  官方 fixture 用例承担；本批**未**构造「分散引用」攻击样本（该面按限界表 §29 登记为已接受残余），
  故 §29 的残余量描述是**算式上界**而非实测读数，不得读作已实测。
- **一处证据边界**：方向二 fixture 的**基线内容**来自本仓探针产物（附件字节与条目字段），
  落盘与历史快照由官方 CLI 写出；即「官方实现端到端读写同一份库」成立，但「从零由官方产出的同形态库」
  未取（`db-create` 恒写 KDBX 3.1，本仓不读 v3；`pykeepass` 侧工具在本会话受沙箱审批限制未能运行——
  按「未执行」口径如实标注，不推断结论）。

---

## 4. 过程留痕

1. **AC①/AC③ 冲突的发现与裁决**：开工前先做全链路核对，发现按 AC① 字面实现会让字节判据恒真、
   使 AC③ 的「数千次仍被拒」失去载体 ⇒ 以三候选形式请用户当场裁决（非「先做再问」），
   裁决记录见 §1 勘误 4。
2. **施工中的两次自纠**：① 常量前向引用（`MAX_POOL_ITEM_MATERIALIZED_BYTES` 引用其后声明的常量）
   导致 `compileDebugKotlin` 失败 ⇒ 调整 companion 内声明顺序；② 文件行数越 `tier1` 阈值 ⇒ 拆文件（§2.6）。
3. **fixture 首版走偏**：`keepassxc-cli db-create` 产 KDBX 3.1 ⇒ 用例 `KdbxUnsupportedVersionException` 首跑即红，
   改走「v4 基线 + 官方改写」并把该事实登记进 `FIXTURE.md`（§2.4）。
4. **判别力实验执行两轮**：第一轮（拆分前、两类）读数 6 红；拆分与 fixture 落地后补跑第二轮（三类）读数 7 红，
   两轮读数均记入 §3.1；补丁临时形如「`accountedPoolReferenceBytes += bytes` 后旧式比较 + 其余判据旁路」，
   还原后 `grep` 复核零残留。
5. **沙箱环境限制**：`pykeepass` 脚本（独立实现读数）在本会话触发敏感内容审批并超时 ⇒ 未执行、不回推、
   改以 `keepassxc-cli`（官方实现）承担双向对拍，并在 §3.5 如实标注该证据边界。
