# §131 非惰性大集合渲染与组合期就地派生收敛批次（`ISSUE-P3-179`）

> **条目范围**：`ISSUE-P3-179`（非惰性大集合展开与组合期就地派生）
> **完成时间**：2026-09-17
> **验证**：`.\gradlew.bat test --rerun-tasks --max-workers=1` → `BUILD SUCCESSFUL in 1m 58s`
> （114 actionable tasks executed），聚合 **`tests=2079 skipped=13 failures=0 errors=0`**
> （§130 为 2078，**+1** = 接线守卫 1 例）。
> **审计闸门**：`bash tools/audit/check_recheck_consistency.sh` → `PASS`（1308 行 / 11 条禁用短语）。

---

## 1. 原条目（自 `ACTIVE_ISSUES.md` 原样收录）

- **背景**：① `app/.../ui/screens/settings/subscreens/DebugSettingsScreen.kt:236` 把上限 500 行的
  `DebugLogBuffer` 整体塞在**单个 LazyColumn item** 内 `forEach` 组合，且每行做 4 次 `line.contains(…)` 判色；
  ② `app/.../ui/screens/vault/VaultListDialogs.kt:241` 把全部分组 `filter{}.forEach{}` 铺进 `AlertDialog` 的 `text` 槽
  （无虚拟化、无高度上限，超出屏幕的分组不可触达）；
  ③ `app/.../ui/screens/edit/EntryEditFormSections.kt:101` 在组合期执行 `availableGroups.filter { !it.isRecycleBin }`
  （每次重组重算整库过滤）且 `items` 无 `key`；`VaultListComponents.kt:249` 面包屑 `items` 同样无 `key`。
- **整改方向**：① 日志改顶层 `items(logLines)` 并按前缀预判等级色；② 分组选择改
  `LazyColumn(Modifier.heightIn(max = 280.dp))` + `items(…, key = { it.id })`（照抄 `AppPickerDialog.kt:138`）；
  ③ 过滤下沉到 ViewModel 或 `remember(availableGroups)`，并补 `key`。
- **验收标准**：三处改为惰性 / 已记忆（结构断言）；对话框内分组可滚动触达（含超长列表）；
  既有对话框与编辑页用例全绿。
- **核实时间点与方式**：2026-09-17 读 `DebugSettingsScreen.kt:225-250`、`VaultListDialogs.kt:230-255`、
  `EntryEditFormSections.kt:90-115` 核实。
- **风险提示**：`AlertDialog` 的 `text` 槽不滚动，改为 `LazyColumn` 时须一并确认弹窗高度约束，
  避免「改好了但按钮被挤出屏幕」。

---

## 2. 本批实现

### 2.1 `VaultListDialogs` 分组选择：惰性化 + 高度上限 + `key`（对应 AC ②，**本批唯一的可用性缺陷修复**）

`AlertDialog` 的 `text` 槽**自身不滚动**，原实现把全部分组 `forEach` 铺进 `Column` ⇒ **分组多时
超出屏幕的部分无法触达**（用户点不到那些分组）。现改为：

- `LazyColumn(modifier = Modifier.heightIn(max = 280.dp))` + `items(selectableGroups, key = { it.id })`；
- 过滤提到组合之外（`remember(allGroups) { allGroups.filter { !it.isRecycleBin } }`）——
  原实现写在 `forEach` 实参里，每次重组都重跑一次整表过滤；
- **「移动到根目录」入口保留在 `LazyColumn` 之前**（它是固定首位项，不随分组列表滚走），
  与 `AppPickerDialog` 的既有形态一致。

**关于条目「风险提示」（按钮被挤出屏幕）的处置**：`AlertDialog` 会为 title / text / buttons 分配
可用高度并**整体约束**（`AlertDialogContent` 内部对内容做最大高度限制），故 `text` 槽内的
`LazyColumn` 只会占据「剩余可用高度」内的 280 dp 上限、不会把按钮挤出屏幕；为稳妥仍取 280 dp
（与 `AppPickerDialog` 同一数值，属既有站内口径）。

### 2.2 `EntryEditFormSections` 组合期过滤下沉 + `key`（对应 AC ③ 前半）

`LazyRow` 的 content 是 `LazyListScope`（**非 `@Composable`**），无法在其中 `remember` ⇒
过滤下沉到 `EntryEditGroupSection` 函数体（`remember(uiState.availableGroups) { … }`），
`items` 补 `key = { it.id }`。原实现每敲一个字符都重跑一次整表过滤并因无 `key` 而按位置复用项。

### 2.3 `VaultListComponents` 面包屑补 `key`（对应 AC ③ 后半）

`items(breadcrumbs, key = { it.id })`——面包屑在分组间跳转时会整体替换，补 `key` 后可稳定复用项。

### 2.4 `DebugSettingsScreen`：等级色预计算（对应 AC ① 的**色彩部分**）

把逐行 `when { line.contains(…) }`（最多 4 次子串扫描/行，**每次重组都重跑**，含滚动引起的重组）
改为 `remember(logLines) { logLines.map { it to <等级色> } }` —— 只在日志内容变化时算一次。

**刻意不做（AC ① 的惰性化部分）**：不用嵌套 `LazyColumn` 替换卡片内的 `Column`。理由：

1. 该卡片是**单个 `item`**，其内部再放 `LazyColumn` 会在外层 `LazyColumn` 内引入**嵌套纵向滚动**
   ——触达全量日志要「先滚内层再滚外层」，是 **UX 回归**而非优化；
2. 日志行数**有硬上限 500**（`DebugLogBuffer.MAX_LINES`，2026-09-17 读源码确认），
   且本页是**调试专用页**（非用户主路径）⇒ 组合成本有界；
3. 彻底惰性化的正确形态是**先把日志卡片移出外层列表语义**（或改为独立滚动容器），
   属结构性 UI 改动，须与 UI 口径一并裁决，**不在本批范围**。

---

## 3. 本批新增用例（1 例）

`app/src/test/.../quality/AlgoHotPathGuardsTest.kt` 增 **`大集合渲染必须惰性且过滤不得留在组合期`**：
逐处断言（剥离注释后）——对话框中必须存在 `LazyColumn(` 与 `Modifier.heightIn(max = 280.dp)`
且不得再用 `allGroups.filter { !it.isRecycleBin }.forEach {`；编辑页不得把整表过滤写在 `items` 实参里、
必须 `items(selectableGroups, key = { it.id })`；面包屑必须 `items(breadcrumbs, key = { it.id })`；
日志页必须 `val coloredLines = remember(logLines) {` 且不得再有 `logLines.forEach { line ->`。

---

## 4. 边界如实声明（不得外推）

1. **无设备侧 / 视觉验证**：本批改动**全部落在 Compose 组合层**，宿主单测**无法渲染**这些 Screen
   ⇒ 证据是**编译通过 + 结构守卫 + 既有用例全绿**。
   **不得**读作「对话框与编辑页在真机上的观感已确认」；尤其 2.1 的**超长分组列表实际可滚动性**
   与「按钮未被挤出屏幕」**未经真机 / 预览核对**（这也是条目风险提示要求确认的点）。
2. **AC ① 只完成色彩预计算**，惰性化**刻意不做**（理由见 2.4）⇒ 单帧仍会组合最多 500 个 `Text`；
   **不得**读作「日志页已惰性化」。
3. **结构守卫的局限**：它是源码文本断言，对「等价写法」无鉴别力（例如把 `LazyColumn` 换成
   `verticalScroll(Column)` 也能通过 `LazyColumn(` 之外的断言——故守卫同时断言了 `heightIn` 与
   不得残留旧 `forEach` 形态）。
4. **`heightIn(max = 280.dp)` 是既有站内口径**（`AppPickerDialog`），本批沿用；它不是实测得出的
   最优值，**不得**读作「已按最长分组列表调优」。
5. **未跑截图 / 预览基线**：本批未执行 `updateDebugScreenshotTest` 或预览导出，
   故**未**更新任何视觉基线；若 CI 侧有像素比对，需另行复跑。
6. **未跑 `lint` / `assembleRelease`**。
7. 本批**未改任何 ViewModel 与状态层**（AC ③ 提到的「过滤下沉到 ViewModel」未采用，
   改在组合层 `remember`——收益等价且改动面更小）。

---

## 5. 过程缺陷与留痕

1. **本批无编译 / 断言失败**（首次全量即绿，1m58s）。
2. **一处事前核对**：`DebugLogBuffer.MAX_LINES = 500` 由读源码确认（而非引用审计结论），
   因为 2.4 的「刻意不做」理由**建立在「行数有硬上限」之上**——该前提若不成立，结论即不成立。

---

## 归档索引行原文（无损迁移承接）

> 迁移前本批次的正文同时存在于三处：总索引行、分册级索引行、本文件。以下按「只搬迁、不改写」
> 原文照录两处索引行正文（更正须另加小节，不得就地改），自此两处索引只留一行指针。
> 承接批次：§154。

### 总索引行原文（§131）

非惰性大集合渲染与组合期就地派生收敛批次（`ISSUE-P3-179`，第四档第三条）：**① 对话框分组选择（本批唯一的可用性缺陷修复）** —— `VaultListDialogs.kt:241` 原把全部分组 `filter{}.forEach{}` 铺进 `AlertDialog` 的 `text` 槽，而该槽**自身不滚动** ⇒ **分组多时超出屏幕的部分无法触达**（用户点不到）；改为 `LazyColumn(modifier = Modifier.heightIn(max = 280.dp))` + `items(selectableGroups, key = { it.id })`，过滤提到组合之外（`remember(allGroups)`；原写在 `forEach` 实参里、每次重组都重跑整表过滤）；「移动到根目录」固定项保留在 `LazyColumn` 之前（与 `AppPickerDialog` 同形态）。条目「风险提示」的处置：`AlertDialogContent` 会为 title / text / buttons 分配并约束可用高度，`text` 槽内的 `LazyColumn` 只占「剩余可用高度」内的 280 dp 上限，不会把按钮挤出屏幕；280 dp 为**既有站内口径**。**② 编辑页组合期过滤下沉 + key** —— `EntryEditFormSections.kt:101` 原在 `items(...)` 实参里做 `availableGroups.filter { !it.isRecycleBin }`（每次重组重跑）且无 `key`；因 `LazyRow` 的 content 是 `LazyListScope`（**非 `@Composable`**）无法在其中 `remember`，故下沉到函数体 `remember(uiState.availableGroups) { … }` 并补 `key = { it.id }`。**③ 面包屑补 key** —— `VaultListComponents.kt:249` 改 `items(breadcrumbs, key = { it.id })`。**④ 日志等级色预计算** —— `DebugSettingsScreen.kt:236` 原对每行做最多 4 次 `contains` 判色、**每次重组（含滚动引起的重组）都重跑**；改为 `remember(logLines) { logLines.map { it to <等级色> } }`，只在日志内容变化时算一次。**刻意不做（AC ① 的惰性化部分）**：卡片仍以单个 `item` 承载全部日志行——该卡片内部再放 `LazyColumn` 会在外层 `LazyColumn` 内引入**嵌套纵向滚动**（触达全量日志要「先滚内层再滚外层」），是 **UX 回归而非优化**；且日志行数**有硬上限 500**（`DebugLogBuffer.MAX_LINES`，**2026-09-17 读源码确认**——因该「刻意不做」的理由正建立在「行数有硬上限」之上）、本页又是**调试专用页** ⇒ 组合成本有界；彻底惰性化的正确形态是先把卡片移出外层列表语义，属结构性 UI 改动，不在本批范围。**验证**：全量 `--rerun-tasks` **1m 58s 绿**，聚合 **`tests=2079 skipped=13 failures=0 errors=0`**（§130 为 2078，**+1**）；审计闸门 `PASS`（1308 行）。**新增 1 例（接线守卫）**：对话框必须存在 `LazyColumn(` 与 `Modifier.heightIn(max = 280.dp)` 且不得残留 `allGroups.filter { !it.isRecycleBin }.forEach {`；编辑页不得把整表过滤写在 `items` 实参里且必须 `items(selectableGroups, key = { it.id })`；面包屑必须 `items(breadcrumbs, key = { it.id })`；日志页必须 `val coloredLines = remember(logLines) {` 且不得再有 `logLines.forEach { line ->`。**边界如实声明**：**无设备侧 / 视觉验证** —— 本批改动**全部落在 Compose 组合层**，宿主单测**无法渲染**这些 Screen ⇒ 证据是**编译通过 + 结构守卫 + 既有用例全绿**，**不得**读作「真机观感已确认」；尤其 ① 的**超长分组列表实际可滚动性**与「按钮未被挤出屏幕」（正是条目风险提示要求确认的点）**未经真机 / 预览核对**；**AC ① 只完成色彩预计算**，惰性化刻意不做 ⇒ 单帧仍组合最多 500 个 `Text`，**不得**读作「日志页已惰性化」；**结构守卫的局限**（源码文本断言，对等价写法无鉴别力——故除 `LazyColumn(` 外同时断言 `heightIn` 与「不得残留旧 `forEach` 形态」）；`heightIn(max = 280.dp)` 是**既有站内口径**（`AppPickerDialog`）而非实测最优值，**不得**读作「已按最长分组列表调优」；**未跑截图 / 预览基线**（未执行 `updateDebugScreenshotTest` 或预览导出，故未更新任何视觉基线；若 CI 侧有像素比对需另行复跑）；未跑 `lint` / `assembleRelease`；本批**未改任何 ViewModel 与状态层**（AC ③ 提到的「过滤下沉到 ViewModel」未采用，改在组合层 `remember`——收益等价且改动面更小）。**过程留痕**：本批无编译 / 断言失败（首次全量即绿）；一处**事前核对**——`DebugLogBuffer.MAX_LINES = 500` 由**读源码**确认（而非引用审计结论），因为「刻意不做」的理由建立在「行数有硬上限」之上，该前提不成立则结论不成立

### 分册 04行原文（§131）

非惰性大集合渲染与组合期就地派生收敛批次（`ISSUE-P3-179`，第四档第三条）：**① 对话框分组选择（本批唯一的可用性缺陷修复）** —— `VaultListDialogs.kt:241` 原把全部分组 `filter{}.forEach{}` 铺进 `AlertDialog` 的 `text` 槽，而该槽**自身不滚动** ⇒ **分组多时超出屏幕的部分无法触达**（用户点不到）；改为 `LazyColumn(modifier = Modifier.heightIn(max = 280.dp))` + `items(selectableGroups, key = { it.id })`，过滤提到组合之外（`remember(allGroups)`，原写在 `forEach` 实参里、每次重组都重跑整表过滤）；「移动到根目录」固定项保留在 `LazyColumn` 之前（与 `AppPickerDialog` 同形态）。条目「风险提示」的处置：`AlertDialogContent` 会为 title / text / buttons 分配并约束可用高度，`text` 槽内的 `LazyColumn` 只占「剩余可用高度」内的 280 dp 上限，不会把按钮挤出屏幕；280 dp 为既有站内口径。**② 编辑页组合期过滤下沉 + key** —— `EntryEditFormSections.kt:101` 原在 `items(...)` 实参里做 `availableGroups.filter { !it.isRecycleBin }`（每次重组重跑）且无 `key`；因 `LazyRow` 的 content 是 `LazyListScope`（**非 `@Composable`**）无法在其中 `remember`，故下沉到函数体 `remember(uiState.availableGroups) { … }` 并补 `key = { it.id }`。**③ 面包屑补 key** —— `VaultListComponents.kt:249` 改 `items(breadcrumbs, key = { it.id })`。**④ 日志等级色预计算** —— `DebugSettingsScreen.kt:236` 原对每行做最多 4 次 `contains` 判色、**每次重组（含滚动引起的重组）都重跑**；改为 `remember(logLines) { logLines.map { it to <等级色> } }`，只在日志内容变化时算一次。**刻意不做（AC ① 的惰性化部分）**：卡片仍以单个 `item` 承载全部日志行——该卡片内部再放 `LazyColumn` 会在外层 `LazyColumn` 内引入**嵌套纵向滚动**（触达全量日志要「先滚内层再滚外层」），是 **UX 回归而非优化**；且日志行数**有硬上限 500**（`DebugLogBuffer.MAX_LINES`，**2026-09-17 读源码确认**，因该「刻意不做」的理由正建立在「行数有硬上限」之上）、本页又是**调试专用页** ⇒ 组合成本有界；彻底惰性化的正确形态是先把卡片移出外层列表语义，属结构性 UI 改动，不在本批范围。**验证**：全量 `--rerun-tasks` **1m 58s 绿**，聚合 **`tests=2079 skipped=13 failures=0 errors=0`**（§130 为 2078，**+1**）；审计闸门 `PASS`（1308 行）。**新增 1 例（接线守卫）**：对话框必须存在 `LazyColumn(` 与 `Modifier.heightIn(max = 280.dp)` 且不得残留 `allGroups.filter { !it.isRecycleBin }.forEach {`；编辑页不得把整表过滤写在 `items` 实参里且必须 `items(selectableGroups, key = { it.id })`；面包屑必须 `items(breadcrumbs, key = { it.id })`；日志页必须 `val coloredLines = remember(logLines) {` 且不得再有 `logLines.forEach { line ->`。**边界如实声明**：**无设备侧 / 视觉验证** —— 本批改动**全部落在 Compose 组合层**，宿主单测**无法渲染**这些 Screen ⇒ 证据是**编译通过 + 结构守卫 + 既有用例全绿**，**不得**读作「真机观感已确认」；尤其 ① 的**超长分组列表实际可滚动性**与「按钮未被挤出屏幕」（正是条目风险提示要求确认的点）**未经真机 / 预览核对**；**AC ① 只完成色彩预计算**，惰性化刻意不做 ⇒ 单帧仍组合最多 500 个 `Text`，**不得**读作「日志页已惰性化」；**结构守卫的局限**（源码文本断言，对等价写法无鉴别力——故除 `LazyColumn(` 外同时断言 `heightIn` 与「不得残留旧 `forEach` 形态」）；`heightIn(max = 280.dp)` 是**既有站内口径**（`AppPickerDialog`）而非实测最优值，**不得**读作「已按最长分组列表调优」；**未跑截图 / 预览基线**（未执行 `updateDebugScreenshotTest` 或预览导出，故未更新任何视觉基线；若 CI 侧有像素比对需另行复跑）；未跑 `lint` / `assembleRelease`；本批**未改任何 ViewModel 与状态层**（AC ③ 提到的「过滤下沉到 ViewModel」未采用，改在组合层 `remember`——收益等价且改动面更小）。**过程留痕**：本批无编译 / 断言失败（首次全量即绿）；一处**事前核对**——`DebugLogBuffer.MAX_LINES = 500` 由读源码确认（而非引用审计结论），因为「刻意不做」的理由建立在「行数有硬上限」之上，该前提不成立则结论不成立
