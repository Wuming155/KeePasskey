<a id="s304"></a>

# §304 步骤 3 缓存判据下沉批次

> `ISSUE-P3-301` **整条闭环**（P3 11 → **10 项**；同批即时补登 `ISSUE-P2-307`）。
> 覆盖面：`ISSUE-P3-301`（`runSyncCycle` 步骤 3 的 `isCached` 判据仍在调用方线程，§274 显式残余）。
> 生产改动＝该判据以 `withContext(Dispatchers.IO)` 包裹（AC①(b) 分支）；`SyncCache.isCached` 同族开放为 `open` 以便行为级探针；
> 扩展 `SyncAssemblyOffMainThreadTest`（新增 DIRTY 态轮次 + `isCached` 探针），**含判别力反证**。

---

## 1. 条目正文（原样收录）

### `ISSUE-P3-301`：`runSyncCycle` 步骤 3 的 `isCached` 判据仍在调用方线程（§274 显式残余）

- **核实时间点**：2026-09-23（§274 整改当日，随批如实登记）。
- **核实方式**：① §274 已把**装配段**（`setupCycleContext`）整体下沉 `Dispatchers.IO`（`SyncCycleRunner.kt:218`），并以 `SyncAssemblyOffMainThreadTest` 锁定凭据解密 / 整库缓存读 / 全库比较 / 原子写四类的执行线程（含栈归因与判别力实验）；② **本条所指调用点不属装配段**——`SyncCycleRunner.kt:243` 的 `if (ctx.isDirty && ctx.syncCache.isCached(ctx.remotePath))`（步骤 3 前置判据）位于该 `withContext` **之外**，仍执行在调用方线程（前台入口持 `viewModelScope`，即 `Main.immediate`）；③ 该调用为 `SyncCache.isCached()`（`SyncCache.kt:59-62`）＝ `exists() && length() > 0`，**一次 stat 级系统调用**，不读内容、不 `fsync`，与四类的量级相差数个数量级——§274 的「主线程零 IO」口径据此把四类与它分列。
- **背景与根因**：§274 整改时**刻意不动**该点，理由是「改它需先论证 `ctx.isCached`（装配段取样值）与步骤 3 重取恒等价」，而该等价依赖「步骤 2 只在返回非 null 时写缓存」这一耦合推理（`establishRemoteBaselineIfMissing` **仅在** `FileNotFound` 分支 `commitLocal` 后 `return`，其余分支返回 null 且不写缓存），牵动同步状态机判定，风险高于收益 ⇒ 当批以「残余如实登记、不宣称为主线程零 IO」收口，本条承接后续处置。
- **涉及文件**：`app/src/main/java/com/keepasskey/app/sync/SyncCycleRunner.kt`（`:243` 重取点；`setupCycleContext` 内 `:127` 为首次取样）、`sync/src/main/java/com/keepasskey/sync/engine/SyncCache.kt`。
- **验收标准**：AC① 二选一但须**单点化**——(a) 把该判据并入已下沉的装配段产出（**不得**直接换成 `ctx.isCached` 了事，须先补「步骤 2 与步骤 3 之间缓存状态不变」的显式不变量声明或断言）；或 (b) 保留重取但以 `withContext(Dispatchers.IO)` 包裹，并写明为何不接受 `ctx.isCached`；AC② 判据语义不得改变：两步之间的**可观测差异**（若存在）须在条目正文写明，禁「为消除一次 stat 而改动状态机判定」；AC③ 新增或扩展守卫，断言该判据不在主线程执行（沿用 `SyncAssemblyOffMainThreadTest` 的探针 + 栈归因口径）；AC④ 若最终裁定「一次 stat 可接受、不作处理」，须登记 [`docs/architecture/已知工程限界.md`](../../architecture/已知工程限界.md) 并写明解除条件，不得无声留着。

**开工前前提复核（2026-09-24）**：成立。判据点确在装配段 `withContext` 之外（当前行号 `:285`，`if (ctx.isDirty && ctx.syncCache.isCached(ctx.remotePath))`）；`SyncCache.isCached` 确是 `exists() && length() > 0` 两次 stat 级调用；同族访问器 `readCache` / `writeCache` / `readBaseContent` / `openCacheStream` / `cacheSize` 均已为 `open`，唯 `isCached` 非 `open`（§274 分列时留下）。

---

## 2. 整改

### 2.1 走 AC①(b)：保留重取 + 下沉 IO

```kotlin
val cachedBeforeFastCommit = withContext(Dispatchers.IO) {
    ctx.syncCache.isCached(ctx.remotePath)
}
if (ctx.isDirty && cachedBeforeFastCommit) { … }
```

- **语义零变更**（AC②）：同一位置、同一表达式、同一时刻求值，只换执行线程；状态机判定未动。
- **为何不取 (a) 的 `ctx.isCached`**（AC①(b) 明文要求写明）：装配段取样发生在**步骤 2 之前**，
  而步骤 2 的 `establishRemoteBaselineIfMissing` **仅**在 `FileNotFound` 分支 `commitLocal` 之后写缓存
  （其余分支返回 null 且不写）。即「步骤 2 与步骤 3 之间缓存状态不变」是一条**尚未显式论证 / 断言**的
  不变量——一旦该分支行为变化，改用 `ctx.isCached` 会让「刚建立基线」的同一轮被误判进入快速提交路径。
  本次不动状态机（AC②），故取 (b)。
- 该理由已以同文写入 `SyncCycleRunner` 的就地 KDoc。

### 2.2 探针可达性：`SyncCache.isCached` 同族开放

`SyncCache` 已显式 `open class`，且 `readCache` / `writeCache` / `readBaseContent` / `openCacheStream` /
`cacheSize` 均为 `open` —— 这是本仓既有的「IO 原语可探针化」设计。`isCached` 是唯一的例外（§274 把它与四类
重活分列时未一并开放）。本批把它改为 `open` 并补 KDoc 说明**为何它也是 IO 判据**（stat 级），
使 AC③ 的「探针 + 栈归因」口径能覆盖它。

> **未外推**：开放 `open` 不改变任何调用方行为；它只让测试替身可记录该方法。**不得**据此把
> 「一次 stat」当作与四类重活同量级的负担（§274 的分列结论未被推翻）。

---

## 3. 验证

### 3.1 扩展守卫（AC③）：`SyncAssemblyOffMainThreadTest`

1. `RecordingCache` 新增 `override fun isCached` 探针（`PROBE_CACHE_IS_CACHED = "步骤3缓存判据 isCached"`）；
2. 用例尾部新增**第三轮次**：只 `saveEntry` **不** `save()` ⇒ 会话状态为 DIRTY，使
   `ctx.isDirty && …` **不再短路**，步骤 3 的判据被真实求值。断言两条：
   - 探针**确实被观测到**（防空转：若 DIRTY 未生效，本用例对该点不成立）；
   - 该轮**没有任何记录落在主线程**。
   （前两轮因落盘后状态为 CLEAN，判据被短路，故原有「主线程零 IO」断言对步骤 3 不成立——这正是本条的缺口。）

### 3.2 判别力反证（本仓强制）

临时把 `withContext(Dispatchers.IO)` 包裹**撤销**（改回内联求值）后单跑该用例 → **FAILED**，
报文精确指出被判据命中的线程为本用例主线程：

```
装配段/引擎的同步调用落在主线程上（ISSUE-P2-277 回归）:
[ProbeRecord(probe=步骤3缓存判据 isCached, fromAssemblySegment=false,
             thread=Thread[#1,Test worker @kotlinx.coroutines.test runner#2,5,main])]
```

随即还原。⇒ 新增探针具备真实判别力，「主线程零 IO」断言不再是空转。

### 3.3 全量回归

- `.\gradlew.bat test --max-workers=1` → **BUILD SUCCESSFUL**；
- 聚合计数（`python tools/doc/count_test_results.py`）：**`xml=396 tests=2688 failures=0 errors=0 skipped=13`**
  （用例数与 §303 持平——本轮为**扩展现有用例**，未新增测试方法）。

> **如实声明（重要）**：本批首次全量回归时出现 **3 例与代码无关的失败**
> （`AutofillPickerViewModelCredentialLookupTest` / `AutofillPickerViewModelSessionLockTest`），
> **原地复跑即全绿**；隔离跑与「与 `SyncAssemblyOffMainThreadTest` 成对跑」两种控制实验皆绿
> （含以 `HEAD` 版测试文件做的对照）。判定为**既存非确定性**（非本批引入），已即时补登
> `ISSUE-P2-307`（含核实方式、未证实的假设与「不得以放宽断言换绿」的边界）。**本批不修正它**。

### 3.4 机检

| 机检 | 结果 |
|---|---|
| `check_md_links.py` / `check_resolved_index_sync.py` / `check_bounded_type_names.py` | EXIT 0 |
| `check_tautological_assertions.py` / `check_recheck_consistency.py` | EXIT 0 |
| `count_line_tiers.py` / `long_functions.py` | 仍 EXIT 1（**既存**，见 `ISSUE-P3-305`） |

---

## 4. 如实声明

1. **取 AC①(b) 而非 (a)**：原因是 (a) 依赖一条未论证的不变量，会触碰状态机判定（AC② 禁止）。该取舍连同理由已就地写入生产代码 KDoc，不是只在批次文档里说明。
2. **未按 AC④ 走「不作处理 + 登记限界」分支**：本批选择了处置（成本极低且判别力已证），故不动限界表。
3. **`SyncCache.isCached` 由非 `open` 改 `open`**：纯可测性改动，无行为影响；已就地说明不得据此推翻 §274 的量级分列。
4. **`hygiene-gate` 两条仍为红**（`tier1=4` / `functions_ge_100=2`），属 `ISSUE-P3-305` 范围，本批未触碰。
5. **无设备侧必跑项**：未触 `*/src/androidTest/**`、未改原生内核 / KDBX 序列化 / 合并 / 附件面。
