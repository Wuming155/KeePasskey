# §256 限界分类标签与口径裁决迁出批次

> 对应条目：`ISSUE-P3-251`（**同批新登记**，非闭环；见 §4）
> 日期：2026-09-21
> 涉及模块：仅 `docs/`（**未触任何生产代码 / 测试代码 / `参考项目/`**）
> 性质：接续 §255 的**分类分流的定标档**——把 [`已知工程限界.md`](../../architecture/已知工程限界.md) 的
> **每条节标题**按「谁能解除」贴上分类标签，并把 8 节**口径裁决**（要不要做某件事 / 按哪个判据算不算缺陷）
> 逐字迁入 [`产品裁决登记.md`](../../architecture/产品裁决登记.md)（`PD-18` ~ `PD-25`），原表只留存根。

---

## 1. 判定口径（两把尺子 → 四类）

| 类 | 判定 | 处置 |
|---|---|---|
| **【客观限界】** | 本仓不依赖上游 / 平台 / 硬件改变即**无法消除**（解除路径在外部） | **留表**，节标题加标签 |
| **【有意取舍】** | 有残余 / 代价面，但**本仓自己就能改**，只是判为「代价 > 收益」而刻意不做 | **留表**，节标题加标签 |
| **口径裁决**（本批迁出） | 内容是「**要不要做某件事 / 按哪个判据算不算缺陷**」，**没有残余风险面**（驳回某项整改主张、判据适用范围裁决） | **迁往** `产品裁决登记.md`（`PD-18` ~ `PD-25`），本表留存根 |
| **【已迁出】**（§255 存根） | §255 已迁出的 11 节（非限界：说明 / 约定 / 验证现状 与 宿主环境现象） | 节标题补标签，**内容一行不动** |

**标签只回答「谁能解除」，不是完成度评分**（两类都属已裁决不整改）。`AGENTS.md` 规则 9 的「限界登记属文档」口径不变。

## 2. 42 条目分类总表（2026-09-21 现查）

| 类别 | 数量 | 节号 |
|---|---:|---|
| **【客观限界】** | **16** | §1.1、§1.3、§1.4、§1.5、§1.6、§2.1、§2.2、§3.4、§5、§6、§16、§17、§23、§25、§26、§27 |
| **【有意取舍】** | **7** | §2.4、§8、§13、§15、§19、§22、§24 |
| **口径裁决 → `PD-18` ~ `PD-25`**（节标题亦带 **【已迁出】**） | **8** | §1.7（`PD-18`）、§9（`PD-19`）、§10（`PD-20`）、§11（`PD-21`）、§14（`PD-22`）、§18（`PD-23`）、§20（`PD-24`）、§21（`PD-25`） |
| **【已迁出】**（§255 存根，本批只补标签） | **11** | §1.2、§2.3、§3.1、§3.2、§3.3、§3.5、§4.1、§4.2、§4.3、§7、§12 |
| **合计** | **42** | 与「27 节 / 42 条目」的现查结构一致（`## 1.` ~ `## 27.` 章标题**不加**标签） |

**机检计数**（逐行读标题前缀后计数）：`客观限界 16 / 有意取舍 7 / 已迁出 19(=8+11)`，**合计 42**，无未打标签的编号节（仅 4 个 `## N.` 章标题无标签，属规则）。

## 3. 迁出 8 节的逐字搬移证据

### 3.1 搬移规则（本批实际执行）

1. **只允许两处改动**：① 字段名（`**事实**` / `**裁定**` / `**裁决**` → `**口径**`，`**边界**` → `**不得外推的边界**`，
   `**解除条件**` → `**重开条件**`；含括号者**保留括号内容**）；② 新增 `**对应条目**` / `**原登记位置**` 两行
   与末尾「；迁出留痕见批次正文 §256。」一句。**正文句子一字未改**。
2. **原表存根 = 节标题（补 **【已迁出】**）+ 该节**全部含「**不得**」的块**（逐字、保持原行）+ 一行指针
   （`> **口径裁决（2026-09-21 §256 迁出）**：… 完整正文见 … `PD-xx`。`）** —— 因代码 KDoc 按 `§x.y`
   引用这些规范句（§10 / §11 在代码里有 4 处以上引用，如
   `sync/src/main/java/com/keepasskey/sync/engine/SyncEngineSupport.kt:73`、
   `app/src/main/java/com/keepasskey/app/sync/KdbxContentComparator.kt:36`），**删句即断链**。
3. 修改后的首行若越 **110 字符**折行口径，**只换行、不动词句**（本批触发 4 处，均为 `不得外推的边界` 首行）。

### 3.2 机检读数（`build/s256/verify.py`，临时脚本不入库）

比对对象：`git show HEAD:docs/architecture/已知工程限界.md`（迁移前）↔ 迁移后限界表 ∪ `PD-18~25`。
共 **41 个块 / 16 个含「不得」的块**（8 个迁出节的全部 bullet 块）。

| 节 | 块数 | 含「不得」块 | A 存根逐行 | B 并集逐行 | C 首行正文 | D 整块去空白逐字 |
|:--:|---:|---:|:--:|:--:|:--:|:--:|
| §1.7 | 4 | 2 | ✔ | ✘ | ✔ | ✔ |
| §9 | 5 | 2 | ✔ | ✘ | ✔ | ✔ |
| §10 | 5 | 3 | ✔ | ✘ | ✔ | ✔ |
| §11 | 4 | 1 | ✔ | ✘ | ✔ | ✔ |
| §14 | 4 | 1 | ✔ | ✔ | ✔ | ✔ |
| §18 | 5 | 3 | ✔ | ✘ | ✔ | ✔ |
| §20 | 4 | 2 | ✔ | ✘ | ✔ | ✔ |
| §21 | 10 | 2 | ✔ | ✘ | ✔ | ✔ |
| **合计** | **41** | **16** | **全 ✔** | 7 处 ✘ | **全 ✔** | **全 ✔** |

- **A（存根逐行）全 ✔**：8 节里**每一个含「不得」的块**都逐行、原样留在原编号位置。
- **D（整块去空白逐字）全 ✔**：把每个块的各行拼接、去掉空白与改写后的字段标签后，**在并集里逐字可寻**。
- **B 的 7 处 ✘ 已逐条核对**：**全部**是各节 `依据` 块的**末行**——按搬移规则 ① 在行尾接上
  「；迁出留痕见批次正文 §256。」（原行末句号**收为分号**）所致（§14 未 ✘：其 `依据` 为单行块，
  正文以子串形式仍在）。**无第二类偏差**。
- **忽略换行的逐字搬移**（`python tools/doc/check_verbatim_move.py <HEAD 版> <迁移后>`）在**全表口径**下报
  `MISSING_KINDS=107`：**逐条核对均为「40 处节标题补标签」+「41 个块首行的字段名改写」**（正文未丢），
  故本条机器判据在本批（**字段名允许改写**）下**不适用**，改以上表 A / C / D 三列为准。

### 3.3 令牌集合机检（`build/s256/token_diff.py`；比对限界表迁移前 ↔ 迁移后 ∪ `PD-18~25`）

| 集合 | 迁移前 | 并集 | **MISSING** | ADDED |
|---|---:|---:|---:|---:|
| `§x.y` 形编号 | 87 | 90 | **0** | 3（`§10` / `§14` 来自「原登记位置」行、`§256` 来自留痕句） |
| 反引号内路径 / 符号 | 455 | 466 | **0** | 11（`PD-18` ~ `PD-25`、`ISSUE-P3-07` / `ISSUE-P1-9`、`已知工程限界.md`） |
| 数字常量 | 248 | 249 | **0** | 1（`23`，来自 `PD-23`） |
| **合计** | | | **`MISSING_TOTAL=0`** | |

（同一脚本对「迁移后限界表 ∪ **整份** `产品裁决登记.md`」复跑亦为 `MISSING_TOTAL=0`，读数
`§ 87→100 / 反引号 455→748 / 数字 248→311`——ADDED 更大是因为把该文件**既有内容**一并计入并集。）

### 3.4 迁出 8 节去向（`PD-18` ~ `PD-25`）

| PD | 原节 | 标题 | 对应条目 |
|:--:|:--:|---|---|
| `PD-18` | §1.7 | 保存路径的整树重建与附件字节副本是**别名隔离契约的代价** | `ISSUE-P3-150`（§115 复核）；与 `ISSUE-P3-07` / `ISSUE-P1-9` 的别名隔离契约相关 |
| `PD-19` | §9 | OTP 取码的 `Mac` 实例复用**刻意不实施** | `ISSUE-P3-172` ④ |
| `PD-20` | §10 | 同步「内容是否变化」与合并「是否被修改」两处判定口径**刻意不同** | `ISSUE-P2-91` |
| `PD-21` | §11 | 基线快照与缓存快照**各写一份** | `ISSUE-P3-167` |
| `PD-22` | §14 | 会话级「内容摘要 → 已解析树」缓存**刻意不实施** | `ISSUE-P3-168` ② |
| `PD-23` | §18 | 「门面型」文件的行数下限：授权**已收窄**为「不得新增含实现体的成员」 | `ISSUE-P3-188` / `ISSUE-P3-250` |
| `PD-24` | §20 | 函数面行数下限：`processFillRequest` 的早退结构**接受为限界** | `ISSUE-P3-188` / `ISSUE-P3-250` |
| `PD-25` | §21 | 供应链扫描面 = **生产可运行面**：AGP 工具链配置族不入扫描 | `ISSUE-P2-219` |

**依据块末行的两处已知偏差（本批有意为之 / 已修，见 §4.0）**：① 各行尾按体例接上
「；迁出留痕见批次正文 §256。」（原行末句号**收为分号**，故 §3.2 的 B 列对 7 个 `依据` 块末行报 ✘）；
② 首版曾误写成「**。；**」（句号后紧跟分号），**同一批次内已修**。

**字段装配**（每条均含 `对应条目 / 原登记位置 / 口径 / [理由] / 不得外推的边界 / 重开条件 / 依据`）：
§1.7 与 §18 另带原样的「同批确认的真实成本面（已修）」/「判据（为何其余部分刻意不拆）」段；
§21 的「配置族与其承载构件 / 血缘」5 块随 `口径`、「机检」块随 `理由` 一并迁入；
§10 / §11 原节无 `事实` 段，其 `口径` 即原 `理由` / `事实` 段本身（**不另撰**）。

---

## 4. §13 核查结论（任务 E）：**补登 `ISSUE-P3-251`，未就地补字段**

**判据（限界表文首第 8 行「登记纪律」）**：凡不打算整改的缺陷 / 残余风险必须登记
「事实 → 边界 → 依据 → 解除条件」；**§13 只有 `解除条件` + `依据`**（缺「事实」与「边界」）。

**取材核查（2026-09-21，定向 `grep`，未通读全仓）**：

| 来源 | 「事实」段 | 「边界」段 |
|---|---|---|
| `docs/resolved/batches/139-验证器页倒计时解耦批次.md` §5.6 | **有**（整段，但其中「由列表页持有…同一对象」已被 §248 `B18` 更正为「两页**各持独立实例**」） | 无 |
| `docs/resolved/batches/114-列表页秒级重建与TOTP重算收敛批次.md` | 仅 1 处提及（非该面陈述） | 无 |
| `docs/resolved/batches/120-设备无关项收尾批次.md` | 零命中 | 无 |
| `docs/records/限界表证据与实测数据汇录.md` §13 | **有**（§253 从限界表移出的原文，含 `TotpCountdownTracker.kt:155-163`、`WhileSubscribed(5000)`、`ISSUE-P2-65`「明文驻留点」口径） | 无 |

**结论**：「事实」段可在 1 处逐字取材，「**边界**」段在三份批次正文与汇录中**均无对应句**——
自行撰写即等于编造一条硬约束。故本批**未**补字段，改为按「取材不到」路径补登
`ISSUE-P3-251`（P3 区 `0 项 → 1 项`，含核实时间点 / 方式、可取材来源、未补原因、整改方向、验收标准、依据指针）。

---

## 5. 机检与验证读数（一律现跑）

| 命令 | 输出要点 | 判定 |
|---|---|:--:|
| `python tools/doc/check_md_links.py` | `BROKEN_MD_LINKS=0` | 通过 |
| `python tools/doc/check_resolved_index_sync.py` | `RESOLVED_INDEX_SYNC=OK（批次正文 254 份；分册登记 256 条；最大 §256）` | 通过（归档后复跑） |
| `bash tools/audit/check_recheck_consistency.sh` | `PASS: 无残留禁用短语（已扫描 1326 行，11 条禁用短语）` | 通过 |
| `python build/s256/token_diff.py …`（临时脚本） | `MISSING_TOTAL=0`（见 §3.3） | 通过 |
| `python build/s256/verify.py …`（临时脚本） | A / C / D 全 ✔、B 列 7 处 ✘（见 §3.2） | 通过 |
| `./gradlew.bat test --max-workers=1` | `BUILD SUCCESSFUL in 2s` / `114 actionable tasks: 1 executed, 113 up-to-date`；**唯一实际执行的任务是 `:crypto:cargoHostBuild`，五模块 `testDebugUnitTest` 全部 UP-TO-DATE ⇒ 未产生新的测试执行** | 通过（本批零代码改动） |
| 上表四项**在 §5A.0 标点收口后复跑** | `BROKEN_MD_LINKS=0`；`RESOLVED_INDEX_SYNC=OK（批次正文 254 份；分册登记 256 条；最大 §256）`；`PASS: 无残留禁用短语（已扫描 1326 行，11 条禁用短语）`；`MISSING_TOTAL= 0`（§ 87→90 / 反引号 455→466 / 数字 248→249）；`grep -rn "。；" docs/` 的 7 处命中**全为本批对自身的引述文字**（承载文件 `产品裁决登记.md` 命中 0） | 通过 |
| `python tools/doc/count_test_results.py`（读数，非本批新执行） | `xml=353 tests=2467 failures=0 errors=0 skipped=13` | 与 §255 基线一致 |

**行数 / 字节（`wc -lc`）**：

| 文档 | 迁移前 | 迁移后 |
|---|---:|---:|
| `architecture/已知工程限界.md` | 665 行 / 70 826 B | **639 行 / 66 226 B** |
| `architecture/产品裁决登记.md` | 605 行 / 56 539 B | **770 行 / 75 157 B**（含 §5A.0 的标点收口） |
| `docs/ACTIVE_ISSUES.md` | 163 行 | **230 行**（补登 `ISSUE-P3-251` / `ISSUE-P3-252`） |

限界表 `>110 字符行 = 0`（迁移后现查，含本批新写的文首块与 4 处折行重排）。

**未跑设备测试**：本批无代码改动、无 `*/src/androidTest/**` 改动 ⇒ 按「测试资产纪律」② **无设备侧必跑项**。

---

## 5A. 施工期自纠与分批发现的缺口

### 5A.0 本批**引入并当场修掉**的畸形标点（同一批次内闭环）

迁出时按体例在 8 条 `依据` 块末行接上「；迁出留痕见批次正文 §256。」，**首版**生成后各末行出现
`。；`（句号后紧跟分号）——`grep -n "。；" docs/architecture/产品裁决登记.md` **命中 8 处**（`PD-18` ~ `PD-25`
各 1 处，行 612 / 626 / 646 / 662 / 680 / 703 / 723 / 755）；全仓 `docs/` 范围内该形态**仅此 8 处**，
即**全部为本批引入**。处置：把 `。；迁出留痕…` 收为 `；迁出留痕…`（8 处一次替换），复核后
`grep -n "。；" docs/architecture/产品裁决登记.md` **命中 0**（全仓 `grep -rn "。；" docs/` 另有 **7 处**
命中，**全部是本批次正文与两处索引行对该畸形标点的引述文字**，非实际行文）。**该畸形由本批引入、在本批内修掉**，未流入后续批次。

### 5A.1 分批登记的缺口一：`ISSUE-P3-251`（限界 §13 缺「事实 / 边界」）

**未就地修的原因**：`边界` 段在批次 139 / 114 / 120 与汇录 §13 中**均无可逐字搬移的对应句**
⇒ 自行撰写即等于**编造**一条硬约束。故**不补字段**、改为登记条目（详证见 §4）。
「事实」段的可取材来源已逐条写入条目，后续批次可直接搬移。

### 5A.2 分批登记的缺口二：`ISSUE-P3-252`（附二 交叉引用悬空）

**未就地修的原因**：「附二」正典该归**限界表**还是归**汇录**属**独立一次处置**（两种收口方向都要动
另一份文件的标题结构与全部引用），**不属于**本批「分类标签 + 口径裁决迁出」的面；且本批搬移纪律
只允许改字段名与新增框架行，**改锚点目标即等于改链接语义**。故本批**一律不改链接语义**，
只登记缺口与两个处置方向（详证见 §7.8）。

### 5A.3 留痕一：`check_verbatim_move.py` 在本批**不适用**

该脚本按「**行多重集**」比对，而本批**允许改写字段名**（`事实`→`口径` 等），必然被判缺行 ——
实测 `orig_content_kinds=507 / MISSING_KINDS=107`，逐条核对**全部**是「40 处节标题补标签」＋
「41 个块首行的字段名改写」，**正文一字未丢**。故改以 §3.2 的**块级判据 A / C / D**（全 ✔）为准。

### 5A.4 留痕二：首版存根生成脚本按**原文行号**取值导致整体错位一节

首版脚本在列表已被改短后仍用**迁移前**的行号去取值，致 8 节存根**整体错位一节**
（如 §18 的存根里出现 §17 的「解除条件」、§20 的存根只剩 §18 边界块的首行）。
改为对**迁移中的列表**取值后复跑，并以 A / C / D 三列判据复核；**中间态未提交**，仅此登记。

### 5A.5 留痕三：两处**字段标签**的复核更正（主代理复核时现查，本批内收口）

机械字段名映射产生了两处**标签与内容不符**，已在本批内更正（**不涉及任何结论句**，被改的仍只是字段名）：

1. **`PD-20`**（原 §10）：原节第二条边界句在源头是**无标签**的 `- **⇒ 不得**据本表…`，映射时被补成了第二个
   `- **不得外推的边界**：`，使该条出现两个同名边界字段。现**还原为无标签的 `- **⇒ 不得**…`**（原文形态）。
2. **`PD-22`**（原 §14）：原节的 `- **条目原文**：…` 被映射成了 `- **不得外推的边界**：**条目原文**：…`——
   内容不是「不得外推」的边界而是条目自身原文。现**还原标签为 `- **条目原文**：`**。

复核口径：对 `PD-18` ~ `PD-25` 逐条**现查字段标签序列**，与 `git show HEAD:docs/architecture/已知工程限界.md`
各原节的标签序列逐条对拍，其余 6 条的字段映射（`事实` / `裁定` / `裁决` → `口径`、`边界` → `不得外推的边界`、
`解除条件` → `重开条件`）与 `对应条目` / `原登记位置` / `迁出留痕` 三处新增**全部对得上**，
唯上述两处不符；更正后重跑 §5 的四项机检，读数不变。

---

## 6. 文档同步

1. [`docs/RESOLVED_LOG.md`](../../RESOLVED_LOG.md)：新增 §256 一行（索引）。
2. [`docs/resolved/README.md`](../README.md)：最大编号 `§255 → §256`、下一批 `§257`（该行是唯一维护处）。
3. [`docs/resolved/BATCH_158_PLUS.md`](../BATCH_158_PLUS.md)：补 §256 一行。
4. [`docs/ACTIVE_ISSUES.md`](../../ACTIVE_ISSUES.md)：P3 区 `0 → 2 项`，补登 `ISSUE-P3-251` 与 `ISSUE-P3-252`，
   并把该区「暂无开放项」改写为**两条并列**的形态、「本区近期变动」句中的「本区由此归零」改为
   「本区**当时**由此归零」。
5. `docs/README.md` **未动**：本批**未新建任何文档**（批次正文属 `resolved/batches/` 既有登记面，按规则不另行登记）。

---

## 7. 如实声明

1. **本批零结论变更**：分类标签只回答「谁能解除」；8 节口径裁决**逐字**迁入 `PD-18` ~ `PD-25`
   （唯一的字面改动是字段名与两行框架行），限界表存根保留全部含「不得」的规范句。
   未重跑任何实测，**未新增 / 未闭环任何原 `ISSUE-*`**；`AGENTS.md` 一字未动。
2. **一处过程中的自纠**：首版存根生成脚本在「列表已被改短后仍用**原文**行号取值」，导致 8 节存根**整体错位一节**
   （§18 存到 §17 的解除条件等）。改为对**迁移中的列表**取值后复跑，并用 §3.2 的 A / C / D 三列机器判据复核；
   中间态**未提交**，仅此登记。
3. **`check_verbatim_move.py` 在本批不适用**：该脚本按「行多重集」比对，**字段名改写**必然被判缺行；
   故本批以 §3.2 的块级判据（A / C / D）替代，并如实登记 `MISSING_KINDS=107` 的逐条性质。
4. **§13 未补字段**：理由与可取材来源见 §4，已转登 `ISSUE-P3-251`（**未编造事实与边界**）。
5. **`产品裁决登记.md` 的分工段已重写**（任务 D）：如实表述三分法（限界表两类 + 本表口径裁决），
   并写明 `PD-18` ~ `PD-25` 系 §256 逐字迁入、结论零变更；**该文件原有的「措辞纪律」句一字未动**。
6. **未触**生产 / 测试代码、`*/src/androidTest/**`、原生面与 `参考项目/`；未新增依赖。
7. **工作区行尾**：本仓 `docs/**.md` 入库态为 **CRLF**（`git cat-file` 实测 HEAD blob 即含 CR；`.gitattributes`
   的 `* text=auto eol=lf` 尚未对存量文件生效），本批写入的限界表与 `产品裁决登记.md` **与该现状一致**，
   `git status --short` 仅列本批 6 个改动文件 + 1 个新增批次正文。
8. **一处随迁的「页内锚点」副作用（如实登记，**本批不改链路语义**，已转登 `ISSUE-P3-252`）**：
   三处 `依据` 块末尾原有指向 `[附二](#附二更正留痕按日期)` 的**页内锚点**（§11 / §18 / §20），
   随 `依据` 段**逐字**迁入 `PD-21` / `PD-23` / `PD-24`；该锚点指向的是**限界表**的
   `## 附二：更正留痕（按日期）`，在 `产品裁决登记.md` 内**无对应标题** ⇒ 在 `PD-*` 内点击不跳转
   （`check_md_links.py` 只校验**文件级**可跳转，故不报红）。
   **现场进一步核实**：限界表正文共 **7 处**同款锚点（第 11 行、§2.4 / §5 / §8 / §13 / §19 / §24），
   而限界表自己的 `## 附二` **只有 3 个无编号块**（§1.1 与 §1.3 的边界补充）——编号留痕实际在
   `docs/records/限界表证据与实测数据汇录.md` 的 `### ## 附二：更正留痕（按日期）` 段内（**21 条**，标题层级畸形）。
   **本批一律不改链接语义**（正典归属属独立一次处置面），缺口与两个处置方向已登记 `ISSUE-P3-252`
   （见 §5A.2）。
