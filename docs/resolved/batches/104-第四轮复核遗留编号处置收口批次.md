<a id="s104"></a>
## §104 第四轮复核遗留编号处置收口批次（2026-09-16，docs + 报告交叉引用）：ISSUE-P3-141

> **批次正文体例说明**：本批**以** [`docs/security/第四轮复核遗留编号处置.md`](../../security/第四轮复核遗留编号处置.md)
> 为**唯一真相源**（386 行，含 25 条逐条对照表、编号清单再核定、18 份逐份退役判定、可直接粘贴的 3 条新条目正文）。
> 本节**不复制**该文件的结论表，以避免产生第二份会漂移的快照；只收录：条目原文（原样搬迁）、验收标准逐项对照、
> 两处前提更正、以及验收证据。

---

### 104.1 条目原文（自 `ACTIVE_ISSUES.md` 原样搬迁，**不改写**）

> ### ISSUE-P3-141：第四轮复核分批判定的处置状态不可核对
>
> - **优先级**：P3
> - **当前状态**：**开放项**——22 个仅存在于复核工作目录的编号尚未逐条给出结论。
> - **来源**：第四轮复核工作目录（`.audit-recheck/`，`.gitignore` 排除）。
> - **核实时间点与核实方式（2026-09-16，对 HEAD `ba11edc`）**：以正则对拍「工作目录 `B01`~`B09` 分批判定文件」与「`SECURITY_RECHECK_2026-09.md` + `ACTIVE_ISSUES.md` + `RESOLVED_LOG.md`/批次正文」的全部编号。
> - **问题描述**：得 **22 个编号仅存在于工作目录**（`ANDROID-01/06/10`、`ISSUE-P1-04/05`、`ISSUE-P2-08`、`ISSUE-P3-25/40`、`NEW-B01-1/5/6`、`NEW-B02-1/5`、`NEW-B04-01/03/04/05/06`、`NEW-B09-01~05`）。**已核实为真实缺口**：`NEW-B02-5`（建库预设算法与 `createDefault` 默认值不一致）与 `NEW-B02-3`（末尾残块明文副本）均属真缺陷，却**从未进入报告或本表**（两条已于 §102 批次以 `ISSUE-P2-85` / `ISSUE-P3-140` 修复）；报告 §6.8 只逐条裁定 `IPC-*`/`SUPPLY-*`，`NEW-B*` 仅少量被 §8 根因矩阵引用。**其余判定的逐条复核未完成**，故登记为开放项（**不伪闭环**）。
> - **验收标准**：① 对上述 22 个编号逐条给出「已由某条目覆盖 / 已在本批修复 / 确属新缺陷 / 前提不成立」四选一结论**并落到文档**（不得只留工作目录）；② 确属新缺陷者登记到本表；③ 结论须与 `SECURITY_RECHECK_2026-09.md` 交叉引用，使「复核未遗留未登记缺陷」从**断言**变成**可核对**；④ 同时评估是否把工作目录的关键分批判定（`.gitignore` 排除、不随仓库分发）按退役纪律分流入库。

> **2026-09-16 就地标注更正（`ISSUE-P3-145` AC④；原句按归档纪律「只搬迁不改写」保留）**：上句「报告 §6.8 只逐条裁定 `IPC-*`/`SUPPLY-*`」**口径过宽**，应读作——待复核区全域为 **19 项**
> （`IPC-01…IPC-11`、`SUPPLY-01…SUPPLY-08`），其中 **10 项已逐条裁定**（§6.8 九项 `IPC-01/02/05/10`、
> `SUPPLY-02/04/06/07/08`；另 `SUPPLY-05` 由 §6.6 裁定），其余 **9 项**（`IPC-03/04/06/07/08/09/11`、`SUPPLY-01/03`）
> 为**重合归并**——去向为 `IPC-03→P2-70`（§53）、`IPC-04→P3-88`（§65）、`IPC-06→P2-72`（§48）、
> `IPC-07→P2-51` + `P3-84`（§48）、`IPC-11→P2-46`（§75，CM 通道另见 `P2-83` §81/§82）、`SUPPLY-01→P2-54`（§49）、
> `SUPPLY-03→P2-55`（§49）、`IPC-08`/`IPC-09` →「产品裁决 / 设计边界」（§29.3 / §72 与 `P3-76`）。
> 该 9 项的重合声明**已逐条核实**（判定 **7 项成立 / 2 项部分成立**），**2 处独立残余**见其 §3.1 / §3.2；
> 核实落点为 [`docs/security/待复核区IPC与SUPPLY项重合声明核实.md`](../../security/待复核区IPC与SUPPLY项重合声明核实.md)（本批 §106 归档时同批入库）。
> 同理，本条目正文另称 `NEW-B02-3`「**从未进入报告**」亦**过宽**——它已在报告 §11 的 INFO 级清单中逐字列出（见 104.4 第 1 条）。

---

### 104.2 验收标准逐项对照

| AC | 结论 | 落点 |
|:--:|---|---|
| ① 逐条四选一结论并落到文档 | **完成**——实际覆盖 **25** 条（见 104.3 口径说明），逐条四选一 + `文件:行号`/commit 证据 | `docs/security/第四轮复核遗留编号处置.md` §3.1~§3.6 |
| ② 确属新缺陷者登记到本表 | **完成**——3 条：`ISSUE-P3-143`（`KdbxHeader` 相等性对 KDF secret 不敏感，当前零消费点）／`ISSUE-P3-144`（`armScheduledClear` 的 `customTimeoutSeconds` 被用户设置静默否决）／`ISSUE-P3-145`（报告「待复核区已逐条补齐」断言与实况不符，10/19） | `ACTIVE_ISSUES.md` 新增三条；正文出处为该文件 §4.1~§4.3 |
| ③ 与复核报告交叉引用 | **完成**——报告 §6.8 之后新增 `### 6.8.1 复核工作目录独有编号的逐条处置（ISSUE-P3-141 收口）` | `docs/security/SECURITY_RECHECK_2026-09.md` §6.8.1 |
| ④ 工作目录分流入库评估 | **完成**——结论：**应「分流结论」，不应「整份入库」**；工作目录（18 份 / 1.4 MB）判定为「结论已分流、可退役」，`.gitignore:123` 排除**维持不变**；并出 18 份**逐份**处置表 | 该文件 §6.1~§6.3 |

---

### 104.3 口径说明：覆盖范围是 25 条，不是 22

`ISSUE-P3-141` 正文自称 **22**，但其**自身枚举为 23**（逐项计数 3+2+1+2+3+2+5+5）；而 §102 批次正文
（`docs/resolved/batches/102-建库预设算法接线与末尾残块明文清零批次.md:117-128`）的枚举集合与它**并不相同**
（多 `ANDROID-02` / `NEW-B02-3`，少 `ISSUE-P3-25` / `ISSUE-P3-40`）。故本批按**两处枚举的并集 25 条逐条处置**，
其中 23 行即 `ACTIVE_ISSUES.md` 原枚举的全覆盖。**「22」这一计数在两处枚举中均无法复原**——这本身就是
「正则对拍口径未固定」的留痕，已固定为**并集**口径。

---

### 104.4 前提更正（三处）

1. **`NEW-B02-3`「从未进入报告」不成立**：报告 §11 的 INFO 级清单（`SECURITY_RECHECK_2026-09.md:842`）**逐字列出**
   `NEW-B02-3（flushBlock 的 chunk）`，§8 `RC` 矩阵（`:646`）亦引用。准确表述应为
   「**被引用过，但从未成为独立条目、也未被任何批次整改**」（§102.2 原文即此表述，准确）。
2. **`NEW-B01-6` 的解除依据须补全**：§102.3 写作「属**有意设计**而非缺漏」——方向正确但不完整。
   真正解除依据是**「全仓相等性零消费点」+「有意设计」两个条件的合取**：因为 `KdbxHeader.kt:39` **确实**比较
   `kdfParameters`，原复核者（标 `[U]`）担心的链路**客观存在**，仅因无消费点而不可利用。
3. **`IPC-*`/`SUPPLY-*` 并非全部逐条裁定**：报告 §1（`:88`）「本轮已逐条补齐（§6.8）」与 §2（`:167`）
   「10+ 条线索从未获结论（**本轮补上**）」两处口径过宽——待复核区全域 **19** 项（报告 `:87` 自述引 `RESOLVED_LOG` §43.5），
   §6.8 只逐条裁定 **9** 项 + §6.6 裁定 `SUPPLY-05`，共 **10/19**；其余 9 项（`IPC-03/04/06/07/08/09/11`、`SUPPLY-01/03`）
   仅有登记表 `00-registry-and-plan.md:341` 的**重合声明**（原文自注「须复核其声明是否正确」——**从未复核**）。
   **口径更正与本条 AC 已落为独立开放项 `ISSUE-P3-145`**（其 AC① 即更正报告 §1/§2 措辞，AC② 即逐条验证这 9 项的重合声明）。

> **另两处机制性坑（已单列成节）**：**M-1 报告使用别名**——报告 §11 `:836-837` 把工作目录 `NEW-B09-01` 记作
> `NEW-B09-x`、`NEW-B04-06` 记作 `NEW-B04-x` ⇒ **纯 ID 对拍必然把它们误判为「未进报告」**；
> **M-2 假阳性**——`ISSUE-P1-04/05`、`ISSUE-P2-08`、`ISSUE-P3-40` 在工作目录中是**被引用的既有已闭环条目**
> （以源码注释形态出现），不是复核新判定。

---

### 104.5 验收证据

- **主控独立复核（不照抄子代理结论）**：对编号清单本身做了**对称展开**复算，独立撞出 §104.3 的
  `ANDROID-02` 缺失与 `ANDROID-10` 仅以 `ANDROID-06/10` 压缩形态存在的问题，并实测
  **「字符串出现 ≠ 处置已落档」**（把 `ACTIVE_ISSUES.md` 计入仓库侧会使条目自身列出的编号**自我豁免**，
  范围在 19 / 2 之间跳变）⇒ 该结论只能走逐条人工判读，不能用集合差集代替；子代理已据此单列 §2.1 判读口径。
- **审计一致性闸门**：`bash tools/audit/check_recheck_consistency.sh` → `PASS`（1294 行 / 11 条禁用短语）。
- **新条目的承重论断经主控独立复核**：`ISSUE-P3-144` 的两条前提（`ClipboardSecurityManager.kt:181` 早于 `:183`；
  全仓 `customTimeoutSeconds` 无生产调用方传入非空值）已由主控 `grep`/直读**逐条证实**，非照抄。
- **行号漂移实测**：报告 §6.8 `IPC-02` 行写的 `PasskeyAssertionActivity.kt:112` → 实测 **`:144`**（另 `:78` 同型）；
  `B09-findings.md:1571` 的 `endpointOverride` 字段整改后**已消失**（现为 `DigitalAssetLinksVerifier.kt:45-54` 的只读策略注入）。

---

### 104.6 边界与残余（如实声明，**不得**读作已核实）

1. `ANDROID-01/02/06/10` 四项在原件中**只有编号、无正文**；其结论据原报告**自身同组陈述**推得，属「最高可核结论」，
   **无法排除**该编号另有所指。
2. 工作目录约 1.1 万行**未整份通读**；结论分流落点为**抽样核对**（B01/B02/B04/B09 逐条，其余按标题对报告节），
   **不构成**「工作目录内不存在其它未登记缺陷」的证明。
3. `IPC-*`/`SUPPLY-*` 那 9 项的**重合声明是否成立未验证**——即 `ISSUE-P3-145` AC②，仍开放。
4. `NEW-B09-02` 涉及的「CM / 通行密钥通道是否支持 Firefox」**未做设备侧验证**。
5. `.audit-recheck/00-registry-and-plan.md:1` 自称「第二轮」而待办称「第四轮」——同一批产物命名不一致，已留痕。
6. 全部行号均为 **2026-09-16 / HEAD `2e5ce38`** 快照（条目原文所记 `ba11edc` 是 §102 的基线）。

---
