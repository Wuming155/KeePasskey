# §181 长函数度量工具化与第 3 目快照分流批次（`ISSUE-P3-188` 文档面 + 一处**结论失真**更正）

> **起因**：剩余清单第 6 项（§176 登记）——「违例清单第 3 目」的 20 处函数名快照属历史内容，
> 按索引纪律应**先分流留痕、再压缩**。分流前先复核，就势把 §175 用一次性脚本量出来的数字
> **工具化**（`tools/doc/long_functions.py`），结果**量出一条不实的结论**（见 §2）。

---

## 1. 新增度量工具，以及它自己踩的两个坑

| 版本 | 报「≥100 行函数」 | 实情 | 病因 |
|---|---:|---:|---|
| §181 首版 | **19** | 应为 28 | 判「单表达式函数」时在**整段函数文本**里找第一个 `=` ⇒ 形参默认值 `modifier: Modifier = Modifier` 抢先命中，**带默认形参的块体 Compose 函数被整条误判成单表达式而漏报**（少 9 条） |
| §181 二版 | **0** | 应为 28 | 定位函数名的正则把参数表的左括号一起吃掉，括号配平从「参数表内部」开始 ⇒ 永远等不到 `depth==0` |
| §181 终版 | **28** | 与已知值逐条吻合 | 判据改为**先配平参数表右括号，再看其后第一个是 `{` 还是 `=`** |

三处已知值反校（工具必须用已知值校准，不校准则「测出来多少」毫无意义）：
`keepasskeySettingsNavGraph` **252** ✓、`CreateVaultWizardDialog` **158**（§179 后）✓、
`OpenExistingVaultDialog` **96 ⇒ 不得出现在 ≥100 清单里** ✓。

已登记进 `AGENTS.md` §5 命令表（与 §177 的 `count_line_tiers.py` 同处）。

## 2. 更正一条失真结论（本批真正的存量问题）

`ISSUE-P3-188` 的「当前进度 · 第 3 目」自 §175 起写的是：

> **非 Compose 逻辑函数已清零**（清单内 20 处全部降到 **≤50 行**）

实测（`python tools/doc/long_functions.py 40`，本批）：

| 函数 | 现值 | 结论 |
|---|---:|---|
| `autofill/KeePasskeyAutofillService.kt::processFillRequest` | **55** | **超出验收线 5 行** |
| `sync/merge/KdbxEntryMerger.kt::mergeConflictedEntry` | **52** | **超出验收线 2 行** |
| 其余 17 处（含 `saveEntryInternal` / `startCreation` / `buildPasskeyEntries` / `analyzeEntries`） | ≤40 | 确实达标 |

⇒ 「清零」的说法**不成立**，已改为「**≥100 行已全部消除，非 Compose 面尚余两处 52 / 55 行未达 ≤50**」，
并把这两处登为剩余清单第 6 项（附本批实测数与出处；该槽位原为「快照分流」，本批完成后就地改写）。**不是把验收线放宽到 55，而是把结论改准**——
前者是「为凑绿放宽判据」，后者是如实登记。

## 3. ≥100 行的当前清单（28 个，全部 Compose 面；即剩余清单第 1 项的工作量）

```
252 keepasskeySettingsNavGraph      227 HealthCheckScreen           202 KeePasskeyApp
172 ConflictResolutionScreen        170 ChildDatabaseDialog         168 Argon2ParametersDialog
164 keepasskeyNavGraph              158 UnlockStandardUnlockContent 158 CreateVaultWizardDialog
153 AboutSettingsScreen             151 EntryDetailTopBar           131 PackageBlocklistManageDialog
130 VaultListDialogHost             121 GeneratorContent            121 EntryDetailScreen
119 SyncStatusCard                  119 EntryDetailDialogHost       118 themeListSection
118 BasicCredentialsCard            117 VaultDatabaseCard           116 HealthCheckScoreCard
116 MasterKeyChangeDialog           115 PrivilegedBrowserSettingsScreen
113 ChildVaultEntryRowView          113 S3ConfigFields              108 EntryEditCustomFieldsSection
107 SafeAttachmentPreviewDialog     104 KeePasskeyTheme
```

与 §175 表**同为 28 个但成员已不同**：§178 消掉了 `OpenExistingVaultDialog`（226→96），
而 §175 的一次性脚本因同样的「`= ` 抢先命中」缺陷**漏报**了 `BasicCredentialsCard` / `VaultDatabaseCard` /
`HealthCheckScoreCard` / `MasterKeyChangeDialog` / `PrivilegedBrowserSettingsScreen` /
`ChildVaultEntryRowView` / `S3ConfigFields` / `EntryEditCustomFieldsSection` / `SafeAttachmentPreviewDialog` /
`KeePasskeyTheme` 等 100~118 区间项（§175 当时只笼统记作「另 10 个」）。
⇒ 本目今后的计数**一律以工具为准**，条目内不再抄录逐个数字，只留指针与本清单出处。

## 4. 违例清单第 3 目的历史快照（自 `ACTIVE_ISSUES.md` **原样迁入**，此后该目只留指针）

核实时刻（2026-09-18）列出的 20 处 ≥100 行函数原文：

> `SyncEngine.openRemote`(≈173)、`PasskeyAssertionActivity.onCreate`(≈153)、
> `SyncCycleRunner.runSyncCycle`(≈130)、`KdbxEntryMerger.mergeConflictedEntry`(≈129)、
> `VaultEntryWriteCoordinator.saveEntryInternal`(≈121)、`KeePasskeyAutofillService.processFillRequest`(≈120)、
> `SyncConflictController.handleConflictMerge`(≈104)、`ExtendedSettingsStore.load`（≈89 行签名起，含 KDoc 记 131）、
> `PasswordFillActivity.onCreate`(≈108)、`PasskeyCreateActivity.startCreation`(≈100)、
> `KeePasskeyCredentialProviderService.buildBeginCreateResponse`(≈97)、`CredentialResponseAssembler.buildPasskeyEntries`(≈93)、
> `InnerHeader.deserialize`、`KdbxKeyDerivation.deriveKeys`、`KdbxXmlMetaSerializer.serialize`、
> `VaultEntryMapper.mapKdbxEntryToUi`(≈112)、`HealthCheckEngine.analyzeEntries`(≈106)、
> `BiometricEnrollmentCoordinator.requestBiometricEnrollment`(≈134) 等

**迁入理由**：其中 `mergeConflictedEntry` / `saveEntryInternal` / `processFillRequest` / `startCreation` /
`buildPasskeyEntries` / `analyzeEntries` **六处在 `docs/resolved/` 内零命中**（§176 实测 `grep -rl`），
直接删掉就失去「当初审过哪些」的唯一文字记录（§147「资产不因暂时用不上而删除」同源）；
本文件即其**分流落点**，此后按 `§号 + 函数名` 检索即可命中。

## 5. 验证

| 项 | 结果 |
|---|---|
| `python tools/doc/long_functions.py`（终版） | `files_scanned=502`、`functions_ge_100=28`，与三处已知值逐条吻合 |
| `python tools/doc/long_functions.py 40` | 命中 `processFillRequest` 55 / `mergeConflictedEntry` 52 ⇒ 触发 §2 的结论更正 |
| `python tools/doc/check_md_links.py` | `BROKEN_MD_LINKS=0`（本批新增文档与 `AGENTS.md` 链接均可跳转） |
| `python tools/doc/count_line_tiers.py` | 第一档 2、第二档 29（本批零 `.kt` 改动） |
| `.\gradlew.bat test --rerun-tasks --max-workers=1` | `BUILD SUCCESSFUL in 2m 11s` / `114 actionable tasks: 114 executed`；聚合 **tests=2198 failures=0 errors=0 skipped=13**，`Uncaught exception` 命中 **0**（与 §176~§180 逐项一致；本批零 `.kt` 改动） |
| `ACTIVE_ISSUES.md` 行数 | **313 → 310**（`git show HEAD:` 与 `wc -l` 对拍）：迁出的是违例清单第 3 目的 **8 行快照**（原样收录于本批 §4）与 §175 的判据 / Top 说明（已固化进 `long_functions.py` 文档串）；换入的是**更长的结论更正说明** ⇒ 本批价值在「把不实结论改准 + 数法收口」，不在行数 |
| **未执行** | `:app:lintDebug`（零 `.kt` 改动）、`cargo test`、`-DliveSyncTest`、四层 `connectedDebugAndroidTest`（无设备） |

## 6. 下一批

剩余清单第 6 项（本批就地改写登为）：把 `processFillRequest`（55）与 `mergeConflictedEntry`（52）削到 ≤50，
使第 3 目「非 Compose 面」真正达到验收线；之后回到第 1 项按 §3 清单继续 Compose 段落拆分。
