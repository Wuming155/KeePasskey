# §250 复核报告四行与代码现况对齐批次

> 对应条目：`ISSUE-P3-249`（**整条闭环**）
> 日期：2026-09-21
> 涉及文件：`docs/security/SECURITY_RECHECK_2026-09.md`（仅此一份报告正文）+ `docs/` 索引四件
> 性质：**纯文档批次**——只更正复核报告四处陈旧文字（**保留原文 + 就地加更正注**）；不改任何生产 / 测试代码、
> 不改 `参考项目/`、不改 `*/src/androidTest/**`

---

## 1. 前提复核

| 序号 | 报告原文前提（行号为施工前快照） | 核实方式 | 结论 |
|:--:|---|---|:--:|
| ① `:232` | `P2-65` 的判定依据写「**关键前提被静态否定**：全仓无锁态驱动的 UI 导航」 | 直读 `app/src/main/java/com/keepasskey/app/ui/KeePasskeyApp.kt:287-295`：`lockEvents.collect { if (latestRoute != Screen.Unlock.route) navController.navigate(Screen.Unlock.route) { popUpTo(0) { inclusive = true } } }`——**正是**锁态驱动的 UI 导航；限界 §13 的「库锁定后该页也已被移出返回栈」亦以此为据 | 前提**不成立**（原文与现况不符） |
| ② `:799` | 表项 10 仍写 `P2-73` AC② 改 `FLAG_MUTABLE`「**不可照做**（本仓这两条路径不消费 fillIn extras）」 | 直读 `app/src/main/java/com/keepasskey/app/autofill/`：`AutofillDatasetBuilders.kt:113/165/348/379`、`KeePasskeyAutofillService.kt:461` 三处认证 / 保存 `PendingIntent` 均为 `PendingIntent.FLAG_MUTABLE or FLAG_UPDATE_CURRENT`；`AutofillAuthResultDelivery.kt:89` 为 `Intent().putExtra(AutofillManager.EXTRA_AUTHENTICATION_RESULT, dataset)` —— 即**现行实现已在照做**，且限界 §4.1 与 `docs/resolved/batches/110-…` / `111-…` 载明**真机已验证通过** | 前提**已失效**（§110 / §111 修复后） |
| ③ `:609-610` | 仍把「CM 通道 `UNBOUND` 越权面**未进** `已知工程限界.md` 登记表」列为独立残余 | 直读限界文件：`## 6. CM（通行密钥）通道「未绑定包名」的退回放行口径（IPC-11 族，P2-83 同族）` **已存在**（`已知工程限界.md:452`），其末条依据**自述**该节系「2026-09-16 复核发现的**登记缺口**……按本文件登记纪律补登」 | 残余**状态已更新**（已登记） |
| ④ `:850` | `SUPPLY-06` 行仍记「`DomainMatcher.extractDomain` 是否允许 IP 字面量（**本轮未直读**）」 | 直读限界 **§25**（`已知工程限界.md:1083`，标题即 `ISSUE-P3-234` / `SUPPLY-06` 余项）与 `docs/resolved/batches/236-存量裁决与P3两项收敛批次.md:23`：§236 已直读定案 **IPv4 字面量可达、IPv6 被拒**，SSRF 该面**唯一覆盖层**为连接期 `SsrfGuardSocketFactory` | 前提**已失效**（§236 已直读定案） |

**核实方式**：四处均以**直读 HEAD 源码 / 限界表正文 + 批次正文交叉核对**完成，未在本批重跑任何用例（见 §4）。

**与定案规格的两处不一致（如实登记）**：

1. 规格（`ISSUE-P3-249` 正文 ③）称「限界 §6 已于 **§82 批次**补登」；**实测出处为 §106 批次**
   （`106-报告口径更正与重合声明核实批次.md`，2026-09-16，commit `1d4f251`，`ISSUE-P3-145` 收口时补登），
   §82 批次建立的是该节所引的 `UNBOUND` 边界口径本身（`82-…批次.md:37-40`，见限界 §6 依据行）。
   ⇒ 报告更正注按**实测出处**（§106）书写，并在其中同时标出 §82 的口径来源关系。
2. 规格称 `:850` 位于「表项」；实测该行位于报告 **§10.1「本轮无法完成、需设备侧 / 外部证据的项」**
   表格内（与 `P3-120` 行同表）。更正注按该表既有体例（`P3-120` 行的「已完成」标注法）书写。

---

## 2. 更正内容（四处，一律**保留原句 + 就地加更正注**）

### ① `:232` — `P2-65` 行（Finding Matrix §3.2.2）

- **原文（保留）**：证据列 `[V]` **关键前提被静态否定**：全仓无锁态驱动的 UI 导航
- **追加更正注**：`**（2026-09-21 更正，ISSUE-P3-249）该依据与 HEAD 现况不符**：app/src/main/java/com/keepasskey/app/ui/KeePasskeyApp.kt:287-295 的 lockEvents → Unlock + popUpTo(0) **正是**锁态驱动的 UI 导航（限界 §13 的「锁库后该页已移出返回栈」亦以此为据）⇒ 依据更正为「**锁态驱动的 UI 导航确实存在**」；本条**终评 LOW / 终态 CONFIRMED VULNERABILITY / 可利受限 / 修 P1 均不变**。`
- **结论不变**：只更正**依据**；`P2-65` 的终评、终态、可利、修复优先级与攻击链 `CHAIN-B` 归属**一字未动**。

### ② `:799` — Fix 陷阱清单表项 10（`P2-73` AC②）

- **原文（保留）**：陷阱列「改 `FLAG_MUTABLE` → **为对齐文档而降低安全性**」；正确做法列「**不可照做**（本仓这两条路径不消费 fillIn extras）」
- **追加更正注**（接在同一单元格「正确做法」列之后）：`**（2026-09-21 更正，ISSUE-P3-249）**上述前提已随 §110 / §111 修复失效：现行实现即 FLAG_MUTABLE + 经 EXTRA_AUTHENTICATION_RESULT 回传**完整**已填充 Dataset（AutofillDatasetBuilders.kt:113/165/348/379、KeePasskeyAutofillService.kt:461、AutofillAuthResultDelivery.kt:89），**真机已验证通过** ⇒ 本条陷阱对现行代码不再适用（指针：限界 §4.1；docs/resolved/batches/110-… / 111-…）。`
- 陷阱列与「正确做法」的**原句一字未删**；同表其余 22 行未触。

### ③ `:609-610` — §6.8.1 的 `ISSUE-P3-145` 补记引用块

- **原文（保留）**：`**（2026-09-16 补，ISSUE-P3-145 AC②③）**……另得 **2 处独立残余**（CM 通道 UNBOUND 越权面未进 已知工程限界.md 登记表；P2-54 的 AC② 分支保护必需检查无活动落点）。`
- **追加更正注**（其后**紧邻新增一段引用块**，原段一字未动）：`**（2026-09-21 更正，ISSUE-P3-249）**上条第 1 处独立残余「CM 通道 UNBOUND 越权面**未进**登记表」**状态已更新**：该取舍已在同一次 ISSUE-P3-145 收口工作中（§106 批次，2026-09-16，commit 1d4f251）补登为 已知工程限界.md **§6**（…）⇒ 该残余**不再是未登记项**；UNBOUND 仍为**明示边界**……第 2 处残余（P2-54 的 AC② 分支保护必需检查无活动落点）**不受本条更正影响**。`
- 定位与体例：直接指到限界 **§6** 与 §106 批次正文。

### ④ `:850` — §10.1 待验表 `SUPPLY-06` 行

- **原文（保留）**：`| SUPPLY-06 | DomainMatcher.extractDomain 是否允许 IP 字面量（本轮未直读） |`
- **追加更正注**（同一单元格内）：`——**（2026-09-21 更正，ISSUE-P3-249）已由 §236 批次直读定案**：IPv4 字面量**可过** PSL 下限、IPv6 **被拒**；SSRF 仅由连接期层 SsrfGuardSocketFactory 覆盖 ⇒ 结论落 已知工程限界.md **§25**（ISSUE-P3-234，来源 docs/resolved/batches/236-…）；**该项不再是待验项**。`
- 同行同表的其它待验项（`IPC-01` / `IPC-10` / `AP-43` / `P3-88` / `P2-54` / `P3-123④` / `M-1` / `M-2`）**一行未触**。

**纪律遵守**：未重排任何表格、未改动四行之外的内容、未删任何原句；四处更正注均带
`2026-09-21` 与 `ISSUE-P3-249` 可检索标记；四处改写文字均**避免**复述
`tools/audit/check_recheck_consistency.sh` 的禁用短语（§15.2(p) 教训），脚本现跑 PASS（见 §3）。

---

## 3. 工程验证（一律现跑）

| 命令 | 真实输出 | 判定 |
|---|---|:--:|
| `bash tools/audit/check_recheck_consistency.sh` | `PASS: 无残留禁用短语（已扫描 1326 行，11 条禁用短语）`（退出码 **0**） | 通过 |
| `python tools/doc/check_md_links.py` | `BROKEN_MD_LINKS=0`（退出码 **0**） | 通过 |
| `python tools/doc/check_resolved_index_sync.py` | `RESOLVED_INDEX_SYNC=OK（批次正文 247 份；分册登记 249 条；最大 §249）`（退出码 **0**；本文件入库后为 **248 份 / 250 条 / 最大 §250**） | 通过 |
| `.\gradlew.bat test --max-workers=1` | `BUILD SUCCESSFUL in 3s`、`114 actionable tasks: 1 executed, 113 up-to-date`，其中 `:sync:testDebugUnitTest UP-TO-DATE` ⇒ **未产生新的测试执行**（本批无代码改动，Gradle 判定全部单元测试任务为最新，**不得**读作「本批跑过测试」） | 无新执行（如实记账） |
| `python tools/doc/count_test_results.py`（补充读数，**非本批新执行**） | `xml=353 tests=2465 failures=0 errors=0 skipped=13`（脚本自行排除非 JVM 单测 XML `{'debug': 5, 'updateDebugScreenshotTest': 1}`）——该读数与 §249 批次**逐字相同**，即**仍为 §249 那一次实跑的遗留 XML**，本批未刷新 | 读数沿用（见 §4） |

> **`check_recheck_consistency.sh` 扫描行数变化**：§249 时为 **1317** 行 ⇒ 本批为 **1326** 行（四处更正注共 +9 行），
> 禁用短语条数不变（11 条），结果仍为 PASS ⇒ 本批改写未在任何行引入禁用短语。

---

## 4. 如实声明

1. **本批只改报告文字**：`docs/security/SECURITY_RECHECK_2026-09.md` 四处（含紧邻指针），
   外加 `docs/ACTIVE_ISSUES.md` / `docs/RESOLVED_LOG.md` / `docs/resolved/README.md` /
   `docs/resolved/BATCH_158_PLUS.md` 与本文件五处索引件。**未改任何生产代码、测试代码、资源、
   构建脚本、`参考项目/`、`*/src/androidTest/**`**（`git diff --stat` 可对）。
2. **未重跑任何设备侧证据**：本批**无设备**，`connectedDebugAndroidTest` 四层（`:app:` / `:crypto:` /
   `:database:` / `:sync:`）均**未执行**；② 的「真机已验证通过」是**引用** §110 / §111 批次与限界 §4.1
   的既有结论，**不是**本批的实测。
3. **四行的「代码现况」依据来源**：全部来自 §236 ~ §249 各批次已入库的**直读记录与用例结论**：
   ① = 本批直读 `KeePasskeyApp.kt:287-295`（+ 限界 §13 既有表述）；② = 直读 autofill 三文件 + 限界 §4.1 / §110 / §111；
   ③ = 直读限界 §6 正文（其依据行自述补登过程）；④ = 直读限界 §25 + §236 批次正文。
   **本批未在这些面重跑任何用例**，故本批**新增零证据**、只做「报告文字 ⇄ 既有结论」的对齐。
4. **`test` 为 `UP-TO-DATE`**：本批无代码改动，`.\gradlew.bat test` **未产生新的测试执行**（见 §3）。
   `count_test_results.py` 的 `xml=353 tests=2465` 是**上一批（§249）实跑的遗留读数**，
   本批既未刷新、也不得据此声称「本批测试全绿」。
5. **一处与规格的出处不一致已登记**：限界 §6 的实际补登批次为 **§106**（非规格所述 §82），
   §82 提供的是该节所引的 `UNBOUND` 边界口径；更正注按实测出处书写（详见 §1 末）。
6. **未跑 `lint`**、未跑 `assembleDebug` / `assembleRelease`：本批无代码 / 资源 / Manifest 改动，
   这些任务不构成判别性证据，故未执行，**不作为证据使用**。
7. **未跑 KPEX 互操作对拍**：本批未触 `.kdbx` 产物格式、`PasskeyData` schema 与 `PasskeyPkcs8Codec`，
   按 `AGENTS.md` 规则 8 不适用，**不对其作任何声称**。
