# §193 子库挂载对话框段落下沉批次（`ISSUE-P3-188` 第 1 目：`ChildDatabaseDialog` 170 → **97 行出表**、≥100 项 21 → 20、待拆 18）

> **起因**：§192 后表内第三大的非豁免项是 `ChildDatabaseDialog`（170 行）。
> 它含**子库主密码明文**，是 `SecureDialogFlagPolicyTest` 点名的敏感对话框，
> 因此本批的可搬面被两条硬边界夹住：**FLAG_SECURE 接线必须留在被点名的文件里**、
> **明文的所有权与擦除义务必须与其使用处同屏**。

---

## 1. 改动

| 对象 | 行数 | 说明 |
|---|---|---|
| `subscreens/ChildDatabaseDialogs.kt` | 397 → **276**（`+14 / −135`） | 根对话框 **170 → 97 出表**；剪除 6 条失效 import |
| `subscreens/ChildDatabaseDialogSections.kt` | **新建 208 行** | `ChildDatabaseDialogIntro`、`ChildDatabaseSourcePickers`、`ChildDatabaseMountListSection` + 随之迁走的 `private ChildDatabaseMountRow` |

≥100 行函数 **21 → 20**（`files_scanned` 507 → 508），扣除 PD-11 豁免的两个 NavGraph ⇒ **待拆 18**。
分档不变：第一档 2、第二档 28。两文件 397 → 484 行，新增全是 wrapper 签名 / import / KDoc。

**为什么仍另起新文件**（§192 立的口径）：原文件 397 行，就地追加 wrapper 必然越过 400
⇒ 凭空造一个新的第二档项。`ChildDatabaseDialogs.kt` 距 400 只有 3 行，**这次是贴着边界走的**。

**`ChildDatabaseMountRow` 为什么跟着走**：它在原文件里的**唯一调用方**就是被搬走的挂载清单段
（凭据补录对话框不用它）。留在原文件就得把 `private` 放宽为 `internal`——为搬移而放宽可见性
是给未来调用方开口子，故连函数一并搬移，在新文件里**仍是 `private`**。

## 2. 刻意**不搬**的两样（各有一条现场约束）

1. **`SecureDialogWindowEffect()`**：`SecureDialogFlagPolicyTest` 是按
   `(文件路径, 调用形态, 期望处数)` 逐文件计数的，`ChildDatabaseDialogs.kt` 必须**恰好 2 处**
   （挂载对话框 + 凭据补录对话框）。段落组件里若也放一处、本体少一处，计数虽仍为 2，
   但**「敏感对话框内容首槽施加窗口 flag」这条接线就离开了被点名的文件**——该守卫将不再能证明它。
   现两处均在原位：搬后 `grep -c "SecureDialogWindowEffect()" ChildDatabaseDialogs.kt` = **2**（实测）。
2. **子库主密码链路**：`password` 的 `CharArray` 副本、`SecurePasswordField`（含 `wipeToken = credentialEpoch`）、
   `DisposableEffect` 的 `onDispose { fill('0') }`、提交后「擦除 + 置零长 + 递增 epoch」三连——
   明文所有权与擦除义务必须同处一屏，与 §179（向导）/ §186（解锁页）同一口径。

## 3. 逐字性（本批接近纯搬，7 条缺失全是接收者改写）

`python tools/doc/check_verbatim_move.py /tmp/old_cd.kt <本体> <段落文件>` 报
**`orig_content_kinds=226 / MISSING_KINDS=7 / NEW_ONLY_KINDS=45`**。7 条逐条为：
`if (state.available)` / `if (state.mounts.isEmpty())` / `state.mounts.forEach` /
`state.feedback?.let` / `text = feedback.message.resolveText()` / `color = if (feedback.isError)`
（段落内改为形参 `available` / `mounts` / `feedback`，其中反馈形参名从 `feedback` 改为 `message`
以避免与 `feedback.message` 叠字）与 `enabled = state.available,` 的 `need 4 have 3`
（段落里那处改为 `enabled = enabled`，由调用方把 `state.available` 传入）。
**无一条判定被改写**：`available` 门控、空清单文案、`isError → error 色 / 否则 primary 色`、
`Opening → 不确定进度`、`canRetryWithCredentials → 解锁入口` 全部原样。

首轮比对曾另有 1 条缺失：来源回显的说明注释被我在 KDoc 里改写了一遍——**已把原注释归位到使用处**
并删掉 KDoc 的复述（注释属于现场，不属于摘要），复跑即为 7 条。

## 4. 验证

| 命令 | 结果 |
|---|---|
| `:app:compileDebugKotlin` | `BUILD SUCCESSFUL in 8s`，**零 error 零 warning** |
| `python tools/doc/long_functions.py` | `functions_ge_100=20`（原 21）、`files_scanned=508` |
| `python tools/doc/logic_lines.py … ChildDatabaseDialog` | **97**（原 170）⇒ 出表 |
| `python tools/doc/count_line_tiers.py` | 第一档 2、第二档 **28**（写盘后复跑） |
| `grep -c "SecureDialogWindowEffect()" <本体>` | **2**（与 `SecureDialogFlagPolicyTest` 的期望处数一致，未搬走） |
| `python tools/doc/check_verbatim_move.py …` | `MISSING_KINDS=7`，逐条见 §3 |
| 失效 import 自查 | 本体命中 8 条，剪除 6 条；`runtime.getValue` / `setValue` **刻意保留**（`by remember` 的隐式操作符导入，属误报）；段落文件另剪 1 条未用 `Arrangement` |
| 全量套件 / lint / 截图包装 | 见 §4.1 |
| `python tools/doc/check_md_links.py` | `BROKEN_MD_LINKS=0` |
| **未执行** | `cargo test`、`test -DliveSyncTest`、四层 `connectedDebugAndroidTest`（无设备；本批不触碰原生面） |

### 4.1 实测数字（跑完后回填）

| 命令 | 结果 |
|---|---|
| `test --rerun-tasks --max-workers=1` | `BUILD SUCCESSFUL in 2m 19s`、`114 actionable tasks: 114 executed`；JVM 单测 **`xml=315 tests=2209 failures=0 errors=0 skipped=13`** ——与 §191 / §192 同值（本批纯搬移不新增用例 ⇒ 增量必须为 0），`Uncaught exception` 命中 **0**；`SecureDialogFlagPolicyTest` 含在全量之内，其「该文件恰 2 处」计数判据随之一并复跑通过 |
| `:app:lintDebug` | `exit=0`、`issue` 元素 = **215**，与 §188~§192 持平 |
| `:app:compileDebugScreenshotTestKotlin --rerun-tasks` | `BUILD SUCCESSFUL in 52s`、`81 actionable tasks: 81 executed`（`ChildDatabaseDialogPreview` 包装仍可编译） |

**证据边界**：与前几批段落拆分相同——只证到「编译 + 成图包装编译 + 全量用例不回归 +
静态计数复核 + 逐字性比对」。**子库挂载对话框的实际呈现与交互（挂载按钮的 `canMount` 门控、
`Opening` 进度、解锁/卸载入口）没有运行时断言**；§3 的等价性来自读实现。
明文擦除路径（`fill('0')` 三连）本批**未搬动**，故其既有证据面不变（不新增也不削弱）。

## 5. 下一批

表内余 **18 个待拆**（头部：`CreateVaultWizardDialog` 158 且有限界 §179 的下限、
`EntryDetailTopBar` 151、`PackageBlocklistManageDialog` 131、`VaultListDialogHost` 130、
`GeneratorContent` 121、`EntryDetailScreen` 121）；另有 `ISSUE-P3-195` 待用户择一修法。
