# 337. 通行密钥详情徽标冗余删除批次（`ISSUE-P3-331`）

> **批次性质**：用户直接反馈 → 登记 → 同批整改。2026-09-26 用户反馈详情页通行密钥卡片的
> 徽标「FIDO2 软件密钥（库内加密存储）」内容多余、观感烦人，代理经全仓渲染点核实后登记
> `ISSUE-P3-331`（原文见 §337.0），同批完成删除、验证与归档。
> **未触**原生内核 / `*/src/androidTest/**` / `参考项目/` / 构建脚本 ⇒ 无设备侧必跑项。

---

## 337.0 原文收录（`ACTIVE_ISSUES.md` 条目正文，原样剪切）

> ### `ISSUE-P3-331` 通行密钥详情卡片冗余徽标「FIDO2 软件密钥（库内加密存储）」
>
> - **核实时间点**：2026-09-26，对工作区当前状态。
> - **核实方式**：全仓 `grep detail_passkey_chip`（`.kt` + `.xml` + `docs/`）——仅 1 个渲染点
>   `EntryDetailCards.kt:226`（`PasskeyCard` 标题行），无测试断言锁定；定义于 `values/strings.xml:154`
>   与 `values-en/strings.xml:146`。行号为核实时刻快照。
> - **背景（2026-09-26 用户反馈）**：详情页通行密钥卡片标题行的徽标
>   「FIDO2 软件密钥（库内加密存储）」**内容多余、观感烦人**——该行与其上方 `SectionTitle`
>   「通行密钥凭据 (WebAuthn)」同屏，卡片内另有 RP ID 行与支持说明行，徽标不承载任何操作信息
>   （§292 AC④ 把原「硬件芯片保护」改为如实措辞后仍保留展示）。
> - **涉及文件**：`app/src/main/java/com/keepasskey/app/ui/screens/detail/EntryDetailCards.kt`（`PasskeyCard`）、
>   `app/src/main/res/values/strings.xml`、`app/src/main/res/values-en/strings.xml`。
> - **验收标准**：
>   1. 删除 `PasskeyCard` 标题行（`Key` 图标 + `detail_passkey_chip` 文本及其间隔），卡片保留
>      RP ID 行与 `detail_passkey_desc` 说明行；`detail_passkey_chip` 键中英**同批**删除，删后全仓零残留引用、`en-only = 0`。
>   2. **不降级 §292 红线**：该批 AC④ 禁的是「谎报硬件保护」；本条为删除冗余徽标，
>      不新增任何安全声明，`SecurityClaimWiringTest` 不改一字须仍全绿。
>   3. 整改后 `.\gradlew.bat test` 全绿 **且** `python tools/doc/gate_readings.py` 7/7 PASS，
>      读数块原样贴入批次文档 §3（§308）；因改动 `PasskeyCard`（含 `@Preview`），另跑
>      `.\gradlew.bat :app:compileDebugScreenshotTestKotlin --rerun`。

## 1. 整改内容

1. **`PasskeyCard` 标题行整删**（`EntryDetailCards.kt`）：`Key` 图标 + `contentDescription`
   + 间隔 `Spacer` + `detail_passkey_chip` 标题文本 + 行后 `Spacer(6.dp)` 一并移除——
   卡片直接以 RP ID 行起头，随后 `detail_passkey_desc` 说明行，卡片底色 / 边框
   （`securityColors.passkeyContainer` / `passkey`）保留，卡片身份不受影响。
2. **字符串中英同删**：`values/strings.xml` `detail_passkey_chip`
   「FIDO2 软件密钥（库内加密存储）」与 `values-en/strings.xml`
   「FIDO2 software key (stored encrypted in vault)」同批删除。
3. **孤儿 import 同批清理**：`Icons.Default.Key` 与 `FontWeight` 删后全文件零引用，两行
   import 随批移除（`Row` / `width` / `Alignment` 等其余符号在同文件仍有点，保留）。
4. **`cd_passkey` 保留**：删除后复扫确认 `SecurityBadge.kt:49` 仍消费该无障碍描述
   （`R.string.cd_passkey`），不属死字符串。

## 2. 测试资产

- **零删除、零修改**：全仓无测试断言锁定 `detail_passkey_chip`（登记时已核实）；
  `SecurityClaimWiringTest`（§292 AC⑥ 接线守卫）**不改一字**而全绿——其锁定的三处
  实测降级声明与本徽标无耦合。
- 无 `androidTest` 变更，无 `crypto/src/main/rust` / `jni_bridge_ext` / `Native*` 变更
  → 未命中「设备侧四层 connected 必跑」触发面（测试资产纪律 ②）。

## 3. 机检读数（原样粘贴）

```
=== 门禁读数（ISSUE-P3-305 AC④：批次文档 §3 须原样粘贴本块）===
[1/7] tools/doc/count_line_tiers.py                EXIT 0  | tier1(>500)=0  tier2(400~500)=33  budget=37
[2/7] tools/doc/long_functions.py                  EXIT 0  | functions_ge_100=0
[3/7] tools/doc/check_md_links.py                  EXIT 0  | BROKEN_MD_LINKS=0
[4/7] tools/doc/check_resolved_index_sync.py       EXIT 0  | RESOLVED_INDEX_SYNC=OK（批次正文 335 份；分册登记 337 条；全量索引 337 条；最大 §337）
[5/7] tools/audit/check_tautological_assertions.py EXIT 0  | 汇总：命中 0 处 / 扫描 458 个测试文件
[6/7] tools/audit/check_recheck_consistency.py     EXIT 0  | PASS: 无残留禁用短语（已扫描 1331 行，11 条禁用短语）
[7/7] tools/doc/check_bounded_type_names.py        EXIT 0  | allowed=12  unregistered_manager_util_helper_common=0
=== 汇总：7/7 PASS ===
```

## 4. 验证与影响面

```
$ .\gradlew.bat test --rerun-tasks --max-workers=1
BUILD SUCCESSFUL in 3m 43s
114 actionable tasks: 114 executed     （GRADLE_EXIT=0）

$ python tools/doc/count_test_results.py
xml=401 tests=2662 failures=0 errors=0 skipped=13
已排除非 JVM 单测 XML：{'debug': 5}（这些目录里的结果是**上一批遗留**，不随 `test` 重跑，不得计入本计数）

$ .\gradlew.bat :app:compileDebugScreenshotTestKotlin --rerun
BUILD SUCCESSFUL in 2s
81 actionable tasks: 2 executed, 79 up-to-date     （真编译任务强制执行）
```

- 双语键位校验（全 `values/` × 全 `values-en/`）：`zh=1175` / `en=1172`，**en-only = 0**；
  `detail_passkey_chip` 两侧均 False；XML 良构。
- 删后全仓复扫（`rg detail_passkey_chip`，排除 `build/`）：仅剩本批次文档与归档史
  （§292 批次正文，历史记录不动）——**代码 / 资源零残留**。
- 影响面：仅详情页通行密钥卡片的展示行与字符串资源；无业务逻辑、无开关、无数据面变化。
  `tests=2662` 与基线（§336）持平。

## 5. 如实留痕

- **§292 AC④ 关系**：该批 AC④ 要求 passkey 徽标「去硬件表述、如实写软件密钥」——其**红线**
  是禁止谎报硬件保护，并非强制常驻该徽标。本批按用户裁决整体删除徽标：不新增、不改写任何
  安全声明，卡片与节标题仍标识「通行密钥凭据 (WebAuthn)」，不存在谎报回潮；
  `SecurityClaimWiringTest` 未改而绿即为佐证。
- **未扩整改面**：卡片内 `detail_passkey_rp`（关联域名）与 `detail_passkey_desc`
  （「支持 Android 14+ Credential Manager …」）两行**保留**——用户仅点名徽标文本，
  按最小改动原则不臆测扩面；若后续认定仍冗余可另起条目处置。
- 本批为纯删除批次：未新增任何 UI 文本，未改变任何行为分支。
