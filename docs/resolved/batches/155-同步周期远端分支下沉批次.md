# §155 同步周期远端分支下沉批次（`ISSUE-P3-188` 剩余清单第 2 项：把 §151 自己写胖的门面回填）

> **起因**：`ISSUE-P3-188` 剩余清单第 2 项——§151 那批「巨型函数拆分」把 `runSyncCycle` 的三段裁决下沉为
> **同文件私有函数** + `RemoteSyncContext` 聚合体，接缝文档使 `SyncCycleRunner.kt` 由 431 行**涨到 515 行**
> （即整改自身引入的第二档违例）。条目已给出修法与前置约束，本批照做。
> **依据**：`.codebuddy/rules/engineering-rules.md` §单一职责与巨型类阈值；`docs/ACTIVE_ISSUES.md` `ISSUE-P3-188`。

---

## 1. 改动

| 对象 | 改动 |
|---|---|
| `app/src/main/java/com/keepasskey/app/sync/SyncCycleRemoteOutcomes.kt`（**新增**，105 行） | `RemoteSyncContext` + `handleRemoteSynced` + `handleConflictDetected` 移为**同包 `internal` 扩展函数**（`AutofillDatasetBuilders.kt` 先例）；**函数体逐字未改**，仅去一级缩进与改声明头 |
| `SyncCycleRunner.kt` | **515 → 430 行**（回到 §151 之前的量级）；`session` / `databaseSession` / `codec` / `conflicts` / `strings` 五个构造成员 `private` → `internal`；类 KDoc 增两行接缝说明 |
| `app/src/test/.../quality/AlgoHotPathGuardsTest.kt` | 「两处合并入口传内存树」的扫描目标由单文件改为**门面 + 分支文件并集**（`joinToString` 后仍以 `" "` 连接，**计数判据仍为 2**）——按条目整改纪律「只放宽定位串、不降低断言强度」 |

**公开 API 与行为零变化**：三个 mover 只被 `SyncCycleRunner` 自己调用（扩展函数同包可见），
Hilt 装配、`SyncOutcome` 口径、锁边界（周期整体在 `SyncSessionState.mutex` 内）逐字未动。

## 2. 验证

- `.\gradlew.bat test --rerun-tasks --max-workers=1 --continue` **连续 2 轮全绿**：
  `tests=2186 failures=0 errors=0 skipped=13`、`uncaught_files=0`（例数与 §153 持平 ⇒ 本批**未新增也未删除用例**，
  纯搬家；`AlgoHotPathGuardsTest` 的内存树判据在并集口径下仍命中 2 处）。
- `.\gradlew.bat :app:lintDebug` **BUILD SUCCESSFUL（0 error）**；两份改动文件**无未使用 import**（脚本按标识符复核）。
- **未执行**：设备侧四件套与 `assembleRelease`（本批不动 `crypto/src/main/rust/**`、不动 `Native*` 分派 / 探活、
  不动 `androidTest` 用例 ⇒ 不触发 `AGENTS.md` §5 的原生面实跑义务）；`cargo test` 与 `-DliveSyncTest`
  仍被本机策略拦下，用户可自行执行。
- 首轮**编译期失败一次**（非运行期）：`AlgoHotPathGuardsTest` 拼接串里的换行转义（反斜杠 + n）经 shell heredoc 落成真实换行，
  Kotlin 字面量被截断 ⇒ `Syntax error: Expecting '"'`；改用 `joinToString(separator = " ")` 后通过（留痕以免误判「改动未生效」）。

## 3. 剩余清单更新与边界

- `ISSUE-P3-188` 剩余清单第 2 项**闭环**；第 1 项（`DatabaseSettingsScreen` 530 行含约 459 行的单个
  `@Composable`）、第 3 项（其余 400~500 行文件约 36 个）与第 4 项（接线守卫与结构耦合的长期代价）**仍开放**，
  条目按「渐进消化」留原状。
- 边界：本批**不**主张性能收益（纯文件层搬家，无一处逻辑改动，也无实测）；扩展函数形态是**同模块内**的
  可见性放宽（`internal`），不进入 `:app:` 的对外 API 面。
