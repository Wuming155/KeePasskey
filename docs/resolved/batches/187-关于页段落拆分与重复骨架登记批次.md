# §187 关于页段落拆分批次（`ISSUE-P3-188` 第 3 目：`AboutSettingsScreen` 153 → 75 行；顺带实测一处**结构性重复**并登记）

> **起因**：第 3 目待拆清单里 §186 认定的**零测试引用**目标 `AboutSettingsScreen`（153 行）。
> 拆完后核对时发现：这 20 行的 `TopAppBar` 块在**十个二级设置页里各有一份**——
> 那是重复而不是缺陷，按规则 6.1 当场补登为剩余清单第 6 项（不只在聊天/本批次文档里）。

---

## 1. 改动

| 对象 | 行数 | 内容 |
|---|---|---|
| `subscreens/AboutSettingsScreen.kt` | 249 → **125** | 页面骨架（`Scaffold` + `TopAppBar` + `LazyColumn` 与各 `item { }` 调用点）与预览 |
| `subscreens/AboutSettingsScreenSections.kt` | 新建 **172** | 三个段落组件 + 一个私有助手：`AboutBrandCard`（品牌大卡片，收 `appVersion` / `buildNumber` 两个字符串）、`AboutSpecSection`（技术规格段）、`AboutPrivacyCard`（隐私段）、`AboutSpecRow`（`private`，仅被规格段使用） |

**函数长度**：`AboutSettingsScreen` **153 → 75 行** ⇒ 出第 3 目表（≥100 项 **25 → 24**；
扣除 PD-11 豁免的两个 NavGraph 后**待拆 22 个**）。分档不变（第一档 2、第二档 28）。

`Scaffold` 的 `containerColor = MaterialTheme.colorScheme.background` 与 `TopAppBar` 整块
**刻意留在页面内**——它们正是 §1 末尾所记「十页重复」的样本，应一次性收敛为共用骨架，
而不是在本批里为单页再造一个只被用一次的抽象。

## 2. 逐字性核对（本批最干净的一次）

`python tools/doc/check_verbatim_move.py <原文件> <本体> <段落文件>`：

- 原文件内容行 **148** 种；
- 在新两侧找不到的仅 **3** 种，全部是形参改名：
  `text = uiState.appVersion,` → `text = appVersion,`、`text = uiState.buildNumber,` → `text = buildNumber,`、
  `icon: androidx.compose.ui.graphics.vector.ImageVector,` → `icon: ImageVector,`（改为 import 后引用）；
- 新侧独有 **22** 种：文件头 KDoc、四个签名与形参、三处调用点。

**文案 / 图标 / 排版实参一字未动**，含 `"KeePasskey for Android"`、`"FIDO2 / WebAuthn Passkey"`
两处英文字面量与 `contentDescription = "Logo"`（沿用原值，未借机改动）。
另剪除 13 条随搬动失效的 import（`Box` / `CircleShape` / `Key` / `Lock` 等）。

## 3. 新登记的结构性重复（剩余清单第 6 项）

按 `^\s+TopAppBar\($` 定位并配平到闭合括号实测（§175「初筛 + 实读」纪律，逐处确认标题 / 返回键 /
`topAppBarColors(surface, onSurface)` 三要素一致）：

| 范围 | 处数 | 行数 |
|---|---:|---:|
| `settings/` 系（10 个二级页，各 **19~20** 行）+ 主页 `SettingsScreen`（42 行） | **11** | **241** |
| 全 `ui/screens/` 范围（含 `EntryDetailTopBar` 135、`VaultListTopBars` 116+26、`AuthenticatorScreen` 60 等） | 19 | 649 |

⇒ 收敛为共用骨架（`SettingsSubscreenScaffold(title, onBackClick) { … }`）可省 ~180 行，
且**同时**把 10 个页面各减约 20 行（对第二档的 `CloudSyncScreen` 427 / `DebugSettingsScreen` 443 /
`SecuritySettingsScreen` 433 / `AuthenticatorScreen` 441 是直接消减项）。
已登为剩余清单第 6 项，附两条硬约束：① 各页 `containerColor` / `contentWindowInsets` 存在差异，
骨架须以参数暴露、**不得强行统一**（`AboutSettingsScreen` 就是一例）；
② 三条按路径点名读文本的守卫须做并集扩扫（剩余清单第 3 项）。

## 4. 验证

| 命令 | 结果 |
|---|---|
| `:app:compileDebugKotlin` | `BUILD SUCCESSFUL`（剪除 13 条失效 import 后再编译一次，仍绿） |
| `test --rerun-tasks --max-workers=1` | `BUILD SUCCESSFUL in 2m 3s` / `114 actionable tasks: 114 executed`；聚合 **tests=2203 failures=0 errors=0 skipped=13**（313 份 XML），`Uncaught exception` 命中 **0**（用例数与 §186 一致，本批未增删用例） |
| `:app:lintDebug` | `BUILD SUCCESSFUL`；`issue` 元素 = **215**，与基线一致、**无新增** |
| `:app:compileDebugScreenshotTestKotlin --rerun` | 任务**确实执行**、`BUILD SUCCESSFUL`——该页的 `AboutSettingsScreenPreview` 未动，包装仍编译通过（这正是本批的**唯一自动化外观证据**） |
| `python tools/doc/logic_lines.py … AboutSettingsScreen` | 总行 **75**、逻辑行 **0** |
| `python tools/doc/long_functions.py` | `functions_ge_100=24`（原 25）、`files_scanned=505` |
| `python tools/doc/count_line_tiers.py` | 第一档 2、第二档 28（两份文件 125 / 172 行，均 <400） |
| `python tools/doc/check_verbatim_move.py` | §2 的 148 / 3 / 22 三组数字 |
| `python tools/doc/check_md_links.py` | `BROKEN_MD_LINKS=0` |
| TopAppBar 重复实测脚本（一次性，数字进 §3 表） | `settings/` 系 **11 处 / 241 行**；全 `ui/screens/` **19 处 / 649 行** |
| **未执行** | `cargo test`、`test -DliveSyncTest`、四层 `connectedDebugAndroidTest`（无设备） |

**余量**：与 §176~§186 各批同——纯 Compose 呈现的**视觉等价性无自动化断言**，
证据止于「逐字搬移 + 编译 + 成图包装编译」。本批的 3 行逐字差异全部是形参改名，
不含任何注释改写或文案调整（对比 §183 / §186 各有 2~3 处注释改写，已各自单列）。

## 5. 下一批

优先做**本批新登的第 6 项**（一次改 10 页、连带削第二档 4 个文件），
它比逐个再拆 22 个 Compose 段落的单位收益更高；
之后回到第 3 目按 §186 §6 的引用面清单推进。
