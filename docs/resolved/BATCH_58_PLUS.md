# 归档分册 04 · §58 起（滚动）

> **本册定位**：分册级索引（2026-09-15 批次）。**每批次正文已拆分为独立文件**，见下表「正文」列；本册只做引用，不重复收录。
> **命名**：本册自 §58 起为**滚动分册**——文件名不再随批次编号增长而改写（避免每批一次重命名）；
> 覆盖范围以本表实际行数为准，超出 100 行时另立分册 05。
> **主索引**：[../RESOLVED_LOG.md](../RESOLVED_LOG.md)（全量批次索引）｜**分册体系**：[README.md](../../README.md)。
> **体例**：正文为**原样迁移**（拆分前后逐行一致），未删改任何结论、裁决、残余登记与「过程缺陷」。

## 本册批次

| § | 批次主题 | 条目范围 | 正文 |
|:--:|---|---|---|
| §58 | 凭据中间量清零批次：远端路径解析凭据擦除 + S3 签名拼接缓冲擦除 + 口令 SHA-1 改字节态 | ISSUE-P3-100 / P3-101 / P3-102 | [`58-凭据中间量清零批次.md`](batches/58-凭据中间量清零批次.md) |
| §59 | 附件字节所有权与敏感窗口接线批次：TOTP 取景窗口补遮挡触摸过滤 + `KdbxAttachment` KDoc 口径更正 + 附件读取双拷贝消除与写出侧交付副本清零 | ISSUE-P3-103 / P3-104 / P3-105 | [`59-附件字节所有权与敏感窗口接线批次.md`](batches/59-附件字节所有权与敏感窗口接线批次.md) |
| §60 | 退路面加固与来源登记批次：备份 / 迁移排除域穷举补全 + SAF 删除前文档 URI 归属判定 + Gradle 分发镜像来源登记 | ISSUE-P3-106 / P3-112 / P3-115 | [`60-退路面加固与来源登记批次.md`](batches/60-退路面加固与来源登记批次.md) |
| §61 | fail-closed 可观测性批次：字段屏蔽签名密钥不可用由静默转为健康自检显式告警 | ISSUE-P3-113 | [`61-可观测性批次.md`](batches/61-可观测性批次.md) |
| §62 | 导出确认令牌下沉与文案一致性批次：明文导出令牌不可伪造 + 剪贴板 / 备份开关文案按实现更正 + `.kdbx.bak` 保留期登记 | ISSUE-P3-107 / P3-110 / P3-114 | [`62-导出确认令牌下沉与文案一致性批次.md`](batches/62-导出确认令牌下沉与文案一致性批次.md) |
| §63 | 密钥文件导出确认批次：密钥文件导出纳入令牌门控 + 二次确认弹窗（§62 附带发现的收口） | ISSUE-P3-128 | [`63-密钥文件导出确认批次.md`](batches/63-密钥文件导出确认批次.md) |
| §64 | 第一轮审计 F 系列收尾批次：整库导出缓冲清零 + 明文导出令牌下沉（同根因）+ 审计短摘要熵修正 + 删除未加固 OTP URI 解析副本 + ChaCha20 标签纠偏 | ISSUE-P3-86 / P3-87 / P3-89 / P3-90 / P3-91 / P3-92 | [`64-第一轮审计-系列收尾批次.md`](batches/64-第一轮审计-系列收尾批次.md) |
| §65 | 浏览器指纹格式守卫批次：Chrome 签名指纹 65-hex 笔误三处同批修正 + 格式与双写法一致性断言 | ISSUE-P3-88 | [`65-浏览器指纹格式守卫批次.md`](batches/65-浏览器指纹格式守卫批次.md) |
| §66 | 构建 fail-closed 与版本策略声明批次（**两条目的 ③ 子项**）：原生内核构建失败不再静默降级 + KDBX 版本策略显式声明与死常量/死字段清理 | ISSUE-P3-125 ③ / P3-126 ③ | [`66-构建-与版本策略声明批次.md`](batches/66-构建-与版本策略声明批次.md) |
| §67 | CI 声明一致性与 DAL 不可重定向批次：CI 产物标注「非发布签名」+ 豁免清单说明去数字漂移 + DAL 端点/时钟改构造注入只读策略 | ISSUE-P3-126（整体）/ P3-125 ② | [`67-声明一致性与-不可重定向批次-整体.md`](batches/67-声明一致性与-不可重定向批次-整体.md) |
| §68 | 退出前清理易失缓存批次：终止动作固定为「退栈 → 清理易失缓存 → 退出进程」，覆盖附件明文与同步密文快照两个面（**2026-09-15 补录**） | ISSUE-P3-116 | [`68-退出前清理易失缓存批次.md`](batches/68-退出前清理易失缓存批次.md) |
| §69 | 文档索引悬空引用收口批次：§68 正文补录 + 第二轮独立复核报告恢复入库（一致性闸门复效）+ `.handoff` 依据承接 | ISSUE-P3-129 / ISSUE-P3-127 | [`69-文档索引悬空引用收口批次.md`](batches/69-文档索引悬空引用收口批次.md) |
| §70 | KDF 盐长边界与 XML 常量归位批次：Argon2 `S` 按官方 `MinSalt/MaxSalt` 校验 + `Compressed` 常量的唯一权威定义上收 | ISSUE-P3-78 / ISSUE-P3-80 | [`70-KDF盐长边界与XML常量归位批次.md`](batches/70-KDF盐长边界与XML常量归位批次.md) |
| §71 | 设备侧验证缺口收口批次：arm64 真机（API 37）逐项实测敏感对话框 `FLAG_SECURE` / 附件冷启动清理 / `DialogWindowProvider` 取到，并补 2 例设备侧清理用例 | ISSUE-P2-42 | [`71-设备侧验证缺口收口批次.md`](batches/71-设备侧验证缺口收口批次.md) |
| §72 | 无障碍信号纳入完整性体系与口令语义显式声明批次：信号采集 + **只提示不降级**策略 + 主密码页提示 + `password` 语义显式化（含 AC 前提更正与真机正负对照） | ISSUE-P2-44 | [`72-无障碍信号与口令语义显式声明批次.md`](batches/72-无障碍信号与口令语义显式声明批次.md) |
| §73 | CI 原生门禁与清单导出面硬化批次：`:crypto:test` 真实执行 + 零跳过 + 4 ABI 导出符号核对；启动器入口 `singleTask`/空 `taskAffinity` + 导出面守卫 | ISSUE-P3-97 / ISSUE-P3-85 | [`73-CI原生门禁与清单导出面硬化批次.md`](batches/73-CI原生门禁与清单导出面硬化批次.md) |
| §74 | 缓存清理幂等契约批次（§73 施工中的附带发现）：`clearAll` 以「删除后是否仍存在」判定成功，消除失败误报与自相矛盾告警 | ISSUE-P2-82 | [`74-缓存清理幂等契约批次.md`](batches/74-缓存清理幂等契约批次.md) |
| §75 | `android://` 维度签名绑定批次：自动填充包名维度改为「包名 + 签名摘要」首次绑定门控（未授权不命中），绑定写入落在选择器；CM 通道拆分为 `ISSUE-P2-83` | ISSUE-P2-46 | [`75-android包名维度签名绑定批次.md`](batches/75-android包名维度签名绑定批次.md) |
| §76 | KDF 墙钟与内存闸门真机分路径实测批次：分路径接受性 + 到达内核内存值 + 默认/重载墙钟与吞吐（回填 `P2-49` AC② 处置路径） | ISSUE-P2-80 | [`76-KDF墙钟与内存闸门真机分路径实测批次.md`](batches/76-KDF墙钟与内存闸门真机分路径实测批次.md) |
| §77 | Popup 窗口敏感内容面盘点批次：4 处调用点 / 8 项全静态 ⇒ 留痕「无需接线」+ 清单锁与敏感记号扫描守卫 | ISSUE-P3-79 | [`77-Popup窗口敏感内容面盘点批次.md`](batches/77-Popup窗口敏感内容面盘点批次.md) |
| §78 | KDF 联合预算收紧批次：Argon2 `I×M` 由 `2^40` 收紧为 `2^33`（实测锚定：最坏 ≈1.4 h → ≈40 s）+ AES-KDF 封顶轮数墙钟实测（≈35 s） | ISSUE-P2-49 | [`78-KDF联合预算收紧批次.md`](batches/78-KDF联合预算收紧批次.md) |
| §79 | 缓存 `.tmp` 残留语义与瞬时删除失败批次：判定为可观测缺陷 + 删除原语有界重试 + `clear`/`clearAll` 统一 + `updateBase`／异常路径覆盖 | ISSUE-P3-108 | [`79-缓存tmp残留语义与瞬时删除失败批次.md`](batches/79-缓存tmp残留语义与瞬时删除失败批次.md) |
| §80 | 解锁节流删键复位与 Keystore-HMAC 真机失效：每库存在性标记 + `P0-10` Mac provider 修复 | ISSUE-P2-45 / ISSUE-P0-10 | [`80-解锁节流删键复位与Keystore-HMAC真机失效批次.md`](batches/80-解锁节流删键复位与Keystore-HMAC真机失效批次.md) |
| §81 | CM 通道调用方绑定与填充归属交叉核对（含 `P2-84` 保存谎报） | ISSUE-P3-111 / P2-83 / P2-84 | [`81-CM通道调用方绑定与填充归属交叉核对批次.md`](batches/81-CM通道调用方绑定与填充归属交叉核对批次.md) |
| §82 | CM 通道签名绑定候选层收口与真机回归 | ISSUE-P2-83 | [`82-CM通道签名绑定候选层收口与真机回归批次.md`](batches/82-CM通道签名绑定候选层收口与真机回归批次.md) |
| §83 | 内层 XML DTD 拦截设备侧前置断言 | ISSUE-P3-82 / P2-47 批注 | [`83-内层XML-DTD拦截设备侧前置断言批次.md`](batches/83-内层XML-DTD拦截设备侧前置断言批次.md) |
| §84 | 自动填充认证结果崩溃级修复与 requestCode 单调化 | ISSUE-P2-73 / P3-122 | [`84-自动填充认证结果崩溃级修复与requestCode单调化批次.md`](batches/84-自动填充认证结果崩溃级修复与requestCode单调化批次.md) |
| §85 | TracerPid 内存取证门控 | ISSUE-P3-83 | [`85-TracerPid内存取证门控批次.md`](batches/85-TracerPid内存取证门控批次.md) |
| §86 | DAL 出口纵深防御 | ISSUE-P3-124 | [`86-DAL出口纵深防御批次.md`](batches/86-DAL出口纵深防御批次.md) |
| §87 | CI 产物可见性与供应链硬化 | ISSUE-P3-123 | [`87-CI产物可见性与供应链硬化批次.md`](batches/87-CI产物可见性与供应链硬化批次.md) |
| §88 | `onSaveRequest` 超时预算 | ISSUE-P3-122（IPC-10） | [`88-onSaveRequest超时预算批次.md`](batches/88-onSaveRequest超时预算批次.md) |
| §89 | 选择器入口不变量与 IME 残余归档 | ISSUE-P3-125 ① / P3-76 | [`89-选择器入口不变量与IME残余归档批次.md`](batches/89-选择器入口不变量与IME残余归档批次.md) |
| §90 | 防回滚引擎级回归补齐 | ISSUE-P2-47 AC③ | [`90-防回滚引擎级回归补齐批次.md`](batches/90-防回滚引擎级回归补齐批次.md) |
| §91 | 运行完整性检测 Frida 真机实测 | ISSUE-P3-120 | [`91-运行完整性检测Frida真机实测批次.md`](batches/91-运行完整性检测Frida真机实测批次.md) |
| §92 | 检测判据面锁定与真机核心流程冒烟 | 无条目闭环 | [`92-检测判据面锁定与真机核心流程冒烟批次.md`](batches/92-检测判据面锁定与真机核心流程冒烟批次.md) |
| §93 | 真机条目流程审计与日志卫生观察 | 无条目闭环 | [`93-真机条目流程审计与日志卫生观察批次.md`](batches/93-真机条目流程审计与日志卫生观察批次.md) |
| §94 | 用户界面核实整改批次：inset / 强度阈值 / 连接验证门控 / 密钥文件行 / TOTP 热区 / i18n | 用户报告 UI 批次 | [`94-用户界面核实整改批次.md`](batches/94-用户界面核实整改批次.md) |
| §95 | MD3 可访问性与交互一致性批次 | ISSUE-P3-130 | [`95-MD3可访问性与交互一致性批次.md`](batches/95-MD3可访问性与交互一致性批次.md) |
| §96 | FAB 收缩 / Slider 手感 / Segmented 选中态 / 热区批次 | ISSUE-P3-131 | [`96-FAB收缩与Slider手感批次.md`](batches/96-FAB收缩与Slider手感批次.md) |
| §97 | 外部 UI 评审「边界定义 / 弹窗规范 / 竖向空间」批次 | ISSUE-P3-132 | [`97-UI评审边界与紧凑化批次.md`](batches/97-UI评审边界与紧凑化批次.md) |
| §98 | 上一会话遗留界面改动落盘批次 | 无既有 ISSUE 编号（遗留未提交改动） | [`98-上一会话遗留界面改动落盘批次.md`](batches/98-上一会话遗留界面改动落盘批次.md) |
| §99 | 浅色二级组件样式割裂与 CJK 断行批次 | ISSUE-P3-133 ~ P3-138（六条闭环） | [`99-浅色二级组件样式割裂与CJK断行批次.md`](batches/99-浅色二级组件样式割裂与CJK断行批次.md) |
| §100 | 应用选择器批次（黑名单 / 条目关联应用改为从系统选取） | ISSUE-P3-139 | [`100-应用选择器批次.md`](batches/100-应用选择器批次.md) |
| §101 | 已知工程限界登记恢复与文档引用一致性批次（原 `AGENTS.md` §6 删除未分流的 40 处悬空引用收口） | 无既有 ISSUE 编号（同轮登记并闭环） | [`101-已知工程限界登记恢复与文档引用一致性批次.md`](batches/101-已知工程限界登记恢复与文档引用一致性批次.md) |
| §102 | 建库预设算法接线与末尾残块明文清零批次（复核工作目录编号对拍撞出两条真缺陷） | ISSUE-P2-85 / ISSUE-P3-140（同轮登记并闭环）+ 新登记留表项 ISSUE-P3-141 | [`102-建库预设算法接线与末尾残块明文清零批次.md`](batches/102-建库预设算法接线与末尾残块明文清零批次.md) |
| §103 | 存量条目前提复核与失真裁决理由更正批次（三条「已裁决暂缓」条目的前提复核；发现 `ISSUE-P3-121` 裁决理由失真，须用户重新裁决） | 无既有 ISSUE 编号（同轮处置） | [`103-存量条目前提复核与失真裁决理由更正批次.md`](batches/103-存量条目前提复核与失真裁决理由更正批次.md) |
| §104 | 第四轮复核遗留编号处置收口批次（编号 + 主题双通道逐条取证 + 斜杠压缩写法展开 + 清单再核定；工作目录结论分流与退役判定） | ISSUE-P3-141（闭环）+ 新登记 ISSUE-P3-143 / ISSUE-P3-144 / ISSUE-P3-145 | [`104-第四轮复核遗留编号处置收口批次.md`](batches/104-第四轮复核遗留编号处置收口批次.md) |
| §105 | 第四轮复核「已覆盖但动作未执行」收尾批次（`PasskeyAssertionActivity` 三处错误归因注释 / 承接文档更正指针 / `BrowserSigningFingerprints` 通道口径 KDoc；附清既有基线告警与矛盾测试名） | 无既有 ISSUE 编号（同轮处置） | [`105-第四轮复核已覆盖动作未执行收尾批次.md`](batches/105-第四轮复核已覆盖动作未执行收尾批次.md) |
| §106 | 报告「待复核区已逐条补齐」口径更正与重合声明逐条核实批次（4 处就地更正；9 项重合声明 7 成立 / 2 部分成立；两项独立残余补上受跟踪落点：R-1 → 已知限界 §6、R-2 → `ISSUE-P3-146`） | ISSUE-P3-145（闭环）+ 新登记 ISSUE-P3-146 | [`106-报告口径更正与重合声明核实批次.md`](batches/106-报告口径更正与重合声明核实批次.md) |
| §107 | SyncCache「大写 `.CACHE.tmp` 残留」定位批次（判为 Windows/NTFS 目录枚举鬼影，非本仓/非外部写入者；断言口径改为「以磁盘真实存在为准」，不构成放宽；两项已接受残余登记于已知限界 §7） | ISSUE-P3-142（闭环） | [`107-SyncCache大写临时文件残留定位批次.md`](batches/107-SyncCache大写临时文件残留定位批次.md) |
| §108 | 两条「防未来接入」缺口收口批次（`KdbxHeader` 秘密不敏感相等性 KDoc + 5 例；`armScheduledClear` 否决语义 KDoc + 8 例；变异验证证明行为层与接线层互补） | ISSUE-P3-143 / ISSUE-P3-144 | [`108-防未来接入缺口收口批次.md`](batches/108-防未来接入缺口收口批次.md) |
| §109 | 自动填充认证链路真机实测收口批次（设备侧入口落地 + 定版 4/4 通过 + 真实凭据写入客户端；撞出框架不自动重发与确认路径不写值两条产品级发现） | ISSUE-P2-73（AC③ 闭环）+ 新登记 ISSUE-P2-86 | [`109-自动填充认证链路真机实测收口批次.md`](batches/109-自动填充认证链路真机实测收口批次.md) |
| §110 | 产品裁决分流与自动填充解锁链路修复批次（**结构性 + 技术性两件事**）：① 三条**本质为产品取舍**的条目（`P2-47` AC② / `P2-79` AC② / `P3-121` AC①）**迁出待办清单**至新建的 `docs/architecture/产品裁决登记.md`（`PD-01`/`PD-02`/`PD-03`，体例含「重开条件」），并把其中**真正的待修缺口**留下转登；② `ISSUE-P2-86` **真机定位并修复**——根因是**交付路径缺失**（解锁页只回传空 extras 并等一个**不会发生**的框架重发），改为「解锁成功后链入选择器并原样转发认证结果」+ 认证 PI 改 `FLAG_MUTABLE` + 基 Intent 去 `NEW_TASK|CLEAR_TOP`（**撤销 AC② 口径**）；**真机验证通过且附负向对照**（回退修复则用例必红）；③ 新登缺口 `ISSUE-P2-87`（KDF 弱因子导入无告警，逐维度判据 + 基线取自身建库默认 + `parallelism` 刻意不作判据）与 `ISSUE-P3-147`（README 未披露「不支持内网 / 自建端点」）**同批闭环**；④ 新登记 `ISSUE-P2-88`（确认路径**确认后仍不写入**，且**已实测「只改 FLAG 无效」**⇒ 与解锁路径不可外推）；⑤ 更正两处既往表述（限界 §4.1「链路未稳定通过」→ 已通过；实测记录 §10「未触碰 AC② FLAG 口径」→ 已变更），并补 **`已知工程限界` §8**（弱因子告警的覆盖面残余：选择器导入路径无提示 / 远端刻意不评估 / `QUICK_UNLOCK` 渲染缺口） | `ISSUE-P2-86` / `ISSUE-P2-87` / `ISSUE-P3-147`（闭环）+ 新登记 `ISSUE-P2-88`；三条转为 `PD-01`/`PD-02`/`PD-03` | [`110-产品裁决分流与自动填充解锁链路修复批次.md`](batches/110-产品裁决分流与自动填充解锁链路修复批次.md) |
| §111 | 自动填充「二次确认」路径凭据交付修复批次（`ISSUE-P2-86` 的**同源兄弟路径**）：库**已解锁**时自动匹配候选点「确认填充」后，目标输入框始终为空。**做法是先取官方契约再推缺什么**，不是试错——官方 `Dataset.Builder#setAuthentication` 明写两条要求，而该路径**两条都违反**：① 认证 PI 用 `FLAG_IMMUTABLE`（官方明文 "the platform needs to fill in the authentication arguments" ⇒ 必须可变）；② 结果**未**携带数据集（返回 `Bundle.EMPTY`，语义是「没有东西可填」）。上一轮只修 ① 所以无效 ⇒ **`FLAG_MUTABLE` 必要但不充分**。修法：确认 PI 改 `FLAG_MUTABLE` + 基 Intent 去掉 `NEW_TASK\|CLEAR_TOP` 并下发目标框 id；确认页在**显式确认成功后**按 `EXTRA_ENTRY_ID` 取回凭据并构造真实 `Dataset` 经 `EXTRA_AUTHENTICATION_RESULT` 回传；交付载荷构造收敛为**单一实现** `AutofillAuthResultDelivery`（选择器与确认页共用，顺带补「零可写字段 ⇒ 如实取消」，旧实现在该退化情形会抛异常）。**真机验证通过**（客户端自述 `username=[ac3-device-user] passwordLength=12 usernameFilled=true passwordFilled=true`，确认窗口内 `requestAutofill` 计数 **5→5** 未重请求；task `BUILD SUCCESSFUL 2m 9s`）+ **负向对照**（只改 FLAG、不回传数据集 ⇒ 用例 **FAILED**，钉成回归）。**一条比推断更细的实测结论**：框架对回传数据集是 **replace 语义**——第一轮回传数据集只有口令字段时，实测 `username=[] passwordLength=12 usernameFilled=false passwordFilled=true`（**只填回传内容、不复用服务端数据集已带的值**）⇒ **回传数据集必须完整**；据此守住两条：**不得摘掉候选数据集自带的真实值**（`ISSUE-P3-42` 会话宽限分支依赖它）、交付构造不得再度分叉。**过程中暴露的第二个缺陷**：`AutofillPickerViewModel` 条目缓存为 `init` 异步填充，而确认页是**按需创建**该 VM（点确认那一刻）⇒ 用户名取到空串 ⇒ 第一轮只回传了口令字段；修法为「缓存未命中回退仓库快照」（锁定态仍 fail-safe）。新登记 **`ISSUE-P3-148`**（确认页每次确认加载**全库非敏感投影**，实际只需一条）。**验证**：宿主 `BUILD SUCCESSFUL 2m 5s`（`tests=1975 skipped=13 failures=0 errors=0`）+ 真机用例绿。**边界如实声明**：未做「保留数据集回传 + PI 改回 `FLAG_IMMUTABLE`」的单变量对照（故「`FLAG_MUTABLE` 单独必要」只有**官方明文**依据，**不得**由本批实测断言）；AC① 备选路线（改写候选数据集为选择器同构造）**未做实验**（结论已达成，无须再走）；框架侧 `onAuthenticationResult` 留痕本轮**抓不到**（0 行）⇒ 结论只建立在功能观测上；阶段 7 未把「客户端未重请求」变成断言；确认页去 `NEW_TASK` 后落在调用方任务内 | `ISSUE-P2-88`（闭环）+ 新登记 `ISSUE-P3-148` | [`111-自动填充确认路径凭据交付修复批次.md`](batches/111-自动填充确认路径凭据交付修复批次.md) |
| §112 | 分支保护必需检查的**治理取舍**批次（`ISSUE-P3-146`，**不是技术缺陷**——「闸门会不会阻断合并」的可核对性 / 治理问题，其「解决」只能由一次**治理决策 + 设置页操作**构成）：**决策为「不启用」**，登记为 [`architecture/产品裁决登记.md`](architecture/产品裁决登记.md) **`PD-04`**。**依据是官方文档 + 所有者一手核实，不是「做不到」**——仓库为**公开仓库**，分支保护在计划层**可用**，故属**主动取舍**：① 官方明写 "the restrictions of a branch protection rule **don't apply to people with admin permissions**" ⇒ 单人仓库下保护规则对唯一行为者几乎不起约束；② 官方对「必需检查是否也约束**直推**」措辞**含糊**（同段既说 "before collaborators can make changes to a protected branch"、又说可以直接 push），**唯一不含糊**的阻断机制是「Require a pull request before merging」，而它会使 `AGENTS.md` §6.4 的「立即直推 main」**不再可行** ⇒ 属**流程改造**；③ 闸门的实质价值（fail-closed 扫描 + CVSS 硬断言 + SARIF，且在 push 与 PR 上**都**跑）**已由工作流本身获得**，分支保护额外买到的仅是「阻断」。**一手核实**：所有者于设置页确认 `main` **无任何分支保护规则**（既无经典规则亦无 rulesets）；本环境经 API 的尝试因 `gh` token 失效**未产生结论**（据实留痕，**不得**当作证据）。**连带处置**：`退役审计承接-43` 的 **`C-10`**（原列「无法确认」）就地给出答案——分支保护不存在 ⇒ **`build` 未被要求通过**，`F-05` 的爆炸半径**不因分支保护而收窄**（该行问的是 `build`，与本题的 `dependency-scan` 不同，但**同一事实**同时回答二者）；`P2-54` 的 AC② 由「未闭环」改为「**有意不实施**（见 `PD-04`）」。**重开条件**：出现第二提交者 / 引入发布流程与 tag 保护 / 发生「坏提交直推 main 造成实际损失」事故 / 需要 `build` 成为**编译硬门禁**；重开时须**四项同批**（Require a PR + 禁止管理员绕过 + 勾必需检查选中 `OWASP Dependency-Check`（job 名全仓**唯一**，不触发官方提醒的同名歧义）+ **同批改 `AGENTS.md` §6.4**），且**必须实测一次「PR 被挡下」**。**本批零代码 / 零测试改动**（验收口径为审计闸门 `PASS` + 文档结构自检）。**如实声明**：AC② 的处置被**越过**而非逐字执行（所有者指示直接裁决，故清单中不再有「待裁决」状态）；**过程留痕**——编辑 `C-10` 行时曾误删行尾换行致 `C-10`/`C-11` 两行合并，已按字节核对定位并拆回、自检「各恰好一行」 | `ISSUE-P3-146`（闭环，编号不复用）+ 新增 `PD-04` | [`112-分支保护必需检查治理取舍批次.md`](batches/112-分支保护必需检查治理取舍批次.md) |
| §113 | 自动填充确认路径整库装载收口批次（`ISSUE-P2-88` 的**同批引入开销**收口）：仓库层新增优先单条查询 `getKdbxEntry`（`findEntry` 深度优先短路，**不物化整份列表**；**不设默认实现**以免未来实现方悄悄退化）+ `getKdbxEntries` 补调用面约束；VM 整库装载**退出 `init`**、改为选择器页显式调用的幂等 `loadEntries()`（**选择器页装载不削减**），`resolveCredentials` 缓存未命中回退改走单条查询；`VaultEntrySecretReader` **9 处**按 id 读取（口令 / 修订 / 受保护字段 / TOTP / 附件）由 `allEntries().firstOrNull{…}` 改 `findEntry`（语义逐层等价、只改成本面）。**负向对照**：回退分支改回整库装载 ⇒ 断言变红 `expected:<0> but was:<1>`。全量 `tests=1977 skipped=13 failures=0 errors=0`（+2）；闸门 `PASS`。**边界**：无性能实测；`findEntry` 仍 O(N)（消除的是整表物化，**不得**读作已 O(1)）；仓库层其余 `allEntries()` 调用点未动 | `ISSUE-P3-148`（闭环） | [`113-自动填充确认路径整库装载收口批次.md`](batches/113-自动填充确认路径整库装载收口批次.md) |
| §114 | 列表页秒级重建与 TOTP 重算收敛批次（用户提出「降低 CPU / 内存占用」后的全仓排查**第一批**；三条同源缺陷：**高频节拍驱动了不该驱动的整页工作**）：**① `ISSUE-P2-89`** 列表页把秒级倒计时并入最外层 `combine` ⇒ **每秒**在主线程重跑整页投影（过滤 / 排序 / 全量 `copy` / 回收站递归），且 `UiVaultEntry` 带 4 个 `List`、全仓无 `@Immutable` ⇒ 行组件不可跳过；改为「两条 TOTP 实时值走**窄通道**」——退出整页状态，UI 只取**状态对象**下传、读取下沉到行内**独立徽标组件**，节拍改**订阅驱动**（删掉 `init` 常驻 `start()`，离屏即停）。**② `ISSUE-P2-90`** 验证码重算原为 O(T×N)（每拍 `findEntry` + Base32 解码 + 新建 `Mac` + HMAC，而**一周期内之码恒定**）；改为数据层**验证码缓存**（`entryId → (周期号, 快照)`；**只缓存结果快照、绝不缓存种子**；**HOTP 一律不入缓存**）+ **批量通道**（一次索引，O(N+T)）+ 两个失效点（`databaseFlow` 变更、落库后同步作废）。**③ `ISSUE-P3-149` ①②④**：`formatInstant` 按 `Locale` 复用已编译格式化器、验证码由 ② 吸收、`getEntry(id)` 改 `findEntry` 短路不再物化整库。**顺带**修一处时钟错配（`computeTotpCode` 增 `timestampMillis`，出码与周期判定共用同一时刻）。**验证**：全量 `--rerun-tasks` **2m 5s** 绿，`tests=1985 skipped=13 failures=0 errors=0`；新增 9 例（含以**实例身份**判别「是否重算」、结构契约「会话状态不得再含 TOTP 字段」、**毒丸**判别「单条查询不物化整库」）；**三处负向对照逐条实测变红后回填**。**边界**：无真机性能实测（不得宣称百分比收益）；验证器页 / 详情页调用点**结构未改**（每秒仍调用一次，被缓存吸收为 O(1)）；列表徽标仍按全局 30 秒周期（`period≠30` 条目不符）；`getEntry` 对小写 hex id 有一处放宽；**③ 的 `flowOn` 未做**并转登 `ISSUE-P3-154`（数据层单例无注入调度器，写死 `Dispatchers.Default` 会把投影放到测试虚拟时钟无法控制的线程池）。**过程留痕**：**工作区 220 个文件行尾为 CRLF/mixed**（违反 `.gitattributes`），本批触及的 5 个文件已字节级归一化、其余约 215 个未处理；另三处自查修正（两套时钟 / 作用域选错 / 回归测试判别力不足） | `ISSUE-P2-89` / `ISSUE-P2-90` / `ISSUE-P3-149`（①②④ 闭环）+ 新登记 `ISSUE-P3-154` | [`114-列表页秒级重建与TOTP重算收敛批次.md`](batches/114-列表页秒级重建与TOTP重算收敛批次.md) |
| §115 | IO 缓冲、XML 写出批量化与落盘附件重复读盘批次（同轮排查**第二批**；两条条目**开工复核各撞出一处前提问题**，均就地更正）：**① `ISSUE-P3-151` ② 修掉一处真缺陷** —— `escape` 以 `writer.write(codePoint)` 逐码点写出，而 `Writer.write(int)` **只写低 16 位** ⇒ 非 BMP 字符（emoji）被写成私用区字符、**保存即静默损坏**（变异验证实测 `expected:<[😀…]> but was:<[开头…]>`，全仓此前无代理对用例）；改为按「无需转义的连续区间」批量写出，代理对按 UTF-16 原样搬运。**② `P3-151` ① 前提更正**：`CipherInputStream/OutputStream` **没有缓冲尺寸构造参数**（内部固定 512 B），「一行改动」只适用于 GZip 三处（传 64 KiB）；AES 侧块粒度需自研分块流（与既有 `transform` 契约不兼容）⇒ **转登 `ISSUE-P3-155`**。**③ `P3-151` ③**：ISO-8601 正则嗅探（每 `<Times>` 调 5 次）改结构化前缀判定（语义逐一等价）。**④ `ISSUE-P3-150` 前提整体更正**：「整树 `copy` + 附件字节 `copyOf()` 是浪费」被复核**推翻** —— 那是**有意的别名隔离契约**、由 P1-9 用例锁定，**刻意不改**（登记 `已知工程限界.md` §1.7）；真实成本面是 `candidateItem` 恒 `withFlags(...)` 新建实例使**实例级 `lazy` 哈希缓存每次保存作废** ⇒ **每次保存把全部落盘附件重新读盘一遍**；改为 `flags` 未变即复用来源实例 ⇒ 读回次数恒为 1。**验证**：全量 **1m 43s 绿**，`tests=1988 skipped=13 failures=0 errors=0`（+3）；新增 3 例；**两处负向对照实测变红后回填**。**边界**：无真机性能实测；落盘读盘次数为**宿主侧假 store 计数**证据；嗅探用例是**契约锁**（新旧实现均通过）；未做「不含 emoji 库的新旧产物二进制对拍」；① 的 cipher 侧与编辑路径转登。**同轮附带**：工作区 **215 个文件**行尾归一化为 LF（`git status` 的「M」经 `porcelain=v2` 核实为 **blob OID 相同**的 stat 缓存假象 ⇒ 零提交内容）。**过程留痕**：全量首跑两项失败 —— 新增用例的 `expected:<1> but was:<2>` 判为**编辑期与并发构建重叠的编译窗口瞬态**（模块级 `--rerun-tasks` 与复跑均绿为证）；`BreachCheckHealthTest` 的 `Dispatchers.Main was accessed…` 属 §18 已记载的**跨用例污染类偶发红**（与本批无因果关系），据此顺手把 §114 引入的 `VaultListViewModelTest` 补齐 §18「先取消作用域、再 `resetMain()`」口径 | `ISSUE-P3-151` / `ISSUE-P3-150`（闭环）+ 新登记 `ISSUE-P3-155` / `ISSUE-P3-156` | [`115-IO缓冲与写出批量化及落盘附件重复读盘批次.md`](batches/115-IO缓冲与写出批量化及落盘附件重复读盘批次.md) |
| §116 | 常驻轮询收敛与完整性重扫参数守卫批次（同轮排查第三批；两处轮询**形似而性质相反**）：① `UnlockedNotificationController` 的 **2 s** `settingsStore.load()` 轮询属**真浪费**（自陈理由「同步 API 无 Flow」已过期——store 自 ISSUE-P2-21 起已有进程级 `settings: StateFlow`，且设置页恒 `publish + save`）⇒ 改为**订阅快照**（零 IO、变更即达），删除轮询常量与 `delay`/`flow`；② `RuntimeIntegrityDetector` 的 **30 s** 重扫（ISSUE-P2-63）是**注入检测时效的上界**，放宽即削弱检测面 ⇒ **刻意不改**，登记 `已知工程限界.md` **§3.5**，两常量 `private → internal` 并以契约用例锁「不得放宽」（放宽即红、收紧不限）。验证：新增 5 例；负向对照（改回轮询 ⇒ 守卫两例同红，备份逐字节还原）；全量见提交信息。边界：①「零 IO」为结构性结论（未做设备侧计数/耗电实测）；①存在「文件 vs 进程内快照」的行为差异；②用例**只锁参数不锁行为**；未做 `load()` 调用面全仓清理。过程留痕：接线守卫首跑**误判**（负向断言命中 KDoc 里刻意引用的旧写法）⇒ 改为先剥离注释再断言 | `ISSUE-P3-152` | [`116-常驻轮询收敛与完整性重扫参数守卫批次.md`](batches/116-常驻轮询收敛与完整性重扫参数守卫批次.md) |
| §117 | 仓库投影流补 `flowOn` 批次（`ISSUE-P3-154`，§114 的**残留**收口——`ISSUE-P3-149` ③）：`getEntries()` / `getGroups()` 的整库投影（逐字段解密 + 时间格式化）此前在**收集上下文**执行（列表页为 `viewModelScope` = Main）；§114 只把**频率**降为「每次数据变更」，**执行线程未变**。按条目原文修法：数据层新增可注入限定符 `@VaultProjectionDispatcher` + Hilt 模块（生产 `Dispatchers.Default`，对齐展示层 `@EntryDisplayDispatcher`），再补 `flowOn`；构造参数**刻意不给默认值**（默认值等于留一条悄悄退回收集上下文的路）；15 处测试构造点收敛为单一入口 `TestScope.newRepository(...)`（默认 `StandardTestDispatcher(testScheduler)`，与 `runTest` 同虚拟时间轴）。**验证**：新增 2 例——①**记录型调度器**（只计数派发、执行委托测试调度器）断言「未收集 0 / `getEntries()` 后 >0 / `getGroups()` 后继续增长」（两条流**分开计数**）；②`assertSame(Dispatchers.Default, …)` 锁生产绑定。**负向对照**：去掉两处 `flowOn` ⇒ 必红（`整库条目投影必须派发到注入的投影调度器`）✔ 后逐字节还原。全量 `--rerun-tasks` **1m 55s 绿**，`tests=1995 skipped=13 failures=0 errors=0`（§116 为 1993，+2）；Hilt 生成代码核实绑定已注入（缺失即编译期失败）。**边界**：无性能实测（不得宣称百分比收益）；只覆盖两条**整库**流，`getEntry(id)` **未**补（不得读作「仓库层投影已全部离开收集上下文」）；两条流独立派发后 `combine` 中间态对齐不再严格同步（终态一致）；`repositoryScope` 仍硬编码 `Dispatchers.Default`（**刻意不合并**，否则常驻作用域落在测试调度器上会 `UncompletedCoroutinesError`）；`FakeVaultRepository` 未补、接口**未**声明该契约 ⇒ 属实现属性非接口保证。**过程留痕**：条目前提更正（「40+ 处直接构造」实测 **15 处**）；用例名含点号触发 Kotlin `Name contains illegal characters` 而改名；**统计口径陷阱**（首次聚合得 1997，多出的 2 例来自 `updateDebugScreenshotTest/` 陈旧结果目录，剔除后与基线对齐）；顺带删 `getEntry` 上 §114 遗留的重复 KDoc 块 | `ISSUE-P3-154` | [`117-仓库投影流补flowOn批次.md`](batches/117-仓库投影流补flowOn批次.md) |
| §118 | 单条编辑路径增量擦除与路径复制批次（同轮排查**第四批**，`ISSUE-P3-150` 原正文 ③④ 转登；**两条前提经开工复核均成立**）：**① 路径复制**——原递归使**每个分组**都被 `copy()`（目标父组不存在时也整树重建），改为「命中即返回、未命中按同一实例原样返回」，`removeEntry` / `removeGroup` / `normalizeParentRefs` 同步；**语义变化 1 处**（重复分组 UUID 由「全部落树」改「首个命中」，依既有 API 的唯一性假设）。**② 替换关系回报**（`EntryEditResult` / `GroupEditResult` + `replaceRootGroup`）。**③ 增量定点擦除**（`KdbxGroup.eraseSupersededSensitiveData`）：候选**只由被替换节点收集一次**，逐级收窄（共享容器快径 → 减新节点可达者 → 减**新树任意位置**可达者 → 余下清零）；步骤 4 不可省（克隆体 / 历史快照 / moved 副本）。**④ 接线**：三条编辑器写路径走增量；删除路径维持不擦除；`updateDatabaseMeta` 保留通用实现，其单条分支转登 `ISSUE-P3-157`。**验证**：新增 **22 例**；全量 `tests=2017 skipped=13 failures=0 errors=0`（§117 为 1995）；`assembleDebug` 绿；**变异验证 6 项**（去掉全树扫描 ⇒ 克隆体与差分用例红；漏擦自定义字段 / 附件 / 历史 ⇒ 对应用例红；恢复整树重建 ⇒ 路径复制 2 例红；去掉快径 ⇒ 全绿，如实声明为纯性能快径）。**关键含义**：破坏路径复制后擦除用例仍全绿 ⇒ 擦除正确性的**唯一守护面**是 `assertSame` 系列。**边界**：无性能实测；候选收敛为**构造性结论 + 结构性守护**（无运行期计数）；差分对拍只覆盖 5 场景；步骤 4 仍是 O(全库) 引用扫描；增量入口要求**列全**被替换节点 | `ISSUE-P3-156`（闭环）+ 新登记 `ISSUE-P3-157` | [`118-单条编辑路径增量擦除与路径复制批次.md`](batches/118-单条编辑路径增量擦除与路径复制批次.md) |
| §119 | 单条条目原子读-改-写入口批次（同轮排查**第五批**；§118 同一路径族的**剩余面**——单条条目更新另有一条**不经编辑器**的 passkey 计数器补丁路径；**两处成本面前提经开工复核均成立**，无需更正正文）：**① 新增 `DatabaseSession.updateEntryById`**（→ `SessionContentMutations.updateEntryById`）——会话 Mutex 内**单次**变换完成「按 id 定位 → transform → 落树 → 增量定点擦除」（擦除复用 §118 的增量原语：候选只取自被替换的那一条旧条目）；返回**落树上线的实例**，未命中返回 `null` 且**零写入、不置 DIRTY**（旧实现命不中也会把同一实例写回并置 DIRTY ⇒ 顺带修正的边界行为）；入口**更新专用**（绝不新增条目）。**② 路径复制定位**（新增 `SessionTreeEditor.updateEntryById`）——按**条目 id** 递归（既有 `updateOrAddEntryIn` 按父组 id），命中即插入变换产物并回报替换关系，未命中分支按同一实例复用。**③ 接线**——`PasskeyEntryCoordinator.applySignCountPatch` 改走新入口并**删除 `replaceEntry`**（O(分组数) 整树重建消失）；回传值改为**从已提交条目读回**，与库内已提交状态同源。**④ `updateDatabaseMeta` 不放宽**（同步合并 / 远端接管 / 墓碑 / 图标引用清理仍是任意整库变换）。**原子性**：读现值 → 计算 → 落树同处一个临界区。**验证**：新增 **8 例**；全量 `--rerun-tasks` **1m 43s 绿**，`tests=2025 skipped=13 failures=0 errors=0`（§118 为 2017，+8）；`assembleDebug` 绿；审计闸门 **PASS**。**变异验证 2 项**：**M1** 注释掉定点擦除 ⇒ app 与 database **两级用例同时红**（`被替换下线的旧计数器实例必须清零 expected null, but was:<5>`）；**M2** 破坏路径复制 ⇒ 编辑器路径复制用例红，而 **app 侧同型断言仍绿**（目标条目是根组直接子项 ⇒ 走命中分支、不经过分组循环）⇒ app 侧不守护路径复制（`--rerun-tasks` 复跑确认真绿）。**边界**：无性能实测；候选收敛为**构造性结论 + 结构性守护**（无运行期计数）；**步骤 4 仍是 O(全库) 引用比较扫描**（passkey 补丁正是该情形）；入口语义收窄（单条 / 仅更新 / 变换产物不得为 null）；§118 的 M5 结论（`assertSame` 为擦除正确性唯一守护面）本批 M2 再次复现；其余 `updateDatabaseMeta` 调用面未动。**过程留痕**：database 级用例原拟断言「未命中不置 DIRTY」因前置 `saveEntry` 已置 DIRTY 而**不可观测**⇒ 改为断言「活动库实例不变（零写入）」，DIRTY 边界交由 app 级用例（OPENED 基线）守护 | `ISSUE-P3-157`（闭环，编号不复用） | [`119-单条条目原子读改写入口批次.md`](batches/119-单条条目原子读改写入口批次.md) |
| §120 | 设备无关项收尾批次（**无既有 ISSUE 编号**；用户指示「还有什么是不需要真机做的事情，全部完成」）：**① 补跑历年批次声明「未跑」的宿主侧闸门** —— `assembleRelease` **绿**（R8 + 资源收缩）、`lint` **绿**（app 209 warning / 0 error）、`cargo test` **57 passed / 0 failed**、语料 `--verify` 与伴生 JSON **逐字段一致**、**官方实现对拍**（`OwnProductInteropProbeTest` 产物经 `keepassxc-cli 2.7.12` 打开，`db-info` / `ls -R` / `export` 均成功，官方重编码时间解出 **63925224374 秒 = 2026-09-17T06:46:14Z** 与 `PROBE.md` 期望逐秒一致 ⇒ D1 时间编码端到端复核）、CVSS 闸门 **0 条达阈**（用 2026-09-10 存量报告，未重跑；本批无依赖变更）、审计闸门 **PASS**。**② `ISSUE-P3-158`（TOTP「全局 30 秒」口径）** —— 列表节拍以 `nowSec % 30` 算倒计时且只在 30 秒边界判翻转 ⇒ `period != 30` 相位错误；**更严重**：`period < 30` 且不整除 30（45 / 20）时「验证码滞留到下一个 30 秒边界」＝**界面显示过期验证码**；另两处进度环分母写死 30。修法：节拍通道由「剩余秒数」改为**秒级刻度**（**§114 性能契约逐条保留**：仍 `WhileSubscribed` / `flowOn` / 只在徽标内读取 / 整页状态不随 tick 重建），倒计时由徽标按 `entry.totpPeriod` 经 `OtpEngine` 现算，跨周期判据改「**任一条目自身周期序号变化**」，两处分母 + 详情页迷你环同取条目周期，预览缺省值改为使截图基线**逐像素一致**的刻度 0。**③ `ISSUE-P3-159`（语料脚本 `--check` 恒假失败）** —— `probe_cli` 用 `db-create --help` 的**退出码**判子命令，而 2.7.12 对 `--help`/`-h`/`-?` **一律先打印用法再 rc=1**（「缺少位置参数」）⇒ `--check` **exit=3** 并误导为「未安装 CLI」（而 `db-info`/`ls`/`export` 全可用）；判据改用**顶层命令清单**（`--help` rc 恒 0 + 文本含 `db-create`，命令名不随语言本地化）。**验证**：全量 `--rerun-tasks` **1m 43s 绿**，`tests=2031 skipped=13 failures=0 errors=0`（§119 为 2025，+6）；`assembleDebug` 绿；**变异验证 3 项**（重算判据退回 30 秒网格 ⇒ 两条 tracker 新用例同红；验证器分母回退 + 列表迷你环回退 ⇒ 接线守卫 2 例红）。**边界**：**无真机渲染验证** —— UI 侧证据是**接线守卫（源码文本，断言前剥离注释）+ 编译期契约**，不得读作「真机视觉已确认」；`period <= 0` 兜底 30 秒；CVSS 为存量报告。**仍需真机 / 需产品口径裁决者已如实列出**：`P3-153`（真机吞吐对比）、`P3-155`（AC 要求设备侧吞吐再决定采纳）、`已知工程限界` §8（设备无关**但需口径裁决**；复核发现设置页**只投影不展示** KDF，方向 ② 工作量大于字面）、§1.6、设备侧用例与视觉核对。**过程留痕**：新用例初版断言「订阅后首拍即重算」实测 `expected:<1> but was:<0>`（`nowSeconds` 初值即当前刻度、首拍被 `distinctUntilChanged` 吸收，**旧实现同理**）⇒ 改为按既有「订阅本身不等于取码」口径断言（断言写错而非实现错）；另误将全仓 grep 送入后台（3 MB 无关输出，已停、工作区未改动） | 无既有编号（同轮登记并闭环 `ISSUE-P3-158` / `ISSUE-P3-159`） | [`120-设备无关项收尾批次.md`](batches/120-设备无关项收尾批次.md) |
| §121 | 算法与数据结构专项批次（**第一档：零风险局部项**；入口是同日登记提交 `9b0be31`——用户提出「看看是不是坏算法 / 有没有坏数据结构」后的五路并行静态审计共登记 23 条，本批闭环其中 4 条）：**① `ISSUE-P3-162`** 分组索引缺失导致的平方级投影——`GroupPathPresenter.pathsOf` 原对每个分组各调一次「自建索引」的 `fullPathOf`（G 次 `associateBy`）、面包屑 `allGroups.find{}` 全表线性查找 + `breadcrumbs.add(0,…)` 头插、回收站后代递归 `allGroups.filter { it.parentId == … }`（每节点重扫全表）；改「一次 `associateBy` + `groupBy` 全程复用」+ `ArrayDeque.addFirst` + `childrenByParent` 单趟 BFS，两处名称排序由 `sortedBy { it.title.lowercase() }`（选择器在**每次比较**中调用）改 `compareBy(String.CASE_INSENSITIVE_ORDER)`。**语义变化 1 处**：面包屑新增环截断（原遇环状父链**无限循环**）。**② `ISSUE-P3-172` ①②③**：`AutofillFieldScanner.tokensOf` 的函数内正则提为对象级常量；`WebDavPropfindParser` 的 `DocumentBuilderFactory` 与 3 个 `SimpleDateFormat` 改按线程缓存；`KdbxXmlParser` 的 XXE 加固探针（原每次解析 5 次「工厂+解析器」实例化）改 companion `by lazy` + `ThreadLocal` 工厂（「为什么必须探测」的 P1-12 依据全文迁入 companion，未删一句）。**③ `ISSUE-P3-173`**：Base32 字母表线性查找改 256 项反查表、`MutableList<Byte>` 装箱改「两趟法 + 精确预分配」、`10.0.pow` 改 `POW10` 表（越界回退原路径，饱和行为逐字保留）。**④ `ISSUE-P3-181`**：密钥文件由「无条件复制去空白」改「单趟探测恰 64 且全 hex 才构造」、CSV 路径改 `ArrayDeque` 栈 + 组级拼一次、`<Tags>` 三中间集合改单趟扫描。**验证**：全量 `--rerun-tasks` **1m 40s 绿**，聚合 **`tests=2045 skipped=13 failures=0 errors=0`**（§120 为 2031，**+14**）；审计闸门 `PASS`（1308 行）；新增 14 例（接线守卫 6 含**断言前剥离注释**、密钥文件形态边界 5 以 `MessageDigest` 独立直算期望值、标签解析 3 把旧表达式**原样抄录**进用例）；`ISSUE-P3-181` 另有子代理的**非 Gradle 差分对拍**（77 019 次比对 0 失败）作**旁证**。**刻意不做并登记 `已知工程限界.md` §9**：`P3-172` ④ 的 `Mac` 实例复用——复用必然让 `Mac` 长期持有密钥副本，与调用方 `fill(0)` 的前提冲突，且 TOTP 取码已有周期缓存。**边界**：**无任何性能实测**（不得宣称百分比收益）；两处缓存均为**按线程**（相关类型均非线程安全）；`KdbxXmlParser` 告警由「每次解析」变「每进程一次」（唯一行为差异）；`stripWhitespace` 另两处调用方未动；排序比较器在极少数码点上与 `lowercase()` 可能不同。**过程留痕**：① 首版面包屑循环误写变量名（编译前自查修正）；② 守卫用例首跑 **1 例失败**——断言写宽误伤合法单趟过滤，收紧后复跑全绿；③ 该失败使首轮在 `:app` 中止（**database / core / sync 未执行**，该轮不构成任何验证结论） | `ISSUE-P3-162` / `ISSUE-P3-172`（①②③）/ `ISSUE-P3-173` / `ISSUE-P3-181`（闭环）+ 刻意不做项 → `已知工程限界.md` §9 | [`121-零风险局部项与守卫用例批次.md`](batches/121-零风险局部项与守卫用例批次.md) |
| §122 | 同步变化判定口径声明与语义锁定批次（`ISSUE-P2-91`，清单**顶部**条目；**开工前提复核不成立 ⇒ 改定修法**）：原条目称「变化检测漏比 `times` 与历史内容 ⇒ 只改 `times` 的编辑不上云」，并留「是否存在可达的、只改 `times` 的编辑面」为未核实项。**复核结论是否定的**：① 生产代码**无任何** `expires` / `expiryTime` 写入者（命中仅 XML 读取端 `KdbxXmlTimesNode` 与默认值 `KdbxXmlTimeHelper`）；② `times` 的改写只经 `KdbxEntry.withField`（**同时**改 `fields`）与 `PasskeyEntryCoordinator` 的计数器补丁（**同时**改 `customFields`）⇒ 该 `times` 变化**必然伴随**已被检出的变化；③ `usageCount` / `lastAccessTime` 无独立写入者（唯一写入点是 `withModified()`）；④ 合并路径写 `times` 发生在冲突流程内、其后必然上传，不经「判无变化即复用旧字节」的决策 ⇒ **「静默丢弃本地改动」撤销、P2 严重度不成立**。**另一面（故明确不做）**：照原 AC 补 `times` 比较**反而有害** —— `KdbxTimes` 混装使用性字段与 `lastModificationTime`，`withField` 经 `withModified()` 一次性改写三者，纳入比较会让「触碰但内容等同」被判成变更 ⇒ 触发重序列化 + 上传 + **前移远端 ETag**，正是该检测器存在的理由（其 KDoc 记 F5：KDBX4 随机 IV 使重序列化字节必然漂移）；合并侧**必须**比 `lastModificationTime`，是因为它回答的是「相对 base 是否被修改」（冲突裁决）这一**不同问题** ⇒ 按原 AC 的**另一分支**执行（「两处判定若需保持不同口径，须在 KDoc 显式声明差异与理由」）。`history` 内容同理不做：可达的历史变更（追加快照 / 按保留期修剪 / 合并并集）**必然改变条数**。**本批实质产出**：① 判定逻辑（原三个 private 函数）逐字搬运为 `internal object KdbxContentComparator`（**动机**：该判定此前**零用例覆盖**——全仓无任何用例引用 `resolveLocalContentChanged`——而它是「本地编辑被静默丢弃」的唯一判据；抽出后可脱离 `SyncDatabaseCodec` / `SyncSessionState` 直测，与本仓 §108 的 `ClipboardClearPolicy` 先例同构）；② **双向口径声明**——新比较器写明「本比较回答『是否需要重新序列化上传』、刻意不比 `times`」并附前提复核结论，`KdbxEntryMerger.isModified` 的 KDoc 增补互补声明（**必须**比 `lastModificationTime`）与「两处口径刻意不同、勿任一侧顺手对齐」的警示；③ **语义锁定用例 6 例**（条目 15 个变异项 + 分组 12 个变异项 + 「id 在新树中缺失」两条分支，**每项双向断言**；并把「仅 `times` 变化」与「历史条数相同内容不同」两条**刻意不判为变化**的口径钉成可执行断言——后者若日后引入等条数改写路径即失效判据）。**验证**：全量 `--rerun-tasks` **1m 37s 绿**，聚合 **`tests=2051 skipped=13 failures=0 errors=0`**（§121 为 2045，**+6**）；审计闸门 `PASS`（1308 行）。**处置**：`ISSUE-P2-91` 按前提复核**撤销**（清单 P2 段回到 0 项），真实残余（两处判定口径刻意不同 + `history` 只比条数）作为**口径而非缺陷**登记 `已知工程限界.md` **§10**（含「**外部客户端**写入只改 `times` 的条目」这一未覆盖面的解除条件）。**边界如实声明**：根级 `changed()`（`deletedObjects` 比较 + 委托）**未覆盖**（需 `KdbxHeader` / `DeletedObject` 夹具，本批未做）；`KdbxAttachment` 未覆写 `equals`，故附件列表实际是**实例级**比较（方向安全但偏保守）；本批**未改同步决策路径**（`SyncCycleRunner` 一行未动），无真机 / 联调验证。**过程留痕**：① 用例首版「子分组内容」变异项与基线**等价**（同 id 同 name）⇒ 断言会假红，跑测试前自查修正为带 `notes` 的真变化（未进构建）；② 首版曾按 AC 原文准备直接整比 `times`，读到 `KdbxTimes` 字段构成与 `withModified()` 改写面后放弃——与 §121 的「`Mac` 复用」同属**对条目 AC 的实质修正** | `ISSUE-P2-91`（**前提复核后撤销**，非「已整改」）+ 残余登记 `已知工程限界.md` §10 | [`122-同步变化判定口径声明与语义锁定批次.md`](batches/122-同步变化判定口径声明与语义锁定批次.md) |
| §123 | 批量树操作单趟化批次（第二档「改循环结构」的头两条，均属「每条目重走整棵树」族）：**① `ISSUE-P3-160`** —— `SessionContentMutations.batchDeleteEntries` 原 `for (id in entryIds) removeEntry(...)`、`batchMoveEntries` 原对每个待移条目各调一次 `removeEntry`，而 `SessionTreeEditor.removeEntry` 又**无条件** `group.subgroups.map { … }` 递归整棵树并新建列表 ⇒ 回收站 100 条 + 200 组时约 100 次全树遍历 + 约 2 万次临时 List 分配，且全程持会话 Mutex（锁持有时间被放大 K 倍）；新增 **`removeEntries(group, entryIds: Set<KdbxUuid>)`**（按 id 集合**单趟**剪枝、列表惰性分配、未命中子树**同一实例**复用、空集直接返回原实例），两处批量入口改用它；`removeEntry`（单条）同步去掉无条件 `map` 分配，**刻意未加**「命中即停止下降」的提前退出（避免在批量化之上再叠一处语义变化）。**② `ISSUE-P3-161`** —— `SyncConflictController.resolveConflicts` 原对每条冲突调 `applyResolvedEntryToGroup`，而后者对**每一层**都 `subgroups.map { … }` + `copy`（目标不在该子树时也照旧复制）⇒ O(裁决条目数 × 分组数) 次分组对象与列表分配（5000 组 × 100 条冲突 ≈ 50 万次 copy，且在 `Dispatchers.Default` + 会话锁内）；改为**先收集成表**（`resolvedByParent`，`getOrPut` 保持冲突原顺序；父组缺失落根组，与逐条实现 `?: group.id` 顶层即根组同语义）**再单趟落树**（新函数 `applyResolvedEntriesToGroup`，仅复制确实含目标的分组），降为 O(G + k)；同批把冗余的全限定名 `com.keepasskey.core.model.KdbxGroup` 收敛为已导入短名。**验证**：全量 `--rerun-tasks` **1m 40s 绿**，聚合 **`tests=2057 skipped=13 failures=0 errors=0`**（§122 为 2051，**+6**）；审计闸门 `PASS`（1308 行）。**新增 6 例**：批量删除与逐条删除的树结构**逐字等价**（含分组层级与各分组条目顺序）、深层与根级条目一次删净且兄弟保留、**未命中子树按同一实例复用**（`assertSame` 即 §118 路径复制契约的守护面）、空集与「id 全不存在」返回同一实例、单条删除未命中不更换实例；另加接线守卫 1 例（不得再逐 id 调 `removeEntry`、`removeEntries` 必须存在、冲突决议必须走批量函数且逐条实现不得残留）。**边界如实声明**：**无任何性能实测**（判据为结构性结论 + 契约用例，**不得**宣称百分比收益）；**P3-161 无新增行为用例**——新函数是依赖多重注入的 private 成员，宿主侧难以单独构造 ⇒ 等价性主要依赖**既有** `SyncCoordinatorTest` / `KdbxMergerTest` 与新加**结构守卫**，**不得**读作「已逐分支验证」；`removeEntry` 的**扫描范围未变**（只消除分配）；`batchDeleteEntries` 仍**无条件**置 `DIRTY` 并写回新库实例（含 id 全不存在的情形，与逐条实现逐字一致，**刻意不**顺手改状态机语义）；批量移动的**插入阶段未改**（`updateOrAddEntry` 仍是 O(深度)/条）；`parentGroupId` 指向的分组不存在时条目同样不落位（既有行为）。**过程留痕**：① 编辑期一次 `old_string` 因漏抄一个「的」而失败（`old_string not found`）⇒ 重新读取后逐字修正，**未进构建**；② P3-161 首版方案曾考虑「只改 `applyResolvedEntryToGroup` 签名」——那样外层「每条各走一次」仍在、复杂度降不下来，故改选「调用点收集 + 新函数单趟落树」 | `ISSUE-P3-160` / `ISSUE-P3-161`（闭环） | [`123-批量树操作单趟化批次.md`](batches/123-批量树操作单趟化批次.md) |
