# 归档分册 04 · §58 起（滚动）

> **本册定位**：分册级索引（2026-09-15 批次）。**每批次正文已拆分为独立文件**，见下表「正文」列；本册只做引用，不重复收录。
> **命名**：本册自 §58 起为**滚动分册**——文件名不再随批次编号增长而改写（避免每批一次重命名）；
> 覆盖范围以本表实际行数为准，超出 100 行时另立分册 05。
> **主索引**：[../RESOLVED_LOG.md](../RESOLVED_LOG.md)（全量批次索引）｜**分册体系**：[README.md](../../README.md)。
> **体例**：正文为**原样迁移**（拆分前后逐行一致），未删改任何结论、裁决、残余登记与「过程缺陷」。

## 本册批次

| § | 批次主题 | 条目范围 | 正文 |
|:--:|---|---|---|
| §58 | 凭据中间量清零批次：远端路径解析凭据擦除 + S3 签名拼接缓冲擦除 + 口令 SHA-1 改字节态 | ISSUE-P3-100 / P3-101 / P3-102 | [`58-凭据中间量清零批次.md`](batches/58-凭据中间量清零批次.md) |
| §59 | 附件字节所有权与敏感窗口接线批次：TOTP 取景窗口补遮挡触摸过滤 + `KdbxAttachment` KDoc 口径更正 + 附件读取双拷贝消除与写出侧交付副本清零 | ISSUE-P3-103 / P3-104 / P3-105 | [`59-附件字节所有权与敏感窗口接线批次.md`](batches/59-附件字节所有权与敏感窗口接线批次.md) |
| §60 | 退路面加固与来源登记批次：备份 / 迁移排除域穷举补全 + SAF 删除前文档 URI 归属判定 + Gradle 分发镜像来源登记 | ISSUE-P3-106 / P3-112 / P3-115 | [`60-退路面加固与来源登记批次.md`](batches/60-退路面加固与来源登记批次.md) |
| §61 | fail-closed 可观测性批次：字段屏蔽签名密钥不可用由静默转为健康自检显式告警 | ISSUE-P3-113 | [`61-可观测性批次.md`](batches/61-可观测性批次.md) |
| §62 | 导出确认令牌下沉与文案一致性批次：明文导出令牌不可伪造 + 剪贴板 / 备份开关文案按实现更正 + `.kdbx.bak` 保留期登记 | ISSUE-P3-107 / P3-110 / P3-114 | [`62-导出确认令牌下沉与文案一致性批次.md`](batches/62-导出确认令牌下沉与文案一致性批次.md) |
| §63 | 密钥文件导出确认批次：密钥文件导出纳入令牌门控 + 二次确认弹窗（§62 附带发现的收口） | ISSUE-P3-128 | [`63-密钥文件导出确认批次.md`](batches/63-密钥文件导出确认批次.md) |
| §64 | 第一轮审计 F 系列收尾批次：整库导出缓冲清零 + 明文导出令牌下沉（同根因）+ 审计短摘要熵修正 + 删除未加固 OTP URI 解析副本 + ChaCha20 标签纠偏 | ISSUE-P3-86 / P3-87 / P3-89 / P3-90 / P3-91 / P3-92 | [`64-第一轮审计-系列收尾批次.md`](batches/64-第一轮审计-系列收尾批次.md) |
| §65 | 浏览器指纹格式守卫批次：Chrome 签名指纹 65-hex 笔误三处同批修正 + 格式与双写法一致性断言 | ISSUE-P3-88 | [`65-浏览器指纹格式守卫批次.md`](batches/65-浏览器指纹格式守卫批次.md) |
| §66 | 构建 fail-closed 与版本策略声明批次（**两条目的 ③ 子项**）：原生内核构建失败不再静默降级 + KDBX 版本策略显式声明与死常量/死字段清理 | ISSUE-P3-125 ③ / P3-126 ③ | [`66-构建-与版本策略声明批次.md`](batches/66-构建-与版本策略声明批次.md) |
| §67 | CI 声明一致性与 DAL 不可重定向批次：CI 产物标注「非发布签名」+ 豁免清单说明去数字漂移 + DAL 端点/时钟改构造注入只读策略 | ISSUE-P3-126（整体）/ P3-125 ② | [`67-声明一致性与-不可重定向批次-整体.md`](batches/67-声明一致性与-不可重定向批次-整体.md) |
| §68 | 退出前清理易失缓存批次：终止动作固定为「退栈 → 清理易失缓存 → 退出进程」，覆盖附件明文与同步密文快照两个面（**2026-09-15 补录**） | ISSUE-P3-116 | [`68-退出前清理易失缓存批次.md`](batches/68-退出前清理易失缓存批次.md) |
| §69 | 文档索引悬空引用收口批次：§68 正文补录 + 第二轮独立复核报告恢复入库（一致性闸门复效）+ `.handoff` 依据承接 | ISSUE-P3-129 / ISSUE-P3-127 | [`69-文档索引悬空引用收口批次.md`](batches/69-文档索引悬空引用收口批次.md) |
| §70 | KDF 盐长边界与 XML 常量归位批次：Argon2 `S` 按官方 `MinSalt/MaxSalt` 校验 + `Compressed` 常量的唯一权威定义上收 | ISSUE-P3-78 / ISSUE-P3-80 | [`70-KDF盐长边界与XML常量归位批次.md`](batches/70-KDF盐长边界与XML常量归位批次.md) |
| §71 | 设备侧验证缺口收口批次：arm64 真机（API 37）逐项实测敏感对话框 `FLAG_SECURE` / 附件冷启动清理 / `DialogWindowProvider` 取到，并补 2 例设备侧清理用例 | ISSUE-P2-42 | [`71-设备侧验证缺口收口批次.md`](batches/71-设备侧验证缺口收口批次.md) |
| §72 | 无障碍信号纳入完整性体系与口令语义显式声明批次：信号采集 + **只提示不降级**策略 + 主密码页提示 + `password` 语义显式化（含 AC 前提更正与真机正负对照） | ISSUE-P2-44 | [`72-无障碍信号与口令语义显式声明批次.md`](batches/72-无障碍信号与口令语义显式声明批次.md) |
| §73 | CI 原生门禁与清单导出面硬化批次：`:crypto:test` 真实执行 + 零跳过 + 4 ABI 导出符号核对；启动器入口 `singleTask`/空 `taskAffinity` + 导出面守卫 | ISSUE-P3-97 / ISSUE-P3-85 | [`73-CI原生门禁与清单导出面硬化批次.md`](batches/73-CI原生门禁与清单导出面硬化批次.md) |
| §74 | 缓存清理幂等契约批次（§73 施工中的附带发现）：`clearAll` 以「删除后是否仍存在」判定成功，消除失败误报与自相矛盾告警 | ISSUE-P2-82 | [`74-缓存清理幂等契约批次.md`](batches/74-缓存清理幂等契约批次.md) |
| §75 | `android://` 维度签名绑定批次：自动填充包名维度改为「包名 + 签名摘要」首次绑定门控（未授权不命中），绑定写入落在选择器；CM 通道拆分为 `ISSUE-P2-83` | ISSUE-P2-46 | [`75-android包名维度签名绑定批次.md`](batches/75-android包名维度签名绑定批次.md) |
| §76 | KDF 墙钟与内存闸门真机分路径实测批次：分路径接受性 + 到达内核内存值 + 默认/重载墙钟与吞吐（回填 `P2-49` AC② 处置路径） | ISSUE-P2-80 | [`76-KDF墙钟与内存闸门真机分路径实测批次.md`](batches/76-KDF墙钟与内存闸门真机分路径实测批次.md) |
| §77 | Popup 窗口敏感内容面盘点批次：4 处调用点 / 8 项全静态 ⇒ 留痕「无需接线」+ 清单锁与敏感记号扫描守卫 | ISSUE-P3-79 | [`77-Popup窗口敏感内容面盘点批次.md`](batches/77-Popup窗口敏感内容面盘点批次.md) |
| §78 | KDF 联合预算收紧批次：Argon2 `I×M` 由 `2^40` 收紧为 `2^33`（实测锚定：最坏 ≈1.4 h → ≈40 s）+ AES-KDF 封顶轮数墙钟实测（≈35 s） | ISSUE-P2-49 | [`78-KDF联合预算收紧批次.md`](batches/78-KDF联合预算收紧批次.md) |
| §79 | 缓存 `.tmp` 残留语义与瞬时删除失败批次：判定为可观测缺陷 + 删除原语有界重试 + `clear`/`clearAll` 统一 + `updateBase`／异常路径覆盖 | ISSUE-P3-108 | [`79-缓存tmp残留语义与瞬时删除失败批次.md`](batches/79-缓存tmp残留语义与瞬时删除失败批次.md) |
| §80 | 解锁节流删键复位与 Keystore-HMAC 真机失效：每库存在性标记 + `P0-10` Mac provider 修复 | ISSUE-P2-45 / ISSUE-P0-10 | [`80-解锁节流删键复位与Keystore-HMAC真机失效批次.md`](batches/80-解锁节流删键复位与Keystore-HMAC真机失效批次.md) |
| §81 | CM 通道调用方绑定与填充归属交叉核对（含 `P2-84` 保存谎报） | ISSUE-P3-111 / P2-83 / P2-84 | [`81-CM通道调用方绑定与填充归属交叉核对批次.md`](batches/81-CM通道调用方绑定与填充归属交叉核对批次.md) |
| §82 | CM 通道签名绑定候选层收口与真机回归 | ISSUE-P2-83 | [`82-CM通道签名绑定候选层收口与真机回归批次.md`](batches/82-CM通道签名绑定候选层收口与真机回归批次.md) |
| §83 | 内层 XML DTD 拦截设备侧前置断言 | ISSUE-P3-82 / P2-47 批注 | [`83-内层XML-DTD拦截设备侧前置断言批次.md`](batches/83-内层XML-DTD拦截设备侧前置断言批次.md) |
| §84 | 自动填充认证结果崩溃级修复与 requestCode 单调化 | ISSUE-P2-73 / P3-122 | [`84-自动填充认证结果崩溃级修复与requestCode单调化批次.md`](batches/84-自动填充认证结果崩溃级修复与requestCode单调化批次.md) |
| §85 | TracerPid 内存取证门控 | ISSUE-P3-83 | [`85-TracerPid内存取证门控批次.md`](batches/85-TracerPid内存取证门控批次.md) |
| §86 | DAL 出口纵深防御 | ISSUE-P3-124 | [`86-DAL出口纵深防御批次.md`](batches/86-DAL出口纵深防御批次.md) |
| §87 | CI 产物可见性与供应链硬化 | ISSUE-P3-123 | [`87-CI产物可见性与供应链硬化批次.md`](batches/87-CI产物可见性与供应链硬化批次.md) |
| §88 | `onSaveRequest` 超时预算 | ISSUE-P3-122（IPC-10） | [`88-onSaveRequest超时预算批次.md`](batches/88-onSaveRequest超时预算批次.md) |
| §89 | 选择器入口不变量与 IME 残余归档 | ISSUE-P3-125 ① / P3-76 | [`89-选择器入口不变量与IME残余归档批次.md`](batches/89-选择器入口不变量与IME残余归档批次.md) |
| §90 | 防回滚引擎级回归补齐 | ISSUE-P2-47 AC③ | [`90-防回滚引擎级回归补齐批次.md`](batches/90-防回滚引擎级回归补齐批次.md) |
| §91 | 运行完整性检测 Frida 真机实测 | ISSUE-P3-120 | [`91-运行完整性检测Frida真机实测批次.md`](batches/91-运行完整性检测Frida真机实测批次.md) |
| §92 | 检测判据面锁定与真机核心流程冒烟 | 无条目闭环 | [`92-检测判据面锁定与真机核心流程冒烟批次.md`](batches/92-检测判据面锁定与真机核心流程冒烟批次.md) |
| §93 | 真机条目流程审计与日志卫生观察 | 无条目闭环 | [`93-真机条目流程审计与日志卫生观察批次.md`](batches/93-真机条目流程审计与日志卫生观察批次.md) |
| §94 | 用户界面核实整改批次：inset / 强度阈值 / 连接验证门控 / 密钥文件行 / TOTP 热区 / i18n | 用户报告 UI 批次 | [`94-用户界面核实整改批次.md`](batches/94-用户界面核实整改批次.md) |
| §95 | MD3 可访问性与交互一致性批次 | ISSUE-P3-130 | [`95-MD3可访问性与交互一致性批次.md`](batches/95-MD3可访问性与交互一致性批次.md) |
| §96 | FAB 收缩 / Slider 手感 / Segmented 选中态 / 热区批次 | ISSUE-P3-131 | [`96-FAB收缩与Slider手感批次.md`](batches/96-FAB收缩与Slider手感批次.md) |
| §97 | 外部 UI 评审「边界定义 / 弹窗规范 / 竖向空间」批次 | ISSUE-P3-132 | [`97-UI评审边界与紧凑化批次.md`](batches/97-UI评审边界与紧凑化批次.md) |
| §98 | 上一会话遗留界面改动落盘批次 | 无既有 ISSUE 编号（遗留未提交改动） | [`98-上一会话遗留界面改动落盘批次.md`](batches/98-上一会话遗留界面改动落盘批次.md) |
| §99 | 浅色二级组件样式割裂与 CJK 断行批次 | ISSUE-P3-133 ~ P3-138（六条闭环） | [`99-浅色二级组件样式割裂与CJK断行批次.md`](batches/99-浅色二级组件样式割裂与CJK断行批次.md) |
| §100 | 应用选择器批次（黑名单 / 条目关联应用改为从系统选取） | ISSUE-P3-139 | [`100-应用选择器批次.md`](batches/100-应用选择器批次.md) |
| §101 | 已知工程限界登记恢复与文档引用一致性批次（原 `AGENTS.md` §6 删除未分流的 40 处悬空引用收口） | 无既有 ISSUE 编号（同轮登记并闭环） | [`101-已知工程限界登记恢复与文档引用一致性批次.md`](batches/101-已知工程限界登记恢复与文档引用一致性批次.md) |
| §102 | 建库预设算法接线与末尾残块明文清零批次（复核工作目录编号对拍撞出两条真缺陷） | ISSUE-P2-85 / ISSUE-P3-140（同轮登记并闭环）+ 新登记留表项 ISSUE-P3-141 | [`102-建库预设算法接线与末尾残块明文清零批次.md`](batches/102-建库预设算法接线与末尾残块明文清零批次.md) |
| §103 | 存量条目前提复核与失真裁决理由更正批次（三条「已裁决暂缓」条目的前提复核；发现 `ISSUE-P3-121` 裁决理由失真，须用户重新裁决） | 无既有 ISSUE 编号（同轮处置） | [`103-存量条目前提复核与失真裁决理由更正批次.md`](batches/103-存量条目前提复核与失真裁决理由更正批次.md) |
| §104 | 第四轮复核遗留编号处置收口批次（编号 + 主题双通道逐条取证 + 斜杠压缩写法展开 + 清单再核定；工作目录结论分流与退役判定） | ISSUE-P3-141（闭环）+ 新登记 ISSUE-P3-143 / ISSUE-P3-144 / ISSUE-P3-145 | [`104-第四轮复核遗留编号处置收口批次.md`](batches/104-第四轮复核遗留编号处置收口批次.md) |
| §105 | 第四轮复核「已覆盖但动作未执行」收尾批次（`PasskeyAssertionActivity` 三处错误归因注释 / 承接文档更正指针 / `BrowserSigningFingerprints` 通道口径 KDoc；附清既有基线告警与矛盾测试名） | 无既有 ISSUE 编号（同轮处置） | [`105-第四轮复核已覆盖动作未执行收尾批次.md`](batches/105-第四轮复核已覆盖动作未执行收尾批次.md) |
| §106 | 报告「待复核区已逐条补齐」口径更正与重合声明逐条核实批次（4 处就地更正；9 项重合声明 7 成立 / 2 部分成立；两项独立残余补上受跟踪落点：R-1 → 已知限界 §6、R-2 → `ISSUE-P3-146`） | ISSUE-P3-145（闭环）+ 新登记 ISSUE-P3-146 | [`106-报告口径更正与重合声明核实批次.md`](batches/106-报告口径更正与重合声明核实批次.md) |
| §107 | SyncCache「大写 `.CACHE.tmp` 残留」定位批次（判为 Windows/NTFS 目录枚举鬼影，非本仓/非外部写入者；断言口径改为「以磁盘真实存在为准」，不构成放宽；两项已接受残余登记于已知限界 §7） | ISSUE-P3-142（闭环） | [`107-SyncCache大写临时文件残留定位批次.md`](batches/107-SyncCache大写临时文件残留定位批次.md) |
| §108 | 两条「防未来接入」缺口收口批次（`KdbxHeader` 秘密不敏感相等性 KDoc + 5 例；`armScheduledClear` 否决语义 KDoc + 8 例；变异验证证明行为层与接线层互补） | ISSUE-P3-143 / ISSUE-P3-144 | [`108-防未来接入缺口收口批次.md`](batches/108-防未来接入缺口收口批次.md) |
| §109 | 自动填充认证链路真机实测收口批次（设备侧入口落地 + 定版 4/4 通过 + 真实凭据写入客户端；撞出框架不自动重发与确认路径不写值两条产品级发现） | ISSUE-P2-73（AC③ 闭环）+ 新登记 ISSUE-P2-86 | [`109-自动填充认证链路真机实测收口批次.md`](batches/109-自动填充认证链路真机实测收口批次.md) |
| §110 | 产品裁决分流与自动填充解锁链路修复批次（**结构性 + 技术性两件事**）：① 三条**本质为产品取舍**的条目（`P2-47` AC② / `P2-79` AC② / `P3-121` AC①）**迁出待办清单**至新建的 `docs/architecture/产品裁决登记.md`（`PD-01`/`PD-02`/`PD-03`，体例含「重开条件」），并把其中**真正的待修缺口**留下转登；② `ISSUE-P2-86` **真机定位并修复**——根因是**交付路径缺失**（解锁页只回传空 extras 并等一个**不会发生**的框架重发），改为「解锁成功后链入选择器并原样转发认证结果」+ 认证 PI 改 `FLAG_MUTABLE` + 基 Intent 去 `NEW_TASK|CLEAR_TOP`（**撤销 AC② 口径**）；**真机验证通过且附负向对照**（回退修复则用例必红）；③ 新登缺口 `ISSUE-P2-87`（KDF 弱因子导入无告警，逐维度判据 + 基线取自身建库默认 + `parallelism` 刻意不作判据）与 `ISSUE-P3-147`（README 未披露「不支持内网 / 自建端点」）**同批闭环**；④ 新登记 `ISSUE-P2-88`（确认路径**确认后仍不写入**，且**已实测「只改 FLAG 无效」**⇒ 与解锁路径不可外推）；⑤ 更正两处既往表述（限界 §4.1「链路未稳定通过」→ 已通过；实测记录 §10「未触碰 AC② FLAG 口径」→ 已变更），并补 **`已知工程限界` §8**（弱因子告警的覆盖面残余：选择器导入路径无提示 / 远端刻意不评估 / `QUICK_UNLOCK` 渲染缺口） | `ISSUE-P2-86` / `ISSUE-P2-87` / `ISSUE-P3-147`（闭环）+ 新登记 `ISSUE-P2-88`；三条转为 `PD-01`/`PD-02`/`PD-03` | [`110-产品裁决分流与自动填充解锁链路修复批次.md`](batches/110-产品裁决分流与自动填充解锁链路修复批次.md) |
| §111 | 自动填充「二次确认」路径凭据交付修复批次（`ISSUE-P2-86` 的**同源兄弟路径**）：库**已解锁**时自动匹配候选点「确认填充」后，目标输入框始终为空。**做法是先取官方契约再推缺什么**，不是试错——官方 `Dataset.Builder#setAuthentication` 明写两条要求，而该路径**两条都违反**：① 认证 PI 用 `FLAG_IMMUTABLE`（官方明文 "the platform needs to fill in the authentication arguments" ⇒ 必须可变）；② 结果**未**携带数据集（返回 `Bundle.EMPTY`，语义是「没有东西可填」）。上一轮只修 ① 所以无效 ⇒ **`FLAG_MUTABLE` 必要但不充分**。修法：确认 PI 改 `FLAG_MUTABLE` + 基 Intent 去掉 `NEW_TASK\|CLEAR_TOP` 并下发目标框 id；确认页在**显式确认成功后**按 `EXTRA_ENTRY_ID` 取回凭据并构造真实 `Dataset` 经 `EXTRA_AUTHENTICATION_RESULT` 回传；交付载荷构造收敛为**单一实现** `AutofillAuthResultDelivery`（选择器与确认页共用，顺带补「零可写字段 ⇒ 如实取消」，旧实现在该退化情形会抛异常）。**真机验证通过**（客户端自述 `username=[ac3-device-user] passwordLength=12 usernameFilled=true passwordFilled=true`，确认窗口内 `requestAutofill` 计数 **5→5** 未重请求；task `BUILD SUCCESSFUL 2m 9s`）+ **负向对照**（只改 FLAG、不回传数据集 ⇒ 用例 **FAILED**，钉成回归）。**一条比推断更细的实测结论**：框架对回传数据集是 **replace 语义**——第一轮回传数据集只有口令字段时，实测 `username=[] passwordLength=12 usernameFilled=false passwordFilled=true`（**只填回传内容、不复用服务端数据集已带的值**）⇒ **回传数据集必须完整**；据此守住两条：**不得摘掉候选数据集自带的真实值**（`ISSUE-P3-42` 会话宽限分支依赖它）、交付构造不得再度分叉。**过程中暴露的第二个缺陷**：`AutofillPickerViewModel` 条目缓存为 `init` 异步填充，而确认页是**按需创建**该 VM（点确认那一刻）⇒ 用户名取到空串 ⇒ 第一轮只回传了口令字段；修法为「缓存未命中回退仓库快照」（锁定态仍 fail-safe）。新登记 **`ISSUE-P3-148`**（确认页每次确认加载**全库非敏感投影**，实际只需一条）。**验证**：宿主 `BUILD SUCCESSFUL 2m 5s`（`tests=1975 skipped=13 failures=0 errors=0`）+ 真机用例绿。**边界如实声明**：未做「保留数据集回传 + PI 改回 `FLAG_IMMUTABLE`」的单变量对照（故「`FLAG_MUTABLE` 单独必要」只有**官方明文**依据，**不得**由本批实测断言）；AC① 备选路线（改写候选数据集为选择器同构造）**未做实验**（结论已达成，无须再走）；框架侧 `onAuthenticationResult` 留痕本轮**抓不到**（0 行）⇒ 结论只建立在功能观测上；阶段 7 未把「客户端未重请求」变成断言；确认页去 `NEW_TASK` 后落在调用方任务内 | `ISSUE-P2-88`（闭环）+ 新登记 `ISSUE-P3-148` | [`111-自动填充确认路径凭据交付修复批次.md`](batches/111-自动填充确认路径凭据交付修复批次.md) |
| §112 | 分支保护必需检查的**治理取舍**批次（`ISSUE-P3-146`，**不是技术缺陷**——「闸门会不会阻断合并」的可核对性 / 治理问题，其「解决」只能由一次**治理决策 + 设置页操作**构成）：**决策为「不启用」**，登记为 [`architecture/产品裁决登记.md`](architecture/产品裁决登记.md) **`PD-04`**。**依据是官方文档 + 所有者一手核实，不是「做不到」**——仓库为**公开仓库**，分支保护在计划层**可用**，故属**主动取舍**：① 官方明写 "the restrictions of a branch protection rule **don't apply to people with admin permissions**" ⇒ 单人仓库下保护规则对唯一行为者几乎不起约束；② 官方对「必需检查是否也约束**直推**」措辞**含糊**（同段既说 "before collaborators can make changes to a protected branch"、又说可以直接 push），**唯一不含糊**的阻断机制是「Require a pull request before merging」，而它会使 `AGENTS.md` §6.4 的「立即直推 main」**不再可行** ⇒ 属**流程改造**；③ 闸门的实质价值（fail-closed 扫描 + CVSS 硬断言 + SARIF，且在 push 与 PR 上**都**跑）**已由工作流本身获得**，分支保护额外买到的仅是「阻断」。**一手核实**：所有者于设置页确认 `main` **无任何分支保护规则**（既无经典规则亦无 rulesets）；本环境经 API 的尝试因 `gh` token 失效**未产生结论**（据实留痕，**不得**当作证据）。**连带处置**：`退役审计承接-43` 的 **`C-10`**（原列「无法确认」）就地给出答案——分支保护不存在 ⇒ **`build` 未被要求通过**，`F-05` 的爆炸半径**不因分支保护而收窄**（该行问的是 `build`，与本题的 `dependency-scan` 不同，但**同一事实**同时回答二者）；`P2-54` 的 AC② 由「未闭环」改为「**有意不实施**（见 `PD-04`）」。**重开条件**：出现第二提交者 / 引入发布流程与 tag 保护 / 发生「坏提交直推 main 造成实际损失」事故 / 需要 `build` 成为**编译硬门禁**；重开时须**四项同批**（Require a PR + 禁止管理员绕过 + 勾必需检查选中 `OWASP Dependency-Check`（job 名全仓**唯一**，不触发官方提醒的同名歧义）+ **同批改 `AGENTS.md` §6.4**），且**必须实测一次「PR 被挡下」**。**本批零代码 / 零测试改动**（验收口径为审计闸门 `PASS` + 文档结构自检）。**如实声明**：AC② 的处置被**越过**而非逐字执行（所有者指示直接裁决，故清单中不再有「待裁决」状态）；**过程留痕**——编辑 `C-10` 行时曾误删行尾换行致 `C-10`/`C-11` 两行合并，已按字节核对定位并拆回、自检「各恰好一行」 | `ISSUE-P3-146`（闭环，编号不复用）+ 新增 `PD-04` | [`112-分支保护必需检查治理取舍批次.md`](batches/112-分支保护必需检查治理取舍批次.md) |
| §113 | 自动填充确认路径整库装载收口批次（`ISSUE-P2-88` 的**同批引入开销**收口）：仓库层新增优先单条查询 `getKdbxEntry`（`findEntry` 深度优先短路，**不物化整份列表**；**不设默认实现**以免未来实现方悄悄退化）+ `getKdbxEntries` 补调用面约束；VM 整库装载**退出 `init`**、改为选择器页显式调用的幂等 `loadEntries()`（**选择器页装载不削减**），`resolveCredentials` 缓存未命中回退改走单条查询；`VaultEntrySecretReader` **9 处**按 id 读取（口令 / 修订 / 受保护字段 / TOTP / 附件）由 `allEntries().firstOrNull{…}` 改 `findEntry`（语义逐层等价、只改成本面）。**负向对照**：回退分支改回整库装载 ⇒ 断言变红 `expected:<0> but was:<1>`。全量 `tests=1977 skipped=13 failures=0 errors=0`（+2）；闸门 `PASS`。**边界**：无性能实测；`findEntry` 仍 O(N)（消除的是整表物化，**不得**读作已 O(1)）；仓库层其余 `allEntries()` 调用点未动 | `ISSUE-P3-148`（闭环） | [`113-自动填充确认路径整库装载收口批次.md`](batches/113-自动填充确认路径整库装载收口批次.md) |
| §114 | 列表页秒级重建与 TOTP 重算收敛批次（用户提出「降低 CPU / 内存占用」后的全仓排查**第一批**；三条同源缺陷：**高频节拍驱动了不该驱动的整页工作**）：**① `ISSUE-P2-89`** 列表页把秒级倒计时并入最外层 `combine` ⇒ **每秒**在主线程重跑整页投影（过滤 / 排序 / 全量 `copy` / 回收站递归），且 `UiVaultEntry` 带 4 个 `List`、全仓无 `@Immutable` ⇒ 行组件不可跳过；改为「两条 TOTP 实时值走**窄通道**」——退出整页状态，UI 只取**状态对象**下传、读取下沉到行内**独立徽标组件**，节拍改**订阅驱动**（删掉 `init` 常驻 `start()`，离屏即停）。**② `ISSUE-P2-90`** 验证码重算原为 O(T×N)（每拍 `findEntry` + Base32 解码 + 新建 `Mac` + HMAC，而**一周期内之码恒定**）；改为数据层**验证码缓存**（`entryId → (周期号, 快照)`；**只缓存结果快照、绝不缓存种子**；**HOTP 一律不入缓存**）+ **批量通道**（一次索引，O(N+T)）+ 两个失效点（`databaseFlow` 变更、落库后同步作废）。**③ `ISSUE-P3-149` ①②④**：`formatInstant` 按 `Locale` 复用已编译格式化器、验证码由 ② 吸收、`getEntry(id)` 改 `findEntry` 短路不再物化整库。**顺带**修一处时钟错配（`computeTotpCode` 增 `timestampMillis`，出码与周期判定共用同一时刻）。**验证**：全量 `--rerun-tasks` **2m 5s** 绿，`tests=1985 skipped=13 failures=0 errors=0`；新增 9 例（含以**实例身份**判别「是否重算」、结构契约「会话状态不得再含 TOTP 字段」、**毒丸**判别「单条查询不物化整库」）；**三处负向对照逐条实测变红后回填**。**边界**：无真机性能实测（不得宣称百分比收益）；验证器页 / 详情页调用点**结构未改**（每秒仍调用一次，被缓存吸收为 O(1)）；列表徽标仍按全局 30 秒周期（`period≠30` 条目不符）；`getEntry` 对小写 hex id 有一处放宽；**③ 的 `flowOn` 未做**并转登 `ISSUE-P3-154`（数据层单例无注入调度器，写死 `Dispatchers.Default` 会把投影放到测试虚拟时钟无法控制的线程池）。**过程留痕**：**工作区 220 个文件行尾为 CRLF/mixed**（违反 `.gitattributes`），本批触及的 5 个文件已字节级归一化、其余约 215 个未处理；另三处自查修正（两套时钟 / 作用域选错 / 回归测试判别力不足） | `ISSUE-P2-89` / `ISSUE-P2-90` / `ISSUE-P3-149`（①②④ 闭环）+ 新登记 `ISSUE-P3-154` | [`114-列表页秒级重建与TOTP重算收敛批次.md`](batches/114-列表页秒级重建与TOTP重算收敛批次.md) |
| §115 | IO 缓冲、XML 写出批量化与落盘附件重复读盘批次（同轮排查**第二批**；两条条目**开工复核各撞出一处前提问题**，均就地更正）：**① `ISSUE-P3-151` ② 修掉一处真缺陷** —— `escape` 以 `writer.write(codePoint)` 逐码点写出，而 `Writer.write(int)` **只写低 16 位** ⇒ 非 BMP 字符（emoji）被写成私用区字符、**保存即静默损坏**（变异验证实测 `expected:<[😀…]> but was:<[开头…]>`，全仓此前无代理对用例）；改为按「无需转义的连续区间」批量写出，代理对按 UTF-16 原样搬运。**② `P3-151` ① 前提更正**：`CipherInputStream/OutputStream` **没有缓冲尺寸构造参数**（内部固定 512 B），「一行改动」只适用于 GZip 三处（传 64 KiB）；AES 侧块粒度需自研分块流（与既有 `transform` 契约不兼容）⇒ **转登 `ISSUE-P3-155`**。**③ `P3-151` ③**：ISO-8601 正则嗅探（每 `<Times>` 调 5 次）改结构化前缀判定（语义逐一等价）。**④ `ISSUE-P3-150` 前提整体更正**：「整树 `copy` + 附件字节 `copyOf()` 是浪费」被复核**推翻** —— 那是**有意的别名隔离契约**、由 P1-9 用例锁定，**刻意不改**（登记 `已知工程限界.md` §1.7）；真实成本面是 `candidateItem` 恒 `withFlags(...)` 新建实例使**实例级 `lazy` 哈希缓存每次保存作废** ⇒ **每次保存把全部落盘附件重新读盘一遍**；改为 `flags` 未变即复用来源实例 ⇒ 读回次数恒为 1。**验证**：全量 **1m 43s 绿**，`tests=1988 skipped=13 failures=0 errors=0`（+3）；新增 3 例；**两处负向对照实测变红后回填**。**边界**：无真机性能实测；落盘读盘次数为**宿主侧假 store 计数**证据；嗅探用例是**契约锁**（新旧实现均通过）；未做「不含 emoji 库的新旧产物二进制对拍」；① 的 cipher 侧与编辑路径转登。**同轮附带**：工作区 **215 个文件**行尾归一化为 LF（`git status` 的「M」经 `porcelain=v2` 核实为 **blob OID 相同**的 stat 缓存假象 ⇒ 零提交内容）。**过程留痕**：全量首跑两项失败 —— 新增用例的 `expected:<1> but was:<2>` 判为**编辑期与并发构建重叠的编译窗口瞬态**（模块级 `--rerun-tasks` 与复跑均绿为证）；`BreachCheckHealthTest` 的 `Dispatchers.Main was accessed…` 属 §18 已记载的**跨用例污染类偶发红**（与本批无因果关系），据此顺手把 §114 引入的 `VaultListViewModelTest` 补齐 §18「先取消作用域、再 `resetMain()`」口径 | `ISSUE-P3-151` / `ISSUE-P3-150`（闭环）+ 新登记 `ISSUE-P3-155` / `ISSUE-P3-156` | [`115-IO缓冲与写出批量化及落盘附件重复读盘批次.md`](batches/115-IO缓冲与写出批量化及落盘附件重复读盘批次.md) |
| §116 | 常驻轮询收敛与完整性重扫参数守卫批次（同轮排查第三批；两处轮询**形似而性质相反**）：① `UnlockedNotificationController` 的 **2 s** `settingsStore.load()` 轮询属**真浪费**（自陈理由「同步 API 无 Flow」已过期——store 自 ISSUE-P2-21 起已有进程级 `settings: StateFlow`，且设置页恒 `publish + save`）⇒ 改为**订阅快照**（零 IO、变更即达），删除轮询常量与 `delay`/`flow`；② `RuntimeIntegrityDetector` 的 **30 s** 重扫（ISSUE-P2-63）是**注入检测时效的上界**，放宽即削弱检测面 ⇒ **刻意不改**，登记 `已知工程限界.md` **§3.5**，两常量 `private → internal` 并以契约用例锁「不得放宽」（放宽即红、收紧不限）。验证：新增 5 例；负向对照（改回轮询 ⇒ 守卫两例同红，备份逐字节还原）；全量见提交信息。边界：①「零 IO」为结构性结论（未做设备侧计数/耗电实测）；①存在「文件 vs 进程内快照」的行为差异；②用例**只锁参数不锁行为**；未做 `load()` 调用面全仓清理。过程留痕：接线守卫首跑**误判**（负向断言命中 KDoc 里刻意引用的旧写法）⇒ 改为先剥离注释再断言 | `ISSUE-P3-152` | [`116-常驻轮询收敛与完整性重扫参数守卫批次.md`](batches/116-常驻轮询收敛与完整性重扫参数守卫批次.md) |
| §117 | 仓库投影流补 `flowOn` 批次（`ISSUE-P3-154`，§114 的**残留**收口——`ISSUE-P3-149` ③）：`getEntries()` / `getGroups()` 的整库投影（逐字段解密 + 时间格式化）此前在**收集上下文**执行（列表页为 `viewModelScope` = Main）；§114 只把**频率**降为「每次数据变更」，**执行线程未变**。按条目原文修法：数据层新增可注入限定符 `@VaultProjectionDispatcher` + Hilt 模块（生产 `Dispatchers.Default`，对齐展示层 `@EntryDisplayDispatcher`），再补 `flowOn`；构造参数**刻意不给默认值**（默认值等于留一条悄悄退回收集上下文的路）；15 处测试构造点收敛为单一入口 `TestScope.newRepository(...)`（默认 `StandardTestDispatcher(testScheduler)`，与 `runTest` 同虚拟时间轴）。**验证**：新增 2 例——①**记录型调度器**（只计数派发、执行委托测试调度器）断言「未收集 0 / `getEntries()` 后 >0 / `getGroups()` 后继续增长」（两条流**分开计数**）；②`assertSame(Dispatchers.Default, …)` 锁生产绑定。**负向对照**：去掉两处 `flowOn` ⇒ 必红（`整库条目投影必须派发到注入的投影调度器`）✔ 后逐字节还原。全量 `--rerun-tasks` **1m 55s 绿**，`tests=1995 skipped=13 failures=0 errors=0`（§116 为 1993，+2）；Hilt 生成代码核实绑定已注入（缺失即编译期失败）。**边界**：无性能实测（不得宣称百分比收益）；只覆盖两条**整库**流，`getEntry(id)` **未**补（不得读作「仓库层投影已全部离开收集上下文」）；两条流独立派发后 `combine` 中间态对齐不再严格同步（终态一致）；`repositoryScope` 仍硬编码 `Dispatchers.Default`（**刻意不合并**，否则常驻作用域落在测试调度器上会 `UncompletedCoroutinesError`）；`FakeVaultRepository` 未补、接口**未**声明该契约 ⇒ 属实现属性非接口保证。**过程留痕**：条目前提更正（「40+ 处直接构造」实测 **15 处**）；用例名含点号触发 Kotlin `Name contains illegal characters` 而改名；**统计口径陷阱**（首次聚合得 1997，多出的 2 例来自 `updateDebugScreenshotTest/` 陈旧结果目录，剔除后与基线对齐）；顺带删 `getEntry` 上 §114 遗留的重复 KDoc 块 | `ISSUE-P3-154` | [`117-仓库投影流补flowOn批次.md`](batches/117-仓库投影流补flowOn批次.md) |
| §118 | 单条编辑路径增量擦除与路径复制批次（同轮排查**第四批**，`ISSUE-P3-150` 原正文 ③④ 转登；**两条前提经开工复核均成立**）：**① 路径复制**——原递归使**每个分组**都被 `copy()`（目标父组不存在时也整树重建），改为「命中即返回、未命中按同一实例原样返回」，`removeEntry` / `removeGroup` / `normalizeParentRefs` 同步；**语义变化 1 处**（重复分组 UUID 由「全部落树」改「首个命中」，依既有 API 的唯一性假设）。**② 替换关系回报**（`EntryEditResult` / `GroupEditResult` + `replaceRootGroup`）。**③ 增量定点擦除**（`KdbxGroup.eraseSupersededSensitiveData`）：候选**只由被替换节点收集一次**，逐级收窄（共享容器快径 → 减新节点可达者 → 减**新树任意位置**可达者 → 余下清零）；步骤 4 不可省（克隆体 / 历史快照 / moved 副本）。**④ 接线**：三条编辑器写路径走增量；删除路径维持不擦除；`updateDatabaseMeta` 保留通用实现，其单条分支转登 `ISSUE-P3-157`。**验证**：新增 **22 例**；全量 `tests=2017 skipped=13 failures=0 errors=0`（§117 为 1995）；`assembleDebug` 绿；**变异验证 6 项**（去掉全树扫描 ⇒ 克隆体与差分用例红；漏擦自定义字段 / 附件 / 历史 ⇒ 对应用例红；恢复整树重建 ⇒ 路径复制 2 例红；去掉快径 ⇒ 全绿，如实声明为纯性能快径）。**关键含义**：破坏路径复制后擦除用例仍全绿 ⇒ 擦除正确性的**唯一守护面**是 `assertSame` 系列。**边界**：无性能实测；候选收敛为**构造性结论 + 结构性守护**（无运行期计数）；差分对拍只覆盖 5 场景；步骤 4 仍是 O(全库) 引用扫描；增量入口要求**列全**被替换节点 | `ISSUE-P3-156`（闭环）+ 新登记 `ISSUE-P3-157` | [`118-单条编辑路径增量擦除与路径复制批次.md`](batches/118-单条编辑路径增量擦除与路径复制批次.md) |
| §119 | 单条条目原子读-改-写入口批次（同轮排查**第五批**；§118 同一路径族的**剩余面**——单条条目更新另有一条**不经编辑器**的 passkey 计数器补丁路径；**两处成本面前提经开工复核均成立**，无需更正正文）：**① 新增 `DatabaseSession.updateEntryById`**（→ `SessionContentMutations.updateEntryById`）——会话 Mutex 内**单次**变换完成「按 id 定位 → transform → 落树 → 增量定点擦除」（擦除复用 §118 的增量原语：候选只取自被替换的那一条旧条目）；返回**落树上线的实例**，未命中返回 `null` 且**零写入、不置 DIRTY**（旧实现命不中也会把同一实例写回并置 DIRTY ⇒ 顺带修正的边界行为）；入口**更新专用**（绝不新增条目）。**② 路径复制定位**（新增 `SessionTreeEditor.updateEntryById`）——按**条目 id** 递归（既有 `updateOrAddEntryIn` 按父组 id），命中即插入变换产物并回报替换关系，未命中分支按同一实例复用。**③ 接线**——`PasskeyEntryCoordinator.applySignCountPatch` 改走新入口并**删除 `replaceEntry`**（O(分组数) 整树重建消失）；回传值改为**从已提交条目读回**，与库内已提交状态同源。**④ `updateDatabaseMeta` 不放宽**（同步合并 / 远端接管 / 墓碑 / 图标引用清理仍是任意整库变换）。**原子性**：读现值 → 计算 → 落树同处一个临界区。**验证**：新增 **8 例**；全量 `--rerun-tasks` **1m 43s 绿**，`tests=2025 skipped=13 failures=0 errors=0`（§118 为 2017，+8）；`assembleDebug` 绿；审计闸门 **PASS**。**变异验证 2 项**：**M1** 注释掉定点擦除 ⇒ app 与 database **两级用例同时红**（`被替换下线的旧计数器实例必须清零 expected null, but was:<5>`）；**M2** 破坏路径复制 ⇒ 编辑器路径复制用例红，而 **app 侧同型断言仍绿**（目标条目是根组直接子项 ⇒ 走命中分支、不经过分组循环）⇒ app 侧不守护路径复制（`--rerun-tasks` 复跑确认真绿）。**边界**：无性能实测；候选收敛为**构造性结论 + 结构性守护**（无运行期计数）；**步骤 4 仍是 O(全库) 引用比较扫描**（passkey 补丁正是该情形）；入口语义收窄（单条 / 仅更新 / 变换产物不得为 null）；§118 的 M5 结论（`assertSame` 为擦除正确性唯一守护面）本批 M2 再次复现；其余 `updateDatabaseMeta` 调用面未动。**过程留痕**：database 级用例原拟断言「未命中不置 DIRTY」因前置 `saveEntry` 已置 DIRTY 而**不可观测**⇒ 改为断言「活动库实例不变（零写入）」，DIRTY 边界交由 app 级用例（OPENED 基线）守护 | `ISSUE-P3-157`（闭环，编号不复用） | [`119-单条条目原子读改写入口批次.md`](batches/119-单条条目原子读改写入口批次.md) |
| §120 | 设备无关项收尾批次（**无既有 ISSUE 编号**；用户指示「还有什么是不需要真机做的事情，全部完成」）：**① 补跑历年批次声明「未跑」的宿主侧闸门** —— `assembleRelease` **绿**（R8 + 资源收缩）、`lint` **绿**（app 209 warning / 0 error）、`cargo test` **57 passed / 0 failed**、语料 `--verify` 与伴生 JSON **逐字段一致**、**官方实现对拍**（`OwnProductInteropProbeTest` 产物经 `keepassxc-cli 2.7.12` 打开，`db-info` / `ls -R` / `export` 均成功，官方重编码时间解出 **63925224374 秒 = 2026-09-17T06:46:14Z** 与 `PROBE.md` 期望逐秒一致 ⇒ D1 时间编码端到端复核）、CVSS 闸门 **0 条达阈**（用 2026-09-10 存量报告，未重跑；本批无依赖变更）、审计闸门 **PASS**。**② `ISSUE-P3-158`（TOTP「全局 30 秒」口径）** —— 列表节拍以 `nowSec % 30` 算倒计时且只在 30 秒边界判翻转 ⇒ `period != 30` 相位错误；**更严重**：`period < 30` 且不整除 30（45 / 20）时「验证码滞留到下一个 30 秒边界」＝**界面显示过期验证码**；另两处进度环分母写死 30。修法：节拍通道由「剩余秒数」改为**秒级刻度**（**§114 性能契约逐条保留**：仍 `WhileSubscribed` / `flowOn` / 只在徽标内读取 / 整页状态不随 tick 重建），倒计时由徽标按 `entry.totpPeriod` 经 `OtpEngine` 现算，跨周期判据改「**任一条目自身周期序号变化**」，两处分母 + 详情页迷你环同取条目周期，预览缺省值改为使截图基线**逐像素一致**的刻度 0。**③ `ISSUE-P3-159`（语料脚本 `--check` 恒假失败）** —— `probe_cli` 用 `db-create --help` 的**退出码**判子命令，而 2.7.12 对 `--help`/`-h`/`-?` **一律先打印用法再 rc=1**（「缺少位置参数」）⇒ `--check` **exit=3** 并误导为「未安装 CLI」（而 `db-info`/`ls`/`export` 全可用）；判据改用**顶层命令清单**（`--help` rc 恒 0 + 文本含 `db-create`，命令名不随语言本地化）。**验证**：全量 `--rerun-tasks` **1m 43s 绿**，`tests=2031 skipped=13 failures=0 errors=0`（§119 为 2025，+6）；`assembleDebug` 绿；**变异验证 3 项**（重算判据退回 30 秒网格 ⇒ 两条 tracker 新用例同红；验证器分母回退 + 列表迷你环回退 ⇒ 接线守卫 2 例红）。**边界**：**无真机渲染验证** —— UI 侧证据是**接线守卫（源码文本，断言前剥离注释）+ 编译期契约**，不得读作「真机视觉已确认」；`period <= 0` 兜底 30 秒；CVSS 为存量报告。**仍需真机 / 需产品口径裁决者已如实列出**：`P3-153`（真机吞吐对比）、`P3-155`（AC 要求设备侧吞吐再决定采纳）、`已知工程限界` §8（设备无关**但需口径裁决**；复核发现设置页**只投影不展示** KDF，方向 ② 工作量大于字面）、§1.6、设备侧用例与视觉核对。**过程留痕**：新用例初版断言「订阅后首拍即重算」实测 `expected:<1> but was:<0>`（`nowSeconds` 初值即当前刻度、首拍被 `distinctUntilChanged` 吸收，**旧实现同理**）⇒ 改为按既有「订阅本身不等于取码」口径断言（断言写错而非实现错）；另误将全仓 grep 送入后台（3 MB 无关输出，已停、工作区未改动） | 无既有编号（同轮登记并闭环 `ISSUE-P3-158` / `ISSUE-P3-159`） | [`120-设备无关项收尾批次.md`](batches/120-设备无关项收尾批次.md) |
| §121 | 算法与数据结构专项批次（**第一档：零风险局部项**；入口是同日登记提交 `9b0be31`——用户提出「看看是不是坏算法 / 有没有坏数据结构」后的五路并行静态审计共登记 23 条，本批闭环其中 4 条）：**① `ISSUE-P3-162`** 分组索引缺失导致的平方级投影——`GroupPathPresenter.pathsOf` 原对每个分组各调一次「自建索引」的 `fullPathOf`（G 次 `associateBy`）、面包屑 `allGroups.find{}` 全表线性查找 + `breadcrumbs.add(0,…)` 头插、回收站后代递归 `allGroups.filter { it.parentId == … }`（每节点重扫全表）；改「一次 `associateBy` + `groupBy` 全程复用」+ `ArrayDeque.addFirst` + `childrenByParent` 单趟 BFS，两处名称排序由 `sortedBy { it.title.lowercase() }`（选择器在**每次比较**中调用）改 `compareBy(String.CASE_INSENSITIVE_ORDER)`。**语义变化 1 处**：面包屑新增环截断（原遇环状父链**无限循环**）。**② `ISSUE-P3-172` ①②③**：`AutofillFieldScanner.tokensOf` 的函数内正则提为对象级常量；`WebDavPropfindParser` 的 `DocumentBuilderFactory` 与 3 个 `SimpleDateFormat` 改按线程缓存；`KdbxXmlParser` 的 XXE 加固探针（原每次解析 5 次「工厂+解析器」实例化）改 companion `by lazy` + `ThreadLocal` 工厂（「为什么必须探测」的 P1-12 依据全文迁入 companion，未删一句）。**③ `ISSUE-P3-173`**：Base32 字母表线性查找改 256 项反查表、`MutableList<Byte>` 装箱改「两趟法 + 精确预分配」、`10.0.pow` 改 `POW10` 表（越界回退原路径，饱和行为逐字保留）。**④ `ISSUE-P3-181`**：密钥文件由「无条件复制去空白」改「单趟探测恰 64 且全 hex 才构造」、CSV 路径改 `ArrayDeque` 栈 + 组级拼一次、`<Tags>` 三中间集合改单趟扫描。**验证**：全量 `--rerun-tasks` **1m 40s 绿**，聚合 **`tests=2045 skipped=13 failures=0 errors=0`**（§120 为 2031，**+14**）；审计闸门 `PASS`（1308 行）；新增 14 例（接线守卫 6 含**断言前剥离注释**、密钥文件形态边界 5 以 `MessageDigest` 独立直算期望值、标签解析 3 把旧表达式**原样抄录**进用例）；`ISSUE-P3-181` 另有子代理的**非 Gradle 差分对拍**（77 019 次比对 0 失败）作**旁证**。**刻意不做并登记 `已知工程限界.md` §9**：`P3-172` ④ 的 `Mac` 实例复用——复用必然让 `Mac` 长期持有密钥副本，与调用方 `fill(0)` 的前提冲突，且 TOTP 取码已有周期缓存。**边界**：**无任何性能实测**（不得宣称百分比收益）；两处缓存均为**按线程**（相关类型均非线程安全）；`KdbxXmlParser` 告警由「每次解析」变「每进程一次」（唯一行为差异）；`stripWhitespace` 另两处调用方未动；排序比较器在极少数码点上与 `lowercase()` 可能不同。**过程留痕**：① 首版面包屑循环误写变量名（编译前自查修正）；② 守卫用例首跑 **1 例失败**——断言写宽误伤合法单趟过滤，收紧后复跑全绿；③ 该失败使首轮在 `:app` 中止（**database / core / sync 未执行**，该轮不构成任何验证结论） | `ISSUE-P3-162` / `ISSUE-P3-172`（①②③）/ `ISSUE-P3-173` / `ISSUE-P3-181`（闭环）+ 刻意不做项 → `已知工程限界.md` §9 | [`121-零风险局部项与守卫用例批次.md`](batches/121-零风险局部项与守卫用例批次.md) |
| §122 | 同步变化判定口径声明与语义锁定批次（`ISSUE-P2-91`，清单**顶部**条目；**开工前提复核不成立 ⇒ 改定修法**）：原条目称「变化检测漏比 `times` 与历史内容 ⇒ 只改 `times` 的编辑不上云」，并留「是否存在可达的、只改 `times` 的编辑面」为未核实项。**复核结论是否定的**：① 生产代码**无任何** `expires` / `expiryTime` 写入者（命中仅 XML 读取端 `KdbxXmlTimesNode` 与默认值 `KdbxXmlTimeHelper`）；② `times` 的改写只经 `KdbxEntry.withField`（**同时**改 `fields`）与 `PasskeyEntryCoordinator` 的计数器补丁（**同时**改 `customFields`）⇒ 该 `times` 变化**必然伴随**已被检出的变化；③ `usageCount` / `lastAccessTime` 无独立写入者（唯一写入点是 `withModified()`）；④ 合并路径写 `times` 发生在冲突流程内、其后必然上传，不经「判无变化即复用旧字节」的决策 ⇒ **「静默丢弃本地改动」撤销、P2 严重度不成立**。**另一面（故明确不做）**：照原 AC 补 `times` 比较**反而有害** —— `KdbxTimes` 混装使用性字段与 `lastModificationTime`，`withField` 经 `withModified()` 一次性改写三者，纳入比较会让「触碰但内容等同」被判成变更 ⇒ 触发重序列化 + 上传 + **前移远端 ETag**，正是该检测器存在的理由（其 KDoc 记 F5：KDBX4 随机 IV 使重序列化字节必然漂移）；合并侧**必须**比 `lastModificationTime`，是因为它回答的是「相对 base 是否被修改」（冲突裁决）这一**不同问题** ⇒ 按原 AC 的**另一分支**执行（「两处判定若需保持不同口径，须在 KDoc 显式声明差异与理由」）。`history` 内容同理不做：可达的历史变更（追加快照 / 按保留期修剪 / 合并并集）**必然改变条数**。**本批实质产出**：① 判定逻辑（原三个 private 函数）逐字搬运为 `internal object KdbxContentComparator`（**动机**：该判定此前**零用例覆盖**——全仓无任何用例引用 `resolveLocalContentChanged`——而它是「本地编辑被静默丢弃」的唯一判据；抽出后可脱离 `SyncDatabaseCodec` / `SyncSessionState` 直测，与本仓 §108 的 `ClipboardClearPolicy` 先例同构）；② **双向口径声明**——新比较器写明「本比较回答『是否需要重新序列化上传』、刻意不比 `times`」并附前提复核结论，`KdbxEntryMerger.isModified` 的 KDoc 增补互补声明（**必须**比 `lastModificationTime`）与「两处口径刻意不同、勿任一侧顺手对齐」的警示；③ **语义锁定用例 6 例**（条目 15 个变异项 + 分组 12 个变异项 + 「id 在新树中缺失」两条分支，**每项双向断言**；并把「仅 `times` 变化」与「历史条数相同内容不同」两条**刻意不判为变化**的口径钉成可执行断言——后者若日后引入等条数改写路径即失效判据）。**验证**：全量 `--rerun-tasks` **1m 37s 绿**，聚合 **`tests=2051 skipped=13 failures=0 errors=0`**（§121 为 2045，**+6**）；审计闸门 `PASS`（1308 行）。**处置**：`ISSUE-P2-91` 按前提复核**撤销**（清单 P2 段回到 0 项），真实残余（两处判定口径刻意不同 + `history` 只比条数）作为**口径而非缺陷**登记 `已知工程限界.md` **§10**（含「**外部客户端**写入只改 `times` 的条目」这一未覆盖面的解除条件）。**边界如实声明**：根级 `changed()`（`deletedObjects` 比较 + 委托）**未覆盖**（需 `KdbxHeader` / `DeletedObject` 夹具，本批未做）；`KdbxAttachment` 未覆写 `equals`，故附件列表实际是**实例级**比较（方向安全但偏保守）；本批**未改同步决策路径**（`SyncCycleRunner` 一行未动），无真机 / 联调验证。**过程留痕**：① 用例首版「子分组内容」变异项与基线**等价**（同 id 同 name）⇒ 断言会假红，跑测试前自查修正为带 `notes` 的真变化（未进构建）；② 首版曾按 AC 原文准备直接整比 `times`，读到 `KdbxTimes` 字段构成与 `withModified()` 改写面后放弃——与 §121 的「`Mac` 复用」同属**对条目 AC 的实质修正** | `ISSUE-P2-91`（**前提复核后撤销**，非「已整改」）+ 残余登记 `已知工程限界.md` §10 | [`122-同步变化判定口径声明与语义锁定批次.md`](batches/122-同步变化判定口径声明与语义锁定批次.md) |
| §123 | 批量树操作单趟化批次（第二档「改循环结构」的头两条，均属「每条目重走整棵树」族）：**① `ISSUE-P3-160`** —— `SessionContentMutations.batchDeleteEntries` 原 `for (id in entryIds) removeEntry(...)`、`batchMoveEntries` 原对每个待移条目各调一次 `removeEntry`，而 `SessionTreeEditor.removeEntry` 又**无条件** `group.subgroups.map { … }` 递归整棵树并新建列表 ⇒ 回收站 100 条 + 200 组时约 100 次全树遍历 + 约 2 万次临时 List 分配，且全程持会话 Mutex（锁持有时间被放大 K 倍）；新增 **`removeEntries(group, entryIds: Set<KdbxUuid>)`**（按 id 集合**单趟**剪枝、列表惰性分配、未命中子树**同一实例**复用、空集直接返回原实例），两处批量入口改用它；`removeEntry`（单条）同步去掉无条件 `map` 分配，**刻意未加**「命中即停止下降」的提前退出（避免在批量化之上再叠一处语义变化）。**② `ISSUE-P3-161`** —— `SyncConflictController.resolveConflicts` 原对每条冲突调 `applyResolvedEntryToGroup`，而后者对**每一层**都 `subgroups.map { … }` + `copy`（目标不在该子树时也照旧复制）⇒ O(裁决条目数 × 分组数) 次分组对象与列表分配（5000 组 × 100 条冲突 ≈ 50 万次 copy，且在 `Dispatchers.Default` + 会话锁内）；改为**先收集成表**（`resolvedByParent`，`getOrPut` 保持冲突原顺序；父组缺失落根组，与逐条实现 `?: group.id` 顶层即根组同语义）**再单趟落树**（新函数 `applyResolvedEntriesToGroup`，仅复制确实含目标的分组），降为 O(G + k)；同批把冗余的全限定名 `com.keepasskey.core.model.KdbxGroup` 收敛为已导入短名。**验证**：全量 `--rerun-tasks` **1m 40s 绿**，聚合 **`tests=2057 skipped=13 failures=0 errors=0`**（§122 为 2051，**+6**）；审计闸门 `PASS`（1308 行）。**新增 6 例**：批量删除与逐条删除的树结构**逐字等价**（含分组层级与各分组条目顺序）、深层与根级条目一次删净且兄弟保留、**未命中子树按同一实例复用**（`assertSame` 即 §118 路径复制契约的守护面）、空集与「id 全不存在」返回同一实例、单条删除未命中不更换实例；另加接线守卫 1 例（不得再逐 id 调 `removeEntry`、`removeEntries` 必须存在、冲突决议必须走批量函数且逐条实现不得残留）。**边界如实声明**：**无任何性能实测**（判据为结构性结论 + 契约用例，**不得**宣称百分比收益）；**P3-161 无新增行为用例**——新函数是依赖多重注入的 private 成员，宿主侧难以单独构造 ⇒ 等价性主要依赖**既有** `SyncCoordinatorTest` / `KdbxMergerTest` 与新加**结构守卫**，**不得**读作「已逐分支验证」；`removeEntry` 的**扫描范围未变**（只消除分配）；`batchDeleteEntries` 仍**无条件**置 `DIRTY` 并写回新库实例（含 id 全不存在的情形，与逐条实现逐字一致，**刻意不**顺手改状态机语义）；批量移动的**插入阶段未改**（`updateOrAddEntry` 仍是 O(深度)/条）；`parentGroupId` 指向的分组不存在时条目同样不落位（既有行为）。**过程留痕**：① 编辑期一次 `old_string` 因漏抄一个「的」而失败（`old_string not found`）⇒ 重新读取后逐字修正，**未进构建**；② P3-161 首版方案曾考虑「只改 `applyResolvedEntryToGroup` 签名」——那样外层「每条各走一次」仍在、复杂度降不下来，故改选「调用点收集 + 新函数单趟落树」 | `ISSUE-P3-160` / `ISSUE-P3-161`（闭环） | [`123-批量树操作单趟化批次.md`](batches/123-批量树操作单趟化批次.md) |
| §124 | 同一份数据重复计算收敛批次（第三档前两条）：**① `ISSUE-P3-166`** 健康检查——第一趟（建重用索引）`readUtf8()` + `sha256`，第二趟又 `readChars()` + `readUtf8()` + 重算 `sha256` ⇒ 同一条口令在一次扫描内**解密 3 次、哈希 2 次**；改为第一趟按条目 id 留档 `hashHex`（`passwordHashByEntryId`）供第二趟直接取用、**删除**第二趟的 SHA-256 调用 ⇒ 哈希 **2 → 1**、解密 **3 → 2**（两趟取哈希的前置条件逐字相同故复用必然命中）；明文中间量仍在各自 `finally` 中清零，未引入跨条目持有。**刻意不做两处**：① 不把 `passLength` 改用 `ProtectedString.length`——`length` 是**密文=明文字节数**（CTR 无填充），而现有实现用 `passChars.size`（**字符**数）并写进用户可见文案「密码过弱（长度: N…）」，改之会让非 ASCII 口令显示字节数（6 个汉字显示 18），属用户可见语义变更；② 不做 `title`/`userName` 的「每条目读一次」——这两个 getter 每次都是一次解密 + `String` 物化，但**只出现在「已判定有问题」的分支内**，提到循环开头会为绝大多数「无问题」条目**凭空新增 2 次解密**（把优化做成回归），等价且无额外分配的写法需重构为「判定与构造分离」并保持 issue 顺序（EXPIRED→WEAK→REUSED），对 P3 项的回归面不划算。**② `ISSUE-P3-169`** 内存驻留加密——原 `seal`/`unseal` 每次 `Cipher.getInstance`、`hmacSha256` 每次 `Mac.getInstance`（装载 1000 条目库 ≥1000 次两套 provider 查找 + 1000 次 HMAC，而等值标签多数从未被比较）；改为**按线程**复用（`ThreadLocal`，`Cipher`/`Mac` 均非线程安全故不可共享单例），`init` 仍按 IV/方向/密钥重做、每次 seal 仍取**全新随机 IV**（密钥流复用语义未变）；两字段声明在 `init { }` **之前**（object 属性初始化按声明顺序，`init` 经 `hmacSha256` 触达 `eqMacs`，顺序错误会在对象初始化期 NPE）。**不扩大秘密驻留面的论证（须与 §121 的相反结论区分）**：复用实例持有的 `SecretKeySpec` 只是 `encKey`/`eqKey` 的副本，而这两把子密钥**本就在本对象字段里随进程存活**（类 KDoc 的 `P2-06` 结论：不轮换不擦除）⇒ 无新增驻留面；`OtpEngine` 的 `Mac` 复用被判定不实施（`已知工程限界.md` §9）是不同情形——那里是**每条目的种子**，复用会让调用方 `fill(0)` 的前提失效。**对条目 AC 的实质修正（第三处）**：AC ② 要求「等值标签改惰性计算」**原理上不可实施**——惰性算标签必须在首次 `equals`/`hashCode` 前**保留明文**供后来 HMAC，而本类设计目的正是「堆中不驻留明文」，故**判定不做**。**验证**：全量 `--rerun-tasks` **1m 43s 绿**，聚合 **`tests=2059 skipped=13 failures=0 errors=0`**（§123 为 2057，**+2** = 两处接线守卫：`HashUtil.sha256(passBytes)` 只允许出现一次；`Cipher.getInstance(TRANSFORMATION)` / `Mac.getInstance(MAC_ALGORITHM)` 各只允许一次且 `sealCiphers.get()`/`eqMacs.get()` 必须真被取用）；审计闸门 `PASS`（1308 行）。**行为面依据**：`HealthCheckEngineTest`（5 例，含**直接覆盖 REUSED 路径**的「测试弱密码与重复密码识别」）与 `ProtectedStringMemoryEncryptionTest`（12 例：往返一致 / 等值标签一致且密文随机化 / 不同明文不同标签 / `clear` 后硬拒绝 / 清零后互不相等 / `useUtf8` 自动清零等）。**边界如实声明**：**无任何性能实测**（「哈希 2→1、解密 3→2」是**源码级调用点计数**，非运行期计数，**不得**宣称百分比收益）；**本批未把解密降到 1 次**——原 AC 的「可合并为 1 次解密」**不准确**，剩下的一次 `readChars()` 是保持「长度为字符数」文案口径的代价；`ThreadLocal` 在长生命周期线程池下**每线程各持一份**（不随线程回收释放），**不得**读作「进程级只创建一次」；守卫是**源码文本**断言，对「把调用点搬进辅助函数」的实现无鉴别力；`P3-166` 的 title/userName 重复读取**仍在**（有问题的条目上最多 6 次解密）；`P3-169` ② 未实施 ⇒ 1000 条目库仍有 1000 次等值标签 HMAC 计算本身（省掉的只是 `Mac.getInstance`）。**过程留痕**：① 对条目 AC 的**第三处**实质修正（前两处为 §121 的 `Mac` 复用与 §122 的「整比 `times`」），三处同源——AC 写在读实现之前，处置一致（读实现 → 就地判定 → KDoc 与批次文档写明依据）；② 自查 `ThreadLocal` 字段必须声明在 `init { }` 之前（否则运行时 NPE，函数体引用后续属性在 Kotlin 里合法） | `ISSUE-P3-166` / `ISSUE-P3-169`（闭环；`P3-169` ② 按「原理冲突」判定不实施并留痕） | [`124-同一份数据重复计算收敛批次.md`](batches/124-同一份数据重复计算收敛批次.md) |
| §125 | 同步接受路径摘要一次化批次（`ISSUE-P3-167`，第三档第三条）：**① 摘要一次化（已做）** —— 接受远端内容时对**同一份 `remoteBytes`** 原先算 3 遍全库 SHA-256（`SyncRollbackGuard.inspect` / `SyncCache.writeCache` / `recordAccepted`）；改为在 `SyncEngine` 接受路径上**一次算出**并贯穿：`SyncRollbackGuard` 新增「摘要已知」重载（`inspect(path, contentDigest)` / `recordAccepted(path, contentDigest)`，原字节重载改为**委托**故既有调用点零改动、**载荷格式与 MAC 计算未动**），`SyncCache.writeCache` 新增 `precomputedDigest: String? = null`（默认现算，既有调用点不变），`SyncEngine` 内 `isReplay` / `recordAccepted` 同样拆双重重载。**收敛范围如实界定**：只收敛**这一条**接受路径上的 3 → 1；其余 4 处重放裁决点与 ETag 缺失探测路径**仍各自现算摘要**（不共享同一份字节），**不得**据此声称「全链路只算一次」。**② 基线前移的「重复写盘跳过」判定不做**（登记 `已知工程限界.md` **§11**）——原建议「内容一致则不写第二份」，复核推翻：逐字节判定须先读全量内容并哈希，而 `advanceBaseAndPersist` **总在内容刚变更后被调用** ⇒ 在最常见路径上**净增**工作量；廉价推断（`basecache` 存在且 `baseversion` 已等于该摘要）**不安全**——写序 `updateBase` → `writeBaseContent` 之间的崩溃会留下「baseversion 已前移、basecache 仍旧字节」，跳过写盘即把**陈旧基准永久固化**，正是基准快照存在的意义所要避免的。两处写盘各自必要（缓存会被工作副本覆盖，基准必须解耦），故「两份完整内容并存」是设计而非缺陷。**验证**：全量 `--rerun-tasks` **1m 50s 绿**，聚合 **`tests=2060 skipped=13 failures=0 errors=0`**（§124 为 2059，**+1**）；审计闸门 `PASS`（1308 行）。**新增 1 例**（`SyncRollbackGuardTest`）：摘要重载与字节重载对 Accept / Unchanged / ReplayDetected **逐项一致**，并锁定「摘要重载记录/读取的正是同一份状态」；该文件既有 **8 例**（含 F23 三条不变式）与 **§90 的两条「重放被拒后本地缓存与基线不被覆盖」**用例在本批全量套件内全绿。**边界如实声明**：**无任何性能实测**（「3→1」是源码级调用点计数，**不得**宣称百分比收益）；**未做任何调用计数断言**——`SyncCache.sha256Hex` 是伴生对象静态方法，宿主侧无法在不改实现前提取数，等价性靠行为用例与源码级阅读承担，**不得**读作「已用运行期计数证明」；`precomputedDigest` 为**可选且不校验**的入参，传错会静默改变裁决（本批唯一调用方在同一函数体内由同一份字节算出，无漂移风险；新增调用方须保证「摘要与字节同源」）；**基线写盘行为零变化** ⇒ 「每次基线前移 2×N 字节写 + 2 次 fsync」「每个远端路径常驻两份完整密文」**依旧成立**。**过程留痕**：① 一次 `old_string` 因把多行 KDoc 按单行抄写而 `not found`（§123 亦有一次同类；两次根因同为「凭记忆抄 KDoc 而非先读再抄」），重新读取后逐字修正、**未进构建**；② 一处引用完整性自查——批次文档与 `advanceBaseAndPersist` KDoc 引用的 `已知工程限界.md` §11 已同批落盘（防 §101 那类悬空引用） | `ISSUE-P3-167`（① 闭环；② 判定不做并登记 `已知工程限界.md` §11） | [`125-同步接受路径摘要一次化批次.md`](batches/125-同步接受路径摘要一次化批次.md) |
| §126 | 自动填充评分逐条目物化收敛批次（`ISSUE-P3-171`，第三档第四条）：评分循环对**全库条目**逐条访问 `KdbxEntry` 属性 getter，而每个 getter 每次都要走 `fields[...]?.readString()`——**该构造器对所有字段一律密封**（`isProtected` 只影响 XML 写出），故每次访问都是一次驻留密文解密 + `String` 物化；并且对每条调 `PasskeyData.fromCustomFields`（原先一律先 `associateBy` 建 Map 再逐字段解密），而绝大多数条目根本没有 passkey 字段。**① `fromCustomFields` 加「不解密、不建 Map」的形状短路**：在 `associateBy` 之前只扫 key 名，三个必需键（`FIELD_RP_ID` / `FIELD_CREDENTIAL_ID` / `FIELD_PRIVATE_KEY`）缺任一即返回 `null`；**判据修正**——AC 原文写「先探 `Passkey.` 前缀键」，但前缀存在比「三必需键齐备」弱得多（前缀命中仍要建 Map 与解密才发现解析失败），故改为判定必需键齐备（既是最强判据，又与既有 null 语义逐字等价）；原 `?: return null` 处理保留未动（不使用 `!!`）。**② `AutofillCandidateRanker` 的 `url` 每条目只读一次**：`entry.url` 原被访问 2~4 次（包名维度、域名维度、`isExactDomain`、`isNotBlank`），改为函数开头一次读出并下传，`isExactDomain` 改签名接收 `entryUrl: String`（其 else 分支原本只读该值，语义逐字等价）；**AC ② 的原文路线（为 `KdbxEntry` 加不解密判定入口）判定不采用**——评分路径上**必须要 URL 文本**（`DomainMatcher.isDomainMatch` / `extractDomain` 都收字符串），故「不解密判定」只能省掉不匹配条目的读，而「2~4 次 → 1 次」已吃掉同一条收益且**不新增 API 面**。**验证**：全量 `--rerun-tasks` **1m 47s 绿**，聚合 **`tests=2065 skipped=13 failures=0 errors=0`**（§125 为 2060，**+5**）；审计闸门 `PASS`（1308 行）。**新增 5 例**：`PasskeyDataFromCustomFieldsTest`（新，4 例）锁定形状短路的判定等价——三必需键**缺任一**均 `null`（四个缺失组合逐一覆盖）、**普通条目的自定义字段（含受保护值）必须直接短路不产出对象**、三键齐备正常解析且可选字段取缺省、无关字段与必需字段并存仍解析要求字段；接线守卫 1 例锁定 `val entryUrl = entry.url` 只出现一次、`isExactDomain` 不得再接收 `entry`、形状短路段内**不得出现 `readString()`**。**行为面依据（既有）**：`AutofillCandidateRankerTest` 与 `PasskeyDataSignCountTest`（11 例）全绿。**边界如实声明**：**无任何性能实测**（均为源码级调用点与分支结构的结论，**不得**宣称百分比收益或推断真机填充时延）；**仍存在的每次播放成本**——评分仍对每个候选条目读一次 URL（域名匹配的输入，无法省）、真的带 passkey 字段的条目仍建 Map 并逐字段读取、`N=5000` 时仍是 O(N) 次评审 + O(N) 次 URL 读取 ⇒ **不得**读作「已与库规模解耦」；**未做分配/调用计数断言**（宿主侧无法在不改实现前提下统计解密次数），等价性靠行为用例与源码文本守卫，守卫对「把读取点搬进辅助函数」无鉴别力；AC ③（投影层一次算好并随 UI 模型缓存）属 UI 投影层的另一件事，**不在本批范围**；**`ISSUE-P3-170`（同一请求内证书摘要 ×2 / Keystore IPC ×3 段 / hex 格式化）仍未做**。**过程留痕**：① 首次全量跑撞上**已知偶发红**——`SyncCacheEvictorTest` 的 `F23 锁库清理缓存不得删除防回滚状态文件` 失败，断言消息给出「目录索引条目 = 磁盘真实存在的文件 = 仅 `<hash>.rollback`，而销毁器日志为『残留 1 项』」⇒ 判为 `已知工程限界.md` **§7** 的 Windows/NTFS 目录枚举鬼影（§107 已定位的第二个派生面），**定向复跑即绿**（4 秒）、随后全量重跑绿，**未改任何断言**（`isFile == true` 的真残留仍必红）；**该轮失败运行不构成任何模块的验证结论**（在 `:app` 中止，其余模块未执行）；本批为该鬼影的**第 3 次记录**；② AC 两处实质修正（前缀探测 → 必需键齐备；子项 ②判定不采用），与 §121 / §122 / §124 同源——AC 写在读实现之前 | `ISSUE-P3-171`（闭环；AC ② 判定不采用并留痕） | [`126-自动填充评分逐条目物化收敛批次.md`](batches/126-自动填充评分逐条目物化收敛批次.md) |
| §127 | 自动填充请求内重复读取收敛批次（`ISSUE-P3-170`，**第三档（同一份数据被算两遍以上）收口**）：**① 证书摘要只算一次** —— `AutofillOriginResolver.kt:41` 与 `AutofillDatasetBuilders.kt:160` 对**同一包名**各算一次调用方证书摘要（每次含 `getPackageInfo` + 逐签名者 `MessageDigest.getInstance` + 32~96 次 `"%02X".format`）；改为 `resolveUsableWebDomain` 增加**可选参数** `certDigests: CallerCertDigests = callingAppCertDigests(callingPackage)`（默认值使既有调用点**零改动**），`appendUnlockedDatasets` 先读一次并同时传给归属解析与包名维度绑定校验；**顺带一致性收益**——同一请求内两处判定恒用**同一份快照**（原实现两次读取之间若调用方身份变化会用不同摘要）。**时序变化如实留痕**：摘要读取由「DAL 校验之后」提前到之前（单请求内语义无变化）。**② Keystore 密钥句柄缓存** —— 每次字段屏蔽检查原走 `containsAlias` + `getEntry` + `Mac.init`（TEE/daemon IPC），新增 `@Volatile cachedKeyEntry`，**2 段 IPC 从每次调用降为每进程首次一次**；`Mac` **仍每次新建**（`javax.crypto.Mac` 非线程安全且 `getInstance` 不走 IPC）⇒ **「同请求内 Mac 复用」这一半判定不做**（省不到 IPC 却引入并发约束）。**失效面复核（安全前提，2026-09-17 检索全仓 `deleteEntry` 确认）**：别名 `com.keepasskey.autofill_field_signature` **无任何删除点**（命中项全是库内条目/分组删除），故无「外部删键后仍用旧句柄」失效面；`Mac.init` 失败即清缓存并返回 null（fail-closed），下次按原路径重建；**秘密面论证**——`SecretKeyEntry` 是 TEE 侧**不可导出**密钥的**引用**，缓存不落密钥材料；并沿用 `§80` 教训（仍以默认 provider 取 `Mac`、把 `AndroidKeyStore` 的 `SecretKey` 交给 `init`）。**③ hex 查表** —— `AutofillFieldSignature.toHexString` 与 `callingAppCertDigests` 的逐字节 `"%02x"`/`"%02X"`.format 改查表（`CharArray` + 位运算），后者同时把 `MessageDigest` 提到循环外**复用一次**（`digest()` 自带复位）；大小写口径逐字保持（字段签名小写 / 证书摘要大写）。**验证**：全量 `--rerun-tasks` **1m 45s 绿**，聚合 **`tests=2066 skipped=13 failures=0 errors=0`**（§126 为 2065，**+1**）；审计闸门 `PASS`（1308 行）。**新增 1 例（接线守卫）**：`autofillOriginResolver.callingAppCertDigests(` 在 `AutofillDatasetBuilders.kt` 中**只允许出现一次**、`resolveUsableWebDomain` 必须接受摘要快照参数、两处 hex 均不得残留逐字节 `format`、Keystore 源必须声明 `cachedKeyEntry` 且 `containsAlias(KEY_ALIAS)` 只允许一处。**行为面依据（既有，全绿）**：`AutofillFieldSignatureTest`（锁定签名**定长 hex 输出** ⇒ 直接守护查表改写）、`TestHmacFieldSignatureSource`、`AndroidPackageBindingPolicyTest`、`CallingOriginResolverAttributionTest`。**边界如实声明**：**无任何性能实测**（均为源码级调用点与结构结论，**不得**宣称百分比收益或推断真机填充时延）；**未做调用计数断言**（`callingAppCertDigests` / Keystore IPC 调用次数宿主侧不可观测，等价性靠行为用例与源码文本守卫，守卫对「把调用点搬进辅助函数」无鉴别力）；**Keystore 句柄缓存的 IPC 收益是「每进程一次」而非「永久一次」**（进程重启后缓存为空，仍需一次 `containsAlias` + `getEntry`；`obtainMac` 仍 `@Synchronized`，并发调用串行）；`Mac` 仍每次新建；AC ① 的「按包名短 TTL 记忆化」**未做**（跨请求记忆化需缓存失效口径与跨进程一致性论证，属另行裁决）；**`UnlockThrottleIntegrity` 的同族 hex 与 `take(16)` 装箱未做**（其别名派生若改算法会使**存量存在性标记失配** = 节流复位，须先定保值替换方案）；锁定态 fail-safe 语义未变。**过程留痕**：本批无编译 / 断言失败（首次全量即绿）；一处前置检索充当安全前提（若不做「全仓无删除点」检索就缓存句柄，等于把一个失效假设写进产品），该结论与日期一并写进就地 KDoc 供后人复核 | `ISSUE-P3-170`（闭环；「同请求内 Mac 复用」与「跨请求记忆化」两项判定不做并留痕） | [`127-自动填充请求内重复读取收敛批次.md`](batches/127-自动填充请求内重复读取收敛批次.md) |
| §128 | 字段引用解析索引一次化批次（`ISSUE-P3-163`，第二档（循环结构改造）剩余项之一）：`FieldReferenceEngine.kt:151` 原在 `REF_REGEX.replace` 的**回调体内**执行 `root.allEntries().firstOrNull { … }`，而该回调**每个引用出现处执行一次**、且解析本身递归（每层各展平一次）⇒ 复杂度 **`O(引用数 × 深度 × 条目数)`** 次整树展平（每次一遍递归遍历 + 一个新 `List` 分配）；改为在 `resolveWith` 入口建**一次**私有嵌套索引 `RefIndex(root, ::valueOf)` 并沿递归全程共享（`resolveInternal` 的 `root` 形参改 `index`）。**关键设计（对 AC 的一处刻意收窄）**：条目树用 `by lazy { root.allEntries() }` **惰性展平**（文本无引用时 `resolveWith` 已短路、根本不触发），**按检索字段分桶的索引同样惰性**——AC 原文建议的「`Map<RefField, Map<String, KdbxEntry>>`」若**一次性建全部字段**，会把**全库口令都 `readString()` 解密一遍**（与 AC 自身「按 `P` 检索不再对未命中条目解密」**相冲突**），属反向优化；现实现只在**实际被引用到的字段**上建索引 ⇒ 无人引用口令时**零口令解密**。**等价性要点**：索引键用 `TreeMap(String.CASE_INSENSITIVE_ORDER)` 而**非** `HashMap` + `lowercase` 归一化——`lowercase` 的等价类与 `equalsIgnoreCase` **不同**（希腊语词尾 sigma：`'ς'` 与 `'Σ'` 在 `equalsIgnoreCase` 下相等而 `lowercase` 不等），用错会造成「原本命中的引用变为未命中」的**静默漂移**；同值只保留**文档序首个**（`TreeMap.containsKey` 走同一比较器，与 `firstOrNull` 语义一致）。保护值判定路径（`isProtected` / 掩码占位 / `ResolveMode` 分支）**逐字未动**（条目「风险提示」的硬约束）。复杂度由 `O(引用数 × 深度 × 条目数)` 次展平降为**一次** `O(n log n)` 构建（仅被引用字段各一次）+ 每次查找 `O(log n)`。**验证**：全量 `--rerun-tasks` **1m 56s 绿**，聚合 **`tests=2070 skipped=13 failures=0 errors=0`**（§127 为 2066，**+4**）；审计闸门 `PASS`（1308 行）。**新增 4 例**：① 多个引用 + **嵌套链**（B 的用户名又是 `{REF:U@T:A}`）同时断言两层展开共享同一索引；② 同值多条目取**文档序首个**（`Same`/`SAME`）；③ **忽略大小写的等价类与逐字符折叠一致**（希腊语 `'Σ'` vs `'ς'`——若改用 `lowercase` 归一化此例会退化为「保持原文」，故该例同时是「为何选 `CASE_INSENSITIVE_ORDER`」的可执行依据）；④ 接线守卫（`allEntries()` 在该文件**只允许一处**且在 `by lazy` 内、回调内不得残留 `firstOrNull { entry ->`、索引键必须用 `CASE_INSENSITIVE_ORDER`）。**行为面依据（既有，全绿）**：`FieldReferenceEngineTest` 原 **19 例** + `FieldReferenceDisplayModeTest` **13 例**（展示侧掩码、递归深度不绕开掩码、深度上限与环状引用）。**边界如实声明**：**无任何性能实测**（复杂度为源码结构结论，**不得**宣称百分比收益）；AC 的「一次解析内 `allEntries()` 至多调用一次」**未做运行期计数**——`KdbxGroup` 是 final `data class`，宿主侧无法在不改生产代码前提下为 `allEntries()` 计数 ⇒ 以**源码文本守卫**替代，**不得**读作「已用运行期计数证明」；索引只覆盖「按字段值检索目标」这一步（`valueOf` 取值与递归深度控制未变；`UUID` 检索仍每条目 `toHexString()` 一次）；索引是**每次解析调用**而非**跨调用缓存**（详情页与列表分别解析时各建一次；跨调用缓存需失效口径，属另行裁决）；`TreeMap` 构建 `O(n log n)`、查找 `O(log n)`（非 `O(1)`），换取与 `equalsIgnoreCase` 完全一致的等价类，该代价已计入；**设备侧未验证**（`FieldReferenceEngineDeviceTest` 4 例本批未运行，它守护 ICU 正则兼容性、与本批改动无交集，但**不得**据此声称「设备侧已确认」）。**过程留痕**：本批无编译 / 断言失败（首次全量即绿）；对 AC 的一处**刻意收窄**并留痕（理由见上，且与 AC 自身第二句冲突） | `ISSUE-P3-163`（闭环；索引改为「按字段惰性」并留痕） | [`128-字段引用解析索引一次化批次.md`](batches/128-字段引用解析索引一次化批次.md) |
| §129 | 列表页整库投影离开收集上下文批次（`ISSUE-P3-174`，第四档「线程落点」首条）：`VaultListViewModel.kt` 的 `uiState` 由 `combine(…) { buildVaultListUiState(…) }.stateIn(scope = viewModelScope, …)` 构成——`stateIn` 的收集上下文即 `viewModelScope`（**Main**），而变换体内是全库过滤 / 排序 / 面包屑上溯 / 回收站后代递归 / 分组路径装配；`ISSUE-P3-154`（§117）只给**数据层**两条投影流补了 `flowOn`，**UI 侧这段变换的执行线程一直未变**。本批在 `stateIn` 之前补 **`.flowOn(displayDispatcher)`**（复用本 VM 已有的 `@EntryDisplayDispatcher`，生产为 `Dispatchers.Default`；补导入 `flowOn`），`combine` 输入流 / 变换体 / `SharingStarted.WhileSubscribed(5000)` / `initialValue` **逐字未改** ⇒ `ISSUE-P2-89` 的节拍契约（窄通道 + 订阅驱动）未受影响。**风险提示的实测处置**：条目记「该 VM 的调度器改动曾两次影响既有用例」，开工即核对测试侧——`VaultListViewModelTest.newViewModel` 早已以 `displayDispatcher = UnconfinedTestDispatcher(testScheduler)` 构造 VM（decorations 原就使用该调度器）⇒ 补 `flowOn` 后整库投影仍落在**测试调度器与同一虚拟时间轴**上、**不引入真实线程池**，故不会造成 §114 那类偶发红；本批全量一次通过（含 `VaultListViewModelTest` 20 例）即该判断的证据。**验收判据取「结构断言」而非「记录型调度器」**（AC 明列二者择一）：`DispatchRecordingDispatcher`（§117）需「订阅 `uiState` + 断言派发计数」的额外编排，而该 VM 已被注入 `UnconfinedTestDispatcher` ⇒ **在其上计数并不等于证明「离开了 Main」**，收益与复杂度不成比例；**代价如实声明**——该守卫只锁「接线未被删」，**不构成**运行期派发证据。**验证**：全量 `--rerun-tasks` **1m 57s 绿**，聚合 **`tests=2071 skipped=13 failures=0 errors=0`**（§128 为 2070，**+1**）；审计闸门 `PASS`（1308 行）。**新增 1 例（接线守卫）**：断言（剥离注释后）存在 `.flowOn(displayDispatcher)` **紧接** `.stateIn(` 的形态——该形态即 §117 为数据层确立的写法（中间若再插算子即改变语义）。**边界如实声明**：**无任何性能实测**（结构性改动，**不得**宣称「列表页不再卡顿」或任何百分比收益，真机帧率 / jank 未测）；守卫是**源码文本**断言，对「把变换体搬进别的函数」或「中间插算子」的部分退化**无鉴别力**，**不构成**「投影确实派发到了该调度器」的运行期证据；`displayDispatcher` 默认值仍为 `Dispatchers.Default`（**生产 Hilt 绑定未变**；若未来有实现方把它绑到 `Dispatchers.Main`，本改动即失效——该契约由 `@EntryDisplayDispatcher` 的模块提供，本批**未加断言**）；本批只处理 `uiState` 这一条，条目内的其它流（decorations / 窄通道刻度）沿用既有线程口径，**`ISSUE-P3-176`（冷流重复订阅与导航图重建）仍未做**；未跑设备侧与 `assembleRelease`（本批不涉 UI 呈现）。**过程留痕**：本批无编译 / 断言失败（首次全量即绿）；批次文档首稿在「审计闸门」一行**误写了一个不存在的脚本名并附了一段无内容的「笔误更正」**（属我自己制造的文档噪声），定稿已删去，只留真实执行的脚本名与结果 | `ISSUE-P3-174`（闭环；验收判据取结构断言并声明其局限） | [`129-列表页整库投影离开收集上下文批次.md`](batches/129-列表页整库投影离开收集上下文批次.md) |
| §130 | 完整性探测字节级化批次（`ISSUE-P3-178`，第四档「线程落点 / 分配面」第二条）：**① `TracerPid`** —— `TracedProcessProbe.kt:88` 每次调用新分配 8 KiB 数组并把整份 `/proc/self/status` 物化成 `String`（只为取一个字段），而它在**每次自动填充 / 凭据提供者请求**上同步执行；改为 `ThreadLocal` 缓冲复用 + 新增**字节级重载** `ProcTracerPid.parse(statusBytes, length)`（按 `\n` 分行、行首逐字节匹配，只物化**命中的那一行**的字段值），两个入口**共用同一个** `parseValue(raw) = raw.trim().toIntOrNull()` ⇒ 不可能漂移（**唯一可分辨差异**是孤立 `\r` 分行，`lineSequence` 把 `\r` 也当行界，而 `/proc` 伪文件不产生该形态，已在 KDoc 写明）。**② maps 扫描** —— `RuntimeIntegrityDetector.kt:245` 原把整份 maps 逐行解码成 `String` 再对每行做 6 次 `contains(ignoreCase = true)`（典型进程数千行 ⇒ 数千个 String + 6×数千次逐字符折叠比较）；改为**逐块读取 + 块间重叠**（保留 `maxMarkerLen - 1 = 8` 字节，保证跨块特征串不漏）+ 块内「首字节快速筛选 + 不区分大小写字节比对」，**全程零 String 分配**；并**新增有界上限** `MAPS_SCAN_MAX_BYTES = 16 MiB`（超限即停止 + 脱敏告警，fail-open）——该上限同时**封住「hook `read` 后喂无限流」的挂死面**（原 `useLines` 在无限流下会读到 OOM）。**关键可测性改动**：把流式匹配抽为**伴生对象**上的 `internal fun containsHookMarker(input, chunkSize = MAPS_SCAN_CHUNK_BYTES)`（它不依赖任何实例状态）——重叠窗口算错会**静默漏掉恰好跨块的特征串**，而那正是「改名 + 已注入」场景的检测依据，故作小块驱动逐项锁定。两份清单（`HOOK_MARKERS` / `HOOK_TRACE_PATHS`）**逐项未改**（`§92` 判据契约）。**刻意不做（AC ③）**：`Debug.isDebuggerConnected() \|\| waitingForDebugger()` 在 `detectSignals` 与 `escalateForLiveSignals` 各探测一次——两个布尔探针是**进程内静态查询**（无 IO、无分配），而收敛它需把「本次已探测信号」在两条路径间传递、动到 `§85` 锁定的「实时信号 ⇒ COMPROMISED」判定顺序，收益与回归面不成比例，**保持未做并如实声明**。**验证**：全量 `--rerun-tasks` **1m 42s 绿**，聚合 **`tests=2078 skipped=13 failures=0 errors=0`**（§129 为 2071，**+7**）；审计闸门 `PASS`（1308 行）。**新增 7 例**：跨块边界 5 种对齐、七种大小写形态、特征串远晚于首块（用例取真机实测**唯一泄漏点**的 `memfd:frida-agent-64.so` 形态）、全无命中不误报、空串 / 短串、**特征串清单必须全为 ASCII**（本批改动的前提不变量：非 ASCII 特征串会让字节折叠与原 `ignoreCase` 不再等价，触发即提示改实现）、接线守卫（两文件不得残留 `useLines` / 逐行 `contains(ignoreCase)` / `String(buffer, 0, read, …)`，必须有重叠 `arraycopy` 且两个解析入口共用同一 `raw.trim().toIntOrNull()`）。**行为面依据（既有，全绿）**：`RuntimeIntegrityDetectionSurfaceTest`（§92：逐项锁定两份清单 + 「两条防线不得被删空」+ 基线文档仍在）、`RuntimeIntegrityRescanContractTest`（§116：30 s / 120 s 参数契约，**放宽即红**）。**边界如实声明**：**无任何性能实测**（源码级分配与调用点结论，**不得**宣称百分比收益）；**`/proc/self/maps` 的真实读取路径未被任何用例覆盖**（宿主 JVM 无该文件，`exists()` 为 false）⇒ 以「流式匹配抽成可注入 `InputStream` 的 internal 函数 + 小块驱动边界」替代，**未做**「真实 maps 内容 + 真实 File 读取」的端到端断言，**不得**读作「maps 层已按真实文件验证」；`MAPS_SCAN_MAX_BYTES` 是**新增行为边界**（超 16 MiB 返回 false 并落告警，属**有意的 fail-open 取舍**，**不得**读作「检测面未变」——超大 maps 下确有收窄，只是不现实）；ASCII 折叠与 `String.contains(ignoreCase)` 的等价性**以清单全 ASCII 为前提**（已由用例锁定；日后加非 ASCII 特征串必须同步改实现）；AC ③ 未做 ⇒ 同一次调用内仍各探测一次；未跑设备侧与 `assembleRelease`，**真机 maps 行为未复测**（本批未改清单与判定门槛，故未安排）。**过程留痕**：① 首次全量**编译失败**（1 例：测试以伴生风格调用**实例方法** `containsHookMarker` 而 `Unresolved reference`）——修法是把该函数连同两个匹配助手**移入伴生对象**（本就不依赖实例状态），该次失败**未进入任何模块的测试执行**；② 第二次全量 **4 例断言失败**：`require(chunkSize > overlap)` 被测试的 `chunkSize = 8` 触发（最长特征串 9 字符 ⇒ 重叠 8，`8 > 8` 不成立）——**测试选参错误而非实现错误**，且该 `require` 恰好防住「分块小于重叠窗口」这一会静默漏报的危险配置，故**作为护栏保留**，测试改用 `chunkSize = 16` 并写入约束说明；③ 两次中间态均如实留痕，便于后人对齐基线时不会误用 | `ISSUE-P3-178`（①② 闭环；AC ③ 判定不做并留痕） | [`130-完整性探测字节级化批次.md`](batches/130-完整性探测字节级化批次.md) |
| §131 | 非惰性大集合渲染与组合期就地派生收敛批次（`ISSUE-P3-179`，第四档第三条）：**① 对话框分组选择（本批唯一的可用性缺陷修复）** —— `VaultListDialogs.kt:241` 原把全部分组 `filter{}.forEach{}` 铺进 `AlertDialog` 的 `text` 槽，而该槽**自身不滚动** ⇒ **分组多时超出屏幕的部分无法触达**（用户点不到）；改为 `LazyColumn(modifier = Modifier.heightIn(max = 280.dp))` + `items(selectableGroups, key = { it.id })`，过滤提到组合之外（`remember(allGroups)`，原写在 `forEach` 实参里、每次重组都重跑整表过滤）；「移动到根目录」固定项保留在 `LazyColumn` 之前（与 `AppPickerDialog` 同形态）。条目「风险提示」的处置：`AlertDialogContent` 会为 title / text / buttons 分配并约束可用高度，`text` 槽内的 `LazyColumn` 只占「剩余可用高度」内的 280 dp 上限，不会把按钮挤出屏幕；280 dp 为既有站内口径。**② 编辑页组合期过滤下沉 + key** —— `EntryEditFormSections.kt:101` 原在 `items(...)` 实参里做 `availableGroups.filter { !it.isRecycleBin }`（每次重组重跑）且无 `key`；因 `LazyRow` 的 content 是 `LazyListScope`（**非 `@Composable`**）无法在其中 `remember`，故下沉到函数体 `remember(uiState.availableGroups) { … }` 并补 `key = { it.id }`。**③ 面包屑补 key** —— `VaultListComponents.kt:249` 改 `items(breadcrumbs, key = { it.id })`。**④ 日志等级色预计算** —— `DebugSettingsScreen.kt:236` 原对每行做最多 4 次 `contains` 判色、**每次重组（含滚动引起的重组）都重跑**；改为 `remember(logLines) { logLines.map { it to <等级色> } }`，只在日志内容变化时算一次。**刻意不做（AC ① 的惰性化部分）**：卡片仍以单个 `item` 承载全部日志行——该卡片内部再放 `LazyColumn` 会在外层 `LazyColumn` 内引入**嵌套纵向滚动**（触达全量日志要「先滚内层再滚外层」），是 **UX 回归而非优化**；且日志行数**有硬上限 500**（`DebugLogBuffer.MAX_LINES`，**2026-09-17 读源码确认**，因该「刻意不做」的理由正建立在「行数有硬上限」之上）、本页又是**调试专用页** ⇒ 组合成本有界；彻底惰性化的正确形态是先把卡片移出外层列表语义，属结构性 UI 改动，不在本批范围。**验证**：全量 `--rerun-tasks` **1m 58s 绿**，聚合 **`tests=2079 skipped=13 failures=0 errors=0`**（§130 为 2078，**+1**）；审计闸门 `PASS`（1308 行）。**新增 1 例（接线守卫）**：对话框必须存在 `LazyColumn(` 与 `Modifier.heightIn(max = 280.dp)` 且不得残留 `allGroups.filter { !it.isRecycleBin }.forEach {`；编辑页不得把整表过滤写在 `items` 实参里且必须 `items(selectableGroups, key = { it.id })`；面包屑必须 `items(breadcrumbs, key = { it.id })`；日志页必须 `val coloredLines = remember(logLines) {` 且不得再有 `logLines.forEach { line ->`。**边界如实声明**：**无设备侧 / 视觉验证** —— 本批改动**全部落在 Compose 组合层**，宿主单测**无法渲染**这些 Screen ⇒ 证据是**编译通过 + 结构守卫 + 既有用例全绿**，**不得**读作「真机观感已确认」；尤其 ① 的**超长分组列表实际可滚动性**与「按钮未被挤出屏幕」（正是条目风险提示要求确认的点）**未经真机 / 预览核对**；**AC ① 只完成色彩预计算**，惰性化刻意不做 ⇒ 单帧仍组合最多 500 个 `Text`，**不得**读作「日志页已惰性化」；**结构守卫的局限**（源码文本断言，对等价写法无鉴别力——故除 `LazyColumn(` 外同时断言 `heightIn` 与「不得残留旧 `forEach` 形态」）；`heightIn(max = 280.dp)` 是**既有站内口径**（`AppPickerDialog`）而非实测最优值，**不得**读作「已按最长分组列表调优」；**未跑截图 / 预览基线**（未执行 `updateDebugScreenshotTest` 或预览导出，故未更新任何视觉基线；若 CI 侧有像素比对需另行复跑）；未跑 `lint` / `assembleRelease`；本批**未改任何 ViewModel 与状态层**（AC ③ 提到的「过滤下沉到 ViewModel」未采用，改在组合层 `remember`——收益等价且改动面更小）。**过程留痕**：本批无编译 / 断言失败（首次全量即绿）；一处**事前核对**——`DebugLogBuffer.MAX_LINES = 500` 由读源码确认（而非引用审计结论），因为「刻意不做」的理由建立在「行数有硬上限」之上，该前提不成立则结论不成立 | `ISSUE-P3-179`（①②③ 闭环；AC ① 的惰性化刻意不做并留痕） | [`131-非惰性大集合渲染与组合期就地派生收敛批次.md`](batches/131-非惰性大集合渲染与组合期就地派生收敛批次.md) |
| §132 | 节拍启停与逐条通道收敛批次（`ISSUE-P3-175`，第四档第四条）：**① 详情页节拍随订阅期启停** —— `EntryDetailViewModel` 原先在 `init` 里 `viewModelScope.launch(Dispatchers.Default) { totpTicker.run(...) }` 常驻启动（只要 ViewModel 活着，含页面退到后台栈 / Activity stopped，就每秒唤醒一次并做一次仓库调用）；改为把启动挂到 `uiState` 链的**订阅期**上——`.onStart { totpTickJob = …launch { totpTicker.run(…) } }` + `.onCompletion { totpTickJob?.cancel() }`，由 `stateIn(WhileSubscribed(5000))` 的取消语义驱动启停；`totpTicker.run` 的**循环体逐字未改**（含 `previous` 状态机与 HOTP 跳过），只改「谁来启动它」。**两处如实留痕**：1) **`StateFlow` 没有 `subscriptionCount`**（首版按 `uiState.subscriptionCount` + `collectLatest` 写，**编译期实测报 `Unresolved reference`**——该属性在 `MutableSharedFlow` 上）⇒ 改用 `onStart`/`onCompletion`，该事实留痕以免后人重试同一路径；2) `liveTotpCodeFlow` **仍为可写 `MutableStateFlow`**（`ISSUE-P2-65` 的会话锁定观察者要把它置 null 以擦除实时码）⇒ **不能**改成派生只读 `StateFlow`，这也正是本批未采用「冷流 + `stateIn`」重构的原因之一。**② 验证器页接入批量通道（前半）** —— `AuthenticatorViewModel` 的 `combine` 变换体内把逐条 `calculateEntryTotp(entry.id)` 改为先取一次 `calculateEntryTotps(filtered.map { it.id })` 再查表取用（格式化 / `isHotp` / 周期取值逐字未变），每拍挂起调用由 **T 次降为 1 次**（一次会话读取 + 一次索引，O(N+T)）。**② 后半（列表与倒计时解耦）转登 `ISSUE-P3-182`**：需改 `TotpCardItem`（去掉 `remainingSeconds`）+ `AuthenticatorScreen` 三处（`isUrgent` / 进度环 / 倒计时文本）+ 码的独立刷新通道，属**状态形状 + UI 联动**的独立改动，且回归面集中在宿主不可渲染的 UI；**转登理由**是该半项与本批已完成两项**无代码耦合**，混批会让「本批验证了什么」变得含混。**③ 列表页周期集合随快照缓存** —— `VaultListTotpTracker.entryPeriods()` 原每拍（1 Hz）重建 `filter` + `map` + `Set` 三个中间集合；现按 `currentEntries()` 的**实例身份**缓存（`entries !== periodsSnapshot` 才重算；生产传入 `{ uiState.value.entries }`，状态对象不可变故实例稳定），仅在节拍链（`flowOn(dispatcher)` 上的 `onEach`）内访问、无需同步。**验证**：全量 `--rerun-tasks` **1m 44s 绿**，聚合 **`tests=2080 skipped=13 failures=0 errors=0`**（§131 为 2079，**+1**）；审计闸门 `PASS`（1308 行）。**新增 1 例（接线守卫）**：详情页必须存在 `private var totpTickJob: Job? = null` 与 `.onCompletion { totpTickJob?.cancel() }`，且**切出 init 体后其中不得再出现 `totpTicker.run`**；验证器页不得残留 `calculateEntryTotp(entry.id)` 且必须出现 `calculateEntryTotps(`；列表页必须存在 `if (entries !== periodsSnapshot) {`。**边界如实声明**：**无任何性能实测**（三项均为源码结构结论，**不得**宣称百分比收益或推断真机耗电 / 帧率）；**AC 的两条运行期判据未按原文落地**（「1 Hz 刻度不触发整页状态新建」与「同拍内批量取码只调用一次」），本批以**源码文本守卫**替代（理由同 §129：宿主侧无计数通道，计数需引入测试替身与订阅编排），**不得**读作「已用运行期计数证明」；**详情页「离屏停止」是结构性结论**（依赖 `WhileSubscribed(5000)` 的取消语义），**未在设备侧观测**；**验证器页仍每秒重建整页状态**（`timerSecondsFlow` 仍在 `combine` 内）⇒「整页重建」这一半**未消除**，**不得**读作「验证器页已与秒级节拍解耦」；周期集合缓存以**实例身份**为键，若未来 `currentEntries()` 改为每次新建列表则缓存永不命中（不会更差，只是白留两个字段）；未跑设备侧 / `assembleRelease` / `lint`，**设备侧渲染未复测**（本批不改周期判据）。**过程留痕**：① 首版编译失败（误用 `StateFlow.subscriptionCount`）——在 `compileDebugKotlin` 阶段即终止、未进入任何模块的测试执行；② 第二版编译失败（1 例：`currentEntryOrNull(): UiVaultEntry?` 引入未导入类型名）⇒ 改为**返回类型推断**避免新增导入；③ **导入清理失误并当场修正**——编辑导入块时一次编辑**重复引入 `Dispatchers` 与 `launch`**（各两次），立即读取导入块合并去重（如实留痕：该重复只会是编译告警，但仍属我制造的多余改动）；④ **全量单测两次撞上已知偶发红（本会话第 4 / 5 次观测）**：两次均为 `SyncCacheEvictorTest`（先 `关闭密码库后同步缓存目录为空`、后 `锁库后同步缓存目录为空`），断言值为**大写键名**的 `<hash>.META.tmp`；**判据本身已是「磁盘实体」口径**（`syncDir.walkTopDown().filter { it.isFile }`）⇒ 本次**不属于** `§7` 的「枚举鬼影」面，而落在其**第二项已接受残余**（`isFile` 非百分百可靠，口径更正后仍有约 **3% 假阳性**；更强判别式「同路径短时有界重枚举 15 ms × 4，实测 380 例假阳性 0 命中」**刻意未采纳**以免时序依赖断言）；处置同 `§107`——**定向复跑 6/6 绿**（`--rerun-tasks`，每次 4 秒）、随后**全量复跑绿**（1m44s），**未改任何断言**（真残留仍必红，不构成放宽）；**与本批改动无因果关系**（三处改动均不触碰 SyncCache）；另记环境事实——该残余在**隔离运行**下命中率明显低于**全量运行**（全量首跑 2/2 红、隔离 6/6 绿），与 `§7` 的「与删后的目录索引有关 ⇒ 文件系统 churn 放大」解释一致 | `ISSUE-P3-175`（①③ 与 ②前半 闭环）+ 新登记 `ISSUE-P3-182`（②后半） | [`132-节拍启停与逐条通道收敛批次.md`](batches/132-节拍启停与逐条通道收敛批次.md) |
| §133 | WebDAV 首传重复探测收敛批次（`ISSUE-P3-180`，第四档第五条）：`WebDavSyncProvider.kt:272` 在 `expectedEtag == null` 时插一次 PROPFIND 探测 `Overwrite`，而上游 `SyncCycleRunner.establishRemoteBaselineIfMissing`（`:204`）**已经**用 `getMetadata` 探过一次并得 `FileNotFound` ⇒ 首传路径 `PUT + PROPFIND + MOVE`（+ 可能的 ETag 回退探测）**多一个 RTT**。**实现（存在性结论参数化下传）**：① `SyncProvider.uploadAtomic` 增第 4 形参 `remoteExists: Boolean? = null`（默认 null ⇒ **既有调用点零改动**），KDoc 写明「非 null 即调用方已探明、实现**不得**重复探测」；② WebDAV 侧 `Overwrite` 判定改 `val exists = remoteExists ?: getMetadata(remotePath).isSuccess`（仅未知才现探），`If` tagged-list 条件写 / MOVE 重试 / 回滚清理 / 412→`ConflictError` **逐字未动**；③ `SyncEngine.commitLocal` 增可选形参并透传；④ `establishRemoteBaselineIfMissing` 在 `FileNotFound` 分支传 `remoteExists = false`（复用**那一次**探测的结论）；⑤ `SyncEngineTest` 的 provider 替身同步接受并**记录** `lastRemoteExists`（与既有 `lastExpectedEtag` 同一手法）。**收益范围精确界定**：首传路径 `PUT + PROPFIND + MOVE` → `PUT + MOVE`（**少一次 PROPFIND = 少一个 RTT**）；其余 **5 处** `uploadAtomic` 调用点仍传 null ⇒ 行为与改动前完全一致，**不得**读作「全链路往返均下降」。**验证**：全量 `--rerun-tasks` **1m 51s 绿**，聚合 **`tests=2083 skipped=13 failures=0 errors=0`**（§132 为 2080，**+3**）；审计闸门 `PASS`（1308 行）。**新增 3 例（本批最强证据：真实 HTTP 请求计数，非结构断言）**，复用既有 `StatefulDavDispatcher` 有状态 MockWebServer（其 PUT/MOVE 均返回 `ETag` ⇒ 不触发 `:333` 的 ETag 回退）：① `remoteExists = false` ⇒ `server.requestCount` 增量**恰为 2**；② `remoteExists = null` ⇒ 增量**恰为 3**（**对侧保守语义未被改动的负向对照**）；③ `remoteExists = true` ⇒ 增量 2 且远端内容确实被覆盖为 `v1`（`Overwrite: T` 生效）。**回归面**：`WebDavSyncScenarioTest` 其余 30+ 例（并发竞态恰好一胜 / MOVE 失败回滚幂等 / 412 转 `ConflictError` / 编码与特殊字符 / 空文件 / 5xx 与 401）与 `SyncEngineTest` 全绿。**边界如实声明**：「少一个往返」是 **MockWebServer 侧的实测请求计数**，**未测真实网络 RTT**、未量化弱网收益，**不得**宣称「同步更快 N%」；**只有首传路径受益**（5 处调用点仍探一次 PROPFIND，属**未消化的既有成本**，本批未登记为缺陷）；`remoteExists` **可选且不校验**——传错不会损坏数据但会退化：错传 `false` 而目标存在 ⇒ MOVE 412 ⇒ `ConflictError`（fail-closed），错传 `true` 而目标缺失 ⇒ MOVE 404 ⇒ 抛错回滚（同样 fail-closed）；**`:333`（MOVE 成功但响应无 ETag 时回退一次 `getMetadata`）与 `:300`（412 分支取当前 ETag）未动**（前者依赖服务端是否返回 `ETag`，本仓 MockWebServer 恒返回故用例覆盖不到该分支的往返），**不得**读作「已消除全部重复探测」；**未跑 `-DliveSyncTest` 真实联调**（需先起 `tools/local-sync`）、未跑设备侧与 `assembleRelease`；接口签名变更影响面已核实——全仓**仅 `WebDavSyncProvider` 覆写**该方法（S3 走接口默认实现 ⇒ 条件写 PUT 本就无存在性探测），测试侧仅 `SyncEngineTest` 替身需同步，三处均已改、其余调用点因**默认参数**零改动。**过程留痕**：① 生产代码一次通过编译（未出现前几批那类 `old_string` 不匹配）；② **全量单测撞上一处新的宿主侧偶发红（本会话第 6 次，机制与既有两类均不同）**——`SyncRollbackGuardTest > 摘要重载与字节重载的裁决逐项一致`（§125 引入）失败，异常为 **`java.nio.file.AccessDeniedException`**，发生在 `SyncRollbackGuard.persist` 的**原子 rename**（`<hash>.rollback.<uuid>.tmp → <hash>.rollback`）：Windows 对「覆盖已存在目标」的 rename 在目标句柄未完全释放（索引器 / 杀软实时扫描）时会拒绝，而 POSIX / Android 的 `rename(2)` **无此语义**；**处置** = **定向复跑 8/8 绿** + 随后**全量复跑绿**（1m51s），**未改任何断言、未改产品代码**（在产品里加 rename 重试等于把宿主侧口径下沉进产品，且 Android 上永不触发 ⇒ 无靶加固），并按 `§7` / `§107` 的登记纪律登记为 `已知工程限界.md` **§12**（含「**不得**据此加 rename 重试 / 放宽断言」的硬约束与解除条件）；触发条件观测：隔离 8/8 绿、全量偶发 ⇒ 与 `§7` 的「文件系统 churn 放大」一致 | `ISSUE-P3-180`（闭环）+ 宿主侧残留登记 `已知工程限界.md` §12 | [`133-WebDAV首传重复探测收敛批次.md`](batches/133-WebDAV首传重复探测收敛批次.md) |
| §134 | 冷流重复订阅收敛批次（`ISSUE-P3-176` 的 **①**；第四档第六条）：`RealVaultRepository.getEntries()` / `getGroups()` 是**冷流**，而列表页 `VaultListViewModel` 的 `uiState` 与 `VaultListDecorationsProvider` **各订阅一次** ⇒ 一次数据变更做 **2 份**整库条目投影 + 2 份分组投影；详情页更密（`EntryDetailStateAssembler` 中 `getEntry(id)` 有**三处**消费者、`getGroups()` **两处**）。**实现**：**① 列表页** —— VM 新增两条共享投影流（`shareIn(viewModelScope, SharingStarted.WhileSubscribed(5000), replay = 1)`；`replay = 1` 让 `combine` 立即拿到最近值、启停与 `uiState` 同口径），`uiState` 的 `combine` 改用它俩（`getDatabases()` 本就只订阅一次、未动），`VaultListDecorationsProvider` 改为**消费传入的共享流**（构造参数 `entries` / `groups`）不再自行订阅 ⇒ 整库投影 **2 份 → 1 份**。**风险提示的核对结论**：两流的消费者只有 `uiState` 与装饰装配，后者只算图标与引用文案（**无副作用、不依赖「被订阅即重算」**），且都在 `displayDispatcher` 上执行、与 `§117` 的调度口径不冲突。**② 详情页** —— `assemble(inputs)` 内新增 `currentEntry`（`entryId.flatMapLatest { getEntry(it) }`）与 `allGroups` 两条共享流（同 `WhileSubscribed(5000)` + `replay = 1`），三处 `getEntry(id)` 收敛为一处、两处 `getGroups()` 收敛为一处；`decorationsFlow` / `groupPathFlow` / `displayPrefsFlow` 改为**接收**共享流；装配器新增构造参数 `scope`（由 VM 传 `viewModelScope`）——`shareIn` 必须有作用域而该装配器此前无作用域，**本批唯一的结构性 API 变更，全仓仅一处构造点**（已核实）。**验证**：全量 `--rerun-tasks` **2m 绿**，聚合 **`tests=2084 skipped=13 failures=0 errors=0`**（§133 为 2083，**+1**）；审计闸门 `PASS`（1308 行）。**新增 1 例（接线守卫）**：列表页必须恰有 **2 处** `shareIn(viewModelScope, SharingStarted.WhileSubscribed(5000), replay = 1)` 且 `combine` 内不得再出现 `combine(vaultRepository.getDatabases(), vaultRepository.getGroups())` 或独占一行的 `vaultRepository.getEntries(),`；装饰装配**不得**出现 `vaultRepository.getEntries()` / `getGroups()`；详情页装配器中 `getEntries(id)` 与 `getGroups()` **各只允许出现一次**。**行为面依据（既有，全绿）**：`VaultListViewModelTest`（20 例）、`VaultListDecorationsTest`、`EntryDetail*` 相关用例。**边界如实声明**：**无任何性能实测、也无投影计数** —— AC 要求的「同一次数据变更内整库投影只执行一次」是**运行期计数**，本批以**源码文本守卫**（订阅点数量）替代（`getEntries()` 是接口方法，计数需引入记录型仓库替身并编排订阅；`§117` 的 `DispatchRecordingDispatcher` 只能计派发、不能计「投影被执行几次」）⇒ **不得**读作「已用运行期计数证明」；**`shareIn` 改变了订阅语义**（新收集者拿 `replay = 1` 的最近值而不触发上游重算），本批已核对两个消费者无该依赖，但**未穷举**未来调用方；**仍存一处未共享的整库读取** —— `EntryReferenceDisplayResolver` 的 `loadEntries = { vaultRepository.getKdbxEntries() }` 是**一次性 suspend 读取**（非流），仅当有条目含 `{REF:}` 时触发（该解析器有无引用即零开销直返的快径），本批**未消除**，**不得**读作「列表页整库读取已收敛为一次」；`uiState` 的 `getDatabases()` 未共享（本就只订阅一次）、`getRepository.getEntry(id)` 单条投影仍未补 `flowOn`（`§117` 已声明该边界）；装配器新增 `scope` 是 API 变更（全仓仅一处构造，本批未加编译期之外的守卫）；**② 导航图重建未做、转登 `ISSUE-P3-183`**（与 ① 机制/文件面不同：① 是流订阅去重，② 是 Compose 状态宽度与 `NavHost` 的 `remember(builder)` 失效；② 会触及 `keepasskeyNavGraph` 形参与全仓导航调用点并须复核预览/截图基线）；**未跑设备侧 / 预览 / 截图基线**（本批影响 `uiState` 发射时序，界面未在设备侧核对）。**过程留痕**：① 一次编译失败（1 例）——`assemble` 头部改完后**漏改两处调用点**（`decorationsFlow(inputs.entryId)` 仍传 `Flow<String?>`、末尾 `.combine(vaultRepository.getGroups())` 未换共享流）⇒ 编译器报 `Argument type mismatch`，补齐后通过；该次失败在 `compileDebugKotlin` 阶段终止、未进入测试执行；② 一处**作业纪律偏离并如实留痕** —— 在 `EntryDetailStateAssembler.kt` 上**同一响应内连发 3 次 Edit**（违反「同一文件不得连发 Edit」），三处 `old_string` 互不重叠且文本唯一故未产生失效匹配，但遇到相邻/有交集的区域会静默改错位置，**后续须改为逐次读取后编辑** | `ISSUE-P3-176`（① 闭环）+ 新登记 `ISSUE-P3-183`（②） | [`134-冷流重复订阅收敛批次.md`](batches/134-冷流重复订阅收敛批次.md) |
| §135 | 保存路径历史修剪惰性分配批次（`ISSUE-P3-164`，第二档剩余项之一）：`DatabaseSession.save` 每次调用 `HistoryManager.pruneGroupHistoryByAge(rootGroup, 365)`（默认配置恒执行），而该实现每层无条件 `group.entries.map { … }` + `group.subgroups.map { … }` ⇒ **即便整库无一条历史快照，也为每个分组各分配两个新 List**（存量 `changed` 判定只避免了 `group.copy`）。**实现**：每层改为 `forEachIndexed` + **首次发现变化时才 `toMutableList()`**（两个列表各自独立惰性），`if (newEntries == null && newSubgroups == null) return group`。**语义逐字未变**：递归范围与判据（`maintenanceHistoryDays <= 0` 直接返回、`pruneHistoryByAge` 的「无历史 / 无超期即返回同一实例」）、「无变化 ⇒ 同一根实例」、「有变化 ⇒ copy 且未受影响子树/条目仍按同一实例进入新树」（路径复制契约）全部保持；调用方 `DatabaseSession.save:290-296` 依赖的 `prunedRoot !== db.rootGroup` 判定与其 `ISSUE-P2-06` 定点擦除分支**未动**。⇒ 消除的是「无人修剪时仍为每分组各分配两个列表」；**递归本身仍是 O(节点数) 的免分配扫描**（该固定成本未变）。**刻意不做（AC 第一半：O(1) 闸门）**：AC 建议「解析/变更时维护计数或惰性标记，无历史即跳过」——**不做**，三条理由：① **维护点必须覆盖全部写路径**（历史由 `recordHistorySnapshot` 写入，但整棵树还会经解析 / 编辑保存 / 冲突合并 / 远端接管 / 模板导入等**整体替换**路径）⇒ 漏一处即让修剪**静默失效**（属「用户数据不删」方向的静默行为变更，正是本条「保守不修剪」取向所忌）；② 收益的等价部分已被惰性分配吃掉（剩下的只是 O(节点数) 次「读字段 + 判 `history.isEmpty()`」的免分配扫描，与维护计数同阶）；③ 为省一次免分配扫描而引入「计数维护错即静默不修剪」的路径，风险不对称。若日后确需，正确形态是把计数收进**唯一变更入口**或在解析期维护「子树是否含历史」缓存（属独立的结构性改动）。**验证**：全量 `--rerun-tasks` **1m 50s 绿**，聚合 **`tests=2085 skipped=13 failures=0 errors=0`**（§134 为 2084，**+1**）；审计闸门 `PASS`（1308 行）。**新增 1 例**（`HistoryManagerTest`）：构造「含超期快照的子树 + 未超期条目的兄弟子树」，断言根实例更换 / 受影响子树被复制 / **未受影响的兄弟子树按同一实例复用**（`assertSame`）/ **未超期条目按同一实例复用** / 超期快照被清除——**该用例是「只消除分配」这一改动唯一的可观测守护面**（实现里若把「未变化」分支也复制一次，后两条断言即失败，而「修剪结果正确」本身不会失败）。**行为面依据（既有，全绿）**：`HistoryManagerTest` 原有 10 例（记录与回滚 / 数量上限 / 体积上限 / 零数量上限 / 默认容量 / 保留期修剪 / 保留期为零不修剪 / **无超期快照返回同一实例** / **整树递归且无变化免拷贝**）+ `DatabaseSession` 保存路径用例。**边界如实声明**：**无任何性能实测、也无分配计数** —— AC 要求的是**运行期计数**，本批以**路径复制契约用例**替代（它证明「未变化的子树/条目未被复制」，但**不直接证明**列表分配次数为 0——「复制了但内容相同」的实现仍可能通过）⇒ **不得**读作「已用运行期计数证明零分配」；**递归的 O(节点数) 扫描仍在**（免分配但仍是每次保存的固定成本），「每次保存都遍历整棵树做一次保留期判定」这一事实**未变**；`maintenanceHistoryDays <= 0` 的短路在函数入口，与默认 365 天配合 ⇒ **默认配置下每次都走完整递归**（触发条件未改）；未跑设备侧 / `assembleRelease`，**未做真机端到端保存验证**。**过程留痕**：一次编译失败（1 例）——新增用例用了 `assertNotSame` 而该文件未导入 ⇒ `Unresolved reference`，补导入后通过；该次失败在 `compileDebugUnitTestKotlin` 阶段终止、未进入测试执行 | `ISSUE-P3-164`（闭环；AC 的 O(1) 闸门判定不做并留痕） | [`135-保存路径历史修剪惰性分配批次.md`](batches/135-保存路径历史修剪惰性分配批次.md) |
| §136 | 导航图参数收窄批次（`ISSUE-P3-183`，第四档第七条；`ISSUE-P3-176` ② 的转登项）：`KeePasskeyApp.kt:61` 的根组合读取**整个** `SettingsUiState`（100+ 字段）并在 `:243` 传给 `keepasskeyNavGraph(appSettings = …)`，而 `NavHost` 以 `remember(route, startDestination, builder)` 建图 ⇒ builder 捕获的状态若含不稳定字段（`SettingsUiState` 带 `List<String>`、未标 `@Immutable`）则**任一无关偏好变化**（同步状态文案 / 健康扫描进度 / 调试开关……）都会让 builder 成为新实例 ⇒ 整图 `createGraph` + `setGraph`，重建全部 `NavDestination`。**关键侦察结论**：该图虽接收整个状态，**实际只用到 `themeMode` 两处**（`:39` 解锁页与 `:66` 列表页的 `currentTheme`）⇒ **收窄形参即可，图内任何路由定义都无需改动**。**实现**：① `KeePasskeyNavGraph.kt` 形参 `appSettings: SettingsUiState` → **`themeMode: AppThemeMode`**，两处 `currentTheme = appSettings.themeMode` → `currentTheme = themeMode`，导入由 `…settings.SettingsUiState` 换成 `…ui.theme.AppThemeMode`，类 KDoc 补「为何收窄」；② `KeePasskeyApp.kt` 调用点由 `appSettings = appSettings,` 改为 **`themeMode = appSettings.themeMode,`**，根组合其余字段的读取（`themePalette` / `showAuthenticatorTab` / `lockWhenNavigateBack` 等）**一律未动**（它们各自的 `remember(...)` 本已按字段为 key）。**为何收窄在此确实能止住整图重建（推理，非实测）**：`NavHost` 建图 key 含 `builder`，而 Kotlin lambda 无 `equals` 覆写 ⇒ 每次重组的新实例都会让 `remember` 失效，**除非**该 lambda 被 Compose 编译器**记忆化**，而记忆化前提是其捕获值**全部稳定**——`SettingsUiState` 含 `List<String>` 不稳定 ⇒ 原实现每次设置变化都重建图；`AppThemeMode` 是**枚举**（稳定）⇒ 收窄后仅主题变化才重建。**验证**：全量 `--rerun-tasks` **2m 绿**，聚合 **`tests=2086 skipped=13 failures=0 errors=0`**（§135 为 2085，**+1**）；审计闸门 `PASS`（1308 行）。**新增 1 例（接线守卫）**：`KeePasskeyNavGraph.kt` 不得出现 `appSettings: SettingsUiState`、必须出现 `themeMode: AppThemeMode`，且调用点必须传 `themeMode = appSettings.themeMode,`。**行为面依据（既有，全绿）**：全部导航 / 页面相关用例（形参改名后 `:app` 与全量单测均绿，含所有引用该图的预览与屏幕用例）。**边界如实声明**：**「不再整图重建」是结构性推理、不是运行期断言** —— 本批**未**用 Compose 运行时测试（如 `ComposeTestRule` 计数 `createGraph`）证明，证据是「形参收窄 + builder 捕获值变为稳定枚举」+ 源码守卫，**不得**读作「已在运行期验证导航图不再重建」；**依赖编译器的 lambda 记忆化行为**（strong skipping / 自动记忆化）——若该优化在某配置下关闭，收窄形参仍会因「每次重组新建 lambda」而失效，本批**未实测**该编译器开关状态，也未改用 AC 的备选方案「`remember(navController, themeMode)` 包一层」；**根组合仍读取整个 `SettingsUiState`**（`:61`）⇒ 根函数体仍会在任一设置变化时重组（其内部各 `remember` 已按字段为 key，不会连带重算），AC 的「根只读窄字段」本批以**另一等价选项**（图收窄形参）满足，若要彻底消除根重组须把根改成读约 10 个窄流（属独立改动，**不做**）；**未复核预览 / 截图基线**（AC 要求「涉及全仓导航调用点」须复核）——未跑 `updateDebugScreenshotTest` 或预览导出，**未更新任何视觉基线**；未跑设备侧 / `assembleRelease` / `lint`。**过程留痕**：一次编译失败（2 例报错、同一根因）—— `KeePasskeyApp` 的调用点**只加了新实参、未删旧实参**（`No parameter with name 'appSettings' found`），且导航图新增的 `AppThemeMode` **未加导入**（`Unresolved reference`），一并修正后通过；两处均属「改了一处、漏改配套处」（与 §134 同类）；另记 `AppThemeMode` 的实际包名为 `com.keepasskey.app.ui.theme`（我先凭猜测找错位置，改用全仓检索定位） | `ISSUE-P3-183`（闭环） | [`136-导航图参数收窄批次.md`](batches/136-导航图参数收窄批次.md) |
| §137 | 分组父链自愈单趟染色批次（`ISSUE-P3-165`，第二档剩余项（原列「安全面」））：`KdbxGroupMerger.sanitizeParentLinks` 原对**每个分组**从自身起上溯父链并用 `mutableSetOf(gid)` 逐组记录 `visited`，链状退化结构（远端可构造）下为 `O(G × d)`、最坏 `O(G²)`，且每组各分配一个 `Set`。**实现（① 已做）**：改为**一次函数图染色**——对「分组 → 父组」单趟遍历，节点状态 `ON_PATH` / `CLEAN` / `CYCLIC`；路径上命中 `CYCLIC` 或 `ON_PATH` 都意味着该路径**全体**节点均进入某个环（与旧算法「重现即判环」——含「父链进入环的链尾」——判据一致）；判定口径逐字保持（需挂回根组 ⟺ 父链为空 / 指向根 / 指向不存在的分组 / 进入环；各组判定均基于**原始**链接、互不影响 ⇒ 与逐组上溯**逐项等价**）；复杂度降为 **`O(G)`**（每节点至多走一次、路径复用同一缓冲）且不再每组分配 Set。**② 装配递归深度上限：已核实为不可达，故不加**（对条目 AC 的实质修正）——合并两侧树均由 `KdbxXmlParser` 解析，该解析器 **`MAX_XML_DEPTH = 64`** 对嵌套深度 **fail-closed** 封顶（`KdbxXmlParser.kt:89-90`，分组嵌套是 XML 嵌套的一部分），且 `subgroupsByParent` 由**净化后**父链构建（环与失链一律挂回根组，只会缩短链）⇒ 递归深度实际有界、**无可达栈溢出路径**；再加一个已被上游封顶的自有上限属**无靶加固**（多一处常量、且未来可能与解析器上限不一致而误拒合法库）。**依赖已登记并就地写进 `assembleGroupTree` 的 KDoc**：若日后放宽 / 移除该解析器深度上限，或引入不经该解析器的树来源（其它格式导入、插件通道），**必须**在此补自有深度上限或改显式栈、超限按既有失败语义 fail-closed 处理。**验证**：全量 `--rerun-tasks` **1m 50s 绿**，聚合 **`tests=2091 skipped=13 failures=0 errors=0`**（§136 为 2086，**+5**）；审计闸门 `PASS`（1308 行）。**新增 5 例**（`KdbxGroupMergerSanitizeTest`，新——**该函数此前零用例覆盖**）：① 自环（`a→a`）/ 互环（`b↔c`）/ 失链父组三类都必须挂回根组；② **父链进入环的链尾**（自身不在环上）也必须挂回根组（旧算法语义的关键面）；③ 健康子分组按**同一实例**透传（`assertSame`），而「直接挂根 / 指向根」的分组按**既有口径**仍走「挂回根组」分支（内容为 copy、父组仍是根）——**如实记录既有行为、未顺手改动**；④ **固定种子随机 300 张图**（每张 1~8 节点、约 1/6 无父组、其余随机指向含自身与不存在 id）与**参照实现**（改动前算法**逐字抄录**）逐项对拍；⑤ 200 节点长链结论正确（**复杂度本身不可由用例证明**，故只锁长链下结论正确）。**行为面依据（既有，全绿）**：`KdbxMergerTest` / `KdbxMergerV2Test`（合并端到端；`sanitizeParentLinks` 与 `assembleGroupTree` 的**唯一**调用方是 `KdbxMerger.mergeDatabases:112/121`）。**边界如实声明**：「最坏 `O(G²)`」是**源码结构结论**，本批**未**做运行期计时或计数，`O(G)` 的依据是「每节点至多走一次 + 路径复用缓冲」这一构造性事实，**不得**宣称耗时下降百分比；① 的「远端可构造」已如实限定——链状/环状父链要进入本函数需先经解析器与合并入口，其可行性上界同样受 §3 的解析器封顶约束 ⇒ 本批收益是**消除平方级退化**（对合法大库同样减少无谓遍历），**不是**修复一个已被利用的攻击；本批用例覆盖的是**判定面**，不含 `assembleGroupTree`（② 未做且其输入受上游约束，行为依据仍是既有合并用例）；随机对拍是**等价性证据、不是全域证明**（300 张固定种子小图覆盖自环/互环/链尾/失链/多环，**不构成**任意图等价的形式化证明）；未跑设备侧 / `-DliveSyncTest` / `assembleRelease`。**过程留痕**：① 一次**断言设计错误（与实现无关）**——首版对「父组即根组」的分组断言 `assertSame`，而既有口径对该情形本就 `g.copy(parentGroupId = rootId)`（旧算法同样如此）⇒ 用例假红，改为断言「父组仍是根组」并把该既有行为写进用例消息、**未改生产代码**；② 一处**前提复核改变了做法**——② 原被我登记为「须加深度上限」，开工后先去找**上游是否已封顶**（而非直接加常量），才发现 `MAX_XML_DEPTH = 64` 已把输入深度封死 ⇒ 改为「不加 + 登记依赖」，与前几批「AC 写在读实现之前」同源：**先读实现与上游约束，再决定改什么** | `ISSUE-P3-165`（① 闭环；② 判定不可达、不加并登记依赖） | [`137-分组父链自愈单趟染色批次.md`](batches/137-分组父链自愈单趟染色批次.md) |
| §138 | CBC 解密流缓冲复用批次（`ISSUE-P3-177`，第四档第八条；**主加密数据面**）：`CbcDecryptingInputStream.refill` 原每读满一个 64 KiB 块产生 `ByteArray(chunkSize)` + `concat` 合并数组（≈64 KiB+16）+ `pending = all.copyOfRange` + `transformBlocks` 的 `slice = source.copyOfRange`，随后整块清零并下轮再清一次 ⇒ 约 **4 次 64 KiB 分配 + 3 遍整块拷贝**（该路径服务原生 Twofish、即整库数据流）。**开工复核的关键事实**：`CbcBlockTransform` 的 KDoc 说「原地与否由实现决定」，但 `transformBlocks` 的 `finally { Arrays.fill(slice, 0) }` **已经依赖「变换返回独立结果数组」**（否则会把刚产出的明文自己抹掉、原生流式用例立刻变红）⇒ 事实上契约即「返回独立数组」，复用输入缓冲与该事实同源、不新增假设；本批据此把它由**隐式前提**升级为**显式校验**。**实现（解密侧：稳态 3 处 64 KiB 分配 → 0）**：① `chunk` 删除，改实例级 `buffer = ByteArray(chunkSize + Pkcs7.BLOCK_SIZE)`（头部 `[0,pendingLength)` 承载扣留末分组、其后为本轮载荷）；② `concat` 删除，`pending` 改「**偏移 + 长度**」（下一轮直接读进 `buffer[pendingLength..]`，末分组用同数组 `arraycopy` 原地前移）；③ 新增实例级 `bodyScratch`（长度恒 `chunkSize`），稳态把载荷 `arraycopy` 进去交给变换（零分配），**首块与尾部**（载荷 ≠ `chunkSize`）沿用原 `transformBlocks`（每流各至多一次）；④ 清零由整块收窄为载荷区 `Arrays.fill(buffer, Pkcs7.BLOCK_SIZE, filled, 0)`（不得覆盖已前移的末分组），`wipeBuffers()` 逐字保留、`close()` 追加清 `buffer`/`bodyScratch`/`chain`/`pendingLength`；⑤ 新增 `requireDistinctResult`：变换结果与 `bodyScratch` 别名即抛 `IllegalStateException`（把「下一块覆盖未交付明文」的**静默错误**变**响亮失败**）。**刻意不做（AC 的 `(offset, len)` 部分）**：`transformBlocks` 的 `copyOfRange` 与加密侧 `emitAlignedBlocks` 的 `buffer.copyOf(aligned)` 都由「变换只接受整数组」这一签名导致，去掉它们必须把 `CbcBlockTransform` 改成 `(key, chain, data, from, to)`；而唯一生产实现是 `NativeTwofish.decryptBlocks(key, iv, data)`（`TwofishCipherEngine.kt:78`），改动会触及 **JNI 定长布局契约**（`AGENTS.md` §3.2 跨 FFI 只传基本类型与数组）⇒ 属**跨 FFI 契约变更**，收益为每块 1 次分配、代价是主数据面 FFI 面改动，**本批不做**（加密侧同因此未做）。**验证**：全量 `--rerun-tasks` **1m 53s 绿**，聚合 **`tests=2092 skipped=13 failures=0 errors=0`**（§137 为 2091，**+1**）；审计闸门 `PASS`（1308 行）。**新增 1 例**：注入「原地返回入参」的变换，读到**稳态满块**那一轮必须以 `IllegalStateException` 失败且消息含「独立结果数组」。**行为面依据（既有，全绿）**：`CbcStreamFramingTest` 原 **10 例**（加密侧与 `CipherOutputStream` 多长度逐字节一致 / 小缓冲一致 / 逐字节写入一致 / **解密侧与 `CipherInputStream` 多长度一致** / 小分块一致 / 底层流逐字节供给一致 / 长度非整数倍与填充非法**同为 IOException** / 空输入语义一致 / 底层流 `IOException` 可见性不弱于基线）+ `TwofishNativeParityTest`（原生流式解密与 BC 流式解密同一明文）+ 数据库侧 `OwnProductInteropProbeTest`。**边界如实声明**：**无任何性能实测**（本批是分配/拷贝次数的结构性削减：稳态 3 → 0 次大数组分配，**未**做吞吐或耗时对比，**不得**宣称百分比收益）；「10 MiB ≈ 160 块」是**量级估算**非实测采样；**实例内存形态变化**——原实现每块临时分配由 GC 回收，现为**连接期常驻** `buffer`(64 KiB+16) + `bodyScratch`(64 KiB) ≈ **128 KiB/流**（该流同时至多一个实例，量级可忽略，但**不得**读作「内存占用下降」）；**清零语义逐条保留但位置有调整**（载荷区改为按 `[Pkcs7.BLOCK_SIZE, filled)` 区间清零，因末分组已前移、不得被覆盖；明文侧与 `chain` 的清零路径未变）；**未新增流式语义用例**（三类错误语义与分段对齐仍由既有 10 例覆盖）；**未跑 `keepassxc-cli` 官方 CLI 对拍**（§120 的跨实现对拍未复跑）⇒ **不得**声称「互操作已复核」；未跑设备侧 / `assembleRelease`；本条只触及 Twofish（原生）流式路径的框架层，AES / ChaCha20 走 JCE 不受影响。**过程留痕**：① **一次实现缺陷被既有用例当场抓住（3 例红）**——首版把「稳态载荷恒为 `chunkSize`」当作无条件前提，**忽略了首块尚未扣留末分组**（载荷为 `chunkSize - 16`）⇒ 首块多解密一个分组、结果错位，`CbcStreamFramingTest` 两例与 `TwofishNativeParityTest` **同时变红**（**这正是既有基线用例的价值**），修法为 `keep == chunkSize` 才走复用快路径；② **一次断言设计错误**——新增契约用例首版直接 `stream.read(64 KiB)`，而单次 `read` 只消费首个 refill 的产物 ⇒ 走不到稳态快路径、校验不触发（假红），改为 `stream.copyTo(...)` 并就地注明「必须读到稳态那一轮」 | `ISSUE-P3-177`（解密侧闭环；`(offset,len)` 契约部分判定不做并留痕） | [`138-CBC解密流缓冲复用批次.md`](batches/138-CBC解密流缓冲复用批次.md) |
| §139 | 验证器页倒计时解耦批次（`ISSUE-P3-182`，第四档：节拍与整页状态宽度；`ISSUE-P3-175` ② 的后半）：`AuthenticatorViewModel` 把秒级 tick 作为整页 `combine` 的**占位参数**（变换体内 `_` 丢弃），其唯一作用是**触发重算** ⇒ 每拍都新建整份 `items`（`List<TotpCardItem>`）且 `remainingSeconds` 每拍都变，全部卡片每秒重组。**实现**：① 删除 `timerSecondsFlow`；② 新增**内容流** `totpEntriesFlow = combine(getEntries(), searchQueryFlow)` → 过滤带 TOTP + 搜索词 → `flowOn(displayDispatcher)` → `stateIn(WhileSubscribed(5000))`（仅在条目 / 搜索词变化时发射）；③ 新增 `@EntryDisplayDispatcher displayDispatcher` 构造参数（与列表页同形）；④ **复用**列表页既有通道——`ui/screens/vault/VaultListTotpTracker.kt` **迁移 + 更名**为 `ui/model/TotpCountdownTracker.kt`（**逻辑逐字未改**：刻度假通道 / 「任一条目自身周期序号变化才重算」/ 批量取码 / §132 的周期集合快照缓存；只改包名、类名与 KDoc），暴露 `nowSeconds` / `liveCodes` 两条窄通道；⑤ `TotpCardItem` 去掉 `remainingSeconds` **与** `codeFormatted`（格式化提为**单一实现** `internal fun formatTotpCode`——显示文本必须改由**实时码**格式化，若状态里另存一份「格式化后的投影之码」即同一信息两个来源、必然漂移；`codeRaw` 仍可空，TASK-33「不显示假码 / 不可复制」语义保留）；⑥ 卡片三处（`isUrgent` / 进度环分母 / 倒计时文本）改为按窄通道刻度 + `item.periodSeconds` 经 `OtpEngine.getRemainingSeconds` 现算，**读取动作落在 `items` 内**⇒ 秒级失效面＝可见卡片，`uiState` 与其 `items` 在周期内零重建；⑦ 显示改 `formatTotpCode(code)`、复制改「当前所见之码」`code`。**三条开工复核结论（前两条 AC 未写）**：**① HOTP 卡片不得取窄通道之码**——`liveCodes` 是**时间驱动**之码（只在周期边界重算），而 HOTP 之码由用户**显式推进计数器**决定（`advanceEntryHotpCounter` 先算码 → 计数器 +1 落库 → 返回该码），改善前推进后 `userMessageFlow` 变更会令 `combine` 重跑重算、卡片显示**推进后**之码；照抄列表页徽标的 `liveCodes[id] ?: fallbackCode` 会让卡片一直显示**刚被消费掉的那个码**（正是用户刚复制走的）⇒ 按 `isHotp` 分流取投影之码，与详情页 `EntryDetailTotpTicker` 只跟 TOTP 的既有口径一致；**② 复用而非复制**——本仓 P1 整改已把「三份逐字重复的秒级节拍」收敛为一，再复制一份即为回归；且直接 `import` 会引入本仓**首个 `screens → screens` 依赖**（列表页包 ↔ 验证器页包兄弟耦合），故提升到 `ui/model`（该包已是 `EntryDisplayDispatcher` / `EntryIconPresenter` / `IconBitmapCache` 等展示层协作者的归处）并更名；**③ 搜索框回显仍直接接 `searchQueryFlow`**（若交给内容流，键入到回显要多等一次条目过滤流水；与 `VaultListViewModel` 把 `filterParamsFlow` 与内容流并列的既有结构同形）。**验证**：全量 `--rerun-tasks` **1m 42s 绿**（114 tasks executed），聚合 **`tests=2093 skipped=13 failures=0 errors=0`**（§138 为 2092，**+1**）；**聚合口径本批起明确**为只统计 `*/build/test-results/testDebugUnitTest/`——`app/build/test-results/` 下另有 2026-09-16 由 `updateDebugScreenshotTest` 留下的 2 例截图用例产物，混入会虚增（含入 **2095** / 不含 **2093**）；审计闸门 `PASS`（1308 行 / 11 条禁用短语）。**新增 1 例（接线守卫）**：`AuthenticatorUiState.kt` 不得出现 `remainingSeconds`、必须存在 `internal fun formatTotpCode(`；`AuthenticatorScreen.kt` 必须存在 `OtpEngine.getRemainingSeconds(` + `nowSeconds.value`、必须存在 `liveCodes.value[item.entryId] ?: item.codeRaw` 与 `formatTotpCode(`。**改写既有断言 4 条**（原「验证器页必须出现 `calculateEntryTotps(`」在本批后不再成立，取码已移出本页）⇒ 改为「不得出现 `timerSecondsFlow`」+「必须出现 `TotpCountdownTracker(`」+「必须出现两条窄通道引用」，批量通道断言**移到** `TotpCountdownTracker.kt`（「不得逐条 `calculateEntryTotp`」不变）。**迁移并更名既有用例**：`VaultListTotpTrackerTest` → `ui/model/TotpCountdownTrackerTest.kt`（**4 例断言逐字未改**，仍以虚拟时钟锁定「无人订阅不计时 / 周期内零重算 / 跨周期必重算 / `period != 30` 按自身边界」，即本批 AC ③ 的行为面依据）；`TotpPeriodWiringGuardTest` 的路径常量同步。**变异验证 1 项**：把 `AuthenticatorScreen` 的取码回退为 `item.codeRaw`（＝不接窄通道）⇒ `AlgoHotPathGuardsTest > 验证器页卡片状态不得随秒级节拍重建` **FAILED** ✔（已还原，还原后全量复跑绿）。**边界如实声明**：**无任何性能实测**，结论全部是源码结构结论（**不得**宣称百分比收益或推断真机帧率 / 耗电）；**AC 的两条运行期判据仍未按原文落地**（「1 Hz 刻度不触发整页状态新建」「同拍内批量取码只调用一次」）⇒ 与 §129 / §132 同口径以**源码文本守卫**替代（宿主侧无「整页状态被新建几次」的计数通道），**不得**读作「已用运行期计数证明」（「单拍内批量取码一次」的部分行为面依据取自迁移后的 4 例：计数型仓库断言 `calculateEntryTotps` 的调用次数）；**设备侧渲染未核对**——未跑 `connectedDebugAndroidTest` / 截图 / 预览渲染 / `lint` / `assembleRelease`，UI 侧证据＝编译期契约 + 接线守卫 + 预览夹具同形改写（夹具刻意保持「剩余 18 秒 / `123 456`」与改前一致，但**未渲染比对像素**）；**倒计时相位来源发生变化**（改前每拍真实墙钟现算 → 改后 1 Hz 刻度，周期边界处可能 **≤1 拍相位差**，与列表页徽标同口径），**未做设备侧逐帧核对**；`liveCodes` 命中前显示**投影兜底之码**（首帧语义，非过期码；查询词变化后新出现的条目同理）；**会话锁定后实时码的驻留**（该 `StateFlow` 未注册 `SessionLockObserver`；自 §114 起由列表页持有**同一对象**，本批未改其擦除语义）已登记 [`architecture/已知工程限界.md`](../architecture/已知工程限界.md) **§13**（含解除条件）；`isHotp` 取投影之码依赖「HOTP 计数器写回触发重新投影」，**未在设备侧实测**；更名涉及 6 处引用，代码引用由编译器覆盖、**KDoc / 注释引用无编译器保障**（已全仓 `grep` 复核，`docs/` 下仅历史归档保留旧名——归档正文按约定只搬迁不改写）。**过程留痕**：**本批无编译失败、无返工、无既知偶发红命中**（定向 4 类 29 例 → 全量 2093 例，两次均绿）；工具调用失误 1 次（首次读 `VaultListViewModel.kt` 误传 `limit`，该工具无此参数被拒，改用 `n_lines` 正常）；文件迁移用 `git mv`（保留历史，diff 呈「重命名 + 少量编辑」） | `ISSUE-P3-182`（闭环） | [`139-验证器页倒计时解耦批次.md`](batches/139-验证器页倒计时解耦批次.md) |
