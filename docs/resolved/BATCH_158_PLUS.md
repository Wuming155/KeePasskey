# 分册 05 · 批次一行索引（§158 起，滚动）

> **定位**：与分册 01 ~ 04 同级，**只放一行索引、不复制批次正文**（立则见 [`README.md`](README.md)）。
> 分册 04 已按「超过 100 行另立」的规则封卷于 §157，本册自 §158 起滚动。
> **总索引**：[`../RESOLVED_LOG.md`](../RESOLVED_LOG.md)；批次正文：[`batches/`](batches/)。

| § | 批次主题 | 条目范围 | 正文 |
|:--:|---|---|---|
| §158 | 静态守卫清洗口径收口批次（`ISSUE-P3-194` 闭环：19 份守卫全部委托双口径工具，余 16 份收口且 84 例原样通过；另立分册 05） | `ISSUE-P3-194`（闭环） | [`158-静态守卫清洗口径收口批次.md`](batches/158-静态守卫清洗口径收口批次.md) |
| §159 | 密码库设置页段落下沉批次（`ISSUE-P3-188` 第 1 项第二段：整页 431 → 369 行，新增段落接线守卫 4 例） | `ISSUE-P3-188`（第 1 项第二段闭环） | [`159-密码库设置页段落下沉批次.md`](batches/159-密码库设置页段落下沉批次.md) |
| §160 | 编辑页分组与绑定应用解析下沉批次（`ISSUE-P3-188`：`EntryEditFormSections` 438 → 351 行；同步把 `AlgoHotPathGuardsTest` 改为两文件并集扫描） | `ISSUE-P3-188`（第一档再降一档） | [`160-编辑页分组与绑定应用解析下沉批次.md`](batches/160-编辑页分组与绑定应用解析下沉批次.md) |
| §161 | 群组生命周期对话框集下沉批次（`ISSUE-P3-188` 第二档：`VaultListDialogs` 490 → 280 行，新文件 242 行；第二档实测重数为 40 个） | `ISSUE-P3-188`（第二档消化一个头部文件） | [`161-群组生命周期对话框集下沉批次.md`](batches/161-群组生命周期对话框集下沉批次.md) |
| §162 | 注册响应材料组装下沉批次（`ISSUE-P3-188` 第二档：`PasskeyCreateActivity` 460 → 359 行，新文件 133 行；该档实测降至 39 个） | `ISSUE-P3-188`（第二档再消化一个头部文件） | [`162-注册响应材料组装下沉批次.md`](batches/162-注册响应材料组装下沉批次.md) |
| §163 | 条目 TOTP 面下沉批次（`ISSUE-P3-188` 第二档：`VaultEntryMapper` 490 → 384 行，新文件 127 行；该档实测降至 38 个） | `ISSUE-P3-188`（第二档再消化一个头部文件） | [`163-条目TOTP面下沉批次.md`](batches/163-条目TOTP面下沉批次.md) |
| §164 | 填充确认页归属块下沉批次（`ISSUE-P3-188` 第二档：`AutofillConfirmActivity` 475 → 366 行，新文件 126 行；该档实测降至 37 个） | `ISSUE-P3-188`（第二档再消化一个头部文件） | [`164-填充确认页归属块下沉批次.md`](batches/164-填充确认页归属块下沉批次.md) |
| §165 | 凭据编辑页屏幕外壳抽出批次（`ISSUE-P3-188` 第二档：`EntryEditScreen` 472 → 319 行，新文件 135 行；该档实测降至 36 个） | `ISSUE-P3-188`（第二档再消化一个头部文件） | [`165-凭据编辑页屏幕外壳抽出批次.md`](batches/165-凭据编辑页屏幕外壳抽出批次.md) |
| §166 | 同步冲突合并判据下沉批次（`ISSUE-P3-188` 第二档：`SyncConflictController` 461 → 355 行，新文件 144 行；该档实测降至 35 个） | `ISSUE-P3-188`（第二档再消化一个头部文件） | [`166-同步冲突合并判据下沉批次.md`](batches/166-同步冲突合并判据下沉批次.md) |
| §167 | 预览深色模式位收敛与魔法数字目复核批次（`ISSUE-P3-188` 第 4 目：`@Preview(uiMode = 0x20)` 151 处全部改用平台命名常量，71 个 `src/main` 文件；附带修掉生成器两处缺陷） | `ISSUE-P3-188`（第 4 目复核与收敛） | [`167-预览深色模式位收敛与魔法数字目复核批次.md`](batches/167-预览深色模式位收敛与魔法数字目复核批次.md) |
| §168 | 魔法数字第 4 目三小项裁定批次（`ISSUE-P3-188` 第 4 目：同值异名常量合并、`ASCII_SPACE` 阈值命名、uint16 上限命名；剩余 169 处完成三类定性，第 4 目裁定毕业） | `ISSUE-P3-188`（第 4 目裁定完成） | [`168-魔法数字第4目三小项裁定批次.md`](batches/168-魔法数字第4目三小项裁定批次.md) |
| §169 | 凭据详情页屏幕本体收敛批次（`ISSUE-P3-188` 第一档：`EntryDetailScreen` 427 → 349 行，预览夹具与明文导出确认对话框归位；第一档仅余 1 个未达标，第二档 34 个） | `ISSUE-P3-188`（第一档消化至仅余 1） | [`169-凭据详情页屏幕本体收敛批次.md`](batches/169-凭据详情页屏幕本体收敛批次.md) |
| §170 | 详情页装配链下沉与会话锁定样板收敛批次（`ISSUE-P3-188` 第一档收尾：`EntryDetailViewModel` 434 → 397 行，第一档维度闭环；第二档 33 个） | `ISSUE-P3-188`（第一档达标） | [`170-详情页装配链下沉与会话锁定样板收敛批次.md`](batches/170-详情页装配链下沉与会话锁定样板收敛批次.md) |
| §171 | 详情页「锁定即擦除」直调用例批次（`ISSUE-P3-188` 剩余清单第 5 项第一段：新增 1 例钉住密码明文与强度读数在锁定后归零；余 edit / settings 两例已注明应断言通道） | `ISSUE-P3-188`（剩余清单第 5 项第一段） | [`171-详情页锁定即擦除直调用例批次.md`](batches/171-详情页锁定即擦除直调用例批次.md) |
| §172 | 待办条目流水压缩批次（「精简文档」：`ISSUE-P3-188` 剩余清单 41 行 → 24 行，剩余清单重编号并留映射；仅文档、链接 185 条失效 0） | 目标「精简文档」（无条目编号） | [`172-待办条目流水压缩批次.md`](batches/172-待办条目流水压缩批次.md) |
| §173 | 断言响应材料下沉批次（`ISSUE-P3-188` 第二档：`PasskeyAssertionActivity` 450 → 267 行，与 §162 注册侧对称；第二档 32 个，并打开新的宿主直调验证面） | `ISSUE-P3-188`（第二档再消化一个头部文件） | [`173-断言响应材料下沉批次.md`](batches/173-断言响应材料下沉批次.md) |
| §174 | 密钥对生成面下沉批次（`ISSUE-P3-188` 第二档：`PasskeyCryptoEngine` 453 → 269 行，生成三族与专属私有辅助整体下沉；第二档 31 个；并核实「断言 payload 宿主直调」在 JVM 不可行的原因与两条可行路） | `ISSUE-P3-188`（第二档再消化一个头部文件） | [`174-密钥对生成面下沉批次.md`](batches/174-密钥对生成面下沉批次.md) |
| §175 | 健康度页仪表段下沉批次（`ISSUE-P3-188` 第 3 目与第二档双击：最长 Compose 函数 326→227、`HealthCheckScreen` 406→299；第二档 30 个；重测口径与 28 个长函数清单入条目） | `ISSUE-P3-188`（第 3 目首段 + 第二档消化一个） | [`175-健康度页仪表段下沉批次.md`](batches/175-健康度页仪表段下沉批次.md) |
| §176 | 设置页局部投影归层批次（`ISSUE-P3-188` 第二档：`SettingsPreferencesController` 446 → 379 行，映射函数与三个局部投影归入既有纯投影层；第二档 29 个） | `ISSUE-P3-188`（第二档再消化一个头部文件） | [`176-设置页局部投影归层批次.md`](batches/176-设置页局部投影归层批次.md) |
| §177 | 待办条目口径复核批次（`ISSUE-P3-188` 文档面：第一档数法三处并一处、`DatabaseSettingsScreen` 369→370 修正、已消化 11→13、新增两档复核脚本；剩余清单补登第 6 项分流欠账） | `ISSUE-P3-188`（文档口径收敛，零生产代码改动） | [`177-待办条目口径复核批次.md`](batches/177-待办条目口径复核批次.md) |
| §178 | 打开已有密码库对话框段落拆分批次（`ISSUE-P3-188` 第 3 目：`OpenExistingVaultDialog` 226 → 96 行，同包新建 8 个窄参数段落组件；≥100 行 Compose 函数 28 → 27；反向逐字核对与三个搬家坑留痕） | `ISSUE-P3-188`（第 3 目再消化一个） | [`178-打开已有密码库对话框段落拆分批次.md`](batches/178-打开已有密码库对话框段落拆分批次.md) |
| §179 | 新建库向导段落拆分批次（`ISSUE-P3-188` 第 3 目：`CreateVaultWizardDialog` 234 → 158 行，新建同包 4 个段落组件；`CreateVaultPresetTest` 向导判据改「本体 + 段落文件」并集扩扫；逐字核对工具化 `tools/doc/check_verbatim_move.py`） | `ISSUE-P3-188`（第 3 目再削一个最长非装配表函数） | [`179-新建库向导段落拆分与守卫并集批次.md`](batches/179-新建库向导段落拆分与守卫并集批次.md) |
| §180 | 文档相对链接断链收口批次（`docs/resolved/batches/` 19 份文件 26 处链接按「相对本文件」口径修正；新增 `tools/doc/check_md_links.py` 自检并登记进 `AGENTS.md` §4 索引纪律 / §5 命令表；围栏代码块内的粘贴正文判为假阳性、不改） | 文档面自发现（用户命题「精简文档」，无既有 ISSUE 条目） | [`180-文档相对链接断链收口批次.md`](batches/180-文档相对链接断链收口批次.md) |
| §181 | 长函数度量工具化与快照分流批次（新增 `tools/doc/long_functions.py`，≥100 行函数复核 = **28 个全为 Compose 面**；**更正一条失真结论**：`processFillRequest` 55 / `mergeConflictedEntry` 52 未达「≤50」验收线，登为剩余清单第 6 项；违例清单第 3 目的 20 处历史快照原样分流至本文件 §4） | `ISSUE-P3-188`（第 3 目度量与结论校准） | [`181-长函数度量工具化与快照分流批次.md`](batches/181-长函数度量工具化与快照分流批次.md) |
| §182 | 合并层冲突对下沉与早退守卫限界批次（第 3 目「≤50 行」验收线逐条结案：`mergeConflictedEntry` 52→41 已下沉；`processFillRequest` 55 登记为**限界 §20**；修 `check_verbatim_move.py` 参数校验 off-by-one；更正 lint 计数口径 216 → **215**（`grep "<issue"` 误含根元素）） | `ISSUE-P3-188`（第 3 目验收线结案 + 度量口径更正） | [`182-合并层冲突对下沉与早退守卫限界批次.md`](batches/182-合并层冲突对下沉与早退守卫限界批次.md) |
| §183 | Argon2 参数对话框段落拆分批次（`ISSUE-P3-188` 第 3 目：`Argon2ParametersDialog` 168 → **80 行出表**，≥100 行 Compose 函数 **28 → 27**；两处内联档位表常量化；`LaunchedEffect` 回填逐项核对；登记一次「切片顺序错置 ⇒ 删除静默落空」的脚本坑） | `ISSUE-P3-188`（第 3 目再消化一个） | [`183-Argon2参数对话框段落拆分批次.md`](batches/183-Argon2参数对话框段落拆分批次.md) |
| §184 | 装配表口径裁决批次（`ISSUE-P3-188` 第 3 目：登记 **PD-11**——两个纯接线 NavGraph（252 逻辑行 1 / 164 逻辑行 0）豁免、`KeePasskeyApp`（202 逻辑行 10）**不豁免**，待拆数 27 → 25；新增 `tools/doc/logic_lines.py` 把 PD-11 重开条件做成可执行判据） | `ISSUE-P3-188`（范围口径，用户裁决后登记 PD-11） | [`184-装配表口径裁决批次.md`](batches/184-装配表口径裁决批次.md) |
| §185 | 应用外壳逻辑下沉批次（`ISSUE-P3-188` 第 3 目：按 PD-11 对**不豁免**的 `KeePasskeyApp` 动手，202 → **86 行出表**、逻辑行 10 → 0；≥100 项 27 → 26（扣除 PD-11 豁免后待拆 24）；新增 `KeePasskeyAppLogicTest` 5 例——该文件首份行为用例，总用例 2198 → **2203**） | `ISSUE-P3-188`（第 3 目再消化一个 + 补验证面） | [`185-应用外壳逻辑下沉批次.md`](batches/185-应用外壳逻辑下沉批次.md) |
| §186 | 解锁区段落拆分批次（`ISSUE-P3-188` **一批双降**：第二档 **29 → 28**（`UnlockContentSections` 430→336）、≥100 行 **26 → 25**（`UnlockStandardUnlockContent` 158→71）；新建 `UnlockStandardUnlockSections.kt` 四段，主密码 `SecurePasswordField` 链路刻意留本体；`UiMd3AlignmentWiringTest` 判据按内层列表并集扩扫；顺带收紧 `logic_lines.py` 一处会误导的豁免判定语） | `ISSUE-P3-188`（第 3 目 + 第二档双降） | [`186-解锁区段落拆分批次.md`](batches/186-解锁区段落拆分批次.md) |
| §187 | 关于页段落拆分批次（`ISSUE-P3-188` 第 3 目：`AboutSettingsScreen` 153 → **75 行出表**，≥100 项 25 → 24、待拆 22；新建 `AboutSettingsScreenSections.kt` 三段 + 一个 private 助手；逐字核对仅 3 处形参改名；**实测并登记剩余清单第 6 项**——11 处 / 241 行重复 `TopAppBar` 骨架） | `ISSUE-P3-188`（第 3 目再消化一个 + 新登结构性重复） | [`187-关于页段落拆分与重复骨架登记批次.md`](batches/187-关于页段落拆分与重复骨架登记批次.md) |
| §188 | 二级设置页共用骨架批次（第 6 项首段：新建 `SettingsSubscreenScaffold.kt`，零守卫引用的五页各 −21 行（About/Theme/Autofill/PrivilegedBrowser/Totp），重复 5 份收敛为 1 份；附带 `PrivilegedBrowserSettingsScreen` 115→95 **出第 3 目表** ⇒ ≥100 项 24 → 23、待拆 21） | `ISSUE-P3-188`（第 6 项推进 + 第 3 目再消化一个） | [`188-二级设置页共用骨架批次.md`](batches/188-二级设置页共用骨架批次.md) |
| §189 | 守卫点名五页接入共用骨架批次（`ISSUE-P3-188` **第 6 项结案**：五页（`Debug`/`CloudSync`/`Security`/`HealthCheck`/`DatabaseSettings`）接入共用骨架，**净 −135 行 ⇒ 十页重复收敛为一份**；`HealthCheckScreen` 227→206 未出表（≥100 仍 23、第二档仍 28）；新增 `tools/doc/scaffold_block_fingerprint.py` 指纹判据，**据此查出 §188 的一处顶栏配色回归 ⇒ 新登 `ISSUE-P3-195`**） | `ISSUE-P3-188`（第 6 项闭环）/ 新登记 `ISSUE-P3-195` | [`189-守卫点名五页接入共用骨架批次.md`](batches/189-守卫点名五页接入共用骨架批次.md) |
| §190 | 两处「锁定即擦除」宿主直调用例批次（`ISSUE-P3-188` **第 4 项结案**：编辑页 +1 例（三层证据，含「锁定后保存不再带明文」的行为级断言）、设置页新建 1 例（WebDAV 口令 + S3 两密钥三条预填通道同时擦除）；新建可写内存 prefs 共享替身；总用例 2203 → **2205**，未触碰生产代码；过程缺陷：临时 `glob` 换尺子 ⇒ 新增 `tools/doc/count_test_results.py` 固定计数口径） | `ISSUE-P3-188`（第 4 项闭环 + 新登第 5 项） | [`190-两处锁定擦除宿主直调用例批次.md`](batches/190-两处锁定擦除宿主直调用例批次.md) |
| §191 | 健康度页审计行下沉批次（`ISSUE-P3-188` 第 1 目：`HealthCheckScreen` **206 → 95 行出表** ⇒ ≥100 项 22、待拆 20；两条同形判定并为 `HealthCountAuditRow` + 纯函数 `healthAuditTone`（补 4 例宿主单测）；两文件净 −89 行；**如实登记本批非逐字搬**，29 条缺失行逐条归类） | `ISSUE-P3-188`（第 1 目再消化一个 + 补验证面） | [`191-健康度页审计行下沉批次.md`](batches/191-健康度页审计行下沉批次.md) |
| §192 | 冲突页段落下沉批次（`ISSUE-P3-188` 第 1 目：`ConflictResolutionScreen` **172 → 91 行出表** ⇒ ≥100 项 21、待拆 19；三段整块内联呈现下沉为新文件 `ConflictResolutionScreenSections.kt`（刻意不追加进 394 行的原文件，免越 400 造新第二档项）；顺带 `FieldDiffRow` 两份选项行并一（83 → 41）；36 条缺失行逐条归类，守卫锚点未动无需扩扫；分档不变 2/28） | `ISSUE-P3-188`（第 1 目再消化一个） | [`192-冲突页段落下沉批次.md`](batches/192-冲突页段落下沉批次.md) |
| §193 | 子库挂载对话框段落下沉批次（`ISSUE-P3-188` 第 1 目：`ChildDatabaseDialog` **170 → 97 行出表** ⇒ ≥100 项 20、待拆 18；非保密三段下沉为新文件 `ChildDatabaseDialogSections.kt`（原文件距 400 仅 3 行）；`SecureDialogWindowEffect()` 两处与子库主密码链路刻意留本体；7 条缺失行全为接收者改写，守卫按文件计数未受影响） | `ISSUE-P3-188`（第 1 目再消化一个） | [`193-子库挂载对话框段落下沉批次.md`](batches/193-子库挂载对话框段落下沉批次.md) |
| §194 | 详情页溢出菜单下沉与 Popup 清单重新盘点批次（`ISSUE-P3-188` 第 1 目：`EntryDetailTopBar` **151 → 84 行出表** ⇒ ≥100 项 19、待拆 17；菜单原样下沉新文件，局部状态随唯一使用者迁入；**首个跨 `PopupSecureFlagInventoryTest` 清单锁的搬移**：集合相等断言当场报红 ⇒ 重新盘点换路径 + 同步 `SecureDialog` KDoc，未删条目 / 未放宽「菜单项 == 8」；3 条缺失行逐条可解释；分档不变 2/28） | `ISSUE-P3-188`（第 1 目再消化一个 + 守卫口径新增一条） | [`194-详情页溢出菜单下沉与Popup清单重新盘点批次.md`](batches/194-详情页溢出菜单下沉与Popup清单重新盘点批次.md) |
| §195 | 测试替身去重批次（`ISSUE-P3-188` **第 5 项结案** ⇒ 剩余清单回到 1~4 项：`SyncCredentialsStoreTest` 改用共享 `InMemorySharedPreferences`，删私有 `Proxy` 夹具、净 −57 行；`memoryStorage` 改属性别名令 24 处断言字面未动；换替身前逐条比对行为差集（超集；唯一可变处 `commit()` 是死路）；该类定向 `tests=10` 全绿、全量 2209 例同值） | `ISSUE-P3-188`（第 5 项闭环） | [`195-测试替身去重批次.md`](batches/195-测试替身去重批次.md) |
| §196 | 库列表对话框宿主分段批次（`ISSUE-P3-188` 第 1 目：`VaultListDialogHost` **130 → 68 行出表** ⇒ ≥100 项 18、待拆 16；按特性切两段 `private` 组件、同文件追加；九个对话框的组合顺序一字未动；**`MISSING_KINDS=0` 的纯逐字搬**；该宿主零测试引用 ⇒ 证据只到编译与不回归；分档不变 2/28） | `ISSUE-P3-188`（第 1 目再消化一个） | [`196-库列表对话框宿主分段批次.md`](batches/196-库列表对话框宿主分段批次.md) |
| §197 | 对拍段登记与前置条件实测批次（文档面：新登 `ISSUE-P3-196`，把 `ISSUE-P3-188` 第 4 目最后一段（格式面 26 处 / 7 份）变成自包含条目；**实测否证其伪阻塞前提**（keepassxc-cli 2.7.12 / pykeepass 4.2.0 可用，cargo 在但代理被权限层拦）；写明设备义务边界与「corpus --verify 不构成互操作证据」；如实登记一次无 `--rerun-tasks` 的 `test` 不构成证据 ⇒ 依据沿用 §196；零代码改动、不减存量数字） | `ISSUE-P3-188`（第 4 目余量登记成条）/ 新登记 `ISSUE-P3-196` | [`197-对拍段登记与前置条件实测批次.md`](batches/197-对拍段登记与前置条件实测批次.md) |
| §198 | 详情页确认对话框下沉批次（`ISSUE-P3-188` 第 1 目：`EntryDetailDialogHost` **119 → 91 出表** ⇒ ≥100 项 17、待拆 15；两份内联 `AlertDialog` 原样下沉同文件 `private` 组件、**刻意不合并**（视觉语义不同），复位留调用点；5 条缺失全是闭包改形参；**新登第 5 项验证缺口**：两个破坏性确认出口零测试引用 ⇒ 优先级在继续削长度之前；分档不变 2/28） | `ISSUE-P3-188`（第 1 目再消化一个 + 新登第 5 项） | [`198-详情页确认对话框下沉批次.md`](batches/198-详情页确认对话框下沉批次.md) |
| §199 | 顶栏配色参数化与接线守卫批次（`ISSUE-P3-195` 结案：§188 的顶栏配色回归已复原；两条修法曾被权限层拦，用户明示自行决策后取①参数化骨架（②会让该页重新入 ≥100 表并留例外）；可空参数表达「不传该参数」而非 `Color.Unspecified`；九页未动；新增 4 例静态接线守卫 + 反向哨兵 ⇒ **一次性核对 ≠ 防护**（搬家口径第四形态），区分力以 `git show` 回归态代入谓词复核；总用例 2209 → 2213） | `ISSUE-P3-195`（结案）/ `ISSUE-P3-188`（口径第四形态） | [199-顶栏配色参数化与接线守卫批次.md](batches/199-顶栏配色参数化与接线守卫批次.md) |
| §200 | 详情页破坏性确认出口决策纯函数化批次（`ISSUE-P3-188` **第 5 项结案** ⇒ 剩余清单回到 1~4 项：§198 §4 证据边界「两个破坏性确认出口零断言」落地——`EntryDetailConfirmIntent` + 出口决策 `entryDetailConfirmExit` + 执行器 `entryDetailConfirmExitHandler`（先复位、再上行，§169 口径）三单元同文件追加，段组件改上报意图、状态所有权仍在调用点；**回滚确认「先上行、再复位」的历史孤例并入口径**（自始如此，§198 文档记述失真，唯一差异在同步抛错路径）；4 例纯函数 + 4 例静态接线守卫（裸调用 == 0 / 执行器调用点 == 2 / 触发态复位各一份），宿主函数 89 行未回表；**复跑纠偏**：§199 复原配色在函数体内 +5 行令 `PrivilegedBrowserSettingsScreen` 恰好 100 重新入表且其验证表漏跑长函数尺子 ⇒ 现跑 18、待拆 16 已就地修正登记；总用例 2213 → 2221、lint 215 持平） | `ISSUE-P3-188`（第 5 项闭环 + 第 1 目计数纠偏） | [`200-破坏性确认出口决策纯函数化批次.md`](batches/200-破坏性确认出口决策纯函数化批次.md) |
| §201 | 格式面 26 处字面量逐处判定批次（`ISSUE-P3-196` **整条结案**、`ISSUE-P3-188` 第 4 目自此闭环：7 份文件 26 处按 §167/§168 判据逐处裁定——**登记 21**（文档引用 / 规范固定字节：WebAuthn §10.1 的 0x00、KeePass 固定 IV、XML 1.0 §2.2 Char 区间、X.690 tag、RFC 8410 OCTET STRING 包裹、KDBX 4 headerHmacKey 的 LE64(2^64−1)）、**命名 5 值**（`PasskeyPrf` 的 `ASCII_LF/CR/SPACE/TAB` 等值剥离语义 + `PasskeyPkcs8Codec` 的 `ASCII_7BIT_MAX`）；全部只挪定义未改取值；`KdbxFile`/`KdbxXmlStreamWriter` 零改动 ⇒ AC2 对拍前置不成立；passkey 三套件 5+9+14 定向全绿、全量 2221 同值、lint **214**（−1）；过程缺陷登记 §200 漏两处归档登记 + 引擎套件包名错位致 `--tests` 静默空匹配） | `ISSUE-P3-196`（结案）/ `ISSUE-P3-188`（第 4 目闭环） | [`201-格式面26处字面量逐处判定批次.md`](batches/201-格式面26处字面量逐处判定批次.md) |
| §202 | 存量条目流水精简批次（文档面、零代码改动：`ACTIVE_ISSUES.md` 355 → **310 行**——按「批次流水以 resolved/ 为单一真相源」纪律，剪去 `ISSUE-P3-188` 条目内复述的 §155~§201 逐批流水（已出表 9 个 / 已消化 14 个的逐文件明细、历史编号映射注记、已闭环叙述）；**逐条核对一个不落全部活约束**：现跑计数与 §200 复跑纠偏、现跑尺子义务、刻意不搬三处、搬移约束四条、刻意排后三条、`CloudSyncScreen` 距出表 1 行、PD-11 豁免与重开条件、第 3 项守卫四形态与第 4 项工作项一字未动；`AGENTS.md` 通读无 §9 违例不动；`BROKEN_MD_LINKS=0`） | 文档面批次（无条目变动） | [`202-存量条目流水精简批次.md`](batches/202-存量条目流水精简批次.md) |
| §203 | 设备侧覆盖余量清单闭环批次（`ISSUE-P2-192` **整条结案**：8 条余量逐条处置——① sync 传输层设备侧 8 例（WebDAV 5 / S3 3，回环 MockWebServer 真实 socket + takeRequest 报文断言）②平台网络策略 5 例（策略读数 false / **设备 LAN IP 非回环明文被拦** / 回环结果与读数一致 / 自签 HTTPS 拒 / SSRF 回环防线；**API 37 真机对回环内置豁免** `loopback=true` 而 default=false，API 36.1 模拟器拦——差异实测登记限界 §4.1）③Hilt 全图testing 入限界（EntryPoint 子图已有 Worker 用例证）④WorkManager 3 例（work-testing 调度/取消/Worker 经 Hilt EntryPoint 真实执行）⑤系统 UI 六面入限界 §4.2 ⑥core 首建 androidTest + AppLog 4 例（logcat nonce 轮询证真实写入/门控/脱敏）⑦**CI device-gate job**（API 36.1 x86_64 模拟器 + 全模块 connected；镜像名实测 `android-36.1` 带 minor）⑧三处硬断言 Assume 化 + AutofillAuthChain **真机连跑两轮全绿**（可重复执行达标）；过程修 4 缺陷（sync 测试 APK 无 INTERNET / targetSdk 37 平台默认禁明文拦回环 / WorkInfo 无 workerClassName 等）；模拟器专有差异（完整性留痕缺失）新立 `ISSUE-P3-197`；**零生产代码改动**，宿主 2221 同值全绿） | `ISSUE-P2-192`（结案）/ `ISSUE-P3-197`（新登） | [`203-设备侧覆盖余量清单闭环批次.md`](batches/203-设备侧覆盖余量清单闭环批次.md) |
| §204 | 断言响应材料宿主写入口径批次（`ISSUE-P3-188` 剩余清单第 4 项**结案** ⇒ 回到 1~3 项：§174 路线②落地——新增宿主可用的极简写入器 `WebAuthnJsonWriter`（键序=插入序 / 紧凑 / AOSP 转义语义含 `\/`、不实现数字与 null），**注册 / 断言两处 payload 全部改走它**（`org.json` import 双双移除，`buildPrfClientExtensionResults` 收敛 private）；**四条宿主离线断言** `PasskeyAssertionPayloadBuildTest`（归属省略与不回退本应用包名含正对照 / prfEval=null 空对象 / authData 37 字节标准布局——条目「前 16 字节」系笔误按标准口径 / JCE 公钥验签 `authData‖SHA-256(clientDataJSON)`，解析走 SimpleJson、签名走生产 PEM 链路）+ 转义回归 8 例 + **设备侧 `org.json` 逐字节对拍 3 例双设备全绿**（该契约只能设备侧锁定）；守卫核对：`CredentialManagerCallerBindingWiringTest` 判据锚门控调用点与组装无交集、零改动；**过程发现并同批修复 `AutofillAuthChainDeviceTest` 两处缺陷**（`linePid()` split 双空格解析对 4 位 pid 恒假——真机此前两轮绿系 pid 恰好 5 位巧合、`INTEGRITY_WAIT_MS` 15s<30s 重扫周期采窗缺陷），修复后真机恢复全绿 64 例，模拟器残余「系统填充 UI 呈现」环境面 CI device-gate 以 notClass 排除、真机承担 ⇒ `ISSUE-P3-197` 更新收窄；宿主 **2221 → 2233**（+12）、lint 214 持平） | `ISSUE-P3-188`（第 4 项闭环）/ `ISSUE-P3-197`（根因定位并修复） | [`204-断言响应材料宿主写入口径批次.md`](batches/204-断言响应材料宿主写入口径批次.md) |
| §205 | Compose 长函数拆分两处批次（`ISSUE-P3-188` 第 1 项渐进：待拆 **16 → 14**——`PackageBlocklistManageDialog` 131 出表（段落**另起新文件** `PackageBlocklistDialogSections.kt`：原文件距 400 余量仅 25 行，按 §192/§196「余量逐案判」口径；Body / Confirm / Dismiss 槽位 / 名单行 + rememberAppOption 同迁 private→internal，状态仍由对话框持有经参数回传不自持；**回调语义保真**：名单模式 confirm 槽=关闭，拆分时曾误写无条件 submit 已即时纠偏）+ `GeneratorContent` 121 出表（`GeneratorScreen.kt` 295 行余量充足、段落**追加原文件**：TopBar / LazyListScope 模式 Tab / 历史区 isEmpty 早退保序）；逐字核对 MISSING 全为回调参数化 / 可见性 / 注释、绘制代码逐字未动；tier2=28 维持不变名单零变动；test 2233 同值全绿、lint 214 持平） | `ISSUE-P3-188`（第 1 项渐进） | [`205-Compose长函数拆分两处批次.md`](batches/205-Compose长函数拆分两处批次.md) |
| §206 | Compose 长函数拆分两处批次（`ISSUE-P3-188` 第 1 项渐进：待拆 **14 → 12**——`BasicCredentialsCard` 118 出表（`EntryDetailCards.kt` 397 行距 400 仅 3 行 ⇒ 段落另起新文件 `EntryDetailCardSections.kt`：用户名行 + 密码区，haptic 同源不自持）+ `SyncStatusCard` 119 出表（`CloudSyncComponents.kt` 追加将贴 400 档沿 ⇒ 新文件 `CloudSyncStatusSections.kt`：状态头行 / 反馈条（null 早退等价）/ 按钮行）；**守卫并集活例**：`UiMd3AlignmentWiringTest` 禁用态配色判据锚门面文件首跑即红（守卫有效证据）⇒ 同批改「门面+分支文件」并集、断言零变化；tier2=28 维持；test 2233 同值全绿、lint 214 持平） | `ISSUE-P3-188`（第 1 项渐进） | [`206-Compose长函数拆分两处批次.md`](batches/206-Compose长函数拆分两处批次.md) |
| §207 | Compose 长函数拆分两处批次（`ISSUE-P3-188` 第 1 项渐进：待拆 **12 → 10**——`themeListSection` 118 出表（密度选择器下沉 `ThemeDensitySelector` 追加原文件，7 个 DisplayPrefRow 声明式序列是最小单元不再动）+ `VaultDatabaseCard` 117 出表（头部行下沉 `VaultDatabaseCardHeader`；**路径行不随迁**——MD3 中段省略守卫锚定 `middleEllipsize` 在宿主现场，免并集化）；tier2 不变；test 2233 同值全绿、lint 214 持平） | `ISSUE-P3-188`（第 1 项渐进） | [`207-Compose长函数拆分两处批次.md`](batches/207-Compose长函数拆分两处批次.md) |
| §208 | Compose 长函数拆分两处批次（`ISSUE-P3-188` 第 1 项渐进：待拆 **10 → 8**——`HealthCheckScoreCard` 116 出表（仪表盘 + 文本序列下沉追加原文件）+ `MasterKeyChangeDialog` 116 出表（**§193 清单锁生效**：`SecureDialog {` 与 AlertDialog 本体留宿主文件，只搬字段渲染面（onPasswordChanged 擦除链回样上行）与按钮渲染面（onClick 提交与 pwdChars finally 擦除链全留现场，类比 §179 单一现场），`SecureDialogFlagPolicyTest` 随套件通过）；test 2233 同值全绿、lint 214 持平） | `ISSUE-P3-188`（第 1 项渐进） | [`208-Compose长函数拆分两处批次.md`](batches/208-Compose长函数拆分两处批次.md) |
| §209 | Compose 长函数拆分两处批次（`ISSUE-P3-188` 第 1 项渐进：待拆 **8 → 6**——`S3ConfigFields` 113 出表（凭据双字段段 + path-style 开关行下沉追加原文件，SecurePasswordField 的 CharArray 通道注释随迁）+ `ChildVaultEntryRowView` 113 出表（次级信息行下沉，hasSecondaryLine 判定仍留调用方先行、本段不渲染空行）；tier2 不变；test 2233 同值全绿、lint 214 持平） | `ISSUE-P3-188`（第 1 项渐进） | [`209-Compose长函数拆分两处批次.md`](batches/209-Compose长函数拆分两处批次.md) |
| §210 | Compose 长函数拆分两处批次（`ISSUE-P3-188` 第 1 项渐进：待拆 **6 → 3**——`EntryEditCustomFieldsSection` 108 出表（单字段编辑卡片下沉追加原文件，TASK-10 擦除通道注释随迁）+ `SafeAttachmentPreviewDialog` 107 出表（隔离提示横幅 + 预览画布下沉；**清单锁零风险设计**：只搬 `SecureDialog {` 包裹内部渲染段，调用点计数不变、守卫零改动；过程缺陷脚本重复执行致函数重复当场修复）；test 2233 同值全绿、lint 214 持平） | `ISSUE-P3-188`（第 1 项渐进） | [`210-Compose长函数拆分两处批次.md`](batches/210-Compose长函数拆分两处批次.md) |
| §211 | Compose 长函数收官批次（`ISSUE-P3-188` 第 1 项**闭环**：待拆 **3 → 0**——`KeePasskeyTheme` 104 出表（resolveAppColorScheme 须 @Composable 因内部读 LocalContext，过程缺陷当场修）+ `PrivilegedBrowserSettingsScreen` 100 出表（**§199 守卫锚零触碰**：顶栏参数与注释留宿主，只下沉内容段）+ `EntryDetailScreen` 121 出表（**§169 全文遵守**：SAF 三态/exportLauncher/确认对话框留 Route 现场，只下沉生命周期编排/Snackbar 消费/Content 纯装配段；**过程纠偏真险情**：onExportAttachment 写路径误搬 Host 不会回写 Route 状态 ⇒ 改 Route 传导出回调）；现跑表内余项全为已登记豁免，「无未登记的超限项」达标；test 2233 同值全绿、真机 connected 64 例全绿、lint 214 持平） | `ISSUE-P3-188`（第 1 项闭环） | [`211-Compose长函数收官批次.md`](batches/211-Compose长函数收官批次.md) |
| §212 | JNI 零拷贝评估批次（`ISSUE-P3-187` **评估项结案**：AC① 三路线契约（Critical=GC 钉住窗口劣于②不采纳 / DirectByteBuffer=唯一零拷贝+无窗口+纪律自洽 / CipherSpi=长线演进与②正交）+ AC⑤ 四维对照 + AC② 探针原型实测——Rust `applyKeystreamDirect`（DirectBuffer 直取就地变换无拷贝无分配）+ Kotlin 绑定 + 探针用例，**现状 24.05ms（基线 24.7 复现一致）vs 直扣 3.7ms ≈6.5×，拷贝+分配 44% 份额消除，AC② 达标**；正确性前置两路径逐字节一致；Redmi 4X 真机缺测如实登记（USB 断连，探针已入库随批补跑）；`cargo test` 73 全绿、crypto connected 36 全绿、四层设备义务模拟器轮完成、符号契约 CI 10→11；**实施另立 `ISSUE-P3-198`**；限界 §15/§17 解除条件已更新；顺带登记 P3-188 第 2 项用户容差裁定（±15%）） | `ISSUE-P3-187`（结案）/ `ISSUE-P3-198`（新立）/ `ISSUE-P3-188`（容差裁定登记） | [`212-JNI零拷贝评估批次.md`](batches/212-JNI零拷贝评估批次.md) |
| §213 | `ISSUE-P3-188` **整条结案**批次（2026-09-18 用户命题「消除巨型类和魔法数字」闭环：第 1 目第一档 §170 闭环、第 3 目 ≥100 行函数 §211 闭环（七批拆 15 处，表内余项全为已登记豁免，现跑 4 个）、第 4 目字面量 §201 闭环、剩余清单第 4 项 §204 闭环；第 2 目第二档按 2026-09-19 用户容差裁定（±15% 以内不为出表而拆，超容差仅 RealVaultRepository/VaultRepository 且属刻意排后）合规留清单，28 个现状；**拆分纪律四形态随条目归档继续有效**；条目原文原样收录本批次；test 2233 同值全绿、lint 214 持平） | `ISSUE-P3-188`（整条结案） | [`213-ISSUE-P3-188整条结案批次.md`](batches/213-ISSUE-P3-188整条结案批次.md) |
| §214 | `ISSUE-P3-197` **结案**批次（三条验收全达成：①真机 64 例多轮全绿 ②模拟器驱动面按条目口径另行立项且链路断言零放宽 ③**本地完整复刻 CI device-gate 命令**（全模块 connected + notClass 排除 autofill）在同镜像 Pixel_10 上 **99 例全绿**（core 4 / app 63 / database 15 / sync 17），排除机制精确生效；CI 云端首跑绿灯因私有仓库无凭据由维护者页面确认，残余风险仅 runner 差异；batch 文档原样收录条目） | `ISSUE-P3-197`（结案） | [`214-ISSUE-P3-197结案批次.md`](batches/214-ISSUE-P3-197结案批次.md) |
| §215 | ChaCha20 / AES 直扣（DirectByteBuffer）生产化批次（`ISSUE-P3-198` **整条结案**：AES 原地内核 `cbc_{encrypt,decrypt}_in_place`（解密侧 `Zeroizing` 暂存本组密文；闸门先行、失败不留半截变换）+ `NativeAes_cbc{Encrypt,Decrypt}BlocksDirect` 两导出；**ChaCha20 解密流与 AES 双流切 direct 直扣**（流实例自持堆外缓冲、交付即归零、`wipeRange` 原语；`CbcStreams` 双形态注入 byte[] 形态逐字保留，`requireDistinctResult` 仅适用 byte[] 形态；`CbcDecryptingInputStream` 拆分落位经逐字搬移复核）；**整块 byte[] 与 ChaCha20 加密流有意维持拷贝桥**（契约使直扣无收益，限界 §15/§17 已登记）；**AC③ 真机补测完成**——Redmi 4X 10 轮中位 ChaCha20 149.8→66.3ms（≈2.26×）、AES 178.9→48.7ms（≈3.67×），模拟器 ChaCha20 23.6→3.7、AES 43.9→14.7（≈内核×1.1，AC② 达标）；探针数字≠生产端到端（字节 API 边界拷贝不可消，口径见实测记录 §9.2）；cargo 76 全绿、宿主 test 2242（+9 `CbcDirectStreamsTest`）skipped=13 为 sync 既有基线、lint 214 持平、长函数 ≥100 回落 4→3、四层 connected 模拟器 crypto 37/database 15/sync 全绿/app 63+1（autofill 环境面经 HEAD 对照归因非本批）、真机探针类 8 例全绿；符号契约 11→13；过程留痕：探针误用演化 iv 当解密链值（真机现形，用例侧缺陷）| `ISSUE-P3-198`（整条结案） | [`215-DirectByteBuffer直扣生产化批次.md`](batches/215-DirectByteBuffer直扣生产化批次.md) |
| §216 | `ISSUE-P3-207` **判为误报归档**批次（四轮循环对抗审查收敛：`SyncEndpointGuard.kt:26-27` 逐字声明双层且声明范围 = 实际生效范围 ⇒ 标题「声明与生效面不符」与背景「接线人无从知道」**均失实**；残余 nit 无独立安全语义、无独有交付物（302 用例与 P2-208 AC② 重复）⇒ 归档，残句 KDoc 澄清**并入 `ISSUE-P2-208`**；同步把 P2-208 由符号级升为**上游源码级**（`okhttp-android-5.5.0-sources.jar` → `RouteSelector.kt:174-184`：字面量在 `dnsLookup` 之前 return）+ **真机级**，更正 `[::1]` 括注（IPv6 字面量**同样**旁路）与 AC① 技术口径（须拦截 `connect()` 的 `Socket`，裸 `SocketFactory` 取不到目标地址）） | `ISSUE-P3-207`（归档：误报）/ `ISSUE-P2-208`（升级） | [216-文档半边误报归档-SyncEndpointGuard-KDoc.md](batches/216-文档半边误报归档-SyncEndpointGuard-KDoc.md) |
| §217 | 对抗审计真机取证批次（五轮闭环：4 个新设备用例坐实 4 项悬置前提——`SsrfRedirectBypassDeviceTest`（`MockWebServer.url()` host = `localhost` 主机名据此**证伪**「IP 字面量假阳性归因」指控 + 302 字面量跳转 Dns 零调用而主机名对照组被拦）、`RawFileWritePermissionDeviceTest`（裸写文件 **0600** / `mkdirs()` 目录 **0700** / 私有目录 **0771** ⇒ P3-202 窗口期暴露真机不存在）、`InlineCompressedBinaryBudgetDeviceTest`（直驱生产 `BinaryNode`：`maxHeap = 192 MiB`、单节点满额内联压缩附件 **`OutOfMemoryError`**）、`PendingIntentMatchKeyDeviceTest`（`filterEquals` 匹配键含 component / extras 不参与））；`:sync:` 22、`:database:` 17、`:app:` 67 全绿；新立 **`ISSUE-P3-209`**（`CleartextPolicyDeviceTest` SSRF 用例恒真、零判别力 ⇒ 限界表 §4.1 证据链引用错误）；过程留痕三处自方缺陷） | 对抗审计取证（新立 `ISSUE-P3-209`） | [217-对抗审计真机取证批次.md](batches/217-对抗审计真机取证批次.md) |
| §218 | 分册索引漏登回填与「分册 ↔ 批次正文」机检守卫批次（文档面批次、零代码改动：`BATCH_158_PLUS.md` 止于 §202 而 `batches/` 已有 **§203~§215 共 13 份正文** ⇒ 无法从任何分册跳达 + `resolved/README.md` 仍写「当前最大 §202」⇒ 派号将重号；`check_md_links.py` 只校验链接**可达**、不校验**漏登**；处置＝原样搬运 `RESOLVED_LOG.md` 的 §203~§215 摘要行回填 + README 口径更正 + 新增 `tools/doc/check_resolved_index_sync.py`（正向／反向／口径三判据，豁免表 `{§42,§43}`）+ 接入 `AGENTS.md` §5；验证＝**先红后绿**） | 文档面批次（无条目变动） | [218-分册索引漏登回填与机检守卫批次.md](batches/218-分册索引漏登回填与机检守卫批次.md) |
| §219 | P2 三条闭环批次（CM 通道 `PendingIntent` requestCode **进程级单调**：`CredentialPendingIntents.nextRequestCode()` 收口四条落地入口、KDoc 更正「与 CM 同构」自称失实，Create/Assertion 两路改以系统注入 `callingRequest` 为权威（create 侧 origin 由**本次** `CallingAppInfo` 重新派生、`providerReq` 缺失即 fail-closed；assertion 侧 challenge 取系统 JSON 且 origin 交叉核对不一致即拒签，extras 降级为展示缓存）+ **载荷累计预算**（`BinaryReferenceBudget` → `AttachmentBudget` 三闸门：池引用累计 / 内联物化累计 64 MiB / 内联压缩节点 1024；`{REF:}` 展开加 `MAX_EXPANSIONS=4000` + `MAX_PRODUCED_CHARS=1 MiB` 双闸门，超限**原样保留引用原文**，列表渲染逐字段 Throwable 兜底）+ **SSRF 连接期复核**（新增 `SsrfGuardSocketFactory` 覆写 `Socket.connect` 拦在 SYN 之前，`SsrfAddressApprovals` 使白名单豁免在 Dns 层与连接期层逐地址一致，消除内网自建可用性回归）；新增 3 个宿主用例类 + 5 个设备用例；宿主 **2259**（本批 +16）全绿、`:sync:` 22 与 `:database:` 17 双设备全绿、`:app:` Redmi 4X 70 全绿（Pixel_10 AVD 1 例 `AutofillAuthChainDeviceTest` 经 `git stash` 回 HEAD 复跑同样失败 ⇒ 既有环境面）| `ISSUE-P2-199` / `ISSUE-P2-200` / `ISSUE-P2-208`（三条整条结案） | [219-CM请求码单调与载荷预算及SSRF连接期复核批次.md](batches/219-CM请求码单调与载荷预算及SSRF连接期复核批次.md) |
| §220 | 通行密钥 KPEX 互操作对拍与 Ed25519 PKCS#8 形态整改批次（`ISSUE-P2-210` **整条结案**：按规则 8 补齐官方实现端到端对拍——`tools/passkey-interop/` 新增对拍脚本与外部 fixture 生成器、`database` 新增探针与读取半边两个用例类；**写入方向**三条 KPEX 条目（ES256/Ed25519/RS256）经 `keepassxc-cli` + `pykeepass` **双实现独立读数**并由 `cryptography` 独立解析 PEM、重导出公钥与产物公开公钥**逐字节相等**（114 判据全绿）；**读取方向**外部产出且**无** `Passkey.*` 扩展键的 fixture 经生产读路径还原、OID 嗅探算法并真实签名成功；**当次暴露并闭环 `ISSUE-P2-211`**：Ed25519 私钥曾为 RFC 5958 `OneAsymmetricKey`（`version=1`+`publicKey [1]`），本仓可读而 **OpenSSL 系（含 KeePassXC）拒收**，经手写 DER 隔离实验定位 `version=1` 即被拒 ⇒ 编码侧改 RFC 8410 `version=0` 最小形态，解码侧仍接受 v0/v1；全量 `test` **2265**（+6 逐例可溯）全绿、真机四层 `crypto 37/database 17/sync 22/app 71`（app 含新增 Ed25519·RS256 用例）**0 skipped/0 failed**；**如实声明**：证据为文件级/离库，**端到端真机贯通（github.com）未执行**（强系统 UI 依赖）已附手工冒烟清单，限界 §4.3 解除条件达成并按边界重写） | `ISSUE-P2-210` / `ISSUE-P2-211`（两条整条结案） | [220-通行密钥互操作对拍与Ed25519-PKCS8形态整改批次.md](batches/220-通行密钥互操作对拍与Ed25519-PKCS8形态整改批次.md) |
| §221 | P3 低危七项收敛批次（`ISSUE-P3-201` picker 交付链路「锁定即丢弃」三处门控 + 守卫测试扩至 picker 源码；`ISSUE-P3-202` 同步缓存与防回滚守卫全走 `writeTmpSynced`「第一字节即仅属主」+ 设备测试 tmp 窗口 / rollback 断言；`ISSUE-P3-203` cargo 双构建 `--locked` + dry-run 核对 + 守卫机检；`ISSUE-P3-204` `NativeArgon2` 探活 KAT 对照（独立参考实现冻结值 + 宿主对拍三方一致）+ `Argon2ProbeKatTest`；`ISSUE-P3-205` 豁免 regex 版本界机检（宽度不变式 + toml 版本 ≥ 2.4.20 降级旁路封闭）；`ISSUE-P3-206` `SyncProvider.download` 流式化（`copyBounded` + `receiveRemote`/`ReceivedRemote`，引擎 4 下载点改造，下载期峰值 ~2×S→~1×S，KDoc 失实前提更正）；`ISSUE-P3-209` 恒真 SSRF 用例去重合并移除 + 限界 §4.1 / §203 证据链追溯更正。宿主 `xml=328 tests=2277 skipped=13` 全绿（+1 类 / +12 例可溯）；真机四层 `crypto 37 / database 17 / sync 24 / app 70` 全绿） | `ISSUE-P3-201` / `202` / `203` / `204` / `205` / `206` / `209`（七条整条结案） | [221-P3低危七项收敛批次.md](batches/221-P3低危七项收敛批次.md) |
| §222 | 通行密钥三项收敛批次（`ISSUE-P3-212` 私钥擦除单点化：新增消费式签名入口 `signAssertionConsumingKey`（`finally` 内无条件清零**调用方**缓冲，成功 / 异常两路径）并由断言路径改走它、活动层私钥副本清理面删除，`signAssertion` 原语义以对照用例并列锁定；`ISSUE-P3-213` v1 → KPEX **写路径就地迁移**：core 新增判据 `PasskeyData.needsKpexMigration`（只扫键名、不解密）+ crypto 新增协作单元 `PasskeyLegacyKeyText`（历史文本解码 + 文本 → PKCS#8 PEM，一切失败归一为 null 的 fail-safe），`PasskeyEntryCoordinator` 在计数器修补的**同一受控变换**内迁移（旧键全清 / 私钥重包受保护 PEM / 失败则不迁移且不阻断计数），app 层私有历史解码逐字搬入 crypto 单点（`PasskeyAssertionPayload` 222 → 185 行）；`ISSUE-P3-214` 旧格式精确嗅探：`sniffAlgorithmId` 顺序重排 + 「恰 64 字符 hex → ES256」「32 字节种子 → Ed25519」（hex 必须先于 Base64——hex 字母表是 Base64 真子集，否则 64 字符 hex 被解成 48 字节无 OID 垃圾）；文档收窄：限界 §4.3 标题与第 4 条、PD-09 边界改为「用到才迁」+ 对拍记录 §5 补注留痕。宿主全量绿 `xml=329 tests=2295 skipped=13 failures=0 errors=0`（§221 基线 328/2277 ⇒ +1 类 / +18 例逐项可溯）、lint 214 持平、tier2 不增、超长函数 3 不变） | `ISSUE-P3-212` / `213` / `214`（三条整条结案） | [222-通行密钥擦除收敛与v1就地迁移批次.md](batches/222-通行密钥擦除收敛与v1就地迁移批次.md) |
| §223 | 生物识别解锁默认关闭与「开启前当场验证」批次（`ISSUE-P2-212` **整条结案**，用户命题「默认虽然开了但不起作用，应由自己手动打开、并在打开那一瞬间验证生物识别」）：① 出厂默认 `true` 与「封印凭据只在主密码解锁成功后登记」相矛盾（`UnlockModePolicy.resolve` 要求开关开 **且** 凭据在）⇒ 新装设备「开关显示已开、解锁页无生物入口」的静默失效；② `SettingsPreferencesController.setBiometricEnabled` 仅裸写偏好，开关可在无任何身份确认下被打开。整改：三处默认值统一改 `false`（`UserSettings` / `RealSettingsRepository` 读回缺省 / `SettingsUiState`）；**新增** `BiometricEnableCoordinator`（241 行）——「开启」先过 Class 3 强生物识别能力闸门（fail-closed，**绝不**降级锁屏凭据）⇒ 有封印凭据时以其解密 Cipher 绑定 `CryptoObject`（准备失败即清除陈旧凭据并退化为纯身份验证）⇒ BiometricPrompt 验证（60 s 超时兜底；`Failed` 视为非终态继续挂起，避免「用户最终成功而开关未开」错位）⇒ **通过才写偏好**，验证中重复触发幂等；关闭免验证直落偏好；删除 `SettingsPreferencesController.setBiometricEnabled` 消除绕过验证的直写旁路；UI 宿主 Activity 改经 `LocalActivity` 获取（`LocalContext.current as? FragmentActivity` 触发 AndroidLint `ContextCastToActivity`，已修）、验证中开关禁用 + 进度圈 + 三类结果反馈（中英各 +4 条），`SettingsUiStateProjection` 五路流 → **六路**（`combine` 改 `Triple`）。宿主全量绿 `xml=330 tests=2303 skipped=13 failures=0 errors=0`（§222 基线 329/2295 ⇒ +1 类 / +8 例逐项可溯）、lint **214** 持平且 `:app:lintDebug` BUILD SUCCESSFUL、tier2 不增（`SecuritySettingsScreen` 407 → 439 本已在档）、超长函数 3 不变；**如实声明**：设备侧 `connectedDebugAndroidTest` **未执行**（未改 `androidTest` 用例，仅 `compileDebugAndroidTestKotlin` 通过），`BiometricPrompt` 实际弹窗仍属限界 §4.1 手工冒烟面） | `ISSUE-P2-212`（整条结案） | [223-生物识别默认关闭与开启当场验证批次.md](batches/223-生物识别默认关闭与开启当场验证批次.md) |
| §224 | 解锁页无障碍提示迁入设置页安全分区批次（`ISSUE-P3-215` **整条结案**，用户命题「首页会提示已启用无障碍服务……不应该出现在提示里面，应该在设置里面显示，纯属冗余」：解锁页移除常驻提示与提示位 / 订阅 / 失效 import，设置页安全分区新增 `AccessibilityStatusCard`（可点直达系统无障碍设置页）；字符串 `unlock_accessibility_notice_*` → `sec_accessibility_notice_*`；判定与「只提示、不降级」语义**零改动**；两处守卫判据改指新承载位置，首轮全量测试**先红后绿**；宿主 `xml=330 tests=2304 skipped=13 failures=0 errors=0` 全绿） | `ISSUE-P3-215`（整条结案） | [224-无障碍提示迁入设置页批次.md](batches/224-无障碍提示迁入设置页批次.md) |
| §225 | 云端 CI 门禁四项缺陷整改批次（`ISSUE-P1-216` **机制性根因**：`applySignCountPatch` 在**会话 Mutex 之外**从落树实例读回计数器，而计数器补丁走**增量定点擦除**、`withSignCount` 每次落树新建计数器 `ProtectedString` ⇒ 第 N 次提交上线的实例恰是第 N+1 次的擦除候选 ⇒ 并发后继提交抢先完成时读回命中 `ProtectedString.checkNotCleared()`（CI 日志 `ProtectedString.kt:173`）。**本机复现率对照**（临时探针，未入库）：`runTest+Dispatchers.Default` 32 路×1200 轮命中 **0**、真实线程+`CyclicBarrier`+GC 搅动 32 路×800 轮命中 **3402** ⇒ 首次机制性解释「本机绿 / CI 红」（挂起态不占 CPU vs 真线程争抢被抢占）。修复=读回点移入 `transform`（临界区内），临界区外只搬运 `Int`，语义不损失；新增 3 例（确定性机制复现+复演 CI 同形异常 / 真实线程屏障 32 路×150 轮，整改前第 11 轮即红 / 多轮并发不丢失更新）。`ISSUE-P2-217`：`lint` 经 AGP lint 模型链（`:crypto:bundleDebugLocalLintAar` → `mergeDebugNativeLibs`）触达 `:crypto:cargoNdkBuild`（本机 `--dry-run` 实测），fast-gate 遂恒失败于「工具缺失」⇒ 改 `./gradlew lint -x :crypto:cargoNdkBuild`（显式排除、不吞失败；本机实跑 **BUILD SUCCESSFUL 3m11s**、`cargoNdkBuild` 0 次、app Lint 计数 **214** 与基线持平）。`ISSUE-P2-218`：`setup-android@v4.0.1` 默认 `packages: 'tools platform-tools'` 而裸 `tools` 包**远端已不存在**（实测 `repository2-1.xml`/`repository2-3.xml` 中 `path="tools"` 计数均为 0）⇒ 两处显式传 `packages: "platform-tools"`，并在 `docs/records/ci-静态校准记录.md` 新增 §12 + 为 §2 的 v3 基线加「已过期」注。`ISSUE-P2-219`：本机同参数复现 CI 的 `1316 实例 / 948 达阈`，逐条定位后 **948 条 100% 落在 AGP 工具链配置族**（`unified-test-platform-*` / `androidLintTool` / `_internal-screenshot-*`，35 构件：netty 4.1.93/4.1.110、grpc 1.57.2、kotlin-stdlib 1.9.0、protos 32.2.1、protobuf-java-util 3.22.3、jsoup 1.6.3），**生产面 0 条** ⇒ 按 AC④ 通道做**扫描面收口**（`skipConfigurations` 显式补齐 + 限界 §21 登记口径 + 新增 `SupplyChainScanSurfaceTest` 两条不变式机检），**未**回调阈值 / 未删断言：`1316/948` → **`5/0`**、断言退出码 `1` → **`0`**，扫描面仍覆盖 **317 构件 / 57 配置**（生产 runtime/compile 面俱在）。宿主全量绿 `xml=331 tests=2309 skipped=13 failures=0 errors=0`（§224 基线 330/2304 ⇒ +1 类 / +5 例逐项可溯）；`BROKEN_MD_LINKS=0`、`RESOLVED_INDEX_SYNC=OK`；**如实声明**——本环境**无 CI 通道且推送不可用**（SSH 被沙箱拦截、`gh` token 失效、artifact 匿名下载 401），故四项 AC 中「CI 实跑 / CI 连续 ≥3 次 push / 两 job 各跑一次 / Code Scanning 计数下降」**一律未达成**，已逐条以本机等价证据替代并留痕） | `ISSUE-P1-216` / `ISSUE-P2-217` / `ISSUE-P2-218` / `ISSUE-P2-219`（四条整条结案） | [225-CI门禁四项缺陷整改批次.md](batches/225-CI门禁四项缺陷整改批次.md) |
| §226 | Passkey 创建链路拒绝原因呈现批次（`ISSUE-P2-220` **整条结案**：用户真机复现「Via 浏览器打开 passkeys.io 创建通行密钥，系统弹窗点『继续』后 KeePasskey 无任何界面」——Via 不在特权浏览器白名单 ⇒ origin 降级 `android:apk-key-hash:` ⇒ 走普通应用 DAL 远程校验 ⇒ passkeys.io 无对 Via 包名的 assetlinks ⇒ `NOT_VERIFIED` ⇒ fail-closed，而该拒绝发生在**任何 UI 呈现之前**（`failAndFinish()` 只回传 `RESULT_CANCELED`），用户视角「点了继续就断」；**判定无错，缺的是把判定交付到用户眼前**）。整改＝①拒绝原因词汇表 `CredentialRejectionReason`（10 个原因各携带预定义 `@StringRes` + `fromDalResult` 纯函数投影，`VERIFIED` → `null` 放行）②门禁抽为 `PasskeyRegistrationGate.evaluate(...): CredentialRejectionReason?`（判定顺序与 fail-closed 口径逐字沿袭、`verifyDal` 参数注入 ⇒ 可穷举断言「哪些分支不得发起 DAL 请求」，Activity 399 → **388** 行）③新增 `CredentialRejectionScreen`——**整页**而非 `AlertDialog`（对话框窗口不继承 `FLAG_SECURE`），经基类 `rejectAndFinish(reason)` 渲染、首条语句仍为 `ApplyObscuredTouchFilter()` ④六处 fail-closed 分支全改走带原因的收尾，**唯一保留的静默收尾**是 `onRejected`（用户主动取消用户验证，非 fail-closed）。**AC② 的结构性保障**＝8 条新增文案**无 `%` 占位符**（承载 rpId / 包名 / 域名 / 凭据 id 的唯一通道即插值）；**AC③**＝确认后仍走 `failAndFinish()`，对系统的回传契约与整改前完全一致。新增 `CredentialRejectionFeedbackTest` 12 例 + `ObscuredTouchWiringTest` 清单纳入 `BaseCredentialActivity.kt`；宿主全量绿 `xml=332 tests=2321 skipped=13 failures=0 errors=0`（§225 基线 331/2309 ⇒ +1 类 / +12 例逐例可溯）、tier1=2 / tier2=29 / 超长函数 3 均不增；**如实声明**：未改 `androidTest` 用例与原生面（无设备侧必跑项），拒绝页真机观感与 Via 场景端到端**未核对**） | `ISSUE-P2-220`（整条结案） | [226-Passkey创建链路拒绝原因呈现批次.md](batches/226-Passkey创建链路拒绝原因呈现批次.md) |
| §227 | Passkey 拒绝页就地补救授权批次（`ISSUE-P3-221` **整条结案**：用户反馈「弹窗说明 xx 不是 xxx 的信息，然后下面两个选择，一个退出，一个是添加到特权名单里面，减少跳转和操作」——将 §226 的「纯原因呈现」深化为「**就地补救**」：拒绝页点名调用方应用展示名（如「Via」），并提供**就地将浏览器添加到特权白名单**的主动作，以及次动作「退出」）。**零跳转**：完全在当前受保护窗口内闭环完成，彻底消除跳转设置页跨 Activity 传参的 DoS 面与未解锁绕过风险。**资格守卫严格 fail-closed**：`BrowserRemedyBuilder` 仅对 DAL 两类拒绝生效，且严格枚举 `installedBrowsers()`（必须能处理 https VIEW 意图且现场可读签名证书）——**原生 App 一律不呈现添加按钮**（避免误导用户），已启用浏览器亦不呈现。**对系统回传契约不变**：即使用户点击添加且授权成功，本次创建仍以 `RESULT_CANCELED` 收尾（origin 拉起时已固定无法原地变更），授权作用于下一次发起（产品裁决 `PD-12`）。写入白名单由宿主协程下沉 `Dispatchers.Default` 执行。新增 6 例守卫断言（总 18 例全绿）；`PasskeyCreateActivity.kt` 397 行守住 <400；长函数 3 个持平；截图编译门禁通过；单测全绿；机检全部 PASS） | `ISSUE-P3-221`（整条结案） | [227-Passkey拒绝页就地补救授权批次.md](batches/227-Passkey拒绝页就地补救授权批次.md) |
