# 分册 05 · 批次一行索引（§158 起，滚动）

> **定位**：与分册 01 ~ 04 同级，**只放一行索引、不复制批次正文**（立则见 [`README.md`](README.md)）。
> 分册 04 已按「超过 100 行另立」的规则封卷于 §157，本册自 §158 起滚动。
> **总索引**：[`../RESOLVED_LOG.md`](../RESOLVED_LOG.md)；批次正文：[`batches/`](batches/)。

| § | 批次主题 | 条目范围 | 正文 |
|:--:|---|---|---|
| §158 | 静态守卫清洗口径收口批次（`ISSUE-P3-194` 闭环：19 份守卫全部委托双口径工具，余 16 份收口且 84 例原样通过；另立分册 05） | `ISSUE-P3-194`（闭环） | [`158-静态守卫清洗口径收口批次.md`](batches/158-静态守卫清洗口径收口批次.md) |
| §159 | 密码库设置页段落下沉批次（`ISSUE-P3-188` 第 1 项第二段：整页 431 → 369 行，新增段落接线守卫 4 例） | `ISSUE-P3-188`（第 1 项第二段闭环） | [`159-密码库设置页段落下沉批次.md`](batches/159-密码库设置页段落下沉批次.md) |
| §160 | 编辑页分组与绑定应用解析下沉批次（`ISSUE-P3-188`：`EntryEditFormSections` 438 → 351 行；同步把 `AlgoHotPathGuardsTest` 改为两文件并集扫描） | `ISSUE-P3-188`（第一档再降一档） | [`160-编辑页分组与绑定应用解析下沉批次.md`](batches/160-编辑页分组与绑定应用解析下沉批次.md) |
| §161 | 群组生命周期对话框集下沉批次（`ISSUE-P3-188` 第二档：`VaultListDialogs` 490 → 280 行，新文件 242 行；第二档实测重数为 40 个） | `ISSUE-P3-188`（第二档消化一个头部文件） | [`161-群组生命周期对话框集下沉批次.md`](batches/161-群组生命周期对话框集下沉批次.md) |
| §162 | 注册响应材料组装下沉批次（`ISSUE-P3-188` 第二档：`PasskeyCreateActivity` 460 → 359 行，新文件 133 行；该档实测降至 39 个） | `ISSUE-P3-188`（第二档再消化一个头部文件） | [`162-注册响应材料组装下沉批次.md`](batches/162-注册响应材料组装下沉批次.md) |
| §163 | 条目 TOTP 面下沉批次（`ISSUE-P3-188` 第二档：`VaultEntryMapper` 490 → 384 行，新文件 127 行；该档实测降至 38 个） | `ISSUE-P3-188`（第二档再消化一个头部文件） | [`163-条目TOTP面下沉批次.md`](batches/163-条目TOTP面下沉批次.md) |
| §164 | 填充确认页归属块下沉批次（`ISSUE-P3-188` 第二档：`AutofillConfirmActivity` 475 → 366 行，新文件 126 行；该档实测降至 37 个） | `ISSUE-P3-188`（第二档再消化一个头部文件） | [`164-填充确认页归属块下沉批次.md`](batches/164-填充确认页归属块下沉批次.md) |
| §165 | 凭据编辑页屏幕外壳抽出批次（`ISSUE-P3-188` 第二档：`EntryEditScreen` 472 → 319 行，新文件 135 行；该档实测降至 36 个） | `ISSUE-P3-188`（第二档再消化一个头部文件） | [`165-凭据编辑页屏幕外壳抽出批次.md`](batches/165-凭据编辑页屏幕外壳抽出批次.md) |
| §166 | 同步冲突合并判据下沉批次（`ISSUE-P3-188` 第二档：`SyncConflictController` 461 → 355 行，新文件 144 行；该档实测降至 35 个） | `ISSUE-P3-188`（第二档再消化一个头部文件） | [`166-同步冲突合并判据下沉批次.md`](batches/166-同步冲突合并判据下沉批次.md) |
| §167 | 预览深色模式位收敛与魔法数字目复核批次（`ISSUE-P3-188` 第 4 目：`@Preview(uiMode = 0x20)` 151 处全部改用平台命名常量，71 个 `src/main` 文件；附带修掉生成器两处缺陷） | `ISSUE-P3-188`（第 4 目复核与收敛） | [`167-预览深色模式位收敛与魔法数字目复核批次.md`](batches/167-预览深色模式位收敛与魔法数字目复核批次.md) |
| §168 | 魔法数字第 4 目三小项裁定批次（`ISSUE-P3-188` 第 4 目：同值异名常量合并、`ASCII_SPACE` 阈值命名、uint16 上限命名；剩余 169 处完成三类定性，第 4 目裁定毕业） | `ISSUE-P3-188`（第 4 目裁定完成） | [`168-魔法数字第4目三小项裁定批次.md`](batches/168-魔法数字第4目三小项裁定批次.md) |
| §169 | 凭据详情页屏幕本体收敛批次（`ISSUE-P3-188` 第一档：`EntryDetailScreen` 427 → 349 行，预览夹具与明文导出确认对话框归位；第一档仅余 1 个未达标，第二档 34 个） | `ISSUE-P3-188`（第一档消化至仅余 1） | [`169-凭据详情页屏幕本体收敛批次.md`](batches/169-凭据详情页屏幕本体收敛批次.md) |
| §170 | 详情页装配链下沉与会话锁定样板收敛批次（`ISSUE-P3-188` 第一档收尾：`EntryDetailViewModel` 434 → 397 行，第一档维度闭环；第二档 33 个） | `ISSUE-P3-188`（第一档达标） | [`170-详情页装配链下沉与会话锁定样板收敛批次.md`](batches/170-详情页装配链下沉与会话锁定样板收敛批次.md) |
| §171 | 详情页「锁定即擦除」直调用例批次（`ISSUE-P3-188` 剩余清单第 5 项第一段：新增 1 例钉住密码明文与强度读数在锁定后归零；余 edit / settings 两例已注明应断言通道） | `ISSUE-P3-188`（剩余清单第 5 项第一段） | [`171-详情页锁定即擦除直调用例批次.md`](batches/171-详情页锁定即擦除直调用例批次.md) |
| §172 | 待办条目流水压缩批次（「精简文档」：`ISSUE-P3-188` 剩余清单 41 行 → 24 行，剩余清单重编号并留映射；仅文档、链接 185 条失效 0） | 目标「精简文档」（无条目编号） | [`172-待办条目流水压缩批次.md`](batches/172-待办条目流水压缩批次.md) |
| §173 | 断言响应材料下沉批次（`ISSUE-P3-188` 第二档：`PasskeyAssertionActivity` 450 → 267 行，与 §162 注册侧对称；第二档 32 个，并打开新的宿主直调验证面） | `ISSUE-P3-188`（第二档再消化一个头部文件） | [`173-断言响应材料下沉批次.md`](batches/173-断言响应材料下沉批次.md) |
| §174 | 密钥对生成面下沉批次（`ISSUE-P3-188` 第二档：`PasskeyCryptoEngine` 453 → 269 行，生成三族与专属私有辅助整体下沉；第二档 31 个；并核实「断言 payload 宿主直调」在 JVM 不可行的原因与两条可行路） | `ISSUE-P3-188`（第二档再消化一个头部文件） | [`174-密钥对生成面下沉批次.md`](batches/174-密钥对生成面下沉批次.md) |
| §175 | 健康度页仪表段下沉批次（`ISSUE-P3-188` 第 3 目与第二档双击：最长 Compose 函数 326→227、`HealthCheckScreen` 406→299；第二档 30 个；重测口径与 28 个长函数清单入条目） | `ISSUE-P3-188`（第 3 目首段 + 第二档消化一个） | [`175-健康度页仪表段下沉批次.md`](batches/175-健康度页仪表段下沉批次.md) |
| §176 | 设置页局部投影归层批次（`ISSUE-P3-188` 第二档：`SettingsPreferencesController` 446 → 379 行，映射函数与三个局部投影归入既有纯投影层；第二档 29 个） | `ISSUE-P3-188`（第二档再消化一个头部文件） | [`176-设置页局部投影归层批次.md`](batches/176-设置页局部投影归层批次.md) |
| §177 | 待办条目口径复核批次（`ISSUE-P3-188` 文档面：第一档数法三处并一处、`DatabaseSettingsScreen` 369→370 修正、已消化 11→13、新增两档复核脚本；剩余清单补登第 6 项分流欠账） | `ISSUE-P3-188`（文档口径收敛，零生产代码改动） | [`177-待办条目口径复核批次.md`](batches/177-待办条目口径复核批次.md) |
| §178 | 打开已有密码库对话框段落拆分批次（`ISSUE-P3-188` 第 3 目：`OpenExistingVaultDialog` 226 → 96 行，同包新建 8 个窄参数段落组件；≥100 行 Compose 函数 28 → 27；反向逐字核对与三个搬家坑留痕） | `ISSUE-P3-188`（第 3 目再消化一个） | [`178-打开已有密码库对话框段落拆分批次.md`](batches/178-打开已有密码库对话框段落拆分批次.md) |
| §179 | 新建库向导段落拆分批次（`ISSUE-P3-188` 第 3 目：`CreateVaultWizardDialog` 234 → 158 行，新建同包 4 个段落组件；`CreateVaultPresetTest` 向导判据改「本体 + 段落文件」并集扩扫；逐字核对工具化 `tools/doc/check_verbatim_move.py`） | `ISSUE-P3-188`（第 3 目再削一个最长非装配表函数） | [`179-新建库向导段落拆分与守卫并集批次.md`](batches/179-新建库向导段落拆分与守卫并集批次.md) |
| §180 | 文档相对链接断链收口批次（`docs/resolved/batches/` 19 份文件 26 处链接按「相对本文件」口径修正；新增 `tools/doc/check_md_links.py` 自检并登记进 `AGENTS.md` §4 索引纪律 / §5 命令表；围栏代码块内的粘贴正文判为假阳性、不改） | 文档面自发现（用户命题「精简文档」，无既有 ISSUE 条目） | [`180-文档相对链接断链收口批次.md`](batches/180-文档相对链接断链收口批次.md) |
| §181 | 长函数度量工具化与快照分流批次（新增 `tools/doc/long_functions.py`，≥100 行函数复核 = **28 个全为 Compose 面**；**更正一条失真结论**：`processFillRequest` 55 / `mergeConflictedEntry` 52 未达「≤50」验收线，登为剩余清单第 6 项；违例清单第 3 目的 20 处历史快照原样分流至本文件 §4） | `ISSUE-P3-188`（第 3 目度量与结论校准） | [`181-长函数度量工具化与快照分流批次.md`](batches/181-长函数度量工具化与快照分流批次.md) |
| §182 | 合并层冲突对下沉与早退守卫限界批次（第 3 目「≤50 行」验收线逐条结案：`mergeConflictedEntry` 52→41 已下沉；`processFillRequest` 55 登记为**限界 §20**；修 `check_verbatim_move.py` 参数校验 off-by-one；更正 lint 计数口径 216 → **215**（`grep "<issue"` 误含根元素）） | `ISSUE-P3-188`（第 3 目验收线结案 + 度量口径更正） | [`182-合并层冲突对下沉与早退守卫限界批次.md`](batches/182-合并层冲突对下沉与早退守卫限界批次.md) |
| §183 | Argon2 参数对话框段落拆分批次（`ISSUE-P3-188` 第 3 目：`Argon2ParametersDialog` 168 → **80 行出表**，≥100 行 Compose 函数 **28 → 27**；两处内联档位表常量化；`LaunchedEffect` 回填逐项核对；登记一次「切片顺序错置 ⇒ 删除静默落空」的脚本坑） | `ISSUE-P3-188`（第 3 目再消化一个） | [`183-Argon2参数对话框段落拆分批次.md`](batches/183-Argon2参数对话框段落拆分批次.md) |
| §184 | 装配表口径裁决批次（`ISSUE-P3-188` 第 3 目：登记 **PD-11**——两个纯接线 NavGraph（252 逻辑行 1 / 164 逻辑行 0）豁免、`KeePasskeyApp`（202 逻辑行 10）**不豁免**，待拆数 27 → 25；新增 `tools/doc/logic_lines.py` 把 PD-11 重开条件做成可执行判据） | `ISSUE-P3-188`（范围口径，用户裁决后登记 PD-11） | [`184-装配表口径裁决批次.md`](batches/184-装配表口径裁决批次.md) |
| §185 | 应用外壳逻辑下沉批次（`ISSUE-P3-188` 第 3 目：按 PD-11 对**不豁免**的 `KeePasskeyApp` 动手，202 → **86 行出表**、逻辑行 10 → 0；≥100 项 27 → 26（扣除 PD-11 豁免后待拆 24）；新增 `KeePasskeyAppLogicTest` 5 例——该文件首份行为用例，总用例 2198 → **2203**） | `ISSUE-P3-188`（第 3 目再消化一个 + 补验证面） | [`185-应用外壳逻辑下沉批次.md`](batches/185-应用外壳逻辑下沉批次.md) |
| §186 | 解锁区段落拆分批次（`ISSUE-P3-188` **一批双降**：第二档 **29 → 28**（`UnlockContentSections` 430→336）、≥100 行 **26 → 25**（`UnlockStandardUnlockContent` 158→71）；新建 `UnlockStandardUnlockSections.kt` 四段，主密码 `SecurePasswordField` 链路刻意留本体；`UiMd3AlignmentWiringTest` 判据按内层列表并集扩扫；顺带收紧 `logic_lines.py` 一处会误导的豁免判定语） | `ISSUE-P3-188`（第 3 目 + 第二档双降） | [`186-解锁区段落拆分批次.md`](batches/186-解锁区段落拆分批次.md) |
| §187 | 关于页段落拆分批次（`ISSUE-P3-188` 第 3 目：`AboutSettingsScreen` 153 → **75 行出表**，≥100 项 25 → 24、待拆 22；新建 `AboutSettingsScreenSections.kt` 三段 + 一个 private 助手；逐字核对仅 3 处形参改名；**实测并登记剩余清单第 6 项**——11 处 / 241 行重复 `TopAppBar` 骨架） | `ISSUE-P3-188`（第 3 目再消化一个 + 新登结构性重复） | [`187-关于页段落拆分与重复骨架登记批次.md`](batches/187-关于页段落拆分与重复骨架登记批次.md) |
| §188 | 二级设置页共用骨架批次（第 6 项首段：新建 `SettingsSubscreenScaffold.kt`，零守卫引用的五页各 −21 行（About/Theme/Autofill/PrivilegedBrowser/Totp），重复 5 份收敛为 1 份；附带 `PrivilegedBrowserSettingsScreen` 115→95 **出第 3 目表** ⇒ ≥100 项 24 → 23、待拆 21） | `ISSUE-P3-188`（第 6 项推进 + 第 3 目再消化一个） | [`188-二级设置页共用骨架批次.md`](batches/188-二级设置页共用骨架批次.md) |
| §189 | 守卫点名五页接入共用骨架批次（`ISSUE-P3-188` **第 6 项结案**：五页（`Debug`/`CloudSync`/`Security`/`HealthCheck`/`DatabaseSettings`）接入共用骨架，**净 −135 行 ⇒ 十页重复收敛为一份**；`HealthCheckScreen` 227→206 未出表（≥100 仍 23、第二档仍 28）；新增 `tools/doc/scaffold_block_fingerprint.py` 指纹判据，**据此查出 §188 的一处顶栏配色回归 ⇒ 新登 `ISSUE-P3-195`**） | `ISSUE-P3-188`（第 6 项闭环）/ 新登记 `ISSUE-P3-195` | [`189-守卫点名五页接入共用骨架批次.md`](batches/189-守卫点名五页接入共用骨架批次.md) |
| §190 | 两处「锁定即擦除」宿主直调用例批次（`ISSUE-P3-188` **第 4 项结案**：编辑页 +1 例（三层证据，含「锁定后保存不再带明文」的行为级断言）、设置页新建 1 例（WebDAV 口令 + S3 两密钥三条预填通道同时擦除）；新建可写内存 prefs 共享替身；总用例 2203 → **2205**，未触碰生产代码；过程缺陷：临时 `glob` 换尺子 ⇒ 新增 `tools/doc/count_test_results.py` 固定计数口径） | `ISSUE-P3-188`（第 4 项闭环 + 新登第 5 项） | [`190-两处锁定擦除宿主直调用例批次.md`](batches/190-两处锁定擦除宿主直调用例批次.md) |
| §191 | 健康度页审计行下沉批次（`ISSUE-P3-188` 第 1 目：`HealthCheckScreen` **206 → 95 行出表** ⇒ ≥100 项 22、待拆 20；两条同形判定并为 `HealthCountAuditRow` + 纯函数 `healthAuditTone`（补 4 例宿主单测）；两文件净 −89 行；**如实登记本批非逐字搬**，29 条缺失行逐条归类） | `ISSUE-P3-188`（第 1 目再消化一个 + 补验证面） | [`191-健康度页审计行下沉批次.md`](batches/191-健康度页审计行下沉批次.md) |
| §192 | 冲突页段落下沉批次（`ISSUE-P3-188` 第 1 目：`ConflictResolutionScreen` **172 → 91 行出表** ⇒ ≥100 项 21、待拆 19；三段整块内联呈现下沉为新文件 `ConflictResolutionScreenSections.kt`（刻意不追加进 394 行的原文件，免越 400 造新第二档项）；顺带 `FieldDiffRow` 两份选项行并一（83 → 41）；36 条缺失行逐条归类，守卫锚点未动无需扩扫；分档不变 2/28） | `ISSUE-P3-188`（第 1 目再消化一个） | [`192-冲突页段落下沉批次.md`](batches/192-冲突页段落下沉批次.md) |
| §193 | 子库挂载对话框段落下沉批次（`ISSUE-P3-188` 第 1 目：`ChildDatabaseDialog` **170 → 97 行出表** ⇒ ≥100 项 20、待拆 18；非保密三段下沉为新文件 `ChildDatabaseDialogSections.kt`（原文件距 400 仅 3 行）；`SecureDialogWindowEffect()` 两处与子库主密码链路刻意留本体；7 条缺失行全为接收者改写，守卫按文件计数未受影响） | `ISSUE-P3-188`（第 1 目再消化一个） | [`193-子库挂载对话框段落下沉批次.md`](batches/193-子库挂载对话框段落下沉批次.md) |
| §194 | 详情页溢出菜单下沉与 Popup 清单重新盘点批次（`ISSUE-P3-188` 第 1 目：`EntryDetailTopBar` **151 → 84 行出表** ⇒ ≥100 项 19、待拆 17；菜单原样下沉新文件，局部状态随唯一使用者迁入；**首个跨 `PopupSecureFlagInventoryTest` 清单锁的搬移**：集合相等断言当场报红 ⇒ 重新盘点换路径 + 同步 `SecureDialog` KDoc，未删条目 / 未放宽「菜单项 == 8」；3 条缺失行逐条可解释；分档不变 2/28） | `ISSUE-P3-188`（第 1 目再消化一个 + 守卫口径新增一条） | [`194-详情页溢出菜单下沉与Popup清单重新盘点批次.md`](batches/194-详情页溢出菜单下沉与Popup清单重新盘点批次.md) |
| §195 | 测试替身去重批次（`ISSUE-P3-188` **第 5 项结案** ⇒ 剩余清单回到 1~4 项：`SyncCredentialsStoreTest` 改用共享 `InMemorySharedPreferences`，删私有 `Proxy` 夹具、净 −57 行；`memoryStorage` 改属性别名令 24 处断言字面未动；换替身前逐条比对行为差集（超集；唯一可变处 `commit()` 是死路）；该类定向 `tests=10` 全绿、全量 2209 例同值） | `ISSUE-P3-188`（第 5 项闭环） | [`195-测试替身去重批次.md`](batches/195-测试替身去重批次.md) |
| §196 | 库列表对话框宿主分段批次（`ISSUE-P3-188` 第 1 目：`VaultListDialogHost` **130 → 68 行出表** ⇒ ≥100 项 18、待拆 16；按特性切两段 `private` 组件、同文件追加；九个对话框的组合顺序一字未动；**`MISSING_KINDS=0` 的纯逐字搬**；该宿主零测试引用 ⇒ 证据只到编译与不回归；分档不变 2/28） | `ISSUE-P3-188`（第 1 目再消化一个） | [`196-库列表对话框宿主分段批次.md`](batches/196-库列表对话框宿主分段批次.md) |
| §197 | 对拍段登记与前置条件实测批次（文档面：新登 `ISSUE-P3-196`，把 `ISSUE-P3-188` 第 4 目最后一段（格式面 26 处 / 7 份）变成自包含条目；**实测否证其伪阻塞前提**（keepassxc-cli 2.7.12 / pykeepass 4.2.0 可用，cargo 在但代理被权限层拦）；写明设备义务边界与「corpus --verify 不构成互操作证据」；如实登记一次无 `--rerun-tasks` 的 `test` 不构成证据 ⇒ 依据沿用 §196；零代码改动、不减存量数字） | `ISSUE-P3-188`（第 4 目余量登记成条）/ 新登记 `ISSUE-P3-196` | [`197-对拍段登记与前置条件实测批次.md`](batches/197-对拍段登记与前置条件实测批次.md) |
| §198 | 详情页确认对话框下沉批次（`ISSUE-P3-188` 第 1 目：`EntryDetailDialogHost` **119 → 91 出表** ⇒ ≥100 项 17、待拆 15；两份内联 `AlertDialog` 原样下沉同文件 `private` 组件、**刻意不合并**（视觉语义不同），复位留调用点；5 条缺失全是闭包改形参；**新登第 5 项验证缺口**：两个破坏性确认出口零测试引用 ⇒ 优先级在继续削长度之前；分档不变 2/28） | `ISSUE-P3-188`（第 1 目再消化一个 + 新登第 5 项） | [`198-详情页确认对话框下沉批次.md`](batches/198-详情页确认对话框下沉批次.md) |
| §199 | 顶栏配色参数化与接线守卫批次（`ISSUE-P3-195` 结案：§188 的顶栏配色回归已复原；两条修法曾被权限层拦，用户明示自行决策后取①参数化骨架（②会让该页重新入 ≥100 表并留例外）；可空参数表达「不传该参数」而非 `Color.Unspecified`；九页未动；新增 4 例静态接线守卫 + 反向哨兵 ⇒ **一次性核对 ≠ 防护**（搬家口径第四形态），区分力以 `git show` 回归态代入谓词复核；总用例 2209 → 2213） | `ISSUE-P3-195`（结案）/ `ISSUE-P3-188`（口径第四形态） | [199-顶栏配色参数化与接线守卫批次.md](batches/199-顶栏配色参数化与接线守卫批次.md) |
| §200 | 详情页破坏性确认出口决策纯函数化批次（`ISSUE-P3-188` **第 5 项结案** ⇒ 剩余清单回到 1~4 项：§198 §4 证据边界「两个破坏性确认出口零断言」落地——`EntryDetailConfirmIntent` + 出口决策 `entryDetailConfirmExit` + 执行器 `entryDetailConfirmExitHandler`（先复位、再上行，§169 口径）三单元同文件追加，段组件改上报意图、状态所有权仍在调用点；**回滚确认「先上行、再复位」的历史孤例并入口径**（自始如此，§198 文档记述失真，唯一差异在同步抛错路径）；4 例纯函数 + 4 例静态接线守卫（裸调用 == 0 / 执行器调用点 == 2 / 触发态复位各一份），宿主函数 89 行未回表；**复跑纠偏**：§199 复原配色在函数体内 +5 行令 `PrivilegedBrowserSettingsScreen` 恰好 100 重新入表且其验证表漏跑长函数尺子 ⇒ 现跑 18、待拆 16 已就地修正登记；总用例 2213 → 2221、lint 215 持平） | `ISSUE-P3-188`（第 5 项闭环 + 第 1 目计数纠偏） | [`200-破坏性确认出口决策纯函数化批次.md`](batches/200-破坏性确认出口决策纯函数化批次.md) |
| §201 | 格式面 26 处字面量逐处判定批次（`ISSUE-P3-196` **整条结案**、`ISSUE-P3-188` 第 4 目自此闭环：7 份文件 26 处按 §167/§168 判据逐处裁定——**登记 21**（文档引用 / 规范固定字节：WebAuthn §10.1 的 0x00、KeePass 固定 IV、XML 1.0 §2.2 Char 区间、X.690 tag、RFC 8410 OCTET STRING 包裹、KDBX 4 headerHmacKey 的 LE64(2^64−1)）、**命名 5 值**（`PasskeyPrf` 的 `ASCII_LF/CR/SPACE/TAB` 等值剥离语义 + `PasskeyPkcs8Codec` 的 `ASCII_7BIT_MAX`）；全部只挪定义未改取值；`KdbxFile`/`KdbxXmlStreamWriter` 零改动 ⇒ AC2 对拍前置不成立；passkey 三套件 5+9+14 定向全绿、全量 2221 同值、lint **214**（−1）；过程缺陷登记 §200 漏两处归档登记 + 引擎套件包名错位致 `--tests` 静默空匹配） | `ISSUE-P3-196`（结案）/ `ISSUE-P3-188`（第 4 目闭环） | [`201-格式面26处字面量逐处判定批次.md`](batches/201-格式面26处字面量逐处判定批次.md) |
| §202 | 存量条目流水精简批次（文档面、零代码改动：`ACTIVE_ISSUES.md` 355 → **310 行**——按「批次流水以 resolved/ 为单一真相源」纪律，剪去 `ISSUE-P3-188` 条目内复述的 §155~§201 逐批流水（已出表 9 个 / 已消化 14 个的逐文件明细、历史编号映射注记、已闭环叙述）；**逐条核对一个不落全部活约束**：现跑计数与 §200 复跑纠偏、现跑尺子义务、刻意不搬三处、搬移约束四条、刻意排后三条、`CloudSyncScreen` 距出表 1 行、PD-11 豁免与重开条件、第 3 项守卫四形态与第 4 项工作项一字未动；`AGENTS.md` 通读无 §9 违例不动；`BROKEN_MD_LINKS=0`） | 文档面批次（无条目变动） | [`202-存量条目流水精简批次.md`](batches/202-存量条目流水精简批次.md) |
| §203 | 设备侧覆盖余量清单闭环批次（`ISSUE-P2-192` **整条结案**：8 条余量逐条处置——① sync 传输层设备侧 8 例（WebDAV 5 / S3 3，回环 MockWebServer 真实 socket + takeRequest 报文断言）②平台网络策略 5 例（策略读数 false / **设备 LAN IP 非回环明文被拦** / 回环结果与读数一致 / 自签 HTTPS 拒 / SSRF 回环防线；**API 37 真机对回环内置豁免** `loopback=true` 而 default=false，API 36.1 模拟器拦——差异实测登记限界 §4.1）③Hilt 全图testing 入限界（EntryPoint 子图已有 Worker 用例证）④WorkManager 3 例（work-testing 调度/取消/Worker 经 Hilt EntryPoint 真实执行）⑤系统 UI 六面入限界 §4.2 ⑥core 首建 androidTest + AppLog 4 例（logcat nonce 轮询证真实写入/门控/脱敏）⑦**CI device-gate job**（API 36.1 x86_64 模拟器 + 全模块 connected；镜像名实测 `android-36.1` 带 minor）⑧三处硬断言 Assume 化 + AutofillAuthChain **真机连跑两轮全绿**（可重复执行达标）；过程修 4 缺陷（sync 测试 APK 无 INTERNET / targetSdk 37 平台默认禁明文拦回环 / WorkInfo 无 workerClassName 等）；模拟器专有差异（完整性留痕缺失）新立 `ISSUE-P3-197`；**零生产代码改动**，宿主 2221 同值全绿） | `ISSUE-P2-192`（结案）/ `ISSUE-P3-197`（新登） | [`203-设备侧覆盖余量清单闭环批次.md`](batches/203-设备侧覆盖余量清单闭环批次.md) |
| §204 | 断言响应材料宿主写入口径批次（`ISSUE-P3-188` 剩余清单第 4 项**结案** ⇒ 回到 1~3 项：§174 路线②落地——新增宿主可用的极简写入器 `WebAuthnJsonWriter`（键序=插入序 / 紧凑 / AOSP 转义语义含 `\/`、不实现数字与 null），**注册 / 断言两处 payload 全部改走它**（`org.json` import 双双移除，`buildPrfClientExtensionResults` 收敛 private）；**四条宿主离线断言** `PasskeyAssertionPayloadBuildTest`（归属省略与不回退本应用包名含正对照 / prfEval=null 空对象 / authData 37 字节标准布局——条目「前 16 字节」系笔误按标准口径 / JCE 公钥验签 `authData‖SHA-256(clientDataJSON)`，解析走 SimpleJson、签名走生产 PEM 链路）+ 转义回归 8 例 + **设备侧 `org.json` 逐字节对拍 3 例双设备全绿**（该契约只能设备侧锁定）；守卫核对：`CredentialManagerCallerBindingWiringTest` 判据锚门控调用点与组装无交集、零改动；**过程发现并同批修复 `AutofillAuthChainDeviceTest` 两处缺陷**（`linePid()` split 双空格解析对 4 位 pid 恒假——真机此前两轮绿系 pid 恰好 5 位巧合、`INTEGRITY_WAIT_MS` 15s<30s 重扫周期采窗缺陷），修复后真机恢复全绿 64 例，模拟器残余「系统填充 UI 呈现」环境面 CI device-gate 以 notClass 排除、真机承担 ⇒ `ISSUE-P3-197` 更新收窄；宿主 **2221 → 2233**（+12）、lint 214 持平） | `ISSUE-P3-188`（第 4 项闭环）/ `ISSUE-P3-197`（根因定位并修复） | [`204-断言响应材料宿主写入口径批次.md`](batches/204-断言响应材料宿主写入口径批次.md) |
| §205 | Compose 长函数拆分两处批次（`ISSUE-P3-188` 第 1 项渐进：待拆 **16 → 14**——`PackageBlocklistManageDialog` 131 出表（段落**另起新文件** `PackageBlocklistDialogSections.kt`：原文件距 400 余量仅 25 行，按 §192/§196「余量逐案判」口径；Body / Confirm / Dismiss 槽位 / 名单行 + rememberAppOption 同迁 private→internal，状态仍由对话框持有经参数回传不自持；**回调语义保真**：名单模式 confirm 槽=关闭，拆分时曾误写无条件 submit 已即时纠偏）+ `GeneratorContent` 121 出表（`GeneratorScreen.kt` 295 行余量充足、段落**追加原文件**：TopBar / LazyListScope 模式 Tab / 历史区 isEmpty 早退保序）；逐字核对 MISSING 全为回调参数化 / 可见性 / 注释、绘制代码逐字未动；tier2=28 维持不变名单零变动；test 2233 同值全绿、lint 214 持平） | `ISSUE-P3-188`（第 1 项渐进） | [`205-Compose长函数拆分两处批次.md`](batches/205-Compose长函数拆分两处批次.md) |
| §206 | Compose 长函数拆分两处批次（`ISSUE-P3-188` 第 1 项渐进：待拆 **14 → 12**——`BasicCredentialsCard` 118 出表（`EntryDetailCards.kt` 397 行距 400 仅 3 行 ⇒ 段落另起新文件 `EntryDetailCardSections.kt`：用户名行 + 密码区，haptic 同源不自持）+ `SyncStatusCard` 119 出表（`CloudSyncComponents.kt` 追加将贴 400 档沿 ⇒ 新文件 `CloudSyncStatusSections.kt`：状态头行 / 反馈条（null 早退等价）/ 按钮行）；**守卫并集活例**：`UiMd3AlignmentWiringTest` 禁用态配色判据锚门面文件首跑即红（守卫有效证据）⇒ 同批改「门面+分支文件」并集、断言零变化；tier2=28 维持；test 2233 同值全绿、lint 214 持平） | `ISSUE-P3-188`（第 1 项渐进） | [`206-Compose长函数拆分两处批次.md`](batches/206-Compose长函数拆分两处批次.md) |
| §207 | Compose 长函数拆分两处批次（`ISSUE-P3-188` 第 1 项渐进：待拆 **12 → 10**——`themeListSection` 118 出表（密度选择器下沉 `ThemeDensitySelector` 追加原文件，7 个 DisplayPrefRow 声明式序列是最小单元不再动）+ `VaultDatabaseCard` 117 出表（头部行下沉 `VaultDatabaseCardHeader`；**路径行不随迁**——MD3 中段省略守卫锚定 `middleEllipsize` 在宿主现场，免并集化）；tier2 不变；test 2233 同值全绿、lint 214 持平） | `ISSUE-P3-188`（第 1 项渐进） | [`207-Compose长函数拆分两处批次.md`](batches/207-Compose长函数拆分两处批次.md) |
| §208 | Compose 长函数拆分两处批次（`ISSUE-P3-188` 第 1 项渐进：待拆 **10 → 8**——`HealthCheckScoreCard` 116 出表（仪表盘 + 文本序列下沉追加原文件）+ `MasterKeyChangeDialog` 116 出表（**§193 清单锁生效**：`SecureDialog {` 与 AlertDialog 本体留宿主文件，只搬字段渲染面（onPasswordChanged 擦除链回样上行）与按钮渲染面（onClick 提交与 pwdChars finally 擦除链全留现场，类比 §179 单一现场），`SecureDialogFlagPolicyTest` 随套件通过）；test 2233 同值全绿、lint 214 持平） | `ISSUE-P3-188`（第 1 项渐进） | [`208-Compose长函数拆分两处批次.md`](batches/208-Compose长函数拆分两处批次.md) |
| §209 | Compose 长函数拆分两处批次（`ISSUE-P3-188` 第 1 项渐进：待拆 **8 → 6**——`S3ConfigFields` 113 出表（凭据双字段段 + path-style 开关行下沉追加原文件，SecurePasswordField 的 CharArray 通道注释随迁）+ `ChildVaultEntryRowView` 113 出表（次级信息行下沉，hasSecondaryLine 判定仍留调用方先行、本段不渲染空行）；tier2 不变；test 2233 同值全绿、lint 214 持平） | `ISSUE-P3-188`（第 1 项渐进） | [`209-Compose长函数拆分两处批次.md`](batches/209-Compose长函数拆分两处批次.md) |
| §210 | Compose 长函数拆分两处批次（`ISSUE-P3-188` 第 1 项渐进：待拆 **6 → 3**——`EntryEditCustomFieldsSection` 108 出表（单字段编辑卡片下沉追加原文件，TASK-10 擦除通道注释随迁）+ `SafeAttachmentPreviewDialog` 107 出表（隔离提示横幅 + 预览画布下沉；**清单锁零风险设计**：只搬 `SecureDialog {` 包裹内部渲染段，调用点计数不变、守卫零改动；过程缺陷脚本重复执行致函数重复当场修复）；test 2233 同值全绿、lint 214 持平） | `ISSUE-P3-188`（第 1 项渐进） | [`210-Compose长函数拆分两处批次.md`](batches/210-Compose长函数拆分两处批次.md) |
| §211 | Compose 长函数收官批次（`ISSUE-P3-188` 第 1 项**闭环**：待拆 **3 → 0**——`KeePasskeyTheme` 104 出表（resolveAppColorScheme 须 @Composable 因内部读 LocalContext，过程缺陷当场修）+ `PrivilegedBrowserSettingsScreen` 100 出表（**§199 守卫锚零触碰**：顶栏参数与注释留宿主，只下沉内容段）+ `EntryDetailScreen` 121 出表（**§169 全文遵守**：SAF 三态/exportLauncher/确认对话框留 Route 现场，只下沉生命周期编排/Snackbar 消费/Content 纯装配段；**过程纠偏真险情**：onExportAttachment 写路径误搬 Host 不会回写 Route 状态 ⇒ 改 Route 传导出回调）；现跑表内余项全为已登记豁免，「无未登记的超限项」达标；test 2233 同值全绿、真机 connected 64 例全绿、lint 214 持平） | `ISSUE-P3-188`（第 1 项闭环） | [`211-Compose长函数收官批次.md`](batches/211-Compose长函数收官批次.md) |
| §212 | JNI 零拷贝评估批次（`ISSUE-P3-187` **评估项结案**：AC① 三路线契约（Critical=GC 钉住窗口劣于②不采纳 / DirectByteBuffer=唯一零拷贝+无窗口+纪律自洽 / CipherSpi=长线演进与②正交）+ AC⑤ 四维对照 + AC② 探针原型实测——Rust `applyKeystreamDirect`（DirectBuffer 直取就地变换无拷贝无分配）+ Kotlin 绑定 + 探针用例，**现状 24.05ms（基线 24.7 复现一致）vs 直扣 3.7ms ≈6.5×，拷贝+分配 44% 份额消除，AC② 达标**；正确性前置两路径逐字节一致；Redmi 4X 真机缺测如实登记（USB 断连，探针已入库随批补跑）；`cargo test` 73 全绿、crypto connected 36 全绿、四层设备义务模拟器轮完成、符号契约 CI 10→11；**实施另立 `ISSUE-P3-198`**；限界 §15/§17 解除条件已更新；顺带登记 P3-188 第 2 项用户容差裁定（±15%）） | `ISSUE-P3-187`（结案）/ `ISSUE-P3-198`（新立）/ `ISSUE-P3-188`（容差裁定登记） | [`212-JNI零拷贝评估批次.md`](batches/212-JNI零拷贝评估批次.md) |
| §213 | `ISSUE-P3-188` **整条结案**批次（2026-09-18 用户命题「消除巨型类和魔法数字」闭环：第 1 目第一档 §170 闭环、第 3 目 ≥100 行函数 §211 闭环（七批拆 15 处，表内余项全为已登记豁免，现跑 4 个）、第 4 目字面量 §201 闭环、剩余清单第 4 项 §204 闭环；第 2 目第二档按 2026-09-19 用户容差裁定（±15% 以内不为出表而拆，超容差仅 RealVaultRepository/VaultRepository 且属刻意排后）合规留清单，28 个现状；**拆分纪律四形态随条目归档继续有效**；条目原文原样收录本批次；test 2233 同值全绿、lint 214 持平） | `ISSUE-P3-188`（整条结案） | [`213-ISSUE-P3-188整条结案批次.md`](batches/213-ISSUE-P3-188整条结案批次.md) |
| §214 | `ISSUE-P3-197` **结案**批次（三条验收全达成：①真机 64 例多轮全绿 ②模拟器驱动面按条目口径另行立项且链路断言零放宽 ③**本地完整复刻 CI device-gate 命令**（全模块 connected + notClass 排除 autofill）在同镜像 Pixel_10 上 **99 例全绿**（core 4 / app 63 / database 15 / sync 17），排除机制精确生效；CI 云端首跑绿灯因私有仓库无凭据由维护者页面确认，残余风险仅 runner 差异；batch 文档原样收录条目） | `ISSUE-P3-197`（结案） | [`214-ISSUE-P3-197结案批次.md`](batches/214-ISSUE-P3-197结案批次.md) |
| §215 | ChaCha20 / AES 直扣（DirectByteBuffer）生产化批次（`ISSUE-P3-198` **整条结案**：AES 原地内核 `cbc_{encrypt,decrypt}_in_place`（解密侧 `Zeroizing` 暂存本组密文；闸门先行、失败不留半截变换）+ `NativeAes_cbc{Encrypt,Decrypt}BlocksDirect` 两导出；**ChaCha20 解密流与 AES 双流切 direct 直扣**（流实例自持堆外缓冲、交付即归零、`wipeRange` 原语；`CbcStreams` 双形态注入 byte[] 形态逐字保留，`requireDistinctResult` 仅适用 byte[] 形态；`CbcDecryptingInputStream` 拆分落位经逐字搬移复核）；**整块 byte[] 与 ChaCha20 加密流有意维持拷贝桥**（契约使直扣无收益，限界 §15/§17 已登记）；**AC③ 真机补测完成**——Redmi 4X 10 轮中位 ChaCha20 149.8→66.3ms（≈2.26×）、AES 178.9→48.7ms（≈3.67×），模拟器 ChaCha20 23.6→3.7、AES 43.9→14.7（≈内核×1.1，AC② 达标）；探针数字≠生产端到端（字节 API 边界拷贝不可消，口径见实测记录 §9.2）；cargo 76 全绿、宿主 test 2242（+9 `CbcDirectStreamsTest`）skipped=13 为 sync 既有基线、lint 214 持平、长函数 ≥100 回落 4→3、四层 connected 模拟器 crypto 37/database 15/sync 全绿/app 63+1（autofill 环境面经 HEAD 对照归因非本批）、真机探针类 8 例全绿；符号契约 11→13；过程留痕：探针误用演化 iv 当解密链值（真机现形，用例侧缺陷）| `ISSUE-P3-198`（整条结案） | [`215-DirectByteBuffer直扣生产化批次.md`](batches/215-DirectByteBuffer直扣生产化批次.md) |
| §216 | `ISSUE-P3-207` **判为误报归档**批次（四轮循环对抗审查收敛：`SyncEndpointGuard.kt:26-27` 逐字声明双层且声明范围 = 实际生效范围 ⇒ 标题「声明与生效面不符」与背景「接线人无从知道」**均失实**；残余 nit 无独立安全语义、无独有交付物（302 用例与 P2-208 AC② 重复）⇒ 归档，残句 KDoc 澄清**并入 `ISSUE-P2-208`**；同步把 P2-208 由符号级升为**上游源码级**（`okhttp-android-5.5.0-sources.jar` → `RouteSelector.kt:174-184`：字面量在 `dnsLookup` 之前 return）+ **真机级**，更正 `[::1]` 括注（IPv6 字面量**同样**旁路）与 AC① 技术口径（须拦截 `connect()` 的 `Socket`，裸 `SocketFactory` 取不到目标地址）） | `ISSUE-P3-207`（归档：误报）/ `ISSUE-P2-208`（升级） | [216-文档半边误报归档-SyncEndpointGuard-KDoc.md](batches/216-文档半边误报归档-SyncEndpointGuard-KDoc.md) |
| §217 | 对抗审计真机取证批次（五轮闭环：4 个新设备用例坐实 4 项悬置前提——`SsrfRedirectBypassDeviceTest`（`MockWebServer.url()` host = `localhost` 主机名据此**证伪**「IP 字面量假阳性归因」指控 + 302 字面量跳转 Dns 零调用而主机名对照组被拦）、`RawFileWritePermissionDeviceTest`（裸写文件 **0600** / `mkdirs()` 目录 **0700** / 私有目录 **0771** ⇒ P3-202 窗口期暴露真机不存在）、`InlineCompressedBinaryBudgetDeviceTest`（直驱生产 `BinaryNode`：`maxHeap = 192 MiB`、单节点满额内联压缩附件 **`OutOfMemoryError`**）、`PendingIntentMatchKeyDeviceTest`（`filterEquals` 匹配键含 component / extras 不参与））；`:sync:` 22、`:database:` 17、`:app:` 67 全绿；新立 **`ISSUE-P3-209`**（`CleartextPolicyDeviceTest` SSRF 用例恒真、零判别力 ⇒ 限界表 §4.1 证据链引用错误）；过程留痕三处自方缺陷） | 对抗审计取证（新立 `ISSUE-P3-209`） | [217-对抗审计真机取证批次.md](batches/217-对抗审计真机取证批次.md) |
| §218 | 分册索引漏登回填与「分册 ↔ 批次正文」机检守卫批次（文档面批次、零代码改动：`BATCH_158_PLUS.md` 止于 §202 而 `batches/` 已有 **§203~§215 共 13 份正文** ⇒ 无法从任何分册跳达 + `resolved/README.md` 仍写「当前最大 §202」⇒ 派号将重号；`check_md_links.py` 只校验链接**可达**、不校验**漏登**；处置＝原样搬运 `RESOLVED_LOG.md` 的 §203~§215 摘要行回填 + README 口径更正 + 新增 `tools/doc/check_resolved_index_sync.py`（正向／反向／口径三判据，豁免表 `{§42,§43}`）+ 接入 `AGENTS.md` §5；验证＝**先红后绿**） | 文档面批次（无条目变动） | [218-分册索引漏登回填与机检守卫批次.md](batches/218-分册索引漏登回填与机检守卫批次.md) |
| §219 | P2 三条闭环批次（CM 通道 `PendingIntent` requestCode **进程级单调**：`CredentialPendingIntents.nextRequestCode()` 收口四条落地入口、KDoc 更正「与 CM 同构」自称失实，Create/Assertion 两路改以系统注入 `callingRequest` 为权威（create 侧 origin 由**本次** `CallingAppInfo` 重新派生、`providerReq` 缺失即 fail-closed；assertion 侧 challenge 取系统 JSON 且 origin 交叉核对不一致即拒签，extras 降级为展示缓存）+ **载荷累计预算**（`BinaryReferenceBudget` → `AttachmentBudget` 三闸门：池引用累计 / 内联物化累计 64 MiB / 内联压缩节点 1024；`{REF:}` 展开加 `MAX_EXPANSIONS=4000` + `MAX_PRODUCED_CHARS=1 MiB` 双闸门，超限**原样保留引用原文**，列表渲染逐字段 Throwable 兜底）+ **SSRF 连接期复核**（新增 `SsrfGuardSocketFactory` 覆写 `Socket.connect` 拦在 SYN 之前，`SsrfAddressApprovals` 使白名单豁免在 Dns 层与连接期层逐地址一致，消除内网自建可用性回归）；新增 3 个宿主用例类 + 5 个设备用例；宿主 **2259**（本批 +16）全绿、`:sync:` 22 与 `:database:` 17 双设备全绿、`:app:` Redmi 4X 70 全绿（Pixel_10 AVD 1 例 `AutofillAuthChainDeviceTest` 经 `git stash` 回 HEAD 复跑同样失败 ⇒ 既有环境面）| `ISSUE-P2-199` / `ISSUE-P2-200` / `ISSUE-P2-208`（三条整条结案） | [219-CM请求码单调与载荷预算及SSRF连接期复核批次.md](batches/219-CM请求码单调与载荷预算及SSRF连接期复核批次.md) |
| §220 | 通行密钥 KPEX 互操作对拍与 Ed25519 PKCS#8 形态整改批次（`ISSUE-P2-210` **整条结案**：按规则 8 补齐官方实现端到端对拍——`tools/passkey-interop/` 新增对拍脚本与外部 fixture 生成器、`database` 新增探针与读取半边两个用例类；**写入方向**三条 KPEX 条目（ES256/Ed25519/RS256）经 `keepassxc-cli` + `pykeepass` **双实现独立读数**并由 `cryptography` 独立解析 PEM、重导出公钥与产物公开公钥**逐字节相等**（114 判据全绿）；**读取方向**外部产出且**无** `Passkey.*` 扩展键的 fixture 经生产读路径还原、OID 嗅探算法并真实签名成功；**当次暴露并闭环 `ISSUE-P2-211`**：Ed25519 私钥曾为 RFC 5958 `OneAsymmetricKey`（`version=1`+`publicKey [1]`），本仓可读而 **OpenSSL 系（含 KeePassXC）拒收**，经手写 DER 隔离实验定位 `version=1` 即被拒 ⇒ 编码侧改 RFC 8410 `version=0` 最小形态，解码侧仍接受 v0/v1；全量 `test` **2265**（+6 逐例可溯）全绿、真机四层 `crypto 37/database 17/sync 22/app 71`（app 含新增 Ed25519·RS256 用例）**0 skipped/0 failed**；**如实声明**：证据为文件级/离库，**端到端真机贯通（github.com）未执行**（强系统 UI 依赖）已附手工冒烟清单，限界 §4.3 解除条件达成并按边界重写） | `ISSUE-P2-210` / `ISSUE-P2-211`（两条整条结案） | [220-通行密钥互操作对拍与Ed25519-PKCS8形态整改批次.md](batches/220-通行密钥互操作对拍与Ed25519-PKCS8形态整改批次.md) |
| §221 | P3 低危七项收敛批次（`ISSUE-P3-201` picker 交付链路「锁定即丢弃」三处门控 + 守卫测试扩至 picker 源码；`ISSUE-P3-202` 同步缓存与防回滚守卫全走 `writeTmpSynced`「第一字节即仅属主」+ 设备测试 tmp 窗口 / rollback 断言；`ISSUE-P3-203` cargo 双构建 `--locked` + dry-run 核对 + 守卫机检；`ISSUE-P3-204` `NativeArgon2` 探活 KAT 对照（独立参考实现冻结值 + 宿主对拍三方一致）+ `Argon2ProbeKatTest`；`ISSUE-P3-205` 豁免 regex 版本界机检（宽度不变式 + toml 版本 ≥ 2.4.20 降级旁路封闭）；`ISSUE-P3-206` `SyncProvider.download` 流式化（`copyBounded` + `receiveRemote`/`ReceivedRemote`，引擎 4 下载点改造，下载期峰值 ~2×S→~1×S，KDoc 失实前提更正）；`ISSUE-P3-209` 恒真 SSRF 用例去重合并移除 + 限界 §4.1 / §203 证据链追溯更正。宿主 `xml=328 tests=2277 skipped=13` 全绿（+1 类 / +12 例可溯）；真机四层 `crypto 37 / database 17 / sync 24 / app 70` 全绿） | `ISSUE-P3-201` / `202` / `203` / `204` / `205` / `206` / `209`（七条整条结案） | [221-P3低危七项收敛批次.md](batches/221-P3低危七项收敛批次.md) |
| §222 | 通行密钥三项收敛批次（`ISSUE-P3-212` 私钥擦除单点化：新增消费式签名入口 `signAssertionConsumingKey`（`finally` 内无条件清零**调用方**缓冲，成功 / 异常两路径）并由断言路径改走它、活动层私钥副本清理面删除，`signAssertion` 原语义以对照用例并列锁定；`ISSUE-P3-213` v1 → KPEX **写路径就地迁移**：core 新增判据 `PasskeyData.needsKpexMigration`（只扫键名、不解密）+ crypto 新增协作单元 `PasskeyLegacyKeyText`（历史文本解码 + 文本 → PKCS#8 PEM，一切失败归一为 null 的 fail-safe），`PasskeyEntryCoordinator` 在计数器修补的**同一受控变换**内迁移（旧键全清 / 私钥重包受保护 PEM / 失败则不迁移且不阻断计数），app 层私有历史解码逐字搬入 crypto 单点（`PasskeyAssertionPayload` 222 → 185 行）；`ISSUE-P3-214` 旧格式精确嗅探：`sniffAlgorithmId` 顺序重排 + 「恰 64 字符 hex → ES256」「32 字节种子 → Ed25519」（hex 必须先于 Base64——hex 字母表是 Base64 真子集，否则 64 字符 hex 被解成 48 字节无 OID 垃圾）；文档收窄：限界 §4.3 标题与第 4 条、PD-09 边界改为「用到才迁」+ 对拍记录 §5 补注留痕。宿主全量绿 `xml=329 tests=2295 skipped=13 failures=0 errors=0`（§221 基线 328/2277 ⇒ +1 类 / +18 例逐项可溯）、lint 214 持平、tier2 不增、超长函数 3 不变） | `ISSUE-P3-212` / `213` / `214`（三条整条结案） | [222-通行密钥擦除收敛与v1就地迁移批次.md](batches/222-通行密钥擦除收敛与v1就地迁移批次.md) |
| §223 | 生物识别解锁默认关闭与「开启前当场验证」批次（`ISSUE-P2-212` **整条结案**，用户命题「默认虽然开了但不起作用，应由自己手动打开、并在打开那一瞬间验证生物识别」）：① 出厂默认 `true` 与「封印凭据只在主密码解锁成功后登记」相矛盾（`UnlockModePolicy.resolve` 要求开关开 **且** 凭据在）⇒ 新装设备「开关显示已开、解锁页无生物入口」的静默失效；② `SettingsPreferencesController.setBiometricEnabled` 仅裸写偏好，开关可在无任何身份确认下被打开。整改：三处默认值统一改 `false`（`UserSettings` / `RealSettingsRepository` 读回缺省 / `SettingsUiState`）；**新增** `BiometricEnableCoordinator`（241 行）——「开启」先过 Class 3 强生物识别能力闸门（fail-closed，**绝不**降级锁屏凭据）⇒ 有封印凭据时以其解密 Cipher 绑定 `CryptoObject`（准备失败即清除陈旧凭据并退化为纯身份验证）⇒ BiometricPrompt 验证（60 s 超时兜底；`Failed` 视为非终态继续挂起，避免「用户最终成功而开关未开」错位）⇒ **通过才写偏好**，验证中重复触发幂等；关闭免验证直落偏好；删除 `SettingsPreferencesController.setBiometricEnabled` 消除绕过验证的直写旁路；UI 宿主 Activity 改经 `LocalActivity` 获取（`LocalContext.current as? FragmentActivity` 触发 AndroidLint `ContextCastToActivity`，已修）、验证中开关禁用 + 进度圈 + 三类结果反馈（中英各 +4 条），`SettingsUiStateProjection` 五路流 → **六路**（`combine` 改 `Triple`）。宿主全量绿 `xml=330 tests=2303 skipped=13 failures=0 errors=0`（§222 基线 329/2295 ⇒ +1 类 / +8 例逐项可溯）、lint **214** 持平且 `:app:lintDebug` BUILD SUCCESSFUL、tier2 不增（`SecuritySettingsScreen` 407 → 439 本已在档）、超长函数 3 不变；**如实声明**：设备侧 `connectedDebugAndroidTest` **未执行**（未改 `androidTest` 用例，仅 `compileDebugAndroidTestKotlin` 通过），`BiometricPrompt` 实际弹窗仍属限界 §4.1 手工冒烟面） | `ISSUE-P2-212`（整条结案） | [223-生物识别默认关闭与开启当场验证批次.md](batches/223-生物识别默认关闭与开启当场验证批次.md) |
| §224 | 解锁页无障碍提示迁入设置页安全分区批次（`ISSUE-P3-215` **整条结案**，用户命题「首页会提示已启用无障碍服务……不应该出现在提示里面，应该在设置里面显示，纯属冗余」：解锁页移除常驻提示与提示位 / 订阅 / 失效 import，设置页安全分区新增 `AccessibilityStatusCard`（可点直达系统无障碍设置页）；字符串 `unlock_accessibility_notice_*` → `sec_accessibility_notice_*`；判定与「只提示、不降级」语义**零改动**；两处守卫判据改指新承载位置，首轮全量测试**先红后绿**；宿主 `xml=330 tests=2304 skipped=13 failures=0 errors=0` 全绿） | `ISSUE-P3-215`（整条结案） | [224-无障碍提示迁入设置页批次.md](batches/224-无障碍提示迁入设置页批次.md) |
| §225 | 云端 CI 门禁四项缺陷整改批次（`ISSUE-P1-216` **机制性根因**：`applySignCountPatch` 在**会话 Mutex 之外**从落树实例读回计数器，而计数器补丁走**增量定点擦除**、`withSignCount` 每次落树新建计数器 `ProtectedString` ⇒ 第 N 次提交上线的实例恰是第 N+1 次的擦除候选 ⇒ 并发后继提交抢先完成时读回命中 `ProtectedString.checkNotCleared()`（CI 日志 `ProtectedString.kt:173`）。**本机复现率对照**（临时探针，未入库）：`runTest+Dispatchers.Default` 32 路×1200 轮命中 **0**、真实线程+`CyclicBarrier`+GC 搅动 32 路×800 轮命中 **3402** ⇒ 首次机制性解释「本机绿 / CI 红」（挂起态不占 CPU vs 真线程争抢被抢占）。修复=读回点移入 `transform`（临界区内），临界区外只搬运 `Int`，语义不损失；新增 3 例（确定性机制复现+复演 CI 同形异常 / 真实线程屏障 32 路×150 轮，整改前第 11 轮即红 / 多轮并发不丢失更新）。`ISSUE-P2-217`：`lint` 经 AGP lint 模型链（`:crypto:bundleDebugLocalLintAar` → `mergeDebugNativeLibs`）触达 `:crypto:cargoNdkBuild`（本机 `--dry-run` 实测），fast-gate 遂恒失败于「工具缺失」⇒ 改 `./gradlew lint -x :crypto:cargoNdkBuild`（显式排除、不吞失败；本机实跑 **BUILD SUCCESSFUL 3m11s**、`cargoNdkBuild` 0 次、app Lint 计数 **214** 与基线持平）。`ISSUE-P2-218`：`setup-android@v4.0.1` 默认 `packages: 'tools platform-tools'` 而裸 `tools` 包**远端已不存在**（实测 `repository2-1.xml`/`repository2-3.xml` 中 `path="tools"` 计数均为 0）⇒ 两处显式传 `packages: "platform-tools"`，并在 `docs/records/ci-静态校准记录.md` 新增 §12 + 为 §2 的 v3 基线加「已过期」注。`ISSUE-P2-219`：本机同参数复现 CI 的 `1316 实例 / 948 达阈`，逐条定位后 **948 条 100% 落在 AGP 工具链配置族**（`unified-test-platform-*` / `androidLintTool` / `_internal-screenshot-*`，35 构件：netty 4.1.93/4.1.110、grpc 1.57.2、kotlin-stdlib 1.9.0、protos 32.2.1、protobuf-java-util 3.22.3、jsoup 1.6.3），**生产面 0 条** ⇒ 按 AC④ 通道做**扫描面收口**（`skipConfigurations` 显式补齐 + 限界 §21 登记口径 + 新增 `SupplyChainScanSurfaceTest` 两条不变式机检），**未**回调阈值 / 未删断言：`1316/948` → **`5/0`**、断言退出码 `1` → **`0`**，扫描面仍覆盖 **317 构件 / 57 配置**（生产 runtime/compile 面俱在）。宿主全量绿 `xml=331 tests=2309 skipped=13 failures=0 errors=0`（§224 基线 330/2304 ⇒ +1 类 / +5 例逐项可溯）；`BROKEN_MD_LINKS=0`、`RESOLVED_INDEX_SYNC=OK`；**如实声明**——本环境**无 CI 通道且推送不可用**（SSH 被沙箱拦截、`gh` token 失效、artifact 匿名下载 401），故四项 AC 中「CI 实跑 / CI 连续 ≥3 次 push / 两 job 各跑一次 / Code Scanning 计数下降」**一律未达成**，已逐条以本机等价证据替代并留痕） | `ISSUE-P1-216` / `ISSUE-P2-217` / `ISSUE-P2-218` / `ISSUE-P2-219`（四条整条结案） | [225-CI门禁四项缺陷整改批次.md](batches/225-CI门禁四项缺陷整改批次.md) |
| §226 | Passkey 创建链路拒绝原因呈现批次（`ISSUE-P2-220` **整条结案**：用户真机复现「Via 浏览器打开 passkeys.io 创建通行密钥，系统弹窗点『继续』后 KeePasskey 无任何界面」——Via 不在特权浏览器白名单 ⇒ origin 降级 `android:apk-key-hash:` ⇒ 走普通应用 DAL 远程校验 ⇒ passkeys.io 无对 Via 包名的 assetlinks ⇒ `NOT_VERIFIED` ⇒ fail-closed，而该拒绝发生在**任何 UI 呈现之前**（`failAndFinish()` 只回传 `RESULT_CANCELED`），用户视角「点了继续就断」；**判定无错，缺的是把判定交付到用户眼前**）。整改＝①拒绝原因词汇表 `CredentialRejectionReason`（10 个原因各携带预定义 `@StringRes` + `fromDalResult` 纯函数投影，`VERIFIED` → `null` 放行）②门禁抽为 `PasskeyRegistrationGate.evaluate(...): CredentialRejectionReason?`（判定顺序与 fail-closed 口径逐字沿袭、`verifyDal` 参数注入 ⇒ 可穷举断言「哪些分支不得发起 DAL 请求」，Activity 399 → **388** 行）③新增 `CredentialRejectionScreen`——**整页**而非 `AlertDialog`（对话框窗口不继承 `FLAG_SECURE`），经基类 `rejectAndFinish(reason)` 渲染、首条语句仍为 `ApplyObscuredTouchFilter()` ④六处 fail-closed 分支全改走带原因的收尾，**唯一保留的静默收尾**是 `onRejected`（用户主动取消用户验证，非 fail-closed）。**AC② 的结构性保障**＝8 条新增文案**无 `%` 占位符**（承载 rpId / 包名 / 域名 / 凭据 id 的唯一通道即插值）；**AC③**＝确认后仍走 `failAndFinish()`，对系统的回传契约与整改前完全一致。新增 `CredentialRejectionFeedbackTest` 12 例 + `ObscuredTouchWiringTest` 清单纳入 `BaseCredentialActivity.kt`；宿主全量绿 `xml=332 tests=2321 skipped=13 failures=0 errors=0`（§225 基线 331/2309 ⇒ +1 类 / +12 例逐例可溯）、tier1=2 / tier2=29 / 超长函数 3 均不增；**如实声明**：未改 `androidTest` 用例与原生面（无设备侧必跑项），拒绝页真机观感与 Via 场景端到端**未核对**） | `ISSUE-P2-220`（整条结案） | [226-Passkey创建链路拒绝原因呈现批次.md](batches/226-Passkey创建链路拒绝原因呈现批次.md) |
| §227 | Passkey 拒绝页就地补救授权批次（`ISSUE-P3-221` **整条结案**：用户反馈「弹窗说明 xx 不是 xxx 的信息，然后下面两个选择，一个退出，一个是添加到特权名单里面，减少跳转和操作」——将 §226 的「纯原因呈现」深化为「**就地补救**」：拒绝页点名调用方应用展示名（如「Via」），并提供**就地将浏览器添加到特权白名单**的主动作，以及次动作「退出」）。**零跳转**：完全在当前受保护窗口内闭环完成，彻底消除跳转设置页跨 Activity 传参的 DoS 面与未解锁绕过风险。**资格守卫严格 fail-closed**：`BrowserRemedyBuilder` 仅对 DAL 两类拒绝生效，且严格枚举 `installedBrowsers()`（必须能处理 https VIEW 意图且现场可读签名证书）——**原生 App 一律不呈现添加按钮**（避免误导用户），已启用浏览器亦不呈现。**对系统回传契约不变**：即使用户点击添加且授权成功，本次创建仍以 `RESULT_CANCELED` 收尾（origin 拉起时已固定无法原地变更），授权作用于下一次发起（产品裁决 `PD-12`）。写入白名单由宿主协程下沉 `Dispatchers.Default` 执行。新增 6 例守卫断言（总 18 例全绿）；`PasskeyCreateActivity.kt` 397 行守住 <400；长函数 3 个持平；截图编译门禁通过；单测全绿；机检全部 PASS） | `ISSUE-P3-221`（整条结案） | [227-Passkey拒绝页就地补救授权批次.md](batches/227-Passkey拒绝页就地补救授权批次.md) |
| §228 | 全局导航与退出动效优化批次（`ISSUE-P3-222` **整条结案**：用户命题「软件的动画可以说是没有，请你调用谷歌开发者知识库，看看怎么优化动画，尤其是退出和切换」——调用 Google 知识库检索并落地 Material 3 Motion 与 Android 14/15/16 Predictive Back 最佳实践。① **系统级预测性返回**：`AndroidManifest.xml` 声明 `android:enableOnBackInvokedCallback="true"`，激活 Back-to-home 桌面平滑缩放与跨 Activity 退出动效；② **Material 3 共享轴（Shared Axis X）下钻转场**：新增 `AppNavigationMotion.kt`，`NavHost` 接入 `defaultEnterTransition`（向左滑入 + 淡入）、`defaultExitTransition`（向左微移视差 + 淡出）、`defaultPopEnterTransition`（从左视差恢复 + 淡入）与 `defaultPopExitTransition`（向右滑出 + 缩放至 0.9f + 淡出），原生挂钩 Navigation Compose 2.8+ 手势驱动；③ **顶层 Tab 贯穿淡入淡出（Fade Through）**：`Unlock`、`VaultList`、`Authenticator`、`Generator` 明确声明 `topLevelEnterTransition` 与 `topLevelExitTransition`，避免平级切换产生横向位移层级错乱，下钻返回仍享受视差恢复；④ 新增 `AppNavigationMotionTest` 4 例守护断言。宿主单测 `xml=333 tests=2331 failures=0 errors=0 skipped=13` 全绿、截图测试编译通过、tier1/tier2/超长函数均零破坏） | `ISSUE-P3-222`（整条结案） | [228-全局导航与退出动效优化批次.md](batches/228-全局导航与退出动效优化批次.md) |
| §229 | 生物识别闪退与自动填充保存及解锁通知倒计时批次（`ISSUE-P1-223` / `ISSUE-P1-224` / `ISSUE-P3-225` **三条整条结案**，代码整改随 commit `b4670f2` 入库、本批为其文档流转归档）：`ISSUE-P1-223`：`BiometricAuthManager` 在纯生物识别（不含 `DEVICE_CREDENTIAL`）模式下 `negativeButtonText` 为 null 时未设负向按钮即 `build()`，AndroidX 硬性要求该模式必须设置非空负向按钮 ⇒ `IllegalArgumentException` 主线程闪退 ⇒ 兜底改为 `takeIf { isNotBlank() } ?: getString(R.string.btn_cancel)` 恒非空，调用方 `BiometricEnableCoordinator` 显式传参；`ISSUE-P1-224`：保存链路三处缺陷一体整改——① `SaveInfo` 拆分 `requiredIds`（密码）/ `optionalIds`（用户名）并设 `FLAG_SAVE_ON_ALL_VIEWS_INVISIBLE` 提升系统保存提示触发率；② 新增 `AutofillSaveExtractor`（75 行）**倒序遍历全部 FillContext** + 密码/用户名两级特征兜底，杜绝 `lastOrNull()` 取到跳转后空页而静默丢弃；③ 库锁定时经 `PendingIntent`（`REQUEST_CODE_SAVE=2300`）回传 `IntentSender` 唤起 `PasswordSaveActivity`（+`EXTRA_PASSWORD` 通道）在受保护窗口解锁承接保存，已解锁路径写盘成功才 `onSuccess()`；`ISSUE-P3-225`：`AutoLockManager` 新增 `lockDeadline: StateFlow<Long?>`（进后台有超时档置 `backgroundTimestamp+delayMillis`，永不档/回前台/到点锁定回落 null），`UnlockedNotificationController` 并入 merge 订阅（零轮询），有效截止点时 `setWhen+setShowWhen(true)+setUsesChronometer+setChronometerCountDown` 由 SystemUI 原生渲染秒级递减，空闲态回退新文案 `notification_unlocked_text_idle`，发布去重扩为「截止点变化即重发」（`lastDeadline`）。**验证**：新增 `BiometricPromptCrashSafetyTest`(3)/`AutofillSaveRobustnessTest`(5)/`UnlockedNotificationWiringTest`+1，宿主全量绿 `xml=335 tests=2340 skipped=13 failures=0 errors=0`（§228 基线 333/2331 ⇒ +2 类 / +9 例逐例可溯）；tier1=2 / tier2=29 / 超长函数 3 均不增、`BROKEN_MD_LINKS=0`；**如实声明**——未改 androidTest 与原生面无设备侧必跑项，BiometricPrompt 弹窗、系统保存提示呈现、通知栏 Chronometer 渲染未做真机核对（限界 §4.1 手工冒烟边界）） | `ISSUE-P1-223` / `ISSUE-P1-224` / `ISSUE-P3-225`（三条整条结案） | [229-生物识别闪退与自动填充保存及解锁通知倒计时批次.md](batches/229-生物识别闪退与自动填充保存及解锁通知倒计时批次.md) |
| §230 | 自动填充自我排除批次（`ISSUE-P2-226` **整条结案**：用户真机反馈「在软件本体上面还能出现填充的选项，不合适」）：`KeePasskeyAutofillService` 全链路对「调用包名 == 自身包」**零排除**（闸门 `AutofillAccessPolicy` 只有 `INTEGRITY_RISK`/`BLOCKLISTED`，`res/xml/autofill_service.xml` 无 `packageNames`），而触发源是自家口令框——`SecurePasswordField` 声明 `KeyboardType.Password` + `password()` 语义、UI 层 `importantForAutofill` 全仓零命中，故框架照常被本应用自己的 `AutofillService` 请求填充；**既有文档前提被现场证伪**（`batches/109-…:54`「自动填充服务对自身包名不生效」）。整改＝①判定收敛到闸门：新增 `AutofillRejection.SELF_APP` + 纯函数 `isSelfApp()`（trim + `Locale.ROOT` 小写等值；空串两侧互不判为自身；不套 `AutofillPackageNames.normalize` 因其为合法性裁决、非法形态随后仍被黑名单 fail-closed），`rejectReason` 增 `selfPackage` 入参、优先级 `INTEGRITY_RISK > SELF_APP > BLOCKLISTED` ②填充路径在 `awaitEnforcement()` **之前**短路（免去自家口令框每次聚焦触发一轮 maps/路径重扫），闸门内 `SELF_APP` 分支保留兜底 ③保存路径将「空白包名」与「即本应用」合并为同一守卫、按 `onSuccess()`（本次无需保存）**静默**收尾而非 `onFailure`（本应用内凭据写入走自有写盘链路，报「保存被拒」反成误导）。**验证**：`AutofillAccessPolicyTest` 6 → **11 例**（+5：命中/大小写空白变形/优先于黑名单/外部包名不受影响/空串不得互判），既有 6 例迁新签名后语义逐条不变；宿主全量绿 `xml=335 tests=2345 skipped=13 failures=0 errors=0`（§229 基线 335/2340 ⇒ 同类 +5 例）；`tier1=2 / tier2=29 / 超长函数 3` 三项持平（中途曾因新增行把该文件推入 tier1、`handleSaveRequest` 推入 ≥100 档，已收敛至 490 行）；**如实声明**——未改 androidTest 与原生面故无设备侧必跑项，但「自家界面不再出现本应用建议条/保存气泡」这一效果**未经真机核对**（本环境无可用设备，限界 §4.1）；第三方填充提供程序在自家口令框上的呈现**不在本批范围**（与本应用无关的另一路），批次 §4.2 已登记 | `ISSUE-P2-226`（整条结案） | [230-自动填充自我排除批次.md](batches/230-自动填充自我排除批次.md) |
| §231 | 生物识别禁用归因点名批次（`ISSUE-P2-227` **整条结案**：用户真机反馈「选择生物识别解锁提醒说设备存在安全问题？啥情况，排查一下」）：**定位为自家闸门而非系统报错**——文案 `sec_biometric_integrity_blocked` 由本应用产生（`BiometricAuthManager:116` 读 `currentEnforcement().disableBiometricQuickUnlock`，ELEVATED/COMPROMISED/UNDETERMINED 三档皆禁），debug 包必命中 `appDebuggable`、adb 安装必命中 `untrustedInstallSource`，故开发者自测环境恒定看到「设备存在安全风险」却无从分辨是「跑的是 debug 版」还是「真被 root/注入」。**用户 2026-09-20 明示只改归因呈现、不放宽判据**（`com.android.shell` 不入受信任清单、debug 构建不豁免）。整改＝①新增 `security/IntegrityBlockReason`（8 项命中信号，**声明顺序即危害度降序**，各携 `@StringRes`，沿 `CredentialRejectionReason` 范式）+ 纯函数 `from(signals, undetermined)` ②`IntegrityEnforcement` 增 `biometricBlockReasons`，由 `evaluate` 与 `UNDETERMINED` 常量同源产出（`escalateForLiveSignals` 走 evaluate 重算故冷启动后新出现的调试器/ptrace 同样入清单），`BiometricResult.Error` 增 `blockReasons` 下行（与禁止外显的 `errString` 分工写明）③呈现分两处：解锁/开关当场验证取**首项**专属文案（清单为空回落既有通用文案，绝不凭空造原因），设置页风险卡 `IntegrityRiskCard(level, reasons)` 逐条点名以**取代**原「可调试构建**或**非受信任安装来源」的笼统枚举 ④文案 +9 zh/+9 en 且**一律无插值**（installer 包名可被任意应用自称，写进文案反成误导）⑤**就地更正 `detectUntrustedInstallSource` 的假前提注释**（原称 adb 直装 installer 为 null，实为 `com.android.shell` 非 null ⇒ adb 装的 release 包同样判 ELEVATED）。**验证**：新增 `IntegrityBlockReasonTest` 9 例（含两条不变式：**凡禁用生物解锁必有可点名原因**、**无障碍信号不得被当作降级原因**）+ `BiometricFailureMessagePolicyTest` +4 例；宿主全量绿 `xml=336 tests=2358 skipped=13 failures=0 errors=0`（§230 基线 335/2345 ⇒ +1 类/+13 例），`RuntimeIntegrityPolicyTest` 27 例（等级矩阵与 fail-closed 分级）逐条未改仍绿；`tier1=2/tier2=29/超长函数 3` 持平、`:app:compileDebugScreenshotTestKotlin --rerun` 通过；**如实声明**——无可用设备故「debug 包解锁时显示新文案」未真机核对；自动填充侧同类文案经系统对话框渲染、归因价值与载体不匹配，本批未改并已登记 | `ISSUE-P2-227`（整条结案） | [231-生物识别禁用归因点名批次.md](batches/231-生物识别禁用归因点名批次.md) |
| §232 | 自动填充通道开关做实批次（`ISSUE-P2-228` **整条结案**：用户命题「安全防护里的已启用无障碍服务，和自动填充里的旧版自动填充（需要无障碍）冲突了吧？后者应该默认关闭」）：**排查证实前提不成立**——Manifest 只注册 `CredentialProviderService` 与 `AutofillService`，全仓**无任何 AccessibilityService**，「跳无障碍设置后才可开启」无对象可落地；`autofill_legacy_sub`「兼容低版本 Android 的无障碍填充方式」（EN「Accessibility-based filling…」）属**虚假描述**（minSdk 36 下 AutofillService 自 API 26 即唯一官方通道），与「安全防护」那行（判据为**非本应用**的第三方无障碍服务）本就无关，冲突源自措辞。更实质者：**三个开关全是摆设**（默认 true、setter 只回写内存 StateFlow、无持久化键、服务侧零消费、唯一去处是健康卡入参 ⇒ 关掉啥也不影响、重启即回 true；同 ISSUE-P3-65 已移除的假开关形态）。经 AskUserQuestion 选定「文案改对 + 开关做成真的」，默认值维持 true（假开关时期通道实际恒开，改默认属另一项裁决）。整改＝①三开关迁入 `ExtendedSettings` + 读写键齐备 + `CHANNEL_SWITCH_DEFAULT`（用例锁定「数据类默认 ⇄ 单键缺省」同值，ISSUE-P2-43 踩坑形态），**删除** `autofillStateFlow`/三个 setter/`AutofillUiState` 载体与那一路 combine 输入流，UI 改从持久化 `extState` 投影，`SettingsViewModel` 门面名不变仅改委托（导航图零改动）②真实消费点：`autofillServiceEnabled` → `onFillRequest`（与 §230 自我排除合并为同一前置守卫）+ `onSaveRequest`；`credentialProviderEnabled` → CM `get`/`create` 两入口空响应；`passkeySupportEnabled` → `buildPasskeyEntries` 候选出口 + 公钥创建分支（密码通道不受影响），均置于 `awaitEnforcement()` 之前 ③`autofill_legacy_*`→`autofill_service_*` 并如实写关闭后果、健康卡文案同步、CM/Passkey 副标题补边界 ④AC④ 系统真实状态沿用既有 `AutofillHealthCard`（`hasEnabledAutofillServices` + `autofillServiceIntent` 一键直达，受 `OneTapInteractionWiringTest` 守卫）且与开关同源 ⑤登记限界 §23「关开关≠从系统列表摘除」（动态禁用已绑定扩展组件会留下『选中但无响应』异常态，刻意不做，文案不承诺移除）。**验证**：新增 `AutofillChannelSwitchWiringTest` 9 例（含**计数**断言防只接一处、旧载体不得复活、读写键成对、无障碍措辞不得残留并带解析条数下限防空转），并用已知坏样本 `autofill_probe_bad` 反校守卫（中英两侧均抓到、真实读数 92/0）；宿主全量绿 `xml=337 tests=2367 skipped=13`（§231 基线 336/2358 ⇒ +1 类/+9 例）、`tier1=2/tier2=29/超长函数 3` 持平、截图编译门禁通过；⚠️ `KeePasskeyAutofillService.kt` 达 498 行距 tier1 仅 2 行已登记；**如实声明**——四条开关行为与「重启保持」须真机冒烟，本环境无设备未核对 | `ISSUE-P2-228`（整条结案） | [232-自动填充通道开关做实批次.md](batches/232-自动填充通道开关做实批次.md) |
| §233 | 新建库落地位置可选批次（`ISSUE-P2-229` **整条结案**：用户命题「新建的文件在安装目录，而不是通过系统的文件管理器选择」）：`VaultLifecycleCoordinator.kt:125/130` 直接 `File(context.filesDir, "$name.kdbx")` 静默落应用私有目录，而**打开已有库早已走 SAF**（`OpenExistingVaultDialog.kt:43` + `takePersistableUriPermission`）⇒ 属遗漏非取舍；`产品裁决登记.md` 无相关裁决，`Privacy-Policy.md:82` 反承诺「或用户自行选择的存储位置」。用户 2026-09-20 选定「新建时二选一：内部存储 / 自选位置」。整改＝①向导增 `VaultStorageLocation` 二选一（默认内部存储）+ `VaultStorageLocationSection`（下沉到既有 Sections 文件，本体不增行），选外部当场拉起 `CreateDocument`、回显文档名，**取消即可见地回落**内部存储；`isLocationValid` 并入 `isFormValid` ⇒ 未挑定文档时创建按钮禁用，**绝不静默改投内部存储** ②`createDatabaseWithKeyFile` 全链增 `targetUri: String?`（接口/Real/协调器/VM 同序），`null` 分支原实现逐字搬入私有 `createInFilesDir`（AC④ 内部路径零回归），SAF 分支抽为独立件 `data/repository/SafVaultCreation.kt`（与内部建库无一行共享代码；留在协调器会恰好顶到 400 行越入 tier2 ⇒ 抽出后协调器回到 301 行，而非压缩注释过线）：**持久化授权先行**（拿不到即在动笔前显式失败，绝不留「重启后打不开」的半成品）→ cacheDir 造临时库（复用原子写通道）→ 搬到目标文档 → 三条失败出口一律 `SafDocumentCleanup.deleteCreatedDocument`（复用已测助手，只删本流程创建的文档）→ `openStream` 读回复核并绑定（让用户停留在已解锁态）→ `importExternalDatabase(..., SYNC_TYPE_FILE_PICKER)` 登记 ③`content://` 的 `"rwt"` 写回通道收敛为 `SafVaultCreation.saveWriter` **单点定义**（原解锁路径内联第二份拷贝消除），`syncType` 两个本机裸字面量提为常量，VM 成功事件改下行 `DatabaseSelected(targetUri ?: fileName)`（外部库标识即 uri，以文件名下行的选中事件无效）④文案 +10 zh/+10 en，外部位置同屏如实告知两条降级（非原子写 + 不参与 WebDAV/S3 同步）⑤登记限界 §24（SAF 库降级的事实/接受理由/三条不得外推边界/解除条件）；**同轮新登记 `ISSUE-P3-230`**：打开已有 SAF 库的授权失败仍是静默 `catch`（新建侧已改前置硬失败，打开侧因涉及「provider 不支持持久化仍须允许本次打开」的容错取向未一并改动）。**验证**：新增 `CreateVaultLocationWiringTest` 7 例（授权先于建库与写盘的**顺序**断言、两处显式失败文案、三失败判定 + 回滚调用 ≥3、`finally` 删临时件、读回复核 + 登记单一出口、`rwt` 只允许一处定义、未挑定必须无效）+ `DatabasePickerKeyFileCreateTest` 2 例（默认不下发 targetUri / 自选位置 uri 原样下行）；守卫按纪律用**已知坏样本**反校（授权挪到 create 之后 ⇒ 顺序断言红；删一处回滚 ⇒ 计数断言红；真文件 3 处通过）；宿主全量绿 `xml=338 tests=2376 skipped=13 failures=0 errors=0`（§232 基线 337/2367 ⇒ +1 类/+9 例）；`tier1=2/tier2=29/超长函数 3` 三项持平、截图编译门禁通过；**如实声明**——五条 SAF 真机场景（系统面板建议名 / 创建后已解锁可保存 / **重启后仍可打开** / 云盘 provider 显式失败且不留空文档 / 内部路径回归）本环境无设备**未核对**；`rwt` 非原子写是**接受的降级**而非「已满足工程规则第 4 条原子写盘」；同名碰撞面本批未复测，不声称已解决 | `ISSUE-P2-229`（整条结案）；新登记 `ISSUE-P3-230` | [233-新建库落地位置可选批次.md](batches/233-新建库落地位置可选批次.md) |
| §234 | 移除安装来源完整性信号批次（`ISSUE-P3-231` 结案：删除 `untrustedInstallSource` 信号——字段/`evaluate` 判定/`IntegrityBlockReason` 枚举/`detectUntrustedInstallSource`+`TRUSTED_INSTALLERS`/专属文案及全部相关用例一并清理；release 包不再因 installer 被禁生物解锁，debug 构建仍判 ELEVATED；篡改防护改靠签名指纹核对） | `ISSUE-P3-231`（整条结案） | [`234-移除安装来源完整性信号批次.md`](batches/234-移除安装来源完整性信号批次.md) |
| §235 | 内层随机流密钥擦除批次（`ISSUE-P3-235` / `RC-02` 收口的**迁移第 1 步 `G1`**，**该条不整条结案**）：KDBX4 的第二把明文密钥 `InnerRandomStreamKey`（64 B，解密全部 `Protected="True"` 字段）**读、写两条路径都没有清零点**——读路径用文件自述密钥构造 `InnerRandomStreamCipher` 后随 `InnerHeader` 出栈留存至 GC、写路径 `freshInnerKey` 同理。按同日交付的《敏感缓冲所有权契约》落地＝①所有权归 `InnerHeader`（O 类，新增 `clearSensitive()`；**不得**让 `InnerRandomStreamCipher` 去擦调用方数组——那是 B 类借用，属契约 R1 违例）②读路径把「构造内层流密码 → 解析 XML → 校验 HMAC 终止块」22 行**原样下沉**为 `decodeInnerPayload`，擦除放进其 `finally`（晚于解析、覆盖三条异常出口；副作用是 `loadPayload` 回到约定行数内）③写路径在 `savePayload` 的级联收尾外套 `try/finally`，收尾抛异常同样擦除（该函数是密钥**实际保护过载荷**的唯一路径；更早失败时密钥未加密任何载荷，故不为此改写 `save()` 控制流）④新增 `InnerStreamKeyWipeTest` 8 例（行为 4 + 静态接线 4：存在性 + **顺序**「读晚于 `KdbxXmlParser(`、写晚于 `KdbxXmlSerializer(`」+ `finally` 归属；顺序断言的前瞻理由是 §147 教训——BC 引擎当前在 `init` 一次性派生状态故「擦早」暂不改结果，一旦改惰性读取即为正确性条件）。**验证**：定向 8 例绿；**坏样本反校**（临时移除 `finally` 内擦除）⇒ 8 completed / 1 failed 且唯一红项正是顺序守卫，随后还原；宿主全量 `test --rerun-tasks --max-workers=1` → BUILD SUCCESSFUL 2m34s，`xml=339 tests=2383 failures=0 errors=0 skipped=13`（同会话改动前现跑 338/2375 ⇒ **+1 类/+8 例**）；`tier1=2 / tier2=29 / 超长函数 3` 三项持平；文档自检 `BROKEN_MD_LINKS=0` 且分册索引同步自检通过。**如实声明**：BC 引擎内部密钥调度无法经公开 API 擦除（残余，缓解方向为内核下沉）；无原生/会话路径改动故无设备侧必跑项；`G2` **经实施前复核原前提两处不成立并已就地更正**（`lastSyncedDb` 与 `pendingLocalDb` 均为**会话活动树的引用**，「只置 null」正确；真实残余仅 `pendingRemoteDb` + `pendingMergedRoot` 的合并器新建节点），且 `clearPendingConflictSession()` 同时服务「合并树已采用为会话库」路径 ⇒ 在该处就地擦除会擦掉刚采用的活动库共享节点（P0 级），故**刻意不实施**；`G4`（100+ 处散落 `fill` 无统一符号）与 `G5`（部分收口点缺契约用例）仍开放 | `ISSUE-P3-235`（迁移第 1 步；该条继续开放） | [`235-内层随机流密钥擦除批次.md`](batches/235-内层随机流密钥擦除批次.md) | |

| §236 | 存量裁决与 P3 两项收敛批次（**两项裁决**：`ISSUE-P2-231` 落重开决策判**仍不引入**依赖完整性锁定并交付可机检的替代缓解（`PD-14`）；`ISSUE-P2-232` 经 `PD-13` 判**维持现状不熔断**——关键补正：`COMPROMISED` 五项构成中**三项是纯文件存在性检查**，字面熔断会让 root/Magisk 与「装过 frida 未运行」的设备在解锁后 ≤30 s 反复被锁而**实质不可用**。**两条收敛**：`ISSUE-P3-233` 复核报告 `P3-120` 状态陈旧（7 处更正 + §15.3 新增「引用结论前对拍 HEAD」方法学）、`ISSUE-P3-234` DAL 出口 IP 字面量面定案（**IPv4 可达 / IPv6 被拒**，登记限界 §25）。**同轮新登并闭环** `ISSUE-P2-236`（`ic_launcher_monochrome.xml` 用 VectorDrawable 不支持的 `<circle>`：24 条 lint Error 致门禁长期红 + 该 6 个图元不渲染）。`test` **340/2387** 全绿、lint **229（0 errors）**、tier1=2 / tier2=29 / 长函数 3 持平；如实声明 lint 计数「214」自 §223 起已陈旧） | `ISSUE-P2-231` / `P2-232` / `P3-233` / `P3-234`（闭环）+ `P2-236`（同轮登记并闭环） | [`236-存量裁决与P3两项收敛批次.md`](batches/236-存量裁决与P3两项收敛批次.md) |

| §237 | 已有 SAF 库授权失败不再静默批次（`ISSUE-P3-230` **整条结案**：静默 `catch` → 「**容错取向不变 + 三处可观测归因**」= ① `AppLog.w` 留痕 ② 解锁页一次性提示 `unlock_msg_no_persisted_permission` ③ 列表卡片状态行 + **重新授权**入口（重选同一文件、复用既有导入路径登记）。判据单点化：新增 `VaultUriPermission.kt`（纯函数 `lacksPersistedReadPermission` + 唯一平台查询 `persistedReadUriStrings`，查询失败返回 **null「未知」** 而非空集 ⇒ **不误报**）。AC③ 不变；`test` **342/2395** 全绿（+2 类/+8 例）、lint **229（0 errors）** 持平、三项分档持平；守卫首轮即红（固定字窗被中文注释挤出窗外）⇒ 改「方法体切片」后转绿。**如实声明**：SAF 真机行为未核对，重授的「同一文件」由用户保证） | `ISSUE-P3-230`（闭环） | [`237-已有SAF库授权失败不再静默批次.md`](batches/237-已有SAF库授权失败不再静默批次.md) |

| §238 | 待决冲突解析树身份判定擦除批次（`ISSUE-P3-235` **整条结案**，其 AC② 的 `G2` 步）：`SyncConflictController` 独占的解密解析树（`pending*` + 一次合并判定的 `localDb` / `remoteDb` / `trustedBase` / 未采用的合并产物）由「只丢引用、等 GC」改为**身份集合判定定点擦除**（新增 `eraseDiscardedDatabase` / `eraseDiscardedGroup` / `eraseDiscardedParseResults`；存活侧 = 当前活动会话树，头部另按 `header` 实例身份判——`KdbxMerger` 原实例复用使裸擦会**静默清空刚采用的活动库**）。收口点：`clearPendingConflictSession()`（决策路径 + 会话终止经 `SyncCoordinator.onSessionLocked`）+ `autoMergeAndUpload` 的成功 / 上传失败两路。**结论更正**：契约原「`clearPendingConflictSession()` 不是合法擦除点」的前提是裸擦；`lastSyncedDb` 赋值点现查 8 处（非 5 处）。新增 `SyncPendingTreeErasureTest` **7 例** + **三次反向反校**（裸擦 3 红 / 去擦除 1 红，重构后复检仍红）。`test` **343/2402** 全绿（+1 类/+7 例）、lint **229（0 errors）**、三项分档持平（施工中 `SyncConflictController.kt` 曾涨至 405 行越 tier2 档沿 ⇒ 编排外移同包辅助文件回落 390 行，留痕见批次文档）。**如实声明**：自动合并成功路径擦除**效果**不可外部观测（用例只锁「不误擦已采用实例」）；未触原生 / `androidTest` ⇒ 无设备侧必跑项（契约原记「须真机」经复核前提不成立，已更正）；池内 `binaries` 仍属限界 §1.6 零改动） | `ISSUE-P3-235`（闭环；**P3 区归零**） | [`238-待决冲突解析树身份判定擦除批次.md`](batches/238-待决冲突解析树身份判定擦除批次.md) |

| §239 | 运行环境完整性检测开关批次（`ISSUE-P3-236` **同轮登记并整条结案**，产品口径登记 `PD-15`）：该检测自 `ISSUE-P2-08` 起**恒开且零配置入口**（无偏好键 / 无 `BuildConfig` / 无 UI），对自有已 Root（含 Magisk）设备等于把指纹快速解锁永久锁死且用户看不到原因、无法选择。整改＝①偏好层 `UserSettings.integrityCheckEnabled = false`（键 `integrity_check_enabled`，读回缺省同 `false`）②裁决层 `RuntimeIntegrityPolicy.evaluate(signals, enforcementEnabled = true)` **拆出 `riskLevelOf()` / `enforcingEnforcement()` 两个私有纯函数逐字承接原 `when`**，关闭态**只解除降级、不改变判定**——`level` / `signals` 仍为真值（Root 命中照样报 `COMPROMISED`，**谎报安全比不检测更坏**）、`enforcement` 全放行且 `biometricBlockReasons` 空清单、无障碍提示正交保留；`escalateForLiveSignals` 同步增参③门控层 `RuntimeIntegrityDetector` 以 `@Volatile enforcementEnabled`（初值 = 出厂默认）镜像开关、订阅设置流且变更即重扫，**指纹（`currentEnforcement`）与自动填充 / CM（`awaitEnforcement`）两个入口各自短路放行，刻意连「未判定 / 快照陈旧」的 fail-closed 保守分支一并绕过**；**关闭只解除阻断不停探测**（30 s 重扫照常，重新开启时快照已是真值）④设置页生物识别卡片新增开关行 + 关闭态常驻代价说明（zh/en 各 +3 键）。新增 `RuntimeIntegrityCheckSwitchTest` **13 例**（内核行为 7 + 门控行为 1 + 接线守卫 5：**两入口短路各 1 处、正则计数 == 2**，少一处即「指纹能用、自动填充仍被拦」的半接态；出厂默认三处一致为 `false`）。`test` **344/2415** 全绿（§238 基线 343/2402 ⇒ +1 类 / +13 例）、`tier1=2 / tier2=29 / functions_ge_100=3` 持平（**施工中探测器曾涨到 505 行越入 tier1**，压缩本批新增 KDoc 回落 490 行——该文件本属 tier2 既有债务，未在本批拆分）。**如实声明**：**无设备 ⇒ 开关最终效果未真机核对**（关闭后真机弹出可解封 / 开启后立即恢复拦截 / 两通道实际下发，三条均未实证）；未触原生与 `androidTest` ⇒ 无设备侧必跑项，但不豁免上述手工冒烟；默认口径经用户明示裁决反转（`PD-15`），`ISSUE-P2-08` / `P3-83` / `P3-120` 探测能力与判据**一行未删**，开启即完整恢复；限界 §3.5 口径不变并就地补注 | `ISSUE-P3-236`（同轮登记并闭环；`PD-15`） | [`239-运行环境完整性检测开关批次.md`](batches/239-运行环境完整性检测开关批次.md) |

| §240 | 凭据保存链路失效真机定位与修复批次（`ISSUE-P1-237` **整条结案**；同轮新登记未闭环的 `ISSUE-P1-238` / `ISSUE-P2-239`）：用户报告「点了保存/继续但信息保存不进软件」。以**测试 APK 内独立包名的真实客户端**（新增 `CredentialSaveClientActivity`，语义等价第三方应用调 `CredentialManager.createCredential`）在真机（Redmi 4X / Android 17 / API 37）复现，得三条**互相独立**的根因——**R1（环境/系统侧，主因）**：`Settings.Secure.credential_service`（已启用提供者清单）为空、`credential_service_primary` 指向**不存在的包** `com.keepasskey.app`（= 应用命名空间，实际包名是 `com.keepasskey`）⇒ 系统在框架层直接以 `TYPE_NO_CREATE_OPTIONS` 丢弃创建请求（四条归因实验 E1~E4 逐条坐实「`credential_service` 是路由的必要条件、`primary` 不影响路由、全新安装仅写 primary 不放行」，并给出已验证的处置命令）；**R2（应用侧）**：冷启动时延逼近系统约 3.0 s 应答预算（同机实测 2.38 / 2.49 / **3.05** s，其中 3.05 s 那轮实测 `Remote provider response timed out`）；**R3（应用侧）**：`res/drawable/ic_launcher.xml` 是非法 `<bitmap android:src="@mipmap/ic_launcher">`（`src` 指向 adaptive-icon ⇒ **任何消费方都加载不了**，系统凭据面板 `toSortedCreationOptionInfoList` 每次抛 `XmlPullParserException: <bitmap> requires a valid 'src' attribute`，由 `a1c1dce` 图标重构引入）。整改＝①图标改 `<layer-list>`（背景层 + 前景层两个 PNG，保持资源名与 5 处调用点不变）②`periodicSyncScheduler.applySavedSchedule()`（会拉起 WorkManager，冷启动主线程最重的一段、且无任何冷启动时序依赖）移入新增 `coldStartBackgroundScope`，其余安全对账类冷启动工作一律保留同步位置；另新增设备侧用例 `CredentialSaveChainDeviceTest`。真机验证：处置 R1 + 整改 R2/R3 后，系统面板呈现 KeePasskey 创建项 → 点「继续」→ `PasswordSaveActivity` 被拉起（`BAL_ALLOW_VISIBLE_WINDOW`），且 R3 修复后 `E/Icon` 计数由「每次非零」降为 **0**；`test` **344/2415** 全绿（计数与 §239 同值，本批未增删 JVM 用例）。**如实声明**：**未定位「谁写坏了 `credential_service`」**（两次针对性实验均未观察到写入）⇒ 「命名空间与 `applicationId` 不一致」这一假设**既未成立亦未证伪**，不作结论；未点击面板的其它分支；通行密钥注册方向未单独真机复算（与密码共用同一入口与同一图标资源）；冷启动时延为**点测非统计**，不得据此宣称百分比收益；新用例在本机恒为 skipped（全新安装后系统不自动登记本应用，属环境前提而非用例缺陷） | `ISSUE-P1-237`（闭环） | [`240-凭据保存链路失效真机定位与修复批次.md`](batches/240-凭据保存链路失效真机定位与修复批次.md) |

| §241 | 通行密钥保存方向真机复测批次（`ISSUE-P1-237` 的**补充验证**；证据并入未闭环的 `ISSUE-P1-238`）：把 §240 的设备侧真实客户端扩展到公钥创建请求（新增 `passkey` / `passkey-malformed` 两模式）。**方法学发现（可复用判据）**：`BeginCreateCredentialRequest$Companion.createFrom` 的异常表显示，`androidx` 对「类型为公钥、但载荷缺 `BUNDLE_KEY_REQUEST_JSON`」的请求会以 `FrameworkClassParsingException` **兜底成 `BeginCreateCustomCredentialRequest`**（类型字符串仍是公钥类型）⇒ 落进 provider 的 `else` 分支、空响应。**故「日志里看到 Custom 请求」≠「应用不支持通行密钥」，须先核对载荷形状**；本探针首版即踩中该形态并一度误判，经 `javap -c` 核对 AAR 字节码改正（两份 Bundle 与 `toCredentialDataBundle$` / `toCandidateDataBundle$` 逐键对齐），误判如实留痕。**真机结论**：冷启动轮 `session created 06:51:35.407 → 回调 06:51:37.864（2.46 s）→ CANCELED 06:51:38.409（2.99 s = 系统预算）→ TYPE_NO_CREATE_OPTIONS` ⇒ 与密码方向**同源**（非通行密钥专属缺陷）；热态轮回调 **21 ms** → `SAVE_ENTRIES_RECEIVED` ⇒ provider 侧创建入口**正常**；面板如实呈现「要创建通行密钥以便登录…吗？ / cm-probe-user • 通行密钥 / 继续」，点「继续」后 `PasskeyCreateActivity` 被拉起 ⇒ **`FLAG_MUTABLE` 填充式握手在通行密钥方向同样成立**（否则会走 `MISSING_REQUEST` 拒绝页），落地页最终停在解锁/建库引导（本机从未建库，属环境前提）。**应用侧整改（本轮唯一代码改动）**：`else -> null` 分支由**完全静默**改为留痕 `AppLog.i(…, request.javaClass.simpleName)`（只记 androidx 三种固定类名，**不记调用方可控的类型字符串**；响应语义不变 = fail-closed）——真机排查时正因该处零日志而误判。真机验证：`passkey-malformed` 复现形态后新留痕生效、规范载荷无回归；`test` **344/2415** 全绿（与 §240 同值）。**如实声明**：未完成注册闭环（需已解锁密码库）；`PasskeyRegistrationGate` 的 DAL 门控未实测（既有设计，本轮未改）；面板其它分支未点；冷启动为点测非统计 | `ISSUE-P1-237`（补充验证） | [`241-通行密钥保存方向真机复测批次.md`](batches/241-通行密钥保存方向真机复测批次.md) |

| §242 | 凭据链路未验证项逐条收口批次（§241 §5 四项逐条闭环；`ISSUE-P1-237` **最终收口**；`ISSUE-P1-238` 补统计证据**仍未闭环**；同轮新登记 `ISSUE-P2-240`）：先补齐真机环境（应用内**新建并解锁** `passwords.kdbx`，主密码为虚构测试值、不入文档）。**① 注册闭环四段全链路**：provider 产出创建项（库已解锁时回调仅 **11 ms**）→ 点「继续」拉起 `PasskeyCreateActivity` → 门控放行（`用户已显式开启「跳过 DAL 校验」…`）→ 用户验证页**如实标注 UV=0**（本机无生物识别/锁屏凭据）→ 点「允许」后调用方收到 `onResult`、Activity 正常收尾 → **落库物证**：库列表出现标题 `cm-probe-user@example.com` + `Passkey` 角标 + 绑定 `android://com.keepasskey.test`。**② DAL 两侧**：拒绝侧真机走**真实网络**（`DAL 响应非 2xx，视为无声明` → `result=NOT_VERIFIED` → `注册门禁拒绝（DAL_UNVERIFIED）` → 用户可见拒绝页「无法确认调用应用对本站点的归属声明…」）；放行侧经用户显式降级开关实证；**正向 `VERIFIED` 真机不可达**（须自控且 TLS 受信、能发布 assetlinks 的域名；本应用还刻意拒绝自签信任锚），该路径由宿主 `DigitalAssetLinksVerifierTest` 12 例覆盖（含`包名与指纹匹配_校验通过`）。**③ 面板分支**：取消 → `TYPE_USER_CANCELED`；「更换提供方」对面板**全部文本节点穷举后确认不存在**该入口（本应用既是首选又是唯一已启用提供方）。**④ 冷启动 10 轮统计**：时延 2.351 / 2.354 / 2.354 / 2.357 / 2.361 / 2.366 / 2.371 / 2.393 / 2.568 s（最小 2.351、最大 2.568、中位 2.359、均值 2.383），**超时 0/10**、10/10 均 `SAVE_ENTRIES_RECEIVED`；对照 §240 修前 2.49 s 与 **3.05 s 超时** ⇒ 分布收敛到约 3.0 s 预算内、余量 0.4~0.65 s。**同轮新发现**：设置页开关文案「跳过浏览器兼容层／不通过浏览器兼容适配直接填充原生表单」实际接线 `skipDalVerification`（**Passkey 注册的 DAL 校验**，真机点击后落 `skip_dal_verification=true` 且注册日志留痕）⇒ 用户看不到 `DigitalAssetLinksVerifier` KDoc 所称的「跳过 DAL 校验」入口，登记 `ISSUE-P2-240`。代码改动仅一处：§241 留痕 KDoc 的误写编号 `ISSUE-P1-240` → `§241`。`test` **344/2415** 全绿。**如实声明**：`ISSUE-P1-238` 未闭环（AC① 要求 10 轮 ≤1.5 s，实测 2.35~2.57 s）；DAL 正向真机不可达；未跑 KPEX 外部对拍 `verify_interop.py`（与本批四项无关，不对其作任何声称）；多提供方并存的面板呈现未覆盖） | `ISSUE-P1-237`（最终收口）/ `ISSUE-P2-240`（新登记） | [`242-凭据链路未验证项逐条收口批次.md`](batches/242-凭据链路未验证项逐条收口批次.md) |
| §243 | 设置页 DAL 降级开关文案更正批次（`ISSUE-P2-240` **整条结案**）：开关文案「跳过浏览器兼容层／不通过浏览器兼容适配直接填充原生表单」实际接线 `skipDalVerification`（**通行密钥注册**侧的 DAL 归属声明校验降级）⇒ 用户看不到 KDoc 所称的降级入口（§232 `ISSUE-P2-228` 同类「措辞指向另一件事」）。整改＝① zh / en 两条资源按真实语义改写（标题「跳过通行密钥站点归属校验」，副标题写明降级后果，默认关闭）②该字面量在 `DigitalAssetLinksVerifier` KDoc / `ExtendedSettings` 字段注释 / `PasskeyCreateActivity` 注释与告警日志**四处逐字同锚**③新增 `DalSkipSwitchCopyWiringTest` **7 例**锁定「文案资源 ↔ 偏好字段 ↔ 消费方」（资源取值现解析、四段接线、四处同锚、防空转下界、无并列 `browserCompat` 偏好项）④AC② 裁决**不新增**独立「跳过浏览器兼容层」偏好项（无实现、无消费方 ⇒ 假开关）落 `PD-16`，并更正 `PD-12` 的陈旧定位。**零判定改动**；`test` **345/2422**（+1 类 / +7 例）、`skipped=13` 全绿，`:app:compileDebugScreenshotTestKotlin --rerun` 通过，`tier1=2 / tier2=29 / functions_ge_100=3` 持平（施工中 `SettingsUiState.kt` 曾 398→400 越档，压回单行注释）。**真机实证（补做）**：`android install` 装入 debug 包后走真机用户路径（解锁 → 设置 → 自动填充与 Passkey → 密码捕获卡），`android layout` 逐字读出该行新标题 `跳过通行密钥站点归属校验` 与新副标题（旧文案已不可见）；点击两次实证落盘 `skip_dal_verification` `true → false → true` 并已还原设备原状态；输入注入（点击 / 滑动 / 文本）Android CLI 无对应命令，按 CLI 官方 `interact.md` 以 `adb shell input` 为退路，界面取证仍走 CLI。**补做**：冷启动后文案与开关持久化仍成立；复用 §240/§241 探针走通行密钥注册链路，真机 `logcat` 实测降级留痕为新措辞 `用户已显式开启「跳过通行密钥站点归属校验」…`（旧措辞零命中），随后取消未落库。**同轮新发现并登记** `ISSUE-P1-241`：库管理「删除」弹窗承诺「不会删除物理文件」，实测应用私有库文件被真删（清理本批测试库时暴露）。**未覆盖**：面板其它分支 / DAL 正向真机不可达 / 注册完成落库；未触原生面 / 无 `androidTest` 增改 ⇒ 无设备侧必跑项） | `ISSUE-P2-240`（整条结案；`PD-16`） | [`243-设置页DAL降级开关文案更正批次.md`](batches/243-设置页DAL降级开关文案更正批次.md) |

| §244 | release 生产包冷启动验证与 `ISSUE-P1-238` 闭环批次（`ISSUE-P1-238` **整条结案**；同批完成《passkeys.io 保存通行密钥失败排查与整改交接》文档的结论分流与退役）：`assembleRelease`（R8 + 资源收缩）装真机（dexopt `[status=verify] [reason=install]`，安装期产 odex，与 debug `run-from-apk` 对照）；卸载清空系统登记经「首选服务」选择器恢复（与 `ISSUE-P2-239` 结论一致）；UI 驱动重建测试库并解锁；复用 §240/§241 独立包名 CM 探针（`MODE=passkey`，未改一行）10 轮冷启动应答实测（每轮 `force-stop` → `logcat -c` → `am start -f 0x10008000`，**CLEAR_TASK 必需**否则 deliver-to-top 不重建、轮次无效——首轮踩坑留痕）。**结果 10/10 达标**：`Start proc` → `onBeginCreateCredentialRequest` 最小 **185** / 最大 **247** / 中位 **213** ms（AC① ≤1.5 s 余量约 6×），`SAVE_ENTRIES_RECEIVED` 240~317 ms，**超时 0/10**（AC②）；AC③④⑤ 全部满足；release 实测**反证**「剩余差距属 debug 构建类型 × 无 AOT 平台属性」。**环境边界承接**：浏览器端到端（passkeys.io）无 GMS 环境不可达（Firefox 140 回退 FIDO2 私有 API / Via-WebView 未接 CredMan），分流为环境边界声明、不作开放项悬挂。**退役**：交接文档结论分流至 `ISSUE-P1-238` 条目与本批 §4，`docs/README.md` 登记行移除、文件删除（git `6e051c1` 可回溯）。`test` **347/2433** 全绿（本批零代码改动，与 §243 后基线一致）；`BROKEN_MD_LINKS=0`、`RESOLVED_INDEX_SYNC=OK`。**如实声明**：AC①② 以 **release 构建类型**达成，debug 无 odex 平台开销依旧（已登记）；未跑 KPEX 对拍；测试探针在 release 上 `enabledProbeThrew=IllegalArgumentException`（探针硬编码类名 + release 环境；服务类名经 `dumpsys` 核实保留原名、路由正常，测试专用代码问题非产品缺陷）；设备现装 release 包 + 虚构口令测试库 | `ISSUE-P1-238`（整条结案） | [`244-release生产包冷启动验证与P1-238闭环批次.md`](batches/244-release生产包冷启动验证与P1-238闭环批次.md) |

| §245 | 条目行 Passkey 徽标防折行与标题自适应让位批次（`ISSUE-P2-242` **整条结案**）：用户真机实拍物证（Redmi 4X / Android 17）确证 `cm-probe-user@example.com`（中长标题）右侧 `PasskeyBadge` 文本被挤压发生软换行（softWrap）竖排成一串字符（`P-a-s-s-k-e-y` 逐字折行），而 46 字符超长标题项 `fake-passkey-probe@example.com@www.passkeys.io` 则将 `PasskeyBadge` 完全挤出屏幕导致视觉消失。根因＝`StandardEntryLayout`（`VaultEntryRowLayouts.kt:166`）标题 `Text` 处在 `Row` 内但**缺少 `Modifier.weight(1f, fill = false)`** 约束（对比第 199 行用户名处即正确配置），非加权 Text 吃满行宽；且 `PasskeyBadge`（`SecurityBadge.kt:54`）内部 `Text` 缺少 `maxLines = 1, softWrap = false`。整改＝①`PasskeyBadge` 锁定单行不折行（`maxLines = 1, softWrap = false`）②`StandardEntryLayout` 标题 Text 增加 `Modifier.weight(1f, fill = false)` 自适应让位并在长标题尾部加省略号 ③`EntryDetailComponents.kt` 详情页 Hero 标题行同步增加该约束 ④新增 `PasskeyBadgeLayoutWiringTest` 3 例锁定布局契约。验证：release 包真机覆盖安装实测两项条目无障碍控件树，两者的 `Passkey` 徽标文本 bounds 均为 `[484,...][562,...]`（宽 78px，高 24px，标准正常横向水平胶囊），长标题正确截断让位，竖排畸变与消失彻底消除；全仓 `test` 全绿 `xml=348 tests=2436 failures=0 errors=0 skipped=13`（+1 类 / +3 例）；`tier1=2 / tier2=29 / functions_ge_100=3` 持平；`BROKEN_MD_LINKS=0`、`RESOLVED_INDEX_SYNC=OK`。**如实声明**：本批仅涉及 UI 排版与自适应权重约束，不触动任何数据模型、加密密钥或存储序列化逻辑；真机开启 FLAG_SECURE 截图全黑，验证完全基于 Android CLI 无障碍树坐标几何数据） | `ISSUE-P2-242`（整条结案） | [`245-条目行Passkey徽标防折行与标题自适应让位批次.md`](batches/245-条目行Passkey徽标防折行与标题自适应让位批次.md) |
| §246 | 移除密码库确认文案与真实行为一致批次（`ISSUE-P1-241` **整条结案**，另立产品口径 `PD-17`）：§243 §7 真机收尾时发现「移除密码库关联」弹窗承诺「不会删除物理文件」，而应用私有库（建库向导默认落点）的 `id` 恰是 `filesDir` 内的文件名 ⇒ `VaultLifecycleCoordinator.removeDatabase` 的 `File(filesDir, id).delete()` **真的把它永久删掉**（无回收站、无差异确认）——文案与行为相反，用户按文案操作即丢库。**行为与文案同时收口**：① 文案按存储类型分列两套（私有库「永久删除应用私有目录中的此文件？」+ 正文指名被删文件 + 「无法恢复」，按钮「永久删除」；外部库维持「移除密码库关联？」+「不会删除物理文件」+「仅移除关联」），原一对共用键 `db_picker_delete_confirm_*` 拆为 `db_picker_remove_private_*` / `_external_*` 各三条（zh/en 同步）② 判据单点化：新增纯函数 `VaultRemovalKind.of(条目 path, filesDir)`（无 I/O，「文件父目录就是 filesDir」⇒ 私有，其余含 `content://` / 应用外绝对路径 / 用户填的裸相对名 / 远端 URL 一律外部；判不出 ⇒ 外部＝不删，方向取安全侧）+ `VaultRemovalConfirmation.of(...)` 投影出标题/正文/按钮/`destructive`；界面只调这一个函数并把 `confirmation.kind` **原样**下行 ③ 仓库接口 `removeDatabase(id, kind)` 增参，数据层**只在** `kind == PRIVATE_FILE` 时删文件（此前按 `id` 形状反推），并把注册表匹配由 `id||path||name` 收紧为 `id||path`（外部登记的 `name` 只是展示名，撞车时会摘掉**另一个库**的登记——同函数内相邻缺陷一并修正）④ 确认弹窗下沉 `VaultRemovalConfirmDialog.kt`（施工中 `DatabasePickerScreen.kt` 曾 394→419 越入 tier2，搬移后回落 393 行）。**AC③ 裁决**：维持「移除即删除」并照实声明（私有库由目录扫描发现，「只摘关联」不存在——不删文件则刷新即复活，改方案只能新增「已忽略库」持久化 + 默认动作静默无效），落 `PD-17`。**验证**：新增 `VaultRemovalPresentationTest` 9 例（类型穷举 + 兜底方向 + 两套措辞互不相同 + zh/en 取值如实）+ `VaultRemovalWiringTest` 4 例（文案与下行判据同源、删除位于 `kind == PRIVATE_FILE` 守卫之后、不得按 `name` 匹配、旧共用键不得复活）；**反向反校**（删除守卫改 `!=` ⇒ 1 红；外部库也置 `destructive` ⇒ 2 红）；全量 `test` **352/2461 全绿**、`tier1=2/tier2=29`、`functions_ge_100=3` 持平。**真机（Redmi 4X / API 37）**：私有库弹窗逐字指名 `e2e-p1-241.kdbx` ⇒ 确认后 `run-as ls -la files/` 该文件**消失**（AC⑤）；外部库（系统文件选择器登记 `test.kdbx`）弹窗明示「不会删除物理文件」⇒ 确认后 `/sdcard/Download/test.kdbx` 3029 B **原样在**、沙盒文件一个未动；两测试库用毕即移除、设备回到操作前状态。**如实声明**：`<库>.kdbx.bak`（原子写滚动备份）不随删除移除且应用内无入口读回，界面**不**把它写成恢复承诺；红底按钮的**像素颜色**未取证（窗口 `FLAG_SECURE`，`screen capture` 全黑，同 §245）；取证曾在真机临时置 `isDebuggable`（同签名 `-r` 覆盖安装，数据未丢），已还原并重装正式 release 包 | `ISSUE-P1-241`（整条结案；`PD-17`） | [`246-移除密码库确认文案与真实行为一致批次.md`](batches/246-移除密码库确认文案与真实行为一致批次.md) |
| §247 | 凭据提供者通道健康检查批次（`ISSUE-P2-239` **整条结案**）：CM 通道「系统未登记本应用」的失效**完全静默**——`credential_service` 为空时框架层直接丢弃创建请求（§240 E3 实测 `TYPE_NO_CREATE_OPTIONS`），用户只看到「点保存没反应」；自动填充通道早有 `AutofillHealthProbe` + 健康卡，CM 通道既无自检也无引导。整改＝① 判定三态化 `passkey/CredentialProviderRegistration`（`REGISTERED` / `NOT_REGISTERED` / `UNKNOWN`）**纯函数**，读取结果以枚举 `CredentialProviderEnabledState`（`ENABLED`/`DISABLED`/`UNREADABLE`）表达——**不用 `Boolean?`**（调用方极易顺手 `?: true` ＝谎报正常）② 平台查询单点化 `CredentialProviderHealthProbe`（`@Singleton`），组件名由 `ComponentName(context, KeePasskeyCredentialProviderService::class.java)` **推导**（`applicationId` 与源码命名空间不一致，硬编码即判错），异常一律收敛 `UNREADABLE` 并落 `AppLog.w` ③ `AutofillHealthReport` 增 `credentialProviderRegistration`（**无默认值**：漏传即编译失败，堵死「静默渲染成正常」）+ 两个异常项；`isLegacyAutofillOperational` **不含** CM 登记态（CM 未登记不影响传统自动填充，计入即给错误指引）④ 健康卡两项各配文案 + 一键入口 `Settings.ACTION_CREDENTIAL_PROVIDER`（API 37 公开常量），不可解析即返回 `null` 只留纯文案 ⑤ zh/en 各 +2 键，禁用词机检「已从系统移除」（组件注册由 Manifest 决定，应用内无法动态摘除）。**一次被现场推翻的初版设计（留痕）**：初版读 `Settings.Secure.credential_service`（§240 证明的路由权威），真机该读抛 **`SecurityException`**（该键对普通应用不可读）⇒ 卡面如实出现「状态未知」（AC② 行为正确）但判定永远给不出结论；改用公开 API `android.credentials.CredentialManager.isEnabledCredentialProviderService`（API 34+，`Context.CREDENTIAL_SERVICE` 为公开常量、无需权限）并取得三条等价证据：调用抵达框架（系统侧日志）／状态 B 下 API=false 且请求被 `TYPE_NO_CREATE_OPTIONS` 丢弃／状态 A 下 API=true 且请求正常路由（`SAVE_ENTRIES_RECEIVED`）。**真机两态读数**（`android layout` 逐字）：已登记 ⇒ 卡面无 CM 异常项；删除该键 ⇒ `• 系统未把本应用登记为凭据提供者，保存密码 / 通行密钥的请求不会被发到本应用（点「保存」会没有反应）；请在系统设置的「凭据提供程序」中把 KeePasskey 设为当前服务`；复原后该项消失。**同轮实测并登记限界 §27**：本机 ROM 无 `android.settings.CREDENTIAL_PROVIDER` 与 `REQUEST_SET_AUTOFILL_SERVICE` 的 activity 命中（shell 视角亦无，对照组无障碍 action 有）⇒ 一键入口如实降级为纯文案。**验证**：新增 `CredentialProviderRegistrationTest` 4 例（输入三态穷举 + AC② 两向反例 + 一一对应反校）、`CredentialProviderHealthWiringTest` 5 例（查询单点 + 全仓不得再读内部键 + 报告消费 + 文案/入口 + 禁用词）、`AutofillHealthPolicyTest` +3 例；**反向反校**（`UNREADABLE→REGISTERED` 与 `UNKNOWN` 分支改 `Unit` 合并注入 ⇒ 3 红）；全量 `test` **352/2461 全绿**、`tier1=2/tier2=29`、`functions_ge_100=3` 持平、`lint` 计数 229 持平（**如实留痕**：lint 须带 `-x :crypto:cargoNdkBuild`，否则 `LintDriver` 崩溃，§236 起即为规范命令）。**如实声明**：「公开 API ⇄ 路由权威等价」仅在本机取得三条证据，未在第二台设备复测；`UNKNOWN` 态的端到端呈现未在最终方案下重现（由 4 例锁定映射）；英文文案只在宿主解析断言下验证；`KeePasskeyCredentialProviderService` 判定/放行语义一行未改；未触原生面与 `androidTest` ⇒ 无设备侧必跑项 | `ISSUE-P2-239`（整条结案；限界 §27） | [`247-凭据提供者通道健康检查批次.md`](batches/247-凭据提供者通道健康检查批次.md) |
| §248 | 限界表独立复核更正与新缺口补登批次（对 `已知工程限界.md` 全文的独立复核：25 处事实性更正 + 8 条新登记条目；**仅文档、无生产代码改动**） | 新登记 `ISSUE-P2-243` / `244` / `245` 与 `ISSUE-P3-246` / `247` / `248` / `249` / `250` | [`248-限界表独立复核更正与新缺口补登批次.md`](batches/248-限界表独立复核更正与新缺口补登批次.md) |

| §249 | S3 条件写缺口与子库附件落盘及两处限界重裁批次（§248 复核补登项的首批整改，`ISSUE-P2-243` / `P2-244` / `P3-248` / `P3-250` **四条整条闭环**）：① `S3SyncProvider.upload` 的「HEAD 成功但未返回 ETag」分支原把空白 `precheckEtag` 放行（该次 PUT 一个条件头都不发＝无条件覆盖），现改为 **fail-closed**（抛 `ProtocolError(200,…)`、不发 PUT），并移除 `takeIf { isNotBlank }` 这一退回无条件 PUT 的后门；新增用例锁定，**反向反校**读数 `expected:<1> but was:<2>`（确证守卫移除后 PUT 真的发出）。② `ChildReadOnlySession` 装载改传应用级 `FileBinaryStore`（同单例、不新增清理路径、投影仍不带附件字段），管理器两处构造点同步；新增含 1 200 000 B 附件的端到端用例 + `RecordingBinaryStore` 替身，**判别力**：改回三参即红。③ `DatabasePickerViewModel` 的弱因子提示 KDoc 改为如实表述（「没有落点承接、该路径当前没有任何弱因子提示」），新增静态守卫 `WeakKdfNoticeHonestyGuardTest`。④ 限界 §18 **收窄授权**（不得新增含实现体成员 + 5 处薄编排待消化清单）/ §20 按 65 行现值**重新裁定为接受为限界**；代码侧仅 `SettingsViewModel.clearBlockedFields()` 收为单语句委托。文档同步：限界 §1.1 / §1.3 / §8 / §18 / §20。**如实声明**：无设备 ⇒ 未跑 connected 层；未触 `androidTest` 与原生面 | `ISSUE-P2-243` / `P2-244` / `P3-248` / `P3-250`（闭环） | [`249-S3条件写缺口与子库附件落盘及两处限界重裁批次.md`](batches/249-S3条件写缺口与子库附件落盘及两处限界重裁批次.md) |
| §250 | 复核报告四行与代码现况对齐批次（`ISSUE-P3-249` **整条闭环**；**纯文档、无代码改动**）：`docs/security/SECURITY_RECHECK_2026-09.md` 四处陈旧文字按「**保留原文 + 就地加更正注**」（均带 `2026-09-21` / `ISSUE-P3-249` 标记）同步现况——① `:232` 的 `P2-65` 判定依据「全仓无锁态驱动的 UI 导航」更正为「**锁态驱动的 UI 导航确实存在**」（`KeePasskeyApp.kt:287-295` 的 `lockEvents` → `Unlock` + `popUpTo(0)`；限界 §13 亦以此为据），该条终评 / 终态 / 可利 / 修**一律不变**；② `:799`（Fix 陷阱表项 10）`P2-73` AC② 改 `FLAG_MUTABLE`「不可照做」的前提已随 §110 / §111 修复失效（现行实现即 `FLAG_MUTABLE` + 经 `EXTRA_AUTHENTICATION_RESULT` 回传完整 `Dataset`，真机已验证）⇒ 指针给限界 §4.1 与 §110 / §111 批次；③ `:609-610` 的「CM 通道 `UNBOUND` 越权面未进限界登记表」残余已不成立（限界 **§6** 已登记，实为 **§106** 批次补登；`UNBOUND` 仍为明示边界），同句 `P2-54` 残余不受影响；④ `:850` 的 `SUPPLY-06`「本轮未直读」已由 **§236** 直读定案（IPv4 可过 PSL 下限、IPv6 被拒；SSRF 仅由连接期层 `SsrfGuardSocketFactory` 覆盖）⇒ 结论落限界 **§25**。文档同步：`ACTIVE_ISSUES`（P3 3→2 + 闭环摘要）、`RESOLVED_LOG`、`resolved/README.md`（最大 §250 / 下一批 §251）、本册。验证：`check_recheck_consistency` **PASS**（1326 行 / 11 条禁用短语）、`BROKEN_MD_LINKS=0`、`RESOLVED_INDEX_SYNC=OK`；`.\gradlew.bat test` **UP-TO-DATE** ⇒ **未产生新的测试执行**。**如实声明**：无设备；四处「代码现况」依据均引自 §236 ~ §249 既有直读 / 用例结论，本批**未重跑任何用例、未新增证据**；未触生产 / 测试代码、`参考项目/`、`*/src/androidTest/**` | `ISSUE-P3-249`（整条闭环） | [`250-复核报告四行与代码现况对齐批次.md`](batches/250-复核报告四行与代码现况对齐批次.md) |
| §251 | 设备侧两处测试口径收窄批次（`ISSUE-P3-246` / `P3-247` **两条整条闭环**；**纯测试资产批次**，只动 `sync/src/androidTest/**`）：① `SyncCacheAndroidRuntimeTest` 的「清理后不得残留任何文件」断言由**原始目录枚举返回值** `(dir.listFiles() ?: emptyArray()).isEmpty()` 改为限界 §7 的**实体口径**（`walkTopDown().filter { it.isFile }` 对 `emptyList<File>()`，与 `SyncCacheTest.kt:136-137` / `app` 侧 `SyncCacheEvictorTest.kt:127-128` 同款写法），**判别力未放宽**，失败信息同时打印索引条目与实体残留两份读数。**规格一处与实际不符**：所称参照文件 `sync/src/test/.../SyncCacheEvictorTest.kt` **不存在**（如实登记，按上述两处既有写法落地）。② `sync/src/androidTest/AndroidManifest.xml` 删除 `android:usesCleartextTraffic="true"`（**整进程**放行明文），改用该测试 APK **专用**的 `res/xml/network_security_config.xml`（**新建**）：`base-config cleartextTrafficPermitted="false"` + 仅系统 CA 信任锚，唯一 `domain-config` **只放行 2 个域** `localhost` 与 `127.0.0.1`（逐份勘察该源集 6 个用例：`WebDavTransferDeviceTest` 用 `localhost`、`S3TransferDeviceTest` 用 `127.0.0.1`、`SsrfRedirectBypassDeviceTest` 明文例同时用两者，**无**假主机名 + 回环 `Dns` 形态 ⇒ 两域恰好完备）；生产 `app/src/main/res/xml/network_security_config.xml` 与 `app/src/androidTest/AndroidManifest.xml` **一行未触**。**真机实证（Redmi 4X / Android 17 · API 37，2026-09-21）**：`:sync:connectedDebugAndroidTest` **24 例 / 0 失败 / 0 error / 0 skipped**；`:app:connectedDebugAndroidTest` **71 例 / 0 失败 / 0 error / 1 skipped**（skipped = `CredentialSaveChainDeviceTest` 的系统登记前提 `Assume`；`CleartextPolicyDeviceTest` 4 例全绿 ⇒ AC③ 生产禁令未被测试侧改动影响）；同轮旁证 `:database:` 17 / `:crypto:` 37 / `:core:` 4 均 0 失败。宿主侧另把「资源 → 合并清单 → APK」六级 `aapt2` 复核到 APK 级（APK 内 config 回读恰为 2 域豁免）。**三处工程留痕（非缺陷）**：`:app:` 首轮 `INSTALL_FAILED_UPDATE_INCOMPATIBLE`（陈旧安装会话，三处核实均无本应用痕迹）⇒ `adb install -r -t` 后重跑通过；UiAutomation 槽位被 `com.android.cli.interact.instrumentation` 占用致 `Cannot call disconnect() while connecting UiAutomation` ⇒ 按限界 §4.1 约束 1 既有口径 `am force-stop` 后通过（**已登记约束的现场复现**）；`:core:` 一度卡 Windows 文件锁（UTP logcat 句柄）⇒ `gradlew --stop` + 清理后通过（与限界 §12 同类）。验证：`check_recheck_consistency` **PASS**、`BROKEN_MD_LINKS=0`、`RESOLVED_INDEX_SYNC=OK`（施工期「[251] 未被分册登记」的中间态红已随归档步转绿）。**如实声明**：未触生产代码与 `app/src/androidTest/**`；**未跑** `.\gradlew.bat test`（`androidTest` 源集不参与宿主 `test` 任务，读数无判别力）；设备读数为上级真机执行后落盘的结果 XML，归档时逐份解析复核一致，三处工程留痕**未复现** | `ISSUE-P3-246` / `ISSUE-P3-247`（两条整条闭环） | [`251-设备侧两处测试口径收窄批次.md`](batches/251-设备侧两处测试口径收窄批次.md) |
| §252 | 对话框窗口遮挡触摸过滤接线批次（`ISSUE-P2-245` **整条闭环**；同轮设备侧实测**揭出并登记** `ISSUE-P2-246`，**未整改**）：`SecureDialogWindowEffect()` 原只施 `FLAG_SECURE`、**无**遮挡触摸过滤，而两者是**同形的窗口级缺口**。整改＝在同一 `DisposableEffect` 内对**对话框窗口的 `decorView`** 叠加 `filterTouchesWhenObscured`（save/restore 语义；取不到窗口时不动任何窗口 = fail-safe 不变；既有 `FLAG_SECURE` 语义与 `SecureDialogFlagPolicy` 两条不变式**一行未改**），`dialogWindowOrNull()` 改 `internal` 供设备用例复用同一口径；KDoc 落「为何用 `decorView`」「过滤覆盖整棵子树的实现依据」「为何**不**用 `setHideOverlayWindows`」「无权限无版本门槛」四项结论。**规格三处与实际不符均按实测落地**：调用点 **7 处**（非 6 处，`CreateVaultWizardDialog.kt:67` 为漏计）；规格所引 API 文档措辞 "the view and its descendants" 在 SDK 源中**不存在**；宿主 Activity 必须落在被测应用进程 ⇒ 新增 `app/src/debug/`（+ debug 清单），首轮声明在 `androidTest` 清单时报 `resolved to different process com.keepasskey.test`。守卫：`ObscuredTouchWiringTest` 新增第 3 条不变式（`SecureDialog.kt` 须同时具备 `FLAG_SECURE` 施加+撤销、过滤的**施加**（仅剩读/还原不算）、对话框窗口解析），既有两条判据未动。**真机**（Redmi 4X / API 37）：新增 `DialogWindowHardeningDeviceTest` **2 例 skipped=0**，全量 `:app:connectedDebugAndroidTest` **73 / 0 失败 / 0 error / 1 skipped**（skipped 系环境前提 `Assume`；§251 的 71 ⇒ +2 逐条可对）；读数：宿主带 flag ⇒ 对话框 `0x1802002`（含）、关闭后 `0x1800002`（已清）；宿主不带 ⇒ `0x1800002` 而 `filterTouchesWhenObscured=true`。**反向反校三条全中**（守卫红 / 设备 2 例全红 / 第 3 条断言 `expected:<0> but was:<8192>`）。宿主全量 `test` **BUILD SUCCESSFUL**、`count_test_results.py` = `xml=353 tests=2466 failures=0 errors=0 skipped=13`（§249 的 353/2465 ⇒ **+1 例**）；三自检 PASS / `BROKEN_MD_LINKS=0` / `RESOLVED_INDEX_SYNC=OK`。限界 §3.3 原「无接线」边界**原文保留作留痕** + 新增「已接线」条与 `ISSUE-P2-246` 残余条。**如实声明**：AC③ 原定第 2 条断言的**归因不可达**——Compose `Dialog` 默认 `SecureFlagPolicy.Inherit` 以**宿主窗口** flag 位为准（源直读），宿主不带时 `setFlags(FLAG_SECURE.inv(), …)` 会**清除**本包装刚施加的 flag（真机对照读数即证）⇒ 该 flag 从来不是由本包装 `addFlags` 保住的；缺口已登记 `ISSUE-P2-246` 并由设备用例第 2 条锁定留证，**本批按上级约束未改 `FLAG_SECURE` 语义**；设备用例证明的是「过滤已真实施加」，**不是**「遮挡触摸确实被丢弃」 | `ISSUE-P2-245`（整条闭环）；新登记 `ISSUE-P2-246` | [`252-对话框窗口遮挡触摸过滤接线批次.md`](batches/252-对话框窗口遮挡触摸过滤接线批次.md) |
| §253 | 限界表瘦身批次（**文档结构批次**，不新增 / 不闭环任何 `ISSUE-*`；只动 `docs/`）：对 [`architecture/已知工程限界.md`](../architecture/已知工程限界.md) 做**证据外迁 + 指针化 + 更正留痕集中**——**① 新建附卷** [`records/限界表证据与实测数据汇录.md`](../records/限界表证据与实测数据汇录.md)（252 行，按 `§x.y` 分节、**逐字搬移**）：§4.1 的设备侧覆盖历史叙事（双设备实跑读数、传输层 / 网络策略子条、`ISSUE-P3-209` 证据链更正原文、自动填充链路全过程、模拟器差异、工具链约束 2 条）、§7 的第二处派生残余与第三处观测点原文、§8 的代码 KDoc 段、§17 的双机 10 轮 AES 表与成本构成（注「与 `records/真机吞吐实测记录_2026-09-17.md` §8.1 / §8.2 同内容」，该记录为权威出处）、§21 的收口效果数字、§22 的两组理由与三条手工核验缺口，以及**原表文末「附」表 40 行的逐字归档**（先核对 §248 批次 §2 只有 25 条更正 + 「补 16 行」汇总句，**不含**逐行对照，故按规则整表搬移）。**② 新增 `## 附二：更正留痕（按日期）`**（34 行 / **21 条**，`§编号 + 日期 + 一句摘要 + 原文`，原文逐字），就地写明收纳判据：只收「原记 X → 实为 Y」且 Y 已在正文出现的沿革型更正；携带当前口径 / 限定 / 判据的更正（§3.5 的 `TracerPid` 限定、§9 的 HOTP 不享周期缓存、§10 的分组六字段未比较、§23 的开关只拦公钥类）留正文。**③ 文末「附」47→9 行摘要**（「原 §6 的 13 条已分别落 §1.1 ~ §4.2，其余为后补」+ 指针）；**④ 文首块 21→13 行**（「由来」4→2 行指向 §248）。**机检（信息零丢失）**：三组令牌集合 **`MISSING_TOTAL=0`**——`§` 编号 125→130、反引号内路径 / 符号 189→198、数字常量 125→128，**只增不减**（交付期曾缺 4 项：两处改为带链接的相对路径、`13.3×` 与 `4条` 因句式与序号字形丢失；已逐项补回复跑至 0）；标题集合 `MISSING_HEADINGS=0`（48→49，新增「附二」）；规范句计数不降（`不得` 88→92 / `刻意不改` 5→5 / `已接受` 11→11 / `刻意未` 3→3 / `刻意不做` 2→2 / `解除条件` 41→40 系 §7 两条同义合并）；`check_verbatim_move.py` 行级 `MISSING_KINDS=844/1060`（原按 ~110 字符软换行、瘦身后一段一行 ⇒ 行边界全变）、段级归一后 `51/105`，逐条核对均为指针替代 / 同级合并 / 措辞收束。**读数**：1235 行 / 47 节 → **461 行 / 47 节 + 附 + 附二**（≤700 达标），字节 125 289 → 116 514（**−7.0%**）——**如实声明**行数下降主要来自**换行收束**，本批未做大规模内容删减，亦未做「取舍型」条目迁移（`产品裁决登记.md` / `AGENTS.md` 一字未动）。文档同步：`docs/README.md`（`records/` 分区登记）、`RESOLVED_LOG`、`resolved/README.md`（最大 **§253** / 下一批 §254）、本册。验证：`BROKEN_MD_LINKS=0`、`RESOLVED_INDEX_SYNC=OK`、`check_recheck_consistency` **PASS**；`\gradlew.bat test --max-workers=1` **UP-TO-DATE** ⇒ **未产生新的测试执行**。**如实声明**：未触生产 / 测试代码、`*/src/androidTest/**`、原生面与 `参考项目/`（无设备侧必跑项）；未重跑任何实测，数字均为文档自述值的搬移 | 无（文档结构批次） | [`253-限界表瘦身批次.md`](batches/253-限界表瘦身批次.md) |
| §254 | 对话框遮罩改由 `SecureFlagPolicy` 强制批次（`ISSUE-P2-246` **整条闭环**；定案＝AC① **方案①**）：§252 设备侧实测揭出 `SecureDialog.kt` KDoc 自陈的「无条件施加 `FLAG_SECURE`（对用户开关的有意 fail-closed 偏离）」**不成立**——该 flag 实际由 Compose 的 `DialogProperties.securePolicy` 决定，默认 `Inherit` 以**调用方（宿主 Activity）窗口**的 flag 位为准，宿主不带时会 `setFlags(FLAG_SECURE.inv(), FLAG_SECURE)` **清除**本包装刚施加的那一次（真机对照：宿主带 ⇒ `0x1802002`；宿主不带 ⇒ `0x1800002`）。整改＝**让实现追上它自己写明的意图**：①4 文件 **7 处**调用点逐处加 `properties = DialogProperties(securePolicy = SecureFlagPolicy.SecureOn)`（`CreateVaultWizardDialog.kt:60/:180`、`MasterKeyChangeDialog.kt:69`、`ChildDatabaseDialogs.kt:84/:179`、`EntryDetailPreviewDiffComponents.kt:72/:232`；该 API 为公开枚举 / 公开参数，**无需 `@OptIn`**）；②`SecureDialog.kt` KDoc 旧自陈改写为如实表述并保留「与用户开关无关」的意图陈述及其实现方式；③`SecureDialogFlagPolicy` 两条不变式与触摸过滤逻辑**一行未改**。守卫：`SecureDialogFlagPolicyTest` 新增「调用点全部显式要求 SecureOn」不变式（按文件计数 2/1/2/2，独立成清单）。设备侧把「锁定缺口」翻转为**锁定修复**（宿主不带 flag 时对话框仍必须带该 flag）+ 新增**同宿主仅改策略参数**的对照例（`Inherit` ⇒ 不含）。**真机**（Redmi 4X / API 37）：全量 `:app:connectedDebugAndroidTest` **74 / 0 失败 / 0 error / 1 skipped**（§252 的 73 ⇒ +1 即对照例）；字段级 `SecureOn+宿主不带 ⇒ 0x1802002`（含）vs `Inherit+同一宿主 ⇒ 0x1800002`（不含）。**反向反校三条全中**（守卫红 / 第 2 例旧口径红 / 第 2 例策略改 Inherit 红）。宿主全量 `test` **BUILD SUCCESSFUL**、`count_test_results.py` = `xml=353 tests=2467 failures=0 errors=0 skipped=13`（§252 的 353/2466 ⇒ +1 例）；`tier1=2 / tier2=29 / functions_ge_100=3` 持平（施工中两个函数曾顶到 100 行越档，已各压为单行注释回落）。**如实声明**：未证明「截屏确实被拦截」（只证明 flag 真实施加并保持）；**产品可见语义变化**＝这三个对话框此后无视用户「防截屏」开关而始终遮罩（依据＝该包装自陈意图，恢复尊重开关须改判）；设备用例由**测试宿主**的 `Dialog` 承载，**不覆盖生产 7 处调用点本身**（由宿主静态守卫覆盖），故「改生产调用点回 Inherit ⇒ 设备用例红」这一注入形态不可达、以探针同一策略参数等价实施 | `ISSUE-P2-246`（整条闭环） | [`254-对话框遮罩改由SecureFlagPolicy强制批次.md`](batches/254-对话框遮罩改由SecureFlagPolicy强制批次.md) |
| §255 | 限界表分类分流起步档批次（**文档结构批次**，不新增 / 不闭环任何 `ISSUE-*`；只动 `docs/`）：按「限界 / 取舍 / 说明 / 宿主现象」四分法，对 [`architecture/已知工程限界.md`](../architecture/已知工程限界.md) 只做**非限界内容的迁出 + 存根化**，**不改任何结论、不碰取舍型条目**。**C 类（说明 / 约定 / 验证现状）9 节**——§1.2 / §2.3 / §3.1 / §3.2 / §3.3 / §3.5 / §4.1 / §4.2 / §4.3——迁往**新建** [`architecture/实现约定与验证现状.md`](../architecture/实现约定与验证现状.md)（183 行 / 15 181 B）；**D 类（宿主开发环境现象）2 节**——§7 / §12——迁往**新建** [`records/宿主开发环境现象.md`](../records/宿主开发环境现象.md)（54 行 / 5 126 B）。**逐字逐行原样搬移（含原折行）**，原表按原编号**存根化**：`### <原编号与标题逐字不变>` + **该节全部规范句逐字保留**（把被软换行拆开的逻辑块先合行再按 `。` 切句，保证句子完整）+ 一行指针「非限界（2026-09-21 §255 迁出）」——**规范句必须留在存根**，因代码 KDoc 按 `§3.3` / `§3.5` 等编号引用（约 20 处）。新文档按原编号组织（`## §1.2 …` / `## §7 …`）并加显式锚点；文首块增补「本表只登记两类内容（客观限界 / 已裁决且不打算整改的工程取舍）」段。**A/B 类共 31 节一字未动**。**机检**：`check_verbatim_move.py`（原文件 = `7192f81`）→ **`orig_content_kinds=561 MISSING_KINDS=0`**（逐字搬移有效，`NEW_ONLY=65` 全为新增标题 / 锚点 / 存根规范句 / 指针）；令牌三集合（vs HEAD 全量，对「主文档 ∪ 汇录 ∪ 两份新文档」）**`MISSING_TOTAL=0`**（§ 107→132 / 路径符号 145→203 / 数字 75→129，均只增）；C/D 规范句 **20 句、存根缺失 0**；主文档 711 行 / 77 037 B → **665 行 / 70 161 B**（字节仅 −8.9%——规范句必须留存根，属设计使然），`>110 字符行 = 0`；`BROKEN_MD_LINKS=0`、`RESOLVED_INDEX_SYNC=OK`、`check_recheck_consistency` **PASS**；`\gradlew.bat test --max-workers=1` 全绿（`count_test_results.py` → `xml=353 tests=2467 failures=0 errors=0 skipped=13`），复跑全 **UP-TO-DATE**。文档同步：`docs/README.md`（architecture/ 与 records/ 各登记一行）、`RESOLVED_LOG`（本行）、`resolved/README.md`（最大 §254 → **§255**，下一批 §256）、`BATCH_158_PLUS.md`（§255 行）。**如实声明**：**A/B 分类分流未做**——B 类取舍是否迁往 `产品裁决登记.md` 待用户过目，本批未新建任何 `PD-xx`、未改该文件与 `AGENTS.md` 一个字；本批**不改任何结论**，只做位置迁移 + 存根化；未触生产 / 测试代码、`*/src/androidTest/**`、原生面与 `参考项目/` ⇒ 无设备侧必跑项；未重跑任何实测 | 无（文档结构批次） | [`255-限界表分类分流起步档批次.md`](batches/255-限界表分类分流起步档批次.md) |
| §256 | 限界分类标签与口径裁决迁出批次（**文档结构批次**，同批新登记 `ISSUE-P3-251` / `ISSUE-P3-252`、**不闭环任何 `ISSUE-*`**；只动 `docs/`）：接 §255 的**定标档**——① [`architecture/已知工程限界.md`](../architecture/已知工程限界.md) 的 **42 条节标题**逐条贴分类标签（**【客观限界】16 /【有意取舍】7 /【已迁出】19**，`## N.` 章标题不加标签），文首块改为「标签只回答『谁能解除』，**不是**完成度评分」＋三类去向（说明 / 约定 / 验证现状、宿主环境现象、口径裁决）；② **8 节口径裁决逐字迁出**——§1.7 / §9 / §10 / §11 / §14 / §18 / §20 / §21 → [`architecture/产品裁决登记.md`](../architecture/产品裁决登记.md) **`PD-18` ~ `PD-25`**（唯一字面改动＝字段名（`事实`/`裁定`/`裁决`→`口径`、`边界`→`不得外推的边界`、`解除条件`→`重开条件`）＋ `对应条目` / `原登记位置` 两行与末尾留痕句），原表按原编号**存根化**＝标题补【已迁出】＋**全部含「不得」的块逐字留在原位**＋一行指针（§10 / §11 的规范句在代码有 4 处以上 `§` 引用，删句即断链）。**机检**：块级判据 **41 块 A（存根逐行）/ C（首行正文）/ D（整块去空白逐字）全 ✔**（B 列 7 处 ✘ 全为末尾留痕句所致）；令牌三集合 **`MISSING_TOTAL=0`**（`§` 87→90 / 反引号 455→466 / 数字 248→249）；`BROKEN_MD_LINKS=0`、`RESOLVED_INDEX_SYNC` 一致、`check_recheck_consistency` **PASS**；`./gradlew.bat test --max-workers=1` **BUILD SUCCESSFUL**，唯一实际执行任务是 `:crypto:cargoHostBuild`、五模块 `testDebugUnitTest` 全 **UP-TO-DATE ⇒ 未产生新的测试执行**。**同批新登记 `ISSUE-P3-251` / `ISSUE-P3-252`**（前者＝限界 §13 缺「事实 / 边界」两字段、违反该表登记纪律；「边界」段在批次 139 / 114 / 120 与汇录 §13 **无可直接搬移的对应句** ⇒ **未编造、未就地补字段**。后者＝限界表正文 **7 处** `[附二](#附二更正留痕按日期) §X` 的编号留痕**不在该文件自己的 `## 附二` 内**（正典在汇录，**21 条**），锚点悬空而 `check_md_links.py` 判不出；本批**不改链接语义**。P3 区 `0 → 2 项`）；另本批**引入并当场修掉**的 `。；` 畸形标点 **8 处**（`PD-18` ~ `PD-25` 的 `依据` 末行，同一批次内闭环；现查 `grep -rn "。；" docs/` 的 7 处命中**全为本批对该畸形标点的引述文字**，承载文件 `产品裁决登记.md` 命中 0）。读数：限界表 665 行 / 70 826 B → **639 行 / 66 226 B**，`产品裁决登记.md` 605 → **770 行**。**如实声明**：**零结论变更**；`check_verbatim_move.py` 因字段名允许改写在本批**不适用**（`MISSING_KINDS=107` 逐条＝40 处标题补标签＋41 处首行改写）；施工中曾出现「用原文行号取值」致 8 节存根**错位一节**的中间态（**未提交**，已自纠复跑）；未触生产 / 测试代码、`*/src/androidTest/**`、原生面与 `参考项目/`；未重跑任何实测 | `ISSUE-P3-251`（**新登记**，未闭环） | [`256-限界分类标签与口径裁决迁出批次.md`](batches/256-限界分类标签与口径裁决迁出批次.md) |
| §257 | 生物识别快速解锁断言密钥形态修复批次（`ISSUE-P1-242` **整条闭环**；用户报告「生物识别能唤起、认证也通过，却提示『快速解锁凭据校验未通过』，主密码重登记 / 杀进程重进仍复现，而同机应用内通行密钥验证正常」）：根因＝`ISSUE-P1-09` 同批引入的**两处同源缺陷**——① 断言私钥规格只调 `setUserAuthenticationParameters(30, AUTH_BIOMETRIC_STRONG)` 而漏 `setUserAuthenticationRequired(true)`（该 API 只**配置**参数、要求本身仅由后者开启；SDK javadoc 原文 *This has effect if the key requires user authentication for its use*；`KeyStore2ParameterUtils.addUserAuthArgs` 在 `!isUserAuthenticationRequired()` 时下发 `KM_TAG_NO_AUTH_REQUIRED`，认证参数被**静默忽略**）⇒ 私钥从未绑定认证；② 断言**读路径**却按 `KeyInfo` 探测「是否绑定认证」，不匹配即 `deleteEntry` 重建 ⇒ 前提恒假 ⇒ **每次断言都重建私钥** ⇒ 新公钥与已登记记录脱钩 ⇒ 验证必然失败 ⇒ 门控 fail-closed 拒绝**每一次**快速解锁（失败在验证侧，故重登记 / 冷启动无效）。整改＝读路径**不轮换**（轮换只在 `enroll` 序列内）+ 规格单点声明于新文件 `security/UnlockPasskeyKeyPolicy.kt`（KDoc 承载平台约束、**API 陷阱**与两条反向约束：不得恢复读路径轮换 / 不得补 `setUserAuthenticationRequired(true)`）+ 删除失效常量 `UNLOCK_PASSKEY_AUTH_VALIDITY_SECONDS` + 四处 KDoc 同步。**双设备证据**（Pixel_10 AVD / API 36 与 Redmi 4X / API 37）：新增 `UnlockPasskeySigningDeviceTest` 3 例，还原 HEAD 后**双设备 2 例判红**（失败信息即根因），整改后 **3/3 全绿**；临时诊断用例（取证后删除）取四形态读数，`ISSUE-P1-09` 原形态实测 `authRequired=false validity=0 authType=0` 且无授权即可签名。宿主新增 `UnlockPasskeyKeySpecTest` 6 例、反向反校注入两处历史形态**恰 3 条判据变红**。文档同步：限界 **§3.6**（新登记【客观限界】）、`ACTIVE_ISSUES` P1 区「本区近期变动」、`RESOLVED_LOG`（§257 行）、`resolved/README.md`（最大 §256 → **§257**，下一批 §258）、本册。**如实声明**：平台约束（时间窗认证无法由 `CryptoObject` 绑定的提示打开）只依据官方指南 + 框架源码，未取得现场读数（无已录入生物识别）；未实跑 `BiometricPrompt` 交互；未跑 `lint` / `assembleRelease` / 截图门禁；未触原生面 ⇒ 无四层义务 | `ISSUE-P1-242`（整条闭环） | [`257-生物识别断言密钥形态修复批次.md`](batches/257-生物识别断言密钥形态修复批次.md) |
| §258 | 限界表 §13 字段补齐与「附二」正典归汇录批次（`ISSUE-P3-251` / `ISSUE-P3-252` **两条整条闭环**；**纯文档批次**，只动 `docs/`）：① **251**——限界 §13（实时验证码窄通道驻留）按登记纪律补两字段：「事实」5 行**逐字搬移**自汇录 §13（搬移断言 5/5 命中、源侧残留 0；汇录原位留一行指针，正典唯一），「边界（不得据本条外推）」按条目 AC「经裁决后另写入并留痕」路径**新写**（批次正文即留痕：候选①钉住「过期码字符串仍驻留 + `clearLiveCodes()` 是待建机制而非现状」，候选②限定只登记 `liveCodes` 一处、不外推其它驻留点），四字段序机检 True；`check_verbatim_move.py` 因单段对抽不适用，以逐行包含断言等价执行。② **252**——按处置方向 **①**「附二正典归汇录」：汇录 `### ## 附二` 标题修层级畸形（slug 不变 ⇒ 页内 3 引用免改即持续可达）、收纳判据（2 行）+ `ISSUE-P2-243` 块（8 行）+ `ISSUE-P2-244` 块（6 行）**逐字迁入**（分别落标题后 / §1.1「原文：」/ §1.3「原文：」空位，搬移断言全 0 缺失 0 残留），限界表附二降为「一行指针 + 3 个块去向」；**5 文件 14 处** `[附二](#…)` 改指 `汇录.md#附二更正留痕按日期`（限界表 7 + 产品裁决 3 + 实现约定 3 + 宿主现象 1；**scope 现场扩展**——条目称三份文件，实查`实现约定与验证现状.md` / `宿主开发环境现象.md` 另有 §255 迁出时平移的 4 处同款悬空锚，按条目维护规则 2 一并改指；汇录页内 3 处不改）。前提复核：252 所称「3 个无编号块」实测为 2 物理块 / 3 逻辑单元，按 3 逻辑单元迁出；`check_md_links.py` 的 `LINK` 正则对含 `#` 目标**整条不匹配**（比条目所述更弱，连锚点存在性也不判）——14 处改指的可达性以「文件存在断言 + slug 人工推演」承担，如实声明无机检闸门。**验证**：`BROKEN_MD_LINKS=0`、`RESOLVED_INDEX_SYNC` 复跑一致、`.\gradlew.bat test --max-workers=1` 照跑（读数见批次 §3 尾注）；零结论变更（§13 解除条件 / 依据 / 汇录「为什么维持现状」一行未动，改指不改引用句正文）；未触生产 / 测试代码、`*/src/androidTest/**`、原生面与 `参考项目/` ⇒ 无设备侧必跑项 | `ISSUE-P3-251` / `ISSUE-P3-252`（两条整条闭环） | [`258-限界表13字段补齐与附二正典归汇录批次.md`](batches/258-限界表13字段补齐与附二正典归汇录批次.md) |
| §259 | 关闭生物识别开关撤销封印数据批次（`ISSUE-P2-253` **整条闭环**；当场发现、当场闭环——同 §257 口径，依 2026-09-22 用户「后续交由你全程整改、不要询问」指示执行，条目未驻留 `ACTIVE_ISSUES`、正文原样收录批次 §1）：2026-09-22 生物识别对照参考项目审查揭出「关闭开关只落偏好、不删封印数据」——原 `BiometricEnableCoordinator.setEnabled(false)` 仅 `setBiometricEnabled(false)`，全仓 `clearCredential` / `clearAll()` 的生产调用点仅在解锁失败与 Cipher 准备失败路径（设置页零命中、全应用无用户可见撤销入口；对照 KeePassDX 菜单删密钥、keepass2android `ClearFingerprintUnlockData` 均有出口），各库封印凭据、断言登记记录与 per-db Keystore 密钥在关闭后全部继续驻留；`ACTIVE_ISSUES` 与产品裁决表检索零命中 ⇒ 属**未登记缺口**。用户最终目的裁决：**关闭开关 = 删除**。整改＝① 新增 [`BiometricCredentialStorage.revokeAllBiometricData()`](../../app/src/main/java/com/keepasskey/app/security/BiometricCredentialStorage.kt)：按 `DB_KEY_SUFFIXES` 枚举全部已登记库 → 逐库删封印密钥与断言密钥对（`runCatching` best-effort，Keystore 不可达跳过、prefs 清空仍执行）→ `clearAll()`；刻意保留 autofill 绑定密钥与完整性 HMAC 密钥 ② 两处关闭路径接线（设置页关闭 ＋ 拒绝软件级降级关闭），次序固定**先落偏好关闸、再删数据** ③ 封印落库前复核 `biometricEnabled` 防挂起期撤销后写回 ④ 别名单点化 `KeystoreManager.sealAliasFor` / `unlockPasskeyAliasFor`。**验证**：新增宿主 **5 例**（存储层 2 ＋ 协调器 1 ＋ 源码守卫 2；既有用例一行未删），Proxy 补 `getAll()`；全量 `test --rerun-tasks --max-workers=1` **BUILD SUCCESSFUL in 2m 9s / 114 executed**、`count_test_results.py` = **`xml=354 tests=2478 failures=0 errors=0 skipped=13`**（§257 基线 354/2473 ⇒ **+5 例**逐条可对）；`tier1=2 / tier2=29 / functions_ge_100=3` 持平；`BROKEN_MD_LINKS=0` / `RESOLVED_INDEX_SYNC=OK` / `check_recheck_consistency` **PASS**。**同日独立小提交（不占批次编号）**：`../architecture/reference-projects.md` 三处章节错链修正 ＋ `../security/SECURITY_RECHECK_2026-09.md:405` 更正注（`ISSUE-P2-76` 已随 §48 闭环）。**如实声明**：未触 `*/src/androidTest/**`、原生面、`参考项目/`、构建脚本、依赖清单 ⇒ 无四层义务；未实跑 `BiometricPrompt` 真实交互（`ISSUE-P2-192` 余量）；Keystore 真实删钥分支宿主不可达、真机未单独复测；未跑 `lint` / `assembleRelease` / 截图门禁 / KPEX 对拍 | `ISSUE-P2-253`（整条闭环） | [`259-关闭生物识别开关撤销封印数据批次.md`](batches/259-关闭生物识别开关撤销封印数据批次.md) |
| §260 | 文档审核悬空引用与登记缺口收口批次（`ISSUE-P3-259` **整条闭环**（未驻留 `ACTIVE_ISSUES`，正文原样收录批次 §1）+ 同批新登记留表 `ISSUE-P2-254` / `P3-255` / `P3-256` / `P3-257` / `P3-258`；**纯文档批次**，触发＝用户命题「审核现存文档，担心把应当解决的问题放到别的地方」）：前提复核 16 项——六处悬空指针改指闭环归档（PD-11→§213、PD-12→§227、迁出说明→同日 §110、限界 §7+宿主 §7 `P3-246`→§251 已改实体口径、契约 RC-05/`P3-235`→`P3-258`/§238、扫码 `:3`/`:306`→批次 03）；两处表内矛盾 + 状态失真（PD-09 边界段按 §220 重写、契约「迁移已完结」→Step 4 未实施、限界 §1.6 解除条件按契约 §6.2 预定动作对齐补指针）；`ACTIVE_ISSUES` 补 P0 区 + P2 `0→1` + P3 `0→4`（`P2-254` 三项验证缺口、`P3-255` P3-221 结案遗留核验、`P3-256` PD-05 待裁决归位、`P3-257` 4 处薄编排、`P3-258` 池内擦除 Step 4 跟踪链修复后推进），源文档指针回填；分类裁定留痕（放对不动项清单 + 不替用户裁决声明）。验证：`BROKEN_MD_LINKS=0` / `RESOLVED_INDEX_SYNC=OK` / `check_recheck_consistency` PASS；`test --max-workers=1` 照跑。如实声明：零代码改动、无设备侧必跑项、未重跑实测；「ISSUE 编号悬空」无机检闸门（人工发现） | `ISSUE-P3-259`（整条闭环）+ 新登记 `ISSUE-P2-254` / `P3-255` / `P3-256` / `P3-257` / `P3-258`（留表） | [`260-文档审核悬空引用与登记缺口收口批次.md`](batches/260-文档审核悬空引用与登记缺口收口批次.md) |
| §261 | P3 低危四项闭环批次（`ISSUE-P3-255` / `P3-256` / `P3-257` / `P3-258` **四条整条闭环**，P3 区归零）：255 三项真机核验（①② 通过、③ 不可达按限界 §22 承接）；256 `PD-05` 裁定候选 C 落地（`AutofillUnlockRouter` 纯函数路由，未绑定 fail-closed 回选择器，解锁页不自建 Dataset）；257 `SettingsViewModel` 4 处薄编排下沉控制器（单语句委托，`PD-23` 清单清空）；258 池内擦除准入①穷举 9 类持有者全满足后按身份集合判定实施（新原语 `clearBinaryPool` + 四收口点，限界 §1.6 已解除、契约 Step 0~4 完结）。验证：全量 test `xml=358 tests=2507 failures=0`（+4 类 / +29 例）、反向反校 10 组、三项度量持平、三自检 PASS；真机 ①② 逐字证据留批次 §2.4 | `ISSUE-P3-255` / `P3-256` / `P3-257` / `P3-258`（四条整条闭环） | [`261-P3低危四项闭环批次.md`](batches/261-P3低危四项闭环批次.md) |
| §262 | 导航转场定标与顶层 Tab 返回栈整改批次（`ISSUE-P2-260` / `ISSUE-P3-261` **两条整条闭环**，P3 区再度归零）：260 = 底栏顶层 Tab 切换的 `popUpTo` 原取 `findStartDestination().id`（＝ `startDestination` `Unlock`），而解锁 / 锁库都把它 inclusive 弹出栈外 ⇒ `NavController` **静默忽略**栈外目标、每次切 Tab 都是纯压栈（`saveState` / `restoreState` 失效、返回栈无界增长、返回键在 Tab 历史回退）；目标改取**栈上当前路由**后三处语义同时收口（顶层 entry 恒为 1、返回键在顶层熔断、同级切换不再走 pop 语义）。261 = 转场动效收敛为**双栏定标**（空间段走 `MotionScheme` 的 spring、效果段走官方 shared axis 的**顺序**淡化时间轴 90ms / 210ms+90ms），`topLevel*` 改为真正 fade through 并把设置页纳族、删无出处 `0.96f`、预测性返回补齐两个专属槽位与 `(.1,.1,0,1)`、现差比例具名且两向镜像、内容层 6 处改读 MotionScheme + 列表项 `animateItem`；四条有意偏离落 `PD-26` ~ `PD-29`；守卫 4 → 9 例（含全仓 `app/src/main` 不得再出现裸 `tween(`）＋ 新增设备用例 2 例（返回栈不增长 + `NavBackStackEntry.id` 不变）。真机（Redmi 4X / API 37）四条路径逐字留痕、`Ignoring popBackStack` 命中 0；**预测性返回未取得**（本机 3 键导航） | `ISSUE-P2-260` / `ISSUE-P3-261`（两条整条闭环；P3 区归零） | [`262-导航转场定标与顶层Tab返回栈整改批次.md`](batches/262-导航转场定标与顶层Tab返回栈整改批次.md) |
| §263 | 注册响应 `attestationObject` 缺失规范键 `authData` 修复批次（`ISSUE-P2-265` **整条闭环**；触发＝用户命题 2026-09-22「本地显示添加成功、网站一律判未成功验证；同设备 / 同站点 / 同浏览器 Monica **100% 成功**而本仓失败」）：根因＝组装 `attestationObject` 时把**响应 JSON 的字段名** `authenticatorData`（17 字符）当成了 **CBOR map 的键**，而 CTAP2 §6.5.4 规定为 **`authData`**（8 字符）；系统侧只校验响应 JSON 合法性、**不解析 CBOR** ⇒ 全程绿灯（`Final credential received` / `resultCode=-1`），浏览器侧按规范查找 `authData` 失败（`E chromium: MojoClassFromJSON failed to convert JSON: field missing or invalid: attestationObject`，报错处为 `webauthn::MakeCredentialResponseFromValue`）⇒ 网页收到 `UnknownError`，**依赖方从未收到任何凭据**，故与浏览器品类 / 站点 / 是否原生 App 全无关。字节级证据（真机 `M332BF` / Android 17 / Google WebView）：`… 71 61 75 74 68 65 6e 74 69 63 61 74 6f 72 44 61 74 61`（`text(17)`）→ `… 68 61 75 74 68 44 61 74 61`（`text(8)`），承载**同一份 148 字节** authData（rpIdHash 与 `SHA256("demo.yubico.com")` 逐字节一致、flags `0x5D`、AAGUID 真实值、credIdLen 16）；修复后同日**两条路径（`mark.via` web origin + `com.ss.android.lark` apk-key-hash）同时通过**、转换失败日志命中 0。同批对齐四项：`publicKey` **COSE → SPKI DER**、credentialId **32 → 16 字节**、AAGUID **全零 → 真实值**、`transports`/`crossOrigin`/`authenticatorData`/`publicKeyAlgorithm` 补齐 + 写入器 `int()`。**已证伪并整条回退**：`https://{rpId}` 兜底（参数 / `degradeWithRpIdFallback` / 调用点开关 / `isDiscoverable` + 三个常量全删），`normalizeOrigin` 因理由独立成立保留。新增防线＝CBOR **原始字节**断言（必须含 `authData`、不得含 `authenticatorData`，键名用**规范字面量**——原用例与被测代码共用常量构成**镜像自证盲区**，是本缺陷从未被拦住的直接原因）+ `PasskeyKeyCodecSpkiTest` / `PasskeyRegistrationPayloadBuildTest` / `CallingOriginResolverAttributionTest` / 写入器整数 parity 设备用例。验证：全量 `test --max-workers=1` **BUILD SUCCESSFUL**，`count_test_results.py` = **`xml=361 tests=2524 failures=0 errors=0 skipped=13`**；`BROKEN_MD_LINKS=0` / `RESOLVED_INDEX_SYNC=OK`。**如实声明**：① 改了 `*/src/androidTest/**` 但**未真机实跑**（设备上为 release 签名包，debug 测试须先卸载）；② **⚠️ 操作事故**：执行 `:app:connectedDebugAndroidTest` 时平台在 `INSTALL_FAILED_UPDATE_INCOMPATIBLE` 后**卸载了 `com.keepasskey`**，应用内部密码库永久丢失（`allowBackup="false"` 无备份），用户裁决不需恢复；**已立规** `AGENTS.md` §5「装有需保留数据的设备禁止直跑 connected 测试，改用 AVD」；③ 未触原生面 / `参考项目/` / 依赖清单 ⇒ 无四层义务，未跑 `lint` / `assembleRelease` / 截图门禁 / KPEX 对拍；④「Monica 100% 成功」为用户现场对照结论，未独立复跑。定位全过程与五条走偏路径见 [`../records/注册响应CBOR键误用定位记录.md`](../records/注册响应CBOR键误用定位记录.md) | `ISSUE-P2-265`（整条闭环） | [`263-注册响应attestationObject缺失authData键修复批次.md`](batches/263-注册响应attestationObject缺失authData键修复批次.md) |
| §264 | AES-KDF 种子定长校验与 CI 口令卫生及 KDoc 笔误批次（`ISSUE-P3-267` / `ISSUE-P3-265` / `ISSUE-P3-269` **三条整条闭环**，P3 6 → **3 项**）：267 = `KdbxKdfParameterCodec` AES-KDF 分支对 `S` 此前长度零校验，恰 16 字节经 `SecretKeySpec` **静默按 AES-128 派生**、其它非法长度解锁期抛非类型化 JCE 异常——新增 `validateAesKdfSeedBounds`（非 32 字节即 `KdbxCorruptFileException`，与 Argon2 盐边界对称）接入 `deserialize`，新增 `AesKdfSeedBoundsTest` 5 例（真实字节流往返：32 通过 / 0/31/33 拒绝 / 恰 16 字节回归项）；265 = `build.yml` 删固定口令 env `keepasskey-ci-ephemeral`，改 `keytool` 前一次 `openssl rand -base64 24` 同供 keytool 与 `GITHUB_ENV` 导出（未分两次随机），全仓该字面量归档后零命中；269 = `InnerHeader` KDoc「GZip 解压前」笔误更正，连同 `references/KeePass-2.61.1-架构分析.md` 载荷层次图把内层头画在压缩流外的同形错误一并收口。验证：全量 `test --rerun-tasks --max-workers=1` **BUILD SUCCESSFUL in 3m 21s / 114 executed**、`count_test_results.py` = **`xml=362 tests=2529 failures=0 errors=0 skipped=13`**（§263 基线 361/2524 ⇒ +1 类 / +5 例逐条可对）；`BROKEN_MD_LINKS=0` / `RESOLVED_INDEX_SYNC=OK`。如实声明：CI 改动未在真实 runner 复跑（纯 shell 变量替换，随机值经 `app/build.gradle.kts` 闸门静态论证）；未触原生面 / `参考项目/` ⇒ 无设备侧必跑项；未跑 `lint` / `assembleRelease` / 截图门禁 / KPEX 对拍 | `ISSUE-P3-267` / `ISSUE-P3-265` / `ISSUE-P3-269`（三条整条闭环） | [`264-AES-KDF种子定长与CI口令卫生及KDoc笔误批次.md`](batches/264-AES-KDF种子定长与CI口令卫生及KDoc笔误批次.md) |
| §265 | 保存黑名单弹窗关闭去状态化与接线守卫批次（`ISSUE-P3-264` **整条闭环**，P3 3 → **2 项**；触发＝用户命题 2026-09-23「评估ISSUE-P3-264整改方案」→ 评估揭出条目前提静态错误后用户指示执行）：**前提勘误**——条目自陈「逐跳接线完整」不成立，`AutofillBlocklistDialogs.kt:155` 的 `else onDismiss` **缺调用括号**（`onDismiss` 为 `() -> Unit` 变量，函数引用求值即被丢弃）⇒ 默认态全弹窗唯一的「关闭」是**死按钮**（用户所报「点了不关」的静态根因），且默认态 dismiss 槽整段不产子项、无任何可见关闭按钮，按条目自身升级条件按 **P2** 严格度整改。整改＝①「关闭」**无条件**落 `dismissButton` 槽（`PackageBlocklistDismissButton` 删 `manualEntry` 参数与分支，与全仓其余 13 处同形），confirm 槽仅 `manualEntry` 时产「新增」②`onToggleManual` 改**真双态切换**＋手工分支补「返回名单模式」入口（新键 `autofill_blacklist_manual_back` zh/en）③新增 `PackageBlocklistDialogWiringTest` **4 例**（谓词式静态接线守卫：confirm 槽不得承载 `btn_close` / 不得吃 `manualEntry`、dismiss 槽必须承载 `btn_close` 且不得状态化、调用点直挂 `onDismiss`、全文件不得出现 `else onDismiss`；锚点缺失计违反项＝§77 防空扫；§205 历史坏形态代入同一谓词须报红＝§199 代入法）。验证：定向 4 例 / 0 失败；全量 `test --rerun-tasks --max-workers=1` **BUILD SUCCESSFUL in 3m 17s / 114 executed**，`count_test_results.py` = **`xml=363 tests=2533 failures=0 errors=0 skipped=13`**（§264 基线 362/2529 ⇒ +1 类 / +4 例逐条可对）；`BROKEN_MD_LINKS=0` / `RESOLVED_INDEX_SYNC=OK`。如实声明：真机 / AVD 手工冒烟未做（设备装有用户数据 release 包，§263 立规禁止直跑），死按钮结论由 Kotlin 求值语义＋用户原始报告互证、不得读作已真机验证；未触原生面 / `androidTest` / `参考项目/` ⇒ 无设备侧必跑项；未跑 `lint` / `assembleRelease` / 截图门禁 / KPEX 对拍 | `ISSUE-P3-264`（整条闭环） | [`265-保存黑名单弹窗关闭去状态化与接线守卫批次.md`](batches/265-保存黑名单弹窗关闭去状态化与接线守卫批次.md) |
| §266 | 动态取色与主题调色盘互斥单选批次（`ISSUE-P3-263` **整条闭环**，P3 2 → **1 项**；触发＝用户命题 2026-09-23「整改ISSUE-P3-263，存储口径选取候选 A（推荐）互斥单选，一定要调用谷歌开发的知识库MCP服务器」，AC⓪ 裁决先决当场作出）：① **判据单点化（AC①）**——新增纯函数 `resolveColorSource(dynamicColorEnabled, sdkInt): ColorSource`（`ThemeMode.kt`），`KeePasskeyTheme` 删内联双写改经该函数推导、设置页 `themePaletteSection` 同读取 `paletteUsable`，API < 31 且偏好 true 显式回落品牌调色盘；② **呈现互斥（AC②③）**——`ThemePaletteItemCard` 新增 `enabled` 参数（`clickable(enabled=…)` + `alpha(0.55f)` 置灰），动态取色生效期间 5 项撤销全部选中态且不可点，分区附行内原因说明 + 「改用主题调色盘」一步切回（`ThemeSettingsScreen` 增 `onSwitchToBrandPalette`，NavGraph 接 `setDynamicColorEnabled(false)`，偏好值保留即恢复）；预览明暗同源收口（`SYSTEM` 态读 `isSystemInDarkTheme()`）；③ **存储互斥（AC④，候选 A）**——`RealSettingsRepository.setThemePalette` 单次 DataStore `edit{}` 原子事务幂等写两键（`theme_palette` + `dynamic_color_enabled=false`，兜底不变量），`FakeSettingsRepository` 镜像；④ **文案（AC⑤）**——`theme_dynamic_sub` / `theme_palette_pick_desc` 以可验证描述替换「覆盖」+ 新增两键 zh/en。**知识库 MCP 如实留痕**：`google-developer-knowledge` MCP 本会话未连接（`ToolSearch` 全量检索不存在），按规则 7 退路以官方文档中国镜像 WebFetch + 检索核对（动态取色＝API 31 起壁纸取色，与门控一致，零改动）。验证：新增 3 类 **11 例**（真值表 / 互斥行为 / 接线守卫含「两键同处一个 edit 块」源码断言）；定向首跑因缺 import 编译失败当场修正；全量 `test --rerun-tasks --max-workers=1` **BUILD SUCCESSFUL**，`count_test_results.py` = **`xml=366 tests=2544 failures=0 errors=0 skipped=13`**（基线 363/2533 ⇒ +3 类 / +11 例）；`BROKEN_MD_LINKS=0` / `RESOLVED_INDEX_SYNC=OK`；裁决登记 `PD-30`。**如实声明**：真机 / AVD 手工冒烟未做（§263 立规禁止直跑 connected），AC③ 真机逐字留痕**未达成**、不得读作已真机验证；原子性由源码守卫 + DataStore 官方事务语义承担（本仓无 Robolectric）；全量首跑红 1 例 `SyncCacheEvictorTest` F23＝KDoc 自陈已登记偶发红（NTFS 鬼影，限界 §7），与本批无交集，复跑通过；未触 `androidTest` / 原生面 / `参考项目/` / 构建脚本 ⇒ 无设备侧必跑项；未跑 `lint` / `assembleRelease` / 截图门禁 / KPEX 对拍 | `ISSUE-P3-263`（整条闭环） | [`266-动态取色与调色盘互斥单选批次.md`](batches/266-动态取色与调色盘互斥单选批次.md) |
| §267 | HMAC 块流读侧上限移除批次（`ISSUE-P3-268` **整条闭环**，P3 1 → **0 项**；触发＝用户命题 2026-09-23「解决这个问题。最好在设置里面可以自定义这个大小」→ 核实参考项目后用户裁决「**既然别家都没有上限，那么咱们也不要设置上限，只检查负数**」，AC⓪ 裁决先决当场作出）：**前提实证补齐**——官方 KeePass 2.61.1（`HmacBlockStream.cs:231-237`）与 keepass2android 同源读侧仅查 `nBlockSize < 0`、按声明直接分配，KeePassXC（`HmacBlockStream.cpp:142-149`）同（1 MiB 仅是写侧默认分块）⇒ 三家读侧均**无上限、均无设置项**，旧仓 1 MiB 读上限是比三家都严格的自加护栏、会把「块长 > 1 MiB」的合法第三方文件误判为损坏；「设置自定义上限」无参考先例且属伪需求（调大即自行放弃防护）。**整改**＝① `HmacBlockStream` 删常量 `MAX_READ_BLOCK_SIZE` 与读侧两处越限拒绝分支，只保留负数块长类型化拒绝；② 上限移除揭出同型缺口——块数据读取的 `EOFException` 原本裸漏出，`readAll` / `loadNextBlock` 统一补 `catch (EOFException) → KdbxCorruptFileException`（与既有 HMAC 读取处同型）；③ 恶意分配由 `LittleEndianUtil.readBytes` 既有 16 MiB 通用护栏（P0-5）兜底，该护栏一行未动；④ KDoc 同步（ISSUE-P3-37「块尺寸上限完全未变」陈旧断言更新）。**测试**（改写非删除）：旧 `readAll_rejectsOversizedBlockSize` 改写为 `readAll_failsClosedOnTruncatedOversizedBlock` + 新增 `readAll_acceptsBlockSizeBeyondLegacy1MiB`（旧上限+1 真实往返必须接受）/ `readAll_rejectsNegativeBlockSize` / `streamingPath_acceptsBlockSizeBeyondLegacy1MiB`。验证：定向三测试类 `--rerun-tasks` 强制真实执行全绿（首跑全程 UP-TO-DATE 不可信）；全量 `test --rerun-tasks --max-workers=1` **BUILD SUCCESSFUL**，`count_test_results.py` = **`xml=366 tests=2547 failures=0 errors=0 skipped=13`**（§266 基线 366/2544 ⇒ **净 +3 例**＝删 1 改写 + 增 4 逐条可对）；`BROKEN_MD_LINKS=0` / `RESOLVED_INDEX_SYNC=OK`；裁决登记 `PD-31`（含不采纳设置项理由与重开条件）。**如实声明**：读侧无上限后恶意文件可声明至多 16 MiB 块长并触发等量临时分配才被拒绝——系用户裁决有意接受的取舍（与三家同一暴露面）；写侧契约不变（恒 1 MiB 分块），读侧放宽只增加可接受文件集；未触 `androidTest` / 原生面 / `参考项目/` / 构建脚本 ⇒ 无设备侧必跑项；未跑 `lint` / `assembleRelease` / 截图门禁 / KPEX 对拍 | `ISSUE-P3-268`（整条闭环） | [`267-HMAC块流读上限移除批次.md`](batches/267-HMAC块流读上限移除批次.md) |
| §268 | 新建库 KDBX 版本声明 4.1 批次（`ISSUE-P2-266` **整条闭环**，P2 2 → **1 项**；触发＝用户命题 2026-09-23「解决这个问题」）：新建库写侧声明 KDBX 4.0（`0x00040000`）却恒写出 6 项 4.1 专有 XML 元素（`SettingsChanged` / `MasterKeyChangeForceOnce` / CustomIcon `Name`·`LastModificationTime` / CustomData Item `LastModificationTime` / Entry `QualityCheck`）——规格明文「For KDBX 4.1, the format version value is `0x00040001`」，「声明小、夹带大」的文件对严格客户端无宽容承诺。**AC⓪ 裁决＝条目推荐方向「新建库直接恒写 4.1」**（对齐官方 KeePass 2.53+ / KeePassXC 新写即 4.1；不采纳条件化升版——写侧恒含 4.1 元素，条件恒真无收益）；前提开工前逐项复核全部成立。**整改**＝① `KdbxConstants.Version` 增写侧常量 `VERSION_4_1 = 0x00040001`（`ISSUE-P3-126③`「不保留死常量」KDoc 同步改写为写侧语义）；② `KdbxHeader` 数据类默认 `version` 改 `VERSION_4_1`（`createDefault` 随之恒写 4.1），反序列化路径与 `validateVersion`（仅校验 major）**一行未动**——既有 4.0 / 4.1 库往返均保留原版本、读取接受集不变，缺陷面仅限新建库；③ `SettingsUiState.kt` 注释口径修正（AC③）。**测试**＝新增 `testNewDatabaseDeclaresKdbx41Version`：createDefault 两 KDF 分支断言 4.1 + **落盘文件第 8..11 字节 LE 逐字节断言 `0x00040001`**（AC① 端到端留证）+ 4.1 读回版本保留 + 既有 4.0 库往返保留 4.0（边界锁定）。验证：全量 `test --rerun-tasks --max-workers=1` **BUILD SUCCESSFUL**（114 executed），`count_test_results.py` = **`xml=366 tests=2548 failures=0 errors=0 skipped=13`**（§267 基线 366/2547 ⇒ **净 +1 例**）；`generate_corpus.py --check` **通过**（keepassxc-cli 2.7.12）；`verify_interop.py` **对拍通过 114 条判据**（pykeepass 4.2.0 + keepassxc-cli 2.7.12）且两份探针产物落盘 version 实测 `0x00040001`——版本声明变更取得官方第三方实现端到端互操作证据（AC②）。**真机验证（同日补做，Redmi 4X / Android 17，用户确认测试机数据无须保留）**：`:database:connectedDebugAndroidTest` **17 tests / 0 failures / 0 skipped**（自产库 4.1 设备侧写入读回 + 官方语料端到端解锁等 6 套全绿）。**如实声明**：对「仅支持 4.0、严格拒绝未知元素」的老客户端，本仓文件此前已夹带 4.1 元素（无宽容承诺），声明 4.1 使声明与内容一致、至少不劣于整改前；`*/src/androidTest/**` 代码本批零改动、未触原生面 / `参考项目/` / 构建脚本；AC④ 不触发（未裁决维持 4.0）；未跑 `lint` / `assembleRelease` / 截图门禁 | `ISSUE-P2-266`（整条闭环） | [`268-新建库KDBX版本声明4.1批次.md`](batches/268-新建库KDBX版本声明4.1批次.md) |

| §269 | 通行密钥交互链路与 PRF 真机对拍及 `ISSUE-P2-254` 结案批次（P2 再次归零）：经真机安装 Release 生产包，在标准 WebAuthn PRF 演练场（Corbado PRF Demo）上完成真机端到端注册、盐值计算与登录断言，UI 明确回显 `Authentication successful (PRF)` 且各项状态全绿；CM 系统凭据选择器弹出已确认，通知栏在系统模态对话框及 `FLAG_SECURE` 保护下无法主动下拉属 Android 系统级安全行为；`实现约定与验证现状.md` §4.3 与 `已知工程限界.md` §4.3 存根的对应边界句已同步收口。 | `ISSUE-P2-254`（闭环；**P2 区再次归零**） | [`269-通行密钥交互链路与PRF真机对拍及ISSUE-P2-254结案批次.md`](batches/269-通行密钥交互链路与PRF真机对拍及ISSUE-P2-254结案批次.md) |
| §270 | 回收站开关真实落库批次（`ISSUE-P2-270` **整条闭环**，P2 2 → **1 项**；触发＝2026-09-23 全仓假开关清点，同日新登 P2-270 / P2-271 / P3-272 ~ P3-274 五项，本条为 P2 区首项整改）：`setRecycleBinEnabled` 由「仅改设置页内存回显」改为经 `updateDatabaseMeta` 写库 Meta + `save()` 真实落盘（与 `setArgon2Parameters` 同口径），init 回填通道补齐该字段实现回显单一真相源（消除显示漂移），删除分流三处读库 Meta 真值一行未触；新增 6 例守卫（`RecycleBinToggleApplyTest` 4 例：关闭 / 重开双向真实落盘重开读回 + 无库如实 no-op + 互操作对拍产物；`RealVaultRepositoryTest` 2 例：禁用时单条 / 分组删除走物理删除 + 墓碑）；官方对拍 keepassxc-cli 2.7.12 `db-info` 对本仓产物报「回收站未启用。」、对 pykeepass 自建对照组报「回收站已启用。」，双向翻转实证（pykeepass 4.2.0 不解析该元素，仅承担对照组建库角色）。全量 `test` 2554 例 / 0 失败 / 0 错误 / 13 skipped；未触原生面 / androidTest ⇒ 无设备侧必跑项。**顺带补登**：§269 批次的 `RESOLVED_LOG.md` 全量索引行此前漏登（滚动分册已有），本批补齐。 | `ISSUE-P2-270`（整条闭环） | [`270-回收站开关真实落库批次.md`](batches/270-回收站开关真实落库批次.md) |
| §271 | 算法选择器真实落库批次（`ISSUE-P2-271` **整条闭环**，P2 1 → **0 项**，本区归零；触发＝2026-09-23 全仓假开关清点，继 §270 后 P2 区第二项整改）：`setEncryptionAlgorithm` / `setKdfAlgorithm` 由「仅回写设置页内存回显」改为经 `CipherLabels.cipherUuidForLabel` 反查 + `updateDatabaseMeta` 写 `KdbxHeader.cipherUuid` / `kdfParameters` + `save()` 真实落库——写侧 `KdbxFile.save` 按保存时头换新 IV（按算法官方长度，P0-4 契约）/ 盐重派生，语义与官方 KeePass「算法 / KDF 保存时生效」一致；换 KDF 变体补齐目标变体参数（Argon2 换型携带现行 I·M·P、→ AES-KDF 取官方缺省 6 000 000 rounds、自 AES-KDF 换入 Argon2 取类型缺省 I·M·P），回显改单一真相源（init 头映射通道统一下发）；同型无谓重派生 / 未知标签 / 无会话如实 no-op；KDF 词汇表按 P3-92 纪律收敛为 `KdfLabels`（投影 / 对话框 / 写侧三处共用）。新增 11 例守卫（`AlgorithmSelectionApplyTest` 6 例：AES→ChaCha20 全链路 + Argon2id→Argon2d 携带 I·M·P + 换入 AES-KDF 补缺省 + 同算法 / 未知标签 / 无会话 no-op + 互操作对拍产物；`KdbxAlgorithmSwitchRoundtripTest` 5 例：`:database:` 层换轴 save→load 往返含错误密码被拒）；官方对拍 keepassxc-cli 2.7.12 `db-info` 对本仓产物报「加密：Twofish 256 位 / KDF：Argon2d（2 次，65536 KB）」逐项一致。全量 `test` 2565 例 / 0 失败 / 0 错误 / 13 skipped；未触原生面 / androidTest ⇒ 无设备侧必跑项。 | `ISSUE-P2-271`（整条闭环） | [`271-算法选择器真实落库批次.md`](batches/271-算法选择器真实落库批次.md) |
| §272 | 冲突时刻 ETag 透传与弱校验收口批次（`ISSUE-P1-275` **整条闭环**，P1 2 → **1 项**；触发＝2026-09-23 两轮全仓审查 P1 区首项整改；本体与叠加缺陷一次收口）：① **AC①**＝自动合并路径 `autoMergeAndUpload` 补透传冲突时刻 ETag（新增 `expectedEtagForConflictUpload` internal 判据，与用户裁决路径**共用同一判据**），`SyncEngine.markResolvedAndUpload` **删除**「空白即重探当前值」回退分支并去掉缺省值——空白改写为「无基线可校验的唯一显式回退＝无预条件上传」，禁在本方法内重探；② **AC②**＝`cleanEtag` 保留弱标记 `W/`（判定改在剥引号前依语法形态，顺带修复「不透明值以 `W/` 开头被误剥」的既有缺陷）、`formatHeaderEtag` 输出 `W/"abc"` 弱形态、MOVE `If` 头回传服务器原形态（RFC 4918 §10.4.4 允许弱比较、§10.4.9 弱形态示例合法）、`upload` 的 If-Match 弱期望**不发送**（RFC 7232 §3.1 强比较下弱标签恒不匹配，发送即恒 412 死锁；乐观锁由 MOVE If 承担）、S3 侧弱期望 fail-closed（对齐 P2-244「构造不出条件写即拒绝」）；③ **AC③**＝两条合并上传路径 412 均以合并产物为本地侧重取最新远端并**重新进入冲突流程**（重入再次分叉照常走用户决策通道；深度界限 1 次，再 412 如实上浮类型化错误；重入侧全新解析与首轮树零共享，四棵失效树按 P3-235/258 身份判定擦除，红线不破）；**AC④**＝新增守卫 8 例（`SyncModelsTest` 4 例 + `SyncEngineTest` 1 例「无基线时显式无条件上传而不重探」＋ 412 用例补透传断言 + `WebDavSyncScenarioTest` 弱 ETag 服务器全链路不恒 412（`StatefulDavDispatcher` 扩展弱标签模拟与 If 弱比较裁决）+ `SyncCoordinatorTest` 2 例 app 层接线（自动合并透传冲突时刻 ETag / 强制双 412 后重入完成二次合并上传））。全量 `test --rerun-tasks --max-workers=1` **BUILD SUCCESSFUL**，`count_test_results.py` = **`xml=369 tests=2572 failures=0 errors=0 skipped=13`**（§271 基线 2565 ⇒ 净 +7 与新增对账）。**AC⑤ 如实声明**：弱 ETag 真实 DAV 服务器矩阵（Apache/mod_dav、nginx-dav、Nextcloud、IIS）**未执行**——本会话无该环境可用，`tools/local-sync` 代理侧不可运行且其 `run_webdav.py` 不产 ETag；证据止于 RFC 全文核对（协议层）+ 有状态 mock 头形态与弱比较裁决（形态层），真实矩阵属「只有真实环境才能证伪」的显式残余，待环境可用时按规则 8 补测回填。未触 `crypto/src/main/rust/**` / `*/src/androidTest/**` / `参考项目/` ⇒ 无设备侧必跑项。 | `ISSUE-P1-275`（整条闭环） | [`272-冲突时刻ETag透传与弱校验收口批次.md`](batches/272-冲突时刻ETag透传与弱校验收口批次.md) |
| §273 | 附件池引用计费重定口径批次（`ISSUE-P1-276` **整条闭环**，P1 1 → **0 项**，本区归零；触发＝2026-09-23 两轮全仓审查 P1 区末项）：旧口径按引用次数累计**整库**字节（`Σᵢ nᵢ·sᵢ > 2·Σⱼ sⱼ + 1 MiB`）⇒ 受害区间恰为 **`(512 KiB, 1 MiB]`**（单条目时化简为 `s·(N − 2) > 1 MiB`，而 `> 1 MiB` 附件走落盘不计费），「中等尺寸附件 + 历史快照」的自产库与官方 CLI 产物**都被判为引用放大攻击、整库打不开**。**AC⓪ 裁决（用户当场作出）**：AC① 去重与 AC③「数千次仍被拒」不能同用字节维度（去重后 `Σ_distinct sᵢ ≤ Σ_pool sⱼ` ⇒ 旧式恒真、防护归零）⇒ 取「**去重计费 + 单条目引用次数 ≤ 1024 + 单条目物化字节 ≤ 64 MiB**」。整改＝`accountPoolReference(refIndex, bytes)` 三面记账：去重式**如实登记为恒真的记账不变量、不承担拦截**，② 次数封放大倍数、③ 单条目字节封内存（取 `MAX_INLINE_MATERIALIZED_BYTES` 派生、消息保留旧措辞「累计物化字节超出预算」⇒ 既有断言一字未改）；旧判据的化简与三式全部落 KDoc（AC②）；按职责拆出 `KdbxAttachmentBudget.kt`（原文件 507 行触 `tier1(>500)` ⇒ 292 / 220 行）。测试：`AttachmentReferenceBudgetTest` 2 → **6 例**（保留两例、其一仅改名；新增 1 MiB 端点 N=4 / 512 KiB+1 下界内侧 / 两条目各 N=3 三例放行 + 次数边界 1024 放行 1025 拒绝）+ 新增 `AttachmentHistoryReferenceBudgetRoundtripTest` **2 例**（`save → load` 真产物解锁、池 1 条目 / 4 引用共享索引、二次往返）+ 新增 `ThirdPartyAttachmentHistoryFixtureTest` **1 例**（读**官方 CLI 落盘**的同形态库）。**判别力实验**：临时还原旧口径 ⇒ **7 例红 / 2 例绿**（含 `4 MiB > 3 MiB` 的整库打不开逐位复现），还原后 9 例全绿。**规则 8 双向对拍**（`keepassxc-cli` 2.7.12）：本仓产物 `db-info` / `ls -R` / `attachment-export` 附件 SHA-256 一致；新增 fixture（官方 CLI 以本仓 v4 基线 `edit` 两轮改写、`<Value Ref="0">` × 6、KDBX 4.0）由本仓读通（**过程勘误**：`db-create` 实测产 KDBX **3.1**、本仓不读 ⇒ 改走该路径，已登记 `FIXTURE.md`）。文档同步：限界表新增 **§29**（只封单条目维度的残余，放大上限 `≤ 1024 × 池字节`，含边界 / 解除条件）+ 汇录 §附 补 §29 行并**顺带补登 §28 漏登行**、附录 A 增块；`ISSUE-P3-292` 后果③ / AC③ 按「单向闭环 + 复评义务」改写、`ISSUE-P3-295` AC③ 指针改指现行常量落点。验证：全量 `test --rerun-tasks --max-workers=1` **BUILD SUCCESSFUL in 2m 32s / 114 executed**，`count_test_results.py` = **`xml=371 tests=2579 failures=0 errors=0 skipped=13`**（§272 基线 369/2572 ⇒ **+2 类 / +7 例**，skipped 持平）；`BROKEN_MD_LINKS=0` / `RESOLVED_INDEX_SYNC=OK` / `check_recheck_consistency` PASS。**如实声明**：无设备、未改原生面 / `androidTest` / `参考项目/` / 构建脚本 ⇒ 无设备侧必跑项；未跑 `lint` / `assembleRelease` / 截图门禁 / `verify_interop.py`；**`pykeepass` 独立实现读数因沙箱敏感内容审批超时未执行**（不回推、不推断）；AC⑥ 证据边界一处：fixture 基线源自本仓探针产物、落盘与历史由官方写出，**未**取得「从零由官方产出同形态 v4 库」；「分散引用」放大面为算式上界而非实测读数（限界表 §29 已登记为接受残余） | `ISSUE-P1-276`（整条闭环） | [`273-附件池引用计费重定口径批次.md`](batches/273-附件池引用计费重定口径批次.md) |
