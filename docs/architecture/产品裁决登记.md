# 产品裁决登记（Product Decisions）

> **定位**：本仓「**已由用户裁决、且属产品 / 架构取舍而非缺陷**」的口径登记表。
>
> **与 [`ACTIVE_ISSUES.md`](../ACTIVE_ISSUES.md) 的分工**：待办清单只放**现存问题**（缺陷 / 缺口，
> 走「认领 → 整改 → 验证 → 归档」闭环）；本表放**已定下的取舍**——它们不是「待修的东西」，
> 而是「已决定不做 / 这么做」的口径，**重新打开的条件**写在每条的「重开条件」里。
> 把取舍混在待办清单里，会让清单长期挂着永远不动的条目，也会把「已决策」误读为「未处理」。
>
> **与 [`已知工程限界.md`](已知工程限界.md) 的分工（§256 定标）**：限界表放**两类**——**【客观限界】**
> （本仓不依赖上游 / 平台 / 硬件改变即无法消除）与 **【有意取舍】**（有残余或代价面，但本仓自己就能改，
> 只是判为「代价 > 收益」而刻意不做）；两者都属**已裁决不整改**，各节按「事实 → 边界 → 依据 → 解除条件」登记。
> 本表放**口径裁决**——「**要不要做某件事 / 按哪个判据算不算缺陷**」，**没有残余风险面**
> （`PD-11` / `PD-13` / `PD-16` / `PD-17` 与本次由限界表迁入的 `PD-18` ~ `PD-25` 均属此类）。
> `PD-18` ~ `PD-25` 系 2026-09-21 §256 由限界表**逐字迁入、结论零变更**；限界表对应节只留存根（含原规范句）。
> **另**：`PD-26` ~ `PD-29` 系 2026-09-22 §262 由 `ISSUE-P3-261`（全局转场动效定标）**新立**——
> 它们**不是**迁入项，而是该条目 AC① / ③ / ⑦ / ⑧ 要求「凡裁决为有意偏离的项必须登记」的当场裁决；
> 与 `PD-01` ~ `PD-17` 同类，均为**口径裁决**（无残余风险面），重开条件写在各自段内。
> `PD-30` ~ `PD-33` 系 2026-09-23 各批（§266 / §267 / §269）**当场新立**，同为口径裁决。
>
> **迁出说明**：本表三条由 `ACTIVE_ISSUES.md` 迁出（2026-09-17），原因即上述分工——
> 它们当时的形态是「已裁决暂缓」的待办条目，但**其内容全是产品取舍**，不是待修问题。
> 其中两条**真正的待修缺口**（KDF 弱参数导入无告警、同步能力文档口径不一致）迁出时留在
> `ACTIVE_ISSUES.md` 跟踪（`ISSUE-P2-87` / `ISSUE-P3-147`），**不随本表迁出**——两条均已于
> **同日 §110 批次闭环**（见 [`../RESOLVED_LOG.md`](../RESOLVED_LOG.md) §110；2026-09-22 §260 更正时态，
> 原文「已留在……继续跟踪」为迁移时点的陈述、现况已变）。
> **另**：本表的 `PD-04` 于同日由 `ISSUE-P3-146` **裁决后**登记——它**不是**「迁出的暂缓项」，
> 而是**当场作出的治理取舍**（不启用分支保护必需检查），其原委见该条正文与「分流去向」段之后的说明。
>
> **措辞纪律**：本表的措辞**本身就是结论**。改写措辞等同于变更口径，须走一次完整批次（归档 + 提交）。
>
> **参考对照来源**：[`docs/references/存量5项开放问题的参考项目对照.md`](../references/存量5项开放问题的参考项目对照.md)
> （5 家参考项目逐项取证对照）；前提核实见 [`docs/records/存量条目前提复核记录.md`](../records/存量条目前提复核记录.md)。

---

## PD-01 建库 KDF 默认强度维持既定值，不接入设备自适应建议值

- **对应历史条目**：`ISSUE-P2-79` AC②（AC① / AC③ 的处置见文末「分流去向」）。
- **口径**：建库默认维持 **`Argon2id m=64 MiB / t=2 / p=2`**
  （`database/src/main/java/com/keepasskey/database/file/KdbxHeader.kt:166-170`），
  **不**在建库路径消费 `KdfBenchmark` 的设备自适应建议值。读取路径对既有库的工作因子照收、
  保存时逐字保留（仅刷新盐 / 种子，`KdbxFile.kt:332,337`）。
- **理由（降强面）**：`KdfBenchmark` 的建议值是**设备自适应**的，直接接入会出现**降强**，
  且**三项都可能弱于现默认**——内存下界 `MIN_ARGON2_MEMORY_BYTES = 8 MiB < 64 MiB`（`KdfBenchmark.kt:44`）、
  迭代可低至 `1 < 默认 2`（`:97`）、并行度为 `cores.coerceIn(1,4)` 故可为 `1 / 3 / 4 ≠ 默认 2`（`:91`）。
  ⇒ 须**逐参数**取 `max(建议值, 现默认)` 才不降强；而「逐参数 max」后已不再是设备自适应，
  且该做法**超出全部参考项目**，实施须自证与官方客户端的互操作。
- **参考对照（可作维持现状的反证）**：官方 C# / KeePassDX / KeePassXC **都有 1 秒级 benchmark**，
  DX 与 KXC 在**建库路径自动消费**。但 DX 的 `KdfEngine.kt:79-83,96-105` 把 memory / parallelism
  **覆盖后当结果返回**，是一条**主动降强**路径 ⇒ 参考项目的自动校准**确有降强缺陷**，不构成必须照抄的理由。
  另：官方**建库默认是 AES-KDF 600k 轮**（不是 Argon2），所谓「强度基线」在三家之间**不在同一坐标系**。
  弱参数**导入告警**只有官方 C# 做了（默认开启，判定后由用户 Yes/No），**且其判据只比 `I×M`、忽略并行度 P**，
  照抄会漏维度——该能力已转登为 `ISSUE-P2-87`。
- **前提核实**：2026-09-16 对 HEAD `2e5ce38` 复核，本条所依赖的默认值、消费方唯一性、削弱陷阱**全部成立**
  （见 [`records/存量条目前提复核记录.md`](../records/存量条目前提复核记录.md) ②）。
- **重开条件**：产品确认「目标强度口径」（如「设备实测约 1 秒」），并同时定义**逐参数** max 规则，
  且以官方客户端互操作对拍自证不降强（`AGENTS.md` §3.8 互操作证据纪律）。
- **依据**：`RESOLVED_LOG.md` §76 / §78；`docs/records/存量条目前提复核记录.md` ②。

---

## PD-02 同步端点默认拒绝内网 / 自建，且不提供终端用户可配置入口

- **对应历史条目**：`ISSUE-P3-121` AC①（AC③ 的处置见文末「分流去向」）。
- **口径**：`SyncNetworkOptions.ssrfAllowedHosts` **维持默认空集**
  （`sync/src/main/java/com/keepasskey/sync/network/SyncNetworkOptions.kt:25`），
  RFC1918 / 链路本地 / ULA / `localhost` / `.localhost` / `.local` / `.internal` 等主机
  **一律被 `SyncEndpointGuard` 拒绝**；**不给出厂可用口径，也不提供终端用户可配置入口**。
- **理由**：SSRF 纵深防御的**默认收紧**——放开等于给「同步端点被指向内网」留口子。
- **⚠️ 登记语义更正（2026-09-16 复核，本表以更正后语义登记）**：原裁决理由称
  「需要内网同步的用户**可经既有显式白名单机制自行配置**」——**不成立**。
  `ssrfAllowedHosts` 仅是 `sync` 模块的**库级构造参数**：**无 UI 入口、无持久化键
  （`SyncCredentialsStore.kt:299-318` 键枚举中无此项）、无 DI 装配点**，全仓**无任何生产写入点**，
  唯一非默认赋值在单测（`SyncEndpointGuardTest.kt:179`）；归档文档
  `docs/resolved/batches/86-DAL出口纵深防御批次.md:17` 亦自述该通道为「**预留**」。
  ⇒ 本条的真实语义是「**内网 / 自建同步对终端用户彻底不可用**」，**不是**「默认收紧但高级用户可自配」。
  两者在产品定位与 README 文案上导向不同结论，故必须按更正后语义引用。
- **参考对照（无口径可援引）**：5 家参考项目**无一家做 SSRF 防护**——keepass2android / Monica /
  官方 C# **地址一律放开**（kp2a 的 SMB / SFTP 输入提示直接写 `192.168.1.10`），安全靠「明文 http 开关 /
  证书三态 / 逐 host SHA-256 指纹 TOFU 弹窗」；KeePassXC 把端点**外包给用户自定义命令**。
  ⇒ 本仓默认收紧是**全场最严、属有意偏离主流**，**不得**以「参考项目都放开」为由要求放宽；
  反之，也**没有**外部口径可援引为「必须收紧」的依据——这是本仓自己的选择。
- **重开条件**：产品要求支持自建 / 内网同步时，须新增「**用户显式声明内网主机 + 风险二次确认**」
  设置入口（默认仍为拒绝），并把 `ssrfAllowedHosts` 从预留参数接成真实可配置项；
  Monica 的证书 TOFU 弹窗（`WebDavCertificateDialog.kt:26-40`）是最接近的现成形态。
  实施时须同时给出默认拒绝与审计留痕证据。
- **依据**：`docs/records/存量条目前提复核记录.md` ①；`docs/references/存量5项开放问题的参考项目对照.md`；
  `docs/resolved/batches/86-DAL出口纵深防御批次.md:17`。

---

## PD-03 封印（快速解锁）凭据不绑定库 header 摘要

- **对应历史条目**：`ISSUE-P2-47` AC②（AC① / AC③ 的处置见文末「分流去向」）。
- **口径**：封印载荷**不**绑定「库 header 摘要 / 封印序号」。快速解锁的失效判定维持现状，
  **不**按原 AC② 原文实施。
- **理由（原 AC② 按原文不可实施，代码取证非推测）**：`KdbxFile.kt:337` 保存时
  `p.copy(salt = freshSalt)`、`:342` `masterSeed = freshMasterSeed`——**KDF 盐与主种子每次保存都重新生成**，
  故任何形式的「库 header 摘要」**每次保存都会变**；照 AC② 原文绑定 header 摘要，结果是
  **每次保存后快速解锁立即失效**（核心功能回归）。另：封印载荷持有的是**主密码**，
  「回滚到旧主密码版本」的旧口令本就解不开当前库（原条文亦自认「价值有限」）；
  而「库与封印被**一致**回滚」属**同步防回滚链**的职责，封印侧无参照可比。
- **参考对照（属自有加固，不是照抄）**：5 家参考项目**全无文件级防回滚**——keepass2android 只有
  单个 `baseversion` 摘要，「远端变 + 本地未变」直接静默 `LoadDb` 重载；官方 C# 只有会话内哈希 +
  保存前三选一；KeePassDX 用**单向** `mtime + 10s`（回退根本不触发）；KeePassXC 零远端版本概念；
  Monica 是唯一有单调序号 + 上一步摘要的（`conflicting digest for segment N` → 持久化 `blockedReason`），
  但依赖自有段协议、只回看一步。
  ⇒ 本仓「已见摘要链」**强于全部参考项目**。**不得**把本条读作「参考项目都没做所以不必做」，
  也不得读作「照抄某家做法」——它是**自有加固的取舍**。
- **同步防回滚的边界与现状**（5 处引擎裁决点、`sequence` 不参与裁决、状态缺失 / MAC 失效 fail-open）
  **登记在 [`已知工程限界.md`](已知工程限界.md) §5**，不在本表重复。
- **重开条件**：若确认必须抗「封印与库被**一致**回滚」，应在**同步防回滚链**层解决；
  若确要保留封印侧绑定，须改设计为绑定「**库文件内容摘要**」并在每次保存后刷新，
  代价是刷新窗口内需回退主密码解锁——属架构决策，须先取得裁决再实施。
- **依据**：`docs/resolved/batches/83-内层XML-DTD拦截设备侧前置断言批次.md`；
  `docs/resolved/batches/90-防回滚引擎级回归补齐批次.md`；`docs/records/存量条目前提复核记录.md` ③。

---

## PD-04 不启用「分支保护必需状态检查」——供应链闸门取**「检测」**而非「阻断」

- **对应历史条目**：`ISSUE-P3-146`（承 `P2-54` 的 AC②「把 `OWASP Dependency-Check` 设为分支保护必需状态检查」）。
- **口径**：`main` **不设**分支保护规则，因而**不**把任何检查设为必需状态检查。供应链闸门维持**「检测」**语义——
  工作流照常在 `push{main}` 与 `pull_request` 上运行、CVSS ≥ 7.0 硬断言 **fail-closed**、SARIF 进 Code Scanning，
  但**不阻断**推送与合并。**`P2-54` 的 AC② 据此判为「有意不实施」**（而非「未闭环」）。
- **一手核实（2026-09-17，仓库所有者于 GitHub 设置页）**：`main` **无任何分支保护规则**（既无经典保护规则、
  亦无 rulesets）。**仓库为公开仓库** ⇒ 分支保护在**计划层是可用**的，故本口径**不是**受计划限制，
  而是**主动做出的取舍**（勿读作「做不到」）。
- **理由（按重要性排列）**：
  1. **单人仓库下，保护规则对唯一行为者几乎不起约束**：官方文档明写
     「By default, the restrictions of a branch protection rule **don't apply to people with admin permissions**」
     ——不勾「Do not allow bypassing the above settings」时，管理员（即唯一提交者）**不受约束**；
     勾上则连自己的紧急直推也会被挡。
  2. **要真「阻断」就必须引入 PR 流程，与本仓工作流冲突**：官方文档对「必需检查是否也约束**直推**」的措辞**含糊**
     （同一段里既有 "before collaborators can make changes to a protected branch"，也有 "After all required
     status checks pass, any commits must either be pushed to another branch and then merged **or pushed directly
     to the protected branch**"）；**唯一不含糊**的阻断机制是「Require a pull request before merging」——
     而它会使 `AGENTS.md` 规则 6.4 的「整改后**立即** `git commit & push` 到 main」**不再可行**，
     属**流程改造**而非加一个勾。
  3. **闸门的实质价值已由工作流本身获得**：「检测」层（fail-closed 扫描 + CVSS 硬断言 + SARIF 上传）
     已在位且**同时在 push 与 PR 上运行**；分支保护额外买到的仅是「阻断」，而在单人仓库上（见理由 1）这层额外收益经不起理由 2 的代价。
- **不得**据此断言「仓库未受保护即无供应链闸门」（闸门是工作流，不是保护规则）；
  也**不得**据此断言「已受保护」。
- **重开条件（任一成立即重新裁决）**：① 出现**第二个提交者**（保护规则开始真正约束他人）；
  ② 引入**发布流程 / 版本 tag** 的保护需求；③ 发生一次「坏提交直推 `main` 且造成实际损失」的事故；
  ④ 需要把 `build` 工作流变成**编译硬门禁**（而不仅是安全扫描）。
  **重开时的完整清单**（须**四项同批**处理，且**必须实测一次「PR 被挡下」**，否则只能写「已启用、未实测阻断」）：
  ① 勾「Require a pull request before merging」；② 勾「Do not allow bypassing the above settings」；
  ③ 勾「Require status checks」并选中 `OWASP Dependency-Check`（其 job 名在**全仓唯一**，
  不触发官方提醒的「同名检查导致结果歧义并卡住 PR」）；④ **同批修改 `AGENTS.md` §6.4** 的「立即直推 `main`」口径。
- **依据**：官方文档 [About protected branches](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches)
  （管理员默认可绕过；「Require status checks before merging」与「Require a pull request before merging」的差别；
  必需检查的 job 名唯一性要求；分支保护在公开仓库的可用性）；
  `RESOLVED_LOG.md` §112；`ISSUE-P3-146`；
  [`docs/security/退役审计承接-43-安全整改方案与附录.md`](../security/退役审计承接-43-安全整改方案与附录.md) 的 `C-10`
  （本条同时给出该行的答案：分支保护不存在 ⇒ `build` 未被要求通过，`F-05` 的爆炸半径**不因分支保护而收窄**）。

---

## PD-05 库锁定填充：唯一强匹配改链确认页——已裁决（C）

- **裁决（2026-09-22）**：取**候选 C**——锁库状态经 Autofill 通道填充、解锁成功后，凡
  **唯一强匹配且调用方已完成 `android://` 首次绑定且本次识别到目标框**，改链
  `AutofillConfirmActivity`（保留调用方归属展示 + 单次生物识别或手动确认，由确认页回传真实
  Dataset）；其余一切情形（多候选 / 零候选 / 调用方未绑定 / 仅弱匹配 / 无目标框 / 判据计算失败）
  维持链入手动选择器 `AutofillPickerActivity`，与裁决前行为逐字一致。
- **落地内容（AC② 已实施）**：路由判据单点化为纯函数 `AutofillUnlockRouter.route`
  （`app/src/main/java/com/keepasskey/app/autofill/AutofillUnlockRouter.kt`，输入 = 候选集合 +
  绑定状态 + 表单上下文，输出 = `CONFIRM(唯一强匹配条目)` 或 `PICKER`；「强匹配」取自
  `AutofillCandidateRanker.MatchReason` 既有 exact 语义）；`AutofillUnlockActivity.chainToPicker`
  按其结论路由，`PICKER` 分支逐字保持旧构造，`CONFIRM` 分支携带确认页既有 extras 启动
  `AutofillConfirmActivity`，两条链的结果均经解锁页既有 launcher **原样转发**（`RESULT_OK + data`
  转发、取消 `RESULT_CANCELED + Bundle.EMPTY`）。解锁页**不自建 Dataset**（对候选 B 的禁令精神延续：
  数据集构造与回传只在落地页）。**本条已于 §261 批次闭环。**
- **跟踪沿革**：裁决作出前，本条的待裁决状态曾登记为 [`../ACTIVE_ISSUES.md`](../ACTIVE_ISSUES.md)
  `ISSUE-P3-256`（§260 补登；彼时本表只承载候选与裁决前口径）；裁决作出后随 §261 批次闭环归档，
  跟踪入口收口于本表。
- **对应发现**：2026-09-17「坏交互排查」（用户命题「本该一次点击就完成，却要点很多次，尤其密码填充」）。
  锁库状态下经 Autofill 通道填充：解锁成功后 `AutofillUnlockActivity.chainToPicker`
  （`app/.../autofill/AutofillUnlockActivity.kt`）**不看候选数量**，一律链入手动选择器
  `AutofillPickerActivity`（搜索 / 点选条目 → 有认证器时再过一次生物识别 → 回传真实 Dataset）。
- **交互成本**：站点 / 应用恰有 1 个严格匹配条目时，用户须完成「解锁（主密码或生物识别）→ 点条目 → 生物识别」；
  解锁页本身已是一次强认证，对刚输完主密码的用户，选择器内的生物识别是同一次填充意图里的**第二次认证**。
- **为什么是取舍而非缺陷**：选择器跳承担着数据集路径没有的安全职责——强制展示请求方归属
  （`ISSUE-P2-70`）+ 写入 `android://` 维度首次绑定（`ISSUE-P2-46`，未绑定调用方**唯一**的补救路径）+
  选中即显式授权（P2-24 语义）。若「唯一强匹配 + 调用方已完成首次绑定」时跳过选择器直达回传，
  等于把这三件事并入解锁页，安全面收窄须显式裁决，故**不登记为 ISSUE**。
- **原三候选（沿革，裁决日 2026-09-22 前的登记原文，均未实施）**：
  - **A. 维持现状**（每次解锁后进选择器）——最大安全面，交互成本最高；
  - **B. 直达回传**：「唯一强匹配 + 调用方已绑定授权」时解锁成功直接构造真实 Dataset 回传
    （解锁动作即确认）——一次认证，但归属展示与显式授权语义缺失；
  - **C. 链入确认页**：解锁成功后若存在唯一强匹配则改链 `AutofillConfirmActivity`
    （保留归属展示 + 单次生物识别 / 手动确认）——两次交互，安全职责基本保留。**← 本裁决所取**
- **历史沿革（原「裁决前口径」，已随 2026-09-22 裁决失效）**：裁决前维持现状 A，`chainToPicker`
  冻结不得改动、也不得在解锁页内自建数据集回传；裁决作出后该冻结解除，按候选 C 实施
  （「解锁页不自建数据集回传」的精神作为 C 的实施约束保留）。
- **重开条件**：
  - 用户改判要求「单次交互直达」（解锁即回传、免确认页）⇒ 重评候选 B 及其安全面收窄
    （归属展示、`android://` 首次绑定补救、选中显式授权三项职责的去向须逐项显式处置）；
  - 系统填充协议变化（认证结果回传 / 链式启动契约变更，例如框架不再接受经解锁页跨 Activity
    转发的 `EXTRA_AUTHENTICATION_RESULT`）⇒ 重评本路由与两条交付链。
- **依据**：`AutofillUnlockActivity` / `AutofillPickerActivity` 源码通读（2026-09-17）；
  `resolved/batches/` 中 P2-86（解锁后链入选择器的真机结论）与 P2-70 / P2-46（选择器安全职责）相关批次；
  2026-09-22 裁决记录与 `ISSUE-P3-256` 实施批次（§261）。

---

## PD-06 密码学实现收敛到 Rust 一份内核库，**不引入** Botan 等第二套密码学栈

- **裁决**：对称密码与 KDF 全部由 `crypto/src/main/rust/`（RustCrypto 官方 crate）提供；
  **不引入** Botan / OpenSSL / libsodium 等第二套密码学实现。
- **理由**：
  1. **效率无收益**：Android 平台已提供硬件加速对称密码（Conscrypt/OpenSSL → ARMv8 `aese`）；
     Botan 在 Android 上用的是**同一批指令**，既不会更快，也不会比现有 Rust 内核（同走硬件后端）快。
  2. **可维护性净负**：会形成**两套密码学栈** + 第二条原生构建链（CMake/NDK × 4 ABI）+ 第二套 JNI 桥；
     KAT / 对拍 / 真机记录 / 契约用例等验证资产大部分需重做（Rust 侧资产无法迁移到 Botan）。
  3. **供应链纪律冲突**：项目已入库闸门要求依赖树**无重复密码学实现**（§145/§146 `cargo deny` 四项全 ok）；
     Botan 是**一整个密码学栈**（含 TLS / 证书 / PKCS#11 等本项目不需要的面），引入即冲突。
- **参考对照**：KeePassXC 用 Botan 的前提是「**桌面 C++/Qt 多平台**，需要一份库覆盖
  Windows/macOS/Linux」；本项目是**单平台 Android**，该前提不成立。KeePassDX / 官方 /
  keepass2android 均为**混合形态**（AES 走平台或自家原生，Twofish / ChaCha20 走 BC 纯 Java 或托管），
  其代价是真机可测的性能与语义漂移（BC ChaCha20 实测 **2.7~40.9 MB/s**，对比 Rust 生产路径 404 MB/s）。
- **如实说明（不构成反对理由）**：Botan 为 2-clause BSD，与主工程 GPL-3.0 单向兼容，**许可证不构成障碍**。
- **重开条件**：出现 **iOS / 桌面等非 Android 分发目标**，且要求「一份密码学实现跨全部平台」。
  **即便届时**，也应先评估把现有 Rust 内核交叉编译过去（KAT 与契约资产可整体带走），而非引入 Botan。
- **依据**：参考对照 [`references/KeePassXC-架构分析.md`](../references/KeePassXC-架构分析.md) /
  [`KeePassDX-架构分析.md`](../references/KeePassDX-架构分析.md) §4.2 /
  [`KeePass-2.61.1-架构分析.md`](../references/KeePass-2.61.1-架构分析.md)；
  实测 [`records/真机吞吐实测记录_2026-09-17.md`](../records/真机吞吐实测记录_2026-09-17.md) §8。

---

## PD-07 流式语义保留**两套**（原生自研骨架 + 兜底 JCA 流），以对拍用例锁定等价

- **裁决**：原生路径用自研 `CbcStreams`（`CbcEncryptingOutputStream` / `CbcDecryptingInputStream`，
  64 KiB 分块 + `Pkcs7`）；兜底路径用 JCA 的 `CipherInputStream` / `CipherOutputStream`。
  **两者并存、不互相改写**——这一点与 KeePassDX「全部塞进 JCA、流语义只有一套」**有意不同**。
- **理由（为什么值得留两套）**：
  1. **性能**：JCA `CipherInputStream` 内部固定 **512 B** 缓冲，真机把解密压到 **15.3 MB/s**；
     自研骨架 64 KiB 分块达 **55.5 MB/s**（3.6×，§144 实测）。若为「只有一套」而全走 JCA，
     等于**主动放弃**该收益。
  2. **安全**：自研骨架承担明文零化纪律与 `ownedSecrets` 契约（§147），能表达 JCA 流表达不了的擦除时点。
- **代价（如实）**：两套流语义必须保持可观测等价。本项目以**刻意对齐 + 逐例锁定**承担该成本：
  `CbcDecryptingInputStream` 的 KDoc 明确「错误语义以实测的 `javax.crypto.CipherInputStream` 基线为准」，
  并由 `CbcStreamFramingTest`（与真实 JCE 逐例对照）与 `CipherFallbackParityTest`
  （强制兜底 ↔ 原生产物逐字节一致）**双重锁定**。
- **不该读作**：不得据此认为「两套流语义是免费的」——它是**用维护成本换性能与擦除时点**的取舍；
  新增流式路径必须同时补两套的等价用例。
- **重开条件**：若将来把两套合一（统一到 JCA + 自持缓冲，或统一到自研骨架），须先证明
  **不损失**上述性能与擦除时点两条，并完整保留等价性用例。
- **依据**：§144 批次（`ISSUE-P3-155` 解密侧分块流）、§147 批次；
  `crypto/src/main/java/com/keepasskey/crypto/cipher/CbcStreams.kt` KDoc；`CbcStreamFramingTest` /
  `CipherFallbackParityTest`。

---

## PD-08 通行密钥二维码导入：载荷采用 **FIDO CXF v1.0 单凭据 Passkey JSON**，入口为「相机扫码 + 相册识码」

- **裁决**（2026-09-18，用户命题「扫码添加通行密钥（添加照片的方式）」）：
  1. **二维码载荷** = FIDO Alliance **Credential Exchange Format (CXF) v1.0**（Proposed Standard
     2025-08-14，errata 2026-03-09）的单凭据 `Passkey` 字典 JSON：`credentialId` / `userHandle` /
     `key` 为 Base64URL，`key` 为 **PKCS#8 ASN.1 DER** 的 Base64URL 编码。解析器兼容三级形态：
     裸 Passkey 对象 → CXF 凭据数组 → CXF 完整文档（`collections[].items[].credentials[]`），取首个有效凭据；
     并**兼容 KeePassXC `.passkey` 单对象 JSON**（`relyingParty` / `privateKey`（PEM 包裹 PKCS#8）等
     6 必需字段，`src/gui/passkeys/PasskeyImporter.cpp` 口径）——KeePassXC 是本项目 Passkey schema 裁决者。
  2. **入口** = 条目编辑页通行密钥区块的**双入口**：相机扫码（复用 `SecureCaptureActivity` 受保护取景）+
     相册照片识码（Photo Picker + zxing 本地解码，无需相机权限）。
  3. **落库口径**（**2026-09-18 §149 批次更正**）：解析结果写入条目 **`KPEX_PASSKEY_*`** 自定义字段
     （KeePassXC / KeePassDX 口径，细则见下文 **PD-09**；原表述为「写入 `Passkey.*`」，
     与本仓同日确立的互操作 schema 相冲突，故按 PD-09 更正），私钥走受保护字段
     CharArray 链路、逐算法还原为**与生成侧完全一致的驻留格式**（原为 hex / Base64 标量口径，
     现统一为 **PKCS#8 PEM**），复用既有保存路径；URL / 用户名留白时以 rpId / userName 补齐；
     条目已有通行密钥时整体替换（KDBX 历史快照可回滚）。**导出二维码不在本期范围**（另行裁决）。
- **理由**：
  1. WebAuthn 场景的二维码（hybrid transport / caBLE）是**临时会话握手**，业界**不存在**「扫码导入
     通行密钥」的传送标准——二维码只是个容器，载荷格式才是裁决点；
  2. CXF 是 FIDO 唯一 sanctioned 的凭据交换格式，Chrome / 1Password / Bitwarden / Dashlane 已采用，
     KeePassXC 2.7.7+ 的 passkey 导入导出同源（`gui/passkeys/PasskeyImporter`）；
  3. CXF `key` = PKCS#8 DER 与本项目 `PasskeyData`（PKCS#8 语义私钥文本 + `ProtectedString`）**天然对齐**，
     无需第二套密钥编解码。
- **重开条件**：FIDO 演进出二维码专用 CXF profile，或业界形成事实上的 QR 传送标准时重评载荷包装。
- **后续演进路线（2026-09-18 补记）**：Google 已发布 Credential Manager **凭据传输 API**
  （`androidx.credentials:providerevents`，API 26+，1.0.0-alpha06），官方明确以**同一 CXF 标准**
  支持 `ExportEntry`（导出器）/ `ImportCredentialsRequest`（导入器）的同设备对等传输——
  本裁决的 CXF 载荷与其同源；后续可另立裁决接入官方传输 API（届时扫码导入仍覆盖
  跨设备 / 照片来源场景，二者互补）。
- **依据**：[FIDO CXF v1.0 PS](https://fidoalliance.org/specs/cx/cxf-v1.0-ps-20250814.html)（§3.3.12
  Passkey Dictionary、errata 版 §3.3.12 `key` = PKCS#8）；[Android 凭据传输指南](https://developer.android.google.cn/identity/sign-in/credential-transfer)；
  KeePassXC `PasskeyImporter.cpp` / `PasskeyExporter.cpp` 源码定点核实（2026-09-18）；
  [`references/KeePassDX-架构分析.md`](../references/KeePassDX-架构分析.md) §3（`privateKeyPem`）/本项目 `crypto`/`PasskeyKeyCodec` 驻留格式与
  `PasskeyAssertionActivity.decodePrivateKeyBytes` 的消费口径（2026-09-18 源码通读）。

---

## PD-09 通行密钥落库 schema 采用 **KeePassXC / KeePassDX 的 `KPEX_PASSKEY_*` + PKCS#8 PEM**（写入口径）

- **口径**（2026-09-18，§149 批次）：
  1. 写入键名一律 `KPEX_PASSKEY_RELYING_PARTY` / `_USERNAME` / `_USER_HANDLE` / `_CREDENTIAL_ID` /
     `_PRIVATE_KEY_PEM` / `_FLAG_BE` / `_FLAG_BS`（`1` / `0`）/ `_PRF`；`CREDENTIAL_ID` / `USER_HANDLE` /
     私钥 / PRF 秘密为**受保护字段**（与 KeePassXC `EntryAttributes`、KeePassDX `PasskeyEntryFields` 逐项一致）；
  2. 私钥文本形态 = **PKCS#8（PrivateKeyInfo）ASN.1 DER 的 PEM**（EC 使用**命名曲线** `secp256r1`）；
  3. 本仓扩展键（KPEX 无对应项，对其它管理器是无关属性）：`Passkey.Algorithm`（使热路径无需解密私钥即可判定算法）/
     `PublicKey` / `SignCount` / `UserDisplayName` / `CreatedAt`；
  4. **读入兼容三形态**：KPEX（新）→ v1 旧键 `Passkey.*`（既有库无需先迁移即可断言）→ 外部管理器条目
     （无扩展键时**纯字节嗅探**算法：先按 PKCS#8 `AlgorithmIdentifier` 的 OID，再按 v1 文本形态
     ——64 字符 hex → ES256、32 字节种子 → Ed25519，`ISSUE-P3-214`）。
- **更正（2026-09-21）**：本节边界原写「**未**用 `keepassxc-cli` / `pykeepass` / 真实 KeePassDX 端到端对拍」，
  该表述已被 §220 批次（`ISSUE-P2-210`）取代——`PasskeyInteropProbeTest` 产出的 `.kdbx` 已由
  `keepassxc-cli 2.7.12` 与 `pykeepass 4.2.0` **各自独立**读出（114 条判据全绿、退出码 0；2026-09-21 复跑通过），
  细则见 `已知工程限界.md` §4.3。**仍未覆盖**的是**交互链路**（系统 CM 选择器 UI / 真实 RP 登录）。
- **理由**：KeePassXC 是本项目 Passkey schema 裁决者（`AGENTS.md` §3）；KeePassDX 源码注释明写
  "field names from KeypassXC are used" ⇒ 三者共用同一 schema 才有**库文件级**互通；
  原自造键名 + hex/Base64 私钥使互操作性为零（且 KDoc 声称「对齐 KeePassXC / KeeWeb / KeePassDX」属**文档失真**，
  本次一并更正）。
- **重开条件**：KeePassXC / KeePassDX 变更 schema，或 FIDO 推出统一凭据存储规范。
- **边界（不得读作「互操作已全面验证」；2026-09-21 §220 后口径，2026-09-22 §260 按同节更正注定稿——原「未用 `keepassxc-cli` / `pykeepass` 端到端对拍」句作废）**：
  **文件级对拍已达成**——`keepassxc-cli 2.7.12` 与 `pykeepass 4.2.0` 各自独立读出、`cryptography` 独立解析 PEM，
  **114 条判据全绿**（细则见 [`已知工程限界.md`](已知工程限界.md) §4.3 与 [`../RESOLVED_LOG.md`](../RESOLVED_LOG.md) §220）；
  **仍未覆盖**的是**交互链路**（系统 CM 选择器 UI / 通知渲染 / 真实 RP 登录 / PRF 真实 RP 对拍），跟踪见 `ISSUE-P2-254`。
  老库中的 v1 旧条目**只在被断言触及（计数器修补）时就地迁移**（`ISSUE-P3-213`，§222），从未使用过的条目仍保持旧形态。
- **依据**：§149 批次；`core/.../PasskeyData.kt`（键名与保护位）、`crypto/.../PasskeyPkcs8Codec.kt`；
  KeePassXC `src/core/EntryAttributes.h`、KeePassDX `database/.../PasskeyEntryFields.kt`（定向只读核对）。

---

## PD-10 通行密钥特权浏览器白名单：内置**仅已取证浏览器**，其余由用户**逐项显式启用**

- **口径**（2026-09-18，§149 批次）：
  1. 内置白名单**由已取证指纹表派生**（`BrowserSigningFingerprints.TRUSTED`：每条注明来源与核实方式），
     不再手抄字面量；
  2. 其它浏览器（Firefox 之外的 Brave / Edge / Samsung / Vivaldi…）由用户在「特权浏览器白名单」设置页**逐项启用**，
     指纹**现场读取该包自身签名证书的 SHA-256**（与系统校验同源，不臆写常量）；
  3. 列表仅呈现能处理 `https` VIEW 意图的应用（`queryIntentActivities`，**不需要** `QUERY_ALL_PACKAGES`）；
  4. 默认**空集**（除内置已取证项）。
- **安全语义（如实声明）**：启用某个应用等于允许它**以任意 web origin 发起凭据请求**——这正是浏览器委派的固有语义
  （CM 通道只在调用方的应用/签名命中白名单时才返回系统背书的 origin）。因此默认关闭、逐项授权、
  换签名或同包名顶替立即失效（fail-closed）；该能力**不得**被描述为「零风险」。
- **理由**：不做用户可配置则非 Chrome 浏览器上通行密钥**完全不出候选**（origin 退化为 `apk-key-hash`，
  与 `https://<rpId>` 绑定条目不匹配）——这是功能可用性与授权面之间的取舍，故交由用户显式决定。
- **重开条件**：CM 通道出现**不依赖白名单**的系统级中立 origin 授权面（如系统统一维护的浏览器清单）时，
  改为消费该系统面并收窄本口径。
- **依据**：§149 批次；`app/.../passkey/CallingOriginResolver.kt`、
  `app/.../data/repository/PasskeyPrivilegedBrowserStore.kt`、设置页 `PrivilegedBrowserSettingsScreen`。

---

## PD-11 「声明式装配表」不计入 `ISSUE-P3-188` 第 3 目的长函数整改目

- **口径**（2026-09-18，§184 批次，**由用户裁决**）：
  1. **纯接线装配表豁免**：`ui/KeePasskeySettingsNavGraph.kt::keepasskeySettingsNavGraph`（252 行）与
     `ui/KeePasskeyNavGraph.kt::keepasskeyNavGraph`（164 行）**不计入**「≥100 行函数须降到 ≤50」这一目；
     第 3 目的待办数因此由 **27 → 25**；
  2. **`ui/KeePasskeyApp.kt::KeePasskeyApp`（202 行）不豁免**，仍留在待拆清单——它含真实分支逻辑，
     不属「同一段接线写长了」；
  3. 豁免**只免除「按行数拆它」这一动作**，不免除其**内容纪律**：装配表内的每一条
     `ViewModel::method` 接线仍须与所接屏幕的形参一致（新增屏幕一律「路由 + 接线」同处登记，不得旁指）。
- **实测依据**（§184，逐行分类计数，脚本口径：`composable(` 分支行 / `name = ` 具名实参 /
  `val x by …State` 绑定 / 注释 / 空行 / `if·when·for·return·throw` 逻辑行）：

  | 函数 | 总行 | `composable` 分支 | 具名实参 | 状态绑定 | 注释 | **逻辑行** |
  |---|---:|---:|---:|---:|---:|---:|
  | `keepasskeySettingsNavGraph` | 252 | 10 | 135 | 31 | 27 | **1** |
  | `keepasskeyNavGraph` | 164 | 9 | 50 | 0 | 16 | **0** |
  | `KeePasskeyApp` | 202 | 0 | 34 | 3 | 22 | **10** |

  前两者 **83% / 54% 的行是具名实参接线**，逻辑行 0~1（`SettingsNavGraph` 唯一一处是
  `if (enabled)` 形式的条件路由注册）；把它按屏幕拆开会得到 10 / 9 个「`hiltViewModel()` +
  一堆 `x = vm::method`」的转发函数——**行数指标变好，但「路由即文档」这一现场被拆散**，
  且每次给屏幕加一个形参就要跨两个文件改。`KeePasskeyApp` 的 10 处逻辑行则是
  语言 / 主题 `when`、路由守卫与 Tab 可见性判定，**属可独立命名与单测的逻辑** ⇒ 不豁免。
- **边界（不得据本条外推）**：
  1. 本条**只**覆盖「`NavGraphBuilder` 扩展函数」这一种装配表；任何屏幕 / 对话框 / ViewModel
     内的长函数**一律不适用**（含 `KeePasskeyApp`，本条明确排除）；
  2. 不得据本条把「长」当作「无需读」——装配表的正确性判据是**路由与形参一致**，
     改屏幕形参时仍须同步该表，遗漏即断链（属缺陷，不属本条豁免范围）；
  3. 本条与限界 §18 / §20 同为「**行数指标不是目的**」的三连裁定之一，
     **不得**合并解读为「长函数整改目可以随意豁免」。
- **重开条件**：任一装配表函数内的**逻辑行 ≥ 5**，或出现 `when` / 循环 / 状态写入
  （即开始承载业务分支而非接线）时，该函数**立即重新计入**第 3 目，并按 §178 配方把逻辑段下沉。
- **依据**：§184 批次；分类计数可用 `python tools/doc/logic_lines.py <文件> <函数名>` 复现
  （口径与其文档串里记的两个定位坑）；`AGENTS.md` §3 规则 4（工程规则见
  `.codebuddy/rules/engineering-rules.md`）；`ISSUE-P3-188` **已于 §213 整条结案**（剩余清单与拆分纪律随批次归档，
  见 [`../RESOLVED_LOG.md`](../RESOLVED_LOG.md) §213；2026-09-22 §260 更正原 `ACTIVE_ISSUES` 悬空指针）。

---

## PD-12 凭据拒绝页至多提供一条**就地授权**的补救动作，唯一目标为加入特权浏览器白名单

- **口径**（2026-09-20，§227 批次，**由用户裁决**）：
  1. 凭据拒绝页（`CredentialRejectionScreen`）在拒绝原因**确有用户可执行的解法**时，在页面内呈现说明句
     与两个选择：主动作**「添加到特权名单」**、次动作**「退出」**；无从下手的原因（`CREDENTIAL_ALREADY_EXISTS`
     这类正常结果、`MISSING_REQUEST` / `INTERNAL_ERROR` 这类系统或内部异常）**维持原布局**（仅退出），不得硬凑入口；
  2. 唯一受支持的补救动作是**将调用方浏览器加入特权名单**。**不得**提供「跳过通行密钥站点归属校验」
     （`skipDalVerification`，即设置页该开关；`ISSUE-P2-240` 前的界面文案误写为「跳过浏览器兼容层」，
     已更正）一类使本次请求通过、或削弱 DAL 核心防线的直达路径——
     把关闭防线做成一步操作，等于给出绕过「任意应用冒充任意域名注册 Passkey」防线的捷径；
  3. 动作**全程在当前受保护窗口内就地完成，不跳转任何其它界面**，彻底消除跨 Activity 路由传递带来的
     DoS 面与未解锁绕过风险；
  4. **严格的资格守卫（fail-closed）**：调用方必须是系统背书的包名、且必须是已安装的浏览器候选
     （能处理 https VIEW 且现场可读签名证书）——**原生 App 一律不呈现该动作**（原生 App 无论加不加白名单
     都无法通过 DAL，给它这个入口就是误导用户）；已在名单里的浏览器亦不呈现；
  5. **本次请求仍然必然失败**：即使用户点了「添加到特权名单」且授权成功，本次创建的 origin
     在拉起时早已被系统解析为 `apk-key-hash`，无法中途变更为 web origin。授权成功后页面就地提示
     「已添加，请回到浏览器重新发起创建」，收尾仍回传 `RESULT_CANCELED`。它改变的是**下一次**发起。
- **理由（为什么这与既有纪律完全自洽）**：
  1. 完全遵守「凭据窗口不提供本应用内导航」的既有取向（`CredentialUnlockPresenter` 等三处 no-op）——
     **没有跳转任何应用内界面**，窗口级 `FLAG_SECURE` 与反悬浮窗加固全程闭环覆盖；
  2. 它也不构成 §226 所否定的「让本次请求通过的第二条出路」：本次被拒请求**仍然必然失败**，
     对系统契约依然是 `RESULT_CANCELED`，授权只作用于下一次发起；
  3. 相比「跳转设置页」方案，就地授权免去了用户跳去设置、找入口、找应用、再切换回浏览器的多步操作，
     直接减少了跳转与操作成本（用户命题原文「减少跳转和操作」）。
- **边界（不得据本条外推）**：
  1. 仅覆盖**调用方确为浏览器**的 DAL 门禁拒绝；不得据此为原生 App 提供任何绕过 DAL 校验的入口；
  2. 不得据本条把「用户授权」做成「让本次请求通过」——任何影响**本次**请求结果的入口
     （重试、跳过校验、降级）都不在本条授权范围内；
  3. 「至多一条」是硬上限：出现第二种补救动作时须重裁（见重开条件），不得直接追加按钮。
- **重开条件**：系统 Credential Manager 提供运行时动态刷新 origin 的中立能力（使本次请求可原地生效）时，
  重新裁决是否允许就地重试。
- **依据**：§227 批次；`app/.../passkey/CredentialRejectionAction.kt`、
  `app/.../passkey/BrowserRemedyBuilder.kt`、`app/.../passkey/BaseCredentialActivity.kt`、
  `app/.../passkey/PasskeyCreateActivity.kt`；`ISSUE-P3-221` **已于 §227 结案**（[`../RESOLVED_LOG.md`](../RESOLVED_LOG.md) §227；
  三项真机核验余量补登 `ISSUE-P3-255`，2026-09-22 §260 更正原 `ACTIVE_ISSUES` 悬空指针）。

---

## PD-13 完整性风险升级**不**触发活动会话熔断：以「提示 + 后续通道门控」为准（**2026-09-20 已裁决 · 方向 A**）

- **对应条目**：`ACTIVE_ISSUES.md` `ISSUE-P2-232`（本条为其**前置裁决**；裁决后该条目按「不实施」收口迁出）。
- **裁决**：**A —— 维持现状**。`RuntimeIntegrityReport` 的消费面保持现有四点（设置页风险提示、
  生物识别快速解锁门控、自动填充 / CM 通道门控），**不**新增 `COMPROMISED → triggerLock` /
  `clearSensitiveData` 接线。
- **裁决理由（含一处对原「误伤形态」登记的关键补正）**：
  1. **`COMPROMISED` 的构成不支持「熔断」这一处置粒度**。`RuntimeIntegrityPolicy.evaluate` 的分档依据
     是五个信号的**或**，其中**三项是纯「文件存在性」检查**：`rootArtifactsDetected`（11 条 `su` /
     Superuser 路径）、`magiskDetected`（6 条 Magisk 痕迹路径）、`hookFrameworkDetected` 的**落点层**
     （5 条 Frida 落点）。⇒ 按候选 B / C 字面实施，**所有 root / Magisk 设备**以及**装过 frida
     但未运行**的设备，会在解锁后 **≤30 s**（`LIVE_RESCAN_INTERVAL_MS`）被强制锁库；本仓**无草稿自动保存**，
     未保存的条目编辑即丢失，且每次解锁都复发 ⇒ 本应用对这类设备**实质不可用**。
     原登记只写出了 frida 落点这一面，**遗漏了 root / Magisk 这一更广的同性质面**——
     本轮直读 `RuntimeIntegrityDetector.detectSignals()` 与 `RuntimeIntegrityPolicy.evaluate()` 时发现
     （2026-09-20，核实方式：直读两函数全文 + `IntegritySignals` 字段构成）。
  2. **熔断的安全增益受探测上界限制**。探测周期为 30 s，而 attach 后的内存 dump 以秒计 ⇒ 熔断主要拦
     「**持续附着**」型对手，对首次 dump 无覆盖；它缩短的是暴露窗口，不是阻止已发生的读取。
  3. **既有门控已覆盖「后续通道」**。四条消费点均 fail-closed：攻击者被探测到后拿不到新解封的主密码、
     也拿不到新下发的自动填充 / CM 凭据。残余仅为「已解锁会话的内存树不被主动驱逐」，
     已按残余风险登记于 [`已知工程限界.md`](已知工程限界.md) §26。
  4. **「只对确证注入熔断」需先动等级语义**：要区分「确证已注入」（`/proc/self/maps` 命中、
     `TracerPid > 0`）与「静态痕迹（文件存在）」，必须**拆分现有信号**——`hookFrameworkDetected`
     当前把 maps 层与落点层合并为一个 `Boolean`——并新增熔断判据；这属安全口径变更，会影响既有等级矩阵
     用例与多处声明，且其验收（真机注入后 ≤1 重扫周期内锁库 + 负例不熔断）**必须真机**。
     裁决时（2026-09-20）经 `android-cli info` 核实**本环境无可用设备** ⇒ 不具备合规闭环条件。
- **残余风险**：已登记于 [`已知工程限界.md`](已知工程限界.md) §26。
- **重开条件（三条须同时满足）**：
  1. 等级语义先细分出「**确证即时附着**」信号（maps 命中 / `TracerPid > 0`），与「静态痕迹（文件存在）」
     分离，且既有各条门控口径同步重述；
  2. `beingTraced` 进入**周期重扫快照**（当前 `detectSignals()` 不采该信号，仅非 suspend 门控经
     `escalateForLiveSignals` 实时求值）——否则被动熔断看不到「持续附着」；
  3. 具备真机条件，以完成「注入后 ≤1 重扫周期内锁库 + 敏感缓冲清零」与「`ELEVATED` 负例不熔断」
     两条设备侧验收。
- **依据**：`app/.../security/RuntimeIntegrityPolicy.kt`（`evaluate` 五项分档）、
  `app/.../security/RuntimeIntegrityDetector.kt`（`detectSignals` / `detectHookFramework` /
  `HOOK_TRACE_PATHS` / `LIVE_RESCAN_INTERVAL_MS = 30_000`）；
  `app/.../security/AutoLockSessionGuard.kt:40-76`（既有熔断仅由熄屏 / 后台超时驱动）；
  [`已知工程限界.md`](已知工程限界.md) §3.5 / §26；
  [`docs/records/运行完整性检测Frida实测基线.md`](../records/运行完整性检测Frida实测基线.md)。

---

## PD-14 Java 依赖面**不**引入完整性锁定：以「来源收口 + 精确版本 + 已知 CVE 闸门」为替代缓解（**2026-09-20 重开决策**）

- **对应条目**：`ACTIVE_ISSUES.md` `ISSUE-P2-231`（本轮为其 AC① 要求的**重开决策**；裁决后该条目
  按「不实施」收口迁出）。
- **覆盖的既有裁决**：`docs/resolved/batches/87-CI产物可见性与供应链硬化批次.md` 已作「本批不引入」，
  理由为「生成完整元数据需在干净环境重写全量依赖图哈希」。本次重开**维持该结论**，但**不是**静默沿用
  ——已逐项复核实施条件（见「裁决理由」）。
- **新证据（评估输入，不改变结论）**：`SECURITY_RECHECK_2026-09.md` §6.8 `SUPPLY-02` 复审结论为
  **`CONFIRMED VULNERABILITY` / MEDIUM**，登记短链「投毒传递依赖 → 未被发现 → 未被校验 → 任意代码执行」
  （前提是上游投毒，**当前无证据**）；§7 `CHAIN-E` 将其列为「构建到分发的全链信任缺失」的组成环。
- **裁决**：**仍不引入**——既 `verification-metadata.xml`（全量 sha256），也不引入 dependency locking。
- **裁决理由**：
  1. **`verification-metadata.xml` 的信任根必须来自可信环境**。Gradle 官方路径是在**干净 CI runner**
     上首次生成并提交；在开发机生成等于把**本机缓存内容**固化为「信任根」（TOFU 固化），
     一旦缓存曾被污染，反而制造虚假安全感。2026-09-20 经本环境核验：**无 CI 通道且推送不可用**
     （同 `§225` 批次如实声明），既无法在干净环境生成，也无法完成条目 AC③ 明令的「CI 实跑通过」
     ——供应链改动**不得**只凭宿主单测判定。
  2. **dependency locking 的收益面窄**。本仓版本声明已全部为**精确版本**
     （`gradle/libs.versions.toml`，2026-09-20 由新增守卫机检：无 `+` / `latest.*` / `SNAPSHOT` / 区间），
     传递依赖版本由各上游 POM 固定 ⇒ 「版本漂移」面极小；且它对条目所述威胁链
     （**同版本内容被替换**）**完全无效**——只有内容哈希覆盖那一面。
  3. **半成品闸门的代价明确**：条目 AC② 明言「否则该闸门将成为长期红灯源」；在无 CI 验证通道时引入，
     等于把一条无法验证的构建约束留在仓库里。
- **替代缓解口径（当下可执行、可机检，均已在 2026-09-20 落地）**：
  1. **已知 CVE 闸门保持 fail-closed**：`dependencyCheck` + `.github/check_dependency_cvss.py` 的
     CVSS ≥ 7.0 硬断言不变（条目 AC④ 明令不得放宽；扫描面口径见 [`已知工程限界.md`](已知工程限界.md) §21）。
  2. **依赖来源收口**（已具备）：`settings.gradle.kts` 显式 `FAIL_ON_PROJECT_REPOS`，仓库仅
     `mavenCentral()` / `google()`，项目内不得私自声明仓库。
  3. **构建可重现性机检**（2026-09-20 新增）：`DependencyResolutionDeterminismTest` 断言版本目录不含
     动态版本 / 快照 / 区间，并自带「防空扫 + 判别力反校」两条不变式——它拦住投毒链里最易被忽略的
     「**发现**」环节（上游发布即被自动采纳，且变更**不出现在本仓任何 diff 中**）。
  4. **依赖变更可见性纪律**：一切依赖坐标经 `gradle/libs.versions.toml` 集中声明并在批次文档留痕；
     禁止在模块构建脚本内内联版本字面量（属纪律项、未机检——插件 DSL 上的合法内联例外会使正则判据
     退化为「为机检而机检」，故不写）。
- **重开条件（两条任一）**：
  1. 具备**干净 CI runner + 推送通道**，可按 ① 在 CI 生成 `verification-metadata.xml` 并提交，
     同批交付「新增依赖时的更新流程」（脚本或 CI 步骤）+ 写入幂等自检 + CI 实跑证据；
  2. 上游 Gradle 提供无需预置信任根即可校验构件来源的机制（可用的透明日志 / 签名验证闭环）。
- **依据**：`docs/resolved/batches/87-CI产物可见性与供应链硬化批次.md`；
  `SECURITY_RECHECK_2026-09.md` §6.8 `SUPPLY-02` / §7 `CHAIN-E`；
  `gradle/libs.versions.toml`、`settings.gradle.kts`、`.github/check_dependency_cvss.py`；
  `app/src/test/.../security/DependencyResolutionDeterminismTest.kt`（2026-09-20 新增，定向实跑通过）；
  [`已知工程限界.md`](已知工程限界.md) §21。

---

## PD-15 运行环境完整性检测改为**用户可配置开关**，出厂默认**关闭**（**2026-09-21 用户裁决**）

- **对应条目**：`ISSUE-P3-236`（同轮登记并整条结案）。批次正文
  [`239-运行环境完整性检测开关批次.md`](../resolved/batches/239-运行环境完整性检测开关批次.md)。
- **口径**：
  1. `UserSettings.integrityCheckEnabled` **出厂默认 `false`**（持久化键 `integrity_check_enabled`，
     读回缺省同为 `false`）；
  2. **关闭只解除降级，不改变判定**——`RuntimeIntegrityPolicy.evaluate()` 在关闭态仍产出真实
     `level` / `signals`（Root 命中照样报 `COMPROMISED`），只是 `enforcement` 全放行、
     `requireRiskNotice = false`、`biometricBlockReasons` 为空；
  3. 关闭后 `RuntimeIntegrityGate` 的两个入口（`currentEnforcement` / `awaitEnforcement`）
     **恒返回 `IntegrityEnforcement.ALLOWED`**，且**连「未判定 / 快照已陈旧」的 fail-closed
     保守分支一并绕过**；
  4. **关闭不停探测**：30 s 周期重扫照常运行，用户重新开启时快照已是真值；
     `ISSUE-P2-08` / `P3-83` / `P3-120` 的全部探测能力与判据**一行未删**，开启即完整恢复。
- **理由**：该检测自 `ISSUE-P2-08` 起恒开且零配置入口，对**自有且已 Root（含 Magisk）**的设备
  等于把指纹快速解锁永久锁死，而用户既看不到「为什么」也无法选择；这类设备恰是密码管理器的
  常见用户群（自建同步 / 定制 ROM）。本裁决与 `PD-13` 的补正同源：`COMPROMISED` 的五项构成中
  **三项是纯文件存在性检查**（root 落点 / Magisk 痕迹 / frida 落点），原口径不区分
  「设备姿态」与「正在发生的攻击」。
- **风险声明（产品已接受，**不**等于「无风险」）**：默认关闭 ⇒ 出厂状态下，Root / Magisk /
  调试器附加 / 注入框架环境**不再阻断**指纹快速解锁与自动填充，检测到的风险也不再触发任何降级。
  设置页「运行环境完整性检测」开关下**常驻**代价说明（`sec_integrity_check_off_notice`）作为
  用户可见披露；在**他人设备 / 用途不明的设备**上建议开启。
- **不得外推的边界**：
  1. 本裁决**不**授权放宽 [`已知工程限界.md`](已知工程限界.md) §3.5 的两项安全参数
     （`LIVE_RESCAN_INTERVAL_MS` / `SNAPSHOT_STALE_AFTER_MS` 不得为省电放宽）；
  2. 本裁决**不**解除限界表中与完整性探测相关的既有残余风险登记；
  3. 「默认关闭」是**产品口径**，不得被引用为「该检测无价值 / 已废弃」——探测面与判据完整保留。
- **重开条件**：产品要求恢复「默认开启」（如需面向高危合规场景的出厂设置），或引入**分级开关**
  （把「环境姿态信号（root / Magisk）」与「动态攻击特征（调试器 / `ptrace` / 注入框架）」拆成
  可独立配置的两项）时，须走一次完整批次（含默认值迁移与既有安装的偏好迁移说明）。
- **前提核实（2026-09-21）**：该检测此前**确无**任何偏好键 / `BuildConfig` 字段 / UI 入口
  （全仓 grep `integrityCheckEnabled` 零命中；`data/repository` 与 `app/build.gradle.kts` 无相关项）；
  门控消费点确为三处——`BiometricAuthManager.authenticate`、`KeePasskeyAutofillService`、
  `KeePasskeyCredentialProviderService`，均经 `RuntimeIntegrityGate` 抽象。
- **依据**：批次正文 [`239-...`](../resolved/batches/239-运行环境完整性检测开关批次.md)；
  `app/.../security/RuntimeIntegrityPolicy.kt`、`.../security/RuntimeIntegrityDetector.kt`、
  `.../data/repository/SettingsRepository.kt`；
  `app/src/test/.../security/RuntimeIntegrityCheckSwitchTest.kt`（13 例）；`PD-13`。

---

## PD-16 不新增独立的「跳过浏览器兼容层」偏好项：措辞漂移按**真实语义**更正（**2026-09-21 裁决**）

- **对应条目**：`ISSUE-P2-240` AC②（同轮闭环；批次正文
  [`243-设置页DAL降级开关文案更正批次.md`](../resolved/batches/243-设置页DAL降级开关文案更正批次.md)）。
- **口径**：
  1. `skipDalVerification`（偏好键 `skip_dal_verification`）只保留**一条**语义——**通行密钥注册侧**
     对调用方的 `DigitalAssetLinksVerifier` 远程归属声明校验降级，默认关闭；界面文案即
     「跳过通行密钥站点归属校验」；
  2. **不新增**「跳过浏览器兼容层 / 不通过浏览器兼容适配直接填充原生表单」偏好项——该行为在本应用
     **既无实现也无消费方**（minSdk 36 下 `AutofillService` 是唯一官方填充通道，不存在「浏览器兼容层」
     实体），新增即 `ISSUE-P2-228` 同类的**假开关**；
  3. 该开关的**界面文案**与 `DigitalAssetLinksVerifier` KDoc / `ExtendedSettings` 字段注释 /
     `PasskeyCreateActivity` 告警日志**逐字同锚**，由 `DalSkipSwitchCopyWiringTest`（7 例）机检锁定。
- **理由**：整改前的界面文案描述的是另一件事，直接后果是 `DigitalAssetLinksVerifier` KDoc 所述
  「用户如确有离线注册需求，可经设置中显式授权降级」的入口**用户看不到名字**；而 DAL 正是
  「任意应用冒充任意域名注册通行密钥」的核心防线 ⇒ 用户既无法知情也无法自主选择。
  该形态与 `ISSUE-P2-228`（通道假开关 + 文案谎称需要无障碍）同类，属**措辞漂移**而非缺功能。
- **边界（不得据本条外推）**：
  1. 本条**不**为「跳过校验」增加任何新入口（凭据拒绝页仍按 `PD-12` 只提供加白名单一条补救动作），
     亦**不**改变 `skipDalVerification` 的默认值（维持关闭）；
  2. 本条**不**改变该开关的作用域——仍只作用于 Passkey 注册侧，自动填充侧无对应开关
     （`ISSUE-P3-124` ④ 的留痕结论，`DigitalAssetLinksVerifier` KDoc 已载明）；
  3. 更正文案**不等于**放宽判定：`PasskeyRegistrationGate` 各分支与 fail-closed 口径一行未改。
- **重开条件**：产品确实需要独立的「浏览器兼容层 / 原生表单直填」行为时，须**先落地该行为与其生产
  消费方**，再作为独立偏好项另行接线，**不得**复用 `skipDalVerification` 字段。
- **依据**：批次正文 §243；`app/src/main/res/values/strings.xml`、`values-en/strings.xml`、
  `app/.../passkey/DigitalAssetLinksVerifier.kt`、`app/.../passkey/PasskeyRegistrationGate.kt`、
  `app/.../ui/screens/settings/ExtendedSettings.kt`；
  `app/src/test/.../passkey/DalSkipSwitchCopyWiringTest.kt`；`PD-12`。

---

## PD-17 「移除密码库」对**应用私有库**维持「移除即永久删除」，以**照实声明**消除误导（**2026-09-21 裁决**）

- **对应条目**：`ISSUE-P1-241` AC③ 的二选一（同轮闭环；批次正文
  [`246-移除密码库确认文案与真实行为一致批次.md`](../resolved/batches/246-移除密码库确认文案与真实行为一致批次.md)）。
- **口径**：
  1. **应用私有目录（`filesDir`）内的库**：移除动作 = **永久删除该文件**，删除后不可恢复；
     确认弹窗按此**照实**表述（红色危险按钮 + 正文指名被删文件 + 明示不可恢复），
     **不改**成「只移除关联」；
  2. **外部来源的库**（`content://` / 应用外路径）：维持「仅摘除本机登记、不动物理文件」，
     文案与按钮与上面那套**三处措辞全不相同**；
  3. 两类的判定为纯函数 `VaultRemovalKind.of(条目 path, filesDir)`（无 I/O），
     文案与动作由同一枚判据投影，且**同一枚判据**下行给数据层决定是否真删文件。
- **理由（技术事实，非偏好）**：
  1. **私有库不存在「只移除关联」这个状态**：列表由 `VaultDatabaseCatalog.buildDatabaseList`
     **按目录扫描** `filesDir/*.kdbx` 构建，判定只认文件本身、登记表里没有内部库的条目 ⇒
     若「移除」不删文件，该卡片在**下一次刷新必然原样回来**——那不是「移除」，而是「按了没反应」；
  2. 走「不删文件」就必须新增一层「已忽略文件」持久化与扫描差集逻辑，且默认动作变成**静默无效**
     （用户以为移除了、实际只是被隐藏）；破坏性动作并未消失，只是被挪到第二层入口，
     却新增了一条「看起来成功其实没动」的默认路径；
  3. 本条目的根因是**「界面说的和实际做的不一样」**，不是「删了文件」——对私有库而言删除是让它
     从列表消失的唯一动作，故正确处置是照实说，而不是把动作改成不生效。
- **不得外推的边界**：
  1. 本条**不**授权对私有库以外的对象做任何删除（外部库的移除一行文件操作都不做）；
  2. 本条**不**改变删除动作的**判定方向**：`kind` 与 `id` 不一致时最坏结果是「该删的没删」，
     **绝不允许**出现「不该删的删了」；
  3. 本条**不**把 `<库>.kdbx.bak`（`AtomicFileWriter` 滚动备份、属用户数据）纳入删除或保护承诺——
     它**不随删除移除**且应用内无入口读回，故**不得**在界面文案里把它写成恢复路径。
- **重开条件**：产品要求「私有库也必须可无痕下架」（即提供一层「已忽略库」持久化使其从列表隐藏、
  文件保留）时，须**先落地该层状态与其差集语义**，并把默认动作明确为「不删文件」，
  再另行裁决「删除文件」入口的位置与二次确认强度；届时须同批更新本条与批次 §4 的结论。
- **依据**：批次正文 §246；`app/.../ui/model/VaultRemovalKind.kt`、
  `app/.../ui/screens/database/VaultRemovalPresentation.kt`、
  `app/.../data/repository/VaultLifecycleCoordinator.kt`（`removeDatabase`）、
  `app/.../data/repository/VaultDatabaseCatalog.kt`（`buildDatabaseList`）；
  `app/src/test/.../database/VaultRemovalPresentationTest.kt` / `VaultRemovalWiringTest.kt`；
  `ISSUE-P2-240`（`PD-16`，同类「措辞与真实语义」缺陷的先例）。

---

## PD-18 保存路径的整树重建与附件字节副本是**别名隔离契约的代价**

- **对应条目**：`ISSUE-P3-150`（§115 复核）；与 `ISSUE-P3-07` / `ISSUE-P1-9` 的别名隔离契约相关
- **原登记位置**：`已知工程限界.md` §1.7（2026-09-21 §256 迁入，**结论零变更**）。
- **口径**：`KdbxBinaryDeduplicator` 在每次保存时**无条件重建整棵树**（逐组 `group.copy`、逐条 `entry.copy`），
  并对每个**内存**附件交付独立副本（`pooled.data.copyOf()`）。这两件事都**不是**可随手省掉的开销，而是 ISSUE-P3-07 / P1-9 确立的**别名隔离契约**：
  池为唯一权威源，外部按 `Closeable` 契约 `attachment.clear()` **不得**波及二进制池。
- **同批确认的真实成本面（已修）**：落盘附件的 `contentHash` 是**实例级** `lazy` 缓存，而 `candidateItem` 原恒以 `withFlags(...)` 新建实例 ⇒
  缓存每次保存作废 ⇒ 每次保存重新读盘全部落盘附件；已改为 `flags` 未变时复用来源实例（读回次数恒为 1）。
- **不得外推的边界（⇒ **不得**据此判定「这是可随手删掉的浪费」）**：`ISSUE-P3-150`（§115）曾以「纯浪费」为由要求删掉这两处，复核后**推翻**——省下的只是若干对象分配（内存附件亦 ≤
  落盘阈值
  1 MiB），代价是放宽一条**被用例守护的所有权保证**（`KdbxBinaryDeduplicatorTest.testAttachmentClearDoesNotZeroBinaryPool`）。
  该用例是硬护栏：任何「复用原附件实例以省分配」的改法都会使它变红。
- **依据**：`docs/resolved/batches/115-IO缓冲与写出批量化及落盘附件重复读盘批次.md`；
  `KdbxBinaryDeduplicator.kt`（`deduplicateEntry` / `candidateItem` 就地 KDoc）；迁出留痕见批次正文 §256。

---

## PD-19 OTP 取码的 `Mac` 实例复用**刻意不实施**

- **对应条目**：`ISSUE-P3-172` ④
- **原登记位置**：`已知工程限界.md` §9（2026-09-21 §256 迁入，**结论零变更**）。
- **口径**：`OtpEngine.calculateHotp` 每次取码都 `Mac.getInstance(...)` + `init(...)`。
- **理由（不是「做不到」，是取舍）**：复用 `Mac` 意味着该实例**长期持有** `init` 传入的 `SecretKeySpec`（内含**密钥字节副本**）；而调用方（TOTP 种子链路）用毕
  `fill(0)` 擦除自己那份的语义前提正是「自己手上那份即唯一副本」。
- **不得外推的边界**：**⇒ 不得**据本表重提「把 `Mac` 缓存复用」；若日后确需优化该路径，须走**不扩大密钥 / 明文驻留面**的方向（如内核下沉、可取消派生），并**另立条目**。
- **重开条件**：出现「无需实例持有密钥态」的等价原语（可在不保留密钥态的前提下复用 provider 句柄），或该路径被**真机实测**确认为瓶颈（需吞吐数据支撑，不得凭推断）。
- **依据**：`docs/resolved/batches/121-零风险局部项与守卫用例批次.md` §3；
  `core/src/main/java/com/keepasskey/core/otp/OtpEngine.kt`；迁出留痕见批次正文 §256。

---

## PD-20 同步「内容是否变化」与合并「是否被修改」两处判定口径**刻意不同**

- **对应条目**：`ISSUE-P2-91`
- **原登记位置**：`已知工程限界.md` §10（2026-09-21 §256 迁入，**结论零变更**）。
- **理由**：`KdbxTimes` **混装**内容语义（`expires` / `expiryTime`）与使用性字段（`lastAccessTime` / `usageCount`）及
  `lastModificationTime`，而 `KdbxEntry.withField` 经 `withModified()` **一次性改写三者**。
- **不得外推的边界（如实声明，勿外推）**：以上结论针对**本仓生产代码的写入面**。**限定**：分组侧**未**比较 `isExpanded` / `defaultAutoTypeSequence` /
  `enableAutoType` / `enableSearching` / `lastTopVisibleEntry` / `previousParentGroup`
  六个字段——`KdbxGroup.kt:14-19` 对 `KdbxContentComparator.kt:55-63`；现生产代码对这六字段**无编辑写入者**，故今日不可达，但**不得**把 ①
  读作「全部内容字段」。
- **⇒ 不得**据本表以「口径漂移」为由在**任一侧**「顺手对齐」（两侧 KDoc 均已就地声明差异与理由）。
- **重开条件**：① 出现**只改 `times`** 的生产写入路径（如新增「编辑到期时间」功能，或 `expires` / `expiryTime` 获得产品入口）——届时该判定须纳入 `expires`
  / `expiryTime`（**仍不得**纳入使用性字段与 `lastModificationTime`）；② 或引入「等条数原地改写历史」的路径——届时历史须改内容级比较。
- **依据**：`docs/resolved/batches/122-同步变化判定口径声明与语义锁定批次.md`；
  `app/src/main/java/com/keepasskey/app/sync/KdbxContentComparator.kt`（类 KDoc）；
  `sync/src/main/java/com/keepasskey/sync/merge/KdbxEntryMerger.kt`（`isModified` KDoc）；
  `app/src/test/java/com/keepasskey/app/sync/KdbxContentComparatorTest.kt`（6 例语义锁定）；迁出留痕见批次正文 §256。

---

## PD-21 基线快照与缓存快照**各写一份**

- **对应条目**：`ISSUE-P3-167`
- **原登记位置**：`已知工程限界.md` §11（2026-09-21 §256 迁入，**结论零变更**）。
- **口径**：`advanceBaseAndPersist`（`sync/src/main/java/com/keepasskey/sync/engine/SyncEngineSupport.kt`）
  **只写**基准快照 `<hash>.basecache`（`SyncCache.writeBaseContent`）；`<hash>.cache`
  由**调用方**在内容变更路径另写（`SyncEngine.kt` 多处）——`recoverFromMetaFailure` 的远端 404 恢复分支（`SyncEngine.kt:184-198`，
  本周期无本地内容变更）**只写一份**。
- **不得外推的边界**：**⇒ 不得**以「纯浪费」为由删除任一份写盘，也不得改为「按 `baseversion` 推断跳过」（理由 ②）。
- **重开条件**：出现**廉价且字节级可靠**的同等性信号（例如平台提供可持久化的内容校验和，且该信号的写入与内容写入**原子绑定**），可在不改语义的前提下跳过重复写盘。
- **依据**：`docs/resolved/batches/125-同步接受路径摘要一次化批次.md`；
  `sync/src/main/java/com/keepasskey/sync/engine/SyncEngineSupport.kt`（`advanceBaseAndPersist` KDoc）。
  沿革见[附二](../records/限界表证据与实测数据汇录.md#附二更正留痕按日期) §11；迁出留痕见批次正文 §256。

---

## PD-22 会话级「内容摘要 → 已解析树」缓存**刻意不实施**

- **对应条目**：`ISSUE-P3-168` ②
- **原登记位置**：`已知工程限界.md` §14（2026-09-21 §256 迁入，**结论零变更**）。
- **口径：不实施**（§140 批次），四条理由：① **收益已被 ① 吃掉**——① 之后「本地侧重新解析」这一处**唯一可省**的解析已改为直取会话内存树，②
  的剩余收益只剩「后续周期重复解析**同一份**远端 / base 快照字节」，而 base 快照字节恰恰会跨周期复用 ⇒ 要拿到这份收益就必须**跨整个会话**持有解密树；②
  **驻留面**——缓存项是**整棵解密树**（含 ≤ 落盘阈值的附件内联明文与受保护字段），生命周期不是「单次判定
  */ 单次合并」而是「直到锁库或淘汰」，这会同时打破 §1.6「树外可达性」清单的**穷举性**，
  而该清单正是 §52 / `ISSUE-P3-119` 擦除边界的成立依据；③ **没有擦除钩子，也没有淘汰口径**——`SyncDatabaseCodec` 是无接口的 `final class`，
  不持有会话锁观察者（`SessionLockObserver` 由 `SyncCoordinator` 实现）。
- **条目原文**：为 `SyncDatabaseCodec` 加会话级「内容摘要 → 已解析树」缓存（锁内使用，锁库 / 超限即 `clearSensitiveData`
  淘汰）。条目自身的风险提示已写明：
  树级缓存**扩大解密明文的驻留面**，必须按 §1.6 同口径登记所有权与擦除责任，**否则不得实施**。
- **重开条件**：愿意为「跨会话解密树缓存」写定所有权、淘汰口径与锁库擦除路径，并重新穷举 §1.6 的「整树引用者」清单——**先补该登记，再实施**。
- **依据**：`docs/resolved/batches/140-冲突合并本地侧直取内存树批次.md` §2 / §5；迁出留痕见批次正文 §256。

---

## PD-23 「门面型」文件的行数下限：授权**已收窄**为「不得新增含实现体的成员」

- **对应条目**：`ISSUE-P3-188` / `ISSUE-P3-250`
- **原登记位置**：`已知工程限界.md` §18（2026-09-21 §256 迁入，**结论零变更**）。
- **口径（2026-09-21 `ISSUE-P3-250` 重裁 → 收窄授权，不是放宽判据；清单跟踪 2026-09-22 §260 补登）**：`SettingsViewModel.kt` **已不符合**本条最初的「成员**全部**为一行委托」
  前提（重裁时含 5 个薄编排 + `init{}`），按本条自订边界 ①，该类文件**不由本条授权**。故本条对该文件的效力**降级**为：① **不得新增**含实现体的成员——新设置项若含逻辑，必须先落控制器，
  ViewModel 只准加委托；② 原列 5 处薄编排的**待消化清单**（消化方式：下沉到已持有 `CoroutineScope` 的控制器；`clearBlockedFields` 已按此在
  §249 直接收为单语句委托，其余 4 处（`verifyConnectionThenSync` / `refreshPrivilegedBrowsers` / `setPrivilegedBrowserEnabled` /
  `enableBreachCheckAndScan`）已于 §261 批次下沉 ⇒ **清单已清空**，`ISSUE-P3-257` 已于 §261 批次闭环）。
- **判据（为何其余部分刻意不拆）**：文件长度仍是其**公开 API 表面**，成员绝大多数是一行委托 + 一行 KDoc，已无「职责混杂」可消。再往下压行数只有两条路，均被判为**净损失**：①
  引入**二级门面**（如 `viewModel.security.setXxx`）——多一层转发、多一处漂移点，且打破「Compose 侧只依赖一个 ViewModel」的既有接线（接线守卫会连带失配）；② 把
  KDoc 移出文件「凑瘦身」——**违反本批整改纪律 ⑤**（不得以注释位移充当行数削减）。
- **不得外推的边界（不得据本条外推）**：① 本条**只**覆盖「成员绝大多数为一行委托」的门面形态；任何仍含**实现体**成员的超长文件（含 Compose 屏幕、对话框接线巨函数）一律**不适用**，仍属
  `ISSUE-P3-188` 的第二档待消化清单；② 不得把「门面长」当作新增成员不做归属判断的理由（见上「裁定」第 ① 条）；③ 判据是「成员是否有实现体」，**不是**「文件是否难读」；难读的门面应按
  KDoc 分组改善，不按行数改善；④ **不得**据本条放宽 `.codebuddy/rules/engineering-rules.md` 的巨型类阈值，
  也**不得**把本条外推到其它文件（`ISSUE-P3-250` ④）。
- **重开条件**：Kotlin 侧出现可承载委托的结构（接口默认实现 / 编译期委托生成器），或 UI 面按功能分区改造为多 ViewModel（届时二级门面自然消失，本条随之失效）；**或**上述 5
  处薄编排全部消化完毕、该类文件回到「成员全部为一行委托」的形态（届时本条对该文件的授权自动恢复）。
- **依据**：`.codebuddy/rules/engineering-rules.md` §单一职责与巨型类阈值；该条已由 §213 结案；本条的**重裁**见
  [`../resolved/batches/249-S3条件写缺口与子库附件落盘及两处限界重裁批次.md`](../resolved/batches/249-S3条件写缺口与子库附件落盘及两处限界重裁批次.md)
  （`ISSUE-P3-250`）。数值与举例更正见[附二](../records/限界表证据与实测数据汇录.md#附二更正留痕按日期) §18；迁出留痕见批次正文 §256。

---

## PD-24 函数面行数下限：`processFillRequest` 的早退结构**接受为限界**

- **对应条目**：`ISSUE-P3-188` / `ISSUE-P3-250`
- **原登记位置**：`已知工程限界.md` §20（2026-09-21 §256 迁入，**结论零变更**）。
- **口径（2026-09-21 `ISSUE-P3-250` → 现值的重新裁定）**：该函数**已越过原解除条件 ① 的 `>60 行`**（增长来自 2026-09-20 的新增早退）。
  本轮裁定**仍接受为限界**，理由：① 该函数名下的 4 段早退（无结构 / 黑名单命中 / 目标字段解析失败 等）**全部是 fail-closed 出口**——即「不下发任何数据集」，
  其语义方向与本项目的安全取向一致；② 改写控制流的**唯一行为级证据在设备侧** `AutofillAuthChainDeviceTest`，而本批（§249）**无设备可跑** ⇒ 无任何工具能即时证伪改写；
  ③ 「为省 5 行而改写 fail-closed 路径的控制流」收益为 0、风险非 0，且另有三份接线守卫锚在该现场。
- **不得外推的边界（不得据本条外推）**：① 本条与 §18 一样**只覆盖已无可独立命名片段**的形态；只要函数体内还有成块且可独立命名的片段（如 §182 同一批里 `mergeConflictedEntry`
  的冲突对构造），**必须先下沉**，不得援引本条留手；② 本条**不豁免**「超过 50 行」这一事实本身，也不构成放宽验收线的先例——它是**逐条裁定**的结果；③ 承载函数（`onCreate` /
  `onFillRequest` 等生命周期壳）**不因本条**而免于拆分其中内联的长分支。
- **重开条件（2026-09-21 `ISSUE-P3-250` 更新）**：① **设备侧 `AutofillAuthChainDeviceTest` 常态化跑绿、可即时证伪改写时**，
  允许把早退改写为**可观测的拒绝计数**，并在**同一次改动**里把行数降到 **≤50 行**；②
  该函数因新需求**继续增长**时，**不得**再顺延，必须**当批达标**（届时「增长」本身即改写风险的证据）。
- **依据**：批次 [`../resolved/batches/182-合并层冲突对下沉与早退守卫限界批次.md`](../resolved/batches/182-合并层冲突对下沉与早退守卫限界批次.md)；
  该条已由 §213 结案；本条的**重新裁定**见 [`../resolved/batches/249-S3条件写缺口与子库附件落盘及两处限界重裁批次.md`](../resolved/batches/249-S
  3条件写缺口与子库附件落盘及两处限界重裁批次.md)（`ISSUE-P3-250`）。段数更正见[附二](../records/限界表证据与实测数据汇录.md#附二更正留痕按日期) §20；迁出留痕见批次正文 §256。

---

## PD-25 供应链扫描面 = **生产可运行面**：AGP 工具链配置族不入扫描

- **对应条目**：`ISSUE-P2-219`
- **原登记位置**：`已知工程限界.md` §21（2026-09-21 §256 迁入，**结论零变更**）。
- **口径（2026-09-20 本机同参数实测）**：`dependencyCheckAggregate` 对项目**全部可解析配置**做扫描，而 AGP 会为自带的**测试 / lint /
  截图校验工具链**创建一批配置。
- **配置族与其承载构件 / 血缘（AGP 生成，每模块同构）**：`unified-test-platform-core`（UTP 核心）——netty 4.1.93 ×11、grpc 1.57.2 ×8、
  protobuf-java-util 3.22.3，血缘 `com.google.testing.platform:core@0.0.9-alpha04`。
- `unified-test-platform-android-test-plugin-host-emulator-control`——netty 4.1.110 ×11，血缘
  `com.android.tools.utp/android-test-plugin-host-emulator-control@32.2.1`。
- `unified-test-platform-launcher` / `-android-driver-instrumentation`——kotlin-stdlib(-common) 1.9.0，血缘
  `com.google.testing.platform:launcher` / `:android-driver-instrumentation@0.0.9-alpha04`。
- `unified-test-platform-android-test-plugin-result-listener-gradle` +
  `androidLintTool`——`com.android.tools.analytics-library:protos@32.2.1`，血缘 `lint-gradle@32.2.1` /
  `android-test-plugin-result-listener-gradle@32.2.1`。
- `_internal-screenshot-test-task-layoutlib` / `_internal-screenshot-validation-junit-engine`（仅 `:app`）
  ——`org.jsoup:jsoup@1.6.3`，血缘 AGP 截图测试内部工具。
- **机检（`ISSUE-P3-205` / §218 的「散文承诺 ≠ 机检」立规）**：`app/src/test/.../security/SupplyChainScanSurfaceTest.kt`
  两条不变式锁定本条——① 上表配置族**必须**留在 skip 名单内（防 AGP 内部件重新回到闸门）；② 以 `CompileClasspath` / `RuntimeClasspath` 结尾且非
  androidTest / screenshotTest 的配置**不得**出现在 skip 名单内（防借本条之名为生产面开口子）。
- **不得外推的边界（不得据本条外推）**：① 本条**不**豁免任何生产面条目，也**不**放宽阈值；② 本条**不**授权「为让闸门变绿而扩大 skip 名单」——新增跳过项必须先落入本条的分类口径（AGP
  工具链族）
  并同步机检清单，否则 `SupplyChainScanSurfaceTest` 先红灯；③ AGP 升级若引入**新的**工具链配置名，闸门会以「新增达阈条目」**响亮报红**（fail-closed），
  届时按同一原则补入并复核，**不得**静默放宽。
- **重开条件**：① 上游（AGP / UTP / lint 工具）提供「仅扫描生产配置」的官方开关时，改用该开关并撤销本表；② 上述构件升到不含已知达阈 CVE 的版本时，
  可移除对应配置族的跳过项并让闸门重新覆盖它们。
- **依据**：批次 [`../resolved/batches/225-CI门禁四项缺陷整改批次.md`](../resolved/batches/225-CI门禁四项缺陷整改批次.md) §2；构件血缘来自
  `dependency-check-report.json` 各条目的 `projectReferences` / `includedBy` 字段（可复跑）。收口效果明细与「非空面退化」读数见[汇录
  §21](../records/限界表证据与实测数据汇录.md)；迁出留痕见批次正文 §256。

---

## PD-26 转场动效按「**空间段对齐 MotionScheme / 效果段对齐设计规范时间轴**」双栏定标

- **对应条目**：`ISSUE-P3-261` AC① / AC③ / AC⑤。
- **口径**：一次转场拆成**两类量**分别定标——
  - **空间段（位移 / 缩放）**：走 `MaterialTheme.motionScheme`（主题已声明 `MotionScheme.expressive()`）的
    `defaultSpatialSpec` / `fastSpatialSpec`（spring，damping 0.8 / stiffness 380 与 0.6 / 800）。
    **不采用** AndroidX `material-components-android` 实现的 `motionDurationLong1`（450ms）与
    `motionEasingEmphasizedInterpolator`（`EasingEmphasizedCubicBezier`）——那是**实现值**，
    而设计规范给 shared axis 的滑动是 300ms + `cubic-bezier(0.4, 0, 0.2, 1)`。
    选 spring 的理由取官方一句：**spring 在被手势打断时保证速度连续**，`tween` 从零重启；
    且底栏 `NavigationBarItem` 指示器本就走 MotionScheme 的 spring，同屏两套语言即「傻快傻快」的来源。
  - **效果段（透明度）**：走官方 shared axis 淡化对——出向 90ms、入向 210ms 且延迟 90ms
    （`AppNavigationMotion.FADE_OUT_MS` / `FADE_IN_MS` / `FADE_IN_DELAY_MS`），**顺序**而非并发。
    顺序淡化必须由**确定时长**表达，故此处刻意**不用** spring。
  - **阈值**：官方原文「At the 35% mark, neither screen is showing」是**约束**而非毫秒数，
    故以两条不变式落地（`FADE_OUT_MS ≤ 300ms × 35%`、`FADE_IN_DELAY_MS ≥ FADE_OUT_MS`）并由
    `AppNavigationMotionTest` 锁定；**注意**：AC② 原文写出的 90 / 210+90 是官方 shared axis 表的
    取值（90/300 = 30%），本裁决按**阈值约束**统一解释，不把它读成「35% 正好等于 90ms」。
  - **AC③ 缩放起点二选一的落地**：两值**分属两种官方表面**，按槽位分工，不再存在无出处的 `0.96f`——
    fade through 的入口取 `0.92f`（AndroidX `MaterialFadeThrough.DEFAULT_START_SCALE`，由小涨上来），
    全屏表面的预测性返回入口取 `110% → 100%`（`PREDICTIVE_ENTER_SCALE = 1.1f`，由大收进来）。
- **不得外推的边界（如实声明）**：① 本裁决**不**主张 spring 的观感与 300ms `tween` 等价——两者时长
  不同（expressive spatial 约 250ms 量级落定），这是**有意**的取舍；② 本裁决**不**授权把
  `AppNavigationMotion` 里的顺序淡化时长改成 spring（顺序语义会随之丢失）；
  ③ `AppNavigationMotionTest` 的「全仓 `app/src/main` 不得再出现裸 `tween(`」豁免清单当前**只有**
  `AppNavigationMotion.kt` 一项，**不得**为让守卫变绿而扩名单。
- **重开条件**：① 主题的 `MotionScheme` 改为 `standard()` 或自定义（届时空间段参数随主题变化，
  须复核底栏指示器是否仍同族）；② 上游 `material-components-android` / Material 3 规范给出
  shared axis 的新淡化对（届时阈值不变式按新表重算）；③ 出现「必须在无手势场景下保证确定时长」的
  无障碍诉求（如系统开启「移除动画」外的减弱动效偏好）——届时改判空间段。
- **依据**：[`../resolved/batches/262-导航转场定标与顶层Tab返回栈整改批次.md`](../resolved/batches/262-导航转场定标与顶层Tab返回栈整改批次.md)；
  `app/src/main/java/com/keepasskey/app/ui/navigation/AppNavigationMotion.kt`（类 KDoc 与常量的取值来源）；
  `app/src/test/java/com/keepasskey/app/ui/navigation/AppNavigationMotionTest.kt`；
  `app/src/main/java/com/keepasskey/app/ui/theme/Theme.kt`（`MotionScheme.expressive()`）。

---

## PD-27 同级 / 层级的转场**选型分配维持现状**（同级 Fade Through、层级共享轴 X），有意偏离 Android Auto 口径

- **对应条目**：`ISSUE-P3-261` AC⑦（偏差表第 14 / 15 项）。
- **事实**：本应用现行为「同级 Tab 用淡入淡出、层级下钻用横向滑动」；而 corpus 内 Google 文档
  （**Android Auto 设计系统 Motion 页**）的口径相反——同级 peer views 用**横向**「side-to-side movement
  reflects the state of staying at the same level」，层级下钻用 **Z-depth**（缩放 + 顺序淡化）。
- **口径：维持现状**（同级 = Fade Through；层级 = 共享轴 X），**不**按 Android Auto 口径对调。两条理由：
  1. **来源层级不同**：Android Auto 的 peer 指引面向**车机 app bar 标签**，其「横向移动」指的是标签指示器
     在标签之间平移，**不是整页横滑**；把它外推到手机整页导航，是把一个更弱、语境不同的来源
     凌驾于 M3 / Navigation Compose 的平台实现口径之上。
  2. **与本仓返回栈语义冲突**：整页横滑在「同级不改变空间层级」上会自相矛盾——同级切换若产生横向位移，
     返回键方向的语义就会被读成「下钻返回」（这正是 `ISSUE-P2-260` 影响④描述的旧观感），
     而 `ISSUE-P3-261` AC② 已把 fade through 定为**最高优先**。
- **偏差表第 15 项（解锁 / 锁库宜用 Z-depth）的落地**：**维持 Fade Through**。解锁 ↔ 库列表是
  「会话门」而非空间层级——锁库的语义是「库门关上」，缩放会把『门』读成『更深一层』；
  且 `Unlock` 与顶层 Tab 共用同一族转场，才能让「闪一下切走 = 会话结束」形成稳定预期。
- **不得外推的边界**：① 本裁决**不**声称 Android Auto 口径有错，只声明它**不适用于**本应用的整页导航；
  ② 本裁决**不**授权新增第三种转场语言（如给某个 Tab 单独一套横滑）——顶层 Tab 必须同族
  （`AppNavigationMotionTest` 按 `BottomNavItem.routes` 逐条锁定）。
- **重开条件**：① M3 / Navigation Compose 官方文档给出「顶层目的地用整页横向、层级用 Z-depth」的
  手机端明文口径（届时按规范对调并同批回写本条）；② 产品明确要求「同级切换也要有方向感」。
- **依据**：`ISSUE-P3-261` 偏差表第 14 / 15 项与 AC②；
  `docs/references/Monica-架构分析.md:184-186`（参考项目中唯一有成文动效设计者）；
  `app/src/main/java/com/keepasskey/app/ui/KeePasskeyNavGraph.kt`（四个顶层 Tab 同族接线）。

---

## PD-28 预测性返回**保留 90% 屏幕缩放**（全屏表面规格），有意偏离「与 Navigation Compose 同用时应避免缩到 90%」的告诫

- **对应条目**：`ISSUE-P3-261` AC④。
- **事实**：官方移动端 predictive back 设计指引同时给出两句话——① 全屏表面规格表：
  Exit Scale `100% → 90%`、Enter Scale `110% → 100%`、以 35% 进度阈值为界、插值器 `(.1, .1, 0, 1)`；
  ② 「（共享元素过渡）does not work with FragmentManager, the Navigation Component, or Navigation Compose.
  **In these cases, avoid scaling the screen by 90%**. Instead, run the shared element transition as the
  user navigates back.」
- **口径**：**保留 90% 缩放**，且只挂在 `predictivePopExitTransition` / `predictivePopEnterTransition`
  两个**手势专属槽位**上；同批**移除**与缩放并发的满屏滑动（原实现为 slide + scale + fade 三效并发），
  并补齐插值器 `(.1, .1, 0, 1)` 与 35% 顺序淡化阈值。
- **理由**：第 ② 句的**替代方案是共享元素过渡**——它说的是「要么做共享元素、要么别缩」，
  而本应用已按 `PD-29` 裁决**不做**共享元素（Navigation Compose 与该能力不兼容），
  故第 ② 句的适用前提在本仓不成立；反过来，若既不做共享元素、又去掉缩放，
  则「返回」只剩一次淡出，层级感反而比整改前更弱。
- **不得外推的边界**：① 本裁决**只**授权手势槽位的缩放，**不**授权把 90% 缩放加回普通返回
  （`popExitTransition` 现为共享轴视差，前进 / 返回镜像）；② 本裁决**未**证明「手势缩放不与系统
  Back-to-home 窗口缩放叠加」——该叠加关系只有真机手势实录可证，见批次正文的**如实声明**段；
  ③ 不得据本条把 `enableOnBackInvokedCallback` 关掉。
- **重开条件**：① 本仓将来引入共享元素过渡（`SharedTransitionLayout` 等）——届时必须回退本条的缩放；
  ② 真机实录证成「本槽位的缩放与系统窗口缩放叠加成 0.81 倍」——届时按第 ② 句去掉缩放。
- **依据**：`ISSUE-P3-261` AC④；`app/src/main/java/com/keepasskey/app/ui/navigation/AppNavigationMotion.kt`
  （`predictivePopEnterTransition` / `predictivePopExitTransition` / `PREDICTIVE_EXIT_SCALE` /
  `PREDICTIVE_ENTER_SCALE` / `SYSTEM_UI_EASING`）。

---

## PD-29 「列表项 → 详情页」共享元素过渡**不做**

- **对应条目**：`ISSUE-P3-261` AC⑧（「评估可行性并给出结论，做或不做均须留痕」）。
- **口径：不做**。三条理由：
  1. **官方声明不兼容**：共享元素过渡「does not work with FragmentManager, the Navigation Component,
     or Navigation Compose」——本应用正是单 Activity + Navigation Compose。
  2. **绕过方案代价高于收益**：官方给的唯一替代是「返回时再跑共享元素」，那需要给列表项与详情页
     各自接入 `SharedTransitionLayout` + `SharedTransitionScope`，并把**列表行组件**（含图标投影缓存、
     批量选择态、只读子库分区）与详情页卡片一并改造成共享元素宿主——为一次视觉连续性改动
     波及三个高频组件与它们的预览 / 截图门禁。
  3. **连续性已有替代承接**：内容层改用 `Modifier.animateItem`（增删 / 重排不再瞬移）+
     返回方向走共享轴的视差还原（列表页从左侧归位），用户能读到「同一屏回来」的连续感。
- **不得外推的边界**：① 本裁决**不**声称共享元素在 Navigation Compose 下无法实现，
  只声明本仓按「代价 > 收益」不做；② **不得**据本条删除 `AppNavigationMotion` 里返回方向的
  视差配置（那是本条给出的替代承接）；③ 未来若 Navigation Compose 官方支持共享元素过渡，
  本裁决须按重开条件重新评估，**不得**直接把本条当作长期结论。
- **重开条件**：① Navigation Compose 官方支持共享元素过渡（届时按新 API 评估）；
  ② 产品明确要求「列表项 → 详情页」的共享元素连续性并接受上述改造范围；
  ③ 出现比本节② 更廉价的等价方案（例如仅对**卡片标题**做共享、不动列表行组件）。
- **依据**：`ISSUE-P3-261` AC⑧与偏差表第 12 项；
  `docs/references/Monica-架构分析.md:186`（参考项目用 `SharedTransitionCompat` 的做法，作为对照而非照抄依据）；
  `app/src/main/java/com/keepasskey/app/ui/screens/vault/VaultListScreen.kt`（`animateItem` 落点）。

---

## PD-30 动态取色与主题调色盘收敛为「配色来源」**互斥单选**——点选调色盘即关闭动态取色（**2026-09-23 用户裁决 · 候选 A**）

- **对应条目**：`ISSUE-P3-263` AC⓪ / AC④（存储口径三选一，用户明示「选取候选 A（推荐）互斥单选」）。
- **事实**：动态取色压过品牌调色盘本身是有意设计（Material You 语义，Android 12 / API 31 起
  `dynamicLightColorScheme` / `dynamicDarkColorScheme` 以壁纸衍生 ColorScheme 为基准）；
  缺陷在设置页缺少互斥与生效反馈——动态取色开启期间调色盘 5 项照常可点、照常呈现「已选中」、
  `theme_palette` 照常落盘，用户感知为「5 种主题全部失效」（2026-09-22 真机反馈）。
- **口径（候选 A 互斥单选）**：调色盘与动态取色归入同一「配色来源」单维度——
  ① 判据单点化：`resolveColorSource(dynamicColorEnabled, sdkInt)` 纯函数为唯一判据，
  设置页与 `KeePasskeyTheme` 共用，禁止双写（API < 31 显式回落品牌调色盘）；
  ② 呈现互斥：动态取色生效期间调色盘 5 项整节置灰不可点、不呈现任何「已选中」，
  附行内原因说明与一步切回动作（「改用主题调色盘」＝关闭动态取色，偏好值保留即恢复）；
  ③ 存储互斥：`setThemePalette` 在**同一次 DataStore 原子事务**内幂等写
  `theme_palette` + `dynamic_color_enabled = false`，任何路径写入调色盘后配色来源必然
  收敛回品牌调色盘；`theme_palette` 保持「永远是当前生效值」的干净语义。
- **不采纳候选 B / C 的理由**：B（主从显式化）保留「两个独立偏好同时看似生效」的存储态，
  需 UI 持续解释主从关系；C（混合叠加）须先证「壁纸衍生容器色 × 品牌主色」对比度不劣化，
  改动面与验证成本最高且违背「同一时刻只有一个配色生效」的用户心智。
- **不得外推的边界**：① 本裁决**不**否定「动态取色优先于品牌调色盘」这一 Material You
  默认语义，只约束设置页的互斥呈现与存储口径；② 互斥写在仓库层是**兜底不变量**，
  不得以「UI 已置灰」为由移除；③ 一步切回动作只关 `dynamic_color_enabled`，
  **不得**顺带重置 `theme_palette`。
- **重开条件**：用户改选候选 C（混合叠加）——届时须先按原条目完成对比度论证，
  并同步改写 `theme_dynamic_sub` 的「暂不生效」表述。
- **依据**：`ISSUE-P3-263` 整改方向第 4 目；
  [resolved/batches/266-动态取色与调色盘互斥单选批次.md](../resolved/batches/266-动态取色与调色盘互斥单选批次.md)；
  `app/src/main/java/com/keepasskey/app/ui/theme/ThemeMode.kt`（`ColorSource` / `resolveColorSource`）。

---

## PD-31 HMAC 块流**读侧块长上限移除**——仅拒绝负数，对齐三家参考实现（**2026-09-23 用户裁决**）

- **对应条目**：`ISSUE-P3-268` AC⓪（裁决项；评估阶段曾提「设置页自定义 / 固定放宽 16 MiB」两候选，
  用户指示核实参考项目后明示「**既然别家都没有上限，那么咱们也不要设置上限，只检查负数**」）。
- **事实**：旧读侧上限 `MAX_READ_BLOCK_SIZE = 1 MiB` 对「块长 > 1 MiB」一律判损坏文件，
  而规格对块长仅定 Int32、1 MiB 只是官方写侧惯例；三家参考实现读侧**均无上限、均无设置项**
  （官方 KeePass 2.61.1 `HmacBlockStream.cs:231-237` 与 keepass2android 同源仅查
  `nBlockSize < 0`；KeePassXC `HmacBlockStream.cpp:142-149` 同，其 1 MiB 仅是写侧默认分块）——
  本仓旧上限是比三家都严格的自加护栏，构成对合法第三方文件的误拒面。
- **口径**：读侧仅拒绝负数块长（与三家对齐）；「声明超大块而流不满足」由块数据读取的
  EOF 路径兜底为 `KdbxCorruptFileException`（类型化，不裸漏 `EOFException`）；
  恶意分配由 `LittleEndianUtil.readBytes` 既有通用护栏（`DEFAULT_MAX_READ_BYTES = 16 MiB`，P0-5）
  兜底——该护栏属工具层通用语义，**不属于**本裁决可支配面，不得以「读侧无上限」为由放宽或绕过。
- **不采纳「设置页自定义上限」的理由**：三家参考实现均无此设置项先例；该上限属格式解析层
  内部护栏，普通用户无从判断取值，且「自定义调大」即用户自行放弃防护——伪需求。
- **不得外推的边界**：① 写侧契约**不变**——本仓恒以 1 MiB（`DEFAULT_BLOCK_SIZE`）分块写出；
  ② 本裁决只支配 HMAC 块流层的块长校验，不构成对其它长度校验（外层头字段 1 MiB /
  内层头 64 MiB / 变体字典 value 1 MiB / `readBytes` 16 MiB 通用护栏）的放宽依据。
- **重开条件**：实测出现「> 16 MiB 单块且合法」的真实客户端文件（届时须先评估
  `readBytes` 护栏与低内存设备的分配上限，再裁决是否收窄护栏例外）。
- **依据**：`ISSUE-P3-268` 整条（含本批补齐的三家实证）；
  [resolved/batches/267-HMAC块流读上限移除批次.md](../resolved/batches/267-HMAC块流读上限移除批次.md)；
  `database/src/main/java/com/keepasskey/database/file/HmacBlockStream.kt`（读侧两处仅负数检查）；
  `database/src/main/java/com/keepasskey/database/io/LittleEndianUtil.kt`（P0-5 护栏，未动）。

---

## PD-32 真实 RP 端到端冒烟的 **RP 口径放宽**——任一真实 RP 贯通即满足 AC① 意图，github.com 不再必测（**2026-09-23 用户裁决**）

- **对应条目**：`ISSUE-P2-254` AC①（原口径「github.com 端到端真机贯通 8 步逐项留痕」）。
- **事实**：§263（2026-09-22）修复 `attestationObject` 缺失规范键 `authData` 后，真机
  （Redmi 4X / Android 17）在 **demo.yubico.com** 上注册路径**双通道通过且留有日志**
  （`mark.via` web origin + `com.ss.android.lark` apk-key-hash；`rpIdHash` 与
  `SHA256("demo.yubico.com")` 逐字节一致）；2026-09-23 用户口述补充：同站**登录断言
  （assertion）亦成功**、系统 CM 选择器 UI 有弹出、PRF 扩展未测。
- **口径**：AC① 的 RP 口径由钉死 github.com **放宽为「任一真实 RP 端到端贯通（注册 +
  登录断言）」**——demo.yubico.com 已实质达成，github.com 不再作为必测项。
- **证据等级如实分层（不得混写）**：注册路径＝**真机日志**（§263 批次留痕）；登录断言与
  CM 选择器弹出＝**用户自陈（口述）**——归档批次须按此等级登记，不得写成「截图 / 日志留痕」。
- **不得外推的边界**：① **PRF 与真实 RP 对拍**不受本裁决影响，仍须执行（AC②）；
  ② CM **通知渲染**核对不受影响（AC③ 剩余面）；③ 本裁决只支配「测哪个 RP」，
  不构成对任何格式 / 协议实现的放宽依据。
  > **2026-09-23 后续（§269）**：上述①已执行并达成——真机在 Corbado PRF Demo 完成 PRF
  > 注册 + 登录断言（页面三项状态全绿 + `Authentication successful (PRF)`）；②经核实属系统
  > 模态对话框与 `FLAG_SECURE` 的**平台硬限制**（弹窗挂起期间通知栏不可下拉），非本仓缺陷，
  > 随 `ISSUE-P2-254` 结案归档。
- **重开条件**：若 github.com 特有流程（其 RP 特定 WebAuthn 参数 / resident key 策略等）
  成为依赖面或出疑，再单独登记专项验证。
- **依据**：用户 2026-09-23 口述与本日裁决；
  [resolved/batches/263-注册响应attestationObject缺失authData键修复批次.md](../resolved/batches/263-注册响应attestationObject缺失authData键修复批次.md)。

---

## PD-33 通行密钥验证渠道登记：Corbado PRF Demo 为**常规首选**站点

- **对应条目**：`ISSUE-P2-254` 结案批次 §269（AC② 的对拍站点选择）。
- **口径**：凡需在**真实 RP** 上验证通行密钥链路（注册 / 登录断言 / PRF 扩展），
  **首选** [Corbado WebAuthn PRF Demo](https://webauthn-passkeys-prf-demo.explore.corbado.com/)
  （`webauthn-passkeys-prf-demo.explore.corbado.com`）。选择理由：该站点**显式下发 `prf` 扩展**
  （注册期空对象、断言期 `prf.eval.first` 盐值），并**逐项回显能力探测结果**——即它同时覆盖
  「基础注册 / 断言」与「PRF 派生」两个面，是少数把 PRF 结论直接呈现在页面上的公开站点；
  其余常见演示站（webauthn.io / Yubico 演示页等）不向用户暴露 PRF 开关，**不能**替代本面。
- **辅助站点（按需，不替代上述首选）**：`demo.yubico.com`（基础注册 + 断言，§263 / `PD-32` 已实证）；
  PRF 仅做深度演练时可用 `passkeyprf.com` 与 `webauthn.me`（其 PRF 入口需在页面内逐步展开，
  可操作性弱于 Corbado）。
- **不得外推的边界**：① 本登记只规定「**用哪个站点做验证**」，**不**改变任何判定口径——
  不因「在某站点通过」而放宽 `PasskeyRegistrationGate` 的 fail-closed 分支、DAL 归属校验或 UV 门控；
  ② **不**构成「互操作已全面验证」的依据：单站点通过 ≠ 所有 RP 可贯通（口径同
  [`实现约定与验证现状.md`](实现约定与验证现状.md) §4.3 的边界段）；
  ③ 站点可用性可随时变化（页面改版 / 下线 / 地区不可达）——遇不可用时**换用辅助站点并如实登记**，
  不得为「保持首选」而沿用陈旧步骤或凭空声称通过；④ 本登记**不**改变 `PD-32` 的 RP 口径
  （仍为「任一真实 RP 贯通即满足 AC① 意图」）。
- **重开条件**：Corbado 站点长期不可达 / 移除 PRF 面，或出现同样显式暴露 PRF 与能力探测、
  且更稳定的公开站点时，另行裁决替换。
- **依据**：用户 2026-09-23 指示「将该网址登记，以后作为通行密钥验证渠道」；
  [resolved/batches/269-通行密钥交互链路与PRF真机对拍及ISSUE-P2-254结案批次.md](../resolved/batches/269-通行密钥交互链路与PRF真机对拍及ISSUE-P2-254结案批次.md)（该站点已实证 PRF 注册 + 断言全绿）。

---

## PD-34 Manager / Util / Helper 命名面：**域前缀有界类型维持现名**，以白名单棘轮收口（2026-09-23）

- **对应条目**：2026-09-23 全仓工程卫生梳理 P2「`HashUtil` / `LittleEndianUtil` / `KdbxXml*Util` / `KdbxXmlTimeHelper` / `KeystoreManager` / `AutoLockManager` / `ClipboardSecurityManager` 等 Manager/Util/Helper 命名——既未改名也未登记豁免」。
- **规则口径**：全局工程原则要求避免的是**无边界**的 `Manager` / `Util` / `Helper` / `Common` 命名（防 God Util / 杂物间 `Manager`），**不是**「后缀一律禁止」。
- **事实（2026-09-23 全量清点）**：五模块 `src/main` 共 **13** 个生产类型使用该族后缀，**每一个都有域前缀 + 单一职责**，可读出唯一变化原因，不构成「无边界」：

  | 类型 | 职责边界（KDoc / 源码自陈） |
  |---|---|
  | `HashUtil` | SHA-256 / SHA-512 / HMAC-SHA256 纯函数集合 |
  | `LittleEndianUtil` | KDBX 二进制 LE 编解码 + `DEFAULT_MAX_READ_BYTES` 护栏 |
  | `KdbxXmlWriteUtil` | KDBX XML 写侧元素助手（流式） |
  | `KdbxXmlValueUtil` | KDBX XML 值编解码（UUID / Base64 容空白） |
  | `KdbxXmlTimeHelper` | KDBX XML 时间戳（.NET 秒）编解码 |
  | `HistoryManager` | 条目历史快照 / 修剪 / 回滚（对齐官方 `PwEntry.MaintainHistory`） |
  | `KeystoreManager` | Android Keystore 硬件密钥生成 / 封印 / 解封 |
  | `AutoLockManager` | 自动锁定 Android 注册管道（生命周期 + 熄屏广播） |
  | `ClipboardSecurityManager` | 剪贴板敏感复制通道 + 定时擦除 |
  | `BiometricAuthManager` | 生物识别检测与 `BiometricPrompt` 认证 |
  | `UnlockPasskeyManager` | 设备绑定解锁通行密钥断言门控 |
  | `UnlockThrottleManager` | 主密码解锁失败节流闸门 |
  | `ChildDatabaseSessionManager` | 子库挂载与只读会话唯一门面 |

- **口径**：① 上表 13 个简单名**维持现名**，记入 [`tools/doc/check_bounded_type_names.py`](../../tools/doc/check_bounded_type_names.py) 的 `ALLOWED` 白名单；② **新增** `*Manager` / `*Util` / `*Helper` / `*Common` 类型**默认红**——优先改名为「域前缀 + 语义名」（如 `LittleEndian` / `KdbxXmlTimes`），确需保留后缀时须**同批**扩 `ALLOWED` 并回写本条清单；③ 测试源（`src/test` / `src/androidTest`）私有助手不入扫面。
- **不改名的理由**：全量改名牵动约 **786** 处引用与若干路径锚定用例，属纯机械 churn、零行为收益（同 `PD` 族「不为措辞无谓改名」口径）；既有 KDoc / 批次 / 探针文档中的类型名锚点一并稳定。
- **不得外推的边界**：① 本条**不**豁免真正的无边界命名——新建 `CommonUtil` / `AppManager` / `Helpers` 之类仍违规；② **不**豁免巨型类 / 巨型函数（另见 `count_line_tiers` / `long_functions` 闸门）；③ 任一登记类型若长成杂物间（多变化原因），应**拆分并改名**，不得以本条为挡箭牌。
- **重开条件**：① 项目采纳禁止该族后缀的编码规范；② 登记类型出现第二职责域并触发拆分；③ 白名单棘轮需下调（改名落地后从 `ALLOWED` 移除并同步本条）。
- **依据**：全局工程原则「避免无边界 `Manager`/`Util`/`Helper`/`Common`」；`tools/doc/check_bounded_type_names.py`（`--selftest` 口径反校 + CI `hygiene-gate` 第 7 条）；
  [resolved/batches/285-ManagerUtilHelper命名豁免登记与有界性机检批次.md](../resolved/batches/285-ManagerUtilHelper命名豁免登记与有界性机检批次.md)。

---

## 分流去向（迁出时同批处置，**不属产品裁决**）

迁出这三条时，其中**真正的待修缺口**未被一并带走，而是留在 `ACTIVE_ISSUES.md` 继续跟踪：

| 迁出条目 | 留下的待修缺口 | 新条目 |
|---|---|---|
| `ISSUE-P2-79` | AC③「对过低工作量的**导入**给出告警或升级选项」从未实施 | `ISSUE-P2-87` |
| `ISSUE-P3-121` | AC③「**文档与实现必须一致**，不得只改其一」从未实施（`README` 未披露内网限制） | `ISSUE-P3-147` |
| `ISSUE-P2-47` | 无（AC① 实质已具备、AC③ 已完成；剩余取舍即 PD-03 本身） | — |

> **编号纪律**：`ISSUE-P2-79` / `ISSUE-P3-121` / `ISSUE-P2-47` **不再复用**（编号续用不复用）。
> 本表条目使用独立前缀 `PD-`，与 `ISSUE-` 体系分离，避免与待办编号混淆。
