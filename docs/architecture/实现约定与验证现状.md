# 实现约定与验证现状

> **定位**：本文件承接自 [`已知工程限界.md`](已知工程限界.md) **§255 批次迁出**的内容（**逐字原样、未重排未改写**）。
> 这些内容**不是**已接受的残余风险 / 工程取舍，而是三类实现层面的事实陈述：① 功能语义与实现约定（§1.2 / §3.1 / §3.2 / §3.3）；
> ② 已达成结论与参数契约（§2.3 / §3.5）；③ 验证覆盖现状（§4.1 / §4.2 / §4.3）。
> `已知工程限界.md` 在对应编号处保留**存根**（节标题 + 该节规范句 + 指针）——代码 KDoc 按 `§x.y` 引用时**以本文件为完整正文**。
> 迁出批次：`§255`（2026-09-21）；迁出前原文见 `git show 7192f81:docs/architecture/已知工程限界.md`。

---

<a id="s1-2"></a>

## §1.2 用户显式「彻底退出应用」的清理面与边界（`ISSUE-P3-116`，§68）


- **事实**：该入口（`showKillAppOption` 开启时呈现）的执行顺序固定为「退栈 → **清理易失缓存** → `exitProcess(0)`」——清理**必须早于**退出，
  否则进程终止后无任何代码可执行；清理动作收敛在 `MainApplication.purgeVolatileCachesBeforeExit()`，覆盖
  `cacheDir/attachments`（附件解密明文）与 `cacheDir/sync`（KDBX 密文快照）**两个面**。
- **不覆盖（如实声明）**：`<库文件>.kdbx.bak`（滚动备份，属用户数据非缓存，口径见 §1.4）与 `filesDir/rollback`（防回滚安全状态，**不得**随退出销毁，否则云侧可重放旧库）。
- **清理为同步 best-effort**：任一实现失败只落脱敏日志、**不阻断退出**；与反取证无关（落盘清理仍是 unlink-only，见 §1.5），**不得**据此断言「退出即不可恢复」。
- **依据**：`app/src/main/java/com/keepasskey/app/MainApplication.kt`；
  `docs/resolved/batches/68-退出前清理易失缓存批次.md`。

---

<a id="s2-3"></a>

## §2.3 原生内核为 Rust，秘密缓冲由 `Zeroizing` RAII 全路径擦除


- **事实**：选择 Rust 的代价是体积，收益是**秘密确定性擦除**与 Argon2 优于纯 Java 路径。
- **互操作证据（2026-09-21 更正后的口径：**文件级对拍已完成**）**：官方客户端对本仓产物的**文件级**端到端对拍**已完成**——`keepassxc-cli 2.7.12` 与
  `pykeepass 4.2.0` 各自独立读出 KPEX 条目（114 条判据全绿、退出码 0；2026-09-21 复跑通过），细则见 §4.3 与
  [`records/通行密钥互操作对拍记录_2026-09-19.md`](../records/通行密钥互操作对拍记录_2026-09-19.md)。
- **依据**：`RESOLVED_LOG.md` §47。

---

<a id="s2-5"></a>

## §2.5 KDF 参数缺失 fail-closed：**不补默认值**（2026-09-22 KDBX 格式核对补登）

- **约定**：VariantDictionary 中 KDF 必需参数（AES-KDF `S`/`R`，Argon2 `S`/`M`/`I`/`P`/`V` 等）缺失时直接抛 `KdbxCorruptFileException`，**不**按默认值补齐（规格允许的默认值语义不采纳）。
- **理由**：官方所有写端（KeePass 2.x / KeePassXC / KeePassDX / pykeepass）恒写全套参数，「缺参数」文件只有损坏或恶意构造两种来源；补默认值有两条坏路径——默认值 ≠ 真实值 ⇒ 派生错钥 ⇒ 用户看到误导性的「主密码错误」而非「文件损坏」；默认 0 直接落入越界参数（如 Argon2 `I=0`）。fail-closed 使失败更早、更类型化，对正常文件的最终语义与官方一致。
- **依据**：`database/src/main/java/com/keepasskey/database/file/KdbxKdfParameterCodec.kt`（`deserialize` 各分支 `?: throw KdbxCorruptFileException`）；核实方式：2026-09-22 对照 `keepass.info/help/kb/kdbx.html` 格式核对走查。

---

<a id="s3-1"></a>

## §3.1 原生库懒加载顺序契约


- **事实**：`System.loadLibrary` 经 `NativeCryptoLibrary.loaded` 统一懒加载；各绑定类的 `available` **必须**先求值该属性再发起原生调用，
  否则库未加载时调用 `external fun` 会抛异常（顺序依赖，属已登记的同类限界）。
- **依据**：`crypto/src/main/java/com/keepasskey/crypto/NativeCryptoLibrary.kt`。

---

<a id="s3-2"></a>

## §3.2 `CipherInputStream` 的异常语义


- **事实**：`CipherInputStream` 对填充非法 / 长度非整数倍抛 `IOException`（**非**静默 EOF）。
- **依据**：`RESOLVED_LOG.md` §38；`crypto` 模块 CBC 流式用例。

---

<a id="s3-3"></a>

## §3.3 遮挡触摸过滤 / 悬浮窗屏蔽的接线面


- **4 类 Compose 对话框窗口（`ISSUE-P2-245`，2026-09-21 §252 已接线；`ISSUE-P2-246` §254 已整改）**：
  `CreateVaultWizardDialog`（含主密码字段）/
  `MasterKeyChangeDialog` / `ChildDatabaseDialogs` / `EntryDetailPreviewDiffComponents` 只经
  `SecureDialogWindowEffect` 施 `FLAG_SECURE`；对话框是独立窗口，**不继承** Activity `decorView` 的过滤，原**无**遮挡触摸过滤接线 ⇒
  **不得**把本节读作「窗口面已无盲区」。
- **`FLAG_SECURE` 的保持已改由调用点 `SecureOn` 强制（2026-09-21 §254，`ISSUE-P2-246`）**：该 flag 实际由 Compose 的
  `DialogProperties.securePolicy` 决定，默认 `Inherit` 以**宿主窗口**为准并会**清除**本包装的施加；故 4 个文件的 7 处调用点
  一律传 `SecureFlagPolicy.SecureOn`。**产品可见行为**：**宿主关闭「防截屏」开关时，本类对话框仍强制遮罩**
  ——这是该包装自陈意图（强制遮罩、不读用户开关）的实现；若日后要恢复「尊重开关」，须**改判**该意图并同步 KDoc 与调用点。
  证据与留痕见 [`docs/records/限界表证据与实测数据汇录.md`](../records/限界表证据与实测数据汇录.md) 对应条。
- **依据**：`docs/resolved/batches/59-附件字节所有权与敏感窗口接线批次.md`（P3-103 补 `SecureCaptureActivity` 与遮挡触摸面）、
  `docs/resolved/batches/226-Passkey创建链路拒绝原因呈现批次.md`（Passkey 拒绝页）；
  `app/src/test/java/com/keepasskey/app/security/ObscuredTouchWiringTest.kt`、
  `SensitiveWindowHardeningTest.kt`（后者含接线守卫）。

---

<a id="s3-5"></a>

## §3.5 完整性重扫的 30 s 检测窗口（`ISSUE-P3-152` 复核结论，**不得为省电放宽**）


- **事实**：`RuntimeIntegrityDetector` 以 **30 s** 周期在 IO 线程重扫（root / Magisk 路径、`/proc/self/maps`），并由 **120 s**
  快照陈旧窗口保证「连丢 4 次重扫 ⇒ 非 suspend 门控（生物识别快速解锁等）转 fail-closed 保守策略」。
- **边界（⇒ **不得**据此判定「这是可省的常态开销」）**：该 30 s 既是**注入框架被捕获的上界**，也是高价值通道判定新鲜度的来源；省下的只是每 30 s 一次的文件探测（IO 线程），
  与「判定新鲜度」不成比例。**不得**为省电调大重扫周期或陈旧窗口——`RuntimeIntegrityRescanContractTest` 已把该结论钉成**可执行契约**（放宽即红；该用例另以
  `assertEquals(间隔 × 4, 陈旧窗口)` 锁定两者**精确比例**，故**单独收紧间隔而不等比调整窗口同样会红**）。`ISSUE-P3-152`
  复核时的另一处同类成本（`UnlockedNotificationController` 的 **2 s** 偏好轮询）属**真浪费**（无安全语义，且进程级快照早已存在）⇒ 已改为订阅内存快照、零 IO，见
  §116。
- **与用户开关的关系（`ISSUE-P3-236` / `PD-15` 补注，2026-09-21）**：本检测改为用户可配置开关、出厂默认关闭，但**关闭只解除通道降级，不停探测**——30 s
  周期重扫照常运行、`report` 仍为真实等级与命中项。故本节两项参数**依旧不得放宽**：「关闭」改变的是「命中后是否阻断」，不是「多久能发现注入」；而 `SNAPSHOT_STALE_AFTER_MS`
  所守护的保守分支只在开关**开启**态才被走到。反向亦不成立——不得以「默认关着，周期快慢无所谓」为由放宽，开关随时可能被用户打开。
- **解除条件**：引入**事件驱动**的等价信号源（例：由系统 / 内核侧回调替代轮询式探测），使同等检测时效不再依赖周期扫描。
- **依据**：`app/.../security/RuntimeIntegrityDetector.kt`（两个常量就地 KDoc）；
  `app/src/test/java/com/keepasskey/app/security/RuntimeIntegrityRescanContractTest.kt`；
  `docs/resolved/batches/116-常驻轮询收敛与完整性重扫参数守卫批次.md`；
  [`../resolved/batches/239-运行环境完整性检测开关批次.md`](../resolved/batches/239-运行环境完整性检测开关批次.md)、
  [`产品裁决登记.md`](产品裁决登记.md) `PD-15`。沿革见[附二](../records/限界表证据与实测数据汇录.md#附二更正留痕按日期) §3.5。

---

---

<a id="s4-1"></a>

## §4.1 设备侧（instrumented）覆盖现状


- **事实（2026-09-21 现跑口径）**：`app` / `database` / `sync` / `crypto` / `core` 五模块均建有 `androidTest` 源集，例数为 `app`
  71 / `sync` 24 / `database` 17 / `crypto` 37 / `core` 4，共 **153** 例（逐模块 `grep -c "@Test"` 于
  `*/src/androidTest`；§203 时点为 132，此后 §217 / §221 / §240 / §241 等批持续新增）。
- **Passkey 系统级交互（口径自 §151 起收窄）**：**provider 侧请求契约已真机实证**——`CredentialProviderRequestContractDeviceTest` 以本
  APK 的**真实 `SigningInfo`** 经**平台 `android.service.credentials.GetCredentialRequest`**
  换型驱动生产解析（`apk-key-hash` origin 独立复算对照、委派 origin 须包名 + 指纹同时命中、ISSUE-P2-72 交叉核对、`clientDataHash` /
  `prf.eval` / `allowCredentials` / `userVerification` 消费、`rp.id` fail-closed）。
- **变量隔离的边界（勿过度解读）**：两次修复**各自都同时改了多项**，
  负向对照只证明「那一组改动合起来是必要的」；**不得**据此断言「某个单项 flag 单独就是成因」
  ——两条路径**共享**的是「认证
  PI 必须 `FLAG_MUTABLE`」（官方明文），**不可外推**的是「只改 FLAG 就能修好」。
- 模拟器残余失败点推进到「系统填充 UI 呈现」——强系统 UI 依赖的环境敏感面（用例设计验证环境为真机），CI `device-gate` 以 `notClass` 显式排除该类、真机承担；
  不得把真机结论外推到模拟器，反之亦然。
- **静态逻辑不得只靠宿主单测**：涉及正则 / XML / 平台 API 的逻辑**不能**仅凭宿主单测判定在 Android 上可用——`ISSUE-P1-12` 与 `ISSUE-P0-04` 均属「JVM
  过、Android 运行时挂」类逃逸。
- **纪律**：自 `ISSUE-P2-192` 结案起，`.github/workflows/build.yml` 新增 **`device-gate` job**（API 36.1 x86_64 模拟器 +
  全模块 `connectedDebugAndroidTest`）；`fast-gate` / `native-gate` / `rust-supply-chain` 仍不运行 connected 层；
  `AssumptionViolatedException` 会被 AGP 记为 `<failure>`，判定以 task 结果为准（见 `AGENTS.md` §5）。
- **依据**：`RESOLVED_LOG.md` §34 / §36 / §46 / §47 / §71 / §76 / §78 / §82 / §203 / §204；本例数更正、测试 APK 明文口径与
  §209 证据链沿革见[附二](../records/限界表证据与实测数据汇录.md#附二更正留痕按日期) §4.1。

---

<a id="s4-2"></a>

## §4.2 视觉与系统 UI 面的未核对项


- **事实**：`Recents` 任务缩略图与 `PrimaryTabRow` 替换后的**视觉效果仍未在真机核对**（见 `RESOLVED_LOG.md` §71.3）；Compose 预览能力经
  screenshot test **宿主 Layoutlib** 渲染承担（产物落 `app/build/test-results/updateDebugScreenshotTest/`、`hostname`
  为构建机名；设备用例则落 `app/build/outputs/androidTest-results/connected/`；本节 §21 自列的
  `_internal-screenshot-test-task-layoutlib` 即其工具链）——**不得**把「预览已渲染」读作「真机已核对」（本机 Android Studio CLI 的
  `render-compose-preview` / `analyze-file` 返回 `not implemented`，属 CLI ↔ Studio 桥接缺失，见 `RESOLVED_LOG.md`
  §100）。
- **系统 UI / 权限依赖面的设备侧自动化余量（`ISSUE-P2-192` 余量第 5 项，2026-09-19 入表处置）**：下列各面的**行为级**验证当前只能靠手工冒烟承担，自动化补用例的成本 /
  收益经评估不成立（强系统 UI 依赖；且用例对环境前提的敏感度高于被测逻辑本身——见 §4.1 模拟器专有差异）：① `BiometricPrompt` **实际弹窗**与 `CryptoObject`
  绑定（逻辑面已有 `QuickUnlockSealDowngradeDeviceTest` 等真机用例）；② 通知渠道渲染；③ 剪贴板（Android 10+ 后台读取限制、
  `EXTRA_IS_SENSITIVE`）；④ `FLAG_SECURE` 与截屏检测（只有人工可证）；⑤ SAF / `ContentResolver` / `FileProvider`
  的附件与库导出（生产关键分支 `CreateDocument` launcher 已有 `BackupAndSafHardeningTest` / `CreateVaultLocationWiringTest`
  静态守卫 + 宿主逻辑用例）；⑥ 扫码相机流。
- **处置口径**：以上各面**已**有逻辑层 / 静态守卫 / 部分设备侧用例的分层覆盖（逐条见上），剩余的纯系统 UI 交互面按「手工冒烟 + 发版前人工核对」承担；**不得**把本表未列的新系统 UI
  面擅自归入本条。**解除条件**：某面出现真实缺陷回归，或引入可靠的系统 UI 自动化基建（如 UiAutomator 截屏比对流水线）时重评。两处引文更正见[附二](../records/限界表证据与实测数据汇录.md#附二更正留痕按日期) §4.2。

---

---

<a id="s4-3"></a>

## §4.3 通行密钥 KPEX schema 互操作：**文件级对拍已达成**（§149 → §220，§222 补写路径自动迁移与 v1 形态嗅探），交互链路仍未覆盖


- **事实**：§149 批次把通行密钥落库 schema 由自造 `Passkey.*` + hex/Base64 私钥改为 **KeePassXC / KeePassDX 口径**的
  `KPEX_PASSKEY_*` + **PKCS#8 PEM**（EC 用命名曲线 `secp256r1`），读侧三形态兼容（KPEX / v1 旧键 / 外部条目按
  `AlgorithmIdentifier` 的 OID 字节嗅探；§222 起 v1 文本形态也被精确嗅探——64 字符 hex → ES256、32 字节种子 → Ed25519，不再静默回落 ES256）。
- **该对拍当次即暴露并闭环一个真实缺陷（`ISSUE-P2-211`）**：Ed25519 私钥曾被写成 RFC 5958 `OneAsymmetricKey`（`version = 1` +
  `publicKey [1]`）——本仓解码器接受，**OpenSSL 系实现（含 KeePassXC）拒收** ⇒ 已改为 RFC 8410 `version = 0` 最小形态；解码侧**继续接受
  v0/v1** 以兼容存量库。⇒ **不得**再以「格式面按源码口径对齐 + 自家往返」替代外部对拍。
- **边界（不得读作「互操作已全面验证」）**：① 证据是**文件级 / 离库**的：**不含** CM 通道交互（注：`ISSUE-P2-254` 经 2026-09-23 真机在 Corbado PRF Demo 上实证已达成真实 RP 的注册、登录断言与 PRF 端到端对拍）；② 系统 **CM 选择器 UI 全链路**（选择器弹出已确认、通知渲染因系统模态限制属系统级不可下拉，见 `ISSUE-P2-254` 结案归档）；③ ~~**PRF 仍未与真实 RP 对拍**~~（**已于 2026-09-23 真机实证达成**）；④ **v1 旧条目只在「被用到时」迁移**（`ISSUE-P3-213`，§222 起）：
  条目因断言而被原子修补签名计数器时，在**同一受控变换内**整条换新为 KPEX（全部 `Passkey.*` 旧键清除，私钥重包为 PKCS#8 PEM 写入
  `KPEX_PASSKEY_PRIVATE_KEY_PEM`）；但**从未被断言触及**的 v1 条目仍是旧形态（读侧永久兼容、断言功能不受影响）⇒ 老库与 KeePassXC / KeePassDX
  的互通**不因升级自动成立**，只在「该凭据被实际使用过」之后才成立。另有两条边界：私钥无法重包（形态非法 / 算法不匹配 / 标量越界）时**放弃迁移并保持原文**（fail-safe，只留日志），
  以及迁移**不阻断计数器写入**。
- **依据**：`tools/passkey-interop/`（探针产物 + 对拍脚本 + fixture 生成器）；
  `database/src/test/.../{PasskeyInteropProbeTest,PasskeyInteropExternalFixtureTest}.kt`；
  `docs/resolved/batches/220-通行密钥互操作对拍与Ed25519-PKCS8形态整改批次.md`；写路径迁移与旧格式嗅探见
  `docs/resolved/batches/222-通行密钥擦除收敛与v1就地迁移批次.md`。

---
