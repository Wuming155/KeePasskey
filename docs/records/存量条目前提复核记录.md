# 存量条目前提复核记录（`ACTIVE_ISSUES.md` 已裁决暂缓条目）

> **文档定位**：对 `docs/ACTIVE_ISSUES.md` 中**三条「已裁决暂缓」条目**（`ISSUE-P2-47` / `ISSUE-P2-79` /
> `ISSUE-P3-121`）做一次**取证式前提复核**，判定其正文前提与**用户裁决所依据的前提**在当前 HEAD 上是否仍成立。
> 依据 `ACTIVE_ISSUES.md` 顶部「条目维护规则（ISSUE-P3-28 确立）」第 1 / 2 条：关于**代码库现状**的前提必须附
> 核实时间点与核实方式，**认领条目时先复核前提**；据不成立的前提做出的用户裁决，其**理由本身即失真**。
> **本文件只做取证与判定**：不改任何代码、不改 `ACTIVE_ISSUES.md`、不替用户裁决；修正文字以「可直接粘贴片段」形式交付主控。

- **复核时间点**：**2026-09-16 21:37 +0800**（`date "+%Y-%m-%d %H:%M:%S %z"` 实测）。
- **复核基线**：HEAD `2e5ce38`（`docs: 精简 ACTIVE_ISSUES 为纯开放项…`，2026-09-16 21:12）；`git status --porcelain` 为空（工作区干净）。
- **对照基线**：条目原文的核实基线为 HEAD `a669a48`（2026-09-13）；与当前 HEAD 的差异经 `git log a669a48..HEAD -- <path>` 单独核对。

> **2026-09-17 后续（阅读本文前先看这一条）**：本文复核的三条已按「产品裁决 / 待修问题」分流——
> **裁决部分**迁出 `ACTIVE_ISSUES.md` 至 [`architecture/产品裁决登记.md`](../architecture/产品裁决登记.md)
> （`PD-01` = 原 `P2-79` AC②、`PD-02` = 原 `P3-121` AC①、`PD-03` = 原 `P2-47` AC②）；
> **留下的待修缺口**转登为 `ISSUE-P2-87`（弱参数导入无告警）与 `ISSUE-P3-147`（同步能力文档口径不一致）。
> ⇒ 本文正文里对 `ACTIVE_ISSUES.md:xx` 的**行号引用是当时快照**，不再对应当前文件（本文属**取证留痕**，
> 按记录体例**不回改**）；「可直接粘贴片段」中的目标行号同理以**语义定位**为准。

## 核实方式（逐条）

| 编号 | 手段 | 具体命令 / 工具 |
|---|---|---|
| ① | 全仓符号检索 + 构造点枚举 + 持久化键枚举 + 用户可见文案检索 | `grep ssrfAllowedHosts`（全仓）；`grep 'SyncNetworkOptions('`（全仓，含测试）；`grep 'createSyncClient('`（全仓）；直读 `SyncNetworkOptions.kt` / `SyncProviderResolver.kt` / `SyncEndpointGuard.kt` / `SyncHttpClientFactory.kt` / `WebDavSyncProvider.kt` / `S3SyncProvider.kt` / `DalVerifierModule.kt` / `SyncCredentialsStore.kt` / `SyncPreferences.kt`；`grep '内网\|白名单\|SSRF\|自建\|公网' app/src/main/res/**/strings.xml`；直读 `README.md`；`grep '预留' docs/resolved/batches/86-*.md` |
| ② | 直读默认值常量 + 消费方全枚举 + 读/写路径逐行核对 + 历史版本对照 | 直读 `KdbxHeader.kt` / `SessionOpener.kt` / `KdbxFile.kt` / `KdbxKdfParameterCodec.kt` / `KdbxConstants.kt` / `KdfBenchmark.kt` / `SettingsKdfBenchmarkController.kt`；`grep KdfBenchmark`（全仓，逐条区分生产 / 测试 / 注释）；`grep 'KdbxHeader\.createDefault'`（全仓）；`git show a669a48:database/.../KdbxKdfParameterCodec.kt`（历史下界对照） |
| ③ | 直读裁决实现 + 引擎裁决点全枚举 + 回归用例检索 + 提交级差异核对 | 直读 `SyncRollbackGuard.kt` / `SyncEngine.kt` / `SyncEngineSupport.kt` / `SyncCycleRunner.kt` / `DatabaseModule.kt` / `KeystoreSyncIntegrityMac.kt` / `KeystoreManager.kt`；`grep 'ReplayDetected\|inspect\|recordAccepted\|RollbackVerdict' SyncEngine.kt`；`grep 'fun \`' SyncRollbackGuardTest.kt SyncEngineTest.kt`；`git log a669a48..HEAD -- .../SyncEngine.kt .../SyncRollbackGuard.kt`；`git blame -L 230,242 .../SyncEngine.kt`；`grep 'KdbxFile.kt:337'` 对应的 `KdbxFile.kt:328-345` 直读 |

---

## 结论速览

| 条号 | 正文前提判定 | 裁决依据判定 | 一句话证据 |
|---|---|---|---|
| `ISSUE-P3-121` | **仍成立** | **已失准（理由失真）** | 默认空集、两处默认构造、内网一律被拒、**确无生产逃生通道**均成立；但裁决理由「可经既有显式白名单机制自行配置」不成立——该参数无 UI / 无持久化 / 无 DI 装配点 |
| `ISSUE-P2-79` | **仍成立**（两处措辞精度待修正） | 依据成立 | 建库默认 `64 MiB/t=2/p=2`（`KdbxHeader.kt:168-170`）；`KdfBenchmark` 生产消费方仅设置页展示（`SettingsKdfBenchmarkController.kt:39`）；弱参数照收并在保存时原样保留（`KdbxFile.kt:332,337`） |
| `ISSUE-P2-47` | **部分失准**（唯一失准点＝裁决点计数） | 依据成立 | 摘要链仍为本地记录、`recent`→`ReplayDetected`、引擎拒绝落地均成立；但「**三处**裁决点」在 HEAD 上实为 **5 处**（`:108/:173/:215/:236/:277`），且 `:236` 在批注写就时即已存在 |

---

## ① `ISSUE-P3-121`：自建内网 WebDAV / NAS 在出厂配置下不可用

### 前提原文（`ACTIVE_ISSUES.md:97-101`）

> `SyncNetworkOptions.ssrfAllowedHosts` 默认空集（`SyncNetworkOptions.kt:25`），`SyncProviderResolver` 两处均传默认
> `SyncNetworkOptions()`（`:52,77`）→ RFC1918 / `.local` 主机一律被 `SyncEndpointGuard` 拒绝，且**无生产逃生通道**；
> 与 `README` 宣称支持 WebDAV（Nextcloud / ownCloud 等常见家用自建形态）存在张力。
>
> **用户裁决（2026-09-16）**：不给出厂可用口径……理由：……需要内网同步的用户**可经既有显式白名单机制自行配置**。

### 取证

**(1) 默认空集 —— 成立（行号精确）**

- `sync/src/main/java/com/keepasskey/sync/network/SyncNetworkOptions.kt:25`：
  `val ssrfAllowedHosts: Set<String> = emptySet()`。

**(2) 两处均传默认 `SyncNetworkOptions()` —— 成立（行号精确）**

- `app/src/main/java/com/keepasskey/app/sync/SyncProviderResolver.kt:52`（WebDAV 分支）、
  `:77`（S3 分支）——两处均 `networkOptions = SyncNetworkOptions()`（不设 `ssrfAllowedHosts`）。

**(3) 内网 / `.local` 主机一律被拒 —— 成立**

- `sync/src/main/java/com/keepasskey/sync/network/SyncEndpointGuard.kt:104-108`（`isHostAllowed`，白名单为空即恒 false）；
  `:116-124` 拒绝 `localhost` / `.localhost` / **`.local`** / `.internal`；
  `:125-130` + `:152-196` 拒绝字面 IP 的内网 / 保留网段（含 RFC1918、链路本地、ULA、100.64/10 等）；
  连接期 `SsrfGuardDns` `:223-241`（解析结果落内网段即抛 `UnknownHostException`）。
- 两处消费点：`WebDavSyncProvider.kt:80`、`S3SyncProvider.kt:105`（均在 `client == null` 的生产门控内，`:69` / `:94`）；
  客户端工厂 `SyncHttpClientFactory.kt:35`。

**(4) 「无生产逃生通道」—— 成立（本次独立复核，**确认**主控初步取证）**

本次复核在**写入本记录之前**全仓检索 `ssrfAllowedHosts` 命中 **10 处** = **代码 5 处** + 文档 5 处
（`ACTIVE_ISSUES.md:98,101`、`RESOLVED_LOG.md:100`、`退役审计承接-42…:63`、`resolved/batches/86…:17`）。代码 5 处为：

| 类别 | 位置 | 性质 |
|---|---|---|
| 定义 | `SyncNetworkOptions.kt:25` | 默认 `emptySet()` |
| 消费 | `SyncHttpClientFactory.kt:35` | 只读 |
| 消费 | `S3SyncProvider.kt:105` | 只读 |
| 消费 | `WebDavSyncProvider.kt:80` | 只读 |
| 注释 | `app/.../di/DalVerifierModule.kt:64-65` | 仅文字说明 |

- **无任何生产写入点**：全仓 `SyncNetworkOptions(` 命中 9 处 = 类声明 1 处（`SyncNetworkOptions.kt:14`）+ 文档提及 1 处
  （`ACTIVE_ISSUES.md:98`）+ 构造 / 默认表达式 7 处。后者逐条为：`SyncProviderResolver.kt:52,77`（两处默认值调用）、
  `SyncHttpClientFactory.kt:31`（工厂默认参数）、`WebDavSyncProvider.kt:42` / `S3SyncProvider.kt:60`（Provider 默认参数）、
  `DalVerifierModule.kt:73-78`（**未置 `ssrfAllowedHosts`**，仅覆盖三个超时 + callTimeout）、
  `sync/src/test/.../SyncHttpClientFactoryTest.kt:36-38`（测试）。**唯一非默认赋值在单测**：
  `sync/src/test/.../SyncEndpointGuardTest.kt:179`（`SsrfGuardDns(delegate, allowedHosts = setOf("nas.home"))`）。
- **无终端用户入口**：
  - 设置页字段仅 `webdavUrl / username / password / remotePath`（`SettingsSyncController.kt:146-183`、`CloudSyncConfigFields.kt`）
    与 `s3Endpoint / bucket / region / accessKey / secretKey / objectKey / usePathStyle`（`SettingsSyncController.kt:190+`）；
  - `grep '内网|白名单|SSRF|自建|公网' app/src/main/res/**/strings.xml` → **零命中**（无任何相关用户文案）；
  - 持久化面仅 `sync_credentials_prefs`，键枚举见 `SyncCredentialsStore.kt:299-318`（provider / webdav_url / webdav_username /
    webdav_remote_path / webdav_password_iv / webdav_password_cipher / 已废弃 cert_pins / s3_* 共 11 个常量）——**无白名单键**；
  - `SyncPreferences.kt` 只读 `ExtendedSettings`（分块上传 / 冲突策略 / 详细日志），**无白名单项**；
  - 全仓 `createSyncClient(` 调用方仅：两个 Provider（传 `networkOptions`）、`DalVerifierModule.kt:72`、4 处测试。
- **归档文档自述为「预留」**：`docs/resolved/batches/86-DAL出口纵深防御批次.md:17` 原文写
  「`ISSUE-P3-121` 为自建内网 WebDAV **预留**的逃生通道」——即该通道**从未实现**，仅保留了库级参数位。
- 代码 KDoc 亦自述为「预留」语义：`SyncNetworkOptions.kt:21-23`「供确需直连特定主机的场景经**上层**显式、可审计地放行；
  生产默认恒为空」——所谓「上层」在仓库内**不存在**。

⇒ **结论：「既有显式白名单机制」对终端用户不可达**。它是 `sync` 模块的一个**库级构造参数**，只有改代码（或写单测）才能置值。

**(5) `README` 与实现一致性（AC③）—— 仍未满足**

- `README.md:8`：「**多方式同步**：WebDAV（Nextcloud、ownCloud 等）与 S3 兼容协议（AWS S3、**MinIO**、Cloudflare R2 等）」；
  `:41`「设置页配置 WebDAV / S3 账号」——**无「仅公网 / 不支持自建」限定**。
- 实现侧 KDoc 明确相反：`SyncNetworkOptions.kt:11-12`「本应用仅面向正规公网商业云服务（**不支持自建服务器**）」；
  `SyncEndpointGuard.kt:13`「本应用仅面向正规公网商业云（AWS S3 / R2 / MinIO **公有端点**、Nextcloud / 坚果云等**公有** WebDAV）」。
- Nextcloud / ownCloud / MinIO 均为**可自建**产品，README 未作「公有托管」限定 ⇒ **文档与实现口径不一致**。
- 第四轮定版口径「原表述『与 `README` 矛盾』过强（须先定产品口径再谈文档一致性）」——**产品口径已于 2026-09-16 由用户定下
  （默认拒绝内网 / 不给出厂可用口径）**，故 AC③ 现已是**可执行且未执行**的状态。

### 判定

| 子项 | 判定 |
|---|---|
| `ssrfAllowedHosts` 默认空集（`:25`） | **仍成立** |
| `SyncProviderResolver` 两处默认构造（`:52,77`） | **仍成立** |
| RFC1918 / `.local` 一律被拒 | **仍成立** |
| **无生产逃生通道** | **仍成立**（本次独立复核确认） |
| 与 README 存在张力 | **仍成立**（且已由第四轮口径转成 AC③ 未满足） |
| **裁决理由「可经既有显式白名单机制自行配置」** | **已失准（不成立）** |

### 建议的正文修正文字（可直接粘贴，供主控应用；本代理未改 `ACTIVE_ISSUES.md`）

替换 `ACTIVE_ISSUES.md:97` 的核实行：

```markdown
- **核实时间点与核实方式（2026-09-13 对 HEAD `a669a48`；2026-09-16 21:37 对 HEAD `2e5ce38` 复核）**：威胁建模 T-17；直读
  `SyncNetworkOptions.kt:25`、`SyncProviderResolver.kt:52,77` 与 `SyncEndpointGuard`。**复核补充**：全仓 `ssrfAllowedHosts`
  得 5 处代码命中（定义 `:25`、消费 `SyncHttpClientFactory.kt:35` / `S3SyncProvider.kt:105` / `WebDavSyncProvider.kt:80`、
  注释 `DalVerifierModule.kt:64`），**无任何生产写入点**；`SyncNetworkOptions(` 构造点仅 `SyncProviderResolver.kt:52,77`
  （默认）与 `DalVerifierModule.kt:73`（未置该字段）；`SyncCredentialsStore.kt:299-318` 键枚举与 `strings.xml` 均无对应入口。
```

在 `ACTIVE_ISSUES.md:101`「用户裁决」之后**追加**（不改动裁决本身，只标注前提）：

```markdown
- **裁决理由前提更正（2026-09-16 复核，须用户重新裁决）**：裁决理由所称「需要内网同步的用户可经**既有显式白名单机制
  自行配置**」**不成立**——`ssrfAllowedHosts` 仅是 `sync` 模块的**库级构造参数**（见上「复核补充」），**无 UI 入口、
  无持久化键、无 DI 装配点**；唯一非默认赋值在单测 `SyncEndpointGuardTest.kt:179`。归档文档
  `docs/resolved/batches/86-DAL出口纵深防御批次.md:17` 亦自述该通道为「**预留**」。
  ⇒ 该裁决的实际语义是「**内网 / 自建同步对终端用户彻底不可用**」，而非「默认收紧但可自配」。**待用户重新裁决**。
```

### 与 AC③ 的连带（同一批内应一起处置）

AC③「文档与实现必须一致，不得只改其一」当前**未满足**。可选的衔接动作（**属产品口径，须用户裁决，不得由实施者自行择一**）：

- **A 案（仅收文档）**：把 `README.md:8` 改为「WebDAV（Nextcloud / ownCloud **等公有托管服务**）与 S3 兼容协议
  （AWS S3、Cloudflare R2、**MinIO 公有端点**等）……**不支持自建内网 / 私有网段端点**」，并在「已知局限」段
  （`README.md:55-61`）登记该口径；
- **B 案（补受控逃生通道 = 原 AC②）**：新增用户显式声明内网主机 + 风险二次确认的设置入口（默认仍拒绝），
  并把 `ssrfAllowedHosts` 从「预留参数」接成真实可配置项——代价是重新打开「同步端点被指向内网」的口子，
  须同时给出审计与默认拒绝证据。

---

## ② `ISSUE-P2-79`：KDF 强度基线未对齐

### 前提原文（`ACTIVE_ISSUES.md:81-87`）

> ① 建库默认 `Argon2id m=64 MiB / t=2 / p=2`（`KdbxHeader.kt:162-171`），而仓库已具备设备自适应推荐 `KdfBenchmark`
> （`SettingsKdfBenchmarkController.kt:17-49`）——**仅设置页展示，建库路径零消费**（核实：`SessionOpener.create` 直接走
> `KdbxHeader.createDefault`）；② 读取路径接受极弱参数（`m=1 MiB, t=1, p=1`；AES-KDF `R=1`）并在保存时**原样保留**（仅刷新盐）。
>
> **第四轮定版口径**：AC① 仓库已满足；**AC② 含削弱陷阱**——直接接入建议值会出现 `8 MiB < 64 MiB` 的**降强**，须取 `max(建议值, 现默认)`。

### 取证

**(1) 建库默认参数当前实际值 —— 成立（行号精确）**

- `database/src/main/java/com/keepasskey/database/file/KdbxHeader.kt:145` `fun createDefault(...)`；
  `:162-171` Argon2 分支，逐字为：
  - `:166` `type = Argon2Type.ARGON2ID`
  - `:168` `parallelism = 2`
  - `:169` `memoryInBytes = 64L * 1024 * 1024`
  - `:170` `iterations = 2L`
- AES 分支 `:175-178`：`rounds = KdbxConstants.Kdf.DEFAULT_AES_KDF_ROUNDS`
  （`core/src/main/java/com/keepasskey/core/model/KdbxConstants.kt:97` = `6_000_000L`）。

**(2) `KdfBenchmark` 全部消费方 —— 「仅设置页展示、建库路径零消费」成立**

全仓 `KdfBenchmark` 命中 73 处，逐个归类后**生产代码消费方只有 1 处**：

| 类别 | 位置 |
|---|---|
| **生产消费（唯一）** | `app/src/main/java/com/keepasskey/app/ui/screens/settings/SettingsKdfBenchmarkController.kt:39`（`KdfBenchmark.benchmarkArgon2(...)`）；入口链 `SettingsViewModel.kt:288` → `KeePasskeySettingsNavGraph.kt:50` → `DatabaseSettingsScreen.kt:330` → `DatabaseSettingsArgon2Dialog.kt:153`，**仅展示推荐值** |
| 生产注释 / KDoc（**非消费**） | `KdbxKdfParameterCodec.kt:47,72`、`Argon2KdfEngine.kt:170`、`AesKdfEngine.kt:11`、`crypto/src/main/rust/src/aes_kdf.rs:13` |
| 测试 | `crypto/src/test/.../KdfBenchmarkTest.kt`（全文件）、`KdbxParametersBoundsTest.kt:116,128`、`Argon2KdfBoundsMirrorTest.kt:118`、`Argon2KdfEngineBoundsTest.kt:68`、`KdfWallClockDeviceTest.kt:121` |
| 文档 | `ACTIVE_ISSUES.md`、`SECURITY_RECHECK_2026-09.md:102`、`退役审计承接-43…:176`、`原生Argon2真机验证记录.md:386`、`resolved/batches/48,76,78` |

- `KdfBenchmark.benchmarkAesKdf`（`KdfBenchmark.kt:61`）的生产消费方**为零**（仅 `KdfBenchmarkTest`）。
- 建库路径：`database/src/main/java/com/keepasskey/database/session/SessionOpener.kt:55` `fun create(...)` → `:69-72`
  `KdbxHeader.createDefault(cipherUuid = cipherUuid, useArgon2 = useArgon2)`——**不读取任何推荐值**。
  该调用点自 `a669a48` 起仅增加了 `cipherUuid` 参数（`ISSUE-P2-85`，外层算法预设），**KDF 参数仍未接线**。
- 其余 `KdbxHeader.createDefault` 生产调用方：`database/src/main/java/com/keepasskey/database/xml/KdbxXmlSerializer.kt:98`
  （同样走默认值），无非默认 KDF 路径。

**(3) 读取路径接受极弱参数并在保存时原样保留 —— 成立（代码位置如下）**

- **读取**：`database/.../file/KdbxKdfParameterCodec.kt:152-200` `deserialize`；
  Argon2 分支 `:164-197`，边界裁决 `validateArgon2Bounds` `:210-242`：
  - `ARGON2_MIN_MEMORY_BYTES = 8192L`（`:90`）——**注意：真实下界是 8192 字节（官方 `MinMemory`），不是条目举例的 1 MiB**；
    该值在条目对照基线 `a669a48` 上**已是 8192**（`git show a669a48:database/.../KdbxKdfParameterCodec.kt` 第 54 行），非本轮回退；
  - 迭代下界 `1`（`:216`）、并行度下界 `1`（`:219`）、联合预算 `2^33`（`:124` / `:231`）、动态堆门槛 `maxMemory/2`（`:238-241`）；
  - ⇒ `m=1 MiB, t=1, p=1` **确实被接受**（`1 MiB ≥ 8192`、`t=1 ≥ 1`、`p=1 ≥ 1`、`1 MiB × 1 ≪ 2^33`）。
- **AES-KDF**：`validateAesKdfBounds` `:261-265` 仅拒 `rounds < 1` ⇒ `R=1` **被接受**。
- **保存原样保留**：`database/.../file/KdbxFile.kt:328-339`
  - `:329-333` AES：`p.copy(seed = freshSeed)`——**只换 seed，`rounds` 原样**；
  - `:334-338` Argon2：`p.copy(salt = freshSalt)`——**只换 salt，`m/t/p/V` 原样**；
  - `:341-345` `header.copy(masterSeed = …, encryptionIv = …, kdfParameters = freshKdfParams)`。
  ⇒ 「仅刷新盐」**逐字成立**。

**(4) 第四轮批注「AC② 含削弱陷阱（8 MiB < 64 MiB），须取 `max(建议值, 现默认)`」—— 仍准确（且可补全）**

- `crypto/src/main/java/com/keepasskey/crypto/kdf/KdfBenchmark.kt:44` `MIN_ARGON2_MEMORY_BYTES = 8 MiB`；
  `:45` `MAX_ARGON2_MEMORY_BYTES = 512 MiB`；`:46` `MAX_MEMORY_RATIO = 0.60`；
  `:49-52` `MIN_ARGON2_ITERATIONS = 1 / MAX = 20`、`MIN_ARGON2_PARALLELISM = 1 / MAX = 4`。
- `benchmarkArgon2`（`:80-104`）：`:86-87` 内存 = `(available × 0.60).coerceIn(8 MiB, 512 MiB)`；
  `:91` 并行度 = `cores.coerceIn(1, 4)`；`:97` 迭代 = `(1000 / elapsed).coerceIn(1, 20)`。
  ⇒ 建议值**可能是** `m ∈ [8, 64) MiB`（低堆设备）、`t = 1`（慢设备）、`p ∈ {1,3,4}`——**三项都可能弱于现默认**，而永远不会超过 512 MiB。
- ⇒ `8 MiB < 64 MiB` 的降强陷阱**仍然准确**；且**该批注只点了内存一项**，**迭代（建议 1 < 默认 2）与并行度（1/3/4 ≠ 2）
  同样存在降强面**，`max(建议值, 现默认)` 的口径应**逐参数**给出，否则按批注实施仍会漏掉 `t=1` 的降强。

### 判定

| 子项 | 判定 |
|---|---|
| ① 建库默认 `Argon2id m=64 MiB / t=2 / p=2`（`KdbxHeader.kt:162-171`） | **仍成立**（行号精确：`:168-170`） |
| ① `KdfBenchmark` 仅设置页展示、建库路径零消费 | **仍成立**（生产消费方唯一：`SettingsKdfBenchmarkController.kt:39`） |
| ② 读取路径接受极弱参数并在保存时原样保留 | **仍成立**（`:210-242` / `:261-265` 接受；`KdbxFile.kt:332,337` 原样保留） |
| ② 「极弱」举例 `m=1 MiB` | **措辞精度问题**：真实下界为 8192 字节（`:90`），举例比真实下界**更保守**，易被误读为「1 MiB 是下界」 |
| ④ 第四轮批注「须取 `max(建议值, 现默认)`」 | **仍准确**；建议补全为「**逐参数**取 max（内存 / 迭代 / 并行度三项）」 |

### 建议的正文修正文字（可直接粘贴）

`ACTIVE_ISSUES.md:84` 第 ② 项，替换为：

```markdown
  - ② 读取路径接受极弱参数并在保存时**原样保留**（仅刷新盐/种子）——**下界为官方 `MinMemory = 8192` 字节
    （`KdbxKdfParameterCodec.kt:90`），故 `m=1 MiB, t=1, p=1` 只是示例而非下界**；AES-KDF 下界 `R=1`
    （`:261-265`）。工作因子在保存时逐字保留：`KdbxFile.kt:332`（AES `p.copy(seed = freshSeed)`）、
    `:337`（Argon2 `p.copy(salt = freshSalt)`）。
```

`ACTIVE_ISSUES.md:86` 第四轮定版口径，替换为：

```markdown
- **第四轮定版口径**：AC① **仓库已满足**（`KdfBenchmark` 已存在）；**AC② 含削弱陷阱**——直接接入建议值会出现
  `8 MiB < 64 MiB` 的**降强**（`KdfBenchmark.kt:44`），须取 `max(建议值, 现默认)`；**2026-09-16 复核补充**：该
  `max` 须**逐参数**施加——建议迭代可低至 `1 < 默认 2`、建议并行度可为 `1/3/4 ≠ 默认 2`（`KdfBenchmark.kt:91,97`），
  仅对内存取 max 仍会漏掉后两项降强。
```

---

## ③ `ISSUE-P2-47`：同步与封印凭据的回滚防护不足

### 前提原文（`ACTIVE_ISSUES.md:48,50-54`，节选）

> - **第四轮定版批注（2026-09-13）**：`SyncRollbackGuard.State.sequence` **已持久化但不参与裁决**（写点 `:142`/`:180`；
>   裁决点 `inspect :118-127` 只比对 `current`/`recent`；类 KDoc `:76-80` 自认）……防护现状为「Keystore-HMAC 已见摘要链 +
>   状态缺失 / MAC 失效 **fail-open**（`:145-158`，已留痕取舍）」。
> - **AC① 的实质已具备（复核确认）**：`SyncRollbackGuard` 的「已见内容摘要链」本身就是**本地记录**（不依赖任何服务端版本号）；
>   命中 `recent` 即 `ReplayDetected`，`SyncEngine` 三处裁决点（`:108` / `:173` / `:215`）均**不应用**远端内容，经
>   `SyncCycleRunner:387` 落为 `SyncOutcome.Error(sync_error_rollback_rejected)`——**不存在静默覆盖**。
> - **AC③ 已完成（§90 批次）**：`SyncRollbackGuardTest` 已有 8 例……§90 据此补齐两例：
>   `重放被拒后本地缓存与基线不被覆盖` 与 `上传路径遇远端重放拒绝合并并保留本地`。

### 取证

**(1) 「已见内容摘要链」仍为本地记录、命中 `recent` 仍 `ReplayDetected`、引擎不应用远端内容 —— 成立**

- **本地记录**：`sync/src/main/java/com/keepasskey/sync/engine/SyncRollbackGuard.kt:89-92`（`stateDir` + `integrityMac`）；
  生产 `stateDir = filesDir/rollback`——`app/.../di/DatabaseModule.kt:84-85`（`File(context.filesDir, STATE_DIR_NAME)`）、
  `SyncCycleRunner.kt:105`；类 KDoc `:35-42`「**本地认证**的『已见内容摘要链』」、`:84-87`「不依赖服务端版本号」。
  MAC 由 `KeystoreSyncIntegrityMac`（`app/.../sync/KeystoreSyncIntegrityMac.kt:23-54`，应用级 HMAC-SHA256 密钥，不可导出）
  实现，`SyncIntegrityModule.kt:22-24` 绑定。
- **裁决逻辑**：`SyncRollbackGuard.kt:118-127` `inspect`：
  `:122` `state.current == null → Accept`；`:123` `state.current == digest → Unchanged`；
  `:124` `digest in state.recent → ReplayDetected`；`:125` else `Accept`。**只比对 `current`/`recent`，`sequence` 未参与**。
- **唯一消费方**：`SyncEngine.kt:30` `rollbackGuard?.inspect(...) == RollbackVerdict.ReplayDetected`（私有 `isReplay`）。
- **引擎裁决点（全枚举）**：HEAD 上 `isReplay` 共 **5 处**，**全部**返回 `RollbackRejected` 且**不写缓存、不写基线、不参与三方合并**：

  | # | 行号 | 场景 | 拒绝后动作 |
  |---|---|---|---|
  | 1 | `:108` | 未缓存首次下载 | `:109` 直接返回 `RollbackRejected`（在 `cache.writeCache` `:112` / 基线前移 `:113` **之前**） |
  | 2 | `:173` | 已缓存、本地无改动、远端有更新 | `:174` 返回（在 `:176-178` 写缓存 / 基线之前） |
  | 3 | `:215` | 本地有改动、上传遇 `ConflictError` 后下载到重放内容 | `:216-219` 返回（在 `ConflictDetected` 三方合并之前） |
  | 4 | `:236` | 本地与远端双方都有改动 | `:237` 返回（在 `:240` `ConflictDetected` 之前） |
  | 5 | `:277` | `commitLocal` 冲突后下载到重放内容 | `:278` 返回 `SyncCommitResult.RollbackRejected(keptLocal = true)`（不进入 `ConflictNeedsMerge` `:280`） |

- **落为显式错误**：`app/.../sync/SyncCycleRunner.kt:387-388`（`SyncOpenResult.RollbackRejected → SyncOutcome.Error(sync_error_rollback_rejected)`）
  与 `:285-286`（`SyncCommitResult.RollbackRejected → 同一错误`）⇒ **不存在静默覆盖**。
- **fail-open 取舍**：`SyncRollbackGuard.kt:145-158`（文件缺失 / 读失败 / MAC 校验失败 → `State()`）→ `inspect :122` `Accept`；行号与批注 `:145-158` **精确**。

**(2) `State.sequence` 仍「已持久化但不参与裁决」—— 成立，且行号全部精确**

- 写点 `:142`（`recordAccepted` 内 `persist(remotePath, State(state.sequence + 1, digest, recent))`）与 `:180`
  （`persist` 载荷 `"$KEY_SEQUENCE=${state.sequence}"`）——**与批注 `:142`/`:180` 逐字一致**。
- 读取 `:160` / `:169` / `:174`；`data class State` `:100-104`。
- 裁决点仍为 `inspect :118-127`（**不读 `sequence`**）；类 KDoc `:76-80` 明写「**当前不参与任何裁决**」「**启用前不得据此
  推断防回滚强度**」——与批注 `:76-80` **精确一致**。

**(3) AC③ 回归用例仍存在且未失效 —— 成立**

- `sync/src/test/java/com/keepasskey/sync/engine/SyncRollbackGuardTest.kt`：**8 例**，逐条为
  `:37 首次内容可接受且记录后同一内容视为未变`、`:47 曾接受过的历史版本被判为重放`、`:60 全新内容不误报以兼容其他客户端写入`、
  `:74 F23 接受A后接受B_经历缓存清理_重放A仍被判为重放`、`:104 F23 SyncCache 清理不得删除防回滚状态文件`、
  `:133 F23 状态文件缺失时按无历史处理`、`:146 F23 升级迁移后旧目录状态不被读取_首轮按无历史处理`、
  `:166 状态MAC失效时按无历史处理不产生误报回退` ⇒ **与条目「已有 8 例」一致**。
- `sync/src/test/java/com/keepasskey/sync/SyncEngineTest.kt`：
  `:487 ISSUE_P2_18 被入侵端点重放旧库被拒绝应用`（断言 `:502-503` `RollbackRejected` + 返回字节）、
  `:507 ISSUE_P2_18 其他客户端写入的全新内容不误报`、
  `:519 ISSUE_P2_18 重放被拒后本地缓存与基线不被覆盖`（断言 `:538-545`：缓存与 **base content** 均仍为 `v2`）、
  `:549 ISSUE_P2_18 上传路径遇远端重放拒绝合并并保留本地`（断言 `:566-578`：`RollbackRejected` + `keptLocal` + 本地缓存保留 `local`）
  ⇒ **§90 补齐的两例都在，且断言未放宽**（仍逐字节比对缓存与基线内容）。

**(4) 是否有任何一项前提已被后续批次改变 —— 除 ① 的计数外，其余未被改变**

- `git log a669a48..HEAD -- sync/.../engine/SyncEngine.kt` → 仅 `803fbd1`（重构：移除冗余包名导入）与其 `f54dd0b`（Revert）
  ⇒ **语义零变更**；当前 `:108/:173/:215` 三处行号**仍精确落点**。
- `git log a669a48..HEAD -- sync/.../engine/SyncRollbackGuard.kt` → **空**（该文件自 `a669a48` 起未改动）。
- AC② 的取证前提仍精确：`database/.../file/KdbxFile.kt:337` `p.copy(salt = freshSalt)`、`:342` `masterSeed = freshMasterSeed`
  （本次直读 `:328-345` 确认，含 AES 侧的 `:332` `p.copy(seed = freshSeed)`）。
- `KeystoreManager.kt:206-218` 注释仍在（`:209` 「无用户认证门控」）——但**该处实为 `ISSUE-P1-09` 解锁通行密钥完整性 HMAC
  的说明**，并非同步防回滚 MAC 的密钥定义；同步侧对应实现是 `KeystoreSyncIntegrityMac.kt:14-21` 与 `:57-58`
  （「**不绑定用户认证**——后台同步需在锁屏态亦可用」）。两处结论方向一致，**批注的引用出处不精确**（不影响结论）。

**(5) 唯一失准点：「三处裁决点」的计数**

- `ACTIVE_ISSUES.md:51` 与 `docs/architecture/已知工程限界.md:210-212` 均写「`SyncEngine` **三处**裁决点（`:108` / `:173` / `:215`）」。
- HEAD 实为 **5 处**（见上表）。`git blame` 显示 `:236` 引入于 `dfda33d0`（**2026-09-11**，早于批注写就的 2026-09-16），
  `:277`（`commitLocal`）亦早已存在 ⇒ **该计数在批注写就时即不完整**，非本轮回退。
- 另：批注同段的「AC③ 已完成」措辞已自述 §90 补齐的用例覆盖「**此前完全无用例**的 `SyncCommitResult.RollbackRejected` 分支」，
  该分支正对应 `:277` —— 即文档自身已隐含第 4/5 处裁决点，与「三处」计数自相矛盾。

### 判定

| 子项 | 判定 |
|---|---|
| ① 摘要链为**本地记录**（不依赖服务端版本号） | **仍成立**（`DatabaseModule.kt:84-85`；`SyncRollbackGuard.kt:35-42,84-87`） |
| ① 命中 `recent` → `ReplayDetected` | **仍成立**（`SyncRollbackGuard.kt:124`） |
| ① 引擎裁决点**不应用**远端内容、无静默覆盖 | **仍成立**；但「**三处**」计数 **已失准** —— 实为 **5 处**（`:108/:173/:215/:236/:277`，全部拒绝） |
| ① 经 `SyncCycleRunner:387` 落为显式错误 | **仍成立**（`:387-388`，另 `:285-286`） |
| ② `State.sequence` 已持久化但不参与裁决 | **仍成立**，行号 `:142`/`:180`/`:118-127`/`:76-80` **全部精确** |
| ② 状态缺失 / MAC 失效 fail-open（`:145-158`） | **仍成立**，行号精确 |
| ③ 8 例 + §90 两例仍在且未失效 | **仍成立** |
| ④ 前提被后续批次改变 | **除「三处」计数外，未被改变**（`SyncEngine.kt` 仅重构 + revert；`SyncRollbackGuard.kt` 零改动） |

### 建议的正文修正文字（可直接粘贴）

`ACTIVE_ISSUES.md:51`，替换该句：

```markdown
  - **AC① 的实质已具备（复核确认）**：`SyncRollbackGuard` 的「已见内容摘要链」本身就是**本地记录**（不依赖任何服务端版本号）；
    命中 `recent` 即 `ReplayDetected`，`SyncEngine` 的**全部重放裁决点**（2026-09-16 复核：**:108 / :173 / :215 / :236 / :277
    共 5 处**；原文「三处」漏计 `:236`（双方改动分支）与 `:277`（`commitLocal` 分支））均**不应用**远端内容，经
    `SyncCycleRunner:387`（另见 `:285` 的 `SyncCommitResult.RollbackRejected`）落为
    `SyncOutcome.Error(sync_error_rollback_rejected)`——**不存在静默覆盖**。
```

> **连带修改（主控须一并安排，本代理未改）**：`docs/architecture/已知工程限界.md:210-212` §5 有**同一句「三处裁决点」**措辞。
> 该文件顶部「措辞纪律」规定：**改写措辞等同于变更结论，须走一次完整批次（归档 + 提交）**——故该处修正不能顺手改，
> 应与 `ACTIVE_ISSUES.md` 的修正放入**同一批次**处置。

---

## 据失真前提做出的裁决（**须用户重新裁决，本代理不自行裁决**）

### A. `ISSUE-P3-121` 的 2026-09-16 用户裁决 —— **理由失真**

- **裁决原文（`ACTIVE_ISSUES.md:101`）**：「**不给出厂可用口径**——`ssrfAllowedHosts` **维持默认空集**……
  理由：这是 SSRF 纵深防御的**默认收紧**……需要内网同步的用户**可经既有显式白名单机制自行配置**（该项 AC① 的
  「明确产品口径」即为本裁决）。」
- **失真点**：后半段理由的「**既有显式白名单机制**」**不存在用户可达形态**——`ssrfAllowedHosts` 是纯库级构造参数，
  无 UI 入口、无持久化键（`SyncCredentialsStore.kt:299-318` 键枚举）、无 DI 装配点、无用户文案（`strings.xml` 零命中）；
  唯一非默认赋值在单测（`SyncEndpointGuardTest.kt:179`）。归档文档自述为「**预留**」的通道
  （`docs/resolved/batches/86-DAL出口纵深防御批次.md:17`）。
- **影响**：该裁决被理解为「**默认收紧，但高级用户可自配**」；实际语义是「**内网 / 自建同步对终端用户彻底不可用，
  且无任何自配路径**」。二者在产品定位、README 文案（AC③）与是否排期 AC② 上导向**不同结论**。
- **需要用户重新裁决的具体问题（二选一或组合，**不得由实施者自行择一**）**：
  1. **产品口径是否维持「不支持自建 / 内网端点」**？若维持——是否同批修正 `README.md:8`（去掉/限定 Nextcloud、ownCloud、
     MinIO 的自建含义）并在「已知局限」段登记该口径，使 AC③ 闭环？
  2. **是否排期原 AC② 的受控逃生通道**（用户显式声明内网主机 + 风险二次确认，默认仍拒绝）？若不排期，
     `ssrfAllowedHosts` 应否显式登记为「预留未接线参数」并收窄其 KDoc 措辞（`SyncNetworkOptions.kt:21-23`
     现称「经**上层**显式、可审计地放行」，而该「上层」在仓库内不存在）？

### B. `ISSUE-P2-79` / `ISSUE-P2-47` 的裁决 —— **未发现理由失真**

- `ISSUE-P2-79` 裁决「建库默认维持 `Argon2id m=64 MiB / t=2 / p=2`，不接入 `KdfBenchmark` 建议值」所依赖的全部前提
  （默认值、消费方、削弱陷阱）**均仍成立**，故该裁决理由**不失真**。
- `ISSUE-P2-47` 裁决「AC② 本轮不实施、仅留痕；AC① 实质已具备；AC③ 已完成」所依赖的前提**除「三处」计数外均成立**；
  该计数为**低估**（实为 5 处、全部拒绝），**不改变**「AC① 实质已具备」的结论，故该裁决理由**不失真**（属措辞需更正，
  非理由失真）。

---

## 新发现（非文档问题的缺陷）

**本次复核未发现新的代码级缺陷。** 三条条目的安全相关前提经独立取证后**均未出现「防护比文档更弱」的反向偏差**：

- ① `ssrfAllowedHosts` 默认空集、内网恒拒——**fail-closed**，无回退风险；
- ② 弱 KDF 参数被接受属**条目已登记**的开放项（AC③ 未实施），非新缺陷；
- ③ 引擎重放裁决点实为 **5 处**（多于文档所述 3 处），**防线强于文档描述**。

以下两项**属文档 / 接口措辞问题**（非代码缺陷），单列供主控分流，**不建议按缺陷修复**：

| 项 | 内容 | 涉及文件 | 建议严重度 |
|---|---|---|---|
| D-1 | KDoc 声称存在一条「经上层显式、可审计地放行」的豁免通道，但仓库内**不存在该上层**（无 UI / 无持久化 / 无 DI） | `sync/src/main/java/com/keepasskey/sync/network/SyncNetworkOptions.kt:21-23`；`SyncEndpointGuard.kt:29-30` | **P3（INFO）** —— 措辞收窄为「预留参数，当前无生产消费方」，或随 AC② 一并接线 |
| D-2 | 「预留的逃生通道」表述散见于归档批次与 DAL KDoc，易被读作「已可用」 | `docs/resolved/batches/86-DAL出口纵深防御批次.md:17`；`app/.../di/DalVerifierModule.kt:64-65` | **P3（INFO）** —— 可在上述同一批次内补「当前无生产写入点」限定 |

---

## 不确定性（如实区分）

**已核实（有 file:line / 命令证据）**

- ① 的全部四项正文前提 + 「无生产逃生通道」+ README 一致性现状 + 裁决理由失真。
- ② 的建库默认值、`KdfBenchmark` 生产消费方唯一性、弱参数读取与保存原样保留、削弱陷阱成立。
- ③ 的本地记录属性、`inspect` 裁决逻辑、5 处引擎裁决点及各自行号、`State.sequence` 写点/裁决点、fail-open 行号、
  4 条引擎回归用例与 8 条守卫用例、`a669a48..HEAD` 的零语义变更。

**未能核实 / 未取证的边界（不作断言）**

- **未运行测试**：本任务明确不调用 Gradle，故「回归用例仍存在且断言未放宽」是**直读源码**的判定，**未实际执行**
  `:sync:testDebugUnitTest` 验证其当前是否全绿。
- **未取设备侧证据**：`KdfBenchmark` 在真实设备上的**实际推荐值区间**（内存 / 迭代 / 并行度）未实测，
  结论仅由 `KdfBenchmark.kt` 的 `coerceIn` 边界**静态推得**；`deviceAvailableMemoryBytes()` 取 `ActivityManager.memoryClass`
  （`SettingsKdfBenchmarkController.kt:58-61`），其与 `benchmarkArgon2` 形参文档所写「设备当前可用物理内存」的**语义差异**
  未进一步评估。
- **未核 `README` 是否另有他处承诺内网同步**：本次仅检索 `WebDAV|Nextcloud|ownCloud|NAS|内网|自建|MinIO|S3` 于 `README.md`
  （3 处命中，见上）；**未**对 `docs/` 全量用户可见文案做同样检索（`AGENTS.md` §4 的文档地图之外未穷举）。
- **未评估 AC② 实施代价**：①-B 案（受控逃生通道）与 ③-AC②（封印载荷绑定内容摘要）的**工作量 / 回归面**均未评估——
  属架构决策范畴，本次只做前提取证。
