# 第四轮复核「遗留编号」逐条处置（ISSUE-P3-141 收口）

> **文档定位**：本文件是 `ISSUE-P3-141`（「第四轮复核分批判定的处置状态不可核对」）的**收口文档**。
> 它把「仅存在于复核工作目录（`.audit-recheck/`，`.gitignore` 排除、不随仓库分发）」的编号**逐条**给出
> 四选一结论（已由某条目覆盖 / 已在本批修复 / 确属新缺陷 / 前提不成立）与可核对证据，使
> 「复核未遗留未登记缺陷」从**断言**变为**可核对**——工作目录退役后，判定依据仍可从仓库复原。
>
> **核实时间点与核实方式（2026-09-16）**：
> 1. **基线**：仓库 HEAD `2e5ce38`（`docs: 精简 ACTIVE_ISSUES 为纯开放项…`）。`ISSUE-P3-141` 正文所记的
>    `ba11edc` 是 §102 批次的核实基线，与本文件的复核基线**不同**（行号漂移已实测，见第 2 节）。
> 2. **手段 A（编号抽取）**：以 POSIX ERE 对 `.audit-recheck/**.md` 全量抽取
>    `ISSUE-P[0-9]-[0-9]+` / `NEW-B[0-9]{2}-[0-9]+` / `ANDROID-[0-9]+` / `NEW-FINDING-[0-9]+` / `IPC-[0-9]+` /
>    `SUPPLY-[0-9]+` 等编号，与 `docs/security/SECURITY_RECHECK_2026-09.md`、`docs/ACTIVE_ISSUES.md`、
>    `docs/RESOLVED_LOG.md`、`docs/resolved/**`、`docs/architecture/**` 侧抽取结果做差集。
> 3. **手段 B（主题复核）**：对每个编号**按主题**（而非仅按字面编号）在报告与归档中检索——**这一步是必需的**，
>    因为报告 §11 对部分工作目录编号使用了**别名**（见第 2 节）。
> 4. **手段 C（代码取证）**：直读当前源码核实各编号前提是否仍成立（行号以本次直读为准）。
> 5. **手段 D（历史取证）**：`git log --grep=<编号>`、`git log -L`、`git ls-files`、`git check-ignore -v` 做对象级核实。
>
> **本文件不含任何未取证结论**；无法确证的项一律写入第 7 节「剩余边界」，不写入四选一。

---

## 1. 计数核对：22 / 23 / 25

`ISSUE-P3-141` 正文自称「22 个编号」，但**其自身枚举为 23 个**（逐项计数：`ANDROID-01/06/10` 3 +
`ISSUE-P1-04/05` 2 + `ISSUE-P2-08` 1 + `ISSUE-P3-25/40` 2 + `NEW-B01-1/5/6` 3 + `NEW-B02-1/5` 2 +
`NEW-B04-01/03/04/05/06` 5 + `NEW-B09-01~05` 5 = **23**）。而 §102 批次正文（`102-建库预设算法接线与末尾残块明文清零批次.md:117-128`）
的枚举集合与 `ACTIVE_ISSUES.md:111` **并不相同**：

| 枚举来源 | 独有的编号 | 条数 |
|---|---|:--:|
| `ACTIVE_ISSUES.md:111` | `ISSUE-P3-25`、`ISSUE-P3-40` | 23 |
| §102 批次 `102.3` | `ANDROID-02`、`NEW-B02-3` | 23 |
| **并集（本文件覆盖范围）** | — | **25** |

> **处置口径**：本文件对**两处枚举的并集（25 个编号）逐条**给出结论；第 3 节的 25 行中，除
> `ANDROID-02`（仅 §102 枚举）与 `NEW-B02-3`（仅 §102 枚举）两行外，其余 **23 行即 `ACTIVE_ISSUES.md:111`
> 枚举的全覆盖**。**「22」这一计数在两处枚举中均无法复原**——这本身是对拍脚本口径未固定的留痕，
> 不另立条目，但建议 §104 批次在归档时把口径固定为**并集**。

---

## 2. 造成「仅存在于工作目录」的两个机制（必须先说清，否则结论会被误读）

| # | 机制 | 实证 | 后果 |
|---|---|---|---|
| M-1 | **原报告对部分工作目录编号使用了别名** | 报告 §11（`docs/security/SECURITY_RECHECK_2026-09.md:836-837`）把工作目录的 `NEW-B09-01` 记为 **`NEW-B09-x`**、`NEW-B04-06` 记为 **`NEW-B04-x`** | **纯 ID 对拍必然把它们判为「未进入报告」**，而事实上报告已收录其内容与归入的根因族。⇒ 编号对拍只能作为**线索生成器**，结论必须走主题检索（手段 B） |
| M-2 | **历史已闭环编号会以「源码注释引用」的形态出现在工作目录** | `ISSUE-P1-04`（`batches/B01-findings.md:1279` 引 `UnlockViewModel` 注释）、`ISSUE-P1-05`（`B09-findings.md:1093`）、`ISSUE-P2-08`（`B04-findings.md:1759,1770`）、`ISSUE-P3-40`（`B09-findings.md:123`） | 这些编号是**复核时被引用的既有已闭环条目**，不是复核产出的新判定。把注释里的编号当作「未处置判定」是**假阳性** |

> **行号漂移实测（本轮）**：报告 §6.8 `IPC-02` 行写的整改落点是 `PasskeyAssertionActivity.kt:112`，
> 本次直读实测在 **`:144`**（另 `:78` 有同型表述）；`B09-findings.md:1571` 写的 `endpointOverride` 在
> `DigitalAssetLinksVerifier.kt:53-59`，整改后该字段已消失（现为 `:45-54` 的只读策略注入）。
> ⇒ 印证 `AGENTS.md` §4 与 `ACTIVE_ISSUES.md` 维护规则第 3 条：**行号一律视为核实时刻的快照**。

### 2.1 编号清单的再核定（AC① 的完整性前提）

> **为什么必须单独写一节**：`ISSUE-P3-141` 的编号清单是**正则对拍的产物**，而该手法对**斜杠压缩写法**
> 不可靠。清单**既不精确也不完备**，若不说明，后来者按字面复核会**继承同样的漏检**。

**(a) 已知偏差（逐条实测）**

| # | 偏差 | 实测证据 | 影响 |
|---|---|---|---|
| D-1 | **`ANDROID-10` 从不以字面出现** | `.audit-recheck/` 全量检索：`ANDROID-10` **零命中**；它只出现在压缩组 `ANDROID-06/10`（`originals/SECURITY_AUDIT_2026-09.md:786,1272,1290`）与 `ANDROID-01/02/06/10`（`:1536,1547`）中 | 任何按字面 `grep` 复核的后来者都会**漏掉它** |
| D-2 | **`ANDROID-02` 同样存在却不在 22 清单内** | `ANDROID-01/02`（`originals/…:783,1536,1371`）、`ANDROID-01/02/06/10`（`:1547`） | 清单**不完备**（本文件已补入，见 3.1 第 2 行） |
| D-3 | **来源错位**：4 个 `ANDROID-*` 不是第四轮分批判定文件的条目 | 它们在 `.audit-recheck/` 中**仅**出现于 `originals/SECURITY_AUDIT_2026-09.md`（第一轮审计原件副本），`batches/B01`~`B09` 全部零命中 | 其语义是原报告 §10「**待复核区**」的**挂账项**（`originals/…:1545-1547` 自述「尚有 4 组未逐条复核…这些未计入 29 项统计」），**不是**本轮新判定 |
| D-4 | **`IPC-*` / `SUPPLY-*` 的同款压缩写法** | `IPC-01/02`（`originals/…:783,1268,1290,1371,1419,1541,1547`）、`IPC-03/05/06/08/11`（`:1547`）、`IPC-01/02/03/05/06/08/11`（`:1541`）、`SUPPLY-02/05/06/07/08`（`:786,1272,1542`）、`SUPPLY-02/04/06/07/08` 与 `IPC-01/02/05/10`（`batches/B09-findings.md:11,1763,1819`、`batches/B04-findings.md:2159`）、`ISSUE-P3-86/87/110`（`batches/B04-findings.md:1366`） | 同一个对拍脚本对**所有**编号族都有系统漏检；故本次对 `IPC-*`/`SUPPLY-*` 做了全表核定（见 2.2） |

**(b) 判读口径（不得用集合差集替代）**

> **字符串出现 ≠ 处置已落档。** 实测反例：若以「编号是否在仓库文档中出现」重算范围，结论随
> **是否把 `docs/ACTIVE_ISSUES.md` 计入仓库侧**而剧烈变化（不排除得 19 项、排除后仅 2 项）——
> 因为 `ACTIVE_ISSUES.md:111` **罗列编号本身**就被算作「仓库侧命中」，条目列出的编号会**自我豁免**。
>
> ⇒ AC① 要求的四选一，只能**逐编号人工判读其处置是否真的被记录在某处**（报告 §6.8 / §6.6 / §8 根因矩阵 /
> `RESOLVED_LOG` 与批次正文 / 代码修复 / 本表），并以「**处置载体 + 载体内结论**」两段式取证。
> 本文件第 3 节的每一行都按此口径给出「载体」与「载体内结论」。

### 2.2 `IPC-*` / `SUPPLY-*` 19 项处置归属全表（核实后引用，不照抄断言）

> `ISSUE-P3-141` 正文有「报告 §6.8 只逐条裁定 `IPC-*`/`SUPPLY-*`」一句。**本次核实结论：该断言
> 作为「全部 `IPC-*`/`SUPPLY-*` 已被逐条裁定」的表述不成立**——§6.8 只覆盖 **9** 项，加 §6.6 的
> `SUPPLY-05` 共 **10** 项；待复核区全域为 `IPC-01…IPC-11` + `SUPPLY-01…SUPPLY-08` = **19 项**
> （全域范围由报告 `:87` 自述引 `RESOLVED_LOG` §43.5 给出）。余 9 项的处置载体**不是** §6.8，而是
> 「复核登记表的**重合声明**」+「对应条目在报告 §3 的逐条裁决」——**重合声明本身从未被第二轮验证**
> （登记表 `00-registry-and-plan.md:341` 明写「须复核其声明是否正确」）。

| 编号 | 处置载体（**核实后**的落点） | 载体内结论 |
|---|---|---|
| `IPC-01` | 报告 §6.8（`:582`） | 成立（部分）→ `DESIGN WEAKNESS` / LOW |
| `IPC-02` | 报告 §6.8（`:583`） | 不成立（作为漏洞）→ `HARDENING` / INFO（附 `PasskeyAssertionActivity` 表述修正建议） |
| `IPC-03` | **非 §6.8**：登记表重合声明 → `ISSUE-P2-70`；条目裁决见报告 §3.2.2（`:229`），闭环 §53 | 归并为「选择器不显示请求方身份」，已闭环 |
| `IPC-04` | **非 §6.8**：→ `ISSUE-P3-88`；条目裁决见报告 §3.3.2（`:261`），闭环 §65 | 归并为「Chrome 指纹 65-hex 笔误」，已闭环 |
| `IPC-05` | 报告 §6.8（`:584`） | `FALSE POSITIVE` |
| `IPC-06` | **非 §6.8**：→ `ISSUE-P2-72`（报告 §3.2.2 `:231`）；闭环 §48 | 归并为「`androidPackageName` 归属错误」，已闭环 |
| `IPC-07` | **非 §6.8**：→ `ISSUE-P2-51`（报告 §3.2.2 `:210`）；闭环 §48 | 归并为「剪贴板生命周期」，已闭环 |
| `IPC-08` | **非 §6.8**：`docs/security/退役审计承接-43-安全整改方案与附录.md:118` | 产品裁决 / 设计边界（与 `IPC-09` 同行） |
| `IPC-09` | **非 §6.8**：同上；另见报告 §4 对 `P2-44` 的评估 | 产品裁决 / 设计边界 |
| `IPC-10` | 报告 §6.8（`:585`） | 成立 → `HARDENING` / LOW |
| `IPC-11` | **非 §6.8**：→ `ISSUE-P2-46`（报告 §3.2.1 `:200`）；闭环 §75 | 归并为「`android://` 缺签名绑定」，已闭环 |
| `SUPPLY-01` | **非 §6.8**：→ `ISSUE-P2-54`（报告 §3.2.2 `:213`）；闭环 §49 | 归并为「依赖 CVSS 闸门触发面」，已闭环 |
| `SUPPLY-02` | 报告 §6.8（`:586`） | 成立 → `CONFIRMED VULNERABILITY` / MEDIUM |
| `SUPPLY-03` | **非 §6.8**：→ `ISSUE-P2-55`（报告 §3.2.2 `:214`）；闭环 §49 | 归并为「发布签名口令=示例值」，已闭环（`CHAIN-E` 已断裂） |
| `SUPPLY-04` | 报告 §6.8（`:587`） | 成立，且补出第二个出口（自动填充通道） |
| `SUPPLY-05` | 报告 **§6.6**（`:569`），**非 §6.8** | 有意设计，有 parity 合同断言 |
| `SUPPLY-06` | 报告 §6.8（`:588`） | 部分成立 → `CONFIRMED VULNERABILITY` / LOW |
| `SUPPLY-07` | 报告 §6.8（`:589`） | 成立，且原记录低估（用例实数 28） |
| `SUPPLY-08` | 报告 §6.8（`:590`） | 成立（部分）→ 实为 JDK / Daemon JVM 未锁 |

**两项由此产生的更正（供主控裁决，见第 4.3 节）**：
1. 报告 §1（`:88`）「本轮已逐条补齐（§6.8）」与 §2（`:167`）「10+ 条线索从未获结论（本轮补上）」
   **对 9 项不成立**（`IPC-03/04/06/07/08/09/11`、`SUPPLY-01/03`）——它们在报告中**无逐条真伪结论**，
   只有「与某既有条目重合」的**声明**，而该声明按登记表原文**尚待复核**。
2. 故 `ISSUE-P3-141` 正文该句应改为：「报告 §6.8 逐条裁定 `IPC-01/02/05/10` 与 `SUPPLY-02/04/06/07/08`
   共 9 项，另 `SUPPLY-05` 由 §6.6 裁定；其余 9 项以**重合声明**落到既有条目（声明未经验证）。」

---

## 3. 25 个编号逐条处置对照表

> **列义**：`工作目录出处` = 原始判定的**文件:行号**（`.audit-recheck/` 相对路径）；
> `结论` = 四选一；`证据` = 可核对的仓库相对定位（`文件:行号` / 批次号 / commit 短哈希）。

### 3.1 原报告占位符组（`ANDROID-*`）

> **来源提示（须先读 2.1 D-3）**：这 4 个编号**不是第四轮分批判定文件（`batches/B01`~`B09`）的条目**——
> 它们是**已退役的第一轮审计原件**（`.audit-recheck/originals/SECURITY_AUDIT_2026-09.md`）§10「待复核区」的
> **挂账项**，语义为「**从未逐条复核**」。其中 `ANDROID-10` 在 `.audit-recheck/` 中**从不以字面出现**
> （仅见于 `ANDROID-06/10` 压缩组），`ANDROID-02` **不在原 22 清单内**（本文件补入）。
> 故下表的结论口径为「**其同组实质项由哪些条目承接**」，而非「该编号自身有判定」。

| 编号 | 原始判定摘要（工作目录出处） | 结论 | 证据 | 后续处置 |
|---|---|---|---|---|
| `ANDROID-01` | **只有编号、无正文**——原报告 §10「待复核区」占位符，与 `IPC-01/02` 同列（`originals/SECURITY_AUDIT_2026-09.md:783`）；T1 子代理交付说明自认「ANDROID-01/02 列 §10」（`:1536`）；风险表把其与 `IPC-01/ANDROID-01+02` 并为组合链 `C-11`（`:1445`） | **已由某条目覆盖** | 报告 §6.8 已对 `IPC-01` / `IPC-02` 逐条裁定（`docs/security/SECURITY_RECHECK_2026-09.md:582-583`）：`IPC-01` 成立（部分）/ `DESIGN WEAKNESS`；`IPC-02` 不成立（作为漏洞）/ `HARDENING` | 无（占位符，无独立正文可复核） |
| `ANDROID-02` | 同上，仅见于 §102 批次枚举（`docs/resolved/batches/102-…md:117-128`）与原报告同组（`originals/…:783,1536,1371`） | **已由某条目覆盖** | 同 `ANDROID-01` | 无 |
| `ANDROID-06` | **只有编号、无正文**——原报告 D 组「低危待复核」与 `IPC-03/06/08/11、SUPPLY-06/07` 同列（`originals/…:786`、`:1272`）；§9G 又把它与 `IPC-01/02` 的 PendingIntent extras 身份链并列（`:1290`） | **已由某条目覆盖**（按原报告自身的同组陈述得出的**最高可核结论**；见第 7 节边界 1） | 同组实质项已裁定或映射：`IPC-01`/`IPC-02` → 报告 §6.8（`docs/security/SECURITY_RECHECK_2026-09.md:582-583`）；`IPC-03→P2-70`、`IPC-04→P3-88`、`IPC-06→P2-72`、`IPC-07→P2-51`、`IPC-11→P2-46`、`SUPPLY-01→P2-54`、`SUPPLY-03→P2-55`（重合声明见 `.audit-recheck/00-registry-and-plan.md:341-345`，闭环批次见本文件 2.2 全表）；`SUPPLY-06→P3-124`（§86 闭环）、`SUPPLY-07→P3-123`（§87 闭环）；`IPC-08/09` 已分流为产品裁决（`docs/security/退役审计承接-43-安全整改方案与附录.md:118`） | 无 |
| `ANDROID-10` | 同上（仅以 `ANDROID-06/10` 连写形态出现：`originals/…:786,1272,1290`） | **已由某条目覆盖**（同 `ANDROID-06`，含同一不确定性） | 同 `ANDROID-06` | 无 |

### 3.2 历史已闭环编号组（被工作目录以源码注释形态引用）

| 编号 | 原始判定摘要（工作目录出处） | 结论 | 证据 | 后续处置 |
|---|---|---|---|---|
| `ISSUE-P1-04` | 非复核判定：`B01` 在复核 `ISSUE-P1-25` 时**引用** `UnlockViewModel` 的既有注释（`batches/B01-findings.md:1279`），并据其评述「未提交态主密码永不清零」未被识别（`:1293`、`:1303`） | **已由某条目覆盖**（该编号自身即已归档条目） | `docs/resolved/batches/02-历史全量代码审计发现项整改归档.md:14`「2.9 P1-04 解锁失败节流与失败态清零」；整改 commit `3c7080b`（`git log --grep=ISSUE-P1-04`） | 无（对拍假阳性，机制 M-2） |
| `ISSUE-P1-05` | 非复核判定：`B09` 引用 `SyncEndpointGuard` 的既有注释（`batches/B09-findings.md:1093`），并指出其整改**未覆盖 DAL 出口**（`:1193`、`:1616`、`:1619`） | **已由某条目覆盖**（该编号自身即 `ZT-05` 已归档条目） | 整改 commit `2cce967`「fix(ISSUE-P1-05): 同步端点 SSRF 与 S3 bucket 名 host 注入双层防线」；归档载体 `docs/resolved/batches/02-…md:11`（2.5 零信任专项 ZT 系列全部闭环）+ `docs/security/退役审计承接-43-…md:150`；**未覆盖面**由 `ISSUE-P3-124` 于 §86 闭环（`docs/RESOLVED_LOG.md:100`） | 无 |
| `ISSUE-P2-08` | 非复核判定：`B04` 引用 `RuntimeIntegrityPolicy` / `KeePasskeyAutofillService` 的既有注释（`batches/B04-findings.md:1759,1770`），并在 `NEW-B04-05` 中把它列为「假加固」家族先例（`:2122`）；`B09` 亦引用（`batches/B09-findings.md:686`） | **已由某条目覆盖**（该编号自身即已归档条目） | `docs/resolved/batches/02-…md:18`「2.18 P2-07/08/09 Autofill 信任边界与运行时完整性」；整改 commit `ff9609e`（P2 九项闭环）+ 残余回登 `43c86d8`；实现仍在（`app/src/main/java/com/keepasskey/app/autofill/KeePasskeyAutofillService.kt:51,125,259`、`app/src/main/java/com/keepasskey/app/security/RuntimeIntegrityGate.kt:4`） | 无 |
| `ISSUE-P3-25` | 非复核判定：`originals/REDTEAM_ATTACK_PATHS.md:346` 引用该编号（巨型类拆分后「验证清除是否真实执行」） | **已由某条目覆盖**（该编号自身即已归档条目） | `docs/resolved/batches/04-残余批次整改归档.md:18`（P3-25 巨型类拆分达成，均 ≤400 行）；commit `8840feb`（`git log --grep=ISSUE-P3-25`） | 无 |
| `ISSUE-P3-40` | 非复核判定：`B09` 引用 `AutofillDatasetBuilders` 的既有注释（`batches/B09-findings.md:123`，`ISSUE-P3-40：手动选择器入口数据集的 requestCode`） | **已由某条目覆盖**（该编号自身即已归档条目） | `docs/resolved/batches/09-自动填充能力对标批次归档.md:9`（P3-40 手动选择器闭环）；commit `d3e6b28`（`git log --grep=P3-40`） | 无 |

### 3.3 `NEW-B01-*` 组

| 编号 | 原始判定摘要（工作目录出处） | 结论 | 证据 | 后续处置 |
|---|---|---|---|---|
| `NEW-B01-1` | `AndroidKeystoreUnlockThrottleIntegrity.mac()` 的 HMAC 返回值与 `verify` 的 `expected` 无清零点（`batches/B01-findings.md:1552-1566`）；B01 自判 `HARDENING`/`INFO`，并自认「泄露价值接近于零」「审计时按『是否存在秘密』筛掉 HMAC 摘要 —— **判断正确**」 | **前提不成立**（非加固缺口） | 该 MAC 是**按设计持久化的校验值**（`UnlockThrottleMacPayload`，供下次校验读回），非秘密材料 ⇒ 内存副本清零无安全收益；当前实现仍未清零（`app/src/main/java/com/keepasskey/app/security/UnlockThrottleIntegrity.kt:110-113`（`mac()` 直接返回 `doFinal`）、`:120-122`（`expected`）），但**无任何安全属性因此受损** | 无 |
| `NEW-B01-5` | `KdbxDatabase.clearSensitiveData()` 不覆盖 `header` ⇒ 与 `P2-60` 的 KDF secret `K` 形成「锁库后仍存活」路径（`batches/B01-findings.md:1628-1633`）；B01 自判「并入 `ISSUE-P2-60`，不新开条目」 | **已由某条目覆盖**（`ISSUE-P2-60`，整改已落地） | `database/src/main/java/com/keepasskey/database/file/KdbxDatabase.kt:71-74` 现含 `header.kdfParameters.clearSensitive()`；commit `5d9d4ba`（`git log -L` 确证该行为该 commit 引入，主题「闭环 ISSUE-P2-60，擦除 KDF secret 的驻留泄漏」） | 无（B01 的并条建议已被采纳并落地） |
| `NEW-B01-6` | `KdfParameters.Argon2` 的 `equals`/`hashCode` 忽略 `secretKey`/`associatedData` ⇒ 两个 **KDF 密钥不同**的 header 被判为相等（`batches/B01-findings.md:1635-1642`，标 `[U]`，因未读完 `KdbxHeader.equals`） | **前提不成立（当前不可达）**，但**存在未登记的结构性缺口** | ① 本次**读到函数结尾**：`KdbxHeader.kt:39` **确实**比较 `kdfParameters` ⇒ B01 担心的链路**客观成立**；② `KdfParameters.kt:60,87-97` 显式忽略 `secretKey`/`associatedData`，且 `:60` 就地注明「刻意忽略本字段（与既有语义一致）」（就地清零后需保持哈希稳定 ⇒ **有意设计**，§102 的方向正确）；③ 不可利用的依据是**全仓相等性零消费点**（`grep 'header ==\|header !=\|== header\|equals'` 在 `database/crypto/app/sync/src/main` 与 `src/test` 均零命中）；`core.database` 虽是 `MutableStateFlow`（按 `equals` 合并，`database/…/session/SessionCore.kt:21`），但 `secretKey` **仅**在反序列化时赋值（`KdbxKdfParameterCodec.kt:194`），且保存会刷新 KDF salt（`KdbxFile.kt:337`）⇒ 不存在「仅 `secretKey` 变化」的赋值路径 | **建议登记**（防未来接入型，同族 `ISSUE-P3-90`/`ISSUE-P3-91`）——见第 4 节**新条目 A**；同时**更正 §102 的解除依据**（不能只写「有意设计」，须写「零消费点 + 有意设计」） |

### 3.4 `NEW-B02-*` 组

| 编号 | 原始判定摘要（工作目录出处） | 结论 | 证据 | 后续处置 |
|---|---|---|---|---|
| `NEW-B02-1` | `KdbxKdfParameterCodec` 的 50% 堆阈值与 `Argon2KdfEngine` 的 60% 不一致，KDoc 口径冲突（`batches/B02-findings.md:1077-1095`）；B02 自判 `DESIGN WEAKNESS`/`INFO`，建议收敛单一常量 | **已由某条目覆盖**（该不一致已被报告公开承载） | 报告 §12 更正 #1（`docs/security/SECURITY_RECHECK_2026-09.md:853`）**明写**「`KdbxKdfParameterCodec` 的 50% 堆门槛**严于** `Argon2KdfEngine` 的 60% ⇒ 原生路径永远拿不到 JVM 会拒的值」，并据此把 `P2-59` 从 `CONFIRMED VULNERABILITY` 降为 `HARDENING`；本次直读两处常量**仍并存**（`database/…/file/KdbxKdfParameterCodec.kt:238` = `maxMemory()/2`；`crypto/…/kdf/Argon2KdfEngine.kt:157-158` = `maxHeap*0.6`） | 无（收敛常量属**可选一致性改进**，非缺陷；若要收敛，唯一安全方向是取**较严**的 0.5） |
| `NEW-B02-5` | `createDefault` 的 `cipherUuid` 默认 `AES_256_CBC` 与向导默认 `ChaCha20 + Argon2id` 不一致；B02 因**未追踪接线**只标 `POTENTIAL`（`batches/B02-findings.md:1160-1186`） | **已在本批修复**（§102 / `ISSUE-P2-85`） | §102.1（`docs/resolved/batches/102-…md:13-51`）；commit `288c8c4`；修复后 `CreateVaultPreset` 枚举为唯一真相源，`SessionOpener.create` 新增 `cipherUuid` 形参 | 无（B02 要求的「读 `DatabasePickerViewModel.createDatabase` 全文」已由 §102 完成，结论为**UI 选择被静默丢弃**，比 B02 的 `POTENTIAL` 更严重） |
| `NEW-B02-3` | `HmacBlockOutputStream.flushBlock` 的末尾残块明文副本 `buffer.copyOf(filled)` 未清零（`batches/B02-findings.md:1122-1134`） | **已在本批修复**（§102 / `ISSUE-P3-140`） | §102.2（`docs/resolved/batches/102-…md:55-107`，含变异验证）；commit `288c8c4`；回归 `HmacBlockOutputStreamTailWipeTest`（2 例） | 无。**并更正一处前提**：`ACTIVE_ISSUES.md:111` 称 `NEW-B02-5` 与 `NEW-B02-3`「均**从未进入报告**或本表」——对 `NEW-B02-3` **不成立**：报告 §11 的 INFO 级清单（`docs/security/SECURITY_RECHECK_2026-09.md:842`）**逐字列出** `NEW-B02-3（flushBlock 的 chunk）`，报告 §8 RC 矩阵（`:646`）亦引用。准确表述应为「**被引用过，但从未成为独立条目、也未被任何批次整改**」（§102.2 原文即此表述，准确）。该偏差**不影响技术结论**，仅影响 `ISSUE-P3-141` 的前提描述 |

### 3.5 `NEW-B04-*` 组

| 编号 | 原始判定摘要（工作目录出处） | 结论 | 证据 | 后续处置 |
|---|---|---|---|---|
| `NEW-B04-01` | `PasskeyAssertionActivity` 注释把「extras 不可伪造」错误归因给 `PendingIntent` 的不可伪造性（`batches/B04-findings.md:2056-2068`）；B04 自判 `HARDENING`（文档准确性） | **已由某条目覆盖** | 报告 §6.8 `IPC-02` 行（`docs/security/SECURITY_RECHECK_2026-09.md:583`）**已给出该整改建议**：「建议修正 `PasskeyAssertionActivity.kt:112` 表述：`PendingIntent` 不可伪造 ≠ **extras** 不可伪造」；行号已漂移（实测 `app/src/main/java/com/keepasskey/app/passkey/PasskeyAssertionActivity.kt:144`，另 `:78` 有同型表述） | **建议动作未执行** ⇒ 见第 5 节（不需新条目） |
| `NEW-B04-03` | `RESOLVED_LOG` §43.7 强项 9「导出组件面仅 `MainActivity` + 两个 `BIND_*` 服务」对**合并清单**不成立（`batches/B04-findings.md:2083-2095`） | **已由某条目覆盖** | 报告 §5.8 逐行更正该表述（`docs/security/SECURITY_RECHECK_2026-09.md:507`）；报告 §8 `RC-08` 根因族亦把「§43.7 强项 9」列为「安全承诺与实现/文档漂移」成员（`:673`）；报告 §13 复述已用更正口径（`:879`） | 可选：在承接文档 `docs/security/退役审计承接-43-…md:153` 加一行指针（退役纪律「归档正文只搬迁、不改写」⇒ 不宜就地改原文） |
| `NEW-B04-04` | `ISSUE-P3-94`（减权限 + 需 `xmlns:tools`）与 `ISSUE-P2-74`（加 `<queries>`）改同一文件同一区域，独立排期会互相覆盖（`batches/B04-findings.md:2097-2103`） | **前提不成立**（排期冲突已消解） | 当前清单**两者已同批落地**：`app/src/main/AndroidManifest.xml:3` 已有 `xmlns:tools`、`:44` 已有 `<queries>`；`ISSUE-P2-74` 于 §54 闭环（`docs/resolved/batches/54-包可见性与序列化缓冲擦除批次.md:12`）、`ISSUE-P3-94` 于 §56 闭环（`docs/RESOLVED_LOG.md:70`） | 无 |
| `NEW-B04-05` | `armScheduledClear` 的**分支顺序**使 `customTimeoutSeconds` 无法绕过用户「关闭自动擦除」设置 ⇒ `ISSUE-P2-43` AC① 备选「强制不可关闭的短擦除」会被静默满足为「仍可关闭」（`batches/B04-findings.md:2105-2123`）；B04 自判 `HARDENING`（Fix 方案缺陷） | **前提不成立（当前无影响）**，但**存在未登记的 Fix 陷阱** | ① 顺序**仍在**（`app/src/main/java/com/keepasskey/app/security/ClipboardSecurityManager.kt:174-188`：`if (!settings.autoClearClipboard) return@launch` 早于 `customTimeoutSeconds` 的消费 `:183`）；② 但**当前无任何调用方传非空 `customTimeoutSeconds`**（全部调用点为二参形态：`EntryDetailViewModel.kt:342,450,478,498`、`AuthenticatorViewModel.kt:136,151`、`GeneratorViewModel.kt:130`、`VaultListActionController.kt:61`、`AutofillConfirmActivity.kt:314`）⇒ 该顺序当前**零行为影响**；③ `ISSUE-P2-43` 已以「三处默认值同步翻转为关闭」在 §51 闭环，AC① 的「强制擦除」备选**未采纳**（`docs/resolved/batches/51-自动填充默认值与内存保护口径批次.md:17`） | **建议登记**（防未来接入型）——见第 4 节**新条目 B** |
| `NEW-B04-06` | `ClipboardSecurityManager.copyPlainText` **无条件** `cancelScheduledClear()`，且 `VaultListActionController.kt:72` 的普通用户名复制独立触发（`batches/B04-findings.md:2125-2139`） | **已由某条目覆盖**（且修复已落地） | 报告 §11 `NEW-B04-x`（`docs/security/SECURITY_RECHECK_2026-09.md:837`，归 `RC-09`）已收录；结构性删除见 `ISSUE-P1-25` AC② / §45（`docs/resolved/batches/45-双项整改批次-确认页调用方归属与首次绑定授权.md:14,25`）；当前实现仅置 `clipboardSuperseded` + 清跨进程标记（`app/src/main/java/com/keepasskey/app/security/ClipboardSecurityManager.kt:201-206`） | 无 |

### 3.6 `NEW-B09-*` 组

| 编号 | 原始判定摘要（工作目录出处） | 结论 | 证据 | 后续处置 |
|---|---|---|---|---|
| `NEW-B09-01` | `DigitalAssetLinksVerifier.endpointOverride` 是 `@Singleton` 上的 `@Volatile internal var` ⇒ 同模块任意生产代码可把 DAL 出口重定向到任意 URL（`batches/B09-findings.md:1569-1591`） | **已由某条目覆盖**（且修复已落地） | 报告 §11 以别名 `NEW-B09-x` 收录（`docs/security/SECURITY_RECHECK_2026-09.md:836`，归 `SUPPLY-06`）；整改见 `ISSUE-P3-125②` / §67（`docs/resolved/batches/67-声明一致性与-不可重定向批次-整体.md:15`）；当前源码已无该字段，注释即整改留痕（`app/src/main/java/com/keepasskey/app/passkey/DigitalAssetLinksVerifier.kt:45-54,254-263`） | 无 |
| `NEW-B09-02` | 两个「受信浏览器」白名单来源不同（`CallingOriginResolver.PRIVILEGED_BROWSER_ALLOWLIST` 与 `AutofillWebDomainPolicy.isTrustedBrowser`）且未经一致性核对；B09 因**未直读 B** 只标 `[I]`/`POTENTIAL`（`batches/B09-findings.md:1593-1601`） | **前提不成立**（本次已直读两侧，无一致性缺陷） | ① `AutofillWebDomainPolicy.isTrustedBrowser`（`app/src/main/java/com/keepasskey/app/autofill/AutofillWebDomainPolicy.kt:55-60`）**委托** `BrowserSigningFingerprints.isTrusted`；② `BrowserSigningFingerprints.TRUSTED` 的 Chrome 两条指纹（`app/src/main/java/com/keepasskey/app/security/BrowserSigningFingerprints.kt:39-42`）与 `CallingOriginResolver` 白名单的规范副本（`app/src/main/java/com/keepasskey/app/passkey/CallingOriginResolver.kt:41-43`）**逐字节一致**（`32A2FC74…849F` / `F0FD6C5B…DB83`）；③ 差异仅在自动填充侧**按已取证来源**加列 `org.mozilla.firefox` / `_beta`（Mozilla 官方 Fenix certificates 文档；来源留痕见 `docs/resolved/batches/22-存量问题由易到难整改闭环批次归档.md:78-79`），CM 侧未同步 ⇒ **较窄的一侧（CM）= fail-closed**，不构成攻击面 | 可选：在 `BrowserSigningFingerprints` KDoc 补一句「CM 侧白名单未含 Firefox，属有意的窄口径」；**Firefox 通行密钥可用性未验证**（见第 7 节） |
| `NEW-B09-03` | `ISSUE-P3-111` 与 `IPC-02` 是同一处整改，且**顺序**受 `ISSUE-P2-73` 制约（先加交叉核对会把「能填充」变成「完全不能填充」）（`batches/B09-findings.md:1603-1612`） | **已由某条目覆盖**（顺序约束已被遵守） | `ISSUE-P3-111` 于 §81 闭环，批次正文**逐字引用**该顺序约束（`docs/resolved/batches/81-CM通道调用方绑定与填充归属交叉核对批次.md:5-7`）；当前实现含 `retrieve*` + `expectedPackage` 交叉核对并对取空 fail-open（`app/src/main/java/com/keepasskey/app/passkey/PasswordFillActivity.kt:70-81`） | 无 |
| `NEW-B09-04` | `RESOLVED_LOG` 的 `P3-69` / `P1-05` 归档记录未声明「未覆盖的同类出口」（Daemon JVM 未锁 / DAL 未纳入 SSRF 整改）（`batches/B09-findings.md:1614-1623`） | **已由某条目覆盖**（两个未覆盖面均已有落点） | ① Daemon JVM：`SUPPLY-08` → §87.3②「登记为已知限界（无落点）」并说明 wrapper `distributionSha256Sum` 已锁（`docs/resolved/batches/87-CI产物可见性与供应链硬化批次.md:43-46`）＋报告 §6.8 `SUPPLY-08` 行（`docs/security/SECURITY_RECHECK_2026-09.md:590`）；② DAL 出口：`SUPPLY-06` → `ISSUE-P3-124` 于 §86 闭环（`docs/RESOLVED_LOG.md:100`） | 无需补指针：`RESOLVED_LOG.md` 已按维护规则精简为**纯索引**，原建议的「在归档记录中追加指针」落点已不存在；承接面已在 §87.3 与 §86 正文 |
| `NEW-B09-05` | 第二轮报告在复核开始时**未被 git 跟踪**，退役后无法经 `git show` 取回（`batches/B09-findings.md:1625-1643`） | **前提不成立**（风险已消解） | `git ls-files docs/security/` 命中 `docs/security/SECURITY_RECHECK_2026-09.md`；报告 §15.1 自载「本报告已纳入 git 跟踪（2026-09-13）」（`docs/security/SECURITY_RECHECK_2026-09.md:952-953`）；`.audit-recheck/` 由 `.gitignore:123` 排除（`git check-ignore -v .audit-recheck/00-registry-and-plan.md` 命中） | 无；但该编号的**教训**适用于 `.audit-recheck/` 本批退役 ⇒ 见第 6 节 |

---

## 4. 确属新缺陷（建议登记到 `ACTIVE_ISSUES.md`）

> 本节只列**经取证、当前仍存在、且未被任何条目/报告覆盖**的项。共 **3 条**：A、B 为 `INFO` 级
> **「防未来接入」**型（与 `ISSUE-P3-90`「公开死函数无参数钳制，零调用点」、`ISSUE-P3-91` 同族）；
> C 为 `P3` 级**可核对性缺口**（工作目录/报告侧的「已逐条补齐」断言与实况不符，与 `ISSUE-P3-141` 同族）。
> **本文件不直接修改 `ACTIVE_ISSUES.md`**——以下为可直接粘贴的条目正文，由主控代理登记。
> 建议编号 `ISSUE-P3-143` / `ISSUE-P3-144` / `ISSUE-P3-145`（已核实 `P3-143/144/145` 在 `docs/` 全量零命中）。

### 4.1 新条目 A（建议编号 `ISSUE-P3-143`）

```markdown
### ISSUE-P3-143：`KdbxHeader` 相等性对 KDF secret 不敏感（零消费点的防未来接入缺口）

- **优先级**：P3（INFO——**当前不可利用**；防未来接入）
- **当前状态**：**开放项**——结构性缺口客观存在（`KdbxHeader.equals` 比较 `kdfParameters`，而 `KdfParameters.Argon2.equals` 忽略 `secretKey`/`associatedData`），但全仓零相等性消费点，故当前无攻击路径。
- **来源**：第四轮复核工作目录 `NEW-B01-6`（原标 `[U]`：复核者未读到 `KdbxHeader.equals` 结尾）；`ISSUE-P3-141` 收口时**读到函数结尾**，前提被证实而非推翻。逐条取证与该编号的四选一结论见 [`docs/security/第四轮复核遗留编号处置.md`](第四轮复核遗留编号处置.md) 第 3.3 节（工作目录原件路径以该文件记载为准，该目录不随仓库分发）。
- **核实时间点与核实方式（2026-09-16，对 HEAD `2e5ce38`）**：直读 `database/src/main/java/com/keepasskey/database/file/KdbxHeader.kt:29-53`（`equals` 第 39 行比较 `kdfParameters`，`hashCode` 第 51 行取 `kdfParameters.hashCode()`）与 `crypto/src/main/java/com/keepasskey/crypto/kdf/KdfParameters.kt:60,87-105`；全仓检索相等性消费点（`header ==` / `header !=` / `== header` / `equals`）在 `database|crypto|app|sync` 的 `src/main` 与 `src/test` **全部零命中**。
- **问题描述**：`KdbxDatabase` 是 `data class` 且持有 `KdbxHeader`，`KdbxHeader.equals` 传递比较 `kdfParameters`，而 `KdfParameters.Argon2.equals`/`hashCode` **刻意忽略** `secretKey`（KDBX4 VariantDictionary `K`，秘密材料）与 `associatedData` ⇒ **两个 KDF secret 不同、其余字段相同的库会被判为相等**。忽略本身是**有意设计**（`secretKey` 是 `var`，`clearSensitive()` 就地置 null；纳入哈希会破坏清零后的哈希稳定性，`KdfParameters.kt:60` 已就地注明）。缺口在于：该「秘密不敏感相等性」**经 `KdbxHeader` 对外暴露**，而 `core.database` 恰是 `MutableStateFlow`（`SessionCore.kt:21`；StateFlow 按 `equals` 合并）——一旦未来出现「仅 `secretKey`/`associatedData` 变化」的赋值或集合去重路径，相应赋值/去重会被**静默吞掉**。
- **当前不可达的依据（不得略去）**：`secretKey` 仅由反序列化赋值（`database/…/file/KdbxKdfParameterCodec.kt:194`），且保存会刷新 KDF salt 与 masterSeed（`database/…/file/KdbxFile.kt:337,342`）⇒ 不存在「其余字段全等、仅 secret 不同」的生产赋值路径。
- **验收标准**：① 二选一并留痕——(a) 在 `KdbxHeader`/`KdfParameters.Argon2` 的 KDoc 显式声明「相等性是**秘密不敏感**语义，禁止用于『凭据是否变化』一类裁决」；或 (b) 新增回归用例断言该语义（含 `secretKey` 清零前后 `hashCode` 稳定），使未来若被消费时不至静默。② 若未来确有「仅 secret 变化」的赋值路径，须改为显式变更标记（如会话级 revision）而非依赖结构相等。③ 回归用例不得改变既有 `equals` 语义（避免引入「清零后哈希变化」的新缺陷）。
```

> **更正提示（供 §104 批次正文引用）**：§102.3 对 `NEW-B01-6` 的解除依据写作「属**有意设计**而非缺漏」——
> 方向正确但**不完整**：真正的解除依据是**「零消费点」+「有意设计」**两个条件的合取。因为
> `KdbxHeader.kt:39` 确实比较 `kdfParameters`，B01 担心的链路**客观存在**，仅因无消费点而不可利用。

### 4.2 新条目 B（建议编号 `ISSUE-P3-144`）

```markdown
### ISSUE-P3-144：`armScheduledClear` 的分支顺序使 `customTimeoutSeconds` 无法强制擦除（Fix 陷阱）

- **优先级**：P3（INFO——**当前零行为影响**；防未来接入）
- **当前状态**：**开放项**——`customTimeoutSeconds` 形参在用户关闭「自动擦除剪贴板」时被静默忽略，且该事实未在 API 契约中声明。
- **来源**：第四轮复核工作目录 `NEW-B04-05`（原判 `HARDENING`／Fix 方案缺陷）。逐条取证与该编号的四选一结论见 [`docs/security/第四轮复核遗留编号处置.md`](第四轮复核遗留编号处置.md) 第 3.5 节（工作目录原件路径以该文件记载为准，该目录不随仓库分发）。
- **核实时间点与核实方式（2026-09-16，对 HEAD `2e5ce38`）**：直读 `app/src/main/java/com/keepasskey/app/security/ClipboardSecurityManager.kt:174-188`（`:181` 的 `if (!settings.autoClearClipboard) return@launch` 早于 `:183` 的 `customTimeoutSeconds` 消费）；全仓检索调用方，确认**无任何调用点传入非空 `customTimeoutSeconds`**（`EntryDetailViewModel.kt:342,450,478,498`、`AuthenticatorViewModel.kt:136,151`、`GeneratorViewModel.kt:130`、`VaultListActionController.kt:61`、`AutofillConfirmActivity.kt:314` 全为二参形态/默认 null）。
- **问题描述**：`ClipboardSecurityManager.copySensitiveText`/`copySensitiveChars` 暴露 `customTimeoutSeconds: Int?`，其 KDoc（`:135`）只写「若为 null 则遵从 UserSettings」，**未声明**「非 null 也仍被用户的『关闭自动擦除』设置否决」。`ISSUE-P2-43` AC① 的备选方案「在敏感复制路径强制**不可关闭**的短擦除」若照此实现，会被静默满足为「仍然可关闭」——即本仓反复出现的**假加固**失效模式。
- **为何当前不是漏洞（不得略去）**：① 无调用方传非空值 ⇒ 该分支当前不可达；② `ISSUE-P2-43` 已以「三处默认值同步翻转为关闭」在 §51 闭环，AC① 的「强制擦除」备选**未采纳**（见 `docs/resolved/batches/51-自动填充默认值与内存保护口径批次.md:17`）。
- **验收标准**：① 在 `armScheduledClear` / 两个公开复制入口的 KDoc 显式声明该**否决语义**（「用户关闭自动擦除时，显式 `customTimeoutSeconds` 同样不生效；任何『强制短擦除』需求须先调整 `:181`/`:183` 的判断顺序」）。② 新增 JVM 回归用例锁定现状与声明一致：`autoClearClipboard=false` + 非空 `customTimeoutSeconds` ⇒ 不调度清除；反向用例 `autoClearClipboard=true` + 非空值 ⇒ 按自定义秒数调度。③ 若未来采纳「强制擦除」，须改为「敏感路径无条件调度，仅把用户设置作为**上限**而非**开关**」并同步改本 KDoc。
```

> **可选替代**：若主控认为 `INFO` 级不宜单开条目，A、B 两条亦可分别作为 KDoc 补注并入
> `ISSUE-P3-129`（文档一致性）一类的既有文档卫生批次——但**必须落在仓库内可检索处**，
> 不得只留工作目录（AC① 的硬要求）。

### 4.3 新条目 C（建议编号 `ISSUE-P3-145`）

```markdown
### ISSUE-P3-145：报告与待办对「待复核区已逐条补齐」的断言与实况不符（`IPC-*`/`SUPPLY-*` 19 项仅 10 项有逐条结论）

- **优先级**：P3（可核对性 / 审计基线准确性——**不是技术漏洞**）
- **当前状态**：**开放项**——`SECURITY_RECHECK_2026-09.md` §1 与 §2 声称待复核区已「逐条补齐」，但 §6.8 只有 9 行结论 + §6.6 的 `SUPPLY-05`，共覆盖 **10/19**。
- **来源**：`ISSUE-P3-141` 收口时对 `IPC-*`/`SUPPLY-*` 处置归属做全表核定时发现（见 `docs/security/第四轮复核遗留编号处置.md` 第 2.2 节）。核定时同时发现该编号族在 `.audit-recheck/` 内**以斜杠压缩组形式出现**（`IPC-01/02/03/05/06/08/11`、`SUPPLY-02/05/06/07/08` 等），字面对拍不可靠。
- **核实时间点与核实方式（2026-09-16，对 HEAD `2e5ce38`）**：① 机器抽取报告 §6.8 表内编号 → 实测 9 个（`IPC-01/02/05/10`、`SUPPLY-02/04/06/07/08`）；② 全域范围取自报告 `:87` 自述引 `RESOLVED_LOG` §43.5（`IPC-01…IPC-11`、`SUPPLY-01…SUPPLY-08`，共 19）；③ 全文检索报告，确认 `IPC-03/04/06/07/08/09/11`、`SUPPLY-01/03` **在报告中无逐条真伪结论**（仅 `:87` 的范围记法命中）；④ 逐项确认其处置载体为「登记表重合声明 → 既有条目」，且声明本身按 `00-registry-and-plan.md:341` 原文**尚待复核**。
- **问题描述**：
  - 报告 §1（`:88`）写「本轮已逐条补齐（§6.8）」、§2（`:167`）写「10+ 条线索从未获结论（**本轮补上**）」，两处均为**口径过宽的断言**：`SUPPLY-05` 实际在 §6.6 裁定，另有 9 项（`IPC-03/04/06/07/08/09/11`、`SUPPLY-01/03`）**从未获得「成立 / 不成立」结论**；
  - 这 9 项的处置载体是**第一轮自述的「重合声明」**（`IPC-03→P2-70`、`IPC-04→P3-88`、`IPC-06→P2-72`、`IPC-07→P2-51`、`IPC-11→P2-46`、`SUPPLY-01→P2-54`、`SUPPLY-03→P2-55`）与退役承接文档（`IPC-08/09` 于 `docs/security/退役审计承接-43-安全整改方案与附录.md:118`）。**对应条目确实有逐条裁决**（报告 §3 Finding Matrix），故**实质面不算漏报**；漏的是「**声明被验证**」与「**该编号自身有结论**」两层，与 `SYS-2`（「待复核区被归并替代复核」）**同型**——报告自己把 `SYS-2` 列为已修，而实际只修了 10/19。
  - `ACTIVE_ISSUES.md:111` 的「报告 §6.8 只逐条裁定 `IPC-*`/`SUPPLY-*`」一句同样过宽，应改为「§6.8 裁定 9 项 + §6.6 裁定 `SUPPLY-05`；其余 9 项以**未经验证的重合声明**落到既有条目」。
- **验收标准**：① 更正报告 §1（`:88`）与 §2（`:167`）的口径，明确「`IPC-*`/`SUPPLY-*` 共 19 项，其中 10 项已逐条裁定、9 项为重合归并（附归并去向）」——**不得**保留「已逐条补齐」的无条件表述（`tools/audit/check_recheck_consistency.sh` 须保持 PASS）。② 对 9 项**逐条**给出「重合声明成立 / 不成立」结论：其中 7 项指向的既有条目均已闭环（`P2-70` §53、`P3-88` §65、`P2-72` §48、`P2-51` §48、`P2-46` §75、`P2-54` §49、`P2-55` §49），复核重点是「该编号所述机制是否**确实**已被对应条目覆盖、有无独立残余」；`IPC-08/09` 复核其「产品裁决 / 设计边界」定性是否仍成立。③ 结论落档于本仓受跟踪文档（不得只留 `.audit-recheck/`）；④ 更新 `ACTIVE_ISSUES.md:111` 的引用口径。
```

---

## 5. 已覆盖但动作未执行（不新开条目）

| 项 | 覆盖它的条目 | 未执行的动作 | 建议处置 |
|---|---|---|---|
| `NEW-B04-01` | 报告 §6.8 `IPC-02` 行（`docs/security/SECURITY_RECHECK_2026-09.md:583`） | 修正 `PasskeyAssertionActivity.kt` 的注释表述（实测在 `:144`，另 `:78` 同型）。报告写的 `:112` 已漂移 | 作为**文档卫生**项随手修：改为「预期包名由本应用在候选组装阶段写入 base `Intent`；其完整性由『落地 Activity `exported=false`』+『`PendingIntent` 仅由系统发送』共同保证」。**无需新条目**（`IPC-02` 结论本就是 `HARDENING`/`INFO`） |
| `NEW-B04-03` | 报告 §5.8（`:507`）+ §8 `RC-08`（`:673`） | 承接文档 `docs/security/退役审计承接-43-…md:153` 仍是原文 | **可选**：在该行加一行指针「（表述更正见 `SECURITY_RECHECK_2026-09.md` §5.8）」；依退役纪律「归档正文只搬迁、不改写」，**不建议**就地改写原句 |
| `NEW-B02-1` | 报告 §12 更正 #1（`:853`） | 收敛 50%/60% 为单一常量 | **可选**且**方向唯一**：收敛时取**较严**的 `0.5`（取 0.6 等于放宽建库侧上限，属削弱）。**不单开条目** |
| `NEW-B09-02` | 本文件 3.6（前提不成立） | 在 `BrowserSigningFingerprints` KDoc 注明「CM 侧白名单未含 Firefox」 | **可选**：一行注释即可，**不单开条目** |

---

## 6. AC④：工作目录关键分批判定是否分流入库？

### 6.1 结论

**应「分流结论」，不应「整份入库」。** 具体为：

1. **必做（本次已完成）**：把工作目录**独有的编号**及其**处置结论**落为**受跟踪文档**（本文件），
   并在 `SECURITY_RECHECK_2026-09.md` 加交叉引用（AC③）——这即 `AGENTS.md` §4 退役纪律要求的
   「文档退役前其结论必须完成分流并同步文档地图」。
2. **不做**：不把 `.audit-recheck/` 的 18 份文件、约 1.4 MB / 约 1.1 万行**纳入 git 跟踪**。
3. **退役时机**：本文件与报告交叉引用**同批入库后**，`.audit-recheck/` 即可判定为「结论已分流、可退役」；
   `.gitignore:123` 的排除**维持不变**。

### 6.2 理由

| # | 理由 | 依据 |
|---|---|---|
| R-1 | 工作目录是**过程证据**，其结论已被报告吸收；整份入库等于「结论双写」，与 `docs/README.md` 的「唯一登记表 = 文档地图」「不得平铺」与 `RESOLVED_LOG.md` 的「纯索引」精简纪律相悖 | `docs/README.md:4`、`AGENTS.md` §4 |
| R-2 | 工作目录含**大量已漂移的 `文件:行号` 快照**，整份入库会持续误导后来者 | 本轮实测：报告 §6.8 的 `PasskeyAssertionActivity.kt:112` → 实际 `:144`；`B09-findings.md:1571` 的 `endpointOverride` → 已不存在；`00-registry-and-plan.md:505-506` 的 `REG-1`/`REG-2` 同类实证（`SYS-5`） |
| R-3 | 体量与索引噪音：18 份 / 1.4 MB，其中 9 份是逐条 13 字段的中间产物，与报告 §3–§11 高度重叠 | `du -sh .audit-recheck/` = 1.4M；`find .audit-recheck -name '*.md' \| wc -l` = 18 |
| R-4 | **`.gitignore` 排除 ⇒ 退役即不可经 `git show` 取回**——这正是 `SYS-1`（一份未跟踪报告永久丢失）的教训；故**退役前必须完成分流**，本次两个动作即该分流的收口 | `AGENTS.md` §4 退役纪律；`git check-ignore -v .audit-recheck/…` 命中 `.gitignore:123` |
| R-5 | 报告 §15.1 **已经**把 `.audit-recheck/` 定位为「证据暂存，不随仓库分发」，并对每类产物给出了「可经 `git show 9b64415:` 取回」的替代路径 | `docs/security/SECURITY_RECHECK_2026-09.md:944-953` |

### 6.3 逐份处置表（退役纪律要求「同批退役的多份文档须逐份处置」）

| 工作目录文件 | 性质 | 结论分流落点 | 处置判定 |
|---|---|---|---|
| `00-registry-and-plan.md`（520 行） | Phase 1/2 台账：89 项登记 + Review Plan | 报告 §3 Finding Matrix（89 项）/ §2 方法与团队 / §15.1 索引 | **可退役** |
| `batches/B01-findings.md` | 秘密生命周期批次逐条判定 | 报告 §4/§5/§6/§11；**未进报告的编号 → 本文件第 3 节** | **可退役** |
| `batches/B02-findings.md` | 密码学与 KDF 批次 | 同上（`NEW-B02-5`/`NEW-B02-3` 另有 §102 批次正文） | **可退役** |
| `batches/B03-findings.md` | Rust/FFI/JNI 批次 | 报告 §5/§6/§11（`B03-N1`~`N4`） | **可退役** |
| `batches/B04-findings.md` | Android 组件/平台语义批次 | 报告 §4/§5/§6/§11；未覆盖项 → 本文件第 3.5 节 | **可退役** |
| `batches/B05-findings.md` | 自动填充与 CM 归属批次 | 报告 §4/§6；`IPC-*` 结论 → 报告 §6.8 | **可退役** |
| `batches/B06-findings.md` | 同步/网络/远端解析批次 | 报告 §4/§7（攻击链） | **可退役** |
| `batches/B07-findings.md` | 供应链/构建/CI 批次 | 报告 §11（`B07-N1`/`N2`）+ §9 整改优先级 | **可退役** |
| `batches/B08-findings.md` | 剪贴板/导出/UI 卫生批次 | 报告 §6/§11 | **可退役** |
| `batches/B09-findings.md` | 验证缺口与待复核区（含 `NEW-B09-*`） | 报告 §6.8 + §11；`NEW-B09-*` → 本文件第 3.6 节 | **可退役** |
| `batches/NEW-FINDINGS.md` | 2 条新发现单列 | 报告 §11 `NEW-FINDING-01`（`:826`）/ `NEW-FINDING-02` + §4 | **可退役** |
| `batches/P1-findings.md` | P1 四项亲审 | 报告 §4/§5 + §3 Finding Matrix | **可退役** |
| `batches/00-cross-analysis.md` | RC 定版 + 攻击链矩阵 + 已裁决 20 项矩阵 | 报告 §7 攻击链 / §8 根因 / §12 更正留痕 | **可退役** |
| `batches/00-cross-verification.md` | 独立确证 / 反证 / 编号冲突 / `UNVERIFIED` / 路径准确性 | 报告 §5/§6/§15.1；`UNVERIFIED` 项见报告 §10 Verification Plan | **可退役** |
| `originals/SECURITY_AUDIT_2026-09.md` | 第一轮报告副本 | 可经 `git show 9b64415:` 取回（报告 §15.1 已载） | **可退役**（非唯一取证来源） |
| `originals/THREAT-MODEL-AUDIT-d32f3e7.md` | 同上 | 同上；结论承接于 `docs/security/退役审计承接-42-…md` | **可退役** |
| `originals/SECURITY_AUDIT_REMEDIATION.md` | 同上 | 同上；附录承接于 `docs/security/退役审计承接-43-…md` | **可退役** |
| `originals/REDTEAM_ATTACK_PATHS.md` | 同上 | 同上；`AP-*` 处置见报告 §5/§6 与 `ISSUE-P1-22~25`/`P2-43~47`/`P3-82~85` | **可退役** |

> **退役动作建议**：在本批（建议 §104）批次正文中引用本表；`.gitignore` 的 `.audit-recheck/`
> 行**保留**（工作目录若再次产生，仍走同一纪律）。**不要**把工作目录移入 `docs/`。

---

## 7. 剩余边界与不确定性（如实声明，不得读作已核实）

1. **`ANDROID-*` 四项的判定强度低于本表其他行**：这四个编号在原报告中**只有编号、无正文**，且**不是**
   第四轮分批判定文件的条目——它们是**已退役的第一轮审计原件**（`.audit-recheck/originals/SECURITY_AUDIT_2026-09.md`
   §10「待复核区」）的**挂账项**，语义为「**从未逐条复核**」（原件自述见 `:1545-1547`）。本文件据原报告
   **自身的同组陈述**（`:783`/`:1290`/`:1536`/`:1547`）判定其实质由 `IPC-01/02` 与同组 `IPC-*`/`SUPPLY-*`
   承接，属**最高可核结论**，但**无法排除**「该编号另有所指」。另：`ANDROID-10` 在 `.audit-recheck/` 中
   **从不以字面出现**（仅见于 `ANDROID-06/10` 压缩组），`ANDROID-02` 则**不在原 22 清单内**（本文件已补入）。
2. **本次直读覆盖范围**：第 3 节 25 行的代码取证为**逐条直读**；但工作目录全文（约 1.1 万行）**未整份通读**——
   第 6.3 节「结论分流落点」是**按编号/主题抽样核对**（B01/B02/B04/B09 逐条，B03/B05/B06/B07/B08 按标题与报告对应节核对），
   **不构成**「工作目录内不存在其它未登记缺陷」的证明。若需该强度的保证，须另立批次做全文逐条对拍。
3. **编号清单本身既不精确也不完备**（见 2.1）：正则对拍对**斜杠压缩写法**（`ANDROID-06/10`、`IPC-03/05/06/08/11`、
   `SUPPLY-02/05/06/07/08`、`ISSUE-P3-86/87/110` 等）系统性漏检。任何后续对拍脚本都应改为
   「**编号 + 主题**双通道 + 压缩写法展开」，否则会重复产生本 ISSUE 的假阳性与漏检。
4. **字符串出现 ≠ 处置已落档**（见 2.1(b)）：AC① 的四选一只能**逐编号判读其处置载体与载体内结论**；
   用「编号是否出现在仓库文档中」做集合差集**不可用**，且结论会随「是否把 `ACTIVE_ISSUES.md` 计入仓库侧」
   而剧烈变化（实测：不排除得 19 项、排除后仅 2 项）。
5. **`IPC-*`/`SUPPLY-*` 中 9 项的「重合声明」本文件未做逐条验证**：本文件只核实了**处置载体**
   （登记表重合声明 → 既有条目 → 该条目已在报告 §3 有裁决、并在 `RESOLVED_LOG` 有闭环批次），
   **未**逐条验证「该编号所述机制确实已被对应条目完全覆盖、无独立残余」——这正是新条目 C
   （`ISSUE-P3-145`）AC② 的内容，须另批完成。
6. **未验证事项**：`NEW-B09-02` 涉及的「CM（通行密钥）通道是否支持 Firefox」**未做设备侧验证**——
   本文件只证实「两侧指纹数据对 Chrome 一致、Firefox 为自动填充侧有据扩列」，**未**断言 Firefox
   通行密钥的可用性或不可用性（需真机 + 真实 Firefox + RP 站点，超出本任务范围）。
7. **`NEW-B01-6` / `NEW-B04-05` 的「当前不可达」结论有时效性**：两者均以「零消费点 / 零非空调用方」
   为依据；一旦出现相应消费点或调用方，须重新评级。**这正是建议登记而非直接关闭的理由。**
8. **工作目录与本次复核的轮次命名不一致**：`.audit-recheck/00-registry-and-plan.md:1` 自称
   「**第二轮**独立安全复核」，而 `ACTIVE_ISSUES.md` / §102 称同一工作目录为「**第四轮**复核」。
   本文件沿用 `ACTIVE_ISSUES.md` 的「第四轮」口径以对齐待办表，但**两者指同一批产物**。
9. **行号一律为 2026-09-16 / HEAD `2e5ce38` 的快照**（`ACTIVE_ISSUES.md` 维护规则第 3 条）。
10. **本文件未修改** `docs/ACTIVE_ISSUES.md` / `docs/RESOLVED_LOG.md` / `docs/README.md` /
    `docs/resolved/batches/**`（硬性文件归属约束）；第 4 节的新条目正文与第 8 节的建议索引行
    均以**可粘贴文本**形式给出，由主控代理统一登记。

---

## 8. 建议索引行（供主控代理写入）

> **批次号占位说明**：以下按 **§104** 给出——核实时（2026-09-16）**§103 已被并行批次占用**
> （`docs/resolved/batches/103-存量条目前提复核与失真裁决理由更正批次.md`）。若该并行批次被重编号，
> 本批仍应**顺延**而不复用 §103（编号续用不复用，`AGENTS.md` §3.6）。

### 8.1 `docs/README.md` → `security/` 分区（追加一行）

```markdown
| [`security/第四轮复核遗留编号处置.md`](security/第四轮复核遗留编号处置.md) | `ISSUE-P3-141` 收口：第四轮复核工作目录「遗留编号」（自称 **22** / 实际枚举 **23** / 两处枚举并集 **25**）**逐条四选一**处置（已由某条目覆盖 / 已在本批修复 / 确属新缺陷 / 前提不成立）+ 可核对证据；两处前提更正；**工作目录（`.audit-recheck/`）结论分流与逐份退役判定** |
```

### 8.2 `docs/RESOLVED_LOG.md` → 全量批次索引表（追加一行，§104）

```markdown
| §104 | 第四轮复核遗留编号处置收口批次：对「仅存在于复核工作目录」的编号做**编号+主题双通道**逐条取证（含**斜杠压缩写法展开**与清单再核定），得 25 条四选一结论（**未发现可利用真缺陷**；新增 3 条 `P3`/`INFO` 条目 `ISSUE-P3-143` / `ISSUE-P3-144` / `ISSUE-P3-145`，均**当前不可达**或**可核对性缺口**）；更正三处前提（`NEW-B02-3` **已在报告 §11 的 INFO 清单中**、`NEW-B01-6` 的解除依据须补「零消费点」、**`IPC-*`/`SUPPLY-*` 报告仅逐条裁定 10/19 项**）；判定 `.audit-recheck/` **结论已分流、可退役**（18 份逐份处置表） | ISSUE-P3-141（闭环）+ 新登记 ISSUE-P3-143 / ISSUE-P3-144 / ISSUE-P3-145 | [`104-第四轮复核遗留编号处置收口批次.md`](resolved/batches/104-第四轮复核遗留编号处置收口批次.md) |
```

### 8.3 `docs/resolved/BATCH_58_PLUS.md`（同步追加同一行，路径按该册体例用 `batches/` 前缀）

```markdown
| §104 | 第四轮复核遗留编号处置收口批次（编号+主题双通道逐条取证 + 斜杠压缩写法展开 + 清单再核定；工作目录结论分流与退役判定） | ISSUE-P3-141（闭环）+ 新登记 ISSUE-P3-143 / ISSUE-P3-144 / ISSUE-P3-145 | [`104-第四轮复核遗留编号处置收口批次.md`](batches/104-第四轮复核遗留编号处置收口批次.md) |
```

### 8.4 `docs/security/SECURITY_RECHECK_2026-09.md`（本次已就地加入，见该文件 §6.8.1）

报告 §6.8 之后已新增小节 **`### 6.8.1 复核工作目录独有编号的逐条处置（ISSUE-P3-141 收口）`**，
内容四点：① 明确 §6.8 逐条裁定的是 `IPC-*`/`SUPPLY-*` 中的 **9 项**（另 `SUPPLY-05` 由 §6.6 裁定）、
待复核区全域 **19 项**，其余 9 项的处置归属与「**未逐条验证**」这一事实指向本文件第 2.2 节；
② 指向本文件第 3、6 节取工作目录独有编号的四选一结论与退役判定；③ 说明报告 §11 对两条编号使用了
**别名**，故结论须走主题复核；④ 记录两处前提更正（`NEW-B02-3`、`NEW-B01-6`）。

> **强制复核**：改动报告后必须重跑 `bash tools/audit/check_recheck_consistency.sh`（`AGENTS.md` §5）；
> 本次改动后实测 **PASS（扫描 1289 行 / 11 条禁用短语）**。若主控另行更正报告 §1（`:88`）与
> §2（`:167`）的「已逐条补齐」口径（新条目 C 的 AC①），须**再次重跑**该脚本。

> **批次正文**建议以本文件为**唯一真相源**（避免复制粘贴造成第二份会漂移的快照），
> §104 正文只保留：条目范围、`ISSUE-P3-141` 四选一验收标准 ①~④ 的逐项对照、
> 两处前提更正、以及指向本文件的链接。
